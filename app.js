/* Cockpit — lightweight project tracker
   Data model is held in `state` and persisted to localStorage as JSON.
   Import/Export gives the same JSON as a file.
   Build single-file bundle: node build.js → dist/cockpit.html */

(function () {
  'use strict';

  /* ----------------------------- helpers ----------------------------- */

  const $ = (sel, root = document) => root.querySelector(sel);
  const $$ = (sel, root = document) => Array.from(root.querySelectorAll(sel));
  const uid = (prefix = 'id') => prefix + '_' + Math.random().toString(36).slice(2, 9);
  const dayMs = 86400000;
  // Local-date YYYY-MM-DD (toISOString uses UTC and can shift the day in non-UTC zones)
  const fmtISO = (d) => {
    const y = d.getFullYear();
    const m = String(d.getMonth() + 1).padStart(2, '0');
    const day = String(d.getDate()).padStart(2, '0');
    return `${y}-${m}-${day}`;
  };
  const todayISO = () => fmtISO(new Date());
  const parseDate = (d) => (d ? new Date(d + 'T00:00:00') : null);
  const fmtDate = (d) => {
    if (!d) return '—';
    const dt = typeof d === 'string' ? parseDate(d) : d;
    return dt.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
  };
  const fmtFull = (d) => {
    if (!d) return '—';
    const dt = typeof d === 'string' ? parseDate(d) : d;
    return dt.toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' });
  };
  const dayDiff = (a, b) => Math.round((parseDate(a) - parseDate(b)) / dayMs);
  const clamp = (n, lo, hi) => Math.max(lo, Math.min(hi, n));
  const escapeHTML = (s) =>
    String(s ?? '')
      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
  const initials = (name) => {
    if (!name) return '?';
    const parts = name.trim().split(/\s+/);
    return ((parts[0]?.[0] || '') + (parts[1]?.[0] || '')).toUpperCase() || '?';
  };
  // Phase A — fuzzy-score (subsequence + bonus for word-start matches).
  // Returns 0 when query doesn't fuzzy-match haystack; higher score = better.
  function fuzzyScore(query, hay) {
    if (!query) return 1;
    if (!hay) return 0;
    const q = query.toLowerCase();
    const h = hay.toLowerCase();
    let qi = 0, score = 0, lastMatch = -1, run = 0;
    for (let i = 0; i < h.length && qi < q.length; i++) {
      if (h[i] === q[qi]) {
        // word-start bonus
        if (i === 0 || /[\s\-_/.]/.test(h[i - 1])) score += 4;
        else score += 1;
        // consecutive-match bonus
        if (lastMatch === i - 1) { run++; score += run; }
        else run = 0;
        lastMatch = i;
        qi++;
      }
    }
    if (qi < q.length) return 0;
    // Shorter haystacks score slightly higher (preserves intent of full match)
    return score + Math.max(0, 30 - h.length) * 0.1;
  }
  // Phase A — Markdown escape for the upcoming status-report export.
  const mdEscape = (s) => String(s ?? '').replace(/([\\`*_{}\[\]()#+\-.!|>])/g, '\\$1');

  // Expand a meeting record into its concrete dates within a window.
  // Handles oneoff and recurring (day / week / month) — single source of
  // truth so the calendar and timeline never disagree about when a meeting
  // recurs. Caps at m.endDate (if set) and the requested grid window.
  function expandMeetingDates(m, gridStartISO, gridEndISO) {
    if (!m) return [];
    if (m.kind === 'oneoff') {
      return (m.date && m.date >= gridStartISO && m.date <= gridEndISO) ? [m.date] : [];
    }
    if (m.kind !== 'recurring') return [];
    const startISO = m.startDate || todayISO();
    const startD   = parseDate(startISO);
    const gridS    = parseDate(gridStartISO);
    const gridE    = parseDate(gridEndISO);
    const cap      = m.endDate ? parseDate(m.endDate) : null;
    const interval = Math.max(1, m.interval || 1);
    const out = [];
    if (m.recurUnit === 'day') {
      let dt = new Date(startD);
      while (dt <= gridE && (!cap || dt <= cap)) {
        if (dt >= gridS) out.push(fmtISO(dt));
        dt = new Date(dt.getTime() + interval * dayMs);
      }
    } else if (m.recurUnit === 'week') {
      const targetDow = (typeof m.dayOfWeek === 'number') ? m.dayOfWeek : startD.getDay();
      // First occurrence ≥ startD landing on targetDow
      let dt = new Date(startD);
      while (dt.getDay() !== targetDow) dt = new Date(dt.getTime() + dayMs);
      while (dt <= gridE && (!cap || dt <= cap)) {
        if (dt >= gridS) out.push(fmtISO(dt));
        dt = new Date(dt.getTime() + interval * 7 * dayMs);
      }
    } else if (m.recurUnit === 'month') {
      // Step `interval` months at a time on the same day-of-month as
      // startD; clamp to last-day if the target month is shorter (Feb 30).
      const dom = startD.getDate();
      let y = startD.getFullYear();
      let mo = startD.getMonth();
      while (true) {
        const lastDay = new Date(y, mo + 1, 0).getDate();
        const dt = new Date(y, mo, Math.min(dom, lastDay));
        if (dt > gridE) break;
        if (cap && dt > cap) break;
        if (dt >= startD && dt >= gridS) out.push(fmtISO(dt));
        mo += interval;
        while (mo >= 12) { mo -= 12; y += 1; }
      }
    }
    return out;
  }
  function meetingRecurrenceLabel(m) {
    if (m.kind === 'oneoff') return 'Meeting';
    if (m.kind !== 'recurring') return 'Meeting';
    const n = Math.max(1, m.interval || 1);
    const unit = m.recurUnit === 'day' ? 'day' : m.recurUnit === 'month' ? 'month' : 'week';
    if (n === 1) return unit === 'day' ? 'Daily' : unit === 'week' ? 'Weekly' : 'Monthly';
    return `Every ${n} ${unit}s`;
  }

  // Phase G — render tag chips for a record. Tags live on the project; a
  // record stores only ids. Unknown ids (e.g. tag was deleted) are silently
  // skipped to avoid empty chips.
  // Minimal dot strip for the Kanban card: one dot per subtask, colored
  // by task status (reuses the Kanban column palette so the same green
  // that means "Done" on a column means "Done" on a dot). Beyond 10
  // tasks we clip and show a "+K" overflow chip; the drawer has the
  // full list. Returns empty string when the action has no tasks so
  // cards without subtasks look exactly like today.
  function renderTaskStripHTML(a) {
    const list = (a?.tasks || []);
    if (!list.length) return '';
    const MAX = 10;
    const shown = list.slice(0, MAX);
    const overflow = list.length - shown.length;
    const doneCount = list.filter((t) => t.status === 'done').length;
    const totalCount = list.filter((t) => t.status !== 'cancelled').length;
    const statusLabel = { todo: 'Not started', doing: 'In progress', blocked: 'Blocked', done: 'Done', cancelled: 'Cancelled' };
    const dots = shown.map((t) =>
      `<span class="task-dot status-${t.status}" title="${escapeHTML(t.title || '(untitled)')} — ${statusLabel[t.status] || t.status}"></span>`
    ).join('');
    const overflowChip = overflow > 0
      ? `<span class="task-dot-more" title="${overflow} more task${overflow === 1 ? '' : 's'}">+${overflow}</span>`
      : '';
    return `
      <div class="task-strip" title="${doneCount}/${totalCount} tasks done">
        ${dots}${overflowChip}
      </div>`;
  }

  function renderTagChipsHTML(tagIds, proj) {
    if (!Array.isArray(tagIds) || !tagIds.length) return '';
    const known = (proj?.tags || []).filter((t) => t && t.id);
    const byId = new Map(known.map((t) => [t.id, t]));
    return tagIds.map((id) => {
      const t = byId.get(id);
      if (!t) return '';
      const rgb = t.rgb || '120, 120, 140';
      return `<span class="tag-chip" style="background:rgba(${rgb},.18);color:rgb(${rgb});border:1px solid rgba(${rgb},.40)" title="${escapeHTML(t.name)}">${escapeHTML(t.name)}</span>`;
    }).join('');
  }
  const TAG_PALETTE = [
    '110, 168, 255',  // blue
    '179, 137, 255',  // purple
    '52,  211, 153',  // green
    '251, 191, 36',   // amber
    '248, 113, 113',  // red
    '129, 140, 248',  // indigo
    '236, 72, 153',   // pink
    '244, 114, 182',  // rose
    '20,  184, 166',  // teal
  ];

  function applyTheme(theme) {
    document.documentElement.dataset.theme = theme;
    const btn = document.getElementById('btnTheme');
    if (btn) btn.textContent = theme === 'light' ? '☀' : '☾';
  }

  function toast(msg, ms = 1800) {
    const el = $('#toast');
    el.textContent = msg;
    el.hidden = false;
    clearTimeout(el._t);
    el._t = setTimeout(() => (el.hidden = true), Math.max(800, ms | 0));
  }

  /* ------------------------ state & persistence ---------------------- */

  const STORAGE_KEY = 'cockpit.v2';
  const HISTORY_LIMIT = 60;

  const STATUSES = [
    { id: 'todo',      name: 'Not started', dot: 'todo' },
    { id: 'doing',     name: 'In progress', dot: 'doing' },
    { id: 'blocked',   name: 'Blocked',     dot: 'blocked' },
    { id: 'done',      name: 'Done',        dot: 'done' },
    { id: 'cancelled', name: 'Cancelled',   dot: 'cancelled' },
  ];
  // A "closed" status — done or cancelled. Used by capacity / utilisation /
  // late-detection / staleness / KPIs so cancelled actions stop counting as open.
  const isClosedStatus = (s) => s === 'done' || s === 'cancelled';

  // Project components (sub-systems / work packages). Each component has
  // an id, a name, and a color from this palette.
  const COMPONENT_COLORS = [
    { id: 'sky',    name: 'Sky',    rgb: '96,165,250' },
    { id: 'cyan',   name: 'Cyan',   rgb: '34,211,238' },
    { id: 'mint',   name: 'Mint',   rgb: '52,211,153' },
    { id: 'lime',   name: 'Lime',   rgb: '163,230,53' },
    { id: 'amber',  name: 'Amber',  rgb: '251,191,36' },
    { id: 'rose',   name: 'Rose',   rgb: '251,113,133' },
    { id: 'violet', name: 'Violet', rgb: '167,139,250' },
    { id: 'indigo', name: 'Indigo', rgb: '129,140,248' },
    { id: 'slate',  name: 'Slate',  rgb: '148,163,184' },
  ];
  const componentColor = (id) => COMPONENT_COLORS.find((c) => c.id === id) || COMPONENT_COLORS[0];
  // Memoized — at scale this lookup runs hundreds of times per render
  // (every action chip, calendar item, Register row). Was O(n·m); now O(1).
  const findComponent = (proj, componentId) => {
    if (!proj || !componentId) return undefined;
    return idMap(proj, 'components').get(componentId);
  };

  // Change-request lifecycle states + their colors
  const CR_STATUSES = [
    { id: 'proposed',     label: 'Proposed',     rgb: '148,163,184' }, // slate
    { id: 'under_review', label: 'Under review', rgb: '96,165,250'  }, // blue
    { id: 'approved',     label: 'Approved',     rgb: '74,222,128'  }, // green
    { id: 'rejected',     label: 'Rejected',     rgb: '248,113,133' }, // rose
    { id: 'implemented',  label: 'Implemented',  rgb: '167,139,250' }, // violet
    { id: 'cancelled',    label: 'Cancelled',    rgb: '107,114,128' }, // gray
  ];
  const crStatus = (id) => CR_STATUSES.find((s) => s.id === id) || CR_STATUSES[0];

  // Open-point criticality palette + labels (4 levels, low → critical)
  const CRITICALITY_RGB = {
    low:      '148,163,184', // slate
    med:      '96,165,250',  // blue
    high:     '251,191,36',  // amber
    critical: '248,113,133', // rose
  };
  const CRITICALITY_LABEL = { low: 'Low', med: 'Medium', high: 'High', critical: 'Critical' };
  const CRITICALITY_TO_PRIORITY = { low: 0, med: 1, high: 2, critical: 3 };
  // Action priority levels — same 4-level scale as criticality. Stored as
  // `a.priorityLevel`; defaults to 'med' for legacy actions in normalizeState.
  // Note: `a.priority` (a numeric integer) is kept untouched as the in-column
  // sort key — both fields coexist and serve different purposes.
  const PRIORITY_LEVELS = [
    { id: 'low',      label: 'Low',      rgb: CRITICALITY_RGB.low },
    { id: 'med',      label: 'Medium',   rgb: CRITICALITY_RGB.med },
    { id: 'high',     label: 'High',     rgb: CRITICALITY_RGB.high },
    { id: 'critical', label: 'Critical', rgb: CRITICALITY_RGB.critical },
  ];
  const priorityLevel = (id) => PRIORITY_LEVELS.find((p) => p.id === id) || PRIORITY_LEVELS[1];

  let state = null;
  let undoStack = [];
  let redoStack = [];

  // Returns one of three shapes so the caller can decide how to recover.
  // We MUST NOT silently fall back to seedState() on parse failure — that
  // would overwrite the user's real data with sample data on the next save.
  function loadState() {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (raw == null) return { kind: 'empty' };
    try {
      return { kind: 'parsed', value: JSON.parse(raw), raw };
    } catch (e) {
      return { kind: 'corrupted', error: String(e?.message || e), raw };
    }
  }
  // Recovery mode flag. While set, saveState() is a no-op so we never
  // overwrite the original (broken-but-recoverable) localStorage with the
  // in-memory placeholder state.
  let _recoveryMode = false;

  // IndexedDB-backed mirror of the state blob. Lifts the 5–10 MB
  // localStorage cap and avoids JSON-string contention on large states.
  // localStorage stays as a fast-read fallback; on every save we write
  // both, but we may stop writing localStorage if it errors (quota).
  // Cached state IDB connection — opened lazily.
  const STATE_IDB_NAME  = 'cockpit-state';
  const STATE_IDB_STORE = 'kv';
  const STATE_IDB_KEY   = 'state';
  let   _stateIdbDb = null;
  let   _localStorageDisabled = false; // set true after a quota error
  function stateIdbOpen() {
    if (_stateIdbDb) return Promise.resolve(_stateIdbDb);
    return new Promise((resolve, reject) => {
      const req = indexedDB.open(STATE_IDB_NAME, 1);
      req.onupgradeneeded = () => req.result.createObjectStore(STATE_IDB_STORE);
      req.onsuccess = () => { _stateIdbDb = req.result; resolve(_stateIdbDb); };
      req.onerror   = () => reject(req.error);
    });
  }
  async function stateIdbWrite(stateObj) {
    if (typeof indexedDB === 'undefined') return false;
    try {
      const db = await stateIdbOpen();
      return await new Promise((resolve, reject) => {
        const tx = db.transaction(STATE_IDB_STORE, 'readwrite');
        tx.objectStore(STATE_IDB_STORE).put(stateObj, STATE_IDB_KEY);
        tx.oncomplete = () => resolve(true);
        tx.onerror    = () => reject(tx.error);
      });
    } catch (e) {
      console.warn('[cockpit] state IDB write failed:', e);
      return false;
    }
  }
  async function stateIdbRead() {
    if (typeof indexedDB === 'undefined') return null;
    try {
      const db = await stateIdbOpen();
      return await new Promise((resolve, reject) => {
        const tx = db.transaction(STATE_IDB_STORE, 'readonly');
        const req = tx.objectStore(STATE_IDB_STORE).get(STATE_IDB_KEY);
        req.onsuccess = () => resolve(req.result || null);
        req.onerror   = () => reject(req.error);
      });
    } catch (e) { return null; }
  }
  // _lastEditAt is bumped whenever the in-memory state has a change
  // that isn't yet reflected in localStorage / OPFS. Compared against
  // _lastSaveAt and _opfsLastSaveAt to drive the status pill so the user
  // can see when something is mid-flight.
  let _lastEditAt = null;
  let _lastSaveAt = null;
  let _lastSaveError = null;

  // Per-render memoization cache. Cleared on commit, undo/redo, render,
  // and any mutation path that bypasses commit(). Lookups for components,
  // people, deliverables, milestones — all O(1) once cached.
  const _renderCache = new Map();
  function invalidateRenderCache() { _renderCache.clear(); }
  function memo(key, fn) {
    if (_renderCache.has(key)) return _renderCache.get(key);
    const v = fn();
    _renderCache.set(key, v);
    return v;
  }
  // ID-map of any per-project array. Built once per render per project.
  function idMap(project, arrKey) {
    const cacheKey = `idmap:${project?.id || '__no'}:${arrKey}`;
    return memo(cacheKey, () => {
      const m = new Map();
      (project?.[arrKey] || []).forEach((x) => { if (x?.id) m.set(x.id, x); });
      return m;
    });
  }
  // Cross-project people are top-level, not per-project. The map is
  // union(state.people, state.stakeholders) — resolves either kind by id.
  // Stakeholders are external contacts (customer POCs, regulators, sister
  // projects). They don't have a team capacity but they CAN own or
  // originate actions, so any owner/originator lookup must find them.
  function peopleMap() {
    return memo('idmap:people', () => {
      const m = new Map();
      (state.people || []).forEach((p) => { if (p?.id) m.set(p.id, p); });
      (state.stakeholders || []).forEach((s) => { if (s?.id) m.set(s.id, s); });
      return m;
    });
  }
  // Union of team members + external stakeholders, for owner / originator
  // pickers. Team members first, stakeholders second (with a divider hint
  // via the `kind` field). Callers can filter by kind when they need to.
  function allActors() {
    const team = (state.people || []).map((p) => ({ ...p, kind: 'person' }));
    const ext  = (state.stakeholders || []).map((s) => ({ ...s, kind: 'stakeholder' }));
    return [...team, ...ext];
  }
  function isExternalActor(id) {
    if (!id) return false;
    return (state.stakeholders || []).some((s) => s.id === id);
  }

  // Mark the in-memory state as dirty — any code path that mutates state
  // before a debounced/async write should call this so the status pill
  // can degrade until the write lands.
  function markDirty() {
    _lastEditAt = new Date().toISOString();
    try { refreshStorageStatusLine(); } catch (_) {}
  }

  function saveState() {
    if (_recoveryMode) return;
    // Always mirror to IndexedDB — no quota cliff at 5 MB and primary
    // store of choice once the dataset grows past localStorage limits.
    try { stateIdbWrite(state); } catch (e) { console.warn('[cockpit] state IDB write rejected:', e); }
    // Mirror to localStorage as a fast-read fallback, until / unless it
    // errors (quota exceeded). Once disabled, stays disabled for this
    // session — the IDB write keeps everything safe.
    if (!_localStorageDisabled) {
      try {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
        _lastSaveError = null;
      } catch (e) {
        const msg = String(e?.message || e);
        if (/quota|exceed/i.test(msg)) {
          _localStorageDisabled = true;
          _lastSaveError = null; // IDB still has it, not a real failure
          console.warn('[cockpit] localStorage quota exceeded — switching to IDB-only.');
        } else {
          _lastSaveError = msg;
          console.warn('[cockpit] localStorage write failed:', e);
        }
      }
    }
    _lastSaveAt = new Date().toISOString();
    // Secondary, durable backup to the Origin Private File System —
    // debounced so rapid edits coalesce into a single write.
    try { scheduleOpfsBackup(); } catch (_) { /* opfs may be undefined during early init */ }
    // Refresh the bottom-right status line whenever a save completes.
    try { refreshStorageUI(); } catch (_) {}
  }

  // Shows a non-dismissable overlay when localStorage data exists but
  // can't be loaded, so the user can rescue their bytes before deciding
  // how to proceed. saveState() is suppressed (via _recoveryMode) until
  // an option is chosen — the original localStorage is never touched.
  function enterRecoveryMode(kind, errMsg, rawBytes) {
    _recoveryMode = true;
    try { state = seedState(); normalizeState(state); }
    catch (e) { /* if even the seed throws, render with a barely-valid stub */
      state = { people: [], projects: [], settings: {}, currentView: 'board', currentProjectId: null };
      try { normalizeState(state); } catch (_) {}
    }
    // Defer the overlay until after init() finishes wiring the shell, so
    // the overlay sits on top of a fully rendered (placeholder) UI.
    setTimeout(() => showRecoveryOverlay(kind, errMsg, rawBytes), 0);
  }

  function showRecoveryOverlay(kind, errMsg, rawBytes) {
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.style.zIndex = '9999';
    const headline = kind === 'parse'
      ? "Your saved data couldn't be parsed (the JSON is corrupted)."
      : "Your saved data was found, but it failed a schema check during load.";
    overlay.innerHTML = `
      <div class="desc-modal" style="width: 560px; max-width: 92vw;">
        <div class="desc-head">
          <div class="desc-title">⚠ Could not load your saved data</div>
        </div>
        <div style="padding: 14px 16px; display: flex; flex-direction: column; gap: 12px;">
          <div>${headline}</div>
          <div><strong>Nothing has been overwritten yet.</strong> Your original bytes are still in the browser's localStorage; the app is currently running on sample data with saving paused so the original isn't replaced. Choose an option below.</div>
          <pre style="background: var(--bg-2); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 10px; font-size: 11px; color: var(--text-faint); white-space: pre-wrap; word-break: break-word; max-height: 120px; overflow: auto; margin: 0;">${escapeHTML(errMsg)}</pre>
          <div style="display: flex; flex-direction: column; gap: 6px;">
            <button class="primary" id="recDownload">↓ Download broken JSON (recommended first)</button>
            <button class="ghost"   id="recImport">  Import a backup file…</button>
            <button class="ghost"   id="recRetry">   Retry — reload the page</button>
            <button class="danger"  id="recForget">  Discard original — start with sample data</button>
          </div>
        </div>
      </div>`;
    document.body.appendChild(overlay);

    overlay.querySelector('#recDownload').addEventListener('click', () => {
      const blob = new Blob([rawBytes ?? ''], { type: 'application/json' });
      const a = document.createElement('a');
      a.href = URL.createObjectURL(blob);
      a.download = `cockpit-recovery-${new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19)}.json`;
      a.click();
      URL.revokeObjectURL(a.href);
      toast('Downloaded raw bytes — keep this safe.');
    });

    overlay.querySelector('#recImport').addEventListener('click', () => {
      const input = document.createElement('input');
      input.type = 'file'; input.accept = 'application/json';
      input.addEventListener('change', () => {
        const file = input.files?.[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = () => {
          try {
            const obj = JSON.parse(reader.result);
            if (!obj || typeof obj !== 'object') throw new Error('Invalid JSON');
            if (!obj.projects || !obj.people) throw new Error('Missing projects/people — does not look like a Cockpit backup');
            delete obj.__schemaVersion;
            delete obj.__exportedAt;
            delete obj.__app;
            const next = normalizeState(obj);
            state = next;
            _recoveryMode = false;
            saveState();
            overlay.remove();
            render();
            toast('Backup restored');
          } catch (e) {
            alert('Could not load that file:\n\n' + (e?.message || e));
          }
        };
        reader.readAsText(file);
      });
      input.click();
    });

    overlay.querySelector('#recRetry').addEventListener('click', () => {
      location.reload();
    });

    overlay.querySelector('#recForget').addEventListener('click', () => {
      const typed = prompt(
        'This will overwrite your saved data with the sample dataset. ' +
        'Make sure you downloaded the broken JSON above before proceeding.\n\n' +
        'Type DISCARD (in caps) to confirm:',
      );
      if ((typed || '').trim() !== 'DISCARD') { toast('Cancelled — original data is still intact'); return; }
      state = seedState();
      normalizeState(state);
      _recoveryMode = false;
      saveState();
      overlay.remove();
      render();
      toast('Started fresh with sample data');
    });
  }

  // structuredClone is browser-native, ~3× faster than JSON.stringify+parse
  // for our state shape, and preserves Date / Map / Set if we ever add them.
  // Falls back to JSON for the rare environment that lacks it.
  function snapshotState(s) {
    try { return typeof structuredClone === 'function' ? structuredClone(s) : JSON.parse(JSON.stringify(s)); }
    catch (_) { return JSON.parse(JSON.stringify(s)); }
  }

  function commit(action = 'change') {
    undoStack.push(snapshotState(state));
    if (undoStack.length > HISTORY_LIMIT) undoStack.shift();
    redoStack = [];
    // Mark dirty BEFORE saveState so the status pill can flash "Saving…"
    // even though localStorage is synchronous; OPFS still rides the
    // 3-second debounce so the pill reads "Saved · backup pending"
    // until that lands.
    _lastEditAt = new Date().toISOString();
    invalidateRenderCache();
    saveState();
    render();
  }
  // Phase A helper — stamp signed-edit metadata on a record (action, CR,
  // open point, …) before committing. Optional `editor` lets future team-mode
  // identify the author; defaults to the local user marker.
  // Also appends an entry to record.__history (capped) so the editor can
  // surface a per-record changelog. `summary` is a short human-readable
  // description of what changed; falls back to a generic label.
  const HISTORY_LIMIT_PER_RECORD = 60;
  function stampEdit(record, editor, summary) {
    if (!record || typeof record !== 'object') return;
    const at = new Date().toISOString();
    const by = editor || (state.settings.localUser || 'me');
    record.__lastEditor = by;
    record.__lastEditAt = at;
    if (!Array.isArray(record.__history)) record.__history = [];
    record.__history.push({ at, by, summary: summary || 'edit' });
    if (record.__history.length > HISTORY_LIMIT_PER_RECORD) {
      record.__history.splice(0, record.__history.length - HISTORY_LIMIT_PER_RECORD);
    }
  }
  // Wraps a mutator: runs it, stamps any returned record(s), then commits.
  // Use as `mutate(() => { … return action; }, 'commit-name')`.
  function mutate(fn, commitName) {
    const result = fn();
    if (result) {
      if (Array.isArray(result)) result.forEach(stampEdit);
      else stampEdit(result);
    }
    commit(commitName || 'edit');
    return result;
  }
  // Stack entries can be either structuredClone snapshots (fast path) or
  // legacy JSON strings (back-compat with existing entries pushed before
  // the snapshotState helper landed). Tolerate both on pop.
  function popStateFromStack(entry) {
    if (typeof entry === 'string') return JSON.parse(entry);
    return snapshotState(entry); // defensive copy to avoid aliasing
  }
  function undo() {
    if (!undoStack.length) return;
    redoStack.push(snapshotState(state));
    state = popStateFromStack(undoStack.pop());
    invalidateRenderCache();
    saveState();
    render();
    toast('Undone');
  }
  function redo() {
    if (!redoStack.length) return;
    undoStack.push(snapshotState(state));
    state = popStateFromStack(redoStack.pop());
    invalidateRenderCache();
    saveState();
    render();
    toast('Redone');
  }

  function seedState() {
    const today = new Date(); today.setHours(0, 0, 0, 0);
    const d = (off) => fmtISO(new Date(today.getTime() + off * dayMs));

    // Deterministic PRNG so reseeding always gives the same dataset
    let _s = 0xC0FFEE;
    const rnd = () => { _s = (Math.imul(_s, 1103515245) + 12345) & 0x7FFFFFFF; return _s / 0x7FFFFFFF; };
    const ri = (lo, hi) => Math.floor(rnd() * (hi - lo + 1)) + lo;
    const pick = (arr) => arr[Math.floor(rnd() * arr.length)];
    const pickW = (entries) => {
      // entries: [[value, weight], ...]
      const total = entries.reduce((s, e) => s + e[1], 0);
      let r = rnd() * total;
      for (const [v, w] of entries) { if ((r -= w) <= 0) return v; }
      return entries[0][0];
    };
    const chance = (p) => rnd() < p;

    // capacity is in % of FTE (1 FTE = 8h/day × 5 days/week, 212 working days/year)
    // assignments split each person's FTE across the three demo projects.
    const people = [
      { id: 'p_sofia', name: 'Sofia Reyes',     role: 'Project Manager',     capacity: 100, hourlyRate: 140, assignments: [{ projectId: 'pr_orbit', commitment: 60 }, { projectId: 'pr_helios', commitment: 40 }] },
      { id: 'p_marie', name: 'Marie Laurent',   role: 'Systems Engineer',    capacity: 100, hourlyRate: 130, assignments: [{ projectId: 'pr_orbit', commitment: 80 }, { projectId: 'pr_falcon', commitment: 20 }] },
      { id: 'p_arjun', name: 'Arjun Patel',     role: 'Avionics Lead',       capacity: 100, hourlyRate: 145, assignments: [{ projectId: 'pr_orbit', commitment: 100 }] },
      { id: 'p_jonas', name: 'Jonas Becker',    role: 'Mechanical',          capacity:  80, hourlyRate: 120, assignments: [{ projectId: 'pr_orbit', commitment: 60 }, { projectId: 'pr_falcon', commitment: 20 }] },
      { id: 'p_kira',  name: 'Kira Nakamura',   role: 'Software Architect',  capacity: 100, hourlyRate: 150, assignments: [{ projectId: 'pr_orbit', commitment: 50 }, { projectId: 'pr_helios', commitment: 50 }] },
      { id: 'p_omar',  name: 'Omar El-Sayed',   role: 'Power Systems',       capacity:  80, hourlyRate: 125, assignments: [{ projectId: 'pr_orbit', commitment: 80 }] },
      { id: 'p_lena',  name: 'Lena Holmberg',   role: 'Thermal Engineer',    capacity:  80, hourlyRate: 125, assignments: [{ projectId: 'pr_orbit', commitment: 60 }, { projectId: 'pr_falcon', commitment: 20 }] },
      { id: 'p_diego', name: 'Diego Ferreira',  role: 'AOCS',                capacity: 100, hourlyRate: 135, assignments: [{ projectId: 'pr_orbit', commitment: 70 }, { projectId: 'pr_falcon', commitment: 30 }] },
      { id: 'p_yuki',  name: 'Yuki Tanaka',     role: 'Software Developer',  capacity: 100, hourlyRate: 110, assignments: [{ projectId: 'pr_helios', commitment: 100 }] },
      { id: 'p_nadia', name: 'Nadia Rahman',    role: 'Test Engineer',       capacity: 100, hourlyRate: 115, assignments: [{ projectId: 'pr_orbit', commitment: 100 }] },
    ];
    // External stakeholders — customer POCs, regulators, sister-project
    // liaisons. No capacity; still ownable / originatable so we can
    // track "waiting on X" actions against them.
    const stakeholders = [
      { id: 'st_customer',  name: 'Isabelle Fournier', role: 'Programme POC',     organization: 'CNES' },
      { id: 'st_regulator', name: 'Hendrik van Loon',  role: 'Frequency Officer', organization: 'ITU' },
      { id: 'st_sister',    name: 'Mateus Vieira',     role: 'Sister-mission SE',  organization: 'Mosaic-3' },
    ];

    // Title pools per component
    const TITLES = {
      power:    ['Power budget v{n}', 'Battery cell qualification', 'Solar array sizing', 'PCDU prototype build', 'Charge regulator FW {n}', 'EPS block diagram', 'Power emergency mode logic', 'Battery thermal coupling', 'Solar string analysis', 'Bus undervoltage analysis'],
      aocs:     ['Reaction wheel sizing', 'Sun sensor algorithm', 'AOCS controller v{n}', 'Star tracker integration', 'Magnetic torquer cal', 'Detumbling logic', 'Slew maneuver simulation', 'Pointing budget update', 'Gyro bias study', 'Safe-mode attitude logic'],
      struct:   ['Primary structure FE model', 'Mass budget update {n}', 'Vibration test plan', 'Panel layout v{n}', 'Connector spec review', 'Insert design review', 'Tank mounting study', 'CFRP layup spec', 'Harness routing study', 'Random-vibe coupon test'],
      thermal:  ['Thermal model v{n}', 'Radiator sizing', 'TVAC test plan', 'Heaters specification', 'MLI coverage analysis', 'Thermal interface specs', 'Thermal balance test prep', 'Hot-case analysis', 'Cold-case worst-case'],
      sw:       ['FSW build v{n}', 'Boot loader review', 'Telemetry decoder', 'Command dictionary v{n}', 'OBSW unit tests', 'OBSW integration build', 'Memory scrubbing logic', 'Watchdog logic', 'FDIR matrix update', 'Software ICD v{n}'],
      payload:  ['Optical bench design', 'Detector readout chain', 'Calibration plan', 'Payload electrical interface', 'Filter wheel mechanism', 'Optical alignment procedure', 'Stray-light analysis'],
      avionics: ['CDPU bring-up', 'Avionics block diagram', 'Connector pin mapping', 'EMC pre-test', 'OBC-MEMS interface', 'Star tracker thermal interface', 'Harness FMEA'],
      pm:       ['Project plan v{n}', 'Milestone review prep', 'Budget reforecast', 'Customer review prep', 'WBS update', 'Subcontractor review {n}', 'Risk review session'],
      test:     ['Vibration test prep', 'TVAC chamber booking', 'EMC test plan', 'Acceptance test procedure', 'Test bench bring-up', 'GSE check-out'],
      be:       ['Auth refactor v{n}', 'Telemetry decoder library', 'Scheduler service', 'Database migration {n}', 'API rate-limit middleware', 'Background worker queue', 'Audit log v{n}'],
      fe:       ['Procedure editor MVP', 'Live telemetry view', 'Pass-plan UI v{n}', 'Operator dashboard', 'Theme + design tokens', 'Form validation pass'],
      ops:      ['Operator training plan', 'Runbook v{n}', 'Pager rotation setup', 'On-call drill', 'Incident postmortem template'],
      mech:     ['Airframe trade study', 'Motor mount design', 'Landing-gear stress analysis', 'Prop balancing rig'],
      flight:   ['Flight controller bring-up', 'IMU calibration', 'PID tuning sortie', 'GPS lock-in study'],
    };

    // Notes pool — small chance an action gets one
    const NOTES = [
      'Awaiting connector spec from supplier.',
      'Pending customer review of options A and B.',
      'Preliminary results look promising, need to confirm.',
      'Coupling with thermal still TBD.',
      'Held up on availability of test hardware.',
      'Re-baselined after design freeze meeting.',
      'Cross-check with sister-mission heritage.',
      'Will need a second iteration after PDR.',
    ];

    /* Generate one project's actions and history.
       phases: array of { startOff, endOff, density, statusBias }.
       statusBias: 'past' (mostly done), 'present' (mix), 'future' (mostly todo). */
    function genActions(spec) {
      const out = [];
      let counter = 0;
      const titleIters = {};
      for (const phase of spec.phases) {
        const { startOff, endOff, density, statusBias } = phase;
        const span = endOff - startOff;
        const count = Math.max(1, Math.round(span * density));
        for (let i = 0; i < count; i++) {
          const startOffset = startOff + Math.floor(rnd() * span);
          const dur = ri(2, 14);
          const dueOffset = startOffset + dur;
          // Only consider components offered for this project
          const cmp = pick(spec.components);
          const pool = TITLES[cmp.key] || ['Task {n}'];
          titleIters[cmp.key] = (titleIters[cmp.key] || 0) + 1;
          const title = pick(pool).replace('{n}', String(titleIters[cmp.key]));
          // Owner: prefer cmp.owner, otherwise pick from project owners weighted
          const owner = chance(0.65) && cmp.owner ? cmp.owner : pick(spec.owners);

          // Determine status from bias
          let status;
          if (dueOffset < -2) {
            // Past due — almost always done; sometimes still doing or blocked
            status = pickW([['done', 88], ['blocked', 6], ['doing', 6]]);
          } else if (dueOffset >= -2 && dueOffset <= 14) {
            status = pickW([['doing', 45], ['todo', 25], ['blocked', 12], ['done', 18]]);
          } else {
            status = pickW([['todo', 75], ['doing', 18], ['blocked', 4], ['done', 3]]);
          }
          if (statusBias === 'past' && dueOffset > -2) status = pickW([['done', 60], ['doing', 25], ['todo', 10], ['blocked', 5]]);
          if (statusBias === 'future' && dueOffset < 0) status = pickW([['done', 70], ['doing', 20], ['todo', 5], ['blocked', 5]]);

          const createdOff = startOffset - ri(7, 28);
          const createdISO = d(createdOff);
          const startISO = d(startOffset);
          const dueISO = d(dueOffset);

          // Build history with realistic schedule changes (mostly stable,
          // some slips, some pull-ins).
          const history = [{ at: createdISO, what: `Created with status todo` }];
          // Slip frequency: blocked items often slip, done items rarely (most landed),
          // active items occasionally, future items least.
          const slipChance = status === 'blocked' ? 0.55
                           : status === 'doing'   ? 0.22
                           : status === 'done'    ? 0.12
                           : 0.10;
          let curStart = startOffset, curDue = dueOffset;
          if (chance(slipChance)) {
            const slips = ri(1, status === 'blocked' ? 3 : 2);
            for (let s = 0; s < slips; s++) {
              const slipOnOff = curStart - ri(0, 7); // before original start
              if (slipOnOff > -1 || slipOnOff < createdOff + 2) continue;
              // ~25% of changes are pull-ins (work came in earlier than feared)
              const isPullIn = chance(0.25);
              const delta = isPullIn ? -ri(2, 6) : ri(2, 12);
              const newStartOff = Math.max(createdOff + 1, curStart + delta);
              const newDur = Math.max(2, (curDue - curStart) + (isPullIn ? ri(-2, 0) : ri(-1, 3)));
              const newDueOff = newStartOff + newDur;
              if (newStartOff === curStart && newDueOff === curDue) continue;
              history.push({ at: d(slipOnOff),
                what: `Schedule: ${d(curStart)} → ${d(newStartOff)}…${d(newDueOff)}` });
              curStart = newStartOff; curDue = newDueOff;
            }
          }

          // For done actions, add a "Status: X → done" entry shortly after due
          let updatedISO = createdISO;
          if (status === 'done') {
            const completedOff = curDue + ri(-2, 5);
            const completedISO = d(Math.min(-1, completedOff)); // never in the future
            history.push({ at: completedISO, what: `Status: doing → done` });
            updatedISO = completedISO;
          } else if (status !== 'todo') {
            const transitionOff = Math.max(createdOff + 1, ri(curStart - 7, curStart + 2));
            const transISO = d(Math.min(0, transitionOff));
            history.push({ at: transISO, what: `Status: todo → ${status}` });
            updatedISO = transISO;
          }

          // Use the most-recent forecast as the visible due
          const finalStartISO = d(curStart);
          const finalDueISO = d(curDue);

          // Commitment % — most actions are full-time (100%), some are part-time
          const commitment = pickW([[100, 60], [75, 12], [50, 18], [25, 10]]);

          out.push({
            id: uid('a'),
            title,
            owner,
            startDate: finalStartISO,
            due: finalDueISO,
            status,
            priority: counter++,
            commitment,
            component: cmp.id,
            deliverable: spec.deliverableFor ? spec.deliverableFor(cmp.id, dueOffset) : null,
            milestone: null,
            notes: chance(0.18) ? pick(NOTES) : '',
            createdAt: createdISO,
            updatedAt: updatedISO,
            originatorDate: createdISO,
            history,
          });
        }
      }
      return out;
    }
    const newDuration = (start, oldDur) => start + Math.max(2, oldDur + ri(-1, 4));

    /* ===== Project 1: Orbit-7 Satellite Bus =====
       Started ~370 days ago, currently in PDR phase, CDR ~120 days out. */
    const orbitComps = [
      { id: 'pt_power',   key: 'power',    name: 'Power',     color: 'amber',  owner: 'p_omar' },
      { id: 'pt_aocs',    key: 'aocs',     name: 'AOCS',      color: 'sky',    owner: 'p_diego' },
      { id: 'pt_struct',  key: 'struct',   name: 'Structure', color: 'slate',  owner: 'p_jonas' },
      { id: 'pt_thermal', key: 'thermal',  name: 'Thermal',   color: 'rose',   owner: 'p_lena' },
      { id: 'pt_sw',      key: 'sw',       name: 'Software',  color: 'violet', owner: 'p_kira' },
      { id: 'pt_payload', key: 'payload',  name: 'Payload',   color: 'cyan',   owner: 'p_marie' },
      { id: 'pt_avionics',key: 'avionics', name: 'Avionics',  color: 'indigo', owner: 'p_arjun' },
      { id: 'pt_pm',      key: 'pm',       name: 'PM',        color: 'mint',   owner: 'p_sofia' },
      { id: 'pt_test',    key: 'test',     name: 'Test',      color: 'lime',   owner: 'p_nadia' },
    ];
    const orbitOwners = ['p_sofia','p_marie','p_arjun','p_jonas','p_kira','p_omar','p_lena','p_diego','p_nadia'];
    const orbitDeliverables = [
      { id: 'd_srr',  name: 'SRR data pack',           dueDate: d(-300), status: 'done' },
      { id: 'd_pdr',  name: 'PDR data pack',           dueDate: d(28),   status: 'doing' },
      { id: 'd_cdr',  name: 'CDR data pack',           dueDate: d(120),  status: 'todo' },
      { id: 'd_tvac', name: 'TVAC test report',        dueDate: d(150),  status: 'todo' },
      { id: 'd_aiv',  name: 'AIV plan',                dueDate: d(60),   status: 'todo' },
    ];
    const orbitActions = genActions({
      components: orbitComps, owners: orbitOwners,
      deliverableFor: (cid, off) => {
        if (off < -210) return 'd_srr';
        if (off < 30)   return 'd_pdr';
        if (off < 120)  return 'd_cdr';
        if (off < 165)  return 'd_tvac';
        return null;
      },
      phases: [
        { startOff: -370, endOff: -300, density: 0.30, statusBias: 'past' },   // SRR campaign
        { startOff: -300, endOff: -180, density: 0.45, statusBias: 'past' },   // Phase A → PDR ramp
        { startOff: -180, endOff: -30,  density: 0.55, statusBias: 'past' },   // PDR push
        { startOff: -30,  endOff: 30,   density: 0.55, statusBias: 'present' },// Now
        { startOff: 30,   endOff: 120,  density: 0.45, statusBias: 'future' }, // PDR → CDR
        { startOff: 120,  endOff: 180,  density: 0.30, statusBias: 'future' }, // Post-CDR (planning)
      ],
    });
    // Wire a few canonical dependency chains so the Gantt's
    // critical-path engine has something to render. Pick the longest 5
    // sequential PM-phase actions per component and chain them in
    // reverse chronology (later one depends on earlier one). This
    // produces ~30-50 edges per project without saturating the view.
    (function seedDeps(actions) {
      const byComp = {};
      actions.forEach((a) => {
        (byComp[a.component] = byComp[a.component] || []).push(a);
      });
      Object.values(byComp).forEach((list) => {
        list.sort((a, b) => (a.due || '').localeCompare(b.due || ''));
        // Chain every 2nd action in the component → previous one (so we
        // get a forest of short chains rather than one giant rope).
        for (let i = 2; i < list.length; i += 2) {
          list[i].dependsOn = [list[i - 2].id];
        }
      });
    })(orbitActions);
    // Baseline ~70% of past + present actions so the dashboard shows
    // realistic CPI / SPI / variance out of the box (without forcing
    // the user to set a baseline themselves on first run).
    orbitActions.forEach((a) => {
      if (Math.random() < 0.7 && a.due) {
        a.baseline = {
          due: a.due,
          startDate: a.startDate || null,
          commitment: a.commitment,
          takenAt: a.createdAt,
        };
      }
    });
    // Log some hours against doing/done actions — sample the commitment
    // × elapsed-days × random scatter so totals look realistic. Use
    // sub-second deterministic IDs (uid handles it).
    orbitActions.forEach((a) => {
      if (a.status !== 'doing' && a.status !== 'done') return;
      if (Math.random() < 0.7) {
        const due = a.due ? new Date(a.due) : null;
        const start = a.startDate ? new Date(a.startDate) : due;
        if (!due || !start) return;
        const dayMsLocal = 86400000;
        const today = new Date();
        const elapsed = Math.max(1, Math.min((due - start) / dayMsLocal, (today - start) / dayMsLocal));
        const plannedHrs = (a.commitment / 100) * 40 * (elapsed / 7);
        const totalLogged = plannedHrs * (0.7 + Math.random() * 0.6); // 70-130% of plan
        const entries = Math.max(1, Math.round(elapsed / 5));
        const perEntry = totalLogged / entries;
        a.loggedHours = [];
        for (let i = 0; i < entries; i++) {
          const at = new Date(start.getTime() + (i / entries) * (due - start));
          a.loggedHours.push({
            id: uid('lh'),
            hours: Math.round(perEntry * 4) / 4,
            at: fmtISO(at),
            by: a.owner,
            note: '',
          });
        }
      }
    });

    /* ===== Project 2: Helios-2 Ground Station Software =====
       Started ~190 days ago, beta in 18 days. */
    const heliosComps = [
      { id: 'cm_be',  key: 'be',  name: 'Backend',   color: 'indigo', owner: 'p_kira' },
      { id: 'cm_fe',  key: 'fe',  name: 'Frontend',  color: 'cyan',   owner: 'p_yuki' },
      { id: 'cm_ops', key: 'ops', name: 'Ops',       color: 'lime',   owner: 'p_sofia' },
    ];
    const heliosOwners = ['p_kira','p_yuki','p_sofia','p_marie'];
    const heliosDeliverables = [
      { id: 'd_alpha', name: 'Internal alpha',      dueDate: d(-90), status: 'done' },
      { id: 'd_beta',  name: 'Beta with ops team',  dueDate: d(18),  status: 'doing' },
      { id: 'd_v1',    name: 'v1.0 release',        dueDate: d(80),  status: 'todo' },
    ];
    const heliosActions = genActions({
      components: heliosComps, owners: heliosOwners,
      deliverableFor: (cid, off) => off < -60 ? 'd_alpha' : off < 25 ? 'd_beta' : 'd_v1',
      phases: [
        { startOff: -190, endOff: -90, density: 0.25, statusBias: 'past' },
        { startOff: -90,  endOff: -10, density: 0.45, statusBias: 'past' },
        { startOff: -10,  endOff: 30,  density: 0.55, statusBias: 'present' },
        { startOff: 30,   endOff: 100, density: 0.40, statusBias: 'future' },
        { startOff: 100,  endOff: 180, density: 0.20, statusBias: 'future' },
      ],
    });

    /* ===== Project 3: Falcon Drone R&D ===== */
    const falconComps = [
      { id: 'cm_avion',  key: 'avionics', name: 'Avionics',   color: 'indigo', owner: 'p_arjun' },
      { id: 'cm_mech',   key: 'mech',     name: 'Mechanical', color: 'slate',  owner: 'p_jonas' },
      { id: 'cm_flight', key: 'flight',   name: 'Flight SW',  color: 'violet', owner: 'p_kira' },
      { id: 'cm_test',   key: 'test',     name: 'Test',       color: 'lime',   owner: 'p_nadia' },
    ];
    const falconOwners = ['p_arjun','p_jonas','p_kira','p_nadia','p_yuki'];
    const falconDeliverables = [
      { id: 'd_proto1', name: 'Prototype P1 hover', dueDate: d(-25), status: 'done' },
      { id: 'd_proto2', name: 'Prototype P2 flight envelope', dueDate: d(45), status: 'doing' },
      { id: 'd_field',  name: 'Field test campaign', dueDate: d(110), status: 'todo' },
    ];
    const falconActions = genActions({
      components: falconComps, owners: falconOwners,
      deliverableFor: (cid, off) => off < -20 ? 'd_proto1' : off < 60 ? 'd_proto2' : 'd_field',
      phases: [
        { startOff: -90, endOff: -25, density: 0.45, statusBias: 'past' },
        { startOff: -25, endOff: 30,  density: 0.45, statusBias: 'present' },
        { startOff: 30,  endOff: 120, density: 0.30, statusBias: 'future' },
      ],
    });

    const proj1 = {
      id: 'pr_orbit', name: 'Orbit-7 Satellite Bus',
      description: 'Mid-class earth-observation platform — phase B, PDR closing in.',
      components: orbitComps.map(({ id, name, color, key }) => ({
        id, name, color,
        // Map components to their owning cost-centre so the budgets panel
        // groups workload correctly. Verification CC owns the Test
        // component; the rest live under engineering.
        costCenter: key === 'test' ? 'CC-TEST-ORBIT' : 'CC-ENG-ORBIT',
      })),
      deliverables: orbitDeliverables,
      milestones: [
        { id: 'm_srr', name: 'System Requirements Review', date: d(-310), status: 'done' },
        { id: 'm_pdr', name: 'Preliminary Design Review',  date: d(28),   status: 'todo' },
        { id: 'm_cdr', name: 'Critical Design Review',     date: d(120),  status: 'todo' },
        { id: 'm_trr', name: 'Test Readiness Review',      date: d(170),  status: 'todo' },
      ],
      risks: [
        { id: 'r_supply', kind: 'risk', title: 'Reaction wheel lead time slip',  inherent: { probability: 4, impact: 4 }, residual: { probability: 2, impact: 3 }, mitigation: 'Dual-source supplier engaged.', owner: 'p_arjun', actionId: orbitActions.find((a) => a.component === 'pt_aocs')?.id || null },
        { id: 'r_mass',   kind: 'risk', title: 'Mass margin trending under 5%',  inherent: { probability: 4, impact: 3 }, residual: { probability: 2, impact: 3 }, mitigation: 'Lightweighting study + panel optimisation.', owner: 'p_jonas', actionId: orbitActions.find((a) => a.component === 'pt_struct')?.id || null },
        { id: 'r_power',  kind: 'risk', title: 'EOL power margin tight',         inherent: { probability: 3, impact: 4 }, residual: { probability: 2, impact: 3 }, mitigation: 'Trade study on cell vendor + duty-cycle review.', owner: 'p_omar' },
        { id: 'r_thermal',kind: 'risk', title: 'Hot-case radiator under-sized',  inherent: { probability: 3, impact: 4 }, residual: { probability: 1, impact: 3 }, mitigation: 'Adding louvres to baseline; thermal balance test scheduled.', owner: 'p_lena' },
        { id: 'r_sw',     kind: 'risk', title: 'FSW timeline at risk',           inherent: { probability: 4, impact: 3 }, residual: { probability: 3, impact: 2 }, mitigation: 'Early integration build, weekly scrum, heritage reuse.', owner: 'p_kira' },
        { id: 'r_test',   kind: 'risk', title: 'TVAC chamber availability',      inherent: { probability: 4, impact: 3 }, residual: { probability: 2, impact: 2 }, mitigation: 'Booked alternate facility on standby.', owner: 'p_nadia' },
        { id: 'o_heritage', kind: 'opportunity', title: 'Reuse Mosaic-3 attitude FSW heritage', inherent: { probability: 3, impact: 3 }, residual: { probability: 4, impact: 4 }, mitigation: 'Negotiate IP transfer with sister mission, save ~6 weeks of FSW work.', owner: 'p_kira' },
        { id: 'o_batch',    kind: 'opportunity', title: 'Volume discount on Li-ion cells',     inherent: { probability: 2, impact: 2 }, residual: { probability: 4, impact: 3 }, mitigation: 'Combine PO with sister mission for 12% unit-price reduction.', owner: 'p_omar' },
        { id: 'o_chamber',  kind: 'opportunity', title: 'Earlier TVAC slot at partner facility', inherent: { probability: 2, impact: 3 }, residual: { probability: 3, impact: 4 }, mitigation: 'Partner facility offered Aug slot — could pull in TVAC by 4 weeks.', owner: 'p_nadia' },
      ],
      decisions: [
        { id: 'dec_bus',    title: 'Down-select to BusFrame v3',   rationale: 'Best mass and thermal envelope after trade study.',    date: d(-220), owner: 'p_sofia', status: 'in_effect' },
        { id: 'dec_rw',     title: 'Reaction wheel: Vendor Bravo', rationale: 'Lifetime + lead time vs Vendor Alpha.',                date: d(-160), owner: 'p_arjun', status: 'in_effect', triggerRef: { type: 'risk', id: 'r_supply' } },
        { id: 'dec_battery',title: 'Li-ion 18650 cell — Vendor C', rationale: 'Heritage in similar LEO mission, 15-yr vendor support.', date: d(-95),  owner: 'p_omar',  status: 'in_effect' },
        { id: 'dec_optic',  title: 'Single-aperture optical bench', rationale: 'Mass and integration win vs dual-aperture option.',    date: d(-60),  owner: 'p_marie', status: 'in_effect' },
        { id: 'dec_pdr',    title: 'PDR slipped 2 weeks',           rationale: 'Customer requested additional FDIR work; risk register updated.', date: d(-12), owner: 'p_sofia', status: 'in_effect', triggerRef: 'Customer request' },
      ],
      changes: [
        { id: 'cr_fdir',    title: 'Add FDIR scope to PDR data pack',     rationale: 'Customer mandated FDIR review at PDR.', description: 'Expand FDIR analysis to include single-point failures of TT&C and AOCS.', analysis: 'Two extra weeks of FSW + verification effort; offset by re-using Mosaic-3 heritage.', status: 'approved', originator: 'p_sofia', originatedDate: d(-22), decisionBy: 'p_sofia', decisionDate: d(-12), priorityLevel: 'high', component: 'pt_pm', impact: { schedule: 14, cost: 28000, scope: 'Adds 2 FSW work-packages.', risk: 'Mitigates r_sw' }, tags: [], comments: [] },
        { id: 'cr_thrtest', title: 'Pull-in TVAC by 4 weeks',             rationale: 'Partner facility offered earlier slot.', description: 'Move TVAC campaign from week 24 to week 20.', analysis: 'Cuts post-CDR margin to 2 weeks. Worth it if r_test materialises.', status: 'under_review', originator: 'p_nadia', originatedDate: d(-8), decisionBy: null, decisionDate: null, priorityLevel: 'med', component: 'pt_test', impact: { schedule: -28, cost: 12000, scope: 'No scope change.', risk: 'Reduces TVAC availability risk.' }, tags: [], comments: [] },
        { id: 'cr_cell',    title: 'Switch Li-ion vendor on batch order', rationale: 'Volume discount opportunity.', description: 'Combine PO with sister mission for 12% reduction.', analysis: 'Slight requalification cost; net positive over the buy.', status: 'proposed', originator: 'p_omar', originatedDate: d(-3), decisionBy: null, decisionDate: null, priorityLevel: 'low', component: 'pt_power', impact: { schedule: 0, cost: -45000, scope: 'No scope change.', risk: 'Late vendor switch risk.' }, tags: [], comments: [] },
      ],
      // Cost-centre codes — bare strings, matching the schema the rest
      // of the app expects (getCostCentres / state.budgets keys).
      costCenters: ['CC-ENG-ORBIT', 'CC-TEST-ORBIT'],
      meetings: [
        { id: 'mtg_standup', kind: 'weekly', title: 'Eng standup',     dayOfWeek: 1, startDate: d(-365), time: '09:30' },
        { id: 'mtg_pmrev',   kind: 'weekly', title: 'PM weekly review', dayOfWeek: 5, startDate: d(-365), time: '15:00' },
        { id: 'mtg_pdrkick', kind: 'oneoff', title: 'PDR pre-meet w/ customer', date: d(14), time: '10:00' },
        { id: 'mtg_pdr',     kind: 'oneoff', title: 'PDR data-pack walkthrough', date: d(28), time: '14:00' },
        { id: 'mtg_riskrev', kind: 'oneoff', title: 'Risk register review',     date: d(7),  time: '11:00' },
      ],
      actions: orbitActions,
    };

    const proj2 = {
      id: 'pr_helios', name: 'Helios-2 Ground Station Software',
      description: 'Operations console rewrite — multi-mission scheduling and live telemetry.',
      components: heliosComps.map(({ id, name, color }) => ({ id, name, color })),
      deliverables: heliosDeliverables,
      milestones: [
        { id: 'm_alpha', name: 'Internal alpha cut',  date: d(-90), status: 'done' },
        { id: 'm_beta',  name: 'Beta with ops team',  date: d(18),  status: 'todo' },
        { id: 'm_ga',    name: 'v1.0 GA',             date: d(80),  status: 'todo' },
      ],
      risks: [
        { id: 'r_h_perf', kind: 'risk', title: 'Telemetry decoder throughput',  probability: 3, impact: 3, mitigation: 'Profile hot path, add backpressure.', owner: 'p_kira' },
        { id: 'r_h_ux',   kind: 'risk', title: 'Procedure editor UX scope',     probability: 3, impact: 2, mitigation: 'Two design rounds with ops users.',     owner: 'p_yuki' },
        { id: 'o_h_oss',  kind: 'opportunity', title: 'Open-source the telemetry decoder', probability: 3, impact: 3, mitigation: 'Adoption could drive contributions and hire pipeline.', owner: 'p_sofia' },
      ],
      decisions: [
        { id: 'dec_h_db',  title: 'Postgres for time-series store', rationale: 'Operational simplicity vs InfluxDB; volume tractable.', date: d(-130), owner: 'p_kira' },
        { id: 'dec_h_auth',title: 'OIDC via central IdP',           rationale: 'Aligns with org-wide SSO programme.',                  date: d(-70),  owner: 'p_sofia' },
      ],
      changes: [],
      actions: heliosActions,
    };

    const proj3 = {
      id: 'pr_falcon', name: 'Falcon Drone R&D',
      description: 'Rapid prototype platform for autonomous flight experiments.',
      components: falconComps.map(({ id, name, color }) => ({ id, name, color })),
      deliverables: falconDeliverables,
      milestones: [
        { id: 'm_p1', name: 'P1 hover demo', date: d(-25), status: 'done' },
        { id: 'm_p2', name: 'P2 flight envelope', date: d(45), status: 'todo' },
        { id: 'm_f1', name: 'Field test campaign 1', date: d(110), status: 'todo' },
      ],
      risks: [
        { id: 'r_f_battery', kind: 'risk', title: 'Battery thermal runaway during fast charge', probability: 2, impact: 5, mitigation: 'Cell-level temperature monitoring; conservative charge profile.', owner: 'p_omar' },
        { id: 'r_f_field',   kind: 'risk', title: 'Outdoor test weather window',                probability: 3, impact: 2, mitigation: 'Two backup test windows scheduled.', owner: 'p_nadia' },
        { id: 'o_f_grant',   kind: 'opportunity', title: 'R&D grant for autonomous swarm tests', probability: 2, impact: 4, mitigation: 'Eligibility confirmed; submission window opens in 6 weeks.', owner: 'p_sofia' },
      ],
      decisions: [
        { id: 'dec_f_motor', title: 'Brushless motor: T-Motor MN5008', rationale: 'Best thrust/weight at target battery voltage.',  date: d(-70), owner: 'p_jonas' },
      ],
      changes: [],
      actions: falconActions,
    };

    // Seed weekly budgets for the Orbit-7 cost centres so the budgets
    // panel + Dashboard CPI / SPI tile have something to render out of
    // the box. ~26 weeks back + 78 forward (matching the default 2-year
    // window). Engineering CC has a heavier load (eng phase running);
    // verification CC is flat zero until ~week 14 then ramps for TVAC.
    const budgets = {};
    (function seedBudgets() {
      const today = new Date(); today.setHours(0,0,0,0);
      let monday = new Date(today);
      while (monday.getDay() !== 1) monday = new Date(monday.getTime() - 86400000);
      monday = new Date(monday.getTime() - 26 * 7 * 86400000);
      const eng  = {}; const test = {};
      for (let w = 0; w < 104; w++) {
        const start = new Date(monday.getTime() + w * 7 * 86400000);
        const iso = fmtISO(start);
        const phase = w - 26; // weeks from today
        // Engineering: high during PDR push (phases past-now), drops
        // post-CDR.
        let engH = 320;
        if (phase < -16) engH = 280;
        else if (phase < 4) engH = 380;
        else if (phase < 18) engH = 360;
        else if (phase < 30) engH = 280;
        else engH = 220;
        // Verification: minimal until TVAC ramp at week ~14, peak at 16-22.
        let testH = 0;
        if (phase >= 12 && phase < 22) testH = 240;
        else if (phase >= 22 && phase < 28) testH = 160;
        eng[iso]  = engH;
        test[iso] = testH;
      }
      budgets['CC-ENG-ORBIT']  = eng;
      budgets['CC-TEST-ORBIT'] = test;
    })();

    /* ─────────────────── augmentDemo() ──────────────────────────────
     * Fills every demo-data gap identified in the seed audit so the
     * shipping fixture exercises every feature: tags, open points,
     * links + folders, project notes, manual % complete, action tags,
     * comments, descriptions, recurrence, snooze, cost centres on the
     * other two projects, change requests on the other two projects,
     * meetings on Helios + Falcon, decision comments, saved views,
     * personal todos.
     * Mutates proj1/proj2/proj3 + budgets in-place; returns the
     * top-level extras (notes, savedViews, todos) for the return
     * object to spread. */
    const extras = (function augmentDemo() {
      // ── Project tags ────────────────────────────────────────────
      proj1.tags = [
        { id: 'tg_o_cust',   name: 'Customer rqmt',  rgb: '99,160,255' },
        { id: 'tg_o_must',   name: 'Must-have',      rgb: '248,113,121' },
        { id: 'tg_o_herit',  name: 'Heritage',       rgb: '101,194,73' },
        { id: 'tg_o_ext',    name: 'External',       rgb: '194,99,168' },
        { id: 'tg_o_block',  name: 'Blocker risk',   rgb: '245,193,80' },
      ];
      proj2.tags = [
        { id: 'tg_h_beta',   name: 'Beta-blocker',   rgb: '248,113,121' },
        { id: 'tg_h_perf',   name: 'Performance',    rgb: '245,193,80' },
        { id: 'tg_h_ux',     name: 'UX',             rgb: '79,194,176' },
        { id: 'tg_h_security', name: 'Security',     rgb: '194,99,168' },
      ];
      proj3.tags = [
        { id: 'tg_f_flight', name: 'Flight-critical', rgb: '248,113,121' },
        { id: 'tg_f_ground', name: 'Ground systems',  rgb: '99,160,255' },
        { id: 'tg_f_rnd',    name: 'Research',        rgb: '168,140,255' },
      ];

      // ── Cost centres + budgets on Helios + Falcon ───────────────
      // Helios: one CC for development, weekly budget.
      proj2.costCenters = ['CC-DEV-HELIOS'];
      // Map the relevant components to that CC so the budgets panel
      // groups them correctly.
      proj2.components.forEach((c) => { c.costCenter = 'CC-DEV-HELIOS'; });
      const helEng = {};
      // Same week schedule as Orbit-7
      (function () {
        const today = new Date(); today.setHours(0,0,0,0);
        let monday = new Date(today);
        while (monday.getDay() !== 1) monday = new Date(monday.getTime() - 86400000);
        monday = new Date(monday.getTime() - 26 * 7 * 86400000);
        for (let w = 0; w < 104; w++) {
          const iso = fmtISO(new Date(monday.getTime() + w * 7 * 86400000));
          // Helios is in a beta push — 200 h/wk steady, dip post-GA.
          const phase = w - 26;
          helEng[iso] = phase < -8 ? 160 : phase < 16 ? 220 : 140;
        }
      })();
      budgets['CC-DEV-HELIOS'] = helEng;

      // Falcon: one CC for R&D
      proj3.costCenters = ['CC-RND-FALCON'];
      proj3.components.forEach((c) => { c.costCenter = 'CC-RND-FALCON'; });
      const falEng = {};
      (function () {
        const today = new Date(); today.setHours(0,0,0,0);
        let monday = new Date(today);
        while (monday.getDay() !== 1) monday = new Date(monday.getTime() - 86400000);
        monday = new Date(monday.getTime() - 26 * 7 * 86400000);
        for (let w = 0; w < 104; w++) {
          const iso = fmtISO(new Date(monday.getTime() + w * 7 * 86400000));
          const phase = w - 26;
          // Lean R&D budget that ramps for field tests around week 16
          falEng[iso] = phase < 0 ? 80 : phase < 18 ? 140 : 100;
        }
      })();
      budgets['CC-RND-FALCON'] = falEng;

      // ── Change requests for Helios + Falcon ─────────────────────
      proj2.changes = [
        { id: 'cr_h_oss',  title: 'Open-source the decoder library',
          rationale: 'Brings in community fixes + recruitment pipeline.',
          description: 'Move the decoder package to GitHub under Apache-2 with CI + contrib guide.',
          analysis: 'Marginal legal review needed; engineering time ~2 weeks.',
          status: 'under_review', originator: 'p_sofia', originatedDate: d(-12),
          decisionBy: null, decisionDate: null, priorityLevel: 'med',
          component: 'cm_be',
          impact: { schedule: 7, cost: 6000, scope: 'Adds OSS publishing workflow.',
                    risk: 'Public API stability commitment.' },
          tags: [], comments: [] },
        { id: 'cr_h_sso',  title: 'Switch from password auth to org SSO',
          rationale: 'IT security mandate for any internal tool.',
          description: 'OIDC integration with central IdP, fallback for service accounts.',
          analysis: 'Backend rework ~3 weeks; frontend ~1 week.',
          status: 'approved', originator: 'p_sofia', originatedDate: d(-30),
          decisionBy: 'p_sofia', decisionDate: d(-18), priorityLevel: 'high',
          component: 'cm_be',
          impact: { schedule: 21, cost: 22000, scope: 'Removes password forms.',
                    risk: 'Loss of break-glass admin access.' },
          tags: [], comments: [] },
      ];
      proj3.changes = [
        { id: 'cr_f_motor', title: 'Upgrade to brushless motor T-Motor MN5008',
          rationale: 'Original choice underpowered for the heavier P2 frame.',
          description: 'Re-select, requalify thrust curves, update CAD + harness.',
          analysis: '6 weeks of mech rework; affects P2 schedule.',
          status: 'proposed', originator: 'p_jonas', originatedDate: d(-5),
          decisionBy: null, decisionDate: null, priorityLevel: 'high',
          component: 'cm_mech',
          impact: { schedule: 42, cost: 18000, scope: 'No scope change.',
                    risk: 'Heat dissipation requalification.' },
          tags: [], comments: [] },
      ];

      // ── Meetings on Helios + Falcon ─────────────────────────────
      proj2.meetings = [
        { id: 'mtg_h_standup', kind: 'weekly', title: 'Helios standup',
          dayOfWeek: 1, startDate: d(-180), time: '10:00' },
        { id: 'mtg_h_betarev', kind: 'oneoff', title: 'Beta review with Ops',
          date: d(18), time: '14:00' },
      ];
      proj3.meetings = [
        { id: 'mtg_f_weekly', kind: 'weekly', title: 'Falcon weekly sync',
          dayOfWeek: 3, startDate: d(-90), time: '11:00' },
        { id: 'mtg_f_field',  kind: 'oneoff', title: 'Field test go/no-go review',
          date: d(60), time: '09:00' },
      ];

      // ── Open Points ─────────────────────────────────────────────
      // Use the same generateOpenPointIdentifier helper the runtime
      // uses so identifiers match the project's initials convention.
      // (op.identifier left blank — normalizeState will fill it.)
      proj1.openPoints = [
        { id: 'op_o_battcap', title: 'Battery capacity margin — should we add 10%?',
          notes: '<p>Latest power budget shows EOL margin at 4.2%. Customer asked if we can push to 8% without exceeding mass.</p>',
          component: 'pt_power', criticality: 'high', priorityLevel: 'critical',
          createdAt: d(-18),
          steps: [
            { id: 'st_o1', text: 'Get vendor quote on 10% larger cells', done: true },
            { id: 'st_o2', text: 'Run mass impact analysis', done: false },
            { id: 'st_o3', text: 'Propose to PDR', done: false },
          ], comments: [], tags: ['tg_o_cust'] },
        { id: 'op_o_startrack', title: 'Star tracker calibration approach',
          notes: 'Vendor recommends two-pass; we have heritage with single-pass. Test results inconclusive at TID.',
          component: 'pt_aocs', criticality: 'med', priorityLevel: 'high',
          createdAt: d(-32),
          steps: [
            { id: 'st_o4', text: 'Heritage data dive (Mosaic-3)', done: true },
            { id: 'st_o5', text: 'Run sensitivity sims', done: true },
            { id: 'st_o6', text: 'Decision memo', done: false },
          ], comments: [], tags: ['tg_o_herit'] },
        { id: 'op_o_solar',   title: 'Vendor lead time for solar arrays slipped',
          notes: 'Original ATP was 14 weeks, now 22. Affects TVAC slot.',
          component: 'pt_power', criticality: 'critical', priorityLevel: 'critical',
          createdAt: d(-3), steps: [], comments: [], tags: ['tg_o_block', 'tg_o_ext'] },
        { id: 'op_o_fsw',     title: 'FSW: CCSDS aux header support?',
          notes: 'Customer requirement mid-cycle — feasible if we drop secondary header parse.',
          component: 'pt_sw', criticality: 'med', priorityLevel: 'med',
          createdAt: d(-44), steps: [], comments: [], tags: [] },
        { id: 'op_o_thermal', title: 'Thermal louvre material — Al vs PEEK?',
          notes: 'Mass cost vs thermal performance trade.',
          component: 'pt_thermal', criticality: 'med', priorityLevel: 'med',
          createdAt: d(-66),
          steps: [
            { id: 'st_o7', text: 'Get vendor samples', done: true },
            { id: 'st_o8', text: 'Bench test under hot case', done: false },
          ], comments: [], tags: [] },
        { id: 'op_o_pmreview',title: 'Customer doc review cycle — 2 weeks ok?',
          notes: '', component: null, criticality: 'low', priorityLevel: 'low',
          createdAt: d(-9), steps: [], comments: [], tags: ['tg_o_cust'] },
        { id: 'op_o_pdr_just',title: 'PDR pack: include heritage justification for FSW reuse?',
          notes: 'Could help, but adds 12 pages.', component: 'pt_pm',
          criticality: 'med', priorityLevel: 'med',
          createdAt: d(-7), steps: [], comments: [], tags: ['tg_o_herit'] },
        { id: 'op_o_tvac',    title: 'Test campaign owner — Marie or Diego?',
          notes: 'Marie has TVAC experience but is overloaded. Diego is free but new.',
          component: 'pt_test', criticality: 'med', priorityLevel: 'high',
          createdAt: d(-15), steps: [], comments: [], tags: [] },
        { id: 'op_o_radhrd',  title: 'Should we ESCC-qualify the secondary MCU?',
          notes: 'Cost ~€38k. Used only in non-flight HK loop.',
          component: 'pt_avionics', criticality: 'low', priorityLevel: 'low',
          createdAt: d(-50), steps: [], comments: [], tags: [] },
      ];

      proj2.openPoints = [
        { id: 'op_h_shard',  title: 'Shard telemetry by mission or by time?',
          notes: 'Current write rate ~3k/sec; time-shard is simpler, mission-shard is faster for per-mission queries.',
          component: 'cm_be', criticality: 'high', priorityLevel: 'high',
          createdAt: d(-21),
          steps: [
            { id: 'st_h1', text: 'Benchmark both with realistic load', done: true },
            { id: 'st_h2', text: 'Plan migration path', done: false },
          ], comments: [], tags: ['tg_h_perf'] },
        { id: 'op_h_ie11',   title: 'Browser support: drop IE11?',
          notes: 'Ops insists. Engineering wants it gone.',
          component: 'cm_fe', criticality: 'med', priorityLevel: 'med',
          createdAt: d(-40), steps: [], comments: [], tags: ['tg_h_ux'] },
        { id: 'op_h_qabeta', title: 'Internal beta with QA team — when?',
          notes: 'Window between feature freeze and customer beta is tight.',
          component: 'cm_ops', criticality: 'med', priorityLevel: 'high',
          createdAt: d(-10), steps: [], comments: [], tags: ['tg_h_beta'] },
        { id: 'op_h_obs',    title: 'Datadog vs Grafana for observability?',
          notes: 'Datadog has the budget but Grafana ties into existing infra.',
          component: 'cm_ops', criticality: 'low', priorityLevel: 'med',
          createdAt: d(-58), steps: [], comments: [], tags: [] },
        { id: 'op_h_audit',  title: 'Add audit log for procedure execution?',
          notes: 'Security review wants this before GA.',
          component: 'cm_be', criticality: 'high', priorityLevel: 'critical',
          createdAt: d(-12),
          steps: [
            { id: 'st_h3', text: 'Define audit-event schema', done: false },
          ], comments: [], tags: ['tg_h_security'] },
      ];

      proj3.openPoints = [
        { id: 'op_f_prop',  title: 'Propeller diameter: 13" or 15"?',
          notes: '13" gives better agility, 15" gives endurance.',
          component: 'cm_mech', criticality: 'high', priorityLevel: 'high',
          createdAt: d(-15),
          steps: [
            { id: 'st_f1', text: 'Bench thrust comparison',  done: true },
            { id: 'st_f2', text: 'Flight test sim',          done: false },
          ], comments: [], tags: ['tg_f_flight'] },
        { id: 'op_f_field', title: 'Field tests: indoor or outdoor first?',
          notes: 'Weather risk vs RF environment.',
          component: 'cm_test', criticality: 'med', priorityLevel: 'med',
          createdAt: d(-8), steps: [], comments: [], tags: ['tg_f_ground'] },
        { id: 'op_f_fc',    title: 'Flight controller firmware — fork or upstream?',
          notes: 'Upstream is more sustainable but blocked by 2 PRs.',
          component: 'cm_flight', criticality: 'high', priorityLevel: 'high',
          createdAt: d(-22), steps: [], comments: [], tags: ['tg_f_rnd'] },
        { id: 'op_f_battery', title: 'P2 battery cell type?',
          notes: '21700 NMC vs 18650 LFP — energy vs safety.',
          component: 'cm_avion', criticality: 'critical', priorityLevel: 'critical',
          createdAt: d(-4), steps: [], comments: [], tags: ['tg_f_flight'] },
      ];

      // ── Links + linkFolders ─────────────────────────────────────
      proj1.linkFolders = [
        { id: 'lf_o_cust', name: 'Customer docs' },
        { id: 'lf_o_tech', name: 'Technical references' },
      ];
      proj1.links = [
        { id: 'l_o_sow',  folderId: 'lf_o_cust', title: 'Customer SoW v3.2',
          url: 'https://customer.example/orbit/sow-v32.pdf', description: 'Signed scope.' },
        { id: 'l_o_icd',  folderId: 'lf_o_cust', title: 'Spacecraft–payload ICD',
          url: 'https://customer.example/orbit/icd.pdf', description: 'Mechanical + electrical interfaces.' },
        { id: 'l_o_data', folderId: 'lf_o_tech', title: 'Vendor data sheets folder',
          url: 'https://drive.example/orbit/datasheets', description: 'All component datasheets.' },
        { id: 'l_o_wiki', folderId: null,      title: 'Internal Confluence: Orbit-7',
          url: 'https://wiki.example/orbit', description: 'Engineering wiki space.' },
        { id: 'l_o_mosaic', folderId: 'lf_o_tech', title: 'Mosaic-3 heritage docs',
          url: 'https://archive.example/mosaic-3', description: 'For FSW reuse + thermal heritage.' },
      ];
      proj2.linkFolders = [
        { id: 'lf_h_arch', name: 'Architecture' },
        { id: 'lf_h_apis', name: 'Vendor APIs' },
      ];
      proj2.links = [
        { id: 'l_h_arch', folderId: 'lf_h_arch', title: 'C4 model',
          url: 'https://wiki.example/helios/c4', description: 'Current system architecture.' },
        { id: 'l_h_repo', folderId: null, title: 'GitHub repo',
          url: 'https://github.com/example/helios', description: 'Source + CI.' },
        { id: 'l_h_postgres', folderId: 'lf_h_apis', title: 'Postgres extensions catalogue',
          url: 'https://postgres.example/extensions', description: '' },
      ];
      proj3.linkFolders = [
        { id: 'lf_f_cat', name: 'Vendor catalogues' },
      ];
      proj3.links = [
        { id: 'l_f_tmotor', folderId: 'lf_f_cat', title: 'T-Motor catalogue',
          url: 'https://t-motor.example/catalogue', description: 'Motor specs.' },
        { id: 'l_f_betaflight', folderId: null, title: 'Betaflight docs',
          url: 'https://betaflight.example/docs', description: 'FC firmware reference.' },
      ];

      // ── Sprinkle missing fields on actions ──────────────────────
      // Manual % complete on a handful so the new feature renders.
      // Pick the first 'doing' action of each project + a couple
      // others with realistic values.
      const sprinklePct = (acts, idxValPairs) => {
        idxValPairs.forEach(([i, v]) => {
          const target = acts.filter((a) => a.status === 'doing')[i];
          if (target) target.percentComplete = v;
        });
      };
      sprinklePct(proj1.actions, [[0, 65], [3, 35], [7, 80]]);
      sprinklePct(proj2.actions, [[0, 70], [2, 40]]);
      sprinklePct(proj3.actions, [[0, 50]]);

      // Description on a handful (long-form scope clarification).
      const orbitDoing = proj1.actions.filter((a) => a.status === 'doing');
      if (orbitDoing[0]) orbitDoing[0].description = 'Includes FDIR coverage for SEU + latch-up. Joint Marie/Kira ownership.';
      if (orbitDoing[1]) orbitDoing[1].description = 'Cover mode transitions + sun-pointing safe state.';
      const helDoing = proj2.actions.filter((a) => a.status === 'doing');
      if (helDoing[0]) helDoing[0].description = 'Telemetry throughput target: 5k samples/s at p95 < 50ms.';

      // Action tags — sprinkle a few across each project.
      const tagOne = (acts, tagId, n) => {
        acts.slice(0, n).forEach((a) => { a.tags = a.tags || []; if (!a.tags.includes(tagId)) a.tags.push(tagId); });
      };
      tagOne(proj1.actions.filter((a) => a.component === 'pt_aocs').slice(0, 3),  'tg_o_must', 3);
      tagOne(proj1.actions.filter((a) => a.component === 'pt_pm').slice(0, 2),    'tg_o_cust', 2);
      tagOne(proj1.actions.filter((a) => a.component === 'pt_sw').slice(0, 2),    'tg_o_herit', 2);
      tagOne(proj2.actions.filter((a) => a.component === 'cm_be').slice(0, 2),    'tg_h_perf', 2);
      tagOne(proj2.actions.filter((a) => a.component === 'cm_ops').slice(0, 2),   'tg_h_beta', 2);
      tagOne(proj3.actions.filter((a) => a.component === 'cm_flight').slice(0, 2),'tg_f_flight', 2);

      // Comments on a handful of in-progress actions.
      const addComment = (acts, idx, by, at, text) => {
        const a = acts.filter((x) => x.status === 'doing')[idx];
        if (a) { a.comments = a.comments || []; a.comments.push({ id: uid('cm'), by, at, text }); }
      };
      addComment(proj1.actions, 0, 'p_kira',  d(-3),  'Heritage code is closer to req than first thought — saves ~1 week.');
      addComment(proj1.actions, 0, 'p_sofia', d(-2),  'Good. Update the WBS accordingly.');
      addComment(proj1.actions, 1, 'p_arjun', d(-10), 'Vendor responded with the lead-time update — see email thread.');
      addComment(proj2.actions, 0, 'p_kira',  d(-5),  'Backend benchmark numbers in the wiki page now.');

      // Recurring action — weekly PM status report on Orbit-7.
      proj1.actions.push({
        id: 'a_o_recur', identifier: null, title: 'Weekly PM status report',
        owner: 'p_sofia', originator: 'p_sofia', status: 'doing', priority: 999,
        priorityLevel: 'med', commitment: 25,
        due: d(7), startDate: d(0), originatorDate: d(-7),
        component: 'pt_pm', deliverable: null, milestone: null,
        description: 'Compose + send the weekly Friday update to the customer.',
        notes: '', createdAt: d(-7), updatedAt: d(-1),
        history: [], dependsOn: [], tags: ['tg_o_cust'], comments: [],
        loggedHours: [], baseline: null, percentComplete: null,
        recurrence: { unit: 'week', interval: 1 }, snoozedUntil: null,
        tasks: [
          { id: 'tk_pm_1', title: 'Refresh milestone slide',       status: 'done' },
          { id: 'tk_pm_2', title: 'Snapshot KPIs from Dashboard',  status: 'doing' },
          { id: 'tk_pm_3', title: 'Write narrative summary',       status: 'todo' },
          { id: 'tk_pm_4', title: 'Send draft to Marie for review', status: 'todo' },
        ],
      });
      // External-stakeholder ownership: the customer is on the hook to
      // deliver a formal PDR ICD sign-off. Tracked here so we can chase.
      proj1.actions.push({
        id: 'a_o_extown', identifier: null, title: 'PDR data-pack ICD sign-off',
        owner: 'st_customer', originator: 'p_sofia', status: 'blocked', priority: 998,
        priorityLevel: 'high', commitment: 100,
        due: d(10), startDate: d(-3), originatorDate: d(-14),
        component: 'pt_pm', deliverable: null, milestone: null,
        description: 'Waiting on formal ICD sign-off from CNES programme office.',
        notes: '', createdAt: d(-14), updatedAt: d(-2),
        history: [], dependsOn: [], tags: ['tg_o_cust'], comments: [],
        loggedHours: [], baseline: null, percentComplete: null,
        recurrence: null, snoozedUntil: null,
        tasks: [
          { id: 'tk_ext_1', title: 'Draft cover memo to CNES',                     status: 'done' },
          { id: 'tk_ext_2', title: 'Compile PDR data-pack v1.4',                    status: 'done' },
          { id: 'tk_ext_3', title: 'Send package to Fournier',                      status: 'done' },
          { id: 'tk_ext_4', title: 'Follow-up after 5 working days',                 status: 'blocked' },
          { id: 'tk_ext_5', title: 'File signed ICD in project archive',            status: 'todo' },
        ],
      });
      // External-stakeholder origination: sister-mission SE flagged a
      // heritage-reuse opportunity we should chase internally.
      proj1.actions.push({
        id: 'a_o_extorig', identifier: null, title: 'Evaluate Mosaic-3 FSW heritage transfer',
        owner: 'p_kira', originator: 'st_sister', status: 'todo', priority: 997,
        priorityLevel: 'med', commitment: 40,
        due: d(21), startDate: d(0), originatorDate: d(-2),
        component: 'pt_sw', deliverable: null, milestone: null,
        description: 'Sister-mission SE proposed IP transfer — assess integration cost.',
        notes: '', createdAt: d(-2), updatedAt: d(-1),
        history: [], dependsOn: [], tags: [], comments: [],
        loggedHours: [], baseline: null, percentComplete: null,
        recurrence: null, snoozedUntil: null,
      });

      // Snoozed action — re-emerges in 10 days.
      proj1.actions.push({
        id: 'a_o_snooze', identifier: null, title: 'Update vendor contact spreadsheet',
        owner: 'p_sofia', originator: 'p_sofia', status: 'todo', priority: 1000,
        priorityLevel: 'low', commitment: 25,
        due: d(30), startDate: d(0), originatorDate: d(-14),
        component: 'pt_pm', deliverable: null, milestone: null,
        description: '', notes: 'Low priority — snoozed until after PDR.',
        createdAt: d(-14), updatedAt: d(-2),
        history: [], dependsOn: [], tags: [], comments: [],
        loggedHours: [], baseline: null, percentComplete: null,
        recurrence: null, snoozedUntil: d(10),
      });

      // ── Project-level rich notes ────────────────────────────────
      const notes = {};
      notes[proj1.id] = `
        <h2>Orbit-7 PDR push</h2>
        <ul>
          <li>PDR data pack final draft due ${d(20)}.</li>
          <li>Customer feedback window: 2 weeks from delivery.</li>
          <li>Key risks: <b>r_supply</b> (reaction wheel), <b>r_sw</b> (FSW timeline).</li>
        </ul>
        <h3>Decisions to make this month</h3>
        <ol>
          <li>Star tracker calibration (single vs two-pass)</li>
          <li>Battery cell capacity bump</li>
          <li>Thermal louvre material</li>
        </ol>
        <p><i>Last updated by Sofia, ${d(-1)}.</i></p>
      `;
      notes[proj2.id] = `
        <h2>Helios-2 beta plan</h2>
        <p>Internal beta with Ops on ${d(18)}, customer beta two weeks later.</p>
        <ul>
          <li>Security review approval needed before customer beta.</li>
          <li>Audit logging on backlog — must land before GA.</li>
        </ul>
      `;
      notes[proj3.id] = `
        <h2>Falcon P2 build</h2>
        <p>Open vendor questions: motor (T-Motor MN5008?), prop (13" vs 15"), battery cells.</p>
        <p>Field tests targeted for ${d(60)} — pre-flight checklist needed.</p>
      `;

      // ── Decision comments on Orbit-7's PDR slip decision ────────
      const pdrDecision = proj1.decisions.find((x) => x.id === 'dec_pdr');
      if (pdrDecision) {
        pdrDecision.comments = [
          { id: uid('cm'), by: 'p_sofia', at: d(-12), text: 'Customer email confirming the 2-week slip is OK.' },
          { id: uid('cm'), by: 'p_arjun', at: d(-11), text: 'Updated the risk register accordingly.' },
        ];
      }

      // ── Saved views ─────────────────────────────────────────────
      const savedViews = [
        { id: 'sv_overdue', name: 'My overdue actions',
          filters: { search: '', owner: 'p_sofia', component: '', status: '', due: 'late', view: 'register' } },
        { id: 'sv_this_week', name: 'Due this week',
          filters: { search: '', owner: '', component: '', status: '', due: 'week', view: 'register' } },
        { id: 'sv_blocked',   name: 'All blocked',
          filters: { search: '', owner: '', component: '', status: 'blocked', due: '', view: 'board' } },
        { id: 'sv_aocs',      name: 'AOCS focus (Orbit)',
          filters: { search: '', owner: '', component: 'pt_aocs', status: '', due: '', view: 'register' } },
      ];

      // ── Personal todos ──────────────────────────────────────────
      const todos = [
        { id: 'td1', text: 'Review PDR slides before customer meeting', done: false },
        { id: 'td2', text: 'Send Q3 budget summary to finance',         done: true  },
        { id: 'td3', text: 'Prep board update on Falcon P2 progress',   done: false },
        { id: 'td4', text: 'Schedule Marie ↔ Diego TVAC handover chat', done: false },
      ];

      return { notes, savedViews, todos };
    })();

    return {
      people,
      stakeholders,
      projects: [proj1, proj2, proj3],
      currentProjectId: proj1.id,
      currentView: 'board',
      settings: { theme: 'dark', holidayCountries: [] },
      budgets,
      notes:      extras.notes,
      savedViews: extras.savedViews,
      todos:      extras.todos,
    };
  }

  function emptyState() {
    // Single-source-of-truth project shape — every per-project array the
    // rest of the app reads is initialised here so a fresh workspace and a
    // restored backup behave identically (no first-render-time defaults).
    const proj = {
      id: uid('p'),
      name: 'Untitled project',
      description: '',
      actions: [],
      deliverables: [],
      milestones: [],
      risks: [],
      decisions: [],
      changes: [],
      components: [],
      meetings: [],
      openPoints: [],
      links: [],
      linkFolders: [],
      costCenters: [],
      archive: [],
      tags: [],
      notes: '',
    };
    return {
      people: [],
      projects: [proj],
      currentProjectId: proj.id,
      currentView: 'board',
      settings: { theme: state?.settings?.theme || 'dark', holidayCountries: [] },
      budgets: {},
      notes: {},
      notesOpen: false,
      todos: [],
      inbox: { dismissed: [] },
      templates: [],
    };
  }

  /* ----------------------- selectors / helpers ----------------------- */

  // Returns the active project. If state.currentProjectId === '__all__',
  // returns a synthetic project that aggregates references (not copies) from
  // every project — mutations to actions/risks/etc. propagate to the real
  // source projects. Adding/deleting collection members in this mode is
  // disallowed (curProjectIsMerged() returns true).
  function curProject() {
    if (state.currentProjectId === '__all__') return mergedProject();
    return state.projects.find((p) => p.id === state.currentProjectId) || state.projects[0];
  }
  function curProjectIsMerged() {
    return state.currentProjectId === '__all__';
  }
  // Returns the projects that should be considered "in scope" for cross-cutting
  // engineering views (budgets, charts) — every project in __all__ mode, or just
  // the selected project otherwise.
  function projectsInScope() {
    if (state.currentProjectId === '__all__') {
      // Cross-project views drop archived projects by default — they're
      // archived because they're done. Show-archived setting overrides.
      return state.settings.showArchivedProjects
        ? state.projects
        : state.projects.filter((p) => !p.archived);
    }
    const cur = state.projects.find((p) => p.id === state.currentProjectId);
    return cur ? [cur] : state.projects;
  }
  function mergedProject() {
    const liveProjects = state.settings.showArchivedProjects
      ? state.projects
      : state.projects.filter((p) => !p.archived);
    const flat = (key) => liveProjects.flatMap((p) => p[key] || []);
    return {
      id: '__all__',
      name: 'All projects',
      description: 'Cross-project view (read-only for adds/deletes).',
      actions: flat('actions'),
      deliverables: flat('deliverables'),
      milestones: flat('milestones'),
      risks: flat('risks'),
      decisions: flat('decisions'),
      components: flat('components'),
      meetings: flat('meetings'),
      links: flat('links'),
      linkFolders: flat('linkFolders'),
      openPoints: flat('openPoints'),
      changes: flat('changes'),
      costCenters: flat('costCenters'),
      tags: flat('tags'),
    };
  }
  // Find the source project for an action by id (works in merged mode too)
  function projectOfAction(actionId) {
    return state.projects.find((p) => (p.actions || []).some((a) => a.id === actionId));
  }
  function personName(id) {
    if (!id) return '—';
    return peopleMap().get(id)?.name || '—';
  }
  // Return the `<option>` markup for a select that must pick an actor
  // (owner or originator). Team members first, then a separator, then
  // external stakeholders. The `includeBlank` flag adds a "— same as owner"
  // top row for originator pickers.
  function actorOptionsHTML(selectedId, includeBlank = false) {
    const team = state.people || [];
    const ext  = state.stakeholders || [];
    let html = includeBlank ? '<option value="">— same as owner</option>' : '';
    html += team.map((p) => `<option value="${escapeHTML(p.id)}" ${p.id === selectedId ? 'selected' : ''}>${escapeHTML(p.name)}</option>`).join('');
    if (ext.length) {
      html += `<option value="" disabled>── External stakeholders ──</option>`;
      html += ext.map((s) => `<option value="${escapeHTML(s.id)}" ${s.id === selectedId ? 'selected' : ''}>◇ ${escapeHTML(s.name)}${s.organization ? ' (' + escapeHTML(s.organization) + ')' : ''}</option>`).join('');
    }
    return html;
  }
  function statusOfDue(dueISO, status) {
    if (!dueISO) return 'ok';
    if (status === 'done') return 'ok';
    const diff = dayDiff(dueISO, todayISO());
    if (diff < 0) return 'late';
    if (diff <= 3) return 'soon';
    return 'ok';
  }

  /* ─────────────────── Actuals / Baseline / EVM helpers ───────────────────
   * The Earned-Value engine reads three numbers per action:
   *   - planned hours  (PH)  = (commitment / 100) × HOURS_PER_WEEK × weeks
   *                            of the action's [startDate, due] window
   *   - actual hours   (AH)  = Σ loggedHours[].hours
   *   - % complete     (PC)  = status-derived (todo=0, doing=50,
   *                            blocked=50, done=100, cancelled=0).
   *                            "doing" with logged hours upgrades to
   *                            min(100, AH / PH × 100) when PH is set.
   * From these we derive: PV (planned cost so far), EV (earned cost),
   * AC (actual cost), CPI = EV/AC, SPI = EV/PV, EAC = BAC/CPI,
   * ETC = EAC - AC. Rates come from people.hourlyRate (default 100).
   * All windows are clamped to today for PV (you can only have
   * "planned" what should have been done by now). */

  // Sum of logged hours for one action (cheap, no defaulting).
  function totalLoggedHours(a) {
    if (!a || !Array.isArray(a.loggedHours)) return 0;
    let s = 0;
    for (const h of a.loggedHours) s += (typeof h.hours === 'number' ? h.hours : 0);
    return s;
  }
  // Working-week length in hours; used everywhere we convert
  // commitment % → hours.
  // (HOURS_PER_WEEK is already defined globally — keep that as canonical.)
  function actionWindowWeeks(a) {
    if (!a || !a.due) return 0;
    const due = parseDate(a.due);
    const start = a.startDate ? parseDate(a.startDate) :
      new Date(due.getTime() - 2 * dayMs);
    const days = Math.max(1, Math.round((due - start) / dayMs) + 1);
    return days / 7;
  }
  // Total planned hours over the action's full [startDate, due] window
  // (the BAC, "budget at completion", expressed in hours).
  function plannedHoursTotal(a) {
    if (!a) return 0;
    const cmt = (typeof a.commitment === 'number') ? a.commitment : 100;
    return (cmt / 100) * HOURS_PER_WEEK * actionWindowWeeks(a);
  }
  // Hours that SHOULD have been planned by today — used for PV. Clamps to
  // total once today is past the due date.
  function plannedHoursToDate(a) {
    if (!a || !a.due) return 0;
    const total = plannedHoursTotal(a);
    if (!total) return 0;
    const due = parseDate(a.due);
    const start = a.startDate ? parseDate(a.startDate) :
      new Date(due.getTime() - 2 * dayMs);
    const today = new Date(); today.setHours(0,0,0,0);
    if (today <= start) return 0;
    if (today >= due) return total;
    const fullDays = Math.max(1, (due - start) / dayMs);
    const doneDays = (today - start) / dayMs;
    return total * (doneDays / fullDays);
  }
  // Progress %, with a four-step fallback chain:
  //   1. Status overrides: done → 100, cancelled → 0.
  //   2. Manual override (a.percentComplete is a number 0..100) wins
  //      over everything below.
  //   3. Auto from time elapsed in [originatorDate, due] — clamped to
  //      [0, 100]. This is the new "if nothing is provided" default
  //      and gives a meaningful figure for in-flight actions before
  //      anyone enters actuals.
  //   4. Actuals refinement: if planned + logged hours exist, use
  //      ah / ph (capped at 100).
  //   5. Last-ditch fallback: status-derived 50 for doing/blocked,
  //      0 for todo.
  function percentComplete(a) {
    if (!a) return 0;
    if (a.status === 'done')      return 100;
    if (a.status === 'cancelled') return 0;
    if (typeof a.percentComplete === 'number') {
      return Math.max(0, Math.min(100, Math.round(a.percentComplete)));
    }
    // Tasks-derived — takes precedence over time-based auto because the
    // owner is explicitly telling us where the work stands. Weights:
    // done=1, doing=0.5, blocked=0.25. Cancelled tasks are excluded from
    // both numerator and denominator (they don't exist for planning).
    const tasks = Array.isArray(a.tasks) ? a.tasks.filter((t) => t.status !== 'cancelled') : [];
    if (tasks.length > 0) {
      const weight = tasks.reduce((s, t) => s + (t.status === 'done' ? 1 : t.status === 'doing' ? 0.5 : t.status === 'blocked' ? 0.25 : 0), 0);
      return Math.max(0, Math.min(100, Math.round((weight / tasks.length) * 100)));
    }
    // Auto from time elapsed — only when we have an origin AND a due.
    const origin = a.originatorDate || a.startDate || a.createdAt;
    if (origin && a.due) {
      const oMs = parseDate(origin)?.getTime();
      const dMs = parseDate(a.due)?.getTime();
      const tMs = parseDate(todayISO())?.getTime();
      if (oMs && dMs && tMs && dMs > oMs) {
        if (tMs <= oMs) return 0;
        if (tMs >= dMs) return 100;
        return Math.round(((tMs - oMs) / (dMs - oMs)) * 100);
      }
    }
    // Actuals refinement if hours are tracked.
    const ph = plannedHoursTotal(a);
    const ah = totalLoggedHours(a);
    if (ph > 0 && ah > 0) return Math.max(0, Math.min(100, Math.round((ah / ph) * 100)));
    // Last-ditch status default.
    if (a.status === 'todo') return 0;
    return 50;
  }
  // Hourly rate of an action's owner, defaulting to 100 when unknown so
  // EVM is still meaningful when seed data is sparse. External
  // stakeholders (state.stakeholders) have no team-side rate — their
  // actions contribute PV/EV/AC of zero, keeping CPI/SPI honest.
  function ownerRate(a) {
    if (!a?.owner) return 100;
    if (isExternalActor(a.owner)) return 0;
    const p = peopleMap().get(a.owner);
    return (p && typeof p.hourlyRate === 'number' && p.hourlyRate > 0) ? p.hourlyRate : 100;
  }
  // EVM bundle for a single action OR an iterable of actions. Returns
  // {pv, ev, ac, cpi, spi, eac, etc, bac} all in currency (€). For an
  // iterable, each metric is summed (CPI / SPI then derived from the
  // totals — not averaged — which is the canonical PMBOK behaviour).
  // (Named with the "Actions" suffix because there's a separate
  // budget-centric evmFor(cc, weeks, mode) lower in the file.)
  function evmForActions(actionOrList) {
    const list = Array.isArray(actionOrList) ? actionOrList : [actionOrList];
    let pv = 0, ev = 0, ac = 0, bac = 0;
    for (const a of list) {
      if (!a) continue;
      // Archived actions still contributed budget and effort — count them.
      const rate = ownerRate(a);
      const phTotal  = plannedHoursTotal(a);
      const phToDate = plannedHoursToDate(a);
      const ah = totalLoggedHours(a);
      const pc = percentComplete(a) / 100;
      bac += phTotal  * rate;
      pv  += phToDate * rate;
      ev  += phTotal  * pc * rate;
      ac  += ah       * rate;
    }
    const cpi = ac > 0 ? ev / ac : null;
    const spi = pv > 0 ? ev / pv : null;
    const eac = (cpi && cpi > 0) ? bac / cpi : null;
    const etc = eac != null ? Math.max(0, eac - ac) : null;
    return { pv, ev, ac, bac, cpi, spi, eac, etc };
  }
  // Days behind / ahead of baseline. Positive = late vs baseline,
  // negative = early. Null if no baseline or no due date.
  function baselineSlipDays(a) {
    if (!a?.baseline?.due || !a.due) return null;
    return dayDiff(a.baseline.due, a.due) * -1;
  }
  // Apply a set of topbar filters and optionally navigate to a different view.
  // Pass undefined to leave a filter unchanged; pass '' to clear it.
  function applyTopbarFilter({ status, due, component, owner, search, clearAll, view } = {}) {
    if (clearAll) {
      $('#search').value = '';
      $('#filterStatus').value = '';
      $('#filterDue').value = '';
      $('#filterComponent').value = '';
      $('#filterOwner').value = '';
    } else {
      if (status !== undefined) $('#filterStatus').value = status;
      if (due !== undefined) $('#filterDue').value = due;
      if (component !== undefined) $('#filterComponent').value = component;
      if (owner !== undefined) $('#filterOwner').value = owner;
      if (search !== undefined) $('#search').value = search;
    }
    if (view) {
      state.currentView = view;
      saveState();
      render();
    } else {
      // re-render the current view
      render();
    }
  }

  // Single source of truth for the topbar search query. Trimmed +
  // lowercased so callers can pass mixed-case values without ceremony.
  function searchQuery() {
    return ($('#search')?.value || '').trim().toLowerCase();
  }

  // Does any of the provided field values contain the topbar's search
  // query? Returns true when the search is empty so the helper can be
  // dropped into a .filter() chain unconditionally. Pass arrays / nullish
  // values freely — they're stringified and skipped.
  function matchesSearch(...fields) {
    const q = searchQuery();
    if (!q) return true;
    for (const f of fields) {
      if (f == null) continue;
      const s = Array.isArray(f) ? f.join(' ') : String(f);
      if (s && s.toLowerCase().includes(q)) return true;
    }
    return false;
  }

  // Helper — push an ISO date forward by N units for recurrence cadence.
  function shiftIsoForward(iso, unit, interval) {
    const d = parseDate(iso) || new Date();
    if (unit === 'day')   d.setDate(d.getDate()  + interval);
    if (unit === 'week')  d.setDate(d.getDate()  + interval * 7);
    if (unit === 'month') d.setMonth(d.getMonth() + interval);
    return fmtISO(d);
  }
  function isSnoozed(a) {
    return a.snoozedUntil && a.snoozedUntil > todayISO();
  }
  // When a recurring action is marked done, drop a fresh sibling with
  // its due date pushed forward by recurrence cadence. Returns the new
  // action so callers can stamp / commit it.
  function spawnRecurrenceFromDone(action, project) {
    if (!action || !action.recurrence) return null;
    const r = action.recurrence;
    const baseDue = action.due || todayISO();
    const next = {
      ...action,
      id: uid('a'),
      status: 'todo',
      due: shiftIsoForward(baseDue, r.unit, r.interval || 1),
      createdAt: todayISO(),
      updatedAt: todayISO(),
      originatorDate: todayISO(),
      history: [{ at: todayISO(), what: `Spawned from recurring "${action.title}"` }],
      __history: [],
      __lastEditor: null,
      __lastEditAt: null,
      archivedAt: null,
      snoozedUntil: null,
      // Keep the recurrence on the new instance so the chain continues
      recurrence: { ...r },
    };
    project.actions = project.actions || [];
    project.actions.push(next);
    return next;
  }

  // Session-only drill filter set by clicking a dashboard chart (e.g. an
  // adherence-trend bar). Not persisted — the chip stays until the user
  // dismisses it. actionMatchesFilters honours it on top of the topbar
  // filters, and the register head renders a dismiss chip.
  let _dashDrill = null;
  function _drillMatches(a) {
    if (!_dashDrill) return true;
    if (_dashDrill.kind === 'adherence-week') {
      const { startISO, endISO, side } = _dashDrill;
      const completedAt = actionCompletedAt(a);
      const promised = actionPromisedDue(a);
      const closedHere = completedAt && completedAt >= startISO && completedAt <= endISO;
      if (side === 'on-time') return closedHere && promised && completedAt <= promised;
      if (side === 'slipped') {
        const closedLate = closedHere && promised && completedAt > promised;
        const openLapsed = a.status !== 'done' && promised && promised < todayISO() && promised >= startISO && promised <= endISO;
        return closedLate || openLapsed;
      }
    }
    if (_dashDrill.kind === 'flow-week') {
      const { startISO, endISO, side } = _dashDrill;
      const inWindow = (iso) => iso && iso >= startISO && iso <= endISO;
      if (side === 'created') return inWindow(a.createdAt);
      if (side === 'done') {
        // Mirror flowMetrics: prefer the "Status: → done" history entry,
        // else fall back to updatedAt for actions still marked done.
        const doneEntry = (a.history || []).filter((h) => /Status:.*→\s*done/.test(h.what)).pop();
        const doneAt = doneEntry ? doneEntry.at : (a.status === 'done' ? a.updatedAt : null);
        return inWindow(doneAt);
      }
      if (side === 'slipped') {
        // Any "Schedule: X → Y" entry within the window where Y > X.
        const slipRe = /Schedule:\s*(\d{4}-\d{2}-\d{2})\s*→\s*(?:\d{4}-\d{2}-\d{2}…)?(\d{4}-\d{2}-\d{2})/;
        return (a.history || []).some((h) => {
          if (!inWindow(h.at)) return false;
          const m = h.what.match(slipRe);
          return m && m[2] > m[1];
        });
      }
    }
    return true;
  }
  function actionMatchesFilters(a) {
    if (!_drillMatches(a)) return false;
    // The dashboard drill deliberately exposes archived actions — the
    // whole point is to see what the KPI actually counted. Skip the
    // archived-hide gate when a drill is active.
    if (!_dashDrill && a.archivedAt) return false; // archived items are hidden by default
    if (isSnoozed(a)) return false; // snoozed actions hidden until their date
    const fOwner = $('#filterOwner').value;
    const fComp = $('#filterComponent').value;
    const fStatus = $('#filterStatus').value;
    const fDue = $('#filterDue').value;
    const q = $('#search').value.trim().toLowerCase();
    if (fOwner && a.owner !== fOwner) return false;
    if (fComp) {
      if (fComp === '__none__' && a.component) return false;
      if (fComp !== '__none__' && a.component !== fComp) return false;
    }
    if (fStatus && a.status !== fStatus) return false;
    if (fDue) {
      const today = todayISO();
      const diff = a.due ? dayDiff(a.due, today) : null;
      if (fDue === 'late' && !(a.due && a.status !== 'done' && diff < 0)) return false;
      if (fDue === 'week' && !(a.due && diff !== null && diff >= 0 && diff <= 7)) return false;
      if (fDue === 'month' && !(a.due && diff !== null && diff >= 0 && diff <= 30)) return false;
    }
    if (q) {
      const hay = (a.title + ' ' + personName(a.owner) + ' ' + (a.notes || '')).toLowerCase();
      if (!hay.includes(q)) return false;
    }
    return true;
  }

  /* --------------------------- KPI engine ---------------------------- */

  /* ─────────────── Delivery metrics: adherence + productivity ───────────────
   * Adherence: of every action with a "promised" due date that has passed
   * within the window, what share were marked done on or before that date?
   * Open-past-due actions count against adherence too (they're not on time
   * just because they haven't been declared late yet).
   *
   * Productivity: planned-hours ÷ logged-hours on actions completed inside
   * the window. >1 means the team finished faster than the plan said.
   *
   * "Promised due" prefers the baseline (frozen commitment) when one was
   * captured; otherwise the current due date is used so newly-created
   * actions still get measured. */

  const _doneHistRe = /Status:.*→\s*done/;
  function actionCompletedAt(a) {
    if (!a || a.status !== 'done') return null;
    // Explicit override wins. Then the history-derived date. Then
    // updatedAt as a last-resort fallback (imported data with no
    // history log). Every automatic path still populates a.completedAt
    // on the status-into-done transition, so most reads take the first
    // branch — but the fallbacks keep old data reading correctly.
    if (typeof a.completedAt === 'string' && a.completedAt) return a.completedAt;
    const entry = (a.history || []).filter((h) => _doneHistRe.test(h.what)).pop();
    return entry?.at || a.updatedAt || null;
  }
  // Auto-fill / preserve the completedAt field around a status change.
  // Called from every status write site. Deliberately does NOT clear
  // the field when leaving Done — the reader gates on status anyway,
  // so a user who toggles Done off and back on keeps their overridden
  // completion date.
  function _syncCompletedAtOnStatus(a, oldStatus) {
    if (a.status === 'done' && oldStatus !== 'done' && !a.completedAt) {
      a.completedAt = todayISO();
    }
  }
  // Auto-fill startDate the first time an action leaves Not started.
  // Any state past todo counts (doing / blocked / done / cancelled) so
  // that an action created and immediately marked Done still gets a
  // start date. Only fills when null — never overwrites a user value.
  function _syncStartDateOnStatus(a, oldStatus) {
    if (oldStatus === 'todo' && a.status !== 'todo' && !a.startDate) {
      a.startDate = todayISO();
    }
  }
  function actionPromisedDue(a) {
    return a?.baseline?.due || a?.due || null;
  }
  function _projectsForDeliveryScope() {
    if (state.currentProjectId === '__all__') {
      return state.settings.showArchivedProjects
        ? state.projects
        : state.projects.filter((p) => !p.archived);
    }
    const cur = state.projects.find((p) => p.id === state.currentProjectId);
    return cur ? [cur] : [];
  }
  // Window is inclusive-inclusive on both ISO dates. Pass `null` to omit
  // either side; today acts as a soft upper bound for the "still open past
  // promised" case so future-due open actions don't count as slipped.
  function deliveryMetricsForWindow(sinceISO, untilISO) {
    const todayStr = todayISO();
    const cap = untilISO || todayStr;
    let onTime = 0, slipped = 0;
    let plannedHrs = 0, loggedHrs = 0, doneInWindow = 0;
    _projectsForDeliveryScope().forEach((p) => {
      (p.actions || []).forEach((a) => {
        // Archived done items were real completions — include them in
        // schedule adherence and productivity numbers.
        const completedAt = actionCompletedAt(a);
        const promised = actionPromisedDue(a);
        const closedHere = completedAt && (!sinceISO || completedAt >= sinceISO) && completedAt <= cap;
        if (closedHere) {
          if (promised && completedAt <= promised) onTime++;
          else if (promised) slipped++;
          // Productivity contribution
          const planned = plannedHoursTotal(a);
          const logged = totalLoggedHours(a);
          if (planned > 0 && logged > 0) {
            plannedHrs += planned;
            loggedHrs += logged;
            doneInWindow++;
          }
        } else if (a.status !== 'done' && promised && promised < todayStr && promised <= cap && (!sinceISO || promised >= sinceISO)) {
          // Open action whose promised date has lapsed inside the window
          slipped++;
        }
      });
    });
    const adherenceN = onTime + slipped;
    return {
      adherence: adherenceN ? onTime / adherenceN : null,
      onTime, slipped, adherenceN,
      productivity: loggedHrs > 0 ? plannedHrs / loggedHrs : null,
      plannedHrs, loggedHrs, doneInWindow,
    };
  }
  // 12 trailing ISO-week buckets (Mon-anchored), each carrying the same
  // metric shape as deliveryMetricsForWindow. Used by the dashboard charts.
  function deliveryWeeklyBuckets(weeks = 12) {
    const today = new Date(); today.setHours(0, 0, 0, 0);
    let weekStart = new Date(today);
    while (weekStart.getDay() !== 1) weekStart = new Date(weekStart.getTime() - dayMs);
    const start = new Date(weekStart.getTime() - (weeks - 1) * 7 * dayMs);
    const buckets = Array.from({ length: weeks }, (_, i) => ({
      start: new Date(start.getTime() + i * 7 * dayMs),
      end:   new Date(start.getTime() + (i + 1) * 7 * dayMs - 1),
    })).map((b) => ({
      ...b,
      startISO: fmtISO(b.start),
      endISO:   fmtISO(b.end),
    }));
    return buckets.map((b) => ({ ...b, ...deliveryMetricsForWindow(b.startISO, b.endISO) }));
  }

  function kpis() {
    const proj = curProject();
    // Archived actions are hidden from list views (via actionMatchesFilters)
    // but every KPI counts them — archiving is a visibility change, not a
    // data one. If you truly no longer owe an action, cancel it; archiving
    // is meant for tidying the board.
    const acts = (proj.actions || []);
    const today = todayISO();
    const total = acts.length;
    const done = acts.filter((a) => a.status === 'done').length;
    const blocked = acts.filter((a) => a.status === 'blocked').length;
    const doing = acts.filter((a) => a.status === 'doing').length;
    const late = acts.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, today) < 0).length;
    const upcoming = acts.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, today) >= 0 && dayDiff(a.due, today) <= 7).length;
    const completionRate = total ? Math.round((done / total) * 100) : 0;
    const lateRate = total ? Math.round((late / total) * 100) : 0;
    const blockedRatio = total ? Math.round((blocked / total) * 100) : 0;
    // Throughput: items completed in last 14 days (using updatedAt for done)
    const since = fmtISO(new Date(Date.now() - 14 * dayMs));
    const throughput = acts.filter((a) => a.status === 'done' && a.updatedAt >= since).length;

    // Workload by person — expressed in % of FTE, not action headcount.
    // committed = Σ commitment% across every open action they own.
    // capacity = person.capacity (also % of FTE — 100 = 1 FTE).
    // Externals (stakeholders) don't have a capacity to compare against
    // and are never surfaced here.
    const workload = state.people.map((p) => {
      const owned = acts.filter((a) => a.owner === p.id && !isClosedStatus(a.status));
      const committed = owned.reduce((s, a) => s + (typeof a.commitment === 'number' ? a.commitment : 100), 0);
      return { id: p.id, name: p.name, committed, openCount: owned.length, capacity: p.capacity || 100 };
    });

    return { total, done, doing, blocked, late, upcoming, completionRate, lateRate, blockedRatio, throughput, workload };
  }

  /* ---------------------------- rendering ---------------------------- */

  function render() {
    // A render walks all derived collections fresh — invalidate any stale
    // memoized id-maps so we don't hand back stale lookup results.
    invalidateRenderCache();
    renderTopbar();
    renderSidebar();
    renderView();
    refreshNoteChips();
    refreshTodoFab();
    refreshBell();
  }

  // Capture the currently-applied topbar filter set as a small object —
  // used both for "save current view" and for comparing against existing
  // saved views (so the matching chip is highlighted).
  function captureFilterState() {
    return {
      search:    $('#search')?.value || '',
      owner:     $('#filterOwner')?.value || '',
      component: $('#filterComponent')?.value || '',
      status:    $('#filterStatus')?.value || '',
      due:       $('#filterDue')?.value || '',
      view:      state.currentView,
    };
  }
  function applyFilterState(f) {
    if (!f) return;
    if ($('#search'))          $('#search').value          = f.search    || '';
    if ($('#filterOwner'))     $('#filterOwner').value     = f.owner     || '';
    if ($('#filterComponent')) $('#filterComponent').value = f.component || '';
    if ($('#filterStatus'))    $('#filterStatus').value    = f.status    || '';
    if ($('#filterDue'))       $('#filterDue').value       = f.due       || '';
    if (f.view) state.currentView = f.view;
    saveState();
    render();
  }
  function filterStateMatches(a, b) {
    return ['search','owner','component','status','due','view'].every((k) => (a[k] || '') === (b[k] || ''));
  }

  // Build the saved-views chip row above the main content. Lazy-creates
  // the container element on first call and updates it in place.
  function renderSavedViewsRow() {
    let row = $('#savedViewsRow');
    if (!row) {
      const topbar = $('header.topbar');
      if (!topbar) return;
      row = document.createElement('div');
      row.id = 'savedViewsRow';
      row.className = 'saved-views-row';
      topbar.insertAdjacentElement('afterend', row);
    }
    const cur = captureFilterState();
    const views = state.savedViews || [];
    const empty = views.length === 0;
    const hasActiveFilters =
      cur.search || cur.owner || cur.component || cur.status || cur.due;
    row.innerHTML = `
      <div class="saved-views-inner">
        <span class="saved-views-label">Views:</span>
        ${empty
          ? '<span class="saved-views-hint">— none yet —</span>'
          : views.map((v) => {
              const match = v.filters && filterStateMatches(v.filters, cur);
              return `<button class="saved-view-chip ${match ? 'is-active' : ''}" data-sv-id="${escapeHTML(v.id)}" title="Apply this view (right-click for options)">${escapeHTML(v.name)}</button>`;
            }).join('')}
        <button class="saved-view-add" id="svSave" title="Save the current filters + view as a named preset"${hasActiveFilters ? '' : ' disabled'}>+ Save view</button>
        ${hasActiveFilters ? '<button class="saved-view-clear" id="svClear" title="Clear all filters">Clear</button>' : ''}
      </div>`;
    row.querySelector('#svSave')?.addEventListener('click', () => {
      const name = prompt('Name this view:');
      if (!name) return;
      const trimmed = name.trim().slice(0, 60);
      if (!trimmed) return;
      state.savedViews = state.savedViews || [];
      state.savedViews.push({ id: uid('sv'), name: trimmed, filters: cur });
      commit('view-save');
      toast(`Saved view "${trimmed}"`);
    });
    row.querySelector('#svClear')?.addEventListener('click', () => {
      applyFilterState({ search: '', owner: '', component: '', status: '', due: '', view: state.currentView });
    });
    row.querySelectorAll('.saved-view-chip').forEach((b) => {
      b.addEventListener('click', () => {
        const v = state.savedViews.find((x) => x.id === b.dataset.svId);
        if (v) applyFilterState(v.filters);
      });
      b.addEventListener('contextmenu', (e) => {
        e.preventDefault();
        const v = state.savedViews.find((x) => x.id === b.dataset.svId);
        if (!v) return;
        showContextMenu(e.clientX, e.clientY, [
          { icon: '✎', label: 'Rename…', onClick: () => {
            const next = prompt('New name:', v.name);
            if (next == null) return;
            v.name = next.trim() || v.name;
            commit('view-rename');
          }},
          { icon: '⟳', label: 'Update to current filters', onClick: () => {
            v.filters = captureFilterState();
            commit('view-update');
            toast(`Updated "${v.name}"`);
          }},
          { divider: true },
          { icon: '×', label: 'Delete view', danger: true, onClick: () => {
            state.savedViews = state.savedViews.filter((x) => x.id !== v.id);
            commit('view-delete');
          }},
        ]);
      });
    });
  }

  function renderTopbar() {
    renderSavedViewsRow();
    const sel = $('#projectSelect');
    const showArchived = !!state.settings.showArchivedProjects;
    const live    = state.projects.filter((p) => !p.archived);
    const arch    = state.projects.filter((p) =>  p.archived);
    // Always include the currently-selected project in the picker even if
    // it's archived — otherwise the dropdown can desync from state.
    const visible = showArchived ? state.projects
                                 : [...live, ...arch.filter((p) => p.id === state.currentProjectId)];
    const archCount = arch.length;
    sel.innerHTML =
      `<option value="__all__" ${state.currentProjectId === '__all__' ? 'selected' : ''}>📚 All projects</option>` +
      `<option disabled>──────────</option>` +
      visible
        .map((p) => `<option value="${p.id}" ${p.id === state.currentProjectId ? 'selected' : ''}>${p.archived ? '📦 ' : ''}${escapeHTML(p.name)}</option>`)
        .join('') +
      (archCount && !showArchived
        ? `<option disabled>──────────</option><option value="__show_archived__">📦 Show ${archCount} archived…</option>`
        : (archCount && showArchived
            ? `<option disabled>──────────</option><option value="__hide_archived__">📦 Hide archived</option>`
            : ''));

    const fOwner = $('#filterOwner');
    const cur = fOwner.value;
    fOwner.innerHTML = `<option value="">All owners</option>` + actorOptionsHTML(cur);

    const fComp = $('#filterComponent');
    const curC = fComp.value;
    const proj = curProject();
    fComp.innerHTML = `<option value="">All components</option>` +
      `<option value="__none__" ${curC === '__none__' ? 'selected' : ''}>— Unassigned —</option>` +
      ((proj && proj.components) || []).map((pt) => `<option value="${pt.id}" ${pt.id === curC ? 'selected' : ''}>${escapeHTML(pt.name)}</option>`).join('');
  }

  function renderSidebar() {
    $$('.nav-item').forEach((b) => {
      b.classList.toggle('active', b.dataset.view === state.currentView);
    });
  }

  function renderView() {
    const main = $('#main');
    main.innerHTML = '';
    const view = state.currentView;
    const fns = {
      portfolio: renderPortfolio,
      people: renderPeople,
      board: renderBoard,
      register: renderRegister,
      actioncloud: renderActionCloud,
      openpoints: renderOpenPoints,
      timeline: renderTimeline,
      dashboard: renderDashboard,
      // Charts merged into Dashboard, Reports merged into Review, and
      // Milestones / Deliverables absorbed by the Calendar — route any
      // stale state.currentView values to their replacement panel so
      // saved-state and palette deeplinks keep working.
      charts: renderDashboard,
      review: renderReview,
      archive: renderArchive,
      components: renderComponents,
      budgets: renderBudgets,
      deliverables: renderCalendar,
      milestones: renderCalendar,
      risks: renderRisks,
      decisions: renderDecisions,
      changes: renderChangeRequests,
      links: renderLinks,
      inbox: renderInbox,
      calendar: renderCalendar,
      reports: renderReports,
    };
    (fns[view] || renderBoard)(main);
  }

  /* ----------------------------- Board ------------------------------- */

  function renderBoard(root) {
    const proj = curProject();
    const view = document.createElement('div');
    view.className = 'view board-view';

    const head = document.createElement('div');
    head.className = 'page-head';
    const liveActions = (proj.actions || []).filter((a) => !a.archivedAt);
    const archivedCount = (proj.actions || []).filter((a) => a.archivedAt).length;
    head.innerHTML = `
      <div>
        <div class="page-title">${escapeHTML(proj.name)}</div>
        <div class="page-sub">${liveActions.length} actions • ${proj.deliverables?.length || 0} deliverables • ${proj.milestones?.length || 0} milestones</div>
      </div>
      <div class="page-actions">
        <button class="ghost" id="btnOpenArchive" title="Open the Archive view">⌫ Archive${archivedCount ? ` <span class="badge-count">${archivedCount}</span>` : ''}</button>
        <button class="ghost" id="btnAddAction">+ Action</button>
      </div>`;
    view.appendChild(head);

    // Drop zones for quick mark-done / archive — visible only while a card is dragging
    const zones = document.createElement('div');
    zones.className = 'board-quick-actions';
    zones.innerHTML = `
      <div class="bqa-zone bqa-done"   data-bqa="done"><span class="bqa-icon">✓</span><span>Drop here to mark done</span></div>
      <div class="bqa-zone bqa-delete" data-bqa="delete"><span class="bqa-icon">🗑</span><span>Drop here to archive</span></div>`;
    view.appendChild(zones);

    const board = document.createElement('div');
    board.className = 'board copyable-chart';
    board.dataset.chart = 'kanban-board';
    // Tell the PNG capturer to relax column scroll constraints so the
    // exported image contains every card, not just what's visible.
    registerChartCapture('kanban-board', { expandSelectors: ['.col-body', '.board'] });
    registerChartData('kanban-board', () => {
      const rows = [];
      STATUSES.forEach((s) => {
        (proj.actions || [])
          .filter((a) => a.status === s.id && actionMatchesFilters(a) && !a.archivedAt)
          .forEach((a) => rows.push([
            s.name, a.id, a.title,
            findComponent(proj, a.component)?.name || '',
            personName(a.owner),
            priorityLevel(a.priorityLevel).label,
            a.due || '',
            percentComplete(a),
          ]));
      });
      return {
        headers: ['Column', 'ID', 'Title', 'Component', 'Owner', 'Priority', 'Due', '% Complete'],
        rows,
      };
    });
    STATUSES.forEach((s) => {
      const items = (proj.actions || [])
        .filter((a) => a.status === s.id && actionMatchesFilters(a))
        .sort((a, b) => (a.priority || 0) - (b.priority || 0));
      // Every status column always renders, even when empty — they
      // need to remain valid drop targets for drag-and-drop. (An
      // earlier version hid the Cancelled column when empty but that
      // made it impossible to cancel a card via drag.)
      const col = document.createElement('div');
      col.className = 'column';
      col.dataset.status = s.id;
      const clearBtn = (s.id === 'done' && items.length > 0)
        ? `<button class="col-clear" title="Archive all ${items.length} done action${items.length === 1 ? '' : 's'}">⌫ Clear</button>`
        : '';
      col.innerHTML = `
        <div class="col-head">
          <span class="col-dot ${s.dot}"></span>
          <span class="col-name">${s.name}</span>
          <span class="col-count">${items.length}</span>
          ${clearBtn}
        </div>
        <div class="col-body" data-status="${s.id}"></div>`;
      const body = $('.col-body', col);
      items.forEach((a) => body.appendChild(makeCard(a)));
      if (!items.length) {
        const empty = document.createElement('div');
        empty.className = 'empty';
        empty.textContent = 'Drop actions here';
        body.appendChild(empty);
      }
      board.appendChild(col);
    });
    view.appendChild(board);
    root.appendChild(view);

    $('#btnAddAction').addEventListener('click', () => openQuickAdd('action'));
    $('#btnOpenArchive').addEventListener('click', () => {
      state.currentView = 'archive';
      saveState(); render();
    });

    // Clear-Done bulk action — archives only the done cards CURRENTLY DISPLAYED
    // (passing the topbar filters / search). Filtered-out done items are kept.
    const clearBtn = view.querySelector('.col-clear');
    if (clearBtn) {
      clearBtn.addEventListener('click', () => {
        const targets = curProjectIsMerged()
          ? state.projects.flatMap((p) => p.actions || [])
          : (proj.actions || []);
        const toArchive = targets.filter((a) => a.status === 'done' && actionMatchesFilters(a));
        if (!toArchive.length) return;
        const filtersActive = !!($('#search').value || $('#filterOwner').value || $('#filterComponent').value || $('#filterStatus').value || $('#filterDue').value);
        const scope = filtersActive ? ' (matching current filters)' : '';
        if (!confirm(`Archive ${toArchive.length} done action${toArchive.length === 1 ? '' : 's'}${scope}? They can be restored from the Archive view.`)) return;
        const today = todayISO();
        toArchive.forEach((a) => {
          a.archivedAt = today;
          a.history.push({ at: today, what: 'Archived (bulk-clear from Done)' });
          a.updatedAt = today;
        });
        commit('clear-done');
        toast(`${toArchive.length} archived`);
      });
    }

    // Card drag is handled via custom mouse events in startCardDrag(),
    // including drops onto the .bqa-zone pills and column bodies.
  }

  function makeCard(a) {
    const due = a.due;
    const dueClass = statusOfDue(due, a.status);
    const card = document.createElement('div');
    card.className = `card ${a.status === 'doing' ? 'doing' : ''} ${a.status === 'cancelled' ? 'cancelled' : ''} ${dueClass}`;
    card.dataset.id = a.id;
    const owner = state.people.find((p) => p.id === a.owner);
    const component = findComponent(curProject(), a.component);
    if (component) {
      const c = componentColor(component.color);
      card.classList.add('has-component');
      card.style.setProperty('--cmp-bg',       `rgba(${c.rgb},.10)`);
      card.style.setProperty('--cmp-bg-hover', `rgba(${c.rgb},.18)`);
      card.style.setProperty('--cmp-border',   `rgba(${c.rgb},.40)`);
      card.style.setProperty('--cmp-chip-bg',  `rgba(${c.rgb},.20)`);
      card.style.setProperty('--cmp-chip-fg',  `rgb(${c.rgb})`);
    }
    const lvl = priorityLevel(a.priorityLevel);
    // Only render a chip when priority is non-default (Medium) — keeps cards
    // calm and lets High/Critical visually pop.
    const showPriorityChip = a.priorityLevel && a.priorityLevel !== 'med';
    const tagChips = renderTagChipsHTML(a.tags, curProject(), a.comments);
    card.innerHTML = `
      <button class="row-overflow" data-action="overflow" title="More actions" aria-label="More actions">⋯</button>
      <div class="card-top-row">
        ${component ? `<div class="component-chip">${escapeHTML(component.name)}</div>` : ''}
        ${showPriorityChip ? `<span class="prio-chip prio-${lvl.id}" title="Priority: ${lvl.label}" style="background:rgba(${lvl.rgb},.18);color:rgb(${lvl.rgb});border:1px solid rgb(${lvl.rgb})">${lvl.label}</span>` : ''}
        ${tagChips}
      </div>
      <div class="card-title">${escapeHTML(a.title)}</div>
      <div class="card-meta">
        <span class="avatar" title="${escapeHTML(owner?.name || 'Unassigned')}">${initials(owner?.name)}</span>
        <span class="due ${dueClass}">${due ? fmtDate(due) : 'no date'}</span>
        ${a.notes ? '<span class="tag">note</span>' : ''}
        ${a.description ? '<span class="tag has-desc" title="Has a description — hover to read">≡</span>' : ''}
        ${(a.comments && a.comments.length) ? `<span class="tag has-comments" title="${a.comments.length} comment${a.comments.length === 1 ? '' : 's'}">💬 ${a.comments.length}</span>` : ''}
      </div>
      ${renderTaskStripHTML(a)}`;
    card.addEventListener('click', (e) => {
      if (e.target.closest('[data-action="overflow"]')) {
        e.stopPropagation();
        const r = e.target.getBoundingClientRect();
        showContextMenu(r.left, r.bottom + 4, actionContextItems(a));
        return;
      }
      if (card._suppressClick) { card._suppressClick = false; return; }
      openDrawer(a.id);
    });
    card.addEventListener('mousedown', (e) => {
      if (e.button !== 0) return; // left mouse only
      // Don't initiate drag from the ⋯ overflow button — startCardDrag
      // hides the card via display:none on mousedown, which would tear
      // the button out from under the click event before it fires.
      if (e.target.closest('[data-action="overflow"]')) return;
      startCardDrag(e, card, a);
    });
    card.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      showContextMenu(e.clientX, e.clientY, actionContextItems(a));
    });
    return card;
  }

  // Custom mouse-event drag for Kanban cards. The ghost is created
  // immediately on mousedown so there's no perceived "two-step" feel
  // between clicking and dragging — the card visually lifts at once.
  function startCardDrag(eDown, card, action) {
    const view = card.closest('.board-view');
    const startX = eDown.clientX, startY = eDown.clientY;
    const rect = card.getBoundingClientRect();
    let movedEnough = false;
    let ghost = null;
    let placeholder = null;      // live preview of where the card will land
    let dropTarget = null;
    let lastEvt = null;
    let rafPending = false;
    let prevHighlight = null;
    const CLICK_THRESHOLD = 3;

    eDown.preventDefault?.();

    // Create the ghost immediately so the card "lifts" on mousedown
    ghost = card.cloneNode(true);
    ghost.classList.add('drag-ghost');
    Object.assign(ghost.style, {
      position: 'fixed',
      left: rect.left + 'px',
      top: rect.top + 'px',
      width: rect.width + 'px',
      margin: '0',
      zIndex: '9999',
      willChange: 'transform',
      transform: 'translate3d(0,0,0)',
      opacity: '0.92',
      boxShadow: '0 12px 30px rgba(0,0,0,.45)',
      transition: 'none',
    });
    document.body.appendChild(ghost);
    card.classList.add('dragging');
    // Remove the original card from layout while dragging — the placeholder
    // takes its conceptual place. This lets same-column reorder work and
    // makes layout calculations stable.
    const prevDisplay = card.style.display;
    card.style.display = 'none';
    if (view) view.classList.add('dragging-active');
    document.body.classList.add('is-card-dragging');

    function ensurePlaceholder() {
      if (placeholder) return placeholder;
      placeholder = card.cloneNode(true);
      placeholder.classList.add('drop-placeholder');
      placeholder.classList.remove('dragging');
      placeholder.style.display = '';
      placeholder.style.background = '';
      placeholder.style.borderColor = '';
      delete placeholder.dataset.id; // distinguishable from cards by absence of id
      return placeholder;
    }
    function detachPlaceholder() {
      if (placeholder?.parentElement) placeholder.parentElement.removeChild(placeholder);
    }

    function applyFrame() {
      rafPending = false;
      if (!ghost || !lastEvt) return;
      const em = lastEvt;
      const dx = em.clientX - startX;
      const dy = em.clientY - startY;
      ghost.style.transform = `translate3d(${dx}px, ${dy}px, 0) rotate(2deg)`;
      // Hit-test under the cursor (ghost has pointer-events: none via CSS)
      const under = document.elementFromPoint(em.clientX, em.clientY);
      const zone = under?.closest('.bqa-zone');
      const col  = under?.closest('.col-body');
      const newTarget = zone || col || null;
      if (newTarget !== prevHighlight) {
        if (prevHighlight) prevHighlight.classList.remove('drop-target', 'over');
        prevHighlight = newTarget;
        if (zone) zone.classList.add('over');
        else if (col) col.classList.add('drop-target');
      }
      if (zone) {
        detachPlaceholder();
        dropTarget = { type: 'zone', el: zone, action: zone.dataset.bqa };
      } else if (col) {
        const ph = ensurePlaceholder();
        // Detach placeholder first so its own height doesn't bias the layout
        detachPlaceholder();
        // Visible cards in this column (the original is display:none, so naturally absent)
        const cards = [...col.children].filter((el) => el.classList?.contains('card'));
        const after = cards.find((el) => {
          const r = el.getBoundingClientRect();
          return em.clientY < r.top + r.height / 2;
        });
        if (after) col.insertBefore(ph, after);
        else col.appendChild(ph);
        dropTarget = { type: 'column', el: col, status: col.dataset.status };
      } else {
        detachPlaceholder();
        dropTarget = null;
      }
    }

    function onMove(em) {
      lastEvt = em;
      if (!movedEnough && Math.hypot(em.clientX - startX, em.clientY - startY) >= CLICK_THRESHOLD) {
        movedEnough = true;
      }
      if (!rafPending) {
        rafPending = true;
        requestAnimationFrame(applyFrame);
      }
    }

    function onUp() {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('mouseup', onUp);
      if (rafPending && lastEvt) applyFrame();
      if (movedEnough && !dropTarget && lastEvt) {
        const u = document.elementFromPoint(lastEvt.clientX, lastEvt.clientY);
        const z = u?.closest('.bqa-zone');
        const c = u?.closest('.col-body');
        if (z) dropTarget = { type: 'zone', el: z, action: z.dataset.bqa };
        else if (c) dropTarget = { type: 'column', el: c, status: c.dataset.status };
      }
      // Snapshot placeholder ordinal BEFORE we tear down DOM. The placeholder
      // sits exactly where the user wants the card; everything else is in
      // visible order. (Original card is display:none so naturally excluded.)
      let placeholderOrder = null;
      if (dropTarget?.type === 'column' && placeholder?.parentElement === dropTarget.el) {
        const live = [...dropTarget.el.children].filter((el) => el.classList?.contains('card'));
        placeholderOrder = live.map((el) => el === placeholder ? action.id : el.dataset.id);
      }
      document.querySelectorAll('.col-body.drop-target, .bqa-zone.over').forEach((el) => {
        el.classList.remove('drop-target', 'over');
      });
      detachPlaceholder();
      document.body.classList.remove('is-card-dragging');
      if (view) view.classList.remove('dragging-active');
      card.classList.remove('dragging');
      card.style.display = prevDisplay; // restore (render() will rebuild anyway on commit)
      if (ghost) { ghost.remove(); ghost = null; }
      if (!movedEnough) return;
      card._suppressClick = true;
      if (!dropTarget) return;
      // Hand the precomputed order to the column-drop branch
      dropTarget.order = placeholderOrder;
      const sourceProj = projectOfAction(action.id);
      const a = sourceProj?.actions.find((x) => x.id === action.id);
      if (!a) return;
      if (dropTarget.type === 'zone') {
        if (dropTarget.action === 'done') {
          if (a.status !== 'done') {
            // Not done yet → mark done (stays on the board)
            const _prev = a.status;
            a.history.push({ at: todayISO(), what: `Status: ${a.status} → done` });
            a.status = 'done';
            _syncCompletedAtOnStatus(a, _prev);
            _syncStartDateOnStatus(a, _prev);
            a.updatedAt = todayISO();
            commit('done');
            toast('Marked done');
          } else {
            // Already done → archive it (leaves the board, recoverable from Archive)
            a.archivedAt = todayISO();
            a.history.push({ at: todayISO(), what: 'Archived from Done' });
            a.updatedAt = todayISO();
            commit('archive-done');
            toast('Archived — restore from Archive view');
          }
        } else {
          a.archivedAt = todayISO();
          a.history.push({ at: todayISO(), what: 'Moved to Archive' });
          a.updatedAt = todayISO();
          commit('archive');
          toast('Moved to Archive — restore from Archive view');
        }
        return;
      }
      // type === 'column'
      const oldStatus = a.status;
      const newStatus = dropTarget.status;
      a.status = newStatus;
      _syncCompletedAtOnStatus(a, oldStatus);
      _syncStartDateOnStatus(a, oldStatus);
      a.updatedAt = todayISO();
      if (oldStatus !== newStatus) {
        a.history.push({ at: todayISO(), what: `Status: ${oldStatus} → ${newStatus}` });
      }
      // Use the order captured from the placeholder position; fall back to
      // the cursor-based insertion if the placeholder was never placed.
      let filtered;
      if (dropTarget.order && dropTarget.order.length) {
        filtered = dropTarget.order;
      } else {
        const cards = [...dropTarget.el.querySelectorAll('.card:not(.dragging)')];
        const dropY = lastEvt?.clientY ?? -Infinity;
        const after = cards.find((el) => {
          const r = el.getBoundingClientRect();
          return dropY < r.top + r.height / 2;
        });
        const ids = cards.map((c) => c.dataset.id);
        filtered = ids.filter((id) => id !== a.id);
        const insertIdx = after ? filtered.indexOf(after.dataset.id) : filtered.length;
        filtered.splice(insertIdx === -1 ? filtered.length : insertIdx, 0, a.id);
      }
      filtered.forEach((id, i) => {
        const aa = sourceProj.actions.find((x) => x.id === id);
        if (aa) aa.priority = i;
      });
      commit('move');
      toast(oldStatus === newStatus ? 'Reordered' : `Moved to ${newStatus}`);
    }

    window.addEventListener('mousemove', onMove);
    window.addEventListener('mouseup', onUp);
  }

  // Shared right-click menu for an action — usable on board cards and gantt bars
  function actionContextItems(a) {
    const setStatus = (st) => () => {
      if (a.status === st) return;
      const _prev = a.status;
      a.history.push({ at: todayISO(), what: `Status: ${a.status} → ${st}` });
      const wasNotDone = a.status !== 'done';
      a.status = st;
      _syncCompletedAtOnStatus(a, _prev);
      _syncStartDateOnStatus(a, _prev);
      a.updatedAt = todayISO();
      stampEdit(a, null, `status → ${st}`);
      // Recurring → spawn next instance
      let recurNote = '';
      if (st === 'done' && wasNotDone && a.recurrence) {
        const sourceProj = projectOfAction(a.id) || curProject();
        const nextA = spawnRecurrenceFromDone(a, sourceProj);
        if (nextA) {
          stampEdit(nextA, null, 'spawned by recurrence');
          recurNote = ` · next due ${nextA.due}`;
        }
      }
      commit('status');
      if (recurNote) toast('Done' + recurNote);
    };
    const snoozeBy = (days, label) => () => {
      const d = new Date(); d.setHours(0, 0, 0, 0);
      d.setDate(d.getDate() + days);
      a.snoozedUntil = fmtISO(d);
      a.updatedAt = todayISO();
      stampEdit(a, null, `snoozed ${label}`);
      commit('snooze');
      toast(`Snoozed ${label} → reappears ${a.snoozedUntil}`);
    };
    return [
      { icon: '✎', label: 'Edit details…', onClick: () => openDrawer(a.id) },
      { icon: '✎', label: a.notes ? 'Edit note…' : 'Add note…',
        onClick: () => openNoteEditor(a.id) },
      { icon: '✎', label: a.description ? 'Edit description…' : 'Add description…',
        onClick: () => openDescriptionEditor(a.id) },
      { divider: true },
      { icon: '○', label: 'Mark not started', onClick: setStatus('todo') },
      { icon: '◐', label: 'Mark in progress', onClick: setStatus('doing') },
      { icon: '⨯', label: 'Mark blocked',     onClick: setStatus('blocked') },
      { icon: '✓', label: 'Mark done',        onClick: setStatus('done') },
      { divider: true },
      a.snoozedUntil
        ? { icon: '⏰', label: `Wake (snoozed until ${a.snoozedUntil})`, onClick: () => {
            a.snoozedUntil = null;
            stampEdit(a, null, 'unsnooze');
            commit('unsnooze');
            toast('Unsnoozed');
          }}
        : { icon: '⏰', label: 'Snooze 1 day',  onClick: snoozeBy(1, '1 day') },
      ...(a.snoozedUntil ? [] : [
        { icon: '⏰', label: 'Snooze 1 week',  onClick: snoozeBy(7, '1 week') },
        { icon: '⏰', label: 'Snooze 1 month', onClick: snoozeBy(30, '1 month') },
      ]),
      { divider: true },
      { icon: '×', label: 'Move to Archive', danger: true, onClick: () => {
        if (!confirm(`Move "${a.title}" to Archive?`)) return;
        a.archivedAt = todayISO();
        a.history.push({ at: todayISO(), what: 'Moved to Archive' });
        a.updatedAt = todayISO();
        commit('delete');
        toast('Moved to Archive');
      }},
    ];
  }

  // Quick plain-text note editor — opens a tiny modal, stores in a.notes
  function openNoteEditor(actionId) {
    const a = state.projects.flatMap((p) => p.actions || []).find((x) => x.id === actionId);
    if (!a) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">${escapeHTML(a.title)} — note</div>
          <button class="icon-btn" id="noteClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px;">
          <textarea id="noteText" placeholder="Plain-text note (use the description for rich text)" style="width:100%; min-height:140px; resize:vertical; background:var(--bg-2); border:1px solid var(--line); border-radius:var(--radius-sm); padding:10px; color:var(--text); font: inherit; line-height:1.5; outline:none;">${escapeHTML(a.notes || '')}</textarea>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="noteCancel">Cancel</button>
          <button class="primary" id="noteSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('noteText').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#noteClose').addEventListener('click', close);
    overlay.querySelector('#noteCancel').addEventListener('click', close);
    overlay.querySelector('#noteSave').addEventListener('click', () => {
      a.notes = document.getElementById('noteText').value.trim();
      a.updatedAt = todayISO();
      commit('note');
      close();
      toast('Note saved');
    });
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
  }

  // Rich-text description editor — opens a small modal, stores HTML in a.description
  function openDescriptionEditor(actionId) {
    const a = state.projects.flatMap((p) => p.actions || []).find((x) => x.id === actionId);
    if (!a) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal">
        <div class="desc-head">
          <div class="desc-title">${escapeHTML(a.title)} — description</div>
          <button class="icon-btn" id="descClose" title="Close">×</button>
        </div>
        <div class="notes-toolbar desc-toolbar">
          <button data-cmd="bold" title="Bold"><b>B</b></button>
          <button data-cmd="italic" title="Italic"><i>I</i></button>
          <button data-cmd="underline" title="Underline"><u>U</u></button>
          <span class="sep"></span>
          <button data-cmd="formatBlock" data-arg="<h3>" title="Heading">H</button>
          <button data-cmd="formatBlock" data-arg="<p>" title="Paragraph">¶</button>
          <button data-cmd="insertUnorderedList" title="Bullet list">• list</button>
          <button data-cmd="insertOrderedList" title="Numbered list">1.</button>
        </div>
        <div class="desc-body" contenteditable="true" spellcheck="true">${a.description || '<p></p>'}</div>
        <div class="desc-foot">
          <button class="ghost" id="descCancel">Cancel</button>
          <button class="primary" id="descSave">Save</button>
          ${a.description ? '<button class="ghost desc-clear" id="descClear">Remove description</button>' : ''}
        </div>
      </div>`;
    document.body.appendChild(overlay);
    const body = overlay.querySelector('.desc-body');
    setTimeout(() => body.focus(), 30);
    overlay.querySelectorAll('.desc-toolbar [data-cmd]').forEach((btn) => {
      btn.addEventListener('mousedown', (e) => {
        e.preventDefault();
        document.execCommand(btn.dataset.cmd, false, btn.dataset.arg || null);
        body.focus();
      });
    });
    const close = () => overlay.remove();
    overlay.querySelector('#descClose').addEventListener('click', close);
    overlay.querySelector('#descCancel').addEventListener('click', close);
    overlay.querySelector('#descSave').addEventListener('click', () => {
      const html = body.innerHTML.trim();
      const isEmpty = !html || html === '<p></p>' || html === '<br>';
      a.description = isEmpty ? null : html;
      a.updatedAt = todayISO();
      commit('description');
      close();
      toast(isEmpty ? 'Description cleared' : 'Description saved');
    });
    overlay.querySelector('#descClear')?.addEventListener('click', () => {
      a.description = null;
      a.updatedAt = todayISO();
      commit('description-clear');
      close();
      toast('Description removed');
    });
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
  }

  // Hover tooltip showing rich description
  let hoverDescEl = null;
  let hoverDescTimer = null;
  let hoverDescAnchor = null;
  function ensureHoverDescEl() {
    if (hoverDescEl) return hoverDescEl;
    hoverDescEl = document.createElement('div');
    hoverDescEl.className = 'action-desc-tooltip';
    document.body.appendChild(hoverDescEl);
    return hoverDescEl;
  }
  function showHoverDesc(a, clientX, clientY) {
    const el = ensureHoverDescEl();
    const parts = [];
    if (a.description) parts.push(`<div class="hover-desc">${a.description}</div>`);
    if (a.notes) {
      const safeNote = escapeHTML(a.notes).replace(/\n/g, '<br>');
      parts.push(`<div class="hover-note"><span class="hover-note-lbl">Note</span>${safeNote}</div>`);
    }
    if (!parts.length) return;
    el.innerHTML = parts.join('');
    el.style.display = 'block';
    el.style.left = '0px';
    el.style.top  = '0px';
    const r = el.getBoundingClientRect();
    let x = clientX + 14;
    let y = clientY + 14;
    if (x + r.width  > innerWidth - 8)  x = innerWidth - r.width - 8;
    if (y + r.height > innerHeight - 8) y = clientY - r.height - 14;
    el.style.left = Math.max(8, x) + 'px';
    el.style.top  = Math.max(8, y) + 'px';
  }
  function hideHoverDesc() {
    if (hoverDescEl) hoverDescEl.style.display = 'none';
    clearTimeout(hoverDescTimer);
    hoverDescTimer = null;
    // anchor is managed by mouseover/mouseout callers — don't clobber it here
  }
  function wireHoverDescOnce() {
    if (document._descWired) return;
    document._descWired = true;
    document.addEventListener('mouseover', (e) => {
      const el = e.target.closest('.card[data-id], .reg-row[data-id], .tl-bar[data-id]');
      if (el === hoverDescAnchor) return;
      // Anchor changed (incl. to null) — kill any pending/visible tooltip first
      hideHoverDesc();
      hoverDescAnchor = el;
      if (!el) return;
      const id = el.dataset.id;
      const a = state.projects.flatMap((p) => p.actions || []).find((x) => x.id === id);
      if (!a || (!a.description && !a.notes)) return;
      hoverDescTimer = setTimeout(() => showHoverDesc(a, e.clientX, e.clientY), 350);
    });
    document.addEventListener('mouseout', (e) => {
      if (!hoverDescAnchor) return;
      const next = e.relatedTarget;
      if (next && hoverDescAnchor.contains(next)) return;
      hideHoverDesc();
      hoverDescAnchor = null;
    });
    document.addEventListener('mousemove', (e) => {
      if (!hoverDescEl || hoverDescEl.style.display === 'none') return;
      const r = hoverDescEl.getBoundingClientRect();
      let x = e.clientX + 14;
      let y = e.clientY + 14;
      if (x + r.width  > innerWidth - 8)  x = innerWidth - r.width - 8;
      if (y + r.height > innerHeight - 8) y = e.clientY - r.height - 14;
      hoverDescEl.style.left = Math.max(8, x) + 'px';
      hoverDescEl.style.top  = Math.max(8, y) + 'px';
    });
  }

  function attachColumnDND(body) {
    body.addEventListener('dragover', (e) => {
      if (!e.dataTransfer.types.includes('text/cockpit-action')) return;
      e.preventDefault();
      e.dataTransfer.dropEffect = 'move';
      body.classList.add('drop-target');
      // Find insertion point
      const after = [...body.querySelectorAll('.card:not(.dragging)')].find((el) => {
        const rect = el.getBoundingClientRect();
        return e.clientY < rect.top + rect.height / 2;
      });
      const dragging = document.querySelector('.card.dragging');
      if (!dragging) return;
      if (after) body.insertBefore(dragging, after);
      else body.appendChild(dragging);
    });
    body.addEventListener('dragleave', () => body.classList.remove('drop-target'));
    body.addEventListener('drop', (e) => {
      e.preventDefault();
      body.classList.remove('drop-target');
      const id = e.dataTransfer.getData('text/cockpit-action');
      const newStatus = body.dataset.status;
      const proj = curProject();
      const a = proj.actions.find((x) => x.id === id);
      if (!a) return;
      const oldStatus = a.status;
      a.status = newStatus;
      _syncCompletedAtOnStatus(a, oldStatus);
      _syncStartDateOnStatus(a, oldStatus);
      a.updatedAt = todayISO();
      a.history.push({ at: todayISO(), what: `Status: ${oldStatus} → ${newStatus}` });
      // recompute priority by visual order
      const ordered = [...body.querySelectorAll('.card')].map((c) => c.dataset.id);
      ordered.forEach((cid, i) => {
        const aa = proj.actions.find((x) => x.id === cid);
        if (aa) aa.priority = i;
      });
      commit('move');
      toast(oldStatus === newStatus ? 'Reordered' : `Moved to ${newStatus}`);
    });
  }

  /* --------------------------- Holidays ------------------------------ */
  /* Built-in national holidays for a small set of countries (2026-2027).
     Dates are observed dates where applicable. */
  const HOLIDAYS = {
    US: { name: 'United States', flag: '🇺🇸', dates: {
      '2026-01-01': "New Year's Day",
      '2026-01-19': 'Martin Luther King Jr. Day',
      '2026-02-16': "Presidents' Day",
      '2026-05-25': 'Memorial Day',
      '2026-06-19': 'Juneteenth',
      '2026-07-03': 'Independence Day (observed)',
      '2026-09-07': 'Labor Day',
      '2026-10-12': 'Columbus Day',
      '2026-11-11': 'Veterans Day',
      '2026-11-26': 'Thanksgiving',
      '2026-12-25': 'Christmas Day',
      '2027-01-01': "New Year's Day",
      '2027-01-18': 'Martin Luther King Jr. Day',
      '2027-02-15': "Presidents' Day",
      '2027-05-31': 'Memorial Day',
      '2027-06-18': 'Juneteenth (observed)',
      '2027-07-05': 'Independence Day (observed)',
      '2027-09-06': 'Labor Day',
      '2027-10-11': 'Columbus Day',
      '2027-11-11': 'Veterans Day',
      '2027-11-25': 'Thanksgiving',
      '2027-12-24': 'Christmas Day (observed)',
    }},
    UK: { name: 'United Kingdom', flag: '🇬🇧', dates: {
      '2026-01-01': "New Year's Day",
      '2026-04-03': 'Good Friday',
      '2026-04-06': 'Easter Monday',
      '2026-05-04': 'Early May Bank Holiday',
      '2026-05-25': 'Spring Bank Holiday',
      '2026-08-31': 'Summer Bank Holiday',
      '2026-12-25': 'Christmas Day',
      '2026-12-28': 'Boxing Day (observed)',
      '2027-01-01': "New Year's Day",
      '2027-03-26': 'Good Friday',
      '2027-03-29': 'Easter Monday',
      '2027-05-03': 'Early May Bank Holiday',
      '2027-05-31': 'Spring Bank Holiday',
      '2027-08-30': 'Summer Bank Holiday',
      '2027-12-27': 'Christmas Day (observed)',
      '2027-12-28': 'Boxing Day (observed)',
    }},
    FR: { name: 'France', flag: '🇫🇷', dates: {
      '2026-01-01': "Jour de l'An",
      '2026-04-06': 'Lundi de Pâques',
      '2026-05-01': 'Fête du Travail',
      '2026-05-08': 'Victoire 1945',
      '2026-05-14': 'Ascension',
      '2026-05-25': 'Lundi de Pentecôte',
      '2026-07-14': 'Fête nationale',
      '2026-08-15': 'Assomption',
      '2026-11-01': 'Toussaint',
      '2026-11-11': 'Armistice',
      '2026-12-25': 'Noël',
      '2027-01-01': "Jour de l'An",
      '2027-03-29': 'Lundi de Pâques',
      '2027-05-01': 'Fête du Travail',
      '2027-05-06': 'Ascension',
      '2027-05-08': 'Victoire 1945',
      '2027-05-17': 'Lundi de Pentecôte',
      '2027-07-14': 'Fête nationale',
      '2027-08-15': 'Assomption',
      '2027-11-01': 'Toussaint',
      '2027-11-11': 'Armistice',
      '2027-12-25': 'Noël',
    }},
    DE: { name: 'Germany', flag: '🇩🇪', dates: {
      '2026-01-01': 'Neujahrstag',
      '2026-04-03': 'Karfreitag',
      '2026-04-06': 'Ostermontag',
      '2026-05-01': 'Tag der Arbeit',
      '2026-05-14': 'Christi Himmelfahrt',
      '2026-05-25': 'Pfingstmontag',
      '2026-10-03': 'Tag der Deutschen Einheit',
      '2026-12-25': '1. Weihnachtstag',
      '2026-12-26': '2. Weihnachtstag',
      '2027-01-01': 'Neujahrstag',
      '2027-03-26': 'Karfreitag',
      '2027-03-29': 'Ostermontag',
      '2027-05-01': 'Tag der Arbeit',
      '2027-05-06': 'Christi Himmelfahrt',
      '2027-05-17': 'Pfingstmontag',
      '2027-10-03': 'Tag der Deutschen Einheit',
      '2027-12-25': '1. Weihnachtstag',
      '2027-12-26': '2. Weihnachtstag',
    }},
    JP: { name: 'Japan', flag: '🇯🇵', dates: {
      '2026-01-01': 'New Year',
      '2026-01-12': 'Coming of Age Day',
      '2026-02-11': 'National Foundation Day',
      '2026-02-23': "Emperor's Birthday",
      '2026-03-20': 'Vernal Equinox',
      '2026-04-29': 'Shōwa Day',
      '2026-05-03': 'Constitution Day',
      '2026-05-04': 'Greenery Day',
      '2026-05-05': "Children's Day",
      '2026-07-20': 'Marine Day',
      '2026-08-11': 'Mountain Day',
      '2026-09-21': 'Respect for the Aged Day',
      '2026-09-23': 'Autumnal Equinox',
      '2026-10-12': 'Sports Day',
      '2026-11-03': 'Culture Day',
      '2026-11-23': 'Labour Thanksgiving Day',
      '2027-01-01': 'New Year',
      '2027-01-11': 'Coming of Age Day',
      '2027-02-11': 'National Foundation Day',
      '2027-02-23': "Emperor's Birthday",
      '2027-03-21': 'Vernal Equinox',
      '2027-04-29': 'Shōwa Day',
      '2027-05-03': 'Constitution Day',
      '2027-05-04': 'Greenery Day',
      '2027-05-05': "Children's Day",
      '2027-07-19': 'Marine Day',
      '2027-08-11': 'Mountain Day',
      '2027-09-20': 'Respect for the Aged Day',
      '2027-09-23': 'Autumnal Equinox',
      '2027-10-11': 'Sports Day',
      '2027-11-03': 'Culture Day',
      '2027-11-23': 'Labour Thanksgiving Day',
    }},
    CA: { name: 'Canada', flag: '🇨🇦', dates: {
      '2026-01-01': "New Year's Day",
      '2026-02-16': 'Family Day',
      '2026-04-03': 'Good Friday',
      '2026-05-18': 'Victoria Day',
      '2026-07-01': 'Canada Day',
      '2026-08-03': 'Civic Holiday',
      '2026-09-07': 'Labour Day',
      '2026-10-12': 'Thanksgiving',
      '2026-11-11': 'Remembrance Day',
      '2026-12-25': 'Christmas Day',
      '2026-12-28': 'Boxing Day (observed)',
      '2027-01-01': "New Year's Day",
      '2027-02-15': 'Family Day',
      '2027-03-26': 'Good Friday',
      '2027-05-24': 'Victoria Day',
      '2027-07-01': 'Canada Day',
      '2027-08-02': 'Civic Holiday',
      '2027-09-06': 'Labour Day',
      '2027-10-11': 'Thanksgiving',
      '2027-11-11': 'Remembrance Day',
      '2027-12-27': 'Christmas Day (observed)',
      '2027-12-28': 'Boxing Day (observed)',
    }},
    IN: { name: 'India', flag: '🇮🇳', dates: {
      '2026-01-26': 'Republic Day',
      '2026-03-04': 'Holi',
      '2026-03-21': 'Eid al-Fitr',
      '2026-04-03': 'Good Friday',
      '2026-08-15': 'Independence Day',
      '2026-10-02': 'Gandhi Jayanti',
      '2026-10-20': 'Diwali',
      '2026-12-25': 'Christmas Day',
      '2027-01-26': 'Republic Day',
      '2027-03-22': 'Holi',
      '2027-08-15': 'Independence Day',
      '2027-10-02': 'Gandhi Jayanti',
      '2027-11-08': 'Diwali',
      '2027-12-25': 'Christmas Day',
    }},
    ES: { name: 'Spain', flag: '🇪🇸', dates: {
      '2026-01-01': 'Año Nuevo',
      '2026-01-06': 'Reyes Magos',
      '2026-04-03': 'Viernes Santo',
      '2026-05-01': 'Día del Trabajo',
      '2026-08-15': 'Asunción',
      '2026-10-12': 'Fiesta Nacional',
      '2026-11-01': 'Todos los Santos',
      '2026-12-06': 'Día de la Constitución',
      '2026-12-08': 'Inmaculada Concepción',
      '2026-12-25': 'Navidad',
      '2027-01-01': 'Año Nuevo',
      '2027-01-06': 'Reyes Magos',
      '2027-03-26': 'Viernes Santo',
      '2027-05-01': 'Día del Trabajo',
      '2027-08-15': 'Asunción',
      '2027-10-12': 'Fiesta Nacional',
      '2027-11-01': 'Todos los Santos',
      '2027-12-06': 'Día de la Constitución',
      '2027-12-08': 'Inmaculada Concepción',
      '2027-12-25': 'Navidad',
    }},
  };

  function activeHolidayCodes() {
    return (state.settings && state.settings.holidayCountries) || [];
  }
  function holidayInfo(iso) {
    const codes = activeHolidayCodes();
    if (!codes.length) return null;
    const matches = [];
    if (codes.includes('WKND')) {
      const day = parseDate(iso).getDay();
      if (day === 0 || day === 6) {
        matches.push({ code: 'WKND', country: 'Weekend', flag: '🛌',
          name: day === 0 ? 'Sunday' : 'Saturday', isWeekend: true });
      }
    }
    codes.forEach((c) => {
      if (c === 'WKND') return;
      const data = HOLIDAYS[c];
      if (data && data.dates[iso]) {
        matches.push({ code: c, country: data.name, flag: data.flag, name: data.dates[iso] });
      }
    });
    return matches.length ? matches : null;
  }

  /* ---------------------------- Archive ------------------------------ */

  function renderArchive(root) {
    const view = document.createElement('div');
    view.className = 'view';
    // Always cross-project — show every soft-deleted action
    const items = state.projects.flatMap((p) => (p.actions || [])
      .filter((a) => a.archivedAt)
      .filter((a) => matchesSearch(a.title, personName(a.owner), a.notes, p.name))
      .map((a) => ({ a, proj: p })));
    items.sort((x, y) => (y.a.archivedAt || '').localeCompare(x.a.archivedAt || ''));
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Archive</div>
          <div class="page-sub">${items.length} archived action${items.length === 1 ? '' : 's'} — restore or permanently delete.</div>
        </div>
      </div>
      ${items.length ? `
        <div class="register archive-table">
          <div class="reg-head archive-row">
            <button class="reg-col">Title</button>
            <button class="reg-col">Project</button>
            <button class="reg-col">Owner</button>
            <button class="reg-col">Was</button>
            <button class="reg-col">Archived</button>
            <span class="reg-col-spacer"></span>
            <span class="reg-col-spacer"></span>
          </div>
          <div class="reg-body">
            ${items.map(({ a, proj: pr }) => `
              <div class="reg-row archive-row" data-id="${a.id}" data-pid="${pr.id}">
                <div class="reg-cell title-cell">${escapeHTML(a.title)}</div>
                <div class="reg-cell muted">${escapeHTML(pr.name)}</div>
                <div class="reg-cell"><span class="avatar">${initials(personName(a.owner))}</span> ${escapeHTML(personName(a.owner))}</div>
                <div class="reg-cell muted">${escapeHTML(a.status)}</div>
                <div class="reg-cell muted">${fmtDate(a.archivedAt)}</div>
                <div class="reg-cell">
                  <button class="ghost archive-restore" title="Restore">↺ Restore</button>
                </div>
                <div class="reg-cell">
                  <button class="row-del archive-purge" title="Delete permanently">×</button>
                </div>
              </div>`).join('')}
          </div>
        </div>` : '<div class="empty">No archived items. Move actions to Archive from the board (drag to bin) or the Register × button.</div>'}`;
    root.appendChild(view);

    view.querySelectorAll('.archive-restore').forEach((btn) => {
      btn.addEventListener('click', () => {
        const row = btn.closest('.reg-row');
        const pid = row.dataset.pid;
        const id = row.dataset.id;
        const proj = state.projects.find((p) => p.id === pid);
        const a = (proj?.actions || []).find((x) => x.id === id);
        if (!a) return;
        a.archivedAt = null;
        a.history.push({ at: todayISO(), what: 'Restored from Archive' });
        a.updatedAt = todayISO();
        commit('restore');
        toast('Restored');
      });
    });
    view.querySelectorAll('.archive-purge').forEach((btn) => {
      btn.addEventListener('click', () => {
        const row = btn.closest('.reg-row');
        const pid = row.dataset.pid;
        const id = row.dataset.id;
        if (!confirm('Permanently delete this action? This cannot be undone (except via undo while still in this session).')) return;
        const proj = state.projects.find((p) => p.id === pid);
        if (proj) proj.actions = proj.actions.filter((x) => x.id !== id);
        commit('purge');
        toast('Permanently deleted');
      });
    });
  }

  /* --------------------------- Open Points --------------------------- */

  // Wire interactions on the resolution-steps section of an open-point row.
  // `getOp()` returns the live op object so handlers always read the current
  // version (the row may be re-rendered after each commit).
  function wireOpStepHandlers(itemEl, getOp) {
    const stepsEl = itemEl.querySelector('.op-steps');
    if (!stepsEl) return;
    const listEl = stepsEl.querySelector('.op-steps-list');

    // Toggle done
    stepsEl.querySelectorAll('.op-step-check').forEach((cb) => {
      cb.addEventListener('click', (e) => {
        e.stopPropagation();
        const op = getOp(); if (!op) return;
        const id = cb.closest('.op-step').dataset.stepId;
        const s = (op.steps || []).find((x) => x.id === id);
        if (!s) return;
        s.done = !s.done;
        commit('op-step-toggle');
      });
    });

    // Inline edit — save on blur, Enter saves & blurs, Escape cancels
    stepsEl.querySelectorAll('.op-step-text').forEach((txt) => {
      // Empty + Backspace deletes the row
      txt.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); txt.blur(); }
        else if (e.key === 'Backspace' && txt.textContent === '') {
          e.preventDefault();
          const op = getOp(); if (!op) return;
          const id = txt.closest('.op-step').dataset.stepId;
          op.steps = (op.steps || []).filter((s) => s.id !== id);
          commit('op-step-delete');
        }
      });
      txt.addEventListener('blur', () => {
        const op = getOp(); if (!op) return;
        const id = txt.closest('.op-step').dataset.stepId;
        const s = (op.steps || []).find((x) => x.id === id);
        if (!s) return;
        const v = txt.textContent.trim();
        if (s.text !== v) { s.text = v; commit('op-step-edit'); }
      });
    });

    // Delete
    stepsEl.querySelectorAll('.op-step-del').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const op = getOp(); if (!op) return;
        const id = btn.closest('.op-step').dataset.stepId;
        op.steps = (op.steps || []).filter((s) => s.id !== id);
        commit('op-step-delete');
      });
    });

    // Add
    const addBtn = stepsEl.querySelector('.op-step-add');
    if (addBtn) {
      addBtn.addEventListener('click', () => {
        const op = getOp(); if (!op) return;
        op.steps = op.steps || [];
        const newStep = { id: uid('st'), text: '', done: false };
        op.steps.push(newStep);
        commit('op-step-add');
        // After re-render, focus the new step's text
        setTimeout(() => {
          const newEl = document.querySelector(`.op-step[data-step-id="${newStep.id}"] .op-step-text`);
          if (newEl) {
            newEl.focus();
            // Move caret to end (empty so just focus)
            const range = document.createRange();
            range.selectNodeContents(newEl);
            range.collapse(false);
            const sel = window.getSelection();
            sel.removeAllRanges(); sel.addRange(range);
          }
        }, 0);
      });
    }

    // Drag-to-reorder via the grip — custom mouse events so we can preview
    // the move live and stay consistent with the rest of the app's drags.
    stepsEl.querySelectorAll('.op-step-grip').forEach((grip) => {
      grip.addEventListener('mousedown', (e) => {
        e.preventDefault();
        const row = grip.closest('.op-step');
        if (!row || !listEl) return;
        row.classList.add('dragging');
        document.body.classList.add('is-step-dragging');
        const onMove = (em) => {
          const siblings = [...listEl.querySelectorAll('.op-step:not(.dragging)')];
          const after = siblings.find((sib) => {
            const r = sib.getBoundingClientRect();
            return em.clientY < r.top + r.height / 2;
          });
          if (after) listEl.insertBefore(row, after);
          else listEl.appendChild(row);
        };
        const onUp = () => {
          row.classList.remove('dragging');
          document.body.classList.remove('is-step-dragging');
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          // Commit the new order from current DOM
          const op = getOp(); if (!op) return;
          const newOrderIds = [...listEl.querySelectorAll('.op-step')].map((r) => r.dataset.stepId);
          const before = (op.steps || []).map((s) => s.id).join(',');
          op.steps = newOrderIds.map((id) => (op.steps || []).find((s) => s.id === id)).filter(Boolean);
          if (op.steps.map((s) => s.id).join(',') !== before) commit('op-step-reorder');
        };
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
      });
    });
  }

  // Persistent filter / sort state for the Open Points page (volatile; not
  // in state because it's pure UI).
  const opFilterState = { q: '', component: '', criticality: '', priority: '', progress: '', sort: 'manual', view: 'list', bubbleZoom: 1 };
  // Severity / priority rank used by both Matrix (cell location) and
  // Bubbles (importance score). Higher = worse.
  const _OP_RANK = { low: 1, med: 2, high: 3, critical: 4 };
  const _OP_LEVELS = ['low', 'med', 'high', 'critical'];
  function _opCritRank(op)  { return _OP_RANK[op.criticality   || 'med']; }
  function _opPrioRank(op)  { return _OP_RANK[op.priorityLevel || 'med']; }
  function _opAgeDays(op)   { return Math.max(0, dayDiff(todayISO(), op.createdAt || todayISO())); }
  function _opStepsRatio(op){
    const t = (op.steps || []).length;
    return t === 0 ? 0 : (op.steps || []).filter((s) => s.done).length / t;
  }
  // Composite importance for the Bubbles view: criticality × priority,
  // bumped by age (logarithmic so a 60-day-old item gets bigger but doesn't
  // dwarf everything fresh). Resolution progress gently shrinks the score
  // — points being actively worked on shouldn't visually dominate.
  function _opImportance(op) {
    const c = _opCritRank(op);
    const p = _opPrioRank(op);
    const ageWeight = 1 + 0.5 * Math.log2(1 + _opAgeDays(op) / 7);
    const stepShrink = 1 - 0.4 * _opStepsRatio(op);
    return c * p * ageWeight * Math.max(0.4, stepShrink);
  }
  /* ───────────────────────── Action Cloud ─────────────────────────────
   * A 2D scatter view of live actions designed for rapid triage. The PM's
   * Monday-morning question — "of N actions, which 8-10 should I look at
   * this week" — is a recognition task; neither the Board (flow) nor the
   * Register (table) is good at it. The cloud gives every action a
   * spatial position so the noteworthy ones pop visually.
   *
   * Two axis presets the user toggles between:
   *   A — Urgency × Progress  (days-to-due × % complete). Diagonal acts
   *       as the "on track" benchmark; the bottom-left quadrant is the
   *       firefighting zone.
   *   B — Owner × Importance  (categorical X by owner sorted by load,
   *       Y by composite importance). Reveals per-owner load
   *       concentration.
   *
   * Selection persists across preset changes so the user can verify
   * both presets surface the same critical items.
   *
   * Reuses: percentComplete(), priorityLevel(), STATUSES,
   * actionMatchesFilters(), CSV export, and the openDrawer / topbar
   * filter wiring already in place.
   * ─────────────────────────────────────────────────────────────────── */

  // Focus mode N is computed at render time from the preset's critical
  // zone count (see renderActionCloud), not a hard-coded constant. The
  // floor / ceiling keep it useful at both extremes — projects with no
  // genuine crisis still get a top-3 spotlight, huge projects don't
  // dilute Focus into "highlight everything."
  const CLOUD_FOCUS_FLOOR = 3;
  const CLOUD_FOCUS_CEIL  = 25;
  const _cloudState = {
    preset: 'A',                 // 'A' = urgency × progress, 'B' = owner × importance
    selection: new Set(),        // action ids — persists across preset toggles
    focusMode: false,
    // Viewport zoom — fractional bounds [0..1] of the full data range.
    // Initial = full range (zoomed out). Preset-agnostic: a fraction
    // of 0 to 1 maps to "show everything" in either preset. The canvas
    // never scales, bubbles never resize — only the visible data range
    // changes, and the axis tick labels regenerate to match.
    viewport: { xMinFrac: 0, xMaxFrac: 1, yMinFrac: 0, yMaxFrac: 1 },
    riskLinkedOnly: false,       // cloud-local filter toggled via the legend
    priorityFilter: '',          // cloud-local filter set by clicking a Priority legend chip
    colorBy: 'status',           // 'status' | 'owner' | 'component' | 'priority' | 'project'
  };
  const CLOUD_COLOR_BY_OPTS = [
    { id: 'status',    label: 'Status' },
    { id: 'owner',     label: 'Owner' },
    { id: 'component', label: 'Component' },
    { id: 'priority',  label: 'Priority' },
    { id: 'project',   label: 'Project', mergedOnly: true },
  ];

  // Hash-stable colour for a string id (used for owners + projects so
  // their chip / bubble / ring colours stay consistent across renders).
  //
  // 10-colour categorical palette designed for at-a-glance separation:
  //   - Hues spaced widely on the colour wheel (~36° apart).
  //   - Avoids amber and pure red ranges — those are reserved for the
  //     risk-attached and overdue edge colours, so categorical chips
  //     don't visually compete with the orthogonal edge signals.
  //   - Tuned for readability on the dark theme (medium saturation,
  //     ~55-65% lightness range).
  function cloudHashColor(id) {
    let h = 0; for (let i = 0; i < id.length; i++) h = (h * 31 + id.charCodeAt(i)) | 0;
    // Palette designed so EVERY pair of adjacent slots reads as a
    // different hue family — the hash maps owners/projects to nearby
    // indices in many real datasets (4-person team → indices 3/6/7/8
    // for the seeded ids), so adjacent slots producing close-by hues
    // would still be confusable at a glance.
    //
    // Strategy: alternate between cool, warm, and neutral hue families
    // every other slot, and keep two similar hues at least 3 indices
    // apart. Muted rose dropped — too close to the overdue red used
    // on edges. Slate replaced with a pinker lilac to avoid two
    // dim-grey neighbours.
    const pal = [
      '99,160,255',    // 1. sky blue        (cool)
      '194,99,168',    // 2. magenta         (warm)
      '101,194,73',    // 3. grass green     (cool)
      '129,99,194',    // 4. violet-purple   (cool-warm bridge)
      '79,194,176',    // 5. teal            (cool)
      '180,140,90',    // 6. tan             (warm-neutral)
      '99,194,194',    // 7. cyan            (cool — 3 away from teal so unlikely to collide)
      '156,117,95',    // 8. brown           (warm-neutral)
      '210,150,210',   // 9. lilac           (warm — distinct from purple in slot 4)
      '150,170,80',    // 10. olive          (warm-neutral)
    ];
    return pal[Math.abs(h) % pal.length];
  }

  // Compute fill/stroke + the category key/label for one bubble under
  // the current colour-by setting. The key is used to bucket the legend
  // chip the bubble belongs to; the label is what's shown in the chip.
  // Returns { fill, stroke, key, label }.
  function bubbleColorEncoding(a, proj) {
    const cb = _cloudState.colorBy;
    if (cb === 'status') {
      if (a.status === 'blocked') return { fill: 'rgba(248,113,121,.6)',  stroke: 'rgb(248,113,121)', key: 'blocked', label: 'Blocked' };
      if (a.status === 'doing')   return { fill: 'rgba(110,168,255,.6)', stroke: 'rgb(110,168,255)', key: 'doing',   label: 'Doing'   };
      return                              { fill: 'rgba(148,163,184,.45)', stroke: 'rgb(148,163,184)', key: 'todo',  label: 'To-do' };
    }
    if (cb === 'owner') {
      const oid = a.owner || '__none__';
      if (oid === '__none__') return { fill: 'rgba(148,163,184,.35)', stroke: 'rgb(148,163,184)', key: '__none__', label: '— No owner —' };
      const rgb = cloudHashColor(oid);
      return { fill: `rgba(${rgb},.55)`, stroke: `rgb(${rgb})`, key: oid, label: personName(oid) };
    }
    if (cb === 'component') {
      const cmpId = a.component || '__none__';
      if (cmpId === '__none__') return { fill: 'rgba(148,163,184,.35)', stroke: 'rgb(148,163,184)', key: '__none__', label: '— No component —' };
      const cmp = findComponent(proj, cmpId);
      if (!cmp) return { fill: 'rgba(148,163,184,.45)', stroke: 'rgb(148,163,184)', key: cmpId, label: 'Component' };
      const c = componentColor(cmp.color);
      return { fill: `rgba(${c.rgb},.55)`, stroke: `rgb(${c.rgb})`, key: cmpId, label: cmp.name };
    }
    if (cb === 'priority') {
      const lvl = priorityLevel(a.priorityLevel);
      const rgb = lvl.rgb;
      return { fill: `rgba(${rgb},.55)`, stroke: `rgb(${rgb})`, key: lvl.id, label: lvl.label };
    }
    if (cb === 'project') {
      const rgb = cloudHashColor(proj.id);
      return { fill: `rgba(${rgb},.55)`, stroke: `rgb(${rgb})`, key: proj.id, label: proj.name };
    }
    // Fallback — should never hit.
    return { fill: 'rgba(148,163,184,.45)', stroke: 'rgb(148,163,184)', key: 'unknown', label: '' };
  }

  // Composite importance — drives bubble size (Preset A), Y position
  // (Preset B), and Focus mode's top-N ranking. Five additive signals,
  // each capped, then bonus for risk-attached / critical-path. The
  // scoring is intentionally smooth (no hard thresholds) so the bubble
  // size distribution feels continuous.
  //
  // Signals:
  //   pri  — priority level rank (critical=4 … low=1)
  //   cmt  — commitment %  (5 → 0.2, 100 → 4)
  //   dsn  — log of downstream-dep count (0 if none)
  //   risk — 2 if any risk has actionId === a.id, else 0
  //   stl  — staleness: min(daysSinceUpdate / 14, 1) × 2
  //   crit — 1.5 if the action is on the critical path (computed
  //          lazily and cached per-render via the optional `criticalSet`).
  function actionImportance(a, ctx = {}) {
    // Priority rank — index in PRIORITY_LEVELS + 1 (low=1, med=2, high=3, critical=4).
    const pri = PRIORITY_LEVELS.findIndex((p) => p.id === (a.priorityLevel || 'med')) + 1 || 2;
    const cmt = ((typeof a.commitment === 'number') ? a.commitment : 100) / 25;
    const dsn = ctx.downstream?.get(a.id) || 0;
    const dsW = Math.log2(1 + dsn);                                // 0,1,2…
    const risk = (ctx.riskLinks?.has(a.id)) ? 2 : 0;
    const ageDays = a.updatedAt ? Math.max(0, dayDiff(todayISO(), a.updatedAt)) : 0;
    const stl = Math.min(ageDays / 14, 1) * 2;
    const crit = (ctx.criticalSet?.has(a.id)) ? 1.5 : 0;
    return pri + cmt + dsW + risk + stl + crit;
  }

  // Symmetric log transform: keeps sign, log-scales magnitude.
  // -90 → -6.5 · -7 → -3 · 0 → 0 · +7 → +3 · +90 → +6.5
  // Used so the immediate-urgency region (next 2 weeks) gets visual
  // breathing room while a 6-month-out item still fits on the canvas.
  function symLog(v) {
    if (!v) return 0;
    return Math.sign(v) * Math.log2(1 + Math.abs(v));
  }

  // How many times the due date has been pushed FORWARD over this
  // action's life. Walks the schedule timeline (history entries of the
  // form "Schedule: X → Y") and counts the transitions where the new
  // due lands later than the prior one. Used by Action Cloud Preset C
  // to surface chronic slippers.
  function actionSlipPushBacks(a) {
    if (!a) return 0;
    const hist = scheduleHistory(a);
    if (hist.length < 2) return 0;
    let count = 0;
    for (let i = 1; i < hist.length; i++) {
      if (hist[i].due > hist[i - 1].due) count++;
    }
    return count;
  }

  // Days-to-due — positive = future, negative = overdue, 0 = today.
  // (dayDiff(due, today) computes due − today directly, which already
  // matches this convention. The earlier * −1 inverted the sign and
  // pushed overdue items to the right side of the cloud's X axis,
  // where the labels say future. Removed.)
  function actionDaysToDue(a) {
    if (!a.due) return null;
    return dayDiff(a.due, todayISO());
  }

  // Build per-render context: downstream-dep counts, risk-link set,
  // (no critical-set yet — Gantt has its own; defer to a later cut).
  function buildCloudCtx(scopeProjects) {
    const downstream = new Map();
    const riskLinks  = new Set();
    scopeProjects.forEach((p) => {
      (p.actions || []).forEach((a) => {
        (a.dependsOn || []).forEach((depId) => {
          downstream.set(depId, (downstream.get(depId) || 0) + 1);
        });
      });
      (p.risks || []).forEach((r) => {
        if (r.actionId) riskLinks.add(r.actionId);
      });
    });
    return { downstream, riskLinks };
  }

  function renderActionCloud(root) {
    const scopeProjects = curProjectIsMerged()
      ? state.projects.filter((p) => !p.archived)
      : [curProject()];
    const ctx = buildCloudCtx(scopeProjects);

    // Source actions — every live (non-archived, non-done, non-cancelled)
    // action across the scope projects, tagged with their owning project.
    // Topbar filters (search / owner / component / status / due) are
    // applied via actionMatchesFilters. The "done / cancelled" exclusion
    // is hard-coded here (not optional via the topbar) because the
    // cloud's purpose is triage — closed items shouldn't take up
    // canvas real estate.
    const allActions = scopeProjects.flatMap((p) => (p.actions || [])
      .filter((a) => !a.archivedAt && a.status !== 'done' && a.status !== 'cancelled' && actionMatchesFilters(a))
      .filter((a) => !_cloudState.riskLinkedOnly || ctx.riskLinks.has(a.id))
      .filter((a) => !_cloudState.priorityFilter || (a.priorityLevel || 'med') === _cloudState.priorityFilter)
      .map((a) => ({ a, proj: p })));

    registerChartData('action-cloud', () => ({
      headers: ['Project', 'Identifier', 'Title', 'Owner', 'Status', 'Priority', 'Component', 'Due', 'Days to due', '% Complete', 'Composite importance', 'Selected'],
      rows: allActions.map(({ a, proj }) => [
        proj.name, a.identifier || '', a.title,
        personName(a.owner), a.status, priorityLevel(a.priorityLevel).label,
        findComponent(proj, a.component)?.name || '',
        a.due || '', actionDaysToDue(a) ?? '',
        percentComplete(a), Number(actionImportance(a, ctx).toFixed(2)),
        _cloudState.selection.has(a.id) ? 'yes' : '',
      ]),
    }));

    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">${escapeHTML(curProjectIsMerged() ? 'All projects' : curProject().name)} — Action cloud</div>
          <div class="page-sub">2D scatter for rapid triage — click bubbles to select · drag to pan · shift+drag to zoom to a box · Ctrl/⌘ + wheel to zoom at cursor. Done &amp; cancelled actions are excluded.</div>
        </div>
        <div class="page-actions">
          <div class="seg" id="cloudPresetSeg" role="tablist" aria-label="Axis preset">
            <button class="seg-btn ${_cloudState.preset === 'A' ? 'active' : ''}" data-cloud-preset="A" title="X = days-to-due · Y = % complete · bubble size = composite importance">A · Urgency × Progress</button>
            <button class="seg-btn ${_cloudState.preset === 'B' ? 'active' : ''}" data-cloud-preset="B" title="X = owner (sorted by load) · Y = composite importance · bubble size = commitment %">B · Owner × Importance</button>
            <button class="seg-btn ${_cloudState.preset === 'C' ? 'active' : ''}" data-cloud-preset="C" title="X = number of times the due date has been pushed back · Y = days overdue now · bubble size = composite importance. Upper-right quadrant = chronic slippers that are also late.">C · Chronic slippers</button>
          </div>
          <button class="ghost ${_cloudState.focusMode ? 'is-on' : ''}" id="cloudFocus" title="Highlight the N most noteworthy bubbles for the current preset. N adapts to context: it counts items inside the preset's critical zone (inner ring in A · critical band in B), clamped to [${CLOUD_FOCUS_FLOOR}, ${CLOUD_FOCUS_CEIL}].">★ Focus top <span id="cloudFocusN">…</span></button>
          <div class="seg cloud-zoom-seg" role="group" aria-label="Zoom">
            <button type="button" class="ghost" id="cloudZoomOut"   title="Zoom out (Ctrl/⌘ + wheel inside the canvas)">−</button>
            <span class="cloud-zoom-val" id="cloudZoomVal">${Math.round(_cloudState.zoom * 100)}%</span>
            <button type="button" class="ghost" id="cloudZoomIn"    title="Zoom in (Ctrl/⌘ + wheel inside the canvas)">+</button>
            <button type="button" class="ghost" id="cloudZoomFit"   title="Fit canvas to width (same effect as 100% in normal layouts)">⤢ Fit</button>
          </div>
        </div>
      </div>
      <div class="cloud-filterbar">
        <div class="cloud-sel-strip" id="cloudSelStrip" hidden></div>
      </div>
      <div class="cloud-canvas-wrap">
        <div class="cloud-canvas-scroll" id="cloudScroll">
          <svg id="cloudSvg" class="cloud-svg copyable-chart" data-chart="action-cloud" preserveAspectRatio="xMidYMid meet"></svg>
        </div>
        <div class="cloud-legend" id="cloudLegend">
          <!-- Legend items are clickable filter toggles. Status items
               and Overdue route through the topbar filter pipeline so
               they stack with other filters; Risk attached is a cloud-
               local toggle since the topbar has no equivalent. Project
               (only in __all__ mode) is informational, not clickable. -->
        </div>
      </div>`;
    root.appendChild(view);

    // Component filtering lives on the topbar "All components"
    // dropdown — it covers single-select, "__none__", and clear-all
    // in one control and applies across every panel. The cloud panel
    // intentionally does not duplicate it.

    // ── Selection strip — shown only when at least one bubble selected
    function refreshSelStrip() {
      const strip = $('#cloudSelStrip');
      if (!strip) return;
      const n = _cloudState.selection.size;
      if (!n) { strip.hidden = true; strip.innerHTML = ''; return; }
      strip.hidden = false;
      strip.innerHTML = `
        <span class="cl-sel-count"><b>${n}</b> selected</span>
        <span class="cl-sel-hint">Selection persists across presets — toggle <b>A ↔ B</b> to verify the same items pop in both lenses.</span>
        <button class="ghost" id="cloudSelOpen" title="Open the first selected action in the drawer">Open first</button>
        <button class="ghost" id="cloudSelClear" title="Clear selection">× Clear</button>`;
      $('#cloudSelClear').addEventListener('click', () => {
        _cloudState.selection.clear();
        refreshSelStrip();
        repaintSelectionOnly();
      });
      $('#cloudSelOpen').addEventListener('click', () => {
        const first = Array.from(_cloudState.selection)[0];
        if (first) openDrawer(first);
      });
    }
    refreshSelStrip();

    // ── Dynamic legend — categorical chips + always-on decorations + colour-by control ──
    //
    // The legend has three groups in a single horizontal row:
    //   1. CATEGORICAL chips — auto-derived from the visible actions
    //      based on _cloudState.colorBy. Bubble fill colours come from
    //      the same encoding so the chip colour ↔ bubble colour match
    //      tautologically. Sorted by count desc, top 10 shown, rest
    //      collapsed into "+N more" that expands on click.
    //   2. DECORATION chips — always present, orthogonal signals that
    //      don't participate in the colour encoding: Risk attached,
    //      Overdue, Project ring (in __all__ mode).
    //   3. Stat strip + COLOR-BY dropdown — far right.
    //
    // Active state pulls from live filter values so the legend always
    // reflects current truth. Click each chip → filter (status / owner
    // / component → topbar; priority → cloud-local; project → switch
    // active project; risk → cloud-local; overdue → topbar due=late).
    function renderLegend() {
      const el = $('#cloudLegend'); if (!el) return;
      const fStatus = $('#filterStatus')?.value || '';
      const fOwner  = $('#filterOwner')?.value || '';
      const fComp   = $('#filterComponent')?.value || '';
      const fDue    = $('#filterDue')?.value || '';

      // ── Categorical chips ──────────────────────────────────────────
      // Bucket the visible actions by colour-by key. The bubble's own
      // encoding is the source of truth — same function, same colours.
      const buckets = new Map();
      allActions.forEach(({ a, proj: p }) => {
        const enc = bubbleColorEncoding(a, p);
        if (!buckets.has(enc.key)) buckets.set(enc.key, { ...enc, count: 0 });
        buckets.get(enc.key).count++;
      });
      const entries = Array.from(buckets.values()).sort((a, b) => b.count - a.count);
      const EXPANDED = el.dataset.expanded === '1';
      const SHOW_N = EXPANDED ? entries.length : 10;
      const visible = entries.slice(0, SHOW_N);
      const hiddenCount = entries.length - visible.length;
      const cb = _cloudState.colorBy;

      // Compute active state per chip based on the current filter:
      const isCategoricalActive = (key) => {
        if (cb === 'status')    return fStatus === key;
        if (cb === 'owner')     return (fOwner || '') === key;
        if (cb === 'component') return (fComp  || '') === key;
        if (cb === 'priority')  return _cloudState.priorityFilter === key;
        if (cb === 'project')   return state.currentProjectId === key && !curProjectIsMerged();
        return false;
      };

      const categoricalChips = visible.map((e) => {
        const active = isCategoricalActive(e.key);
        return `<button type="button" class="cl-leg-item cl-leg-cat${active ? ' is-on' : ''}" data-leg-cat="${escapeHTML(e.key)}" title="${escapeHTML(e.label)} — ${e.count} action${e.count === 1 ? '' : 's'}${active ? ' · click to clear' : ' · click to filter'}">
          <span class="cl-leg-swatch" style="background:${e.fill};border-color:${e.stroke};"></span>
          <span class="cl-leg-cat-lbl">${escapeHTML(e.label)}</span>
          <span class="cl-leg-cat-n">${e.count}</span>
        </button>`;
      }).join('');
      const moreChip = hiddenCount > 0
        ? `<button type="button" class="cl-leg-item cl-leg-more" data-leg-more="1" title="Show ${hiddenCount} more">+${hiddenCount} more</button>`
        : (EXPANDED && entries.length > 10
            ? `<button type="button" class="cl-leg-item cl-leg-more" data-leg-more="0" title="Collapse">collapse</button>`
            : '');

      // ── Decoration chips (always present) ──────────────────────────
      const decoChips = [
        { kind: 'risk',    cls: 'cl-leg-ring cl-leg-risk', label: 'Risk attached', active: _cloudState.riskLinkedOnly },
        { kind: 'overdue', val: 'late', cls: 'cl-leg-ring cl-leg-late', label: 'Overdue',       active: fDue === 'late' },
      ];
      if (curProjectIsMerged()) {
        decoChips.push({ kind: 'info', cls: 'cl-leg-ring cl-leg-proj', label: 'Project ring', active: false });
      }
      const decoHTML = decoChips.map((it) =>
        `<button type="button" class="cl-leg-item${it.active ? ' is-on' : ''}${it.kind === 'info' ? ' cl-leg-info' : ''}" data-leg-kind="${it.kind}" ${it.val ? `data-leg-val="${escapeHTML(it.val)}"` : ''} title="${it.kind === 'info' ? 'Each bubble carries an outer colour ring matching its project — informational only.' : (it.active ? 'Click to clear this filter' : 'Click to filter')}">
          <span class="${it.cls}"></span>${escapeHTML(it.label)}
        </button>`).join('');

      // ── Color-by dropdown ──────────────────────────────────────────
      const cbOpts = CLOUD_COLOR_BY_OPTS
        .filter((o) => !o.mergedOnly || curProjectIsMerged())
        .map((o) => `<option value="${o.id}" ${o.id === cb ? 'selected' : ''}>${escapeHTML(o.label)}</option>`).join('');
      const cbControl = `
        <span class="cl-leg-cby-lbl">Color by</span>
        <select class="cl-leg-cby-sel" id="cloudColorBy" title="Choose which attribute drives bubble colour and the categorical chips on the left">${cbOpts}</select>`;

      el.innerHTML = `
        <span class="cl-leg-group cl-leg-group-cat">${categoricalChips}${moreChip}</span>
        <span class="cl-leg-divider" aria-hidden="true"></span>
        <span class="cl-leg-group">${decoHTML}</span>
        <span class="cl-leg-spacer"></span>
        <span class="cl-leg-stat" id="cloudStat"></span>
        <span class="cl-leg-cby">${cbControl}</span>`;

      // Categorical chip clicks → filter axis (depends on colorBy).
      $$('.cl-leg-cat', el).forEach((btn) => {
        btn.addEventListener('click', () => {
          const key = btn.dataset.legCat;
          const wasActive = btn.classList.contains('is-on');
          if (cb === 'status')    applyTopbarFilter({ status:    wasActive ? '' : key });
          else if (cb === 'owner')     applyTopbarFilter({ owner:     wasActive ? '' : (key === '__none__' ? '' : key) });
          else if (cb === 'component') applyTopbarFilter({ component: wasActive ? '' : (key === '__none__' ? '__none__' : key) });
          else if (cb === 'priority')  { _cloudState.priorityFilter = wasActive ? '' : key; render(); }
          else if (cb === 'project')   { state.currentProjectId = key; saveState(); render(); }
        });
      });
      // "+N more" expand / collapse — local state on the element only.
      $$('.cl-leg-more', el).forEach((btn) => {
        btn.addEventListener('click', () => {
          el.dataset.expanded = btn.dataset.legMore === '1' ? '1' : '';
          renderLegend();
        });
      });
      // Decoration chip clicks (unchanged behaviour from before).
      $$('.cl-leg-item:not(.cl-leg-cat):not(.cl-leg-more):not(.cl-leg-info)', el).forEach((btn) => {
        btn.addEventListener('click', () => {
          const kind = btn.dataset.legKind;
          const val  = btn.dataset.legVal;
          const wasActive = btn.classList.contains('is-on');
          if (kind === 'overdue') applyTopbarFilter({ due: wasActive ? '' : val });
          else if (kind === 'risk') { _cloudState.riskLinkedOnly = !wasActive; render(); }
        });
      });
      // Color-by select change — re-render the whole cloud so bubble
      // colours regenerate alongside the legend.
      $('#cloudColorBy')?.addEventListener('change', (e) => {
        _cloudState.colorBy = e.target.value;
        // Clear priority filter when leaving priority mode — its only
        // entry-point is a Priority chip click.
        if (_cloudState.colorBy !== 'priority') _cloudState.priorityFilter = '';
        render();
      });
    }
    renderLegend();

    // ── Layout ─────────────────────────────────────────────────────────
    // Viewport zoom: canvas dimensions are fixed at the panel size.
    // Zoom changes the VISIBLE DATA RANGE, not the canvas. Bubble
    // sizes never change; axis pixel positions never move; only the
    // axis tick labels regenerate when the viewport changes.
    const SVG = $('#cloudSvg');
    const scroller = $('#cloudScroll');
    const W = Math.max(720, scroller.clientWidth || 1200);
    const H = Math.max(420, Math.min(window.innerHeight - 280, 720));
    const padL = 48, padR = 24, padT = 14, padB = 36;
    const innerW = W - padL - padR;
    const innerH = H - padT - padB;
    SVG.setAttribute('viewBox', `0 0 ${W} ${H}`);
    SVG.setAttribute('width', W);
    SVG.setAttribute('height', H);

    // Current viewport (fractional bounds 0..1 of the preset's full
    // data range). Clamped here in case stale state escapes.
    const vp = _cloudState.viewport;
    vp.xMinFrac = Math.max(0, Math.min(1, vp.xMinFrac));
    vp.xMaxFrac = Math.max(vp.xMinFrac + 0.01, Math.min(1, vp.xMaxFrac));
    vp.yMinFrac = Math.max(0, Math.min(1, vp.yMinFrac));
    vp.yMaxFrac = Math.max(vp.yMinFrac + 0.01, Math.min(1, vp.yMaxFrac));

    // Bubble radius scale — sqrt of composite importance, anchored
    // between 5 and 18 px. Identical across presets so an action's
    // visual weight stays consistent when the user toggles presets.
    const compositeScores = allActions.map(({ a }) => actionImportance(a, ctx));
    const maxComp = Math.max(4, ...compositeScores);
    const radiusForComp = (c) => 5 + Math.min(13, Math.sqrt(c) * 3);

    // Bubble colour encoding now driven by _cloudState.colorBy via
    // bubbleColorEncoding(a, proj). The legacy statusPalette is gone;
    // the encoding returns fill + stroke AND the categorical key/label
    // that the legend chips group by.

    // Project palette — used in __all__ mode to draw a thin outer ring
    // around each bubble carrying its project's signature colour.
    function projectRing(p) {
      // Hash the project id to a fixed colour from the palette so
      // ring colour is stable across renders.
      let h = 0; for (let i = 0; i < p.id.length; i++) h = (h * 31 + p.id.charCodeAt(i)) | 0;
      const pal = ['rgb(245,193,80)','rgb(110,168,255)','rgb(168,140,255)','rgb(72,206,200)','rgb(244,131,89)','rgb(74,222,128)'];
      return pal[Math.abs(h) % pal.length];
    }

    // ── Position calculation per preset ───────────────────────────────
    // Preset A: X = sym-log days-to-due, Y = % complete (inverted: 100% top).
    //           Items without a due date sit in a dedicated "no-date" lane
    //           at the far right.
    // Preset B: X = owner slot (sorted by total commitment load desc).
    //           Y = composite importance (sym-log so the spread is
    //           readable when one action dwarfs the rest).
    // Only include owners who currently have at least one visible
    // action in the cloud's allActions set. Showing every person in the
    // roster wastes lane real estate on owners with nothing to display
    // — especially when filters narrow the set or in __all__ mode where
    // not every person has work in every project.
    const ownersWithActions = new Set(allActions.map(({ a }) => a.owner).filter(Boolean));
    const owners = state.people.filter((p) => ownersWithActions.has(p.id));
    owners.sort((a, b) => {
      const la = (a._load = scopeProjects.reduce((s, p) =>
        s + (p.actions || []).filter((x) => x.owner === a.id && !x.archivedAt && x.status !== 'done' && x.status !== 'cancelled').length, 0));
      const lb = (b._load = scopeProjects.reduce((s, p) =>
        s + (p.actions || []).filter((x) => x.owner === b.id && !x.archivedAt && x.status !== 'done' && x.status !== 'cancelled').length, 0));
      return lb - la;
    });
    const ownerIdx = new Map(owners.map((p, i) => [p.id, i]));

    // Full data range per preset — viewport fractions [0..1] map into
    // these ranges. Stored so axis-tick rendering can use them too.
    let fullXMin, fullXMax, fullYMin, fullYMax;
    // Preset C numbers — slip count (linear, 0..maxObserved+1, floor 3
    // so a fresh project still has a sensible canvas) and days overdue
    // (sym-log, 0..symLog(180) so a 6-month overdue still fits).
    let cMaxSlips = 3;
    if (_cloudState.preset === 'C') {
      cMaxSlips = Math.max(3, ...allActions.map(({ a }) => actionSlipPushBacks(a)));
    }
    const cYTop = symLog(180);  // top of Y range — 180 days overdue
    if (_cloudState.preset === 'A') {
      fullXMin = -6.6; fullXMax = 7.5;       // sym-log days, +7.5 is the no-date lane
      fullYMin = 0;    fullYMax = 100;       // % complete
    } else if (_cloudState.preset === 'C') {
      fullXMin = 0;    fullXMax = cMaxSlips + 1; // slip count + pad
      fullYMin = 0;    fullYMax = cYTop;         // sym-log days overdue
    } else {
      fullXMin = 0;    fullXMax = Math.max(1, owners.length); // owner slot indices + unassigned
      fullYMin = 0;    fullYMax = Math.log2(1 + maxComp);     // log composite importance
    }
    // Visible viewport in DATA coords (computed from fractions).
    const xMinV = fullXMin + vp.xMinFrac * (fullXMax - fullXMin);
    const xMaxV = fullXMin + vp.xMaxFrac * (fullXMax - fullXMin);
    const yMinV = fullYMin + vp.yMinFrac * (fullYMax - fullYMin);
    const yMaxV = fullYMin + vp.yMaxFrac * (fullYMax - fullYMin);
    // Map data → pixel through the current viewport.
    const dataToPx = (xRaw, yRaw) => ({
      x: padL + ((xRaw - xMinV) / (xMaxV - xMinV)) * innerW,
      y: padT + (1 - (yRaw - yMinV) / (yMaxV - yMinV)) * innerH,
    });
    const placed = allActions.map(({ a, proj: p }, i) => {
      const r = radiusForComp(compositeScores[i]);
      const palette = bubbleColorEncoding(a, p);
      const dtd = actionDaysToDue(a);
      const pct = percentComplete(a);
      let xRaw, yRaw;
      if (_cloudState.preset === 'A') {
        if (dtd == null) {
          xRaw = 7.5; // no-date lane (right of +90 sym-log)
        } else {
          xRaw = Math.max(-6.6, Math.min(6.6, symLog(dtd)));
        }
        yRaw = pct;
      } else if (_cloudState.preset === 'C') {
        // Slip count (linear) × days overdue (sym-log, 0 floor — actions
        // due in the future sit on the baseline, the closer to upper-
        // right the worse the action's track record).
        xRaw = actionSlipPushBacks(a);
        const overdueDays = Math.max(0, dtd == null ? 0 : -dtd);
        yRaw = Math.min(cYTop, symLog(overdueDays));
      } else {
        xRaw = ownerIdx.has(a.owner) ? ownerIdx.get(a.owner) : owners.length;
        yRaw = Math.log2(1 + compositeScores[i]);
      }
      const px = dataToPx(xRaw, yRaw);
      // Mark whether this bubble's data position is INSIDE the current
      // viewport. Out-of-viewport bubbles still render but get pinned
      // to a chart edge with reduced opacity — visual hint that the
      // user has zoomed past them.
      const inView = xRaw >= xMinV && xRaw <= xMaxV && yRaw >= yMinV && yRaw <= yMaxV;
      return {
        id: a.id, a, proj: p,
        xRaw, yRaw,
        x: px.x, y: px.y, r, palette,
        importance: compositeScores[i],
        hasRisk: ctx.riskLinks.has(a.id),
        isLate: a.status !== 'done' && a.due && dayDiff(a.due, todayISO()) < 0,
        isBlocked: a.status === 'blocked',
        commitment: (typeof a.commitment === 'number') ? a.commitment : 100,
        inView,
      };
    });

    // Light collision resolution among IN-VIEWPORT bubbles only —
    // out-of-viewport bubbles are clipped or pinned to edges, so they
    // shouldn't affect the in-view layout.
    (function relax() {
      const inView = placed.filter((p) => p.inView);
      for (let iter = 0; iter < 30; iter++) {
        let moved = false;
        for (let i = 0; i < inView.length; i++) {
          for (let j = i + 1; j < inView.length; j++) {
            const a = inView[i], b = inView[j];
            const dx = b.x - a.x, dy = b.y - a.y;
            const dist = Math.sqrt(dx * dx + dy * dy) || 0.01;
            const min = a.r + b.r + 1.5;
            if (dist < min) {
              const push = (min - dist) / 2;
              const ux = dx / dist, uy = dy / dist;
              a.x -= ux * push; a.y -= uy * push;
              b.x += ux * push; b.y += uy * push;
              moved = true;
            }
          }
        }
        if (!moved) break;
      }
      // Clamp in-viewport bubbles into the chart area.
      inView.forEach((p) => {
        p.x = Math.max(padL + p.r + 1, Math.min(W - padR - p.r - 1, p.x));
        p.y = Math.max(padT + p.r + 1, Math.min(H - padB - p.r - 1, p.y));
      });
    })();

    // Focus mode ranking — preset-specific so each lens surfaces what
    // its own geometry calls noteworthy.
    //
    //   Preset A — N bubbles CLOSEST TO BOTTOM-LEFT in canvas coords
    //              (firefight epicenter: most-overdue × 0% complete).
    //              Self-consistent with the concentric heat rings —
    //              the inner ring literally contains the focused set.
    //
    //   Preset B — N HIGHEST-IMPORTANCE bubbles, ignoring the X axis
    //              entirely. X is categorical (owner slot) so distance
    //              along it is meaningless; the chart's signal is
    //              purely vertical. The horizontal critical band
    //              contains the focused set by construction.
    // Focus N is computed from the preset's critical zone — exactly the
    // items the chart already calls "critical." The floor / ceiling
    // keep it useful at both extremes.
    let focusN;
    if (_cloudState.preset === 'A') {
      // Preset A — items inside the inner heat ring (diag × 0.22 from
      // the bottom-left origin).
      const oX = padL;
      const oY = padT + innerH;
      const critR = Math.sqrt(innerW * innerW + innerH * innerH) * 0.22;
      const critR2 = critR * critR;
      const inCrit = placed.reduce((n, p) =>
        n + (((p.x - oX) * (p.x - oX) + (p.y - oY) * (p.y - oY) <= critR2) ? 1 : 0), 0);
      focusN = Math.max(CLOUD_FOCUS_FLOOR, Math.min(CLOUD_FOCUS_CEIL, inCrit));
    } else if (_cloudState.preset === 'C') {
      // Preset C — items inside the upper-right danger quadrant
      // (slips ≥ ceil(cMaxSlips × 0.5) AND overdue ≥ 7 days). Rank by
      // distance to the top-right corner.
      const slipThr = Math.max(1, Math.ceil(cMaxSlips * 0.5));
      const yThr = symLog(7);
      const inCrit = placed.reduce((n, p) => {
        const isSlipper = p.xRaw >= slipThr;
        const isOverdue = p.yRaw >= yThr;
        return n + (isSlipper && isOverdue ? 1 : 0);
      }, 0);
      focusN = Math.max(CLOUD_FOCUS_FLOOR, Math.min(CLOUD_FOCUS_CEIL, inCrit));
    } else {
      // Preset B — items inside the critical horizontal band
      // (importance ≥ maxComp × 0.75).
      const critThr = maxComp * 0.75;
      const inCrit = placed.reduce((n, p) => n + (p.importance >= critThr ? 1 : 0), 0);
      focusN = Math.max(CLOUD_FOCUS_FLOOR, Math.min(CLOUD_FOCUS_CEIL, inCrit));
    }
    const focusIds = new Set();
    if (_cloudState.focusMode) {
      if (_cloudState.preset === 'A') {
        const oX = padL;
        const oY = padT + innerH;
        placed.slice().sort((a, b) => {
          const da = (a.x - oX) * (a.x - oX) + (a.y - oY) * (a.y - oY);
          const db = (b.x - oX) * (b.x - oX) + (b.y - oY) * (b.y - oY);
          return da - db;
        }).slice(0, focusN).forEach((p) => focusIds.add(p.id));
      } else if (_cloudState.preset === 'C') {
        // Rank by composite slip score: slips dominate, overdue tie-breaks.
        placed.slice().sort((a, b) => {
          const sa = a.xRaw * 30 + Math.pow(2, a.yRaw); // expand sym-log back to days
          const sb = b.xRaw * 30 + Math.pow(2, b.yRaw);
          return sb - sa;
        }).slice(0, focusN).forEach((p) => focusIds.add(p.id));
      } else {
        placed.slice().sort((a, b) => b.importance - a.importance)
          .slice(0, focusN).forEach((p) => focusIds.add(p.id));
      }
    }

    // ── Three SVG layers ──────────────────────────────────────────────
    //   svgDecorFrag  — canvas-anchored decorations (heat fill, heat
    //                   rings, zone labels). Never panned — they label
    //                   the chart's panel corners, not data positions.
    //   svgFrag       — data-bound plot content (bubbles, today line,
    //                   grid lines, importance bands). Wrapped in
    //                   #cloudPlotPan, which gets a live SVG transform
    //                   during a pan drag.
    //   svgAxesFrag   — axes (lines, ticks, titles). Always at fixed
    //                   pixel positions on the panel.
    let svgDecorFrag = '';
    let svgFrag      = '';
    let svgAxesFrag  = '';
    // (Viewport zoom: no SVG scaling, so no inline <style> block needed —
    // axes always render at their CSS-declared base size.)
    // Shared clip path so heat rings can be rendered as full circles
    // (centred at origin) and naturally clipped to the chart area —
    // avoids manual arc-direction maths and keeps the geometry honest
    // when the canvas is resized.
    // Defs lead the decor layer so the clip-path + gradients are
    // available before the heat fill / rings reference them.
    svgDecorFrag += `<defs>
      <clipPath id="cl-chart-clip">
        <rect x="${padL}" y="${padT}" width="${innerW}" height="${innerH}" rx="2"/>
      </clipPath>
      <radialGradient id="cl-heat-grad-a" cx="0%" cy="100%" r="60%" fx="0%" fy="100%">
        <stop offset="0%"   stop-color="rgba(248,113,121,.22)"/>
        <stop offset="55%"  stop-color="rgba(248,113,121,.06)"/>
        <stop offset="100%" stop-color="rgba(248,113,121,0)"/>
      </radialGradient>
      <linearGradient id="cl-heat-grad-b" x1="0%" y1="0%" x2="0%" y2="100%">
        <stop offset="0%"   stop-color="rgba(248,113,121,.18)"/>
        <stop offset="70%"  stop-color="rgba(248,113,121,0)"/>
      </linearGradient>
      <radialGradient id="cl-heat-grad-c" cx="100%" cy="0%" r="65%" fx="100%" fy="0%">
        <stop offset="0%"   stop-color="rgba(248,113,121,.24)"/>
        <stop offset="55%"  stop-color="rgba(248,113,121,.06)"/>
        <stop offset="100%" stop-color="rgba(248,113,121,0)"/>
      </radialGradient>
    </defs>`;
    if (_cloudState.preset === 'A') {
      // Heat backdrop — radial red gradient anchored at the firefight
      // origin (bottom-left). Gives the danger zone a quiet visual
      // temperature so the eye lands there before reading any bubble.
      // ── CANVAS-ANCHORED DECOR (heat fill + rings + zone labels) ─────
      // These label the chart panel's CORNERS, not data positions, so
      // they must stay still during pan. Routed to svgDecorFrag which
      // sits OUTSIDE the pan group.
      svgDecorFrag += `<rect class="cl-heat-fill" x="${padL}" y="${padT}" width="${innerW}" height="${innerH}" fill="url(#cl-heat-grad-a)" clip-path="url(#cl-chart-clip)" />`;
      const originX = padL;
      const originY = padT + innerH;
      const diag = Math.sqrt(innerW * innerW + innerH * innerH);
      [diag * 0.22, diag * 0.42, diag * 0.62].forEach((r, i) => {
        svgDecorFrag += `<circle class="cl-heat-ring cl-heat-ring-${i}" cx="${originX}" cy="${originY}" r="${r.toFixed(1)}" clip-path="url(#cl-chart-clip)"></circle>`;
      });
      svgDecorFrag += `<text class="cl-zone fire" x="${padL + 12}" y="${padT + innerH - 12}">🔥 firefight zone</text>`;
      svgDecorFrag += `<text class="cl-zone safe" x="${padL + innerW - 12}" y="${padT + 16}" text-anchor="end">safe zone ✓</text>`;

      // ── DATA-BOUND PLOT CONTENT (today line, grid, bubbles) ─────────
      // Today vertical reference line — renders only when today (xRaw=0)
      // is inside the current viewport.
      if (0 >= xMinV && 0 <= xMaxV) {
        const todayPx = dataToPx(0, yMaxV).x;
        svgFrag += `<line class="cl-today" x1="${todayPx}" y1="${padT}" x2="${todayPx}" y2="${padT + innerH}"></line>`;
        svgFrag += `<text class="cl-today-lbl" x="${todayPx + 3}" y="${padT + 12}">today</text>`;
      }
      // Y grid lines at the visible % marks (multiples of 25; if the
      // viewport's Y span is small, fall through to 10 / 5 / 1).
      const yStep = (yMaxV - yMinV) >= 50 ? 25 : (yMaxV - yMinV) >= 20 ? 10 : (yMaxV - yMinV) >= 8 ? 5 : 1;
      for (let p = Math.ceil(yMinV / yStep) * yStep; p <= yMaxV; p += yStep) {
        const y = dataToPx(xMinV, p).y;
        svgFrag += `<line class="cl-grid" x1="${padL}" y1="${y}" x2="${padL + innerW}" y2="${y}"></line>`;
      }

      // ── Y AXIS (sticky to viewport left) ────────────────────────────
      svgAxesFrag += `<g class="cl-y-axis" id="cloudYAxis">`;
      // Opaque BG that covers the left gutter so plot content scrolling
      // under the axis area doesn't bleed through the labels.
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="0" width="${padL - 0.5}" height="${H}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT}" x2="${padL}" y2="${padT + innerH}"></line>`;
      for (let p = Math.ceil(yMinV / yStep) * yStep; p <= yMaxV; p += yStep) {
        const y = dataToPx(xMinV, p).y;
        svgAxesFrag += `<text class="cl-tick" x="${padL - 6}" y="${y + 3}" text-anchor="end">${p}%</text>`;
      }
      svgAxesFrag += `<text class="cl-axis-title" transform="translate(14, ${padT + innerH / 2}) rotate(-90)" text-anchor="middle">% Complete →</text>`;
      svgAxesFrag += `</g>`;

      // ── X AXIS — renders ticks for whichever days are in the viewport
      svgAxesFrag += `<g class="cl-x-axis" id="cloudXAxis">`;
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="${padT + innerH + 0.5}" width="${W}" height="${H - padT - innerH}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT + innerH}" x2="${padL + innerW}" y2="${padT + innerH}"></line>`;
      // Show only tick candidates that fall within the visible X span.
      [{ d: -90, lbl: 'overdue 90d' }, { d: -30, lbl: 'overdue 30d' }, { d: -7, lbl: '-7d' },
       { d: 0,   lbl: 'today' },        { d: 7, lbl: '+7d' },          { d: 30, lbl: '+30d' },
       { d: 90,  lbl: '+90d' }].forEach(({ d, lbl }) => {
        const xRaw = symLog(d);
        if (xRaw < xMinV || xRaw > xMaxV) return;
        const x = dataToPx(xRaw, yMinV).x;
        svgAxesFrag += `<text class="cl-tick" x="${x}" y="${padT + innerH + 16}" text-anchor="middle">${lbl}</text>`;
      });
      // No-date lane label — only if the no-date slot (xRaw = 7.5) falls
      // in the viewport.
      if (7.5 >= xMinV && 7.5 <= xMaxV) {
        const ndX = dataToPx(7.5, yMinV).x;
        svgAxesFrag += `<text class="cl-tick muted" x="${ndX - 6}" y="${padT + innerH + 16}" text-anchor="end">no date</text>`;
      }
      svgAxesFrag += `<text class="cl-axis-title" x="${padL + innerW / 2}" y="${H - 4}" text-anchor="middle">Days to due (negative = overdue) →</text>`;
      svgAxesFrag += `</g>`;
    } else if (_cloudState.preset === 'C') {
      // ── Preset C decor — danger gradient in the upper-right corner.
      // Concentric rings would mislead here (X and Y are different
      // units), so a single radial gradient marks the danger quadrant.
      svgDecorFrag += `<rect class="cl-heat-fill" x="${padL}" y="${padT}" width="${innerW}" height="${innerH}" fill="url(#cl-heat-grad-c)" clip-path="url(#cl-chart-clip)" />`;
      svgDecorFrag += `<text class="cl-zone safe" x="${padL + 12}" y="${padT + innerH - 12}">on-track ✓</text>`;
      svgDecorFrag += `<text class="cl-zone fire" x="${padL + innerW - 12}" y="${padT + 18}" text-anchor="end">🔥 chronic slippers</text>`;

      // Vertical gridlines at integer slip counts (0..cMaxSlips+1).
      for (let s = 0; s <= cMaxSlips + 1; s++) {
        if (s < xMinV || s > xMaxV) continue;
        const x = dataToPx(s, yMinV).x;
        svgFrag += `<line class="cl-grid" x1="${x}" y1="${padT}" x2="${x}" y2="${padT + innerH}"></line>`;
      }
      // Horizontal gridlines at meaningful overdue marks.
      const overdueTicks = [0, 7, 30, 90, 180];
      overdueTicks.forEach((d) => {
        const yRaw = symLog(d);
        if (yRaw < yMinV || yRaw > yMaxV) return;
        const y = dataToPx(xMinV, yRaw).y;
        svgFrag += `<line class="cl-grid" x1="${padL}" y1="${y}" x2="${padL + innerW}" y2="${y}"></line>`;
      });

      // Y axis — days overdue ticks.
      svgAxesFrag += `<g class="cl-y-axis" id="cloudYAxis">`;
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="0" width="${padL - 0.5}" height="${H}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT}" x2="${padL}" y2="${padT + innerH}"></line>`;
      overdueTicks.forEach((d) => {
        const yRaw = symLog(d);
        if (yRaw < yMinV || yRaw > yMaxV) return;
        const y = dataToPx(xMinV, yRaw).y;
        const lbl = d === 0 ? 'on time' : `${d}d`;
        svgAxesFrag += `<text class="cl-tick" x="${padL - 6}" y="${y + 3}" text-anchor="end">${lbl}</text>`;
      });
      svgAxesFrag += `<text class="cl-axis-title" transform="translate(14, ${padT + innerH / 2}) rotate(-90)" text-anchor="middle">Days overdue ↑</text>`;
      svgAxesFrag += `</g>`;

      // X axis — slip count ticks (every integer).
      svgAxesFrag += `<g class="cl-x-axis" id="cloudXAxis">`;
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="${padT + innerH + 0.5}" width="${W}" height="${H - padT - innerH}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT + innerH}" x2="${padL + innerW}" y2="${padT + innerH}"></line>`;
      for (let s = 0; s <= cMaxSlips + 1; s++) {
        if (s < xMinV || s > xMaxV) continue;
        const x = dataToPx(s, yMinV).x;
        svgAxesFrag += `<text class="cl-tick" x="${x}" y="${padT + innerH + 16}" text-anchor="middle">${s}×</text>`;
      }
      svgAxesFrag += `<text class="cl-axis-title" x="${padL + innerW / 2}" y="${H - 4}" text-anchor="middle">Times the due date was pushed back →</text>`;
      svgAxesFrag += `</g>`;
    } else {
      // Preset B — horizontal importance bands (NOT concentric rings).
      //
      // Why bands, not rings: in Preset B the X axis is CATEGORICAL
      // (owner slots) and Y is continuous importance. A radial metric
      // from any corner mixes incompatible units — a high-importance
      // action on the rightmost owner would penalised vs a mid-
      // importance action on the leftmost owner just for being on the
      // right. The chart's actual signal is purely vertical: importance.
      // Three horizontal bands let the same vocabulary (critical /
      // watch / monitor) describe the right geometry.
      //
      // Thresholds align to the existing Y-grid (1/2/5/10/20). If the
      // dataset's maxComp is small, thresholds scale down proportionally
      // so the bands stay meaningful on small projects.
      const impToY = (v) =>
        padT + (1 - Math.log2(1 + v) / Math.log2(1 + maxComp)) * innerH;
      // Proportional thresholds so the three bands are always visible
      // regardless of dataset size. Absolute thresholds (e.g. ≥10) were
      // tempting because they align with the Y-grid ticks, but they
      // collapse the critical band to zero height as soon as the
      // dataset's max barely exceeds the threshold.
      const bandSpec = [
        { lo: maxComp * 0.75, hi: Infinity,       label: '🔥 critical', cls: 'cl-band-0' },
        { lo: maxComp * 0.50, hi: maxComp * 0.75, label: '⚠ watch',    cls: 'cl-band-1' },
        { lo: maxComp * 0.25, hi: maxComp * 0.50, label: '👁 monitor',  cls: 'cl-band-2' },
      ];
      bandSpec.forEach(({ lo, hi, label, cls }) => {
        // Bands in viewport (data) space. impToY is the LEGACY mapper
        // that uses the full Y range; convert to viewport-aware y via
        // dataToPx with the log-importance value.
        const yTop = isFinite(hi) ? dataToPx(0, Math.log2(1 + hi)).y : padT;
        const yBot = dataToPx(0, Math.log2(1 + lo)).y;
        const h = Math.max(0, yBot - yTop);
        if (h < 4) return;
        svgFrag += `<rect class="cl-band ${cls}" x="${padL}" y="${yTop.toFixed(1)}" width="${innerW}" height="${h.toFixed(1)}" clip-path="url(#cl-chart-clip)"></rect>`;
      });
      // Owner-lane grid lines — only render lanes whose index lies in
      // the X viewport.
      owners.forEach((p, i) => {
        if (i < xMinV || i > xMaxV) return;
        const x = dataToPx(i, yMinV).x;
        svgFrag += `<line class="cl-grid" x1="${x}" y1="${padT}" x2="${x}" y2="${padT + innerH}"></line>`;
      });
      // Importance horizontal grid lines — only marks in the Y viewport.
      [1, 2, 5, 10, 20].forEach((imp) => {
        if (imp > maxComp) return;
        const yRaw = Math.log2(1 + imp);
        if (yRaw < yMinV || yRaw > yMaxV) return;
        const y = dataToPx(xMinV, yRaw).y;
        svgFrag += `<line class="cl-grid" x1="${padL}" y1="${y}" x2="${padL + innerW}" y2="${y}"></line>`;
      });

      // ── Y AXIS (sticky to viewport left) ────────────────────────────
      svgAxesFrag += `<g class="cl-y-axis" id="cloudYAxis">`;
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="0" width="${padL - 0.5}" height="${H}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT}" x2="${padL}" y2="${padT + innerH}"></line>`;
      [1, 2, 5, 10, 20].forEach((imp) => {
        if (imp > maxComp) return;
        const yRaw = Math.log2(1 + imp);
        if (yRaw < yMinV || yRaw > yMaxV) return;
        const y = dataToPx(xMinV, yRaw).y;
        svgAxesFrag += `<text class="cl-tick" x="${padL - 6}" y="${y + 3}" text-anchor="end">${imp}</text>`;
      });
      svgAxesFrag += `<text class="cl-axis-title" transform="translate(14, ${padT + innerH / 2}) rotate(-90)" text-anchor="middle">Composite importance →</text>`;
      svgAxesFrag += `</g>`;

      // ── X AXIS — owner ticks filtered to visible lanes
      svgAxesFrag += `<g class="cl-x-axis" id="cloudXAxis">`;
      svgAxesFrag += `<rect class="cl-axis-bg" x="0" y="${padT + innerH + 0.5}" width="${W}" height="${H - padT - innerH}" />`;
      svgAxesFrag += `<line class="cl-axis" x1="${padL}" y1="${padT + innerH}" x2="${padL + innerW}" y2="${padT + innerH}"></line>`;
      owners.forEach((p, i) => {
        if (i < xMinV || i > xMaxV) return;
        const x = dataToPx(i, yMinV).x;
        const initials = (p.name || '').split(/\s+/).map((w) => w[0]).filter(Boolean).slice(0, 2).join('').toUpperCase();
        svgAxesFrag += `<text class="cl-tick" x="${x}" y="${padT + innerH + 14}" text-anchor="middle" title="${escapeHTML(p.name)}">${escapeHTML(initials)}</text>`;
        svgAxesFrag += `<text class="cl-tick muted" x="${x}" y="${padT + innerH + 26}" text-anchor="middle">${p._load || 0}</text>`;
      });
      svgAxesFrag += `<text class="cl-axis-title" x="${padL + innerW / 2}" y="${H - 4}" text-anchor="middle">Owner (sorted by load) →</text>`;
      svgAxesFrag += `</g>`;
    }

    // ── Bubbles ────────────────────────────────────────────────────────
    // Edge colouring replaces the old risk / overdue halos. Every
    // bubble's stroke encodes its status signal — neutral for the
    // baseline, amber for risk-attached, red for overdue. Overdue
    // wins when both apply (the more time-sensitive signal). The
    // fill encodes the user's chosen colour-by dimension. This keeps
    // each bubble compact and visually separable from its neighbours
    // — far cleaner than three concentric halo rings.
    //
    // Two halos still render: the project ring (informational, only
    // in __all__ mode) and the selection ring (UI affordance). The
    // selection ring is drawn last so it always sits on top.
    placed.forEach((b) => {
      // Out-of-viewport bubbles aren't drawn — they're outside the
      // visible data range after the user zoomed in. The user can
      // zoom out to see them again.
      if (!b.inView) return;
      const sel = _cloudState.selection.has(b.id);
      const focusDim = _cloudState.focusMode && !focusIds.has(b.id) && !sel;
      let halos = '';
      if (curProjectIsMerged()) {
        const ring = projectRing(b.proj);
        halos += `<circle class="cl-ring cl-ring-proj" cx="${b.x.toFixed(1)}" cy="${b.y.toFixed(1)}" r="${(b.r + 3).toFixed(1)}" stroke="${ring}"></circle>`;
      }
      if (sel) {
        halos += `<circle class="cl-ring cl-ring-sel" cx="${b.x.toFixed(1)}" cy="${b.y.toFixed(1)}" r="${(b.r + 6).toFixed(1)}"></circle>`;
      }
      // Edge encoding — overdue > risk-attached > neutral.
      let strokeColor;
      let strokeWidth;
      let strokeClass = '';
      if (b.isLate) {
        strokeColor = 'rgb(248,113,121)';
        strokeWidth = 2.5;
        strokeClass = ' is-late';
      } else if (b.hasRisk) {
        strokeColor = 'rgb(245,193,80)';
        strokeWidth = 2.2;
        strokeClass = ' is-risk';
      } else {
        strokeColor = 'rgba(255,255,255,.5)';
        strokeWidth = 1;
      }
      // Inline identifier — only shown when bubble is big enough to host it.
      const showIdent = b.r >= 11;
      const ident = showIdent && (b.a.identifier || (b.a.title || '').match(/[A-Z]{2,}-\d+/)?.[0])
        ? `<text class="cl-bubble-ident" x="${b.x.toFixed(1)}" y="${(b.y + 3).toFixed(1)}" text-anchor="middle">${escapeHTML(b.a.identifier || '')}</text>`
        : '';
      svgFrag += `<g class="cl-bubble-g${sel ? ' is-sel' : ''}${focusDim ? ' is-dim' : ''}${strokeClass}" data-action-id="${escapeHTML(b.id)}" data-proj-id="${escapeHTML(b.proj.id)}">
        ${halos}
        <circle class="cl-bubble" cx="${b.x.toFixed(1)}" cy="${b.y.toFixed(1)}" r="${b.r.toFixed(1)}" fill="${b.palette.fill}" stroke="${strokeColor}" stroke-width="${strokeWidth}"></circle>
        ${ident}
      </g>`;
    });

    // Three-layer SVG assembly:
    //   svgDecorFrag — canvas-anchored (heat fill, rings, zone labels).
    //                  Outside pan group so it never moves with drag.
    //   svgFrag      — data-bound plot content (today line, grid,
    //                  bubbles). Wrapped in #cloudPlotPan, which the
    //                  pan handler translates live.
    //   svgAxesFrag  — axes, also outside pan, always anchored.
    //
    // Defs (clip-path, gradients) extracted from svgFrag's prefix and
    // emitted first so they're available to all three layers.
    SVG.innerHTML = svgDecorFrag
      + `<g id="cloudPlotPan">${svgFrag}</g>`
      + svgAxesFrag;
    const presetLabel = { A: 'Urgency × Progress', B: 'Owner × Importance', C: 'Chronic slippers' }[_cloudState.preset] || _cloudState.preset;
    $('#cloudStat').textContent = `${allActions.length} action${allActions.length === 1 ? '' : 's'} · ${presetLabel}${_cloudState.focusMode ? ` · top ${focusN} highlighted` : ''}`;
    // Patch the button's N now that we've computed it from the
    // critical-zone count.
    const focusNEl = document.getElementById('cloudFocusN');
    if (focusNEl) focusNEl.textContent = focusN;

    // Repaint just selection halos (skip full layout) — used after
    // selection toggles so we don't pay the relax cost on each click.
    function repaintSelectionOnly() {
      $$('.cl-bubble-g', SVG).forEach((g) => {
        const id = g.dataset.actionId;
        const sel = _cloudState.selection.has(id);
        g.classList.toggle('is-sel', sel);
        const existing = g.querySelector('.cl-ring-sel');
        if (sel && !existing) {
          const ref = placed.find((p) => p.id === id);
          if (!ref) return;
          const c = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
          c.setAttribute('class', 'cl-ring cl-ring-sel');
          c.setAttribute('cx', ref.x.toFixed(1));
          c.setAttribute('cy', ref.y.toFixed(1));
          c.setAttribute('r',  (ref.r + 6).toFixed(1));
          g.insertBefore(c, g.querySelector('.cl-bubble'));
        } else if (!sel && existing) {
          existing.remove();
        }
      });
    }

    // ── Interactions ───────────────────────────────────────────────────
    // Click bubble → toggle selection. Shift-click adds (no remove).
    // Cmd/Ctrl-click → open the action's drawer directly.
    SVG.addEventListener('click', (e) => {
      const g = e.target.closest('.cl-bubble-g');
      if (!g) {
        // Empty space — clear selection (only if there's something to clear).
        if (_cloudState.selection.size && !e.shiftKey) {
          _cloudState.selection.clear();
          refreshSelStrip();
          repaintSelectionOnly();
        }
        return;
      }
      const id = g.dataset.actionId;
      if (e.metaKey || e.ctrlKey) { openDrawer(id); return; }
      if (e.shiftKey) {
        _cloudState.selection.add(id);
      } else {
        if (_cloudState.selection.has(id)) _cloudState.selection.delete(id);
        else _cloudState.selection.add(id);
      }
      refreshSelStrip();
      repaintSelectionOnly();
    });
    SVG.addEventListener('dblclick', (e) => {
      const g = e.target.closest('.cl-bubble-g');
      if (g) openDrawer(g.dataset.actionId);
    });

    // Hover card — singleton appended to <body> so it escapes SVG clip.
    let hoverCard = document.getElementById('actionHoverCard');
    if (!hoverCard) {
      hoverCard = document.createElement('div');
      hoverCard.id = 'actionHoverCard';
      hoverCard.className = 'action-hover-card';
      document.body.appendChild(hoverCard);
    }
    const hideHover = () => hoverCard.classList.remove('show');
    const positionHover = (e) => {
      const pad = 12;
      const rect = hoverCard.getBoundingClientRect();
      const x = Math.min(window.innerWidth - rect.width - pad, e.clientX + 14);
      const y = Math.min(window.innerHeight - rect.height - pad, e.clientY + 14);
      hoverCard.style.left = Math.max(pad, x) + 'px';
      hoverCard.style.top  = Math.max(pad, y) + 'px';
    };
    const showHover = (a, p, e) => {
      const lvl = priorityLevel(a.priorityLevel);
      const cmp = a.component ? findComponent(p, a.component) : null;
      const cRgb = cmp ? componentColor(cmp.color).rgb : null;
      const owner = personName(a.owner);
      const dtd = actionDaysToDue(a);
      const pct = percentComplete(a);
      const ph  = plannedHoursTotal(a);
      const ah  = totalLoggedHours(a);
      const slip = baselineSlipDays(a);
      const dueLbl = a.due
        ? (dtd < 0 ? `overdue ${Math.abs(dtd)}d` : dtd === 0 ? 'due today' : `due in ${dtd}d`)
        : 'no due date';
      hoverCard.innerHTML = `
        ${a.identifier ? `<div class="hc-ident">${escapeHTML(a.identifier)}</div>` : ''}
        <div class="hc-title">${escapeHTML(a.title || '(untitled)')}</div>
        <div class="hc-chips">
          ${curProjectIsMerged() ? `<span class="hc-chip" style="border-color:${projectRing(p)};color:${projectRing(p)};">${escapeHTML(p.name)}</span>` : ''}
          ${cmp ? `<span class="hc-chip" style="background:rgba(${cRgb},.18);color:rgb(${cRgb});border-color:rgba(${cRgb},.5)">${escapeHTML(cmp.name)}</span>` : ''}
          <span class="hc-chip" style="color:rgb(${lvl.rgb}); border-color:rgba(${lvl.rgb},.5)">${escapeHTML(lvl.label)} priority</span>
          <span class="hc-chip" data-cs="${a.status}">${escapeHTML(a.status)}</span>
        </div>
        <div class="hc-meta">
          <span>${escapeHTML(owner)}</span>
          <span class="hc-sep">·</span>
          <span class="${a.due && dtd < 0 && a.status !== 'done' ? 'bad' : ''}">${escapeHTML(dueLbl)}</span>
          <span class="hc-sep">·</span>
          <span>${pct}% complete</span>
        </div>
        <div class="hc-meta">
          <span>${ah.toFixed(1)}h / ${ph.toFixed(1)}h planned</span>
          ${slip != null ? `<span class="hc-sep">·</span><span class="${slip > 0 ? 'bad' : 'ok'}">${slip > 0 ? '+' : ''}${slip}d vs baseline</span>` : ''}
          ${(() => { const n = actionSlipPushBacks(a); return n > 0 ? `<span class="hc-sep">·</span><span class="${n >= 2 ? 'bad' : 'warn'}">pushed back ${n}×</span>` : ''; })()}
        </div>
        <div class="hc-hint">click to ${_cloudState.selection.has(a.id) ? 'deselect' : 'select'} · ⌘/ctrl-click to open</div>
      `;
      hoverCard.classList.add('show');
      positionHover(e);
    };
    SVG.addEventListener('mousemove', (e) => {
      // Suppress hover-card lookups during a drag — they fire on every
      // mousemove, do DOM closest() + searches, and visibly add lag to
      // the pan transform updates.
      if (dragState) { hideHover(); return; }
      const g = e.target.closest('.cl-bubble-g');
      if (!g) { hideHover(); return; }
      const id = g.dataset.actionId;
      const ref = placed.find((p) => p.id === id);
      if (ref) showHover(ref.a, ref.proj, e);
    });
    SVG.addEventListener('mouseleave', hideHover);

    // ── Toolbar buttons ────────────────────────────────────────────────
    $$('[data-cloud-preset]', view).forEach((btn) => {
      btn.addEventListener('click', () => {
        _cloudState.preset = btn.dataset.cloudPreset;
        render();
      });
    });
    $('#cloudFocus').addEventListener('click', () => {
      _cloudState.focusMode = !_cloudState.focusMode;
      render();
    });
    // ── Viewport-zoom controls ────────────────────────────────────────
    // Zoom changes the visible data RANGE (a.k.a. viewport). The canvas
    // stays at its panel size, bubbles never resize, axes never move —
    // only their tick labels regenerate. The user can:
    //   • drag a box inside the chart to zoom to that box
    //   • click +/- buttons to zoom in/out around the centre
    //   • Ctrl/⌘ + wheel to zoom in/out anchored on the cursor
    //   • ⤢ Reset to return to the full data range
    //   • keyboard Ctrl/⌘ + +/-/0 mirroring the buttons

    // Compute the zoom percentage label from the viewport's X span
    // (a useful 1-number summary; if x span = full, that's 100%).
    const refreshZoomLbl = () => {
      const xSpan = _cloudState.viewport.xMaxFrac - _cloudState.viewport.xMinFrac;
      const ySpan = _cloudState.viewport.yMaxFrac - _cloudState.viewport.yMinFrac;
      // Effective zoom relative to full = sqrt(area inverse). Clean 1-number summary.
      const effZoom = 1 / Math.sqrt(xSpan * ySpan);
      $('#cloudZoomVal').textContent = Math.round(effZoom * 100) + '%';
    };
    refreshZoomLbl();

    // Zoom out/in by a factor (e.g. 0.7 → zoom in, 1.4 → zoom out).
    // Optional cursor anchor (in viewBox / pixel coords) so the data
    // point under the cursor stays under the cursor.
    const zoomByFactor = (factor, anchor) => {
      const vp = _cloudState.viewport;
      // Anchor expressed as fractions of the chart area; default = centre.
      let ax = 0.5, ay = 0.5;
      if (anchor) {
        ax = Math.max(0, Math.min(1, (anchor.x - padL) / innerW));
        ay = Math.max(0, Math.min(1, (anchor.y - padT) / innerH));
      }
      const xSpan = (vp.xMaxFrac - vp.xMinFrac) * factor;
      const ySpan = (vp.yMaxFrac - vp.yMinFrac) * factor;
      // Anchor fraction in data space = vp.xMin + ax * span
      const ancX = vp.xMinFrac + ax * (vp.xMaxFrac - vp.xMinFrac);
      const ancY = vp.yMinFrac + (1 - ay) * (vp.yMaxFrac - vp.yMinFrac); // inverted: y=0 is top of chart
      let xMin = ancX - ax * xSpan;
      let xMax = xMin + xSpan;
      let yMin = ancY - (1 - ay) * ySpan;
      let yMax = yMin + ySpan;
      // Clamp into [0, 1] preserving span when possible.
      if (xMin < 0) { xMax -= xMin; xMin = 0; }
      if (xMax > 1) { xMin -= (xMax - 1); xMax = 1; xMin = Math.max(0, xMin); }
      if (yMin < 0) { yMax -= yMin; yMin = 0; }
      if (yMax > 1) { yMin -= (yMax - 1); yMax = 1; yMin = Math.max(0, yMin); }
      // Enforce minimum visible span so we can't zoom in past 30× from full.
      const MIN_SPAN = 0.03;
      if (xMax - xMin < MIN_SPAN) {
        const mid = (xMin + xMax) / 2; xMin = mid - MIN_SPAN/2; xMax = mid + MIN_SPAN/2;
      }
      if (yMax - yMin < MIN_SPAN) {
        const mid = (yMin + yMax) / 2; yMin = mid - MIN_SPAN/2; yMax = mid + MIN_SPAN/2;
      }
      vp.xMinFrac = xMin; vp.xMaxFrac = xMax;
      vp.yMinFrac = yMin; vp.yMaxFrac = yMax;
      render();
    };
    // Reset to full range.
    const zoomReset = () => {
      _cloudState.viewport = { xMinFrac: 0, xMaxFrac: 1, yMinFrac: 0, yMaxFrac: 1 };
      render();
    };

    $('#cloudZoomOut').addEventListener('click', () => zoomByFactor(1.4));
    $('#cloudZoomIn') .addEventListener('click', () => zoomByFactor(0.7));
    $('#cloudZoomFit').addEventListener('click', zoomReset);

    // Ctrl/⌘ + wheel = zoom at cursor; plain wheel ignored.
    SVG.addEventListener('wheel', (e) => {
      if (!(e.ctrlKey || e.metaKey)) return;
      e.preventDefault();
      const rect = SVG.getBoundingClientRect();
      const anchor = { x: e.clientX - rect.x, y: e.clientY - rect.y };
      zoomByFactor(e.deltaY < 0 ? 0.85 : 1.18, anchor);
    }, { passive: false });

    // Keyboard shortcuts: Ctrl/⌘ + +/- to zoom in/out around centre, 0 to reset.
    const cloudKeyHandler = (e) => {
      if (!(e.ctrlKey || e.metaKey)) return;
      if (e.target?.closest('input, textarea, [contenteditable]')) return;
      if (e.key === '=' || e.key === '+') {
        e.preventDefault(); zoomByFactor(0.7);
      } else if (e.key === '-' || e.key === '_') {
        e.preventDefault(); zoomByFactor(1.4);
      } else if (e.key === '0') {
        e.preventDefault(); zoomReset();
      }
    };
    document.addEventListener('keydown', cloudKeyHandler);
    view.__cloudCleanup = () => document.removeEventListener('keydown', cloudKeyHandler);

    // ── Pan with plain drag · zoom-to-box with shift-drag ────────────
    //
    // Mouse-down on the chart background (NOT on a bubble) starts a
    // drag. The interpretation depends on whether Shift is held at
    // mousedown time:
    //
    //   plain drag  → PAN. The plot content translates live via SVG
    //                 transform on #cloudPlotPan; on release the
    //                 viewport fractions are shifted and the chart
    //                 re-renders so the axis tick labels update.
    //
    //   shift+drag  → ZOOM TO BOX. A selection rectangle follows the
    //                 cursor; on release the viewport snaps to it.
    //
    // Bubble clicks are unaffected (selection still toggles).
    let dragState = null;
    SVG.addEventListener('mousedown', (e) => {
      if (e.button !== 0) return;
      if (e.target.closest('.cl-bubble-g')) return; // bubble click → selection
      const rect = SVG.getBoundingClientRect();
      const px = e.clientX - rect.x;
      const py = e.clientY - rect.y;
      if (px < padL || px > padL + innerW || py < padT || py > padT + innerH) return;
      const isZoomBox = e.shiftKey;
      dragState = { mode: isZoomBox ? 'zoom' : 'pan', startX: px, startY: py, curX: px, curY: py };
      if (!isZoomBox) {
        SVG.style.cursor = 'grabbing';
        // Hint the browser to promote #cloudPlotPan to its own
        // compositor layer so transform updates are paint-free.
        // (Removed on mouseup / mouseleave so the hint isn't kept
        // around when not needed.)
        const pan = document.getElementById('cloudPlotPan');
        if (pan) pan.style.willChange = 'transform';
      }
      e.preventDefault();
    });
    SVG.addEventListener('mousemove', (e) => {
      if (!dragState) return;
      const rect = SVG.getBoundingClientRect();
      dragState.curX = Math.max(padL, Math.min(padL + innerW, e.clientX - rect.x));
      dragState.curY = Math.max(padT, Math.min(padT + innerH, e.clientY - rect.y));
      if (dragState.mode === 'pan') {
        // Live pan via SVG transform on the plot group. Translate so
        // the point that was at startX is now under curX.
        const dx = dragState.curX - dragState.startX;
        const dy = dragState.curY - dragState.startY;
        const pan = document.getElementById('cloudPlotPan');
        if (pan) pan.setAttribute('transform', `translate(${dx.toFixed(1)}, ${dy.toFixed(1)})`);
      } else {
        // Live-render the zoom selection rect.
        let r = document.getElementById('cloudZoomRect');
        if (!r) {
          r = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
          r.setAttribute('id', 'cloudZoomRect');
          r.setAttribute('class', 'cl-zoom-rect');
          SVG.appendChild(r);
        }
        const x1 = Math.min(dragState.startX, dragState.curX);
        const y1 = Math.min(dragState.startY, dragState.curY);
        const x2 = Math.max(dragState.startX, dragState.curX);
        const y2 = Math.max(dragState.startY, dragState.curY);
        r.setAttribute('x', x1);
        r.setAttribute('y', y1);
        r.setAttribute('width', x2 - x1);
        r.setAttribute('height', y2 - y1);
      }
    });
    SVG.addEventListener('mouseup', () => {
      const r = document.getElementById('cloudZoomRect');
      if (r) r.remove();
      SVG.style.cursor = '';
      const pan = document.getElementById('cloudPlotPan');
      if (pan) pan.style.willChange = '';
      if (!dragState) return;
      const ds = dragState;
      dragState = null;
      const dx = ds.curX - ds.startX;
      const dy = ds.curY - ds.startY;
      if (Math.abs(dx) < 4 && Math.abs(dy) < 4) {
        // Click without drag — clear any in-progress transform and bail.
        const pan = document.getElementById('cloudPlotPan');
        if (pan) pan.removeAttribute('transform');
        return;
      }
      const vp = _cloudState.viewport;
      const xSpan = vp.xMaxFrac - vp.xMinFrac;
      const ySpan = vp.yMaxFrac - vp.yMinFrac;
      if (ds.mode === 'pan') {
        // Convert pixel delta to viewport-fraction shift. A positive dx
        // (drag right) means the user wants to see what was to the
        // LEFT, so xMin/xMax shift LEFT.
        const dxFrac = (dx / innerW) * xSpan;
        const dyFrac = (dy / innerH) * ySpan;
        let xMin = vp.xMinFrac - dxFrac;
        let xMax = vp.xMaxFrac - dxFrac;
        // Y is inverted: drag down (positive dy) means user wants to see
        // what was above (higher Y values in data space).
        let yMin = vp.yMinFrac + dyFrac;
        let yMax = vp.yMaxFrac + dyFrac;
        // Clamp into [0, 1] preserving span.
        if (xMin < 0) { xMax -= xMin; xMin = 0; }
        if (xMax > 1) { xMin -= (xMax - 1); xMax = 1; xMin = Math.max(0, xMin); }
        if (yMin < 0) { yMax -= yMin; yMin = 0; }
        if (yMax > 1) { yMin -= (yMax - 1); yMax = 1; yMin = Math.max(0, yMin); }
        vp.xMinFrac = xMin; vp.xMaxFrac = xMax;
        vp.yMinFrac = yMin; vp.yMaxFrac = yMax;
        render();
        return;
      }
      // ZOOM-TO-BOX path
      const x1 = Math.min(ds.startX, ds.curX);
      const y1 = Math.min(ds.startY, ds.curY);
      const x2 = Math.max(ds.startX, ds.curX);
      const y2 = Math.max(ds.startY, ds.curY);
      if (x2 - x1 < 8 || y2 - y1 < 8) return; // box too small
      const xMinNew = vp.xMinFrac + ((x1 - padL) / innerW) * xSpan;
      const xMaxNew = vp.xMinFrac + ((x2 - padL) / innerW) * xSpan;
      const yMaxNew = vp.yMinFrac + (1 - (y1 - padT) / innerH) * ySpan;
      const yMinNew = vp.yMinFrac + (1 - (y2 - padT) / innerH) * ySpan;
      const MIN_SPAN = 0.03;
      vp.xMinFrac = Math.max(0, Math.min(1 - MIN_SPAN, xMinNew));
      vp.xMaxFrac = Math.max(vp.xMinFrac + MIN_SPAN, Math.min(1, xMaxNew));
      vp.yMinFrac = Math.max(0, Math.min(1 - MIN_SPAN, yMinNew));
      vp.yMaxFrac = Math.max(vp.yMinFrac + MIN_SPAN, Math.min(1, yMaxNew));
      render();
    });
    // Cancel drag on leave so a stuck drag-rect or pan-transform doesn't linger.
    SVG.addEventListener('mouseleave', () => {
      const r = document.getElementById('cloudZoomRect');
      if (r) r.remove();
      const pan = document.getElementById('cloudPlotPan');
      if (pan && dragState?.mode === 'pan') {
        pan.removeAttribute('transform');
        pan.style.willChange = '';
      }
      SVG.style.cursor = '';
      dragState = null;
    });
  }
  /* ─────────────────────── /Action Cloud ─────────────────────────── */

  function renderOpenPoints(root) {
    const proj = curProject();
    proj.openPoints = proj.openPoints || [];
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Open points</div>
          <div class="page-sub">Quick capture for ideas, follow-ups, and questions. Convert any item to an action when ready.</div>
        </div>
        <div class="page-actions">
          <div class="seg" id="opViewSeg" role="tablist" aria-label="View mode">
            <button class="seg-btn ${opFilterState.view === 'list'    ? 'active' : ''}" data-op-view="list"   title="Inline-editable list" role="tab">≣ List</button>
            <button class="seg-btn ${opFilterState.view === 'matrix'  ? 'active' : ''}" data-op-view="matrix" title="4×4 criticality × priority matrix — bubbles sized by age" role="tab">▦ Matrix</button>
            <button class="seg-btn ${opFilterState.view === 'bubbles' ? 'active' : ''}" data-op-view="bubbles" title="Packed bubbles — sized by composite importance, coloured by component" role="tab">◯ Bubbles</button>
          </div>
        </div>
      </div>
      <div class="op-quick">
        <input id="opInput" type="text" placeholder="Type and press Enter — anything to remember (e.g. ‘ask vendor about cell datasheet’)" />
        <select id="opQuickComp" title="Optional: link to a component on capture">
          <option value="">— component</option>
          ${(proj.components || []).map((pt) => `<option value="${pt.id}">${escapeHTML(pt.name)}</option>`).join('')}
        </select>
        <button class="ghost" id="opAdd">Add</button>
      </div>
      <div class="op-filterbar">
        <input id="opFilterQ" type="search" placeholder="Filter by text…" value="${escapeHTML(opFilterState.q)}" />
        <select id="opFilterComp" title="Filter by component">
          <option value="">All components</option>
          <option value="__none">— No component</option>
          ${(proj.components || []).map((pt) => `<option value="${pt.id}" ${pt.id === opFilterState.component ? 'selected' : ''}>${escapeHTML(pt.name)}</option>`).join('')}
        </select>
        <select id="opFilterCrit" title="Filter by criticality">
          <option value="">All criticality</option>
          ${['low','med','high','critical'].map((k) => `<option value="${k}" ${k === opFilterState.criticality ? 'selected' : ''}>${CRITICALITY_LABEL[k]}</option>`).join('')}
        </select>
        <select id="opFilterPrio" title="Filter by priority">
          <option value="">All priority</option>
          ${PRIORITY_LEVELS.map((p) => `<option value="${p.id}" ${p.id === opFilterState.priority ? 'selected' : ''}>${p.label}</option>`).join('')}
        </select>
        <select id="opFilterProg" title="Filter by step progress">
          <option value="">All progress</option>
          <option value="none"      ${opFilterState.progress === 'none' ? 'selected' : ''}>No steps</option>
          <option value="some"      ${opFilterState.progress === 'some' ? 'selected' : ''}>In progress</option>
          <option value="all-done"  ${opFilterState.progress === 'all-done' ? 'selected' : ''}>All steps done</option>
        </select>
        <select id="opSort" title="Sort">
          <option value="manual"    ${opFilterState.sort === 'manual' ? 'selected' : ''}>Manual order</option>
          <option value="newest"    ${opFilterState.sort === 'newest' ? 'selected' : ''}>Newest first</option>
          <option value="oldest"    ${opFilterState.sort === 'oldest' ? 'selected' : ''}>Oldest first</option>
          <option value="crit-desc" ${opFilterState.sort === 'crit-desc' ? 'selected' : ''}>Criticality ↓</option>
          <option value="prio-desc" ${opFilterState.sort === 'prio-desc' ? 'selected' : ''}>Priority ↓</option>
          <option value="title-asc" ${opFilterState.sort === 'title-asc' ? 'selected' : ''}>Title A–Z</option>
          <option value="progress"  ${opFilterState.sort === 'progress' ? 'selected' : ''}>Step progress</option>
        </select>
        <button class="ghost" id="opFilterReset" title="Clear filters">Reset</button>
      </div>
      <div class="op-list" id="opList"></div>`;
    root.appendChild(view);

    // Helpers — sortable rank (Critical → Low) and matcher
    const CRIT_RANK = { critical: 0, high: 1, med: 2, low: 3 };
    function matchesFilters(op) {
      // Topbar global search — applied alongside the panel's own filters.
      if (!matchesSearch(op.title, op.notes, (op.steps || []).map((s) => s.text))) return false;
      if (opFilterState.q) {
        const q = opFilterState.q.toLowerCase();
        const hay = (op.title + ' ' + (op.notes || '') + ' ' + (op.steps || []).map((s) => s.text).join(' ')).toLowerCase();
        if (!hay.includes(q)) return false;
      }
      if (opFilterState.component) {
        if (opFilterState.component === '__none') { if (op.component) return false; }
        else if (op.component !== opFilterState.component) return false;
      }
      if (opFilterState.criticality && (op.criticality || 'med') !== opFilterState.criticality) return false;
      if (opFilterState.priority    && (op.priorityLevel || 'med') !== opFilterState.priority) return false;
      if (opFilterState.progress) {
        const total = (op.steps || []).length;
        const done  = (op.steps || []).filter((s) => s.done).length;
        if (opFilterState.progress === 'none' && total > 0) return false;
        if (opFilterState.progress === 'some' && (total === 0 || done === total)) return false;
        if (opFilterState.progress === 'all-done' && (total === 0 || done < total)) return false;
      }
      return true;
    }
    function compareForSort(a, b) {
      switch (opFilterState.sort) {
        case 'newest':    return (b.createdAt || '').localeCompare(a.createdAt || '');
        case 'oldest':    return (a.createdAt || '').localeCompare(b.createdAt || '');
        case 'crit-desc': return (CRIT_RANK[a.criticality || 'med'] - CRIT_RANK[b.criticality || 'med']);
        case 'prio-desc': return (CRIT_RANK[a.priorityLevel || 'med'] - CRIT_RANK[b.priorityLevel || 'med']);
        case 'title-asc': return (a.title || '').localeCompare(b.title || '');
        case 'progress': {
          const pa = ((a.steps || []).filter((s) => s.done).length) / Math.max(1, (a.steps || []).length);
          const pb = ((b.steps || []).filter((s) => s.done).length) / Math.max(1, (b.steps || []).length);
          return pa - pb;
        }
        default: return 0; // manual — preserve array order
      }
    }

    function draw() {
      const list = $('#opList');
      const all = proj.openPoints || [];
      const filtered = all.filter(matchesFilters);
      const items = opFilterState.sort === 'manual' ? filtered : filtered.slice().sort(compareForSort);
      // Reflect the active view as a class on the container — used by CSS
      // to switch layout and to enable / disable inline editing.
      list.className = 'op-list op-view-' + opFilterState.view;
      if (!all.length) {
        list.innerHTML = '<div class="empty">No open points yet — capture something above.</div>';
        return;
      }
      if (!items.length) {
        list.innerHTML = '<div class="empty">No open points match the current filters.</div>';
        return;
      }
      if (opFilterState.view === 'matrix') {
        drawMatrixView(items);
        return;
      }
      if (opFilterState.view === 'bubbles') {
        drawBubblesView(items);
        return;
      }
      list.innerHTML = items.map((op) => {
        // Backfill defaults for legacy items
        if (!op.criticality) op.criticality = 'med';
        // Left-border tint reflects the *dominant* severity of either
        // criticality or priority — whichever is higher drives the colour
        // so a Critical-priority point is red even if its criticality is
        // only Medium, and vice versa.
        const SEV_ORDER = ['low', 'med', 'high', 'critical'];
        const critIdx = Math.max(0, SEV_ORDER.indexOf(op.criticality || 'med'));
        const prioIdx = Math.max(0, SEV_ORDER.indexOf(op.priorityLevel || 'med'));
        const domKey = SEV_ORDER[Math.max(critIdx, prioIdx)];
        const domRgb = CRITICALITY_RGB[domKey];
        if (!op.createdAt) op.createdAt = todayISO();
        if (!Array.isArray(op.steps)) op.steps = [];
        const cmp = findComponent(proj, op.component);
        const c = cmp ? componentColor(cmp.color) : null;
        const critRgb = CRITICALITY_RGB[op.criticality] || CRITICALITY_RGB.med;
        // The left border tracks criticality (component shown as a chip below).
        const tint = `style="border-left-color: rgb(${domRgb})"`;
        const critLabel = CRITICALITY_LABEL[op.criticality] || 'Medium';
        // op.notes holds rich HTML; legacy plain-string entries render as text
        const contextHtml = op.notes && /<\w+/.test(op.notes) ? op.notes : escapeHTML(op.notes || '');
        const stepDone = op.steps.filter((s) => s.done).length;
        const stepTotal = op.steps.length;
        const stepsAllDone = stepTotal > 0 && stepDone === stepTotal;
        const prio = priorityLevel(op.priorityLevel);
        // Both chips ALWAYS render so the user can click them to change
        // values. The "med" (default) state uses a quieter visual treatment
        // so the title row stays calm at rest and chips only "light up"
        // when they carry a non-default signal.
        const prioId = op.priorityLevel || 'med';
        const prioQuiet = prioId === 'med';
        const critQuiet = (op.criticality || 'med') === 'med';
        const critChipStyle = critQuiet
          ? `background:transparent;color:rgb(${critRgb});border:1px dashed rgba(${critRgb},.55)`
          : `background:rgba(${critRgb},.18);color:rgb(${critRgb});border:1px solid rgb(${critRgb})`;
        const prioChipStyle = prioQuiet
          ? `background:transparent;color:rgba(${prio.rgb},.85);border:1px dashed rgba(${prio.rgb},.55)`
          : `background:rgba(${prio.rgb},.18);color:rgb(${prio.rgb});border:1px solid rgb(${prio.rgb})`;
        return `
        <div class="op-item crit-${op.criticality}" data-id="${op.id}" ${tint}>
          <span class="op-row-grip" title="Drag to reorder" aria-hidden="true">⋮⋮</span>
          <div class="op-content">
            <div class="op-title-row">
              <span class="op-ident" contenteditable="true" data-field="identifier" spellcheck="false" title="Identifier — click to edit (auto-generated from project name, override accepted)">${escapeHTML(op.identifier || '')}</span>
              <div class="op-title" contenteditable="true" data-field="title">${escapeHTML(op.title)}</div>
            </div>
            <div class="op-context-wrap">
              <div class="op-toolbar">
                <button type="button" data-cmd="bold" title="Bold"><b>B</b></button>
                <button type="button" data-cmd="italic" title="Italic"><i>I</i></button>
                <button type="button" data-cmd="underline" title="Underline"><u>U</u></button>
                <span class="sep"></span>
                <button type="button" data-cmd="insertUnorderedList" title="Bullet list">•</button>
                <button type="button" data-cmd="insertOrderedList" title="Numbered list">1.</button>
                <span class="sep"></span>
                <button type="button" data-cmd="createLink" title="Insert link">🔗</button>
                <label class="op-color" title="Text colour"><input type="color" data-cmd="foreColor" /></label>
                <button type="button" data-cmd="removeFormat" title="Clear formatting">✕</button>
              </div>
              <div class="op-context" contenteditable="true" data-placeholder="Add rich context — bold, lists, links, colour…">${contextHtml}</div>
            </div>
            <div class="op-steps ${stepsAllDone ? 'all-done' : ''}">
              ${stepTotal ? `
                <div class="op-steps-head">
                  <span class="op-steps-lbl">Resolution steps</span>
                  <span class="op-steps-progress" aria-hidden="true">
                    <span class="op-steps-bar"><span class="op-steps-bar-fill" style="width:${stepTotal ? Math.round(stepDone / stepTotal * 100) : 0}%"></span></span>
                    <span class="op-steps-count">${stepDone}/${stepTotal}</span>
                  </span>
                </div>
              ` : ''}
              <div class="op-steps-list">
                ${op.steps.map((s) => `
                  <div class="op-step ${s.done ? 'done' : ''}" data-step-id="${s.id}">
                    <span class="op-step-grip" title="Drag to reorder" aria-hidden="true">⋮⋮</span>
                    <button type="button" class="op-step-check ${s.done ? 'on' : ''}" aria-label="${s.done ? 'Mark as not done' : 'Mark as done'}"></button>
                    <span class="op-step-text" contenteditable="true" data-placeholder="Step…">${escapeHTML(s.text)}</span>
                    <button type="button" class="op-step-del" title="Delete step" aria-label="Delete step">×</button>
                  </div>
                `).join('')}
              </div>
              <button type="button" class="op-step-add">+ Add step</button>
            </div>
            <div class="op-meta">
              <span class="op-origin" title="Auto-set when this open point was originated">Originated ${fmtFull(op.createdAt)}</span>
              ${stepTotal ? `<span class="op-meta-steps ${stepsAllDone ? 'ok' : ''}" title="Resolution steps">✓ ${stepDone}/${stepTotal} steps</span>` : ''}
              <button type="button" class="op-level-chip op-crit-chip ${critQuiet ? 'is-default' : ''}" data-kind="criticality" title="Click to set criticality (severity if not addressed)" style="${critChipStyle}"><span class="chip-axis">Sev</span> ${critLabel}</button>
              <button type="button" class="op-level-chip prio-chip prio-${prio.id} ${prioQuiet ? 'is-default' : ''}" data-kind="priority" title="Click to set priority (urgency to act)" style="${prioChipStyle}"><span class="chip-axis">Pri</span> ${prio.label}</button>
              ${cmp ? `<span class="component-chip" style="background:rgba(${c.rgb},.2);color:rgb(${c.rgb})">${escapeHTML(cmp.name)}</span>` : ''}
            </div>
          </div>
          <div class="op-actions">
            <select class="op-component" title="Link to a component">
              <option value="">— component</option>
              ${(proj.components || []).map((pt) => `<option value="${pt.id}" ${pt.id === op.component ? 'selected' : ''}>${escapeHTML(pt.name)}</option>`).join('')}
            </select>
            <button class="primary op-promote">→ Action</button>
            <button class="ghost op-discard" title="Discard">×</button>
          </div>
        </div>`;
      }).join('');
      $$('.op-item', list).forEach((el) => {
        const id = el.dataset.id;
        // Title — plain text, save on blur
        const titleEl = el.querySelector('.op-title');
        if (titleEl) {
          titleEl.addEventListener('blur', () => {
            const op = proj.openPoints.find((x) => x.id === id);
            if (!op) return;
            const v = titleEl.textContent.trim();
            if (op.title !== v) { op.title = v; commit('op-title'); }
          });
          titleEl.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); titleEl.blur(); }
          });
        }
        // Identifier — short code, save on blur. Empty value re-derives
        // the auto identifier so users can clear-and-regenerate.
        const identEl = el.querySelector('.op-ident');
        if (identEl) {
          identEl.addEventListener('blur', () => {
            const op = proj.openPoints.find((x) => x.id === id);
            if (!op) return;
            const v = identEl.textContent.trim();
            const next = v || generateOpenPointIdentifier(proj);
            if (op.identifier !== next) {
              op.identifier = next;
              commit('op-identifier');
            } else if (!v) {
              // Empty input that resolved to the same identifier — repaint
              // the cell so the placeholder text shows again.
              identEl.textContent = op.identifier;
            }
          });
          identEl.addEventListener('keydown', (e) => {
            if (e.key === 'Enter') { e.preventDefault(); identEl.blur(); }
          });
        }
        // Context — rich HTML, save on blur
        const ctxEl = el.querySelector('.op-context');
        if (ctxEl) {
          ctxEl.addEventListener('blur', () => {
            const op = proj.openPoints.find((x) => x.id === id);
            if (!op) return;
            let html = ctxEl.innerHTML.trim();
            if (html === '<br>' || html === '<p></p>') html = '';
            if ((op.notes || '') !== html) { op.notes = html; commit('op-context'); }
          });
        }
        // Toolbar — execCommand on click; mousedown preventDefault preserves selection
        el.querySelectorAll('.op-toolbar [data-cmd]').forEach((btn) => {
          btn.addEventListener('mousedown', (e) => {
            e.preventDefault();
            const cmd = btn.dataset.cmd;
            ctxEl?.focus();
            if (cmd === 'createLink') {
              const url = prompt('Link URL (https://…):');
              if (url) document.execCommand('createLink', false, url);
            } else if (cmd === 'foreColor') {
              // Triggered by color input change instead
            } else {
              document.execCommand(cmd, false, null);
            }
          });
        });
        const colorInput = el.querySelector('.op-toolbar input[type="color"]');
        if (colorInput) {
          colorInput.addEventListener('input', (e) => {
            ctxEl?.focus();
            document.execCommand('foreColor', false, e.target.value);
          });
        }
        const compSel = el.querySelector('.op-component');
        if (compSel) {
          compSel.addEventListener('change', () => {
            const op = proj.openPoints.find((x) => x.id === id);
            if (!op) return;
            op.component = compSel.value || null;
            commit('op-component');
          });
        }
        // Chip-driven criticality + priority — click the chip on the title
        // row to open a colour-swatch popover, pick a level, commit. The
        // <select> dropdowns these used to live in were removed: the chips
        // now own the mutation, declutters the right-side actions row.
        el.querySelectorAll('.op-level-chip').forEach((chip) => {
          chip.addEventListener('click', (e) => {
            e.stopPropagation();
            const op = proj.openPoints.find((x) => x.id === id);
            if (!op) return;
            const kind = chip.dataset.kind; // 'criticality' | 'priority'
            const current = (kind === 'criticality') ? (op.criticality || 'med') : (op.priorityLevel || 'med');
            showLevelPopover(chip, kind, current, (val) => {
              if (kind === 'criticality') { op.criticality = val; commit('op-criticality'); }
              else                        { op.priorityLevel = val; commit('op-priority'); }
            });
          });
        });
        // Resolution steps — checkbox / edit / delete / add / drag-to-reorder
        wireOpStepHandlers(el, () => proj.openPoints.find((x) => x.id === id));

        el.querySelector('.op-promote').addEventListener('click', () => {
          const op = proj.openPoints.find((x) => x.id === id);
          if (!op) return;
          // Send rich-HTML context to the action's `description` field, leaving
          // the plain `notes` empty (unless the legacy notes were plain text).
          const isHtml = op.notes && /<\w+/.test(op.notes);
          openQuickAdd('action', {
            title: op.title,
            notes: isHtml ? '' : (op.notes || ''),
            description: isHtml ? op.notes : '',
            component: op.component,
            priorityLevel: op.priorityLevel || op.criticality || 'med',
          }, () => {
            proj.openPoints = proj.openPoints.filter((x) => x.id !== id);
            commit('op-promote');
            toast('Converted to action');
          });
        });
        el.querySelector('.op-discard').addEventListener('click', () => {
          if (!confirm('Discard this open point?')) return;
          proj.openPoints = proj.openPoints.filter((x) => x.id !== id);
          commit('op-discard');
        });
        // Drag the row by its grip to reorder open points up or down. Works
        // in both single-project and merged All-Projects views — for merged,
        // each item's source project is reordered independently.
        const rowGrip = el.querySelector('.op-row-grip');
        if (rowGrip) {
          rowGrip.addEventListener('mousedown', (e) => {
            e.preventDefault();
            // Manual reorder implies manual sort — flip the dropdown so the new
            // order isn't immediately re-sorted away after the next render.
            if (opFilterState.sort !== 'manual') {
              opFilterState.sort = 'manual';
              const sortSel = $('#opSort');
              if (sortSel) sortSel.value = 'manual';
            }
            // Snapshot the set of currently-visible (filtered) ids so we can
            // preserve hidden items in their original slots after the drop.
            const visibleIds = new Set(
              [...list.querySelectorAll('.op-item[data-id]')].map((r) => r.dataset.id)
            );
            const listEl = list;
            el.classList.add('dragging');
            document.body.classList.add('is-op-dragging');
            const onMove = (em) => {
              em.preventDefault();
              const sibs = [...listEl.querySelectorAll('.op-item:not(.dragging)')];
              const after = sibs.find((sib) => {
                const r = sib.getBoundingClientRect();
                return em.clientY < r.top + r.height / 2;
              });
              if (after) listEl.insertBefore(el, after);
              else listEl.appendChild(el);
            };
            // Suppress fresh selections that the browser tries to start when
            // the cursor sweeps over a contenteditable mid-drag.
            const onSelectStart = (ev) => ev.preventDefault();
            const onUp = () => {
              el.classList.remove('dragging');
              document.body.classList.remove('is-op-dragging');
              window.removeEventListener('mousemove', onMove);
              window.removeEventListener('mouseup', onUp);
              document.removeEventListener('selectstart', onSelectStart);
              // Clear any selection the browser still managed to start
              try { window.getSelection()?.removeAllRanges(); } catch (_) {}
              // The new visual order of the visible (filtered) rows
              const newVisibleOrder = [...listEl.querySelectorAll('.op-item[data-id]')].map((r) => r.dataset.id);
              const merged = curProjectIsMerged();
              if (merged) {
                // Reorder each source project independently. Items are placed
                // in the order they appear among the visible rows; hidden
                // items in each project keep their original slots.
                let changed = false;
                state.projects.forEach((sourceProj) => {
                  const ops = sourceProj.openPoints || [];
                  if (!ops.length) return;
                  const idsHere = new Set(ops.map((o) => o.id));
                  const visibleHere = new Set([...visibleIds].filter((id) => idsHere.has(id)));
                  const newOrderHere = newVisibleOrder.filter((id) => idsHere.has(id));
                  const byId = Object.fromEntries(ops.map((o) => [o.id, o]));
                  const before = ops.map((o) => o.id).join(',');
                  let vi = 0;
                  const next = ops.map((op) => visibleHere.has(op.id) ? (byId[newOrderHere[vi++]] || op) : op);
                  if (next.map((o) => o.id).join(',') !== before) {
                    sourceProj.openPoints = next;
                    changed = true;
                  }
                });
                if (changed) commit('op-reorder');
                return;
              }
              // Single-project: walk the original array and substitute visible slots
              const before = (proj.openPoints || []).map((o) => o.id).join(',');
              const byId = Object.fromEntries((proj.openPoints || []).map((o) => [o.id, o]));
              let visIdx = 0;
              const result = (proj.openPoints || []).map((op) => {
                if (visibleIds.has(op.id)) {
                  return byId[newVisibleOrder[visIdx++]] || op;
                }
                return op;
              });
              proj.openPoints = result;
              if (proj.openPoints.map((o) => o.id).join(',') !== before) commit('op-reorder');
            };
            window.addEventListener('mousemove', onMove);
            window.addEventListener('mouseup', onUp);
            document.addEventListener('selectstart', onSelectStart);
          });
        }
      });
    }

    const addPoint = () => {
      if (curProjectIsMerged()) { toast('Pick a single project to add open points.'); return; }
      const v = $('#opInput').value.trim();
      if (!v) return;
      const comp = $('#opQuickComp')?.value || null;
      proj.openPoints.unshift({ id: uid('op'), identifier: generateOpenPointIdentifier(proj), title: v, notes: '', component: comp, criticality: 'med', createdAt: todayISO() });
      $('#opInput').value = '';
      if ($('#opQuickComp')) $('#opQuickComp').value = '';
      commit('op-add');
      toast('Captured');
    };
    $('#opAdd').addEventListener('click', addPoint);
    $('#opInput').addEventListener('keydown', (e) => {
      if (e.key === 'Enter') { e.preventDefault(); addPoint(); }
    });
    // Filter / sort wiring
    const fq = $('#opFilterQ');
    if (fq) fq.addEventListener('input', () => { opFilterState.q = fq.value; draw(); });
    const fc = $('#opFilterComp');
    if (fc) fc.addEventListener('change', () => { opFilterState.component = fc.value; draw(); });
    const fcrit = $('#opFilterCrit');
    if (fcrit) fcrit.addEventListener('change', () => { opFilterState.criticality = fcrit.value; draw(); });
    const fprio = $('#opFilterPrio');
    if (fprio) fprio.addEventListener('change', () => { opFilterState.priority = fprio.value; draw(); });
    const fprog = $('#opFilterProg');
    if (fprog) fprog.addEventListener('change', () => { opFilterState.progress = fprog.value; draw(); });
    const fsort = $('#opSort');
    if (fsort) fsort.addEventListener('change', () => { opFilterState.sort = fsort.value; draw(); });
    const fres = $('#opFilterReset');
    if (fres) fres.addEventListener('click', () => {
      opFilterState.q = ''; opFilterState.component = ''; opFilterState.criticality = '';
      opFilterState.priority = ''; opFilterState.progress = ''; opFilterState.sort = 'manual';
      if (fq) fq.value = '';
      [fc, fcrit, fprio, fprog].forEach((el) => { if (el) el.value = ''; });
      if (fsort) fsort.value = 'manual';
      draw();
    });

    // View toggle — segmented control switches between List, Matrix, and
    // Bubbles. Re-renders in place; selection state goes back to '' so
    // bubble views don't carry stale highlights.
    const segView = view.querySelector('#opViewSeg');
    if (segView) {
      segView.querySelectorAll('[data-op-view]').forEach((b) => {
        b.addEventListener('click', () => {
          opFilterState.view = b.dataset.opView;
          segView.querySelectorAll('[data-op-view]').forEach((x) =>
            x.classList.toggle('active', x.dataset.opView === opFilterState.view));
          draw();
        });
      });
    }

    // ─── Matrix view: 4×4 criticality (rows) × priority (cols) ────────
    // Each cell holds bubbles for the points falling into that bucket;
    // bubble size = age in days (newer ones smaller); colour = component
    // (with severity fallback when no component is set). Hover for full
    // detail; click to switch to List view scrolled to that point.
    function drawMatrixView(items) {
      const list = $('#opList');
      // Group items into 16 cells indexed by `${critRank}-${prioRank}`.
      const cells = new Map();
      items.forEach((op) => {
        const k = `${_opCritRank(op)}-${_opPrioRank(op)}`;
        if (!cells.has(k)) cells.set(k, []);
        cells.get(k).push(op);
      });
      // Build the grid header row + 4 row labels + 16 cells. Top row =
      // critical, bottom row = low; left = low priority, right = critical.
      // The "danger" cells (high+, high+) get a subtle red wash so the
      // dangerous quadrant pops without screaming.
      const rowOrder = [4, 3, 2, 1]; // criticality
      const colOrder = [1, 2, 3, 4]; // priority
      let html = '<div class="op-matrix">';
      // Header — column labels (priority axis)
      html += '<div class="op-matrix-corner"><div class="op-matrix-axis-y">Criticality →</div><div class="op-matrix-axis-x">Priority →</div></div>';
      colOrder.forEach((p) => {
        const lbl = _OP_LEVELS[p - 1];
        html += `<div class="op-matrix-col-head crit-${lbl}">${escapeHTML(CRITICALITY_LABEL[lbl])}</div>`;
      });
      // Each row = one criticality, then 4 cells for the priority axis.
      rowOrder.forEach((c) => {
        const cLbl = _OP_LEVELS[c - 1];
        html += `<div class="op-matrix-row-head crit-${cLbl}">${escapeHTML(CRITICALITY_LABEL[cLbl])}</div>`;
        colOrder.forEach((p) => {
          const k = `${c}-${p}`;
          const bucket = cells.get(k) || [];
          const danger = (c >= 3 && p >= 3) ? ' is-danger' : '';
          const count  = bucket.length;
          html += `<div class="op-matrix-cell${danger}" data-cell="${k}">`;
          html += `<div class="op-matrix-cell-count">${count}</div>`;
          html += `<svg class="op-matrix-cell-svg" preserveAspectRatio="xMidYMid meet" viewBox="0 0 200 100"></svg>`;
          html += `</div>`;
        });
      });
      html += '</div>';
      list.innerHTML = html;

      // Pack each cell's bubbles after the DOM is in place so we know
      // each SVG's actual pixel size.
      list.querySelectorAll('.op-matrix-cell').forEach((cellEl) => {
        const k = cellEl.dataset.cell;
        const bucket = cells.get(k) || [];
        if (!bucket.length) return;
        const svg = cellEl.querySelector('svg');
        // Use a fixed viewBox so packing is deterministic; the SVG scales
        // to fit the cell.
        const W = 200, H = 100;
        svg.setAttribute('viewBox', `0 0 ${W} ${H}`);
        // Bubble radius: 5 + log(age) — capped — so a fresh point is small,
        // an old point is big. Range ≈ [5, 16] px in viewBox units.
        const bubbles = bucket.map((op) => {
          const r = 5 + Math.min(11, Math.log2(1 + _opAgeDays(op)) * 1.8);
          const cmp = findComponent(proj, op.component);
          const c   = cmp ? componentColor(cmp.color) : null;
          const fallbackRgb = CRITICALITY_RGB[op.criticality || 'med'];
          const rgb = c ? c.rgb : fallbackRgb;
          return { op, r, rgb };
        });
        packBubbles(bubbles, W, H, 1.3);
        const cxC = W / 2, cyC = H / 2;
        const frag = bubbles.map((b) => {
          // Identifier label placed along the polar angle from the cell
          // centre — so when multiple bubbles share a cell their labels
          // fan outward instead of stacking on top of one another. The
          // anchor flips at the x-axis so labels never run off the cell
          // edge. Monospace + paint-order stroke fill for readability
          // over coloured backgrounds.
          let identSvg = '';
          if (b.op.identifier) {
            const dx = b.x - cxC, dy = b.y - cyC;
            const len = Math.sqrt(dx * dx + dy * dy) || 1;
            const offset = b.r + 4;
            const lx = Math.max(2, Math.min(W - 2, b.x + (dx / len) * offset));
            const ly = Math.max(8, Math.min(H - 2, b.y + (dy / len) * offset));
            const anchor = dx >= 0 ? 'start' : 'end';
            identSvg = `<text class="op-bubble-ident" x="${lx.toFixed(1)}" y="${ly.toFixed(1)}" text-anchor="${anchor}" dy="3">${escapeHTML(b.op.identifier)}</text>`;
          }
          return `<g class="op-bubble-g" data-op-id="${escapeHTML(b.op.id)}">` +
            `<circle class="op-bubble" data-op-id="${escapeHTML(b.op.id)}" cx="${b.x.toFixed(1)}" cy="${b.y.toFixed(1)}" r="${b.r.toFixed(1)}" fill="rgba(${b.rgb}, .55)" stroke="rgb(${b.rgb})" stroke-width="1"></circle>` +
            identSvg +
          `</g>`;
        }).join('');
        svg.innerHTML = frag;
      });
      wireBubbleClicks(list);
      wireOpHoverCards(list);

      // Make the whole matrix (an HTML grid) right-click copyable.
      const matrixEl = list.querySelector('.op-matrix');
      if (matrixEl) {
        matrixEl.classList.add('copyable-chart');
        matrixEl.dataset.chart = 'op-matrix';
        registerChartData('op-matrix', () => ({
          headers: ['Criticality', 'Priority', 'Identifier', 'Title', 'Component', 'Age (days)'],
          rows: items.map((op) => [
            CRITICALITY_LABEL[op.criticality] || op.criticality || '',
            priorityLevel(op.priorityLevel).label,
            op.identifier || '',
            op.title || '',
            findComponent(proj, op.component)?.name || '',
            _opAgeDays(op),
          ]),
        }));
      }
    }

    // ─── Bubbles view: single packed circle pile, importance-sized ────
    function drawBubblesView(items) {
      const list = $('#opList');
      registerChartData('op-bubbles', () => ({
        headers: ['Identifier', 'Title', 'Criticality', 'Priority', 'Component', 'Composite importance', 'Age (days)'],
        rows: items.map((op) => [
          op.identifier || '', op.title || '',
          CRITICALITY_LABEL[op.criticality] || op.criticality || '',
          priorityLevel(op.priorityLevel).label,
          findComponent(proj, op.component)?.name || '',
          Number(_opImportance(op).toFixed(2)),
          _opAgeDays(op),
        ]),
      }));
      // Bigger natural canvas (1600 × 900) than before so bubbles have
      // room to breathe; the wrapper scrolls horizontally + vertically
      // when the zoomed SVG exceeds the visible area. Sort largest first
      // so the simulation has a stable order.
      const baseW = 1600, baseH = 900;
      const bubbles = items.slice()
        .sort((a, b) => _opImportance(b) - _opImportance(a))
        .map((op) => {
          // Slightly larger base radius — more space for labels inside.
          const r   = 14 + Math.sqrt(_opImportance(op)) * 6;
          const cmp = findComponent(proj, op.component);
          const c   = cmp ? componentColor(cmp.color) : null;
          const fallbackRgb = CRITICALITY_RGB[op.criticality || 'med'];
          const rgb = c ? c.rgb : fallbackRgb;
          return { op, r, rgb };
        });
      // pad = 14 (was 2): bubbles keep a generous gap so neighbouring
      // labels don't visually run into each other.
      packBubbles(bubbles, baseW, baseH, 14);
      const fontFor = (r) => Math.max(11, Math.min(15, Math.round(r * 0.34)));
      const labelOf = (op, r) => {
        const t = (op.title || '').trim();
        // Bigger bubbles can carry longer labels — cap ramps with radius
        // so giant bubbles can show ~52 chars while small ones still
        // clamp at ~14.
        const maxLen = Math.max(14, Math.min(56, Math.round(r * 1.7)));
        return t.length > maxLen ? t.slice(0, maxLen - 2) + '…' : t;
      };
      // Multi-line label support — break the title every ~ceil(r) chars
      // so the text wraps inside the circle instead of running off the
      // edge. Bigger bubbles → more lines.
      const wrapLines = (op, r) => {
        const max = Math.max(10, Math.min(36, Math.round(r * 1.1)));
        const words = (op.title || '').trim().split(/\s+/);
        const lines = [];
        let line = '';
        for (const w of words) {
          if (!line.length) { line = w; continue; }
          if ((line + ' ' + w).length <= max) line += ' ' + w;
          else { lines.push(line); line = w; }
        }
        if (line) lines.push(line);
        // Bigger bubbles get up to 3 lines; smaller get 1.
        const maxLines = r > 38 ? 3 : r > 26 ? 2 : 1;
        if (lines.length > maxLines) {
          lines.length = maxLines;
          lines[maxLines - 1] = (lines[maxLines - 1] || '').replace(/.{0,3}$/, '…');
        }
        return lines;
      };
      const zoom = Math.max(0.4, Math.min(3, opFilterState.bubbleZoom || 1));
      const renderW = Math.round(baseW * zoom);
      const renderH = Math.round(baseH * zoom);
      list.innerHTML = `
        <div class="op-bubbles-shell">
          <div class="op-bubbles-toolbar">
            <button type="button" class="ghost" id="opBzOut"   title="Zoom out (Ctrl+wheel)">−</button>
            <span class="op-bubbles-zoom-val" id="opBzVal">${Math.round(zoom * 100)}%</span>
            <button type="button" class="ghost" id="opBzIn"    title="Zoom in (Ctrl+wheel)">+</button>
            <button type="button" class="ghost" id="opBzReset" title="Reset to 100%">100%</button>
            <button type="button" class="ghost" id="opBzFit"   title="Fit width">⤢ Fit</button>
            <span class="op-leg-hint" style="margin-left: 10px;">Bubble size = composite importance · Colour = component · Click any bubble to jump to its row</span>
          </div>
          <div class="op-bubbles-scroll" id="opBubblesScroll">
            <svg class="op-bubbles-svg copyable-chart" data-chart="op-bubbles" id="opBubblesSvg" viewBox="0 0 ${baseW} ${baseH}" width="${renderW}" height="${renderH}">
              ${bubbles.map((b) => {
                let labelSvg = '';
                if (b.r > 14) {
                  const fs     = fontFor(b.r);
                  const lines  = wrapLines(b.op, b.r);
                  // Identifier renders as a compact monospace caption
                  // above the title block when the bubble is big enough
                  // to comfortably fit it. Skip on the smallest sizes so
                  // text doesn't crowd out the title.
                  const showIdent = b.op.identifier && b.r > 22;
                  const identFs  = Math.max(9, Math.round(fs * 0.78));
                  const identGap = showIdent ? identFs * 1.25 : 0;
                  const startDy  = -((lines.length - 1) * fs * 0.55) - identGap;
                  const identSvg = showIdent
                    ? `<text class="op-bubble-ident-in" x="${b.x.toFixed(1)}" y="${(b.y + startDy - fs * 0.4).toFixed(1)}" text-anchor="middle" font-size="${identFs}">${escapeHTML(b.op.identifier)}</text>`
                    : '';
                  labelSvg = identSvg + lines.map((ln, i) =>
                    `<text class="op-bubble-lbl" x="${b.x.toFixed(1)}" y="${(b.y + startDy + i * fs * 1.1 + 4).toFixed(1)}" text-anchor="middle" font-size="${fs}">${escapeHTML(ln)}</text>`
                  ).join('');
                } else if (b.op.identifier) {
                  // Tiny bubble: render the identifier just below the
                  // circle so users can still identify it visually.
                  labelSvg = `<text class="op-bubble-ident" x="${b.x.toFixed(1)}" y="${(b.y + b.r + 10).toFixed(1)}" text-anchor="middle">${escapeHTML(b.op.identifier)}</text>`;
                }
                return `
                  <g class="op-bubble-g" data-op-id="${escapeHTML(b.op.id)}">
                    <circle class="op-bubble" cx="${b.x.toFixed(1)}" cy="${b.y.toFixed(1)}" r="${b.r.toFixed(1)}" fill="rgba(${b.rgb}, .50)" stroke="rgb(${b.rgb})" stroke-width="1.5"></circle>
                    ${labelSvg}
                  </g>`;
              }).join('')}
            </svg>
          </div>
        </div>`;
      wireBubbleClicks(list);
      wireOpHoverCards(list);
      // Zoom controls — buttons re-set opFilterState.bubbleZoom and
      // re-render only the bubbles view to keep the packing stable.
      const applyZoom = (z) => {
        opFilterState.bubbleZoom = Math.max(0.4, Math.min(3, z));
        drawBubblesView(items);
      };
      list.querySelector('#opBzOut')?.addEventListener('click',   () => applyZoom(zoom - 0.2));
      list.querySelector('#opBzIn')?.addEventListener('click',    () => applyZoom(zoom + 0.2));
      list.querySelector('#opBzReset')?.addEventListener('click', () => applyZoom(1));
      list.querySelector('#opBzFit')?.addEventListener('click',   () => {
        const sc = list.querySelector('#opBubblesScroll');
        if (sc) applyZoom(sc.clientWidth / baseW);
      });
      // Ctrl/⌘ + wheel inside the scroller zooms; plain wheel scrolls
      // (default behaviour).
      list.querySelector('#opBubblesScroll')?.addEventListener('wheel', (e) => {
        if (!(e.ctrlKey || e.metaKey)) return;
        e.preventDefault();
        const step = e.deltaY < 0 ? 0.12 : -0.12;
        applyZoom(zoom + step);
      }, { passive: false });
    }

    // Both bubble views share the same click affordance: clicking a
    // bubble switches to List view filtered to that one point's title,
    // so the user can instantly inspect / edit the underlying card. The
    // q-filter is set so the list narrows to that exact item; clearing
    // the filter (Reset) brings everything back.
    function wireBubbleClicks(scope) {
      scope.querySelectorAll('[data-op-id]').forEach((el) => {
        el.addEventListener('click', (e) => {
          e.preventDefault();
          const id = el.dataset.opId;
          const op = (proj.openPoints || []).find((x) => x.id === id);
          if (!op) return;
          opFilterState.view = 'list';
          opFilterState.q = op.title;
          const fq = $('#opFilterQ'); if (fq) fq.value = op.title;
          const seg = $('#opViewSeg');
          if (seg) seg.querySelectorAll('[data-op-view]').forEach((x) =>
            x.classList.toggle('active', x.dataset.opView === 'list'));
          draw();
          setTimeout(() => {
            const card = list.querySelector(`.op-item[data-id="${CSS.escape(id)}"]`);
            if (card) {
              card.scrollIntoView({ behavior: 'smooth', block: 'center' });
              card.classList.add('op-flash');
              setTimeout(() => card.classList.remove('op-flash'), 1400);
            }
          }, 30);
        });
      });
    }

    // Rich hover card for open-point bubbles. Replaces the basic SVG
    // <title> tooltip with a styled overlay that shows the identifier,
    // title, component, criticality, priority, age, step progress, and
    // a notes snippet. The card is a singleton appended to <body> so
    // it can escape the cell SVG's clip and follow the cursor.
    function wireOpHoverCards(scope) {
      let card = document.getElementById('opHoverCard');
      if (!card) {
        card = document.createElement('div');
        card.id = 'opHoverCard';
        card.className = 'op-hover-card';
        card.setAttribute('role', 'tooltip');
        document.body.appendChild(card);
      }
      const hide = () => { card.classList.remove('show'); };
      const show = (op, e) => {
        const cmp       = findComponent(proj, op.component);
        const compName  = cmp ? cmp.name : '— no component —';
        const compRgb   = cmp ? componentColor(cmp.color).rgb : null;
        const ageDays   = _opAgeDays(op);
        const steps     = op.steps || [];
        const doneCount = steps.filter((s) => s.done).length;
        const stepsLbl  = steps.length ? `${doneCount}/${steps.length} steps` : 'no steps yet';
        const stepsPct  = steps.length ? Math.round(doneCount / steps.length * 100) : 0;
        const critKey   = op.criticality || 'med';
        const critRgb   = CRITICALITY_RGB[critKey];
        const prio      = priorityLevel(op.priorityLevel);
        // Notes excerpt — strip HTML, collapse whitespace, cap at ~180 chars.
        const noteText  = (op.notes || '').replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').trim();
        const noteSnip  = noteText.length > 180 ? noteText.slice(0, 178) + '…' : noteText;
        card.innerHTML = `
          ${op.identifier ? `<div class="hc-ident">${escapeHTML(op.identifier)}</div>` : ''}
          <div class="hc-title">${escapeHTML(op.title || '(untitled)')}</div>
          <div class="hc-chips">
            ${compName ? `<span class="hc-chip"${compRgb ? ` style="background:rgba(${compRgb},.18);color:rgb(${compRgb});border-color:rgba(${compRgb},.5)"` : ''}>${escapeHTML(compName)}</span>` : ''}
            <span class="hc-chip" style="background:rgba(${critRgb},.18);color:rgb(${critRgb});border-color:rgba(${critRgb},.5)">${escapeHTML(CRITICALITY_LABEL[critKey])} criticality</span>
            <span class="hc-chip" style="background:rgba(${prio.rgb},.18);color:rgb(${prio.rgb});border-color:rgba(${prio.rgb},.5)">${escapeHTML(prio.label)} priority</span>
          </div>
          <div class="hc-meta">
            <span title="Age in days">Age ${ageDays}d</span>
            <span class="hc-sep">·</span>
            <span title="Resolution step progress">${escapeHTML(stepsLbl)}${steps.length ? ` (${stepsPct}%)` : ''}</span>
          </div>
          ${noteSnip ? `<div class="hc-notes">${escapeHTML(noteSnip)}</div>` : ''}
          <div class="hc-hint">Click to jump to the list row</div>
        `;
        card.classList.add('show');
        position(e);
      };
      const position = (e) => {
        const pad = 12;
        const rect = card.getBoundingClientRect();
        const x = Math.min(window.innerWidth - rect.width - pad, e.clientX + 14);
        const y = Math.min(window.innerHeight - rect.height - pad, e.clientY + 14);
        card.style.left = Math.max(pad, x) + 'px';
        card.style.top  = Math.max(pad, y) + 'px';
      };
      scope.querySelectorAll('[data-op-id]').forEach((el) => {
        const id = el.dataset.opId;
        el.addEventListener('mouseenter', (e) => {
          const op = (proj.openPoints || []).find((x) => x.id === id);
          if (op) show(op, e);
        });
        el.addEventListener('mousemove', position);
        el.addEventListener('mouseleave', hide);
      });
    }

    draw();
  }

  // Simple force-style packing for circles. Iterates centre-attraction +
  // collision resolution; plenty good for ~300 bubbles. Mutates each
  // entry to add { x, y } in [r..W-r] × [r..H-r]. `pad` adds margin so
  // bubbles don't kiss their neighbours.
  function packBubbles(bubbles, W, H, pad = 1) {
    if (!bubbles.length) return;
    const cx = W / 2, cy = H / 2;
    // Initial positions — concentric ring spread, biggest first at the
    // centre so the layout feels "weighted".
    bubbles.forEach((b, i) => {
      const a = (i * 137.508) * Math.PI / 180; // golden-angle spiral
      const d = Math.sqrt(i + 0.5) * (Math.min(W, H) / Math.sqrt(bubbles.length + 1) / 2);
      b.x = cx + d * Math.cos(a);
      b.y = cy + d * Math.sin(a);
    });
    const iters = 90;
    for (let it = 0; it < iters; it++) {
      // Centre-pull (gentle, decaying with iteration so we settle).
      const pull = 0.4 * (1 - it / iters) + 0.05;
      bubbles.forEach((b) => {
        b.x += (cx - b.x) * pull * 0.04;
        b.y += (cy - b.y) * pull * 0.04;
      });
      // Pairwise collision separation.
      for (let i = 0; i < bubbles.length; i++) {
        const a = bubbles[i];
        for (let j = i + 1; j < bubbles.length; j++) {
          const c = bubbles[j];
          const dx = c.x - a.x, dy = c.y - a.y;
          const d = Math.sqrt(dx * dx + dy * dy) || 0.01;
          const minD = a.r + c.r + pad;
          if (d < minD) {
            const overlap = (minD - d) / 2;
            const ux = dx / d, uy = dy / d;
            a.x -= ux * overlap;
            a.y -= uy * overlap;
            c.x += ux * overlap;
            c.y += uy * overlap;
          }
        }
      }
      // Clamp to bounds.
      bubbles.forEach((b) => {
        b.x = Math.max(b.r + 2, Math.min(W - b.r - 2, b.x));
        b.y = Math.max(b.r + 2, Math.min(H - b.r - 2, b.y));
      });
    }
  }

  /* ---------------------------- Register ----------------------------- */

  // Persistent sort state (so it survives navigation away and back)
  // Multi-column sort: regState.sortStack is an ordered list of
  //   { col, dir }
  // entries that drive a stable, lexicographic comparator. The first
  // entry is the primary sort, the rest are tie-breakers in order. An
  // empty stack means "manual" — drag-reorder works as the source of
  // truth and we render the project's actions in their array order.
  const regState = { sortStack: [{ col: 'due', dir: 'asc' }] };

  // Predicted completion = explicit override or the due date.
  function predictedCompletion(a) {
    return a.predictedCompletion || a.due || '';
  }
  // Actual completion = explicit override, otherwise mined from history
  // (latest "Status: ... → done"), otherwise updatedAt for already-done.
  function actualCompletion(a) {
    if (a.actualCompletion) return a.actualCompletion;
    if (a.status === 'done') {
      const entry = (a.history || []).filter((h) => /Status:.*→\s*done/.test(h.what)).pop();
      return entry?.at || a.updatedAt || '';
    }
    return '';
  }

  function regSortValue(a, col, proj) {
    switch (col) {
      case 'title':          return (a.title || '').toLowerCase();
      case 'component':      return (findComponent(proj, a.component)?.name || 'zzz').toLowerCase();
      case 'owner':          return personName(a.owner).toLowerCase();
      // Open states first (todo · doing · blocked), then both closed
       // states grouped at the same extreme (done · cancelled). Sorting
       // ascending puts what's open at the top; descending pushes the
       // closed pile up. Cancelled was previously missing from the
       // index, which broke the order entirely.
      case 'status':         return ['todo','doing','blocked','done','cancelled'].indexOf(a.status);
      case 'priority':       return ['critical','high','med','low'].indexOf(a.priorityLevel || 'med');
      case 'due':            return a.due || '9999-99-99';
      case 'predicted':      return predictedCompletion(a) || '9999-99-99';
      case 'actual':         return actualCompletion(a) || '9999-99-99';
      case 'commitment':     return typeof a.commitment === 'number' ? a.commitment : 100;
      case 'updatedAt':      return a.updatedAt || '0000-00-00';
      case 'originatorDate': return a.originatorDate || '0000-00-00';
    }
    return 0;
  }

  // Per-render selection set — held in module scope so it survives a
  // re-render of the same view (e.g. after a sort change), but reset
  // on view switch and on filter/search changes.
  const _regSelection = new Set();
  let _regLastClickedId = null; // for shift-click range selection

  function renderRegister(root) {
    const proj = curProject();
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">${escapeHTML(proj.name)} — Register</div>
          <div class="page-sub">Editable table — change any cell to update. KPIs above reflect the current filters. Click the row checkbox or shift-click rows to select multiple, then use the bulk toolbar.</div>
          ${_dashDrill ? `
            <div class="drill-chip" role="status">
              <span class="drill-chip-lbl">Drilled from Dashboard:</span>
              <b>${escapeHTML(_dashDrill.label)}</b>
              <button type="button" class="drill-chip-clear" id="btnClearDrill" title="Clear the drill scope">×</button>
            </div>` : ''}
        </div>
        <div class="page-actions">
          <button class="ghost" id="btnOpenArchive" title="Open the Archive view">⌫ Archive${(proj.actions || []).filter((a) => a.archivedAt).length ? ` <span class="badge-count">${(proj.actions || []).filter((a) => a.archivedAt).length}</span>` : ''}</button>
          <button class="ghost" id="btnArchiveDone" title="Move all currently visible done actions to Archive">⌫ Archive done</button>
          <button class="ghost" id="btnAddAction">+ Action</button>
        </div>
      </div>
      <div class="reg-kpis" id="regKpis"></div>
      <div class="register">
        <div class="reg-head">
          <span class="reg-col-check"><input type="checkbox" id="regSelectAll" title="Select / deselect all visible" /></span>
          <button class="reg-col" data-col="title"          title="Click to sort · Shift-click to add as secondary · Right-click to remove">Title</button>
          <button class="reg-col" data-col="component"      title="Click to sort · Shift-click to add as secondary · Right-click to remove">Component</button>
          <button class="reg-col" data-col="owner"          title="Click to sort · Shift-click to add as secondary · Right-click to remove">Owner</button>
          <button class="reg-col" data-col="status"         title="Click to sort · Shift-click to add as secondary · Right-click to remove">Status</button>
          <button class="reg-col" data-col="priority"       title="Click to sort · Shift-click to add as secondary · Right-click to remove">Priority</button>
          <button class="reg-col" data-col="due"            title="Click to sort · Shift-click to add as secondary · Right-click to remove">Due</button>
          <button class="reg-col" data-col="predicted"      title="Predicted completion (defaults to due) · Shift-click to add as secondary · Right-click to remove">Predicted</button>
          <button class="reg-col" data-col="originatorDate" title="When this action was originated · Shift-click to add as secondary · Right-click to remove">Originator date</button>
          <span class="reg-col-spacer" aria-hidden="true"></span>
        </div>
        <div class="reg-body" id="regBody"></div>
      </div>
      <div class="bulk-bar" id="bulkBar" hidden>
        <span class="bulk-bar-count"><span id="bulkCount">0</span> selected</span>
        <span class="bulk-bar-divider"></span>
        <select class="bulk-bar-select" id="bulkStatus"><option value="">Set status…</option>${STATUSES.map((s) => `<option value="${s.id}">${s.name}</option>`).join('')}</select>
        <select class="bulk-bar-select" id="bulkOwner"><option value="">Set owner…</option>${actorOptionsHTML('')}</select>
        <select class="bulk-bar-select" id="bulkComp"><option value="">Set component…</option><option value="__none__">— None —</option>${(proj.components || []).map((c) => `<option value="${c.id}">${escapeHTML(c.name)}</option>`).join('')}</select>
        <select class="bulk-bar-select" id="bulkPrio"><option value="">Set priority…</option>${PRIORITY_LEVELS.map((p) => `<option value="${p.id}">${p.label}</option>`).join('')}</select>
        <button class="ghost" id="bulkSnooze7" title="Snooze 7 days">⏰ +7 d</button>
        <button class="ghost" id="bulkArchive" title="Move selected to Archive">⌫ Archive</button>
        <span class="bulk-bar-divider"></span>
        <button class="ghost" id="bulkClear" title="Clear selection">×</button>
      </div>`;
    root.appendChild(view);

    function drawKpis() {
      const filtered = (proj.actions || []).filter(actionMatchesFilters);
      const total = filtered.length;
      const today = todayISO();
      const cnt = { todo: 0, doing: 0, blocked: 0, done: 0, cancelled: 0 };
      let overdue = 0, soon = 0, openCmtSum = 0, openCount = 0;
      const byComp = new Map();
      filtered.forEach((a) => {
        cnt[a.status] = (cnt[a.status] || 0) + 1;
        if (!isClosedStatus(a.status)) {
          if (a.due) {
            const dd = dayDiff(a.due, today);
            if (dd < 0) overdue++;
            else if (dd <= 7) soon++;
          }
          openCmtSum += (typeof a.commitment === 'number') ? a.commitment : 100;
          openCount++;
        }
        const k = a.component || '__none';
        byComp.set(k, (byComp.get(k) || 0) + 1);
      });
      const donePct = total ? Math.round((cnt.done / total) * 100) : 0;
      const avgOpenCmt = openCount ? Math.round(openCmtSum / openCount) : 0;
      // Per-person work-equivalent: total open commitment ÷ number of people
      const personLoad = state.people.length ? Math.round(openCmtSum / state.people.length) : 0;

      // Donut
      const r = 26, cx = 32, cy = 32;
      const circ = 2 * Math.PI * r;
      let off = 0;
      const slices = [
        { v: cnt.done,    color: 'var(--ok)',     name: 'Done' },
        { v: cnt.doing,   color: 'var(--accent)', name: 'In progress' },
        { v: cnt.blocked, color: 'var(--bad)',    name: 'Blocked' },
        { v: cnt.todo,    color: 'var(--neutral)',name: 'Not started' },
      ];
      const statusKeys = ['done', 'doing', 'blocked', 'todo'];
      const donutSegs = slices.map((s, i) => {
        const len = total ? (s.v / total) * circ : 0;
        const seg = `<circle class="donut-slice" data-set-status="${statusKeys[i]}" cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="${s.color}" stroke-width="9" stroke-dasharray="${len.toFixed(2)} ${(circ - len).toFixed(2)}" stroke-dashoffset="${(-off).toFixed(2)}" transform="rotate(-90 ${cx} ${cy})"><title>${s.name}: ${s.v} (click to filter)</title></circle>`;
        off += len;
        return seg;
      }).join('');

      // Throughput sparkline (last 8 weeks)
      const today2 = new Date(); today2.setHours(0, 0, 0, 0);
      let weekStart = new Date(today2);
      while (weekStart.getDay() !== 1) weekStart = new Date(weekStart.getTime() - dayMs);
      const startW = new Date(weekStart.getTime() - 7 * 7 * dayMs);
      const buckets = new Array(8).fill(0);
      const re = /Status:.*→\s*done/;
      filtered.forEach((a) => {
        const e = (a.history || []).filter((h) => re.test(h.what)).pop();
        const w = e ? e.at : (a.status === 'done' ? a.updatedAt : null);
        if (!w) return;
        const idx = Math.floor((parseDate(w) - startW) / dayMs / 7);
        if (idx >= 0 && idx < 8) buckets[idx]++;
      });
      const maxB = Math.max(1, ...buckets);
      const sparkBars = buckets.map((v, i) => {
        const x = i * 11 + 1;
        const h = (v / maxB) * 28;
        const y = 32 - h;
        return `<rect x="${x}" y="${y}" width="9" height="${h.toFixed(1)}" rx="1" fill="var(--ok)" opacity="0.85"><title>${v} done</title></rect>`;
      }).join('');

      // Opened vs Completed — daily line chart over the past 30 days
      const days30 = 30;
      const startDay = new Date(today2.getTime() - (days30 - 1) * dayMs);
      const opened = new Array(days30).fill(0);
      const closed = new Array(days30).fill(0);
      filtered.forEach((a) => {
        if (a.createdAt) {
          const idx = Math.floor((parseDate(a.createdAt) - startDay) / dayMs);
          if (idx >= 0 && idx < days30) opened[idx]++;
        }
        const ent = (a.history || []).filter((h) => re.test(h.what)).pop();
        const when = ent?.at || (a.status === 'done' ? a.updatedAt : null);
        if (when) {
          const idx = Math.floor((parseDate(when) - startDay) / dayMs);
          if (idx >= 0 && idx < days30) closed[idx]++;
        }
      });
      const totalOpened = opened.reduce((s, v) => s + v, 0);
      const totalClosed = closed.reduce((s, v) => s + v, 0);
      // Compute total-open count over time by walking back from "today"
      const todayOpenCount = filtered.filter((x) => x.status !== 'done').length;
      const openSeries = new Array(days30).fill(0);
      let runningOpen = todayOpenCount;
      for (let i = days30 - 1; i >= 0; i--) {
        openSeries[i] = runningOpen;
        runningOpen = runningOpen - opened[i] + closed[i];
      }
      const lcW = 220, lcH = 70, lcPadL = 4, lcPadR = 4, lcPadT = 4, lcPadB = 4;
      const lcInnerW = lcW - lcPadL - lcPadR, lcInnerH = lcH - lcPadT - lcPadB;
      const lcMax = Math.max(1, ...opened, ...closed, ...openSeries);
      const lcX = (i) => lcPadL + (i / (days30 - 1)) * lcInnerW;
      const lcY = (v) => lcPadT + lcInnerH - (v / lcMax) * lcInnerH;
      const linePath = (arr) => arr.map((v, i) =>
        `${i === 0 ? 'M' : 'L'} ${lcX(i).toFixed(1)} ${lcY(v).toFixed(1)}`).join(' ');
      const openAreaPath = `M ${lcX(0).toFixed(1)} ${lcY(openSeries[0]).toFixed(1)} ` +
        openSeries.slice(1).map((v, i) => `L ${lcX(i + 1).toFixed(1)} ${lcY(v).toFixed(1)}`).join(' ') +
        ` L ${lcX(days30 - 1).toFixed(1)} ${lcY(0).toFixed(1)} L ${lcX(0).toFixed(1)} ${lcY(0).toFixed(1)} Z`;
      const openedPath = linePath(opened);
      const closedPath = linePath(closed);
      const lcTodayX = lcX(days30 - 1).toFixed(1);
      const lineChartSVG = `
        <svg class="kpi-linechart" viewBox="0 0 ${lcW} ${lcH}" preserveAspectRatio="none">
          <line class="lc-baseline" x1="${lcPadL}" x2="${lcW - lcPadR}" y1="${lcY(0).toFixed(1)}" y2="${lcY(0).toFixed(1)}" />
          <path class="lc-open-area" d="${openAreaPath}" />
          <path class="lc-opened" d="${openedPath}" />
          <path class="lc-closed" d="${closedPath}" />
          <line class="lc-today" x1="${lcTodayX}" x2="${lcTodayX}" y1="${lcPadT}" y2="${lcH - lcPadB}" />
        </svg>`;

      // Component distribution stacked bar
      const compEntries = [...byComp.entries()].sort((a, b) => b[1] - a[1]);
      const compBars = compEntries.map(([cid, n]) => {
        const cmp = cid === '__none' ? null : findComponent(proj, cid);
        const c = cmp ? componentColor(cmp.color) : null;
        const w = total ? (n / total) * 100 : 0;
        const color = c ? `rgba(${c.rgb},.85)` : 'var(--neutral)';
        const name = cmp ? cmp.name : 'Unassigned';
        const filter = cid === '__none' ? '__none__' : cid;
        return `<div class="seg clickable" data-set-component="${filter}" style="width:${w}%; background:${color};" title="${escapeHTML(name)}: ${n} (click to filter)"></div>`;
      }).join('');
      const compLegend = compEntries.slice(0, 6).map(([cid, n]) => {
        const cmp = cid === '__none' ? null : findComponent(proj, cid);
        const c = cmp ? componentColor(cmp.color) : null;
        const color = c ? `rgba(${c.rgb},.95)` : 'var(--neutral)';
        const filter = cid === '__none' ? '__none__' : cid;
        return `<span class="cmp-leg-item clickable" data-set-component="${filter}"><span class="dot" style="background:${color}"></span>${escapeHTML(cmp ? cmp.name : 'Unassigned')} <b>${n}</b></span>`;
      }).join('');

      $('#regKpis').innerHTML = `
        <div class="reg-kpi clickable" data-clear-filters title="Click to clear filters">
          <div class="kpi-num">${total}</div>
          <div class="kpi-lbl">${total === 1 ? 'action' : 'actions'} (filtered)</div>
        </div>
        <div class="reg-kpi donut-kpi">
          <svg class="kpi-donut" viewBox="0 0 64 64">
            <circle cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="var(--bg-3)" stroke-width="9" />
            ${donutSegs}
            <text x="${cx}" y="${cy + 4}" text-anchor="middle" class="kpi-donut-num">${donePct}%</text>
          </svg>
          <div class="kpi-donut-legend">
            <span class="cmp-leg-item clickable" data-set-status="done"><span class="dot" style="background:var(--ok)"></span>Done <b>${cnt.done}</b></span>
            <span class="cmp-leg-item clickable" data-set-status="doing"><span class="dot" style="background:var(--accent)"></span>Doing <b>${cnt.doing}</b></span>
            <span class="cmp-leg-item clickable" data-set-status="blocked"><span class="dot" style="background:var(--bad)"></span>Blocked <b>${cnt.blocked}</b></span>
            <span class="cmp-leg-item clickable" data-set-status="todo"><span class="dot" style="background:var(--neutral)"></span>Todo <b>${cnt.todo}</b></span>
          </div>
        </div>
        <div class="reg-kpi clickable" data-set-due="late" title="Click to filter to overdue">
          <div class="kpi-num ${overdue > 0 ? 'bad' : ''}">${overdue}</div>
          <div class="kpi-lbl">overdue</div>
          <div class="kpi-sub clickable-inline" data-set-due="week" title="Click to filter to due this week">${soon} due ≤ 7d</div>
        </div>
        <div class="reg-kpi grow">
          <div class="kpi-lbl">By component</div>
          <div class="kpi-stack">${compBars}</div>
          <div class="cmp-legend">${compLegend}</div>
        </div>
        <div class="reg-kpi grow">
          <div class="kpi-lbl">Opened vs completed (30d)</div>
          ${lineChartSVG}
          <div class="cmp-legend">
            <span class="cmp-leg-item"><span class="dot" style="background:rgba(154,161,184,.7)"></span>Open now <b>${todayOpenCount}</b></span>
            <span class="cmp-leg-item"><span class="dot" style="background:var(--accent)"></span>Opened <b>${totalOpened}</b></span>
            <span class="cmp-leg-item"><span class="dot" style="background:var(--ok)"></span>Completed <b>${totalClosed}</b></span>
            <span class="cmp-leg-item"><span class="dot" style="background:var(--text-faint);opacity:.5"></span>today</span>
          </div>
        </div>
        `;

      // Wire KPI click filters (idempotent — replaces previous listener).
      // Click on a KPI TOGGLES that filter: setting the value when off,
      // clearing it when the same value is already the active filter. So
      // re-clicking the same KPI removes the filter — no need for a
      // separate "clear" gesture, though dblclick keeps the same effect
      // for muscle-memory.
      const kpiPanel = $('#regKpis');
      const handleKpi = (e) => {
        const el = e.target.closest('[data-set-status],[data-set-due],[data-set-component],[data-clear-filters]');
        if (!el) return;
        e.preventDefault();
        if (el.dataset.clearFilters !== undefined) {
          applyTopbarFilter({ clearAll: true });
          return;
        }
        const set = {};
        const cur = {
          status:    $('#filterStatus')?.value    || '',
          due:       $('#filterDue')?.value       || '',
          component: $('#filterComponent')?.value || '',
        };
        if (el.dataset.setStatus !== undefined) {
          set.status = (cur.status === el.dataset.setStatus) ? '' : el.dataset.setStatus;
        }
        if (el.dataset.setDue !== undefined) {
          set.due = (cur.due === el.dataset.setDue) ? '' : el.dataset.setDue;
        }
        if (el.dataset.setComponent !== undefined) {
          set.component = (cur.component === el.dataset.setComponent) ? '' : el.dataset.setComponent;
        }
        applyTopbarFilter(set);
      };
      kpiPanel.onclick    = handleKpi;
      kpiPanel.ondblclick = handleKpi;

      // Visually mark KPIs whose value matches the current filter so the
      // user can see at a glance which one is "on" and that re-clicking
      // will turn it off.
      const cur = {
        status:    $('#filterStatus')?.value    || '',
        due:       $('#filterDue')?.value       || '',
        component: $('#filterComponent')?.value || '',
      };
      $$('[data-set-status]', kpiPanel).forEach((el) => {
        el.classList.toggle('is-active-filter', el.dataset.setStatus === cur.status && cur.status !== '');
      });
      $$('[data-set-due]', kpiPanel).forEach((el) => {
        el.classList.toggle('is-active-filter', el.dataset.setDue === cur.due && cur.due !== '');
      });
      $$('[data-set-component]', kpiPanel).forEach((el) => {
        el.classList.toggle('is-active-filter', el.dataset.setComponent === cur.component && cur.component !== '');
      });
    }

    function rowHTML(a) {
      const cmp = findComponent(proj, a.component);
      const c = cmp ? componentColor(cmp.color) : null;
      const dueCls = statusOfDue(a.due, a.status);
      const stat = STATUSES.find((s) => s.id === a.status);
      const lvl = priorityLevel(a.priorityLevel);
      // Visual encoding: left edge = COMPONENT (allocation) so the user
      // can scan the table for "what kind of work" at a glance, and the
      // ROW BACKGROUND = STATUS so in-trouble actions (overdue, blocked)
      // pop visually with a tinted fill.
      const tintProps = c ? `--comp-rgb: ${c.rgb};` : '';
      const tint = tintProps ? ` style="${tintProps}"` : '';
      // Lateness as a 'drag tail' — a thin red trail whose width grows
      // with days late, capped at 30 days so a 90-d-late item doesn't
      // bully the title cell. Replaces the old ⏰ emoji.
      const isOverdue = a.status !== 'done' && a.due && dayDiff(a.due, todayISO()) < 0;
      const lateDays = isOverdue ? Math.abs(dayDiff(a.due, todayISO())) : 0;
      const dragTail = isOverdue
        ? `<span class="reg-late-tail" style="--days:${Math.min(lateDays, 30)};" title="Late by ${lateDays} day${lateDays === 1 ? '' : 's'} · was due ${escapeHTML(fmtDate(a.due))}"><span class="reg-late-icon" aria-hidden="true">⏰</span><span class="reg-late-num">+${lateDays}d</span></span>`
        : '';
      const isDone      = a.status === 'done';
      const isCancelled = a.status === 'cancelled';
      const checked = _regSelection.has(a.id) ? 'checked' : '';
      return `
        <div class="reg-row prio-${lvl.id} status-${a.status} ${isOverdue ? 'is-overdue' : ''} ${isDone ? 'is-done' : ''} ${isCancelled ? 'is-cancelled' : ''} ${_regSelection.has(a.id) ? 'is-selected' : ''}" data-id="${a.id}"${tint}>
          <div class="reg-cell reg-cell-check">
            <input type="checkbox" class="reg-row-check" data-row-id="${a.id}" ${checked} aria-label="Select row" />
          </div>
          <div class="reg-cell title-cell">
            ${ROW_GRIP_HTML}
            <input type="text" class="reg-inp title-inp" data-field="title" value="${escapeHTML(a.title)}" />
            ${dragTail}
            ${a.notes ? '<span class="tag" title="Has notes">note</span>' : ''}
          </div>
          <div class="reg-cell">
            <select class="reg-inp reg-comp" data-field="component" ${c ? `style="color:rgb(${c.rgb}); border-color:rgba(${c.rgb},.4);"` : ''}>
              <option value="">— None</option>
              ${(proj.components || []).map((m) => `<option value="${m.id}" ${m.id === a.component ? 'selected' : ''}>${escapeHTML(m.name)}</option>`).join('')}
            </select>
          </div>
          <div class="reg-cell">
            <select class="reg-inp" data-field="owner">
              ${actorOptionsHTML(a.owner)}
            </select>
          </div>
          <div class="reg-cell status-cell">
            <span class="col-dot ${stat?.dot}"></span>
            <select class="reg-inp" data-field="status">
              ${STATUSES.map((s) => `<option value="${s.id}" ${s.id === a.status ? 'selected' : ''}>${s.name}</option>`).join('')}
            </select>
          </div>
          <div class="reg-cell priority-cell">
            <select class="reg-inp" data-field="priorityLevel" style="color:rgb(${lvl.rgb});">
              ${PRIORITY_LEVELS.map((p) => `<option value="${p.id}" ${p.id === (a.priorityLevel || 'med') ? 'selected' : ''}>${p.label}</option>`).join('')}
            </select>
          </div>
          <div class="reg-cell due-cell">
            <input type="date" class="reg-inp ${dueCls}" data-field="due" value="${a.due || ''}" />
            ${(() => {
              const slip = baselineSlipDays(a);
              if (slip == null) return '';
              const cls = slip > 0 ? 'warn' : slip < 0 ? 'ok' : 'muted';
              const sign = slip > 0 ? '+' : '';
              const lbl = slip === 0 ? '0d' : `${sign}${slip}d`;
              const title = `Schedule variance vs baseline (${a.baseline.due || 'no date'}): ${slip > 0 ? slip + ' day' + (slip === 1 ? '' : 's') + ' late' : slip < 0 ? Math.abs(slip) + ' day' + (slip === -1 ? '' : 's') + ' early' : 'on baseline'}`;
              return `<span class="reg-var ${cls}" title="${escapeHTML(title)}">${lbl}</span>`;
            })()}
          </div>
          <div class="reg-cell">
            <input type="date" class="reg-inp ${a.predictedCompletion ? 'overridden' : 'derived'}" data-field="predictedCompletion" value="${predictedCompletion(a)}" title="${a.predictedCompletion ? 'Custom predicted date — click 𝕩 to clear' : 'Defaults to due date'}" />
          </div>
          <div class="reg-cell">
            <input type="date" class="reg-inp" data-field="originatorDate" value="${a.originatorDate || a.createdAt || ''}" title="When this action was originated" />
          </div>
          <div class="reg-cell">
            <button class="row-del" title="Delete action" aria-label="Delete">×</button>
          </div>
        </div>`;
    }

    function drawTable() {
      let acts = (proj.actions || []).filter(actionMatchesFilters).slice();
      // Empty sortStack = "manual" mode (drag-reorder source-of-truth).
      // Otherwise compare entries in stack order, returning the first
      // non-zero comparison.
      const stack = regState.sortStack || [];
      if (stack.length) {
        acts.sort((a, b) => {
          for (const s of stack) {
            const av = regSortValue(a, s.col, proj);
            const bv = regSortValue(b, s.col, proj);
            if (av < bv) return s.dir === 'asc' ? -1 : 1;
            if (av > bv) return s.dir === 'asc' ?  1 : -1;
          }
          return 0;
        });
      }
      const body = $('#regBody');
      if (!acts.length) {
        body.innerHTML = '<div class="empty">No actions match the current filters.</div>';
      } else {
        body.innerHTML = acts.map(rowHTML).join('');
      }
      // Reset every header indicator, then paint the current sort stack.
      $$('.reg-col', view).forEach((b) => {
        b.classList.remove('asc', 'desc');
        b.removeAttribute('data-sort-priority');
      });
      stack.forEach((s, i) => {
        const el = view.querySelector(`.reg-col[data-col="${s.col}"]`);
        if (!el) return;
        el.classList.add(s.dir);
        // Show the "1 / 2 / 3" priority badge only when there's more
        // than one sort column — single-column sort doesn't need it.
        if (stack.length > 1) el.setAttribute('data-sort-priority', String(i + 1));
      });
      // Wire drag-to-reorder on the rendered rows
      wireListReorder(body, {
        rowSelector: '.reg-row[data-id]',
        idAttr: 'id',
        getArray: () => proj.actions,
        setOrder: (visibleIds) => {
          // The visibleIds are only currently-displayed rows; rebuild proj.actions
          // by interleaving the new order back into the full array (preserving
          // the relative order of off-screen / filtered-out actions).
          const visibleSet = new Set(visibleIds);
          const queue = visibleIds.slice();
          const next = [];
          (proj.actions || []).forEach((a) => {
            if (visibleSet.has(a.id)) {
              const id = queue.shift();
              const item = (proj.actions || []).find((x) => x.id === id);
              if (item) next.push(item);
            } else {
              next.push(a);
            }
          });
          proj.actions = next;
          // Switch to manual sort so the new order is preserved across re-renders
          regState.sortStack = [];
        },
        commitName: 'register-reorder',
      });
    }

    // Snapshot + lightweight save: don't full-render the view (would lose
    // input focus and table scroll) — just save and refresh KPIs/row tint.
    function snapshotForUndo() {
      undoStack.push(JSON.stringify(state));
      if (undoStack.length > HISTORY_LIMIT) undoStack.shift();
      redoStack = [];
    }
    function applyEdit(id, field, raw) {
      const a = proj.actions.find((x) => x.id === id);
      if (!a) return;
      let changed = false;
      const today = todayISO();
      if (field === 'title') {
        const v = String(raw).trim();
        if (v && v !== a.title) { a.title = v; changed = true; }
      } else if (field === 'component') {
        const v = raw || null;
        if (v !== a.component) { a.component = v; changed = true; }
      } else if (field === 'owner') {
        const v = String(raw);
        if (v && v !== a.owner) {
          a.history.push({ at: today, what: `Owner: ${personName(a.owner)} → ${personName(v)}` });
          a.owner = v; changed = true;
        }
      } else if (field === 'status') {
        const v = String(raw);
        if (v && v !== a.status) {
          const _prev = a.status;
          a.history.push({ at: today, what: `Status: ${a.status} → ${v}` });
          a.status = v; changed = true;
          _syncCompletedAtOnStatus(a, _prev);
          _syncStartDateOnStatus(a, _prev);
        }
      } else if (field === 'due') {
        const v = raw || null;
        if (v !== a.due) {
          a.history.push({ at: today, what: `Due: ${a.due || '—'} → ${v || '—'}` });
          a.due = v; changed = true;
        }
      } else if (field === 'commitment') {
        const v = clamp(parseInt(raw, 10) || 100, 5, 100);
        if (v !== a.commitment) { a.commitment = v; changed = true; }
      } else if (field === 'predictedCompletion') {
        const v = raw || null;
        if ((a.predictedCompletion || null) !== v) { a.predictedCompletion = v; changed = true; }
      } else if (field === 'actualCompletion') {
        const v = raw || null;
        if ((a.actualCompletion || null) !== v) { a.actualCompletion = v; changed = true; }
      } else if (field === 'priorityLevel') {
        const v = PRIORITY_LEVELS.some((p) => p.id === raw) ? raw : 'med';
        if ((a.priorityLevel || 'med') !== v) {
          a.history = a.history || [];
          a.history.push({ at: today, what: `Priority: ${priorityLevel(a.priorityLevel || 'med').label} → ${priorityLevel(v).label}` });
          a.priorityLevel = v;
          changed = true;
        }
      } else if (field === 'originatorDate') {
        const v = raw || null;
        if ((a.originatorDate || null) !== v) {
          a.history = a.history || [];
          a.history.push({ at: today, what: `Originator date: ${a.originatorDate || '—'} → ${v || '—'}` });
          a.originatorDate = v;
          changed = true;
        }
      }
      if (changed) {
        a.updatedAt = today;
        snapshotForUndo();
        saveState();
        drawKpis();
      }
    }

    $$('.reg-col', view).forEach((btn) => {
      btn.addEventListener('click', (e) => {
        const col = btn.dataset.col;
        const stack = regState.sortStack;
        if (e.shiftKey) {
          // Additive — add this column as a secondary sort, OR if it's
          // already in the stack, flip its direction.
          const existing = stack.find((s) => s.col === col);
          if (existing) existing.dir = existing.dir === 'asc' ? 'desc' : 'asc';
          else stack.push({ col, dir: 'asc' });
        } else {
          // Replace — single-column primary sort. If the column is
          // already the only sort, flip its direction (classic toggle).
          if (stack.length === 1 && stack[0].col === col) {
            stack[0].dir = stack[0].dir === 'asc' ? 'desc' : 'asc';
          } else {
            regState.sortStack = [{ col, dir: 'asc' }];
          }
        }
        drawTable();
      });
      btn.addEventListener('contextmenu', (e) => {
        const col = btn.dataset.col;
        const stack = regState.sortStack;
        const idx = stack.findIndex((s) => s.col === col);
        if (idx === -1) return; // not sorted — let the browser context menu open
        e.preventDefault();
        stack.splice(idx, 1);
        drawTable();
      });
    });

    // Event delegation on the body for inline edits and row deletion
    const body = $('#regBody');
    body.addEventListener('change', (e) => {
      const inp = e.target.closest('.reg-inp');
      if (!inp) return;
      const row = inp.closest('.reg-row');
      const id = row?.dataset.id;
      if (!id) return;
      applyEdit(id, inp.dataset.field, inp.value);
      // Refresh just this row so swatches/colors/late state update
      const a = (curProject().actions || []).find((x) => x.id === id);
      if (a) {
        const newRow = document.createElement('div');
        newRow.innerHTML = rowHTML(a).trim();
        row.replaceWith(newRow.firstChild);
      }
    });
    body.addEventListener('click', (e) => {
      const del = e.target.closest('.row-del');
      if (!del) return;
      const row = del.closest('.reg-row');
      const id = row?.dataset.id;
      const a = (curProject().actions || []).find((x) => x.id === id);
      if (!a) return;
      if (!confirm(`Move "${a.title}" to Archive? You can restore it later.`)) return;
      const sourceProj = projectOfAction(id) || proj;
      const target = (sourceProj.actions || []).find((x) => x.id === id);
      if (target) {
        target.archivedAt = todayISO();
        target.history.push({ at: todayISO(), what: 'Moved to Archive' });
        target.updatedAt = todayISO();
      }
      snapshotForUndo();
      saveState();
      drawKpis();
      row.remove();
    });
    // Right-click on a register row → action context menu
    body.addEventListener('contextmenu', (e) => {
      const row = e.target.closest('.reg-row[data-id]');
      if (!row) return;
      e.preventDefault();
      const id = row.dataset.id;
      const a = (curProject().actions || []).find((x) => x.id === id);
      if (!a) return;
      showContextMenu(e.clientX, e.clientY, actionContextItems(a));
    });

    $('#btnAddAction').addEventListener('click', () => openQuickAdd('action'));
    $('#btnOpenArchive').addEventListener('click', () => {
      state.currentView = 'archive';
      saveState(); render();
    });
    $('#btnClearDrill')?.addEventListener('click', () => { _dashDrill = null; render(); });

    // Bulk select / bulk-actions wiring. The selection is module-scoped
    // so it survives a re-render of the same view (sort change, status
    // edit), but we keep it sane by intersecting with currently-visible
    // rows on every refresh of the bulk bar.
    function refreshBulkBar() {
      const bar = $('#bulkBar');
      if (!bar) return;
      const cnt = _regSelection.size;
      bar.hidden = cnt === 0;
      const cntEl = $('#bulkCount');
      if (cntEl) cntEl.textContent = cnt;
      const all = $('#regSelectAll');
      if (all) {
        const ids = view.querySelectorAll('#regBody .reg-row[data-id]');
        const total = ids.length;
        const sel = [...ids].filter((r) => _regSelection.has(r.dataset.id)).length;
        all.checked = total > 0 && sel === total;
        all.indeterminate = sel > 0 && sel < total;
      }
      view.querySelectorAll('.reg-row[data-id]').forEach((r) => {
        const on = _regSelection.has(r.dataset.id);
        r.classList.toggle('is-selected', on);
        const cb = r.querySelector('.reg-row-check');
        if (cb) cb.checked = on;
      });
    }
    function selectedActions() {
      const ids = new Set(_regSelection);
      return (proj.actions || []).filter((a) => ids.has(a.id));
    }

    view.addEventListener('click', (e) => {
      const cb = e.target.closest('.reg-row-check');
      if (!cb) return;
      const id = cb.dataset.rowId;
      const allRows = [...view.querySelectorAll('#regBody .reg-row[data-id]')];
      if (e.shiftKey && _regLastClickedId) {
        const fromIdx = allRows.findIndex((r) => r.dataset.id === _regLastClickedId);
        const toIdx   = allRows.findIndex((r) => r.dataset.id === id);
        if (fromIdx > -1 && toIdx > -1) {
          const [lo, hi] = fromIdx < toIdx ? [fromIdx, toIdx] : [toIdx, fromIdx];
          for (let i = lo; i <= hi; i++) _regSelection.add(allRows[i].dataset.id);
        }
      } else if (cb.checked) {
        _regSelection.add(id);
      } else {
        _regSelection.delete(id);
      }
      _regLastClickedId = id;
      refreshBulkBar();
    });

    $('#regSelectAll')?.addEventListener('change', (e) => {
      const ids = [...view.querySelectorAll('#regBody .reg-row[data-id]')].map((r) => r.dataset.id);
      if (e.target.checked) ids.forEach((id) => _regSelection.add(id));
      else                  ids.forEach((id) => _regSelection.delete(id));
      refreshBulkBar();
    });

    $('#bulkClear')?.addEventListener('click', () => {
      _regSelection.clear();
      refreshBulkBar();
    });

    function applyBulk(label, mutator) {
      const items = selectedActions();
      if (!items.length) return;
      const count = items.length;
      const today = todayISO();
      items.forEach((a) => { mutator(a, today); a.updatedAt = today; stampEdit(a, null, `bulk · ${label}`); });
      _regSelection.clear();
      commit('bulk-' + label.replace(/\s+/g, '-').toLowerCase());
      toast(`${count} updated · ${label}`);
    }

    $('#bulkStatus')?.addEventListener('change', (e) => {
      const v = e.target.value; if (!v) return;
      applyBulk(`status → ${v}`, (a) => {
        if (a.status === v) return;
        const wasNotDone = a.status !== 'done';
        const _prev = a.status;
        a.history = a.history || [];
        a.history.push({ at: todayISO(), what: `Status: ${a.status} → ${v}` });
        a.status = v;
        _syncCompletedAtOnStatus(a, _prev);
        _syncStartDateOnStatus(a, _prev);
        if (v === 'done' && wasNotDone && a.recurrence) {
          const sourceProj = projectOfAction(a.id) || curProject();
          const nextA = spawnRecurrenceFromDone(a, sourceProj);
          if (nextA) stampEdit(nextA, null, 'spawned by recurrence (bulk)');
        }
      });
      e.target.value = '';
    });
    $('#bulkOwner')?.addEventListener('change', (e) => {
      const v = e.target.value; if (!v) return;
      applyBulk(`owner → ${personName(v)}`, (a) => { a.owner = v; });
      e.target.value = '';
    });
    $('#bulkComp')?.addEventListener('change', (e) => {
      const v = e.target.value; if (!v) return;
      const target = v === '__none__' ? null : v;
      const label = target ? `component → ${(curProject().components.find((c) => c.id === target) || {}).name || '?'}` : 'component cleared';
      applyBulk(label, (a) => { a.component = target; });
      e.target.value = '';
    });
    $('#bulkPrio')?.addEventListener('change', (e) => {
      const v = e.target.value; if (!v) return;
      applyBulk(`priority → ${priorityLevel(v).label}`, (a) => { a.priorityLevel = v; });
      e.target.value = '';
    });
    $('#bulkSnooze7')?.addEventListener('click', () => {
      const d = new Date(); d.setHours(0, 0, 0, 0); d.setDate(d.getDate() + 7);
      const iso = fmtISO(d);
      applyBulk(`snoozed until ${iso}`, (a) => { a.snoozedUntil = iso; });
    });
    $('#bulkArchive')?.addEventListener('click', () => {
      const items = selectedActions();
      if (!items.length) return;
      if (!confirm(`Move ${items.length} selected action${items.length === 1 ? '' : 's'} to Archive?`)) return;
      const today = todayISO();
      items.forEach((a) => {
        a.archivedAt = today;
        a.history = a.history || [];
        a.history.push({ at: today, what: 'Moved to Archive (bulk)' });
        a.updatedAt = today;
        stampEdit(a, null, 'bulk · archive');
      });
      _regSelection.clear();
      commit('bulk-archive');
      toast(`${items.length} archived`);
    });

    // Filter / search changes invalidate selection (rows scroll out of
    // view; keeping a "phantom" selection is confusing).
    ['#search', '#filterOwner', '#filterComponent', '#filterStatus', '#filterDue'].forEach((sel) => {
      $(sel)?.addEventListener('input',  () => { _regSelection.clear(); refreshBulkBar(); });
      $(sel)?.addEventListener('change', () => { _regSelection.clear(); refreshBulkBar(); });
    });
    refreshBulkBar();

    // Archive every currently-visible done action (respecting active filters)
    // → they disappear from the register, remain visible in the Archive panel.
    $('#btnArchiveDone').addEventListener('click', () => {
      const proj2 = curProject();
      const candidates = (proj2.actions || []).filter((a) => !a.archivedAt && a.status === 'done' && actionMatchesFilters(a));
      if (!candidates.length) { toast('No done actions match the current filters'); return; }
      if (!confirm(`Archive ${candidates.length} done action${candidates.length === 1 ? '' : 's'}? They will move to the Archive panel and disappear from the register.`)) return;
      const today = todayISO();
      candidates.forEach((a) => {
        a.archivedAt = today;
        a.history = a.history || [];
        a.history.push({ at: today, what: 'Moved to Archive (bulk-archive done from Register)' });
        a.updatedAt = today;
      });
      commit('archive-done-bulk');
      toast(`${candidates.length} archived`);
    });

    drawKpis();
    drawTable();
  }

  /* ---------------------------- Timeline ----------------------------- */

  let tlState = null;
  const GRANULARITIES = {
    day:   { name: 'Day',   defaultDw: 24, minDw: 14, maxDw: 80, snapDays: 1,  totalDays: 180 },
    week:  { name: 'Week',  defaultDw: 8,  minDw: 4,  maxDw: 24, snapDays: 7,  totalDays: 365 },
    month: { name: 'Month', defaultDw: 3,  minDw: 1,  maxDw: 8,  snapDays: 30, totalDays: 730 },
  };

  function renderTimeline(root) {
    const proj = curProject();
    const acts = (proj.actions || []).filter(actionMatchesFilters);
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">${escapeHTML(proj.name)} — Gantt</div>
          <div class="page-sub">Drag bar body to shift • Drag edges to resize • Drag vertically to reassign • Double-click empty space to create</div>
        </div>
        <div class="page-actions">
          <div class="seg" role="tablist" aria-label="Granularity">
            <button class="seg-btn" data-gran="day">Day</button>
            <button class="seg-btn" data-gran="week">Week</button>
            <button class="seg-btn" data-gran="month">Month</button>
          </div>
          <div class="popover-anchor">
            <button class="ghost" id="btnTLHolidays" title="Choose national holidays to consider">Holidays <span id="holidayBadge" class="badge"></span></button>
            <div class="popover" id="holidayPopover" hidden></div>
          </div>
          <button class="icon-btn" id="btnTLZoomOut" title="Zoom out">−</button>
          <button class="icon-btn" id="btnTLZoomIn" title="Zoom in">+</button>
          <button class="ghost" id="btnTLToday">Today</button>
          <button class="ghost ${tlState?.criticalOnly ? 'is-on' : ''}" id="btnTLCritical" title="Highlight the critical path — actions whose dependsOn chain ends at an open milestone. Dims every other bar so the longest blocking chain stands out. Empty if no actions are linked to an open milestone.">◈ Critical path</button>
        </div>
      </div>
      <div class="timeline copyable-chart" data-chart="gantt">
        <div class="tl-lanes" id="tlLanes"></div>
        <div class="tl-grid-wrap" id="tlGridWrap">
          <div class="tl-axis" id="tlAxis"></div>
          <div class="tl-grid" id="tlGrid"></div>
          <div class="tl-tooltip" id="tlTooltip" hidden></div>
        </div>
      </div>`;
    root.appendChild(view);
    // Relax the horizontal scroll so the PNG capture includes the full
    // timeline width, not just the current viewport.
    registerChartCapture('gantt', { expandSelectors: ['.tl-grid-wrap', '.timeline'] });
    registerChartData('gantt', () => ({
      headers: ['ID', 'Title', 'Component', 'Owner', 'Status', 'Start', 'Due', 'Baseline due', '% Complete', 'Logged hours', 'Planned hours'],
      rows: acts.filter((a) => !a.archivedAt).map((a) => [
        a.id, a.title,
        findComponent(proj, a.component)?.name || '',
        personName(a.owner),
        (STATUSES.find((s) => s.id === a.status) || {}).name || a.status,
        a.startDate || '', a.due || '',
        a.baseline?.due || '',
        percentComplete(a),
        Number(totalLoggedHours(a).toFixed(2)),
        Number(plannedHoursTotal(a).toFixed(2)),
      ]),
    }));

    if (!tlState) tlState = { granularity: 'day', dayWidth: GRANULARITIES.day.defaultDw, startOffsetDays: -14, criticalOnly: false };
    if (tlState.criticalOnly == null) tlState.criticalOnly = false;
    if (!state.settings) state.settings = { holidayCountries: [] };

    // Mark active granularity
    $$('.seg-btn', view).forEach((b) => {
      b.classList.toggle('active', b.dataset.gran === tlState.granularity);
      b.addEventListener('click', () => {
        if (tlState.granularity === b.dataset.gran) return;
        tlState.granularity = b.dataset.gran;
        tlState.dayWidth = GRANULARITIES[tlState.granularity].defaultDw;
        // re-mark
        $$('.seg-btn', view).forEach((x) => x.classList.toggle('active', x.dataset.gran === tlState.granularity));
        drawTimeline(acts);
        scrollToToday();
      });
    });

    // Holiday popover
    updateHolidayBadge();
    $('#btnTLHolidays').addEventListener('click', (e) => {
      e.stopPropagation();
      toggleHolidayPopover(acts);
    });
    // Outside-click closer is registered once globally (see init()).

    drawTimeline(acts);
    // Scroll to today on first paint
    setTimeout(scrollToToday, 0);

    $('#btnTLZoomIn').addEventListener('click', () => {
      const g = GRANULARITIES[tlState.granularity];
      tlState.dayWidth = clamp(tlState.dayWidth * 1.35, g.minDw, g.maxDw);
      drawTimeline(acts);
    });
    $('#btnTLZoomOut').addEventListener('click', () => {
      const g = GRANULARITIES[tlState.granularity];
      tlState.dayWidth = clamp(tlState.dayWidth / 1.35, g.minDw, g.maxDw);
      drawTimeline(acts);
    });
    $('#btnTLToday').addEventListener('click', scrollToToday);
    $('#btnTLCritical')?.addEventListener('click', () => {
      tlState.criticalOnly = !tlState.criticalOnly;
      $('#btnTLCritical').classList.toggle('is-on', tlState.criticalOnly);
      drawTimeline(acts);
    });
  }

  function scrollToToday() {
    const wrap = $('#tlGridWrap');
    if (!wrap) return;
    const todayX = -tlState.startOffsetDays * tlState.dayWidth;
    wrap.scrollLeft = Math.max(0, todayX - wrap.clientWidth / 3);
  }

  function updateHolidayBadge() {
    const badge = $('#holidayBadge');
    if (!badge) return;
    const codes = activeHolidayCodes();
    badge.textContent = codes.length ? String(codes.length) : '';
    badge.style.display = codes.length ? '' : 'none';
  }

  function toggleHolidayPopover(acts) {
    const pop = $('#holidayPopover');
    if (!pop) return;
    if (!pop.hidden) { pop.hidden = true; return; }
    const codes = activeHolidayCodes();
    pop.innerHTML = `
      <div class="popover-head">Non-working days</div>
      <div class="popover-sub">Selected days appear as bands on the Gantt and warn when a task lands on one.</div>
      <div class="popover-list">
        <label class="popover-item">
          <input type="checkbox" data-code="WKND" ${codes.includes('WKND') ? 'checked' : ''} />
          <span class="flag">🛌</span>
          <span>Weekends</span>
          <span class="muted">Sat &amp; Sun</span>
        </label>
        <div class="popover-divider"></div>
        ${Object.entries(HOLIDAYS).map(([code, h]) => `
          <label class="popover-item">
            <input type="checkbox" data-code="${code}" ${codes.includes(code) ? 'checked' : ''} />
            <span class="flag">${h.flag}</span>
            <span>${escapeHTML(h.name)}</span>
            <span class="muted">${Object.keys(h.dates).length}</span>
          </label>`).join('')}
      </div>
      <div class="popover-foot">
        <button class="ghost" id="holNone">Clear</button>
        <button class="ghost" id="holClose">Done</button>
      </div>`;
    pop.hidden = false;

    $$('input[type=checkbox]', pop).forEach((cb) => {
      cb.addEventListener('change', () => {
        const code = cb.dataset.code;
        let codes = activeHolidayCodes().slice();
        if (cb.checked && !codes.includes(code)) codes.push(code);
        if (!cb.checked) codes = codes.filter((c) => c !== code);
        state.settings = state.settings || {};
        state.settings.holidayCountries = codes;
        saveState();
        updateHolidayBadge();
        drawTimeline(acts);
      });
    });
    $('#holNone').addEventListener('click', () => {
      state.settings.holidayCountries = [];
      saveState();
      updateHolidayBadge();
      drawTimeline(acts);
      $$('input[type=checkbox]', pop).forEach((cb) => cb.checked = false);
    });
    $('#holClose').addEventListener('click', () => { pop.hidden = true; });
  }

  function drawAxis(axisEl, start, totalDays, dw, granularity) {
    axisEl.innerHTML = '';
    axisEl.style.width = (totalDays * dw) + 'px';

    const addTick = (d, isMajor) => {
      const tick = document.createElement('div');
      tick.className = 'tl-axis-tick' + (isMajor ? ' major' : '');
      tick.style.left = (d * dw) + 'px';
      axisEl.appendChild(tick);
    };
    const addLabel = (d, text, sub = '') => {
      const lbl = document.createElement('div');
      lbl.className = 'tl-axis-label';
      lbl.style.left = (d * dw) + 'px';
      lbl.innerHTML = sub ? `<b>${escapeHTML(text)}</b><span>${escapeHTML(sub)}</span>` : escapeHTML(text);
      axisEl.appendChild(lbl);
    };

    if (granularity === 'day') {
      const labelEvery = dw >= 30 ? 1 : dw >= 18 ? 2 : 7;
      for (let d = 0; d <= totalDays; d++) {
        const date = new Date(start.getTime() + d * dayMs);
        const isMonthStart = date.getDate() === 1;
        const isWeek = date.getDay() === 1; // Mondays
        if (isMonthStart || isWeek) addTick(d, isMonthStart);
        else if (dw >= 14) addTick(d, false);
        if (d % labelEvery === 0 || isMonthStart) {
          const text = date.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
          addLabel(d, text);
        }
      }
    } else if (granularity === 'week') {
      // First Monday on or after start
      let dt = new Date(start);
      while (dt.getDay() !== 1) dt = new Date(dt.getTime() + dayMs);
      let d = Math.round((dt - start) / dayMs);
      // Major ticks at month starts; minor at every Monday
      while (d <= totalDays) {
        const major = dt.getDate() <= 7; // first Monday of the month
        addTick(d, major);
        if (major) addLabel(d, dt.toLocaleDateString(undefined, { month: 'short' }), String(dt.getFullYear()));
        else if (dw >= 16) addLabel(d, dt.toLocaleDateString(undefined, { day: 'numeric' }));
        dt = new Date(dt.getTime() + 7 * dayMs);
        d += 7;
      }
    } else if (granularity === 'month') {
      // First day of month
      let dt = new Date(start);
      if (dt.getDate() !== 1) dt = new Date(dt.getFullYear(), dt.getMonth() + 1, 1);
      while (true) {
        const d = Math.round((dt - start) / dayMs);
        if (d > totalDays) break;
        const major = dt.getMonth() % 3 === 0; // quarter starts
        addTick(d, major);
        addLabel(d, dt.toLocaleDateString(undefined, { month: 'short' }), String(dt.getFullYear()));
        dt = new Date(dt.getFullYear(), dt.getMonth() + 1, 1);
      }
    }
  }

  function drawTimeline(actions) {
    const lanesEl = $('#tlLanes');
    const axisEl = $('#tlAxis');
    const gridEl = $('#tlGrid');
    if (!lanesEl) return;

    const g = GRANULARITIES[tlState.granularity];
    const totalDays = g.totalDays;
    const dw = tlState.dayWidth;
    const width = totalDays * dw;
    // Align start to the granularity unit
    let start = new Date(Date.now() + tlState.startOffsetDays * dayMs);
    start.setHours(0, 0, 0, 0);
    if (tlState.granularity === 'week') {
      while (start.getDay() !== 1) start = new Date(start.getTime() - dayMs);
    } else if (tlState.granularity === 'month') {
      start = new Date(start.getFullYear(), start.getMonth(), 1);
    }
    const startISO = fmtISO(start);

    // Build lanes by person + a bottom events cell to align with the strip
    const people = state.people;
    lanesEl.innerHTML = `<div class="tl-lane header">Owner</div>` +
      people.map((p) => `<div class="tl-lane" data-owner="${p.id}">${escapeHTML(p.name)}</div>`).join('') +
      `<div class="tl-lane events-lane">Events</div>`;

    // Axis
    drawAxis(axisEl, start, totalDays, dw, tlState.granularity);

    // Grid rows per person
    gridEl.innerHTML = '';
    gridEl.style.width = width + 'px';
    people.forEach((p) => {
      const row = document.createElement('div');
      row.className = 'tl-grid-row';
      row.dataset.owner = p.id;
      gridEl.appendChild(row);
    });

    // Vertical gridlines on grid (mirror axis ticks for readability)
    const gridLines = document.createElement('div');
    gridLines.className = 'tl-gridlines';
    gridLines.style.width = width + 'px';
    drawAxis(gridLines, start, totalDays, dw, tlState.granularity);
    gridEl.appendChild(gridLines);

    // Holiday & weekend bands (drawn before today line so it sits on top)
    if (activeHolidayCodes().length) {
      const seen = {};
      for (let d = 0; d <= totalDays; d++) {
        const date = new Date(start.getTime() + d * dayMs);
        const iso = fmtISO(date);
        const info = holidayInfo(iso);
        if (!info) continue;
        if (seen[iso]) continue;
        seen[iso] = true;
        const onlyWeekend = info.every((h) => h.isWeekend);
        const band = document.createElement('div');
        band.className = 'tl-holiday-band' + (onlyWeekend ? ' weekend' : '');
        band.style.left = (d * dw) + 'px';
        band.style.width = dw + 'px';
        band.title = info.map((h) => h.isWeekend ? `${h.flag} ${h.name}` : `${h.flag} ${h.name} (${h.country})`).join('\n');
        // Tag (flag emoji) only on national holidays in day mode at generous zoom
        if (!onlyWeekend && tlState.granularity === 'day' && dw >= 18) {
          const nat = info.find((h) => !h.isWeekend);
          if (nat) {
            const tag = document.createElement('span');
            tag.className = 'tl-holiday-tag';
            tag.textContent = nat.flag;
            band.appendChild(tag);
          }
        }
        gridEl.appendChild(band);
      }
    }

    // Compute per-owner per-day commitment load. An action contributes its
    // commitment % to every day in its [startDate..due] window — done items
    // do not count toward future load.
    const ownerLoads = {};      // ownerId -> { iso -> totalCommitment }
    const actOverloads = {};    // actionId -> { badDays, peak }
    actions.forEach((a) => {
      if (!a.due || a.status === 'done') return;
      const cmt = (typeof a.commitment === 'number') ? a.commitment : 100;
      const sIso = a.startDate || fmtISO(new Date(parseDate(a.due).getTime() - 2 * dayMs));
      const sT = parseDate(sIso).getTime();
      const eT = parseDate(a.due).getTime();
      if (!ownerLoads[a.owner]) ownerLoads[a.owner] = {};
      for (let t = sT; t <= eT; t += dayMs) {
        const iso = fmtISO(new Date(t));
        ownerLoads[a.owner][iso] = (ownerLoads[a.owner][iso] || 0) + cmt;
      }
    });
    actions.forEach((a) => {
      if (!a.due || a.status === 'done') return;
      const sIso = a.startDate || fmtISO(new Date(parseDate(a.due).getTime() - 2 * dayMs));
      const sT = parseDate(sIso).getTime();
      const eT = parseDate(a.due).getTime();
      let bad = 0, peak = 0;
      for (let t = sT; t <= eT; t += dayMs) {
        const iso = fmtISO(new Date(t));
        const v = ownerLoads[a.owner]?.[iso] || 0;
        if (v > 100) { bad++; if (v > peak) peak = v; }
      }
      if (bad) actOverloads[a.id] = { badDays: bad, peak: Math.round(peak) };
    });

    // Render per-owner over-allocation overlays — one band per day where load > 100%
    people.forEach((p, pi) => {
      const ld = ownerLoads[p.id];
      if (!ld) return;
      const row = gridEl.children[pi];
      for (let dy = 0; dy <= totalDays; dy++) {
        const date = new Date(start.getTime() + dy * dayMs);
        const iso = fmtISO(date);
        const v = ld[iso];
        if (!v || v <= 100) continue;
        const overlay = document.createElement('div');
        overlay.className = 'tl-overload';
        overlay.style.left = (dy * dw) + 'px';
        overlay.style.width = dw + 'px';
        const intensity = Math.min(0.45, 0.15 + (v - 100) / 300);
        overlay.style.background = `rgba(248,113,113,${intensity.toFixed(2)})`;
        overlay.title = `${p.name}: ${Math.round(v)}% load on ${fmtFull(iso)}`;
        row.appendChild(overlay);
      }
    });

    // Today line
    const today = new Date(); today.setHours(0,0,0,0);
    const todayOffset = Math.round((today - start) / dayMs) * dw;
    const todayLine = document.createElement('div');
    todayLine.className = 'tl-today';
    todayLine.style.left = todayOffset + 'px';
    gridEl.appendChild(todayLine);

    // Build the unified events list (milestones / deliverables / meetings)
    // and render vertical lines spanning the whole grid + a label strip
    // pinned to the bottom of the grid to avoid clutter.
    const proj = curProject();
    const events = [];
    (proj.milestones || []).forEach((m) => {
      if (m.date) events.push({ kind: 'milestone', date: m.date, name: m.name, status: m.status, sub: 'Milestone' });
    });
    (proj.deliverables || []).forEach((dv) => {
      if (dv.dueDate) events.push({ kind: 'deliverable', date: dv.dueDate, name: dv.name, status: dv.status, sub: 'Deliverable' });
    });
    (proj.meetings || []).forEach((mt) => {
      const winStartISO = fmtISO(start);
      const winEndISO   = fmtISO(new Date(start.getTime() + totalDays * dayMs));
      const dates = expandMeetingDates(mt, winStartISO, winEndISO);
      if (!dates.length) return;
      const baseLabel = meetingRecurrenceLabel(mt);
      const sub = baseLabel + (mt.time ? ' ' + mt.time : '');
      const isRecurring = mt.kind === 'recurring';
      dates.forEach((iso, i) => {
        events.push({
          kind: isRecurring ? 'meeting-weekly' : 'meeting', // legacy class name kept for CSS; covers all recurrence
          date: iso,
          name: mt.title,
          sub,
          isFirst: i === 0,
        });
      });
    });

    // Filter to visible window and sort
    const winStart = start.getTime();
    const winEnd = winStart + totalDays * dayMs;
    const visibleEvents = events.filter((e) => {
      const t = parseDate(e.date).getTime();
      return t >= winStart && t <= winEnd;
    }).sort((a, b) => a.date.localeCompare(b.date));

    // Vertical line per event (spans whole grid including the events row)
    visibleEvents.forEach((e) => {
      const offset = Math.round((parseDate(e.date) - start) / dayMs);
      const x = offset * dw;
      const line = document.createElement('div');
      line.className = `tl-event-line tl-evt-${e.kind}`;
      line.style.left = x + 'px';
      gridEl.appendChild(line);
    });

    // Events row at the bottom of the grid (inside tl-grid, so vertical
    // lines naturally extend down to it). Markers stack to avoid overlap.
    const eventsRow = document.createElement('div');
    eventsRow.className = 'tl-events-row';
    gridEl.appendChild(eventsRow);

    // Place markers, alternating top/bottom slots and skipping repeated weekly
    // labels (only the first occurrence gets a label) to keep the strip clean.
    let slotIdx = 0;
    visibleEvents.forEach((e) => {
      const offset = Math.round((parseDate(e.date) - start) / dayMs);
      const x = offset * dw;
      const labelSuppress = e.kind === 'meeting-weekly' && !e.isFirst;
      if (labelSuppress) return;
      const slot = slotIdx % 3; // 3 vertical lanes inside the strip
      slotIdx++;
      const m = document.createElement('div');
      m.className = `tl-event-mark tl-evt-${e.kind} slot-${slot}`;
      m.style.left = x + 'px';
      const icon = e.kind === 'milestone' ? '◇'
                 : e.kind === 'deliverable' ? '◆'
                 : '⊕';
      m.title = `${e.sub}: ${e.name} — ${fmtFull(e.date)}`;
      m.innerHTML = `<span class="evt-icon">${icon}</span><span class="evt-name">${escapeHTML(e.name)}</span>`;
      eventsRow.appendChild(m);
    });

    // Critical-path detection — for every open milestone, walk back via
    // dependsOn to find the longest unbroken chain. Every action on that
    // chain gets a class on its bar; it also includes the actions linked
    // to the milestone via a.milestone (since the milestone date is the
    // chain's endpoint).
    const criticalSet = computeCriticalActions(proj);

    // "Critical path only" toggle — when on, the .timeline container
    // picks up .cp-mode, which the stylesheet uses to dim every bar
    // and dependency arrow that's NOT on the critical path. When the
    // critical set is empty (no actions linked to an open milestone)
    // surface a one-line hint above the grid so the user understands
    // why nothing seems to be happening.
    const timelineEl = document.querySelector('.timeline');
    if (timelineEl) timelineEl.classList.toggle('cp-mode', !!tlState.criticalOnly);
    const cpHintId = 'tlCriticalHint';
    document.getElementById(cpHintId)?.remove();
    if (tlState.criticalOnly && criticalSet.size === 0) {
      const hint = document.createElement('div');
      hint.id = cpHintId;
      hint.className = 'tl-cp-hint';
      hint.innerHTML = 'No critical path detected — link at least one action to an open milestone (Action drawer → Milestone) and connect upstream work via the Depends-on field.';
      timelineEl?.parentElement?.insertBefore(hint, timelineEl);
    }

    // Draw bars
    actions.forEach((a) => {
      if (!a.due) return;
      const ownerIdx = people.findIndex((p) => p.id === a.owner);
      if (ownerIdx < 0) return;
      // Default bar duration: 3 days ending at due, unless startDate is set
      const defaultDur = 3;
      const endD = a.due;
      const startD = a.startDate ||
        fmtISO(new Date(parseDate(endD).getTime() - (defaultDur - 1) * dayMs));
      const startOffset = Math.round((parseDate(startD) - start) / dayMs);
      const endOffset = Math.round((parseDate(endD) - start) / dayMs);
      const barLen = Math.max(1, endOffset - startOffset + 1);
      const left = startOffset * dw;
      const w = Math.max(g.minDw, barLen * dw - 2);

      const bar = document.createElement('div');
      const dueCls = statusOfDue(a.due, a.status);
      const holDue = holidayInfo(a.due);
      const holStart = holidayInfo(startD);
      const onHoliday = (holDue || holStart) && a.status !== 'done';
      const overload = actOverloads[a.id];
      const cmt = (typeof a.commitment === 'number') ? a.commitment : 100;
      bar.className = `tl-bar ${a.status} ${dueCls === 'late' && a.status !== 'done' ? 'late' : ''} ${onHoliday ? 'holiday-conflict' : ''} ${overload ? 'over-allocated' : ''} ${criticalSet.has(a.id) ? 'critical-path' : ''}`;
      bar.style.left = left + 'px';
      bar.style.width = w + 'px';
      bar.dataset.id = a.id;
      const ownerName = personName(a.owner);
      const holNote = holDue ? `\n⚠ Due on holiday: ${holDue.map((h) => h.name).join(', ')}` : '';
      const overNote = overload ? `\n⚡ Over-allocated: ${ownerName} reaches ${overload.peak}% on ${overload.badDays} day${overload.badDays === 1 ? '' : 's'}` : '';
      const cmtNote = cmt !== 100 ? `\nCommitment: ${cmt}%` : '';
      bar.title = `${a.title}\n${fmtFull(startD)} → ${fmtFull(endD)} (${barLen}d)${cmtNote}${holNote}${overNote}`;
      const warnIcons = (onHoliday ? '<span class="bar-warn hol" title="Falls on a non-working day">⚠</span>' : '')
        + (overload ? `<span class="bar-warn over" title="Owner over-allocated">⚡</span>` : '');
      const cmtChip = cmt !== 100 ? `<span class="bar-pct">${cmt}%</span>` : '';
      bar.innerHTML = `
        <div class="resize-handle left" title="Drag to change start"></div>
        <span class="bar-label">${warnIcons}${cmtChip ? ' ' + cmtChip : ''} ${escapeHTML(a.title)}</span>
        <div class="resize-handle right" title="Drag to change end"></div>`;
      attachBarDND(bar, a, startISO);
      const row = gridEl.children[ownerIdx];
      row.appendChild(bar);
      // If the bar's usable interior is too narrow to legibly hold the title,
      // render an overflow label to the right of the bar. Reserve space for the
      // resize handles (8 px each) and the side padding (6 px each), plus any
      // warn icons / commitment chip glued to the front of the label.
      const HANDLES_PAD = 8 + 8 + 6 + 6; // 28 px reserved by handles + padding
      const PREFIX_PX = (warnIcons ? 14 : 0) + (cmtChip ? 30 : 0);
      const titlePx = a.title.length * 6.2 + 4; // estimate (font 11, weight 400)
      const usable = w - HANDLES_PAD - PREFIX_PX;
      if (usable < titlePx) {
        const overflow = document.createElement('span');
        overflow.className = `tl-bar-overflow ${a.status}`;
        overflow.style.left = (left + w + 4) + 'px';
        overflow.textContent = a.title;
        overflow.title = `${a.title} — due ${fmtFull(a.due)}`;
        row.appendChild(overflow);
      }
    });

    // Double-click empty space creates an action
    [...gridEl.querySelectorAll('.tl-grid-row')].forEach((row) => {
      row.addEventListener('dblclick', (e) => {
        if (e.target !== row) return;
        const x = e.offsetX;
        const snapDays = GRANULARITIES[tlState.granularity].snapDays;
        const dayOffset = Math.round(x / dw / snapDays) * snapDays;
        const date = fmtISO(new Date(start.getTime() + dayOffset * dayMs));
        const owner = row.dataset.owner;
        const proj = curProject();
        const a = {
          id: uid('a'), title: 'New action', owner, due: date,
          startDate: date, status: 'todo', priority: 0,
          deliverable: null, milestone: null, notes: '',
          createdAt: todayISO(), updatedAt: todayISO(),
          originatorDate: todayISO(),
          history: [{ at: todayISO(), what: 'Created from timeline' }],
        };
        proj.actions.push(a);
        commit('create');
        toast('Action created — double-click bar to edit');
      });
    });

    // Phase F — render dependency arrows on top of all bars. Drawn last so
    // they sit above the grid lines, but pointer-events: none so bars stay
    // interactive.
    drawDependencyArrows(actions, gridEl, criticalSet);
  }

  // Walk back from every open milestone through dependsOn to flag the
  // critical actions. Returns a Set of action ids on the longest chain to
  // each open milestone (plus the actions directly linked to it).
  function computeCriticalActions(proj) {
    const acts = (proj.actions || []).filter((a) => !a.archivedAt);
    const byId = new Map(acts.map((a) => [a.id, a]));
    const critical = new Set();
    const depths = new Map();
    const onStack = new Set();
    function depth(id) {
      if (depths.has(id)) return depths.get(id);
      if (onStack.has(id)) return 0; // cycle guard
      onStack.add(id);
      const a = byId.get(id);
      if (!a) { onStack.delete(id); return 0; }
      const deps = (a.dependsOn || []).filter((d) => byId.has(d));
      let best = 0;
      deps.forEach((d) => { best = Math.max(best, depth(d)); });
      onStack.delete(id);
      depths.set(id, 1 + best);
      return 1 + best;
    }
    function chain(id) {
      const a = byId.get(id);
      if (!a || critical.has(id)) return;
      critical.add(id);
      const deps = (a.dependsOn || []).filter((d) => byId.has(d));
      if (!deps.length) return;
      let bestDep = null, bestVal = -1;
      deps.forEach((d) => { const v = depth(d); if (v > bestVal) { bestVal = v; bestDep = d; } });
      if (bestDep) chain(bestDep);
    }
    (proj.milestones || []).forEach((m) => {
      if (m.done || m.status === 'done') return;
      const targets = acts.filter((a) => a.milestone === m.id && !isClosedStatus(a.status));
      targets.forEach((t) => { depth(t.id); chain(t.id); });
    });
    return critical;
  }

  function drawDependencyArrows(actions, gridEl, criticalSet) {
    const bars = Array.from(gridEl.querySelectorAll('.tl-bar[data-id]'));
    if (!bars.length) return;
    const byId = new Map(actions.map((a) => [a.id, a]));
    const barById = new Map(bars.map((b) => [b.dataset.id, b]));

    // Need positions relative to gridEl
    const gridRect = gridEl.getBoundingClientRect();
    function rectOf(b) {
      const r = b.getBoundingClientRect();
      return {
        x: r.left - gridRect.left + gridEl.scrollLeft,
        y: r.top - gridRect.top + gridEl.scrollTop,
        w: r.width,
        h: r.height,
      };
    }
    const pairs = [];
    actions.forEach((a) => {
      if (!a.due) return;
      const deps = a.dependsOn || [];
      if (!deps.length) return;
      const target = barById.get(a.id);
      if (!target) return;
      deps.forEach((depId) => {
        const src = byId.get(depId);
        const srcBar = barById.get(depId);
        if (!src || !srcBar) return;
        pairs.push({ src, srcBar, dst: a, dstBar: target });
      });
    });
    if (!pairs.length) return;

    // SVG overlay sized to the grid's full content
    const w = gridEl.scrollWidth || gridEl.clientWidth;
    const h = gridEl.scrollHeight || gridEl.clientHeight;
    const NS = 'http://www.w3.org/2000/svg';
    const svg = document.createElementNS(NS, 'svg');
    svg.setAttribute('class', 'tl-dep-svg');
    svg.setAttribute('width', String(w));
    svg.setAttribute('height', String(h));
    svg.setAttribute('viewBox', `0 0 ${w} ${h}`);
    svg.style.position = 'absolute';
    svg.style.left = '0';
    svg.style.top = '0';
    svg.style.pointerEvents = 'none';

    const defs = document.createElementNS(NS, 'defs');
    defs.innerHTML = `
      <marker id="depArrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M0,0 L10,5 L0,10 z" fill="currentColor" />
      </marker>
      <marker id="depArrowCrit" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M0,0 L10,5 L0,10 z" fill="#f87171" />
      </marker>`;
    svg.appendChild(defs);

    pairs.forEach(({ src, dst, srcBar, dstBar }) => {
      const a = rectOf(srcBar);
      const b = rectOf(dstBar);
      const x1 = a.x + a.w;
      const y1 = a.y + a.h / 2;
      const x2 = b.x;
      const y2 = b.y + b.h / 2;
      const isCritical = criticalSet.has(src.id) && criticalSet.has(dst.id);
      const path = document.createElementNS(NS, 'path');
      // Cubic bezier — a small horizontal lead-out and lead-in so connectors
      // look intentional and don't clip into bar edges.
      const handle = Math.max(12, Math.min(60, (x2 - x1) / 2));
      const d = `M ${x1} ${y1} C ${x1 + handle} ${y1}, ${x2 - handle} ${y2}, ${x2 - 4} ${y2}`;
      path.setAttribute('d', d);
      path.setAttribute('fill', 'none');
      path.setAttribute('class', 'tl-dep-arrow' + (isCritical ? ' critical' : ''));
      path.setAttribute('marker-end', isCritical ? 'url(#depArrowCrit)' : 'url(#depArrow)');
      svg.appendChild(path);
    });
    gridEl.appendChild(svg);
  }

  function showTLTooltip(html, clientX, clientY) {
    const tip = $('#tlTooltip');
    if (!tip) return;
    tip.innerHTML = html;
    tip.hidden = false;
    const wrap = $('#tlGridWrap');
    const wrapRect = wrap.getBoundingClientRect();
    const x = clientX - wrapRect.left + wrap.scrollLeft + 12;
    const y = clientY - wrapRect.top + wrap.scrollTop - 30;
    tip.style.left = x + 'px';
    tip.style.top = Math.max(4, y) + 'px';
  }
  function hideTLTooltip() {
    const tip = $('#tlTooltip');
    if (tip) tip.hidden = true;
  }

  function attachBarDND(bar, action, startISO) {
    let mode = null; // 'move' | 'resize-l' | 'resize-r'
    let originX = 0;
    // Track everything in days from startISO, so granularity/zoom never warps the bar.
    let baseStart = 0, baseLen = 1, baseOwner = null; // captured at mousedown
    let curStart = 0, curLen = 1, curOwner = null;    // committed visually during drag

    const dw = () => tlState.dayWidth;
    const snap = () => GRANULARITIES[tlState.granularity].snapDays;
    const minDw = () => GRANULARITIES[tlState.granularity].minDw;

    function isoOf(dayOffset) {
      return fmtISO(new Date(parseDate(startISO).getTime() + dayOffset * dayMs));
    }
    function applyVisual() {
      bar.style.left = (curStart * dw()) + 'px';
      bar.style.width = Math.max(minDw(), curLen * dw() - 2) + 'px';
    }
    // Snap the drag DELTA (not the absolute position) so misaligned bars
    // keep their original day-of-week / day-of-month while moving in whole units.
    function snapDeltaDays(dx) {
      return Math.round(dx / dw() / snap()) * snap();
    }

    bar.addEventListener('mousedown', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const handle = e.target.classList.contains('resize-handle');
      if (handle) mode = e.target.classList.contains('left') ? 'resize-l' : 'resize-r';
      else mode = 'move';
      originX = e.clientX;

      const proj = curProject();
      const a = proj.actions.find((x) => x.id === action.id);
      if (!a) { mode = null; return; }
      const defaultDur = 3;
      const endD = a.due;
      const startD = a.startDate ||
        fmtISO(new Date(parseDate(endD).getTime() - (defaultDur - 1) * dayMs));
      baseStart = Math.round((parseDate(startD) - parseDate(startISO)) / dayMs);
      baseLen = Math.max(1, Math.round((parseDate(endD) - parseDate(startD)) / dayMs) + 1);
      baseOwner = a.owner;
      curStart = baseStart;
      curLen = baseLen;
      curOwner = a.owner;

      bar.classList.add('dragging');
      window.addEventListener('mousemove', onMove);
      window.addEventListener('mouseup', onUp, { once: true });
    });

    bar.addEventListener('dblclick', (e) => {
      e.stopPropagation();
      openDrawer(action.id);
    });
    bar.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const a = (curProject().actions || []).find((x) => x.id === action.id);
      if (a) showContextMenu(e.clientX, e.clientY, actionContextItems(a));
    });

    function onMove(e) {
      const dx = e.clientX - originX;
      const dDays = snapDeltaDays(dx);

      if (mode === 'move') {
        curStart = baseStart + dDays;
        // Lane change (vertical)
        const grid = $('#tlGrid');
        const rect = grid.getBoundingClientRect();
        const rowH = 36;
        const yIn = e.clientY - rect.top;
        const idx = clamp(Math.floor(yIn / rowH), 0, state.people.length - 1);
        curOwner = state.people[idx]?.id || curOwner;
        bar.dataset.targetOwner = curOwner;
        applyVisual();
        const owner = state.people.find((p) => p.id === curOwner);
        showTLTooltip(
          `<b>${escapeHTML(action.title)}</b><br>${fmtFull(isoOf(curStart))} → ${fmtFull(isoOf(curStart + curLen - 1))}<br><span style="color:var(--text-faint)">${escapeHTML(owner?.name || '')}</span>`,
          e.clientX, e.clientY);
      } else if (mode === 'resize-l') {
        const newStart = baseStart + dDays;
        const newLen = baseLen - dDays;
        if (newLen >= 1) {
          curStart = newStart;
          curLen = newLen;
          applyVisual();
          showTLTooltip(
            `Start: <b>${fmtFull(isoOf(curStart))}</b><br>End: ${fmtFull(isoOf(curStart + curLen - 1))}<br><span style="color:var(--text-faint)">${curLen} day${curLen === 1 ? '' : 's'}</span>`,
            e.clientX, e.clientY);
        }
      } else if (mode === 'resize-r') {
        const newLen = baseLen + dDays;
        if (newLen >= 1) {
          curLen = newLen;
          applyVisual();
          showTLTooltip(
            `Start: ${fmtFull(isoOf(curStart))}<br>End: <b>${fmtFull(isoOf(curStart + curLen - 1))}</b><br><span style="color:var(--text-faint)">${curLen} day${curLen === 1 ? '' : 's'}</span>`,
            e.clientX, e.clientY);
        }
      }
    }

    function onUp() {
      window.removeEventListener('mousemove', onMove);
      bar.classList.remove('dragging');
      hideTLTooltip();

      // No-op click: nothing actually moved during this drag — leave action untouched.
      if (curStart === baseStart && curLen === baseLen && curOwner === baseOwner) {
        mode = null;
        return;
      }

      const proj = curProject();
      const a = proj.actions.find((x) => x.id === action.id);
      if (!a) return;

      const newStart = isoOf(curStart);
      const newEnd = isoOf(curStart + curLen - 1);
      const ownerChanged = !!(curOwner && curOwner !== baseOwner);
      const startChanged = a.startDate !== newStart;
      const endChanged = a.due !== newEnd;
      if (ownerChanged) {
        a.history.push({ at: todayISO(), what: `Owner: ${personName(a.owner)} → ${personName(curOwner)}` });
        a.owner = curOwner;
      }
      if (startChanged || endChanged) {
        a.history.push({ at: todayISO(), what: `Schedule: ${a.startDate || a.due} → ${newStart}…${newEnd}` });
        a.startDate = newStart;
        a.due = newEnd;
      }
      a.updatedAt = todayISO();
      mode = null;
      if (ownerChanged || startChanged || endChanged) {
        commit('timeline');
        toast(ownerChanged ? 'Reassigned' : 'Schedule updated');
      }
      // No-op clicks just leave the bar where it was.
    }
  }

  /* ---------------------------- Dashboard ---------------------------- */

  // Compute the 5 decision-making KPIs surfaced at the top of the Dashboard.
  // These are derived metrics — no schema additions, just better synthesis of
  // existing fields (commitments, risks, action timestamps, EVM, CR dates).
  function computeDecisionKpis(proj) {
    const todayISO_ = todayISO();

    // 1. Team utilisation — for each horizon, sum committed % FTE across all
    // people / sum capacity. Spare expressed in FTE-weeks.
    const totalCapPct = state.people.reduce((s, p) => s + (p.capacity || 100), 0) || 1;
    const utilFor = (weeksAhead) => {
      let totalCommit = 0;
      state.people.forEach((p) => {
        weeklyLoad(p.id, weeksAhead).forEach((w) => { totalCommit += w.count || 0; });
      });
      const totalCapPeriod = totalCapPct * weeksAhead;
      const pct = totalCapPeriod ? Math.round((totalCommit / totalCapPeriod) * 100) : 0;
      const spareFte = ((totalCapPeriod - totalCommit) / 100);
      return { pct, spareFte };
    };
    const u4  = utilFor(4);
    const u8  = utilFor(8);
    const u12 = utilFor(12);

    // 2. Risk exposure — sum of P×I, residual vs. inherent, with unmitigated tally
    const risks = (proj.risks || []).filter((r) => (r.kind || 'risk') !== 'opportunity');
    let inh = 0, res = 0, unmitigated = 0;
    risks.forEach((r) => {
      const i  = ((r.inherent && r.inherent.probability) || 0) * ((r.inherent && r.inherent.impact) || 0);
      const rs = ((r.residual && r.residual.probability) || 0) * ((r.residual && r.residual.impact) || 0);
      inh += i;
      res += rs;
      if (!r.actionId) unmitigated += rs;
    });
    const reduction = inh > 0 ? Math.round((1 - res / inh) * 100) : 0;

    // 2b. Opportunity value — same P×I lattice but interpreted upward:
    // baseline = value if nothing is done, captured = value after capture.
    // Reuses inherent / residual JSON keys for backwards compat.
    const opps = (proj.risks || []).filter((r) => (r.kind || 'risk') === 'opportunity');
    let oppBase = 0, oppCap = 0, oppUnactioned = 0;
    opps.forEach((r) => {
      const b = ((r.inherent && r.inherent.probability) || 0) * ((r.inherent && r.inherent.impact) || 0);
      const c = ((r.residual && r.residual.probability) || 0) * ((r.residual && r.residual.impact) || 0);
      oppBase += b;
      oppCap  += c;
      if (!r.actionId) oppUnactioned += c;
    });
    const oppLift = oppBase > 0 ? Math.round(((oppCap - oppBase) / oppBase) * 100) : 0;

    // 3. Stale actions — open and not updated in ≥ 14 days
    const STALE_DAYS = 14;
    const open = (proj.actions || []).filter((a) => !a.archivedAt && !isClosedStatus(a.status));
    const stale = open.filter((a) => {
      if (!a.updatedAt) return true;
      return dayDiff(todayISO_, a.updatedAt) >= STALE_DAYS;
    });
    const topStale = stale.slice().sort((a, b) => (a.updatedAt || '').localeCompare(b.updatedAt || '')).slice(0, 3);
    const stalePct = open.length ? Math.round((stale.length / open.length) * 100) : 0;

    // 4. Project EVM rollup — sum BAC/PV/AC/EV across cost-centres in scope
    const ccs = getCostCentres();
    let BAC = 0, PV = 0, AC = 0, EV = 0;
    if (ccs.length) {
      // Same week table evmFor builds; reuse the helper for consistency
      const today = new Date(); today.setHours(0, 0, 0, 0);
      let monday = new Date(today);
      while (monday.getDay() !== 1) monday = new Date(monday.getTime() - dayMs);
      const start = new Date(monday.getTime() - 12 * 7 * dayMs);
      const weeks = [];
      for (let i = 0; i < 52; i++) {
        const s = new Date(start.getTime() + i * 7 * dayMs);
        weeks.push({ start: s, isoStart: fmtISO(s) });
      }
      ccs.forEach((cc) => {
        const e = evmFor(cc, weeks, 'cost');
        BAC += e.BAC; PV += e.PV; AC += e.AC; EV += e.EV;
      });
    }
    const CPI = AC > 0 ? EV / AC : 1;
    const SPI = PV > 0 ? EV / PV : 1;

    // 5. CR governance — median turnaround + count of pending > 14 d
    const changes = proj.changes || [];
    const decided = changes.filter((c) => c.status === 'approved' || c.status === 'rejected' || c.status === 'implemented' || c.status === 'cancelled');
    const turnarounds = decided
      .filter((c) => c.originatedDate && c.decisionDate)
      .map((c) => Math.max(0, dayDiff(c.decisionDate, c.originatedDate)));
    let medianTurn = null;
    if (turnarounds.length) {
      const sorted = turnarounds.slice().sort((a, b) => a - b);
      const mid = Math.floor(sorted.length / 2);
      medianTurn = sorted.length % 2 ? sorted[mid] : Math.round((sorted[mid - 1] + sorted[mid]) / 2);
    }
    const pending = changes.filter((c) => c.status === 'proposed' || c.status === 'under_review');
    const stalePending = pending.filter((c) => c.originatedDate && dayDiff(todayISO_, c.originatedDate) >= 14).length;

    return {
      util: { u4, u8, u12 },
      risk: { inh, res, unmitigated, reduction, count: risks.length },
      opp:  { base: oppBase, captured: oppCap, unactioned: oppUnactioned, lift: oppLift, count: opps.length },
      stale: { count: stale.length, totalOpen: open.length, stalePct, top: topStale },
      evm: { BAC, PV, AC, EV, CPI, SPI, hasData: ccs.length > 0 && AC > 0 },
      cr: { medianTurn, pending: pending.length, stalePending, decided: decided.length, total: changes.length },
    };
  }

  function decisionKpisHTML(d) {
    const todayISO_ = todayISO();
    const utilCls = (p) => p > 100 ? 'bad' : p > 85 ? 'warn' : 'ok';
    const fmtFte = (f) => (f >= 0 ? '+' : '') + f.toFixed(1) + ' FTE-w';
    const riskCls = d.risk.res >= 100 ? 'bad' : d.risk.res >= 50 ? 'warn' : d.risk.count === 0 ? '' : 'ok';
    const staleCls = d.stale.count > 8 ? 'bad' : d.stale.count > 3 ? 'warn' : 'ok';
    const cpiCls = d.evm.CPI >= 1 ? 'ok' : d.evm.CPI >= 0.9 ? 'warn' : 'bad';
    const spiCls = d.evm.SPI >= 1 ? 'ok' : d.evm.SPI >= 0.9 ? 'warn' : 'bad';
    const crCls  = (d.cr.medianTurn ?? 0) > 14 ? 'bad' : (d.cr.medianTurn ?? 0) > 7 ? 'warn' : (d.cr.medianTurn != null ? 'ok' : '');
    const utilBars = [['4 w', d.util.u4], ['8 w', d.util.u8], ['12 w', d.util.u12]].map(([lbl, u]) => `
      <div class="dkpi-mb">
        <span class="dkpi-mb-lbl">${lbl}</span>
        <span class="dkpi-mb-track"><span class="dkpi-mb-fill ${utilCls(u.pct)}" style="width:${Math.min(100, u.pct)}%"></span></span>
        <span class="dkpi-mb-val">${u.pct}%</span>
      </div>`).join('');
    const staleList = d.stale.top.length
      ? d.stale.top.map((a) => {
          const days = a.updatedAt ? Math.abs(dayDiff(todayISO_, a.updatedAt)) : '—';
          return `<div class="dkpi-list-row clickable" data-action-id="${a.id}" title="Open action">
            <span class="dkpi-list-text">${escapeHTML(a.title)}</span>
            <span class="dkpi-list-meta">${days}${typeof days === 'number' ? 'd' : ''}</span>
          </div>`;
        }).join('')
      : '<div class="dkpi-list-empty">No stale actions — nice.</div>';

    return `
      <div class="dkpi-grid">
        <div class="dkpi">
          <div class="dkpi-label">Team utilisation</div>
          <div class="dkpi-num ${utilCls(d.util.u4.pct)}">${d.util.u4.pct}%</div>
          <div class="dkpi-sub">next 4 w · ${fmtFte(d.util.u4.spareFte)} spare</div>
          <div class="dkpi-mini">${utilBars}</div>
        </div>

        <div class="dkpi">
          <div class="dkpi-label">Risk exposure</div>
          <div class="dkpi-num ${riskCls}">${d.risk.res}</div>
          <div class="dkpi-sub">residual P×I · ${d.risk.count} risk${d.risk.count === 1 ? '' : 's'}</div>
          <div class="dkpi-strip">
            <span class="dkpi-strip-pill">↓ ${d.risk.reduction}% from ${d.risk.inh}</span>
            ${d.risk.unmitigated > 0
              ? `<span class="dkpi-strip-warn">⚠ ${d.risk.unmitigated} unmitigated</span>`
              : (d.risk.count > 0 ? '<span class="dkpi-strip-ok">all linked</span>' : '<span class="dkpi-strip-muted">none logged</span>')}
          </div>
        </div>

        <div class="dkpi">
          <div class="dkpi-label">Opportunity value</div>
          <div class="dkpi-num ${d.opp.captured >= 25 ? 'ok' : d.opp.count === 0 ? '' : 'muted'}">${d.opp.captured}</div>
          <div class="dkpi-sub">captured P×I · ${d.opp.count} opportunit${d.opp.count === 1 ? 'y' : 'ies'}</div>
          <div class="dkpi-strip">
            ${d.opp.count > 0 ? `<span class="dkpi-strip-pill">↑ ${d.opp.lift}% from ${d.opp.base}</span>` : ''}
            ${d.opp.unactioned > 0
              ? `<span class="dkpi-strip-warn">⚠ ${d.opp.unactioned} unactioned</span>`
              : (d.opp.count > 0 ? '<span class="dkpi-strip-ok">all pursued</span>' : '<span class="dkpi-strip-muted">none logged</span>')}
          </div>
        </div>

        <div class="dkpi">
          <div class="dkpi-label">Stale actions</div>
          <div class="dkpi-num ${staleCls}">${d.stale.count}</div>
          <div class="dkpi-sub">of ${d.stale.totalOpen} open · ${d.stale.stalePct}% untouched ≥14 d</div>
          <div class="dkpi-list">${staleList}</div>
        </div>

        <div class="dkpi">
          <div class="dkpi-label">Project performance</div>
          <div class="dkpi-dual">
            <div class="dkpi-dual-cell">
              <div class="dkpi-dual-num ${cpiCls}">${d.evm.hasData ? d.evm.CPI.toFixed(2) : '—'}</div>
              <div class="dkpi-dual-lbl">CPI</div>
            </div>
            <div class="dkpi-dual-sep"></div>
            <div class="dkpi-dual-cell">
              <div class="dkpi-dual-num ${spiCls}">${d.evm.hasData ? d.evm.SPI.toFixed(2) : '—'}</div>
              <div class="dkpi-dual-lbl">SPI</div>
            </div>
          </div>
          <div class="dkpi-sub">${d.evm.hasData ? 'cost / schedule index · all CCs' : 'add a budget to a cost-centre'}</div>
        </div>

        <div class="dkpi">
          <div class="dkpi-label">Change governance</div>
          <div class="dkpi-num ${crCls}">${d.cr.medianTurn != null ? d.cr.medianTurn + ' d' : '—'}</div>
          <div class="dkpi-sub">${d.cr.medianTurn != null ? 'median turnaround · ' : ''}${d.cr.decided} decided · ${d.cr.pending} open</div>
          <div class="dkpi-strip">
            ${d.cr.stalePending > 0
              ? `<span class="dkpi-strip-warn">⚠ ${d.cr.stalePending} pending &gt;14 d</span>`
              : (d.cr.total > 0 ? '<span class="dkpi-strip-ok">no aging pending</span>' : '<span class="dkpi-strip-muted">no CRs yet</span>')}
          </div>
        </div>
      </div>`;
  }

  // Tile helpers — used by the Schedule adherence + Productivity dashboard
  // tiles. Format the headline, choose a colour band, and pick a friendly
  // trend arrow vs the previous equal-length window.
  function _trendArrow(curr, prev, opts = {}) {
    if (curr == null || prev == null) return { html: '', cls: '' };
    const delta = curr - prev;
    const eps = opts.eps ?? 0.005;
    if (Math.abs(delta) < eps) return { html: '→ flat vs prior 30d', cls: 'neutral' };
    const arrow = delta > 0 ? '↑' : '↓';
    const isImprovement = (opts.higherIsBetter ?? true) ? delta > 0 : delta < 0;
    return {
      html: `${arrow} ${opts.fmt ? opts.fmt(delta) : Math.abs(delta).toFixed(2)} vs prior 30d`,
      cls: isImprovement ? 'ok' : 'bad',
    };
  }

  // Compose the rich hover-tooltip content for a dashboard KPI tile.
  // Every tile gets a hidden .kpi-tt panel with:
  //   • a short prose meaning of the KPI
  //   • the exact formula in monospace
  //   • a bulleted list of inputs with human-readable definitions
  //   • a "minimum data required" list telling the user which fields
  //     they must fill in per action / per person for the KPI to be
  //     accurate. This is the guide the user asked for — the point
  //     that turns "why is this blank / wrong?" into a to-do list.
  //   • an optional "why can't this be computed" callout
  // Positioning is JS-driven — see the delegated hover handler in
  // renderDashboard so parent panel overflow / stacking contexts
  // never clip the tooltip.
  function _kpiTooltip({ title, meaning, formula, inputs = [], minimumData = [], notComputable = null }) {
    const inputsHTML = inputs.length
      ? `<div class="kpi-tt-label">Inputs</div>
         <ul class="kpi-tt-inputs">${inputs.map((i) =>
           `<li><b>${escapeHTML(i.name)}</b> — ${escapeHTML(i.desc)}</li>`).join('')}</ul>`
      : '';
    const minimumDataHTML = minimumData.length
      ? `<div class="kpi-tt-label">Minimum data to compute accurately</div>
         <ul class="kpi-tt-minimum">${minimumData.map((i) =>
           `<li><b>${escapeHTML(i.scope)}</b> ${escapeHTML(i.desc)}</li>`).join('')}</ul>`
      : '';
    const notComputableHTML = notComputable
      ? `<div class="kpi-tt-note">${escapeHTML(notComputable)}</div>` : '';
    return `<div class="kpi-tt" role="tooltip">
        <h4 class="kpi-tt-title">${escapeHTML(title)}</h4>
        <p class="kpi-tt-meaning">${escapeHTML(meaning)}</p>
        <div class="kpi-tt-label">Formula</div>
        <code class="kpi-tt-formula">${escapeHTML(formula)}</code>
        ${inputsHTML}
        ${minimumDataHTML}
        ${notComputableHTML}
      </div>`;
  }

  function deliveryTileHTML() {
    // Two 30-day windows: current and prior — same width so the trend is
    // apples-to-apples. Thirty days is short enough to feel "now" yet long
    // enough that one slow day doesn't whip the number around.
    const today = new Date(); today.setHours(0,0,0,0);
    const day = (n) => fmtISO(new Date(today.getTime() - n * dayMs));
    const curr = deliveryMetricsForWindow(day(30), fmtISO(today));
    const prev = deliveryMetricsForWindow(day(60), day(31));
    const MIN_SAMPLE = 5;

    // Adherence tile
    const adhInputs = [
      { name: 'on-time',        desc: 'Actions marked Done in the last 30 days whose completion date is on or before their promised due.' },
      { name: 'slipped',        desc: 'Actions marked Done in the window whose completion date is AFTER their promised due, plus actions still open whose promised due has already passed (in the window).' },
      { name: 'promised due',   desc: 'The action\'s baseline due date if a project baseline was ever set; otherwise the current due date.' },
    ];
    const adhMinimum = [
      { scope: 'Per action', desc: 'a due date, so the action has a "promised" date at all.' },
      { scope: 'Per action', desc: 'a status that actually flips to Done when the work lands — the KPI reads the "→ Done" transition in the history to date completion; if you never move cards off To do, everything looks slipped.' },
      { scope: 'Per project', desc: 'take a baseline (Dashboard → 📌 Set project baseline) as early as possible. Without a baseline, "promised due" falls back to the current due, which slides as the work slides — the KPI then just tracks the moving target.' },
    ];
    let adhTile;
    if (curr.adherenceN < MIN_SAMPLE) {
      const tt = _kpiTooltip({
        title: 'Schedule adherence (30d)',
        meaning: 'The share of actions that hit the date you promised. A trust signal for downstream stakeholders — how reliably the team delivers on its own commitments.',
        formula: 'on-time ÷ (on-time + slipped) × 100 %',
        inputs: adhInputs,
        minimumData: adhMinimum,
        notComputable: `Cannot compute — needs at least ${MIN_SAMPLE} actions whose promised due has already passed within the last 30 days. Currently ${curr.adherenceN}. Take a project baseline and let a few actions age before this KPI stabilises.`,
      });
      adhTile = `
        <div class="kpi" data-tile="adherence">
          <div class="kpi-label">Schedule adherence</div>
          <div class="kpi-value muted">—</div>
          <div class="kpi-sub">Not enough data (${curr.adherenceN}/${MIN_SAMPLE})</div>
          ${tt}
        </div>`;
    } else {
      const pct = Math.round(curr.adherence * 100);
      const band = pct >= 85 ? 'ok' : pct >= 70 ? 'warn' : 'bad';
      const trend = _trendArrow(curr.adherence, prev.adherence, {
        eps: 0.01, higherIsBetter: true,
        fmt: (d) => `${d > 0 ? '+' : '−'}${Math.abs(Math.round(d * 100))} pts`,
      });
      const tt = _kpiTooltip({
        title: 'Schedule adherence (30d)',
        meaning: `The share of actions that hit the date you promised. A trust signal for downstream stakeholders — how reliably the team delivers on its own commitments. Currently ${curr.onTime} delivered on time, ${curr.slipped} slipped or still open past due.`,
        formula: 'on-time ÷ (on-time + slipped) × 100 %',
        inputs: adhInputs,
        minimumData: adhMinimum,
      });
      adhTile = `
        <div class="kpi" data-tile="adherence">
          <div class="kpi-label">Schedule adherence</div>
          <div class="kpi-value ${band}">${pct}%</div>
          <div class="kpi-sub">${curr.onTime} on time / ${curr.adherenceN} due
            ${trend.html ? `<span class="kpi-trend ${trend.cls}">${trend.html}</span>` : ''}
          </div>
          ${tt}
        </div>`;
    }

    // Productivity tile
    const prodInputs = [
      { name: 'Σ planned hours', desc: 'Sum of planned hours across every action completed in the last 30 days. Planned hours = (commitment % ÷ 100) × 40h × weeks in the [start, due] window.' },
      { name: 'Σ logged hours',  desc: 'Sum of hours logged via the "Log time" action drawer control for those same completed actions.' },
    ];
    const prodMinimum = [
      { scope: 'Per action', desc: 'a start date AND a due date — that\'s how the KPI computes the planned window. Without both, planned hours = 0 and the action drops out of the numerator.' },
      { scope: 'Per action', desc: 'a commitment % (defaults to 100%). Set it lower for anyone splitting time across multiple projects.' },
      { scope: 'Per action', desc: 'at least one logged-hours entry (Action drawer → Log time). No log = no denominator contribution = KPI blind to that piece of work.' },
    ];
    let prodTile;
    if (curr.doneInWindow < MIN_SAMPLE) {
      const tt = _kpiTooltip({
        title: 'Productivity (30d)',
        meaning: 'How the team\'s actual pace compares to what was planned. > 1.00× means the work landed with fewer hours than planned; < 1.00× means it took longer than expected.',
        formula: 'Σ planned hours ÷ Σ logged hours   (over the last 30 days of completed actions)',
        inputs: prodInputs,
        minimumData: prodMinimum,
        notComputable: `Cannot compute — needs at least ${MIN_SAMPLE} actions completed in the last 30 days with BOTH a planned-hours value (needs start + due + commitment %) and at least one logged-hours entry. Currently ${curr.doneInWindow}. Ask owners to log their time on completed work.`,
      });
      prodTile = `
        <div class="kpi" data-tile="productivity">
          <div class="kpi-label">Productivity</div>
          <div class="kpi-value muted">—</div>
          <div class="kpi-sub">Not enough data (${curr.doneInWindow}/${MIN_SAMPLE})</div>
          ${tt}
        </div>`;
    } else {
      const ratio = curr.productivity;
      const band = ratio >= 1.0 ? 'ok' : ratio >= 0.85 ? 'warn' : 'bad';
      const trend = _trendArrow(curr.productivity, prev.productivity, {
        eps: 0.02, higherIsBetter: true,
        fmt: (d) => `${d > 0 ? '+' : '−'}${Math.abs(d).toFixed(2)}×`,
      });
      const tt = _kpiTooltip({
        title: 'Productivity (30d)',
        meaning: `How the team's actual pace compares to what was planned. > 1.00× means the work landed with fewer hours than planned; < 1.00× means it took longer than expected. Currently ${Math.round(curr.plannedHrs)}h planned vs ${Math.round(curr.loggedHrs)}h logged across ${curr.doneInWindow} actions.`,
        formula: 'Σ planned hours ÷ Σ logged hours   (over the last 30 days of completed actions)',
        inputs: prodInputs,
        minimumData: prodMinimum,
      });
      prodTile = `
        <div class="kpi" data-tile="productivity">
          <div class="kpi-label">Productivity</div>
          <div class="kpi-value ${band}">${ratio.toFixed(2)}×</div>
          <div class="kpi-sub">${Math.round(curr.plannedHrs)}h planned / ${Math.round(curr.loggedHrs)}h logged
            ${trend.html ? `<span class="kpi-trend ${trend.cls}">${trend.html}</span>` : ''}
          </div>
          ${tt}
        </div>`;
    }
    return adhTile + prodTile;
  }

  // --- Detailed weekly charts for the two delivery KPIs ---
  function chartAdherenceTrend(weeks = 12) {
    const W = 600, H = 220;
    const padL = 38, padR = 38, padT = 14, padB = 26;
    const innerW = W - padL - padR, innerH = H - padT - padB;
    const buckets = deliveryWeeklyBuckets(weeks);
    const counts = buckets.map((b) => b.onTime + b.slipped);
    const maxCount = Math.max(4, ...counts);
    const groupW = innerW / weeks;
    const barW = (groupW - 4) / 2;
    const xFor = (i) => padL + i * groupW;
    const yForC = (v) => padT + innerH - (v / maxCount) * innerH;
    const yForP = (v) => padT + innerH - v * innerH;

    const bars = buckets.map((b, i) => {
      const xBase = xFor(i) + 2;
      const yOn = yForC(b.onTime);
      const ySl = yForC(b.slipped);
      const baseLine = padT + innerH;
      const wkLabel = b.start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
      return `
        <rect class="adh-bar-on ${b.onTime > 0 ? 'is-clickable' : ''}" x="${xBase}" y="${yOn}" width="${barW}" height="${Math.max(0, baseLine - yOn)}" rx="2"
              data-adh-side="on-time" data-adh-start="${b.startISO}" data-adh-end="${b.endISO}" data-adh-label="${escapeHTML(wkLabel)}">
          <title>Week of ${wkLabel} — ${b.onTime} on time${b.onTime > 0 ? ' · click to open in Register' : ''}</title>
        </rect>
        <rect class="adh-bar-sl ${b.slipped > 0 ? 'is-clickable' : ''}" x="${xBase + barW}" y="${ySl}" width="${barW}" height="${Math.max(0, baseLine - ySl)}" rx="2"
              data-adh-side="slipped" data-adh-start="${b.startISO}" data-adh-end="${b.endISO}" data-adh-label="${escapeHTML(wkLabel)}">
          <title>Week of ${wkLabel} — ${b.slipped} slipped${b.slipped > 0 ? ' · click to open in Register' : ''}</title>
        </rect>`;
    }).join('');

    // Adherence ratio overlay (right-axis scale). Connect only buckets that
    // have enough data to be meaningful — gaps over empty weeks.
    const pts = buckets.map((b, i) => b.adherence == null ? null
      : { x: xFor(i) + groupW / 2, y: yForP(b.adherence), pct: b.adherence });
    let pathD = '';
    pts.forEach((p, i) => {
      if (!p) return;
      pathD += (pathD.endsWith('M') || pathD === '' ? `M ${p.x.toFixed(1)} ${p.y.toFixed(1)}` : ` L ${p.x.toFixed(1)} ${p.y.toFixed(1)}`);
    });
    const dots = pts.map((p, i) => !p ? '' :
      `<circle cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="3" class="adh-line-dot">
         <title>Week of ${buckets[i].start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })} — ${Math.round(p.pct * 100)}% on time</title>
       </circle>`).join('');

    const months = buckets.map((b, i) => {
      if (b.start.getDate() > 7) return '';
      return `<text class="chart-tick" x="${(xFor(i) + groupW/2)}" y="${H - 6}" text-anchor="middle">${b.start.toLocaleDateString(undefined, { month: 'short' })}</text>`;
    }).join('');
    const yTicksL = [0, Math.round(maxCount / 2), maxCount].map((v) =>
      `<g class="ytick"><line x1="${padL - 3}" x2="${padL}" y1="${yForC(v)}" y2="${yForC(v)}"/><text x="${padL - 6}" y="${yForC(v) + 3}" text-anchor="end">${v}</text></g>`).join('');
    const yTicksR = [0, 0.5, 1].map((v) =>
      `<g class="ytick"><line x1="${W - padR}" x2="${W - padR + 3}" y1="${yForP(v)}" y2="${yForP(v)}"/><text x="${W - padR + 6}" y="${yForP(v) + 3}" text-anchor="start">${Math.round(v * 100)}%</text></g>`).join('');
    const target = `<line class="adh-target" x1="${padL}" x2="${W - padR}" y1="${yForP(0.85)}" y2="${yForP(0.85)}" stroke-dasharray="3 4"/>`;

    const totals = buckets.reduce((a, b) => ({
      onTime: a.onTime + b.onTime, slipped: a.slipped + b.slipped,
    }), { onTime: 0, slipped: 0 });
    const overall = totals.onTime + totals.slipped;
    const overallPct = overall ? Math.round(totals.onTime / overall * 100) : null;

    registerChartData('adherence-trend', () => ({
      headers: ['Week start', 'On time', 'Slipped', 'Adherence %'],
      rows: buckets.map((b) => [
        b.startISO, b.onTime, b.slipped,
        b.adherence == null ? '' : Math.round(b.adherence * 100),
      ]),
    }));

    return `
      <svg viewBox="0 0 ${W} ${H}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
        ${yTicksL}${yTicksR}
        ${target}
        ${bars}
        <path d="${pathD}" class="adh-line"/>
        ${dots}
        ${months}
      </svg>
      <div class="chart-legend">
        <span class="legend-item"><span class="dot" style="background:var(--ok)"></span>On time</span>
        <span class="legend-item"><span class="dot" style="background:var(--bad)"></span>Slipped</span>
        <span class="legend-item"><span class="dot" style="background:var(--accent-2)"></span>Adherence %</span>
        <span class="legend-item legend-target">— — target 85%</span>
      </div>
      <div class="chart-foot">
        Last ${weeks}w — <b>${totals.onTime}</b> on time, <b>${totals.slipped}</b> slipped, overall <b class="${overallPct == null ? '' : overallPct >= 85 ? 'ok' : overallPct >= 70 ? 'warn' : 'bad'}">${overallPct == null ? '—' : overallPct + '%'}</b>
      </div>`;
  }

  function chartProductivityTrend(weeks = 12) {
    const W = 600, H = 220;
    const padL = 42, padR = 42, padT = 14, padB = 26;
    const innerW = W - padL - padR, innerH = H - padT - padB;
    const buckets = deliveryWeeklyBuckets(weeks);
    const hourMax = Math.max(20, ...buckets.flatMap((b) => [b.plannedHrs, b.loggedHrs]));
    // Ratio axis uses a symmetric clamp around 1× so over/underperformance
    // are both readable; cap at ~2× so a single outlier doesn't squash the
    // line to nothing.
    const ratios = buckets.map((b) => b.productivity).filter((r) => r != null);
    const ratioCap = Math.min(2, Math.max(1.5, ...ratios.map((r) => r + 0.1)));
    const groupW = innerW / weeks;
    const barW = (groupW - 4) / 2;
    const xFor = (i) => padL + i * groupW;
    const yForH = (v) => padT + innerH - (v / hourMax) * innerH;
    const yForR = (v) => padT + innerH - (v / ratioCap) * innerH;

    const bars = buckets.map((b, i) => {
      const xBase = xFor(i) + 2;
      const yP = yForH(b.plannedHrs);
      const yL = yForH(b.loggedHrs);
      const baseLine = padT + innerH;
      return `
        <rect class="prod-bar-pl" x="${xBase}" y="${yP}" width="${barW}" height="${Math.max(0, baseLine - yP)}" rx="2">
          <title>Week of ${b.start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })} — ${Math.round(b.plannedHrs)}h planned</title>
        </rect>
        <rect class="prod-bar-lg" x="${xBase + barW}" y="${yL}" width="${barW}" height="${Math.max(0, baseLine - yL)}" rx="2">
          <title>Week of ${b.start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })} — ${Math.round(b.loggedHrs)}h logged</title>
        </rect>`;
    }).join('');

    const pts = buckets.map((b, i) => b.productivity == null ? null
      : { x: xFor(i) + groupW / 2, y: yForR(Math.min(ratioCap, b.productivity)), r: b.productivity });
    let pathD = '';
    pts.forEach((p) => {
      if (!p) return;
      pathD += (pathD === '' ? `M ${p.x.toFixed(1)} ${p.y.toFixed(1)}` : ` L ${p.x.toFixed(1)} ${p.y.toFixed(1)}`);
    });
    const dots = pts.map((p, i) => !p ? '' :
      `<circle cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="3" class="prod-line-dot">
         <title>Week of ${buckets[i].start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })} — ${p.r.toFixed(2)}× productivity</title>
       </circle>`).join('');

    const months = buckets.map((b, i) => {
      if (b.start.getDate() > 7) return '';
      return `<text class="chart-tick" x="${(xFor(i) + groupW/2)}" y="${H - 6}" text-anchor="middle">${b.start.toLocaleDateString(undefined, { month: 'short' })}</text>`;
    }).join('');
    const yTicksL = [0, Math.round(hourMax / 2), Math.round(hourMax)].map((v) =>
      `<g class="ytick"><line x1="${padL - 3}" x2="${padL}" y1="${yForH(v)}" y2="${yForH(v)}"/><text x="${padL - 6}" y="${yForH(v) + 3}" text-anchor="end">${v}h</text></g>`).join('');
    const yTicksR = [0, 1, ratioCap].map((v) =>
      `<g class="ytick"><line x1="${W - padR}" x2="${W - padR + 3}" y1="${yForR(v)}" y2="${yForR(v)}"/><text x="${W - padR + 6}" y="${yForR(v) + 3}" text-anchor="start">${v.toFixed(1)}×</text></g>`).join('');
    const onePar = `<line class="prod-target" x1="${padL}" x2="${W - padR}" y1="${yForR(1)}" y2="${yForR(1)}" stroke-dasharray="3 4"/>`;

    const totals = buckets.reduce((a, b) => ({
      planned: a.planned + b.plannedHrs, logged: a.logged + b.loggedHrs, done: a.done + b.doneInWindow,
    }), { planned: 0, logged: 0, done: 0 });
    const overall = totals.logged > 0 ? totals.planned / totals.logged : null;

    registerChartData('productivity-trend', () => ({
      headers: ['Week start', 'Planned hours', 'Logged hours', 'Done actions', 'Productivity ×'],
      rows: buckets.map((b) => [
        b.startISO,
        Number(b.plannedHrs.toFixed(2)),
        Number(b.loggedHrs.toFixed(2)),
        b.doneInWindow,
        b.productivity == null ? '' : Number(b.productivity.toFixed(3)),
      ]),
    }));

    return `
      <svg viewBox="0 0 ${W} ${H}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
        ${yTicksL}${yTicksR}
        ${onePar}
        ${bars}
        <path d="${pathD}" class="prod-line"/>
        ${dots}
        ${months}
      </svg>
      <div class="chart-legend">
        <span class="legend-item"><span class="dot" style="background:var(--accent)"></span>Planned hours</span>
        <span class="legend-item"><span class="dot" style="background:var(--accent-2)"></span>Logged hours</span>
        <span class="legend-item"><span class="dot" style="background:var(--ok)"></span>Productivity ×</span>
        <span class="legend-item legend-target">— — par 1.0×</span>
      </div>
      <div class="chart-foot">
        Last ${weeks}w — <b>${Math.round(totals.planned)}h</b> planned / <b>${Math.round(totals.logged)}h</b> logged across <b>${totals.done}</b> done actions, overall <b class="${overall == null ? '' : overall >= 1 ? 'ok' : overall >= 0.85 ? 'warn' : 'bad'}">${overall == null ? '—' : overall.toFixed(2) + '×'}</b>
      </div>`;
  }

  // Global hover-tooltip system. Every anchor keeps its `.kpi-tt`
  // element as source content (kept in DOM for authoring locality).
  // On hover we CLONE that content into a single body-level portal —
  // this escapes every parent stacking context so the tooltip cannot
  // be painted over by sibling panels. Placement never overlaps the
  // anchor: prefer the side (below / above) that fits fully; if
  // neither side has room, pick whichever has more and let the panel
  // scroll internally.
  function _getKpiTooltipPortal() {
    let portal = document.getElementById('kpiTooltipPortal');
    if (!portal) {
      portal = document.createElement('div');
      portal.id = 'kpiTooltipPortal';
      portal.className = 'kpi-tt kpi-tt-portal';
      portal.setAttribute('role', 'tooltip');
      document.body.appendChild(portal);
    }
    return portal;
  }
  function _wireDashboardTooltips(root) {
    const portal = _getKpiTooltipPortal();
    let currentAnchor = null;
    let hideTimer = null;
    const HIDE_DELAY = 180; // ms — long enough to cross the 8px gap between tile and portal
    const cancelHide = () => {
      if (hideTimer) { clearTimeout(hideTimer); hideTimer = null; }
    };
    const hide = () => {
      cancelHide();
      if (!currentAnchor) return;
      portal.classList.remove('is-visible');
      portal.style.maxHeight = '';
      currentAnchor = null;
    };
    const scheduleHide = () => {
      cancelHide();
      hideTimer = setTimeout(hide, HIDE_DELAY);
    };
    // Portal itself is a hover target — moving the pointer INTO the
    // portal cancels a pending hide (so scrollable tooltips are
    // reachable). Leaving the portal schedules a hide unless the
    // pointer went back into the anchor.
    portal.addEventListener('mouseenter', cancelHide);
    portal.addEventListener('mouseleave', (e) => {
      if (currentAnchor && currentAnchor.contains(e.relatedTarget)) return;
      scheduleHide();
    });
    const place = (anchor, sourceHtml) => {
      portal.innerHTML = sourceHtml;
      portal.style.maxHeight = ''; // let it grow to intrinsic size for measure
      portal.style.left = '-99999px';
      portal.style.top = '0';
      portal.classList.add('is-visible');
      // Measure and place in the next frame — the browser applies the
      // reset first, then we can trust offsetWidth / offsetHeight.
      requestAnimationFrame(() => {
        const rect = anchor.getBoundingClientRect();
        const vw = window.innerWidth;
        const vh = window.innerHeight;
        const margin = 12;
        const gap = 8;
        const ttWidth  = portal.offsetWidth;
        const ttHeight = portal.offsetHeight;
        const spaceBelow = vh - rect.bottom - gap - margin;
        const spaceAbove = rect.top - gap - margin;
        let top, maxH = null;
        if (ttHeight <= spaceBelow) {
          // Fits fully below — the preferred side.
          top = rect.bottom + gap;
        } else if (ttHeight <= spaceAbove) {
          // Fits fully above — flip.
          top = rect.top - ttHeight - gap;
        } else if (spaceBelow >= spaceAbove) {
          // Neither side fits fully; below has more room. Constrain
          // maxHeight to the EXACT space so the panel scrolls
          // internally instead of crossing back into the anchor.
          top = rect.bottom + gap;
          maxH = spaceBelow;
        } else {
          // Above has more room. top = margin + (rect.top-spaceAbove-gap),
          // which reduces to just `margin`, but we derive it from spaceAbove
          // so the maxHeight below can't push the tooltip's bottom into the
          // anchor's top edge.
          top = rect.top - spaceAbove - gap;
          maxH = spaceAbove;
        }
        // Horizontal: start at anchor's left, shift left only if it would
        // run off the right edge.
        let left = rect.left;
        if (left + ttWidth > vw - margin) {
          left = Math.max(margin, vw - ttWidth - margin);
        }
        portal.style.left = Math.round(left) + 'px';
        portal.style.top  = Math.round(top) + 'px';
        if (maxH != null) portal.style.maxHeight = Math.round(maxH) + 'px';
      });
    };
    root.addEventListener('mouseover', (e) => {
      // Walk up to find the nearest anchor that has a direct .kpi-tt
      // source element (and is NOT the portal itself).
      let node = e.target;
      let tt = null;
      while (node && node !== root) {
        if (node.classList && !node.classList.contains('kpi-tt-portal')) {
          tt = node.querySelector && node.querySelector(':scope > .kpi-tt');
          if (tt) break;
        }
        node = node.parentElement;
      }
      if (!tt) return;
      cancelHide(); // pointer is on a valid anchor — don't hide
      if (currentAnchor === node) return;
      currentAnchor = node;
      place(node, tt.innerHTML);
    });
    root.addEventListener('mouseout', (e) => {
      if (!currentAnchor) return;
      const to = e.relatedTarget;
      // Stay open when the pointer is still inside the current anchor.
      if (to && currentAnchor.contains(to)) return;
      // If the pointer is heading TO the portal, don't hide — the
      // scheduleHide lets it survive the 8px gap between tile and
      // portal, and the portal's own mouseenter cancels the timer.
      if (to && portal.contains(to)) { cancelHide(); return; }
      scheduleHide();
    });
    // Clean up on document scroll / resize so the tooltip doesn't
    // drift out of its target position. IMPORTANT: ignore scroll
    // events that come from INSIDE the portal itself — otherwise the
    // scrollbar in a scrollable tooltip would immediately dismiss it.
    window.addEventListener('scroll', (e) => {
      if (portal.contains(e.target)) return;
      hide();
    }, { passive: true, capture: true });
    window.addEventListener('resize', hide);
  }

  // Dashboard heatmap segment — 'risk' or 'opportunity'. Session-scoped;
  // persists across renders but not across reloads.
  let _heatKind = 'risk';

  function renderDashboard(root) {
    const proj = curProject();
    const k = kpis();
    const decisionKpis = computeDecisionKpis(proj);
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">${escapeHTML(proj.name)} — Dashboard</div>
          <div class="page-sub">Health summary based on current data</div>
        </div>
        <div class="page-actions">
          <button class="ghost" id="btnProjBaseline" title="Freeze the current plan as a baseline for every live action — drives SPI and the schedule-variance column in the Register.">📌 Set project baseline</button>
        </div>
      </div>
      ${decisionKpisHTML(decisionKpis)}
      <div class="dashboard">
        <div class="kpi clickable" data-dash-filter="late">
          <div class="kpi-label">Late items</div>
          <div class="kpi-value ${k.late > 0 ? 'bad' : 'ok'}">${k.late}</div>
          <div class="kpi-sub">${k.lateRate}% of all actions</div>
          ${_kpiTooltip({
            title: 'Late items',
            meaning: 'Open actions whose due date has passed. Every late item is a broken commitment — replan or complete now.',
            formula: 'count( actions where a.due < today AND a.status ∉ {Done, Cancelled} AND a is live )',
            inputs: [
              { name: 'a.due',    desc: 'Current due date on each action.' },
              { name: 'today',    desc: 'Today\'s calendar date.' },
              { name: 'a.status', desc: 'One of Not started · In progress · Blocked · Done · Cancelled.' },
              { name: 'live',     desc: 'Non-snoozed. Archived actions are counted — archiving is a UI hide, not a data drop.' },
            ],
            minimumData: [
              { scope: 'Per action', desc: 'a due date and a status (default status is "Not started"). Actions without a due date are ignored — the KPI can\'t know if they\'re late.' },
            ],
          })}
        </div>
        <div class="kpi clickable" data-dash-filter="blocked">
          <div class="kpi-label">Blocked</div>
          <div class="kpi-value ${k.blocked > 0 ? 'warn' : 'ok'}">${k.blocked}</div>
          <div class="kpi-sub">${k.blockedRatio}% of all actions</div>
          ${_kpiTooltip({
            title: 'Blocked',
            meaning: 'Actions the owner cannot progress because of an external dependency. Investigate what\'s blocking and clear the path or reassign.',
            formula: 'count( actions where a.status == "Blocked" AND a is live )',
            inputs: [
              { name: 'a.status', desc: 'The action\'s current status column on the Board.' },
              { name: 'live',     desc: 'Non-snoozed. Archived actions are counted — archiving is a UI hide, not a data drop.' },
            ],
            minimumData: [
              { scope: 'Per action', desc: 'a status — move the card to the Blocked column on the Board (or set status to Blocked in the Register / drawer) as soon as work stops.' },
            ],
          })}
        </div>
        <div class="kpi clickable" data-dash-filter="week">
          <div class="kpi-label">Due ≤ 7 days</div>
          <div class="kpi-value ${k.upcoming > 4 ? 'warn' : ''}">${k.upcoming}</div>
          <div class="kpi-sub">Upcoming workload</div>
          ${_kpiTooltip({
            title: 'Due within 7 days',
            meaning: 'Open actions whose due date falls in the next seven days. Sanity-check for the coming week — is anyone drowning, is any hand-off missing an owner?',
            formula: 'count( actions where 0 ≤ (a.due − today) ≤ 7 AND a.status ∉ {Done, Cancelled} AND a is live )',
            inputs: [
              { name: 'a.due',    desc: 'Current due date on each action.' },
              { name: 'today',    desc: 'Today\'s calendar date.' },
              { name: 'a.status', desc: 'The action\'s current status column on the Board.' },
            ],
            minimumData: [
              { scope: 'Per action', desc: 'a due date. Actions without one aren\'t "upcoming" as far as this KPI can tell.' },
            ],
          })}
        </div>
        <div class="kpi clickable" data-dash-filter="done">
          <div class="kpi-label">Completion</div>
          <div class="kpi-value ${k.completionRate >= 70 ? 'ok' : ''}">${k.completionRate}%</div>
          <div class="kpi-sub">${k.done} / ${k.total} done • throughput ${k.throughput}/2w</div>
          ${_kpiTooltip({
            title: 'Completion',
            meaning: 'The share of the project\'s live actions that are marked Done. A snapshot of progress against the current scope — but it doesn\'t know about scope creep, so it\'s meaningless for cumulative reporting unless the scope is stable.',
            formula: 'done ÷ total × 100 %',
            inputs: [
              { name: 'done',  desc: 'Count of actions with status = Done.' },
              { name: 'total', desc: 'Count of live actions (non-deleted, non-snoozed, includes every status except archived).' },
              { name: 'throughput (2w)', desc: 'Sub-metric shown below: number of actions marked Done in the last 14 days. Higher = more work landing per week.' },
            ],
            minimumData: [
              { scope: 'Per action', desc: 'a status, kept up to date. Actions dragged to Done on the Board or ticked off in the drawer count as done. Actions with the default "Not started" status count as pending — leaving old work in that column will lower the number.' },
            ],
            notComputable: k.total === 0
              ? 'Cannot compute — the project has no live actions yet. Add some via the Board or the Register.'
              : null,
          })}
        </div>
        ${deliveryTileHTML()}

        <div class="panel half">
          <div class="panel-title">Critical focus</div>
          <div class="crit-list" id="critList"></div>
        </div>
        <div class="panel half">
          <div class="panel-title">Workload by person
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Workload by person',
                meaning: 'Each row is one team member; the bar shows what share of their FTE is currently committed across their open actions vs their configured capacity. Amber above 80 %, red above 100 %. External stakeholders don\'t appear here — they don\'t have a team-side capacity.',
                formula: 'load%(p) = Σ commitment%(a) ÷ capacity%(p) × 100  ·  over open actions owned by p',
                inputs: [
                  { name: 'commitment%(a)', desc: 'The action\'s Commitment slider in the drawer — its share of one FTE while it\'s in flight.' },
                  { name: 'capacity%(p)',   desc: 'The person\'s "Capacity %" on the People page. 100 = 1 full FTE, 50 = half-time.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'an assigned owner, a status, and a commitment %. Actions with no commitment default to 100 %.' },
                  { scope: 'Per person', desc: 'a capacity % (defaults to 100). Zero-capacity people are treated as unavailable.' },
                ],
              })}
            </span>
          </div>
          <div id="workload"></div>
        </div>

        <div class="panel">
          <div class="panel-title">Status mix</div>
          <div id="statusMix"></div>
        </div>

        <div class="panel evm-panel">
          <div class="panel-title">Earned-value rollup
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Earned-value rollup',
                meaning: 'A one-panel view of the eight core EVM numbers rolled up across every live action. Read left-to-right: BAC / PV / EV / AC are money numbers, CPI and SPI are the health indices (>1 = good), EAC / ETC are the forecasts.',
                formula: 'See each cell for its formula. Everything flows from planned_hours × hourly_rate and logged_hours × hourly_rate.',
                inputs: [
                  { name: 'action.startDate / due / commitment',       desc: 'Define the planning window and effort level.' },
                  { name: 'action.percentComplete or status',           desc: 'Drives EV — how much of BAC has been earned.' },
                  { name: 'action.loggedHours entries',                 desc: 'Drive AC — money actually spent.' },
                  { name: 'person.hourlyRate',                          desc: 'Converts hours to money.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'a start date, a due date, an owner, a commitment %, plus at least one logged-hours entry when the work starts. If the action carries no baseline, its BAC still counts — but slippage won\'t show unless you take a baseline (Dashboard → 📌 Set project baseline).' },
                  { scope: 'Per person', desc: 'an hourly rate (People page). Without it, every cost roll-up understates.' },
                ],
              })}
            </span>
            <span class="legend">action-level · uses baselines + logged hours · independent of cost-centre budgets</span>
          </div>
          <div id="evmRollup"></div>
        </div>

        <div class="panel risk-heat-panel copyable-chart" data-chart="risk-heatmap" data-heat-kind="${_heatKind}">
          <div class="panel-title">
            Portfolio heatmap
            <div class="seg" role="tablist" style="margin-left:10px;">
              <button type="button" class="seg-btn ${_heatKind === 'risk' ? 'active' : ''}" data-heat-kind="risk">▲ Risks</button>
              <button type="button" class="seg-btn ${_heatKind === 'opportunity' ? 'active' : ''}" data-heat-kind="opportunity">▽ Opportunities</button>
            </div>
            <span class="legend">5×5 · cells aggregate matching items across projects · click a cell to filter the register</span>
          </div>
          <div id="riskHeat"></div>
        </div>
      </div>

      <div class="dashboard-section-break">
        <div class="dashboard-section-title">Charts</div>
        <div class="dashboard-section-sub">Trends and projections across the portfolio</div>
      </div>
      <div class="charts-grid">
        <div class="panel chart-panel half copyable-chart" data-chart="adherence-trend">
          <div class="panel-title">Schedule adherence trend
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Schedule adherence — 12-week trend',
                meaning: 'Weekly bars split into ON-TIME (green) and SLIPPED (red) counts; the blue line is the same-week adherence %. Trend up = the team is getting more reliable; sudden drops flag a bad week to dig into.',
                formula: 'per week w: adherence% = on-time(w) ÷ (on-time(w) + slipped(w)) × 100',
                inputs: [
                  { name: 'on-time(w)', desc: 'Actions completed in week w whose completed date ≤ promised due.' },
                  { name: 'slipped(w)', desc: 'Actions completed in week w whose completed date > promised due, plus open actions past their promised due within the week.' },
                  { name: 'promised due', desc: 'Baseline due if set, else current due.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'a due date and a status that flips to Done when the work lands. Actions with no due date are ignored.' },
                  { scope: 'Per project', desc: 'take a baseline early. Without it, the line will always look flat because the "promise" slides with the work.' },
                ],
              })}
            </span>
            <span class="legend">last 12 weeks • bars = closed actions per week • line = % on-time</span>
          </div>
          ${chartAdherenceTrend(12)}
        </div>
        <div class="panel chart-panel half copyable-chart" data-chart="productivity-trend">
          <div class="panel-title">Productivity trend
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Productivity — 12-week trend',
                meaning: 'Per-week bars show the sum of planned vs logged hours across actions completed that week; the green line is the weekly productivity ratio. Line above 1.0× = beating the plan; below 1.0× = burning more effort than budgeted.',
                formula: 'per week w: productivity(w) = Σ planned_hours(a) ÷ Σ logged_hours(a)   over actions completed in week w',
                inputs: [
                  { name: 'planned_hours(a)', desc: '(commitment_%/100) × 40h × weeks in [start, due].' },
                  { name: 'logged_hours(a)',  desc: 'Sum of "Log time" entries on the action.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'a start date, a due date, a commitment %, plus at least one logged-hours entry when the work is complete.' },
                  { scope: 'Per person', desc: 'nothing extra — the ratio is unit-less.' },
                ],
              })}
            </span>
            <span class="legend">last 12 weeks • bars = hours per week • line = planned ÷ logged</span>
          </div>
          ${chartProductivityTrend(12)}
        </div>
        <div class="panel chart-panel copyable-chart" data-chart="waterfall">
          <div class="panel-title">Schedule deviation waterfall
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Schedule deviation waterfall',
                meaning: 'Every open action is one line. X = when the forecast was made, Y = what date the forecast said. A horizontal line = the forecast held. A rising line = the due date got pushed. The 45° diagonal is "you are here now" — any line that ends on it landed on time; anything above the diagonal is currently late.',
                formula: 'For each action a and each history entry h of type "Schedule: X → Y": plot the point (h.at, Y).',
                inputs: [
                  { name: 'action.history', desc: 'Every drag-to-reschedule or manual due-date edit records a "Schedule: <old> → <new>" entry with a timestamp.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'a due date — and reschedule it in-app (drag on the Gantt, edit in the drawer) rather than deleting/recreating. Every move is logged automatically.' },
                ],
              })}
            </span>
            <span class="legend">x = when forecast was made • y = forecast due • diagonal = delivered now</span>
          </div>
          ${chartWaterfall()}
        </div>
        <div class="panel chart-panel half copyable-chart" data-chart="cumulative-workload">
          <div class="panel-title">Cumulative workload (next 12 weeks)
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Cumulative workload — next 12 weeks',
                meaning: 'Stacked area of each person\'s forecast open workload week-by-week, ending at the visible cap line (sum of capacities). Peaks above the cap = the team is over-committed in that week — reallocate or delay.',
                formula: 'per week w, per person p: load(w, p) = count( a where owner(a)=p AND a.due within week w AND a.status ∉ {Done, Cancelled} )',
                inputs: [
                  { name: 'action.owner', desc: 'Assigned person.' },
                  { name: 'action.due',   desc: 'Determines the week each action lands in.' },
                  { name: 'person.capacity', desc: 'Sums into the cap line.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'an assigned owner AND a due date. Unassigned or undated actions vanish from this forecast.' },
                  { scope: 'Per person', desc: 'a capacity value (defaults to 5).' },
                ],
              })}
            </span>
            <span class="legend">click a name to hide / show</span>
          </div>
          <div id="cumWlSlot">${chartCumulativeWorkload(12)}</div>
        </div>
        <div class="panel chart-panel half copyable-chart" data-chart="flow">
          <div class="panel-title">Activity / week (last 12 weeks)
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Activity / week — last 12 weeks',
                meaning: 'Per-week creation vs completion. Line = actions created that week. Bars = actions done (green) or slipped (red). Net = done − created in the trailing period; positive = the backlog is shrinking.',
                formula: 'per week w: created(w) = count(a where a.createdAt in w); done(w) = count(a where done-transition in a.history is in w); slipped(w) = count of schedule-forward events in w.',
                inputs: [
                  { name: 'action.createdAt',       desc: 'Timestamp of action creation.' },
                  { name: 'action.history',         desc: 'Contains the "Status: → Done" transitions and "Schedule: X → Y" moves.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'Create actions in-app rather than importing bulk. The chart reads timestamps that the app writes automatically. Manually-imported actions with no history entries won\'t register as done/slipped events.' },
                ],
              })}
            </span>
          </div>
          ${chartFlow(12)}
        </div>
        <div class="panel chart-panel copyable-chart" data-chart="per-person-workload">
          <div class="panel-title">Per-person workload (next 12 weeks)
            <span class="tt-info" aria-label="Info">i
              ${_kpiTooltip({
                title: 'Per-person workload — next 12 weeks',
                meaning: 'One mini-chart per person: forecast open workload week-by-week, with a dashed cap line at their capacity. Peak bars label the week they\'re most stretched. Reading tall spikes across multiple people at once = a critical week.',
                formula: 'per week w, per person p: bar height = count(open actions with due in w); cap = person.capacity.',
                inputs: [
                  { name: 'action.owner', desc: 'Assigned person.' },
                  { name: 'action.due',   desc: 'Which week the bar lands in.' },
                  { name: 'person.capacity', desc: 'The dashed cap line.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'an owner AND a due date.' },
                  { scope: 'Per person', desc: 'a capacity value; leave at 5 if you have no better estimate.' },
                ],
              })}
            </span>
          </div>
          ${chartPerPerson()}
        </div>
      </div>`;
    root.appendChild(view);
    wireCumWlLegend();
    _wireDashboardTooltips(view);
    // Heatmap segment toggle — flip between risks and opportunities.
    view.querySelector('[data-chart="risk-heatmap"]')?.addEventListener('click', (e) => {
      const btn = e.target.closest('.seg-btn[data-heat-kind]');
      if (!btn) return;
      _heatKind = btn.dataset.heatKind;
      render();
    });
    // Waterfall legend → toggle a line's visibility (matches the
    // cumulative-workload chart's pattern).
    view.querySelector('[data-chart="waterfall"]')?.addEventListener('click', (e) => {
      const btn = e.target.closest('.legend-item[data-wf-id]');
      if (!btn) return;
      const id = btn.dataset.wfId;
      if (_wfHidden.has(id)) _wfHidden.delete(id); else _wfHidden.add(id);
      const panel = view.querySelector('[data-chart="waterfall"]');
      const title = panel.querySelector('.panel-title');
      // Re-render just the waterfall body so the rest of the dashboard
      // stays scrolled where the user left it.
      const rest = panel.innerHTML;
      const newBody = chartWaterfall();
      // Replace everything after the title.
      panel.innerHTML = '';
      panel.appendChild(title);
      const holder = document.createElement('div');
      holder.innerHTML = newBody;
      while (holder.firstChild) panel.appendChild(holder.firstChild);
    });
    // Adherence-trend bars → drill into Register scoped to that week and
    // side (on-time / slipped). Only bars with a non-zero count carry
    // .is-clickable, so we gate on that.
    view.querySelector('[data-chart="adherence-trend"]')?.addEventListener('click', (e) => {
      const rect = e.target.closest('rect.is-clickable[data-adh-side]');
      if (!rect) return;
      _dashDrill = {
        kind: 'adherence-week',
        side: rect.dataset.adhSide,
        startISO: rect.dataset.adhStart,
        endISO: rect.dataset.adhEnd,
        label: `${rect.dataset.adhSide === 'on-time' ? 'On time' : 'Slipped'} · week of ${rect.dataset.adhLabel}`,
      };
      state.currentView = 'register';
      saveState();
      render();
    });
    // Activity/week (flow) bars + created-line dots → same drill mechanic
    // as adherence. done bar / slipped bar / created dot each surface a
    // different week-scoped filter.
    view.querySelector('[data-chart="flow"]')?.addEventListener('click', (e) => {
      const hit = e.target.closest('.is-clickable[data-flow-side]');
      if (!hit) return;
      const side = hit.dataset.flowSide;
      const sideLabel = side === 'done' ? 'Done' : side === 'slipped' ? 'Slipped' : 'Created';
      _dashDrill = {
        kind: 'flow-week',
        side,
        startISO: hit.dataset.flowStart,
        endISO: hit.dataset.flowEnd,
        label: `${sideLabel} · week of ${hit.dataset.flowLabel}`,
      };
      state.currentView = 'register';
      saveState();
      render();
    });

    // Stale-action rows in the decision-KPI panel → open the drawer for that action
    $$('.dkpi-list-row[data-action-id]', view).forEach((el) => {
      el.addEventListener('click', () => openDrawer(el.dataset.actionId));
    });

    // Project-wide baseline command — freezes the plan for every live action.
    // Uses the action-level helpers so it Just Works without cost-centre setup.
    $('#btnProjBaseline')?.addEventListener('click', () => {
      const targetProjects = curProjectIsMerged() ? state.projects : [proj];
      const live = targetProjects.flatMap((p) => (p.actions || []).filter((a) => !a.archivedAt && !isClosedStatus(a.status)));
      const already = live.filter((a) => a.baseline).length;
      const fresh   = live.length - already;
      const summary = already
        ? `Baseline ${live.length} live actions? ${already} already have a baseline (will be re-set).`
        : `Baseline ${live.length} live actions?`;
      if (!confirm(summary + '\nThe current due / start / commitment will be frozen as each action\'s baseline.')) return;
      const today = todayISO();
      live.forEach((a) => {
        a.baseline = {
          due: a.due || null,
          startDate: a.startDate || null,
          commitment: (typeof a.commitment === 'number') ? a.commitment : 100,
          takenAt: today,
        };
        a.history.push({ at: today, what: 'Baseline taken (project-wide)' });
      });
      commit('baseline-project');
      toast(`Baseline set on ${live.length} action${live.length === 1 ? '' : 's'}${already ? ` (${fresh} new, ${already} re-set)` : ''}`);
    });

    // Dashboard KPI clicks → navigate to Register pre-filtered.
    // Double-click clears all filters and still navigates to Register.
    $$('.kpi.clickable[data-dash-filter]', view).forEach((el) => {
      el.addEventListener('click', () => {
        const f = el.dataset.dashFilter;
        if (f === 'late' || f === 'week') applyTopbarFilter({ due: f, status: '', view: 'register' });
        else if (f === 'blocked' || f === 'done') applyTopbarFilter({ status: f, due: '', view: 'register' });
      });
      el.addEventListener('dblclick', (e) => {
        e.preventDefault();
        applyTopbarFilter({ clearAll: true, view: 'register' });
      });
    });

    // Critical focus: late + blocked, then top upcoming
    const today = todayISO();
    const critical = (proj.actions || [])
      .filter((a) => (a.status !== 'done') && ((a.due && dayDiff(a.due, today) < 0) || a.status === 'blocked'))
      .sort((a, b) => (a.due || '9999').localeCompare(b.due || '9999'))
      .slice(0, 6);
    const list = $('#critList');
    if (!critical.length) {
      list.innerHTML = '<div class="empty">No late or blocked items — nice.</div>';
    } else {
      list.innerHTML = critical.map((a) => `
        <div class="crit-item clickable" data-id="${a.id}">
          <span class="dot" style="background:${a.status === 'blocked' ? 'var(--bad)' : 'var(--bad)'}"></span>
          <span>${escapeHTML(a.title)}</span>
          <span class="meta">${escapeHTML(personName(a.owner))} • ${a.due ? fmtDate(a.due) : 'no date'} • ${a.status}</span>
        </div>`).join('');
      $$('.crit-item', list).forEach((el) => el.addEventListener('click', () => openDrawer(el.dataset.id)));
    }

    // Workload bars — % of FTE committed vs available.
    const w = $('#workload');
    w.innerHTML = k.workload.map((p) => {
      const pct = p.capacity > 0 ? clamp(Math.round((p.committed / p.capacity) * 100), 0, 200) : 0;
      const cls = pct > 100 ? 'over' : pct > 80 ? 'warn' : 'ok';
      return `
        <div class="bar-row" title="${p.openCount} open action${p.openCount === 1 ? '' : 's'} · ${p.committed}% committed against ${p.capacity}% capacity">
          <div>${escapeHTML(p.name)}</div>
          <div class="bar-track"><div class="bar-fill ${cls}" style="width:${Math.min(100, pct)}%"></div></div>
          <div class="bar-val">${p.committed}/${p.capacity}%</div>
        </div>`;
    }).join('');

    // Status mix
    const sm = $('#statusMix');
    const total = k.total || 1;
    const segs = STATUSES.map((s) => {
      const n = (proj.actions || []).filter((a) => a.status === s.id).length;
      const pct = Math.round((n / total) * 100);
      return { name: s.name, n, pct, dot: s.dot };
    });
    sm.innerHTML = segs.map((s) => `
      <div class="bar-row">
        <div><span class="col-dot ${s.dot}" style="display:inline-block;margin-right:6px;"></span>${s.name}</div>
        <div class="bar-track"><div class="bar-fill" style="width:${s.pct}%"></div></div>
        <div class="bar-val">${s.n}</div>
      </div>`).join('');

    // ── Portfolio risk heatmap ─────────────────────────────────────────
    const heatEl = $('#riskHeat');
    if (heatEl) {
      // Collect every risk + opp across the projects in scope, tagged with
      // their owning project so the user can see provenance on hover and
      // drill back into the source.
      const scopeProjs = curProjectIsMerged()
        ? state.projects.filter((p) => !p.archived)
        : [proj];
      // Filter by the segment toggle above the heatmap. Default 'risk'
      // (initialised in the closure above renderDashboard).
      const riskItems = scopeProjs.flatMap((p) => (p.risks || [])
        .filter((r) => (r.kind || 'risk') === _heatKind)
        .map((r) => ({ r, proj: p })));
      // Build a 5×5 cell index keyed by `${p}-${i}` of residual scores
      // (1-5 each axis). Cells store the matching items so the cell click
      // can drill in.
      const cells = new Map();
      riskItems.forEach(({ r, proj: pr }) => {
        ensureRiskShape(r);
        const res = getResidual(r);
        const p = Math.max(1, Math.min(5, res.probability || 0));
        const i = Math.max(1, Math.min(5, res.impact || 0));
        const key = `${p}-${i}`;
        if (!cells.has(key)) cells.set(key, []);
        cells.get(key).push({ r, proj: pr });
      });
      // Project breakdown — per-project residual exposure (sum of scores).
      const perProject = scopeProjs.map((p) => {
        const items = (p.risks || []).map((r) => ({ r, score: (getResidual(r).probability || 0) * (getResidual(r).impact || 0) }));
        const tot = items.reduce((s, x) => s + x.score, 0);
        const high = items.filter((x) => x.score >= 12).length;
        const opp = (p.risks || []).filter((r) => (r.kind || 'risk') === 'opportunity').length;
        return { p, tot, high, count: items.length, opp };
      }).sort((a, b) => b.tot - a.tot);
      // Build the SVG matrix: P (x) low→high, I (y) low→high. Colour cells
      // by total residual score within the cell. Critical zone (P×I ≥ 12)
      // shaded amber, severe (≥ 16) red. Empty cells render as faint.
      const W = 360, H = 220, padL = 36, padB = 30, padT = 8, padR = 10;
      const innerW = W - padL - padR;
      const innerH = H - padT - padB;
      const cellW = innerW / 5;
      const cellH = innerH / 5;
      registerChartData('risk-heatmap', () => {
        const rows = [];
        for (let p = 1; p <= 5; p++) for (let i = 1; i <= 5; i++) {
          const cell = cells.get(`${p}-${i}`) || [];
          const tot = cell.reduce((s, x) => s + (getResidual(x.r).probability || 0) * (getResidual(x.r).impact || 0), 0);
          rows.push([p, i, p * i, cell.length, tot]);
        }
        return { headers: ['Probability', 'Impact', 'Base score', 'Items in cell', 'Sum of residual scores'], rows };
      });
      let svg = `<svg class="risk-heat-svg" viewBox="0 0 ${W} ${H}" width="${W}" height="${H}">`;
      // Axis labels
      svg += `<text class="rh-axis" x="${padL + innerW/2}" y="${H - 6}" text-anchor="middle">Probability →</text>`;
      svg += `<text class="rh-axis" transform="translate(10, ${padT + innerH/2}) rotate(-90)" text-anchor="middle">Impact →</text>`;
      for (let p = 1; p <= 5; p++) {
        const x = padL + (p - 1) * cellW;
        svg += `<text class="rh-tick" x="${x + cellW/2}" y="${H - padB + 14}" text-anchor="middle">${p}</text>`;
        for (let i = 1; i <= 5; i++) {
          const y = padT + (5 - i) * cellH; // higher impact at top
          const cell = cells.get(`${p}-${i}`) || [];
          const totScore = cell.reduce((s, x) => s + (getResidual(x.r).probability || 0) * (getResidual(x.r).impact || 0), 0);
          const baseScore = p * i;
          // Cell tint = base risk severity. Heat overlay = density of items
          // in this cell (sum of scores).
          const zoneCls = baseScore >= 15 ? 'rh-severe' : baseScore >= 9 ? 'rh-warn' : baseScore >= 4 ? 'rh-mid' : 'rh-low';
          const opacity = Math.min(.8, .15 + totScore * 0.02);
          svg += `<rect class="rh-cell ${zoneCls}" data-p="${p}" data-i="${i}" x="${x}" y="${y}" width="${cellW - 1}" height="${cellH - 1}" rx="3" style="opacity:${opacity.toFixed(2)};"></rect>`;
          if (cell.length > 0) {
            svg += `<text class="rh-cell-num" x="${x + cellW/2}" y="${y + cellH/2 + 4}" text-anchor="middle">${cell.length}</text>`;
          }
          if (p === 1) {
            svg += `<text class="rh-tick" x="${padL - 6}" y="${y + cellH/2 + 3}" text-anchor="end">${i}</text>`;
          }
        }
      }
      svg += `</svg>`;
      // Top-3 highest-residual risks across the portfolio
      const sortedRisks = riskItems
        .map(({ r, proj: pr }) => ({ r, proj: pr, score: (getResidual(r).probability || 0) * (getResidual(r).impact || 0) }))
        .filter((x) => x.score > 0)
        .sort((a, b) => b.score - a.score)
        .slice(0, 5);
      heatEl.innerHTML = `
        <div class="rh-wrap">
          ${svg}
          <div class="rh-side">
            <div class="rh-side-title">Top residual exposure</div>
            ${sortedRisks.length ? sortedRisks.map(({ r, proj: pr, score }) => `
              <div class="rh-top-row clickable" data-open-risk="${escapeHTML(r.id)}" data-jump-proj="${escapeHTML(pr.id)}" title="${escapeHTML(r.title)} (${escapeHTML(pr.name)})">
                <span class="rh-top-score sev-${score >= 12 ? 'high' : score >= 6 ? 'mid' : 'low'}">${score}</span>
                <span class="rh-top-title">${escapeHTML(r.title.length > 38 ? r.title.slice(0, 36) + '…' : r.title)}</span>
                <span class="rh-top-proj">${escapeHTML(pr.name)}</span>
              </div>`).join('') : '<div class="rh-empty muted">No residual exposure logged yet.</div>'}
            ${perProject.length > 1 ? `
              <div class="rh-side-title rh-side-title-2">By project</div>
              ${perProject.map(({ p, tot, high, count, opp }) => `
                <div class="rh-proj-row clickable" data-jump-proj="${escapeHTML(p.id)}" title="${escapeHTML(p.name)}: ${count} risks/opps, total residual ${tot}, ${high} high-severity">
                  <span class="rh-proj-name">${escapeHTML(p.name.length > 24 ? p.name.slice(0, 22) + '…' : p.name)}</span>
                  <span class="rh-proj-bar"><span class="rh-proj-bar-fill ${high > 0 ? 'bad' : 'ok'}" style="width:${Math.min(100, tot * 1.5)}%"></span></span>
                  <span class="rh-proj-tot">${tot}</span>
                </div>`).join('')}
            ` : ''}
          </div>
        </div>`;
      // Cell click → filter Risks view by residual cell (or just jump there).
      heatEl.querySelectorAll('.rh-cell').forEach((rect) => {
        rect.addEventListener('click', () => {
          state.currentView = 'risks';
          saveState(); render();
        });
      });
      heatEl.querySelectorAll('[data-open-risk]').forEach((el) => {
        el.addEventListener('click', () => {
          const projId = el.dataset.jumpProj;
          if (projId && projId !== state.currentProjectId) {
            state.currentProjectId = projId;
          }
          state.currentView = 'risks';
          saveState(); render();
          setTimeout(() => openRiskEditor(el.dataset.openRisk), 80);
        });
      });
      heatEl.querySelectorAll('.rh-proj-row[data-jump-proj]').forEach((el) => {
        el.addEventListener('click', () => {
          state.currentProjectId = el.dataset.jumpProj;
          state.currentView = 'risks';
          saveState(); render();
        });
      });
    }

    // ── Earned-value rollup ────────────────────────────────────────────
    const evmEl = $('#evmRollup');
    if (evmEl) {
      // EVM rollup — archived actions still contributed real budget and
      // effort, so they belong here. Only cancelled actions are excluded
      // because their value never earned.
      const scope = curProjectIsMerged() ? state.projects : [proj];
      const liveActs = scope.flatMap((p) => (p.actions || [])
        .filter((a) => a.status !== 'cancelled'));
      const evm = evmForActions(liveActs);
      const baselined = liveActs.filter((a) => a.baseline).length;
      const withActuals = liveActs.filter((a) => totalLoggedHours(a) > 0).length;
      const coverage = liveActs.length;
      const fmt$ = (n) => '€' + Math.round(n).toLocaleString();
      const fmtPct = (n) => (n == null ? '—' : (n * 100).toFixed(0) + '%');
      const cpiCls = evm.cpi == null ? 'muted' : evm.cpi >= 1 ? 'ok' : evm.cpi >= .85 ? 'warn' : 'bad';
      const spiCls = evm.spi == null ? 'muted' : evm.spi >= 1 ? 'ok' : evm.spi >= .85 ? 'warn' : 'bad';
      const sv  = evm.ev - evm.pv;
      const cv  = evm.ev - evm.ac;
      const vac = (evm.eac != null) ? evm.bac - evm.eac : null;
      const hasAny = evm.bac > 0;
      // Coverage strip — tells the PM what fraction of actions feed the rollup.
      const coverageBaselinePct = coverage ? Math.round((baselined / coverage) * 100) : 0;
      const coverageActualsPct  = coverage ? Math.round((withActuals / coverage) * 100) : 0;
      const coverageColor = (pct) => pct >= 70 ? 'ok' : pct >= 30 ? 'warn' : 'bad';
      if (!hasAny) {
        evmEl.innerHTML = `
          <div class="evm-empty">
            <div class="evm-empty-title">No EVM data yet.</div>
            <div class="evm-empty-sub">Take a project baseline (button at the top of this page) and log a few hours against actions to populate.</div>
            <div class="evm-coverage">
              <span class="evm-cov-pill ${coverageColor(coverageBaselinePct)}">Baseline: ${baselined}/${coverage} (${coverageBaselinePct}%)</span>
              <span class="evm-cov-pill ${coverageColor(coverageActualsPct)}">Actuals: ${withActuals}/${coverage} (${coverageActualsPct}%)</span>
            </div>
          </div>`;
      } else {
        evmEl.innerHTML = `
          <div class="evm-grid">
            <div class="evm-cell">
              <span class="evm-lbl">BAC</span><b>${fmt$(evm.bac)}</b>
              ${_kpiTooltip({
                title: 'BAC — Budget at Completion',
                meaning: 'The total planned cost of every non-cancelled action rolled up. The finish-line number: if everything ships as scheduled at planned effort, this is what it costs.',
                formula: 'BAC = Σ ( planned_hours(a) × hourly_rate(a.owner) )   over live, non-cancelled actions',
                inputs: [
                  { name: 'planned_hours(a)', desc: '(commitment_% / 100) × 40h × weeks in [a.startDate, a.due].' },
                  { name: 'hourly_rate(a.owner)', desc: 'From People → Hourly rate. Defaults to 0 when unset — actions without a rated owner contribute nothing.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'a start date, a due date, a commitment %, and an owner.' },
                  { scope: 'Per person', desc: 'an hourly rate (People page). Rate 0 = that owner\'s work costs 0.' },
                ],
                notComputable: evm.bac === 0 ? 'BAC is 0 — no live action has both a scheduled window and a rated owner. Fill in start / due / owner / hourly rate to populate.' : null,
              })}
            </div>
            <div class="evm-cell">
              <span class="evm-lbl">PV</span><b>${fmt$(evm.pv)}</b>
              ${_kpiTooltip({
                title: 'PV — Planned Value',
                meaning: 'How much of the BAC we planned to have earned by today. The pace we said we\'d walk.',
                formula: 'PV = Σ ( planned_hours_to_date(a) × hourly_rate(a.owner) )',
                inputs: [
                  { name: 'planned_hours_to_date(a)', desc: 'BAC hours for the action, prorated by the fraction of its [start, due] window that has elapsed today. Clamped to 0 before start and BAC after due.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'the same as BAC — start, due, commitment %, owner.' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
              })}
            </div>
            <div class="evm-cell">
              <span class="evm-lbl">EV</span><b>${fmt$(evm.ev)}</b>
              ${_kpiTooltip({
                title: 'EV — Earned Value',
                meaning: 'How much of the BAC has actually been produced so far. The pace we\'re actually walking.',
                formula: 'EV = Σ ( planned_hours(a) × % complete(a) × hourly_rate(a.owner) )',
                inputs: [
                  { name: '% complete(a)', desc: 'Comes from the action drawer\'s manual field. Falls back to auto (from status: Done=100 %, Cancelled=0 %, otherwise inferred from elapsed time in [start, due]).' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'start / due / commitment %/ owner AND an up-to-date % complete. Leaving % complete at auto-mode is fine as long as the action\'s status flips to Done when the work lands.' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
              })}
            </div>
            <div class="evm-cell">
              <span class="evm-lbl">AC</span><b>${fmt$(evm.ac)}</b>
              ${_kpiTooltip({
                title: 'AC — Actual Cost',
                meaning: 'What has actually been spent so far. Logged hours × the owner\'s rate.',
                formula: 'AC = Σ ( logged_hours(a) × hourly_rate(a.owner) )',
                inputs: [
                  { name: 'logged_hours(a)', desc: 'Sum of every "Log time" entry on the action.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'at least one logged-hours entry (Action drawer → Log time). Without any logs the action contributes 0 to AC — CPI will look artificially high.' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
                notComputable: evm.ac === 0 ? 'AC is 0 — no action has any logged hours yet. Encourage owners to record time (Action drawer → Log time) so cost signals stay honest.' : null,
              })}
            </div>
            <div class="evm-cell evm-cell-index">
              <span class="evm-lbl">CPI</span><b class="${cpiCls}">${evm.cpi == null ? '—' : evm.cpi.toFixed(2)}</b><span class="evm-sub">${evm.cpi == null ? 'no actuals' : evm.cpi >= 1 ? 'under budget' : 'over budget'}</span>
              ${_kpiTooltip({
                title: 'CPI — Cost Performance Index',
                meaning: 'How much value earned per unit of cost spent. > 1 = under budget; < 1 = over budget; = 1 = exactly on plan. Multiply your CPI by 100 to read it as "cents on the dollar earned".',
                formula: 'CPI = EV ÷ AC',
                inputs: [
                  { name: 'EV', desc: 'Earned Value — see the EV cell.' },
                  { name: 'AC', desc: 'Actual Cost — see the AC cell.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'the union of what EV and AC need: start / due / commitment % / owner / % complete / logged hours.' },
                  { scope: 'Per person', desc: 'an hourly rate — cancels out of the ratio in principle, but a mix of rated and unrated owners silently distorts CPI.' },
                ],
                notComputable: evm.cpi == null ? 'Cannot compute — AC is 0 (division by zero). At least one action needs a logged-hours entry.' : null,
              })}
            </div>
            <div class="evm-cell evm-cell-index">
              <span class="evm-lbl">SPI</span><b class="${spiCls}">${evm.spi == null ? '—' : evm.spi.toFixed(2)}</b><span class="evm-sub">${evm.spi == null ? 'no progress' : evm.spi >= 1 ? 'on schedule' : 'behind'}</span>
              ${_kpiTooltip({
                title: 'SPI — Schedule Performance Index',
                meaning: 'How on-schedule the work is by earned value. > 1 = ahead of schedule; < 1 = behind; = 1 = exactly on plan.',
                formula: 'SPI = EV ÷ PV',
                inputs: [
                  { name: 'EV', desc: 'Earned Value — see the EV cell.' },
                  { name: 'PV', desc: 'Planned Value — see the PV cell.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'start / due / commitment % / owner / an up-to-date % complete (or an accurate status).' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
                notComputable: evm.spi == null ? 'Cannot compute — PV is 0. No live action has both a scheduled window (start + due) and a rated owner.' : null,
              })}
            </div>
            <div class="evm-cell">
              <span class="evm-lbl">EAC</span><b>${evm.eac == null ? '—' : fmt$(evm.eac)}</b>
              ${_kpiTooltip({
                title: 'EAC — Estimate at Completion',
                meaning: 'What the whole project will cost if the current cost-efficiency (CPI) holds to the end.',
                formula: 'EAC = BAC ÷ CPI',
                inputs: [
                  { name: 'BAC', desc: 'Budget at Completion — see the BAC cell.' },
                  { name: 'CPI', desc: 'Cost Performance Index — see the CPI cell.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'whatever BAC + CPI need — start, due, commitment %, owner, % complete, logged hours.' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
                notComputable: evm.eac == null ? 'Cannot compute — CPI is undefined (AC = 0).' : null,
              })}
            </div>
            <div class="evm-cell">
              <span class="evm-lbl">ETC</span><b>${evm.etc == null ? '—' : fmt$(evm.etc)}</b>
              ${_kpiTooltip({
                title: 'ETC — Estimate to Complete',
                meaning: 'How much more it will cost from here to the end, at the current cost-efficiency.',
                formula: 'ETC = EAC − AC',
                inputs: [
                  { name: 'EAC', desc: 'Estimate at Completion — see the EAC cell.' },
                  { name: 'AC',  desc: 'Actual Cost — see the AC cell.' },
                ],
                minimumData: [
                  { scope: 'Per action', desc: 'the union of what EAC and AC need.' },
                  { scope: 'Per person', desc: 'an hourly rate.' },
                ],
                notComputable: evm.etc == null ? 'Cannot compute — EAC is undefined (CPI = undefined).' : null,
              })}
            </div>
          </div>
          <div class="evm-deltas">
            <span class="evm-delta ${cv >= 0 ? 'ok' : 'bad'}" title="Cost Variance: EV − AC. Positive = under budget.">CV ${cv >= 0 ? '+' : ''}${fmt$(cv)}</span>
            <span class="evm-delta ${sv >= 0 ? 'ok' : 'bad'}" title="Schedule Variance: EV − PV. Positive = ahead of schedule.">SV ${sv >= 0 ? '+' : ''}${fmt$(sv)}</span>
            ${vac != null ? `<span class="evm-delta ${vac >= 0 ? 'ok' : 'bad'}" title="Variance at Completion: BAC − EAC. Positive = expected to land under budget.">VAC ${vac >= 0 ? '+' : ''}${fmt$(vac)}</span>` : ''}
          </div>
          <div class="evm-coverage">
            <span class="evm-cov-pill ${coverageColor(coverageBaselinePct)}">Baseline: ${baselined}/${coverage} (${coverageBaselinePct}%)</span>
            <span class="evm-cov-pill ${coverageColor(coverageActualsPct)}">Actuals: ${withActuals}/${coverage} (${coverageActualsPct}%)</span>
            ${coverageBaselinePct < 50 || coverageActualsPct < 50 ? '<span class="evm-cov-hint">Low coverage — rollup may not be representative.</span>' : ''}
          </div>`;
      }
    }
  }

  /* ----------------------------- Review ------------------------------ */

  let reviewStep = 0;
  const REVIEW_STEPS = [
    { id: 'changes', name: 'What changed' },
    { id: 'late', name: 'Late & blocked' },
    { id: 'next', name: 'What\'s next' },
    { id: 'decisions', name: 'Decisions' },
    { id: 'summary', name: 'Summary' },
  ];

  /* ----------------------------- Charts ------------------------------ */

  // Pick a stable color from the COMPONENT_COLORS palette based on a string id.
  function colorFor(id) {
    let h = 0; for (let i = 0; i < id.length; i++) h = (h * 31 + id.charCodeAt(i)) | 0;
    return COMPONENT_COLORS[Math.abs(h) % COMPONENT_COLORS.length];
  }

  // Distinct palette for stacked layers where collision matters (people in
  // budget chart, cumulative workload, etc). Sized > number of seeded people
  // so neighbours never share a hue.
  const PERSON_PALETTE = [
    { rgb: '96,165,250'  }, // sky
    { rgb: '244,114,182' }, // pink
    { rgb: '52,211,153'  }, // mint
    { rgb: '251,191,36'  }, // amber
    { rgb: '167,139,250' }, // violet
    { rgb: '34,211,238'  }, // cyan
    { rgb: '249,115,22'  }, // orange
    { rgb: '163,230,53'  }, // lime
    { rgb: '236,72,153'  }, // fuchsia
    { rgb: '20,184,166'  }, // teal
    { rgb: '251,113,133' }, // rose
    { rgb: '129,140,248' }, // indigo
    { rgb: '74,222,128'  }, // green
    { rgb: '125,211,252' }, // light-blue
    { rgb: '253,164,175' }, // peach
    { rgb: '161,98,7'    }, // burnt-amber
  ];
  function personColorByIndex(i) { return PERSON_PALETTE[i % PERSON_PALETTE.length]; }

  // Mine an action's history for "Schedule: A → B…C" entries → array of
  // { at: ISO snapshot date, due: ISO forecast end date }.
  // The entry kept at index 0 is the original creation (using the first
  // entry's `at`).
  function scheduleHistory(action) {
    const points = [];
    const re = /Schedule:\s*([\d-]+)\s*→\s*([\d-]+)(?:[…\.]+([\d-]+))?/;
    (action.history || []).forEach((h) => {
      const m = h.what.match(re);
      if (!m) return;
      // First time we encounter Schedule, push the "from" too as the original
      if (!points.length) points.push({ at: action.createdAt || h.at, due: m[1] });
      points.push({ at: h.at, due: m[3] || m[2] });
    });
    if (!points.length && action.due) {
      points.push({ at: action.createdAt || action.updatedAt || todayISO(), due: action.due });
    }
    // For done actions, terminate the line on the 45° diagonal at the actual
    // completion date — that's the visual signal that the work landed.
    if (action.status === 'done') {
      const doneEntry = (action.history || []).filter((h) => /Status:.*→\s*done/.test(h.what)).pop();
      const completedAt = doneEntry?.at || action.updatedAt;
      if (completedAt) points.push({ at: completedAt, due: completedAt });
    } else if (points.length && points[points.length - 1].due !== action.due) {
      // Open action — extend the line to today with the current forecast
      points.push({ at: todayISO(), due: action.due });
    }
    return points;
  }

  // Persistent set of people hidden from the cumulative workload chart.
  // Toggled by clicking the legend.
  const cumWlHidden = new Set();
  // Waterfall chart — session-scoped set of hidden action ids. Cleared
  // only when the user reloads or explicitly re-toggles a chip.
  const _wfHidden = new Set();

  // ----- Chart 1: Cumulative workload across all people, weekly -----
  function chartCumulativeWorkload(weeks = 12) {
    const W = 600, H = 220;
    const padL = 38, padR = 12, padT = 10, padB = 24;
    const innerW = W - padL - padR, innerH = H - padT - padB;

    const allPeople = state.people.map((p) => ({
      person: p, color: colorFor(p.id),
      series: weeklyLoad(p.id, weeks),
      hidden: cumWlHidden.has(p.id),
    }));
    const visible = allPeople.filter((ps) => !ps.hidden);
    const visibleCap = visible.reduce((s, ps) => s + (ps.person.capacity || 5), 0);
    const totals = [];
    for (let w = 0; w < weeks; w++) {
      let t = 0; visible.forEach((ps) => t += ps.series[w].count);
      totals.push(t);
    }
    const maxY = Math.max(visibleCap * 1.2 || 4, ...totals, 4);

    const xFor = (i) => padL + (i + 0.5) / weeks * innerW;
    const yFor = (v) => padT + innerH - (v / maxY) * innerH;

    let cumPrev = new Array(weeks).fill(0);
    const layers = visible.map((ps) => {
      const top = ps.series.map((s, i) => cumPrev[i] + s.count);
      const fwd = top.map((v, i) => `${i === 0 ? 'M' : 'L'} ${xFor(i).toFixed(1)} ${yFor(v).toFixed(1)}`).join(' ');
      const back = cumPrev.slice().reverse().map((v, i) => `L ${xFor(weeks - 1 - i).toFixed(1)} ${yFor(v).toFixed(1)}`).join(' ');
      const path = fwd + ' ' + back + ' Z';
      cumPrev = top;
      return { ps, path, top };
    });

    const months = (allPeople[0]?.series || []).map((s, i) => {
      if (s.weekStart.getDate() > 7) return '';
      return `<text class="chart-tick" x="${xFor(i)}" y="${H - 6}" text-anchor="middle">${s.weekStart.toLocaleDateString(undefined, { month: 'short' })}</text>`;
    }).join('');

    const yTicks = [0, Math.round(maxY/2), Math.round(maxY)].map((v) =>
      `<g class="ytick">
        <line x1="${padL - 3}" x2="${padL}" y1="${yFor(v)}" y2="${yFor(v)}" />
        <text x="${padL - 6}" y="${yFor(v) + 3}" text-anchor="end">${v}</text>
      </g>`).join('');

    // Minor horizontal gridlines every 100% (= 1 FTE)
    const FTE_UNIT = 100;
    const minorLines = [];
    for (let v = FTE_UNIT; v <= Math.ceil(maxY); v += FTE_UNIT) {
      const y = yFor(v).toFixed(1);
      minorLines.push(`<line class="cumwl-minor" x1="${padL}" x2="${W - padR}" y1="${y}" y2="${y}" />`);
    }

    const capLine = visibleCap > 0
      ? `<line class="chart-cap" x1="${padL}" x2="${W - padR}" y1="${yFor(visibleCap)}" y2="${yFor(visibleCap)}" />
         <text class="chart-label" x="${W - padR - 4}" y="${Math.max(11, yFor(visibleCap) - 3)}" text-anchor="end">cap ${visibleCap}</text>`
      : '';

    registerChartData('cumulative-workload', () => {
      const seriesPeople = allPeople.filter((ps) => !ps.hidden);
      const headers = ['Week start', ...seriesPeople.map((ps) => ps.person.name), 'Total %', 'Cap %'];
      const rows = (seriesPeople[0]?.series || []).map((s, i) => {
        const perPerson = seriesPeople.map((ps) => ps.series[i]?.count ?? 0);
        const tot = perPerson.reduce((a, b) => a + b, 0);
        return [fmtISO(s.weekStart), ...perPerson, tot, visibleCap];
      });
      return { headers, rows };
    });

    return `
      <svg viewBox="0 0 ${W} ${H}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
        ${minorLines.join('')}
        ${capLine}
        ${layers.map((l) => `<path d="${l.path}" fill="rgba(${l.ps.color.rgb},.65)" stroke="rgba(${l.ps.color.rgb},.95)" stroke-width="0.7"><title>${escapeHTML(l.ps.person.name)}</title></path>`).join('')}
        ${yTicks}
        ${months}
      </svg>
      <div class="chart-legend" data-legend="cumwl">
        ${allPeople.map((ps) => `
          <button type="button" class="legend-item ${ps.hidden ? 'is-hidden' : ''}" data-person-id="${ps.person.id}" title="Click to ${ps.hidden ? 'show' : 'hide'}">
            <span class="dot" style="background:rgba(${ps.color.rgb},.95)"></span>${escapeHTML(ps.person.name)}
          </button>`).join('')}
      </div>`;
  }

  function wireCumWlLegend() {
    const wrap = document.getElementById('cumWlSlot');
    if (!wrap) return;
    wrap.addEventListener('click', (e) => {
      const btn = e.target.closest('.legend-item[data-person-id]');
      if (!btn || !wrap.contains(btn)) return;
      const pid = btn.dataset.personId;
      if (cumWlHidden.has(pid)) cumWlHidden.delete(pid);
      else cumWlHidden.add(pid);
      wrap.innerHTML = chartCumulativeWorkload(12);
    });
  }

  // ----- Chart 2: Flow chart (created / done / slipped per week) -----
  function flowMetrics(weeks = 12) {
    const today = new Date(); today.setHours(0, 0, 0, 0);
    let weekStart = new Date(today);
    while (weekStart.getDay() !== 1) weekStart = new Date(weekStart.getTime() - dayMs);
    const start = new Date(weekStart.getTime() - (weeks - 1) * 7 * dayMs);

    const buckets = new Array(weeks).fill(0).map((_, i) => ({
      start: new Date(start.getTime() + i * 7 * dayMs),
      created: 0, done: 0, slipped: 0, pullIn: 0,
    }));

    const isDoneTrans = /Status:.*→\s*done/;
    const slipRe = /Schedule:\s*([\d-]+)\s*→\s*(?:[\d-]+…)?([\d-]+)/;
    const todayStr = todayISO();
    let overdue = 0, openTotal = 0, blockedTotal = 0;

    state.projects.forEach((proj) => {
      (proj.actions || []).forEach((a) => {
        // Flow chart is a historical view — archived actions still count.
        // created
        if (a.createdAt) {
          const idx = Math.floor((parseDate(a.createdAt) - start) / dayMs / 7);
          if (idx >= 0 && idx < weeks) buckets[idx].created++;
        }
        // done — prefer history transition, else updatedAt for status === 'done'
        const doneEntry = (a.history || []).filter((h) => isDoneTrans.test(h.what)).pop();
        let doneAt = doneEntry ? doneEntry.at : (a.status === 'done' ? a.updatedAt : null);
        if (doneAt) {
          const idx = Math.floor((parseDate(doneAt) - start) / dayMs / 7);
          if (idx >= 0 && idx < weeks) buckets[idx].done++;
        }
        // schedule events: slipped vs pulled-in
        (a.history || []).forEach((h) => {
          const m = h.what.match(slipRe);
          if (!m) return;
          const before = parseDate(m[1]);
          const after = parseDate(m[2]);
          const idx = Math.floor((parseDate(h.at) - start) / dayMs / 7);
          if (idx < 0 || idx >= weeks) return;
          if (after > before) buckets[idx].slipped++;
          else if (after < before) buckets[idx].pullIn++;
        });
        // snapshot counters
        if (a.status !== 'done') {
          openTotal++;
          if (a.due && dayDiff(a.due, todayStr) < 0) overdue++;
          if (a.status === 'blocked') blockedTotal++;
        }
      });
    });

    return { buckets, overdue, openTotal, blockedTotal };
  }

  function chartFlow(weeks = 12) {
    const W = 600, H = 220;
    const padL = 38, padR = 12, padT = 12, padB = 26;
    const innerW = W - padL - padR, innerH = H - padT - padB;

    const { buckets, overdue, openTotal, blockedTotal } = flowMetrics(weeks);
    const maxY = Math.max(4, ...buckets.flatMap((b) => [b.created, b.done, b.slipped]));
    const groupW = innerW / weeks;
    const barW = (groupW - 4) / 2;
    const xFor = (i) => padL + i * groupW;
    const yFor = (v) => padT + innerH - (v / maxY) * innerH;

    // Bars + created-line dots carry data-flow-* attributes so the panel
    // click listener can drill into the Register scoped to that week and
    // side. Zero-count bars stay unclickable (nothing to reveal).
    const bars = buckets.map((b, i) => {
      const xBase = xFor(i) + 2;
      const yDone = yFor(b.done);
      const ySlip = yFor(b.slipped);
      const baseLine = padT + innerH;
      const wkLabel = b.start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
      const wkEnd = fmtISO(new Date(b.start.getTime() + 6 * dayMs));
      const attrs = `data-flow-start="${b.startISO || fmtISO(b.start)}" data-flow-end="${wkEnd}" data-flow-label="${escapeHTML(wkLabel)}"`;
      return `
        <rect class="bar-done ${b.done > 0 ? 'is-clickable' : ''}" x="${xBase}" y="${yDone}" width="${barW}" height="${Math.max(0, baseLine - yDone)}" rx="2"
              data-flow-side="done" ${attrs}>
          <title>Week of ${wkLabel} — ${b.done} done${b.done > 0 ? ' · click to open in Register' : ''}</title>
        </rect>
        <rect class="bar-slip ${b.slipped > 0 ? 'is-clickable' : ''}" x="${xBase + barW}" y="${ySlip}" width="${barW}" height="${Math.max(0, baseLine - ySlip)}" rx="2"
              data-flow-side="slipped" ${attrs}>
          <title>Week of ${wkLabel} — ${b.slipped} slipped${b.pullIn ? `, ${b.pullIn} pulled-in` : ''}${b.slipped > 0 ? ' · click to open in Register' : ''}</title>
        </rect>`;
    }).join('');

    // Created line + dots — clickable when the count is non-zero.
    const linePts = buckets.map((b, i) => `${(xFor(i) + groupW/2).toFixed(1)},${yFor(b.created).toFixed(1)}`);
    const linePath = `M ${linePts.join(' L ')}`;
    const lineDots = buckets.map((b, i) => {
      const wkLabel = b.start.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
      const wkEnd = fmtISO(new Date(b.start.getTime() + 6 * dayMs));
      const clickable = b.created > 0 ? 'is-clickable' : '';
      return `<circle cx="${(xFor(i) + groupW/2).toFixed(1)}" cy="${yFor(b.created).toFixed(1)}" r="${b.created > 0 ? 4 : 2.5}" class="dot-created ${clickable}"
              data-flow-side="created" data-flow-start="${b.startISO || fmtISO(b.start)}" data-flow-end="${wkEnd}" data-flow-label="${escapeHTML(wkLabel)}"><title>Week of ${wkLabel} — ${b.created} created${b.created > 0 ? ' · click to open in Register' : ''}</title></circle>`;
    }).join('');

    const months = buckets.map((b, i) => {
      if (b.start.getDate() > 7) return '';
      return `<text class="chart-tick" x="${(xFor(i) + groupW/2)}" y="${H - 6}" text-anchor="middle">${b.start.toLocaleDateString(undefined, { month: 'short' })}</text>`;
    }).join('');

    const yTicks = [0, Math.round(maxY/2), maxY].map((v) =>
      `<g class="ytick">
        <line x1="${padL - 3}" x2="${padL}" y1="${yFor(v)}" y2="${yFor(v)}" />
        <text x="${padL - 6}" y="${yFor(v) + 3}" text-anchor="end">${v}</text>
      </g>`).join('');

    const totals = buckets.reduce((acc, b) => ({
      created: acc.created + b.created,
      done: acc.done + b.done,
      slipped: acc.slipped + b.slipped,
      pullIn: acc.pullIn + b.pullIn,
    }), { created: 0, done: 0, slipped: 0, pullIn: 0 });
    const net = totals.done - totals.created;
    const netCls = net >= 0 ? 'ok' : 'bad';

    registerChartData('flow', () => ({
      headers: ['Week start', 'Created', 'Done', 'Slipped', 'Pulled in'],
      rows: buckets.map((b) => [fmtISO(b.start), b.created, b.done, b.slipped, b.pullIn]),
    }));

    return `
      <svg viewBox="0 0 ${W} ${H}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
        ${yTicks}
        ${bars}
        <path d="${linePath}" class="line-created" />
        ${lineDots}
        ${months}
      </svg>
      <div class="chart-legend">
        <span class="legend-item"><span class="dot" style="background:var(--ok)"></span>Done</span>
        <span class="legend-item"><span class="dot" style="background:var(--bad)"></span>Slipped</span>
        <span class="legend-item"><span class="dot" style="background:var(--accent)"></span>Created</span>
      </div>
      <div class="chart-foot">
        Done <b>${totals.done}</b> • Created <b>${totals.created}</b> • Slipped <b>${totals.slipped}</b>${totals.pullIn ? ` • Pulled-in <b>${totals.pullIn}</b>` : ''}
        • Net <b class="${netCls}">${net >= 0 ? '+' : ''}${net}</b> over ${weeks}w
        <span class="chart-foot-snap">
          Open now <b>${openTotal}</b>
          • Overdue <b class="${overdue > 0 ? 'bad' : ''}">${overdue}</b>
          • Blocked <b class="${blockedTotal > 0 ? 'warn' : ''}">${blockedTotal}</b>
        </span>
      </div>`;
  }

  // ----- Chart 3: 45° Schedule deviation waterfall -----
  function chartWaterfall() {
    const W = 760, H = 360;
    const padL = 64, padR = 12, padT = 14, padB = 38;
    const innerW = W - padL - padR, innerH = H - padT - padB;

    // Pick top N actions by current due date (focus on most relevant).
    const acts = state.projects.flatMap((p) => (p.actions || []).map((a) => ({ proj: p, a })));
    const candidates = acts.filter(({ a }) => a.due && !a.archivedAt).map(({ proj, a }) => ({
      proj, a, hist: scheduleHistory(a),
    })).filter(({ hist }) => hist.length >= 1);
    // Prefer those with movement
    candidates.sort((x, y) => (y.hist.length - x.hist.length) || (x.a.due.localeCompare(y.a.due)));
    const top = candidates.slice(0, 12);
    if (!top.length) {
      return `<div class="empty">No scheduled actions yet — add some, then dragging them on the Gantt will populate this chart over time.</div>`;
    }
    // Session-scoped hidden set: matches the cumulative-workload pattern.
    // Filters lines out of the domain computation and the render pass so
    // hiding a distant line rescales the chart to what's left.
    const visible = top.filter(({ a }) => !_wfHidden.has(a.id));
    const domainSource = visible.length ? visible : top;

    // Domain: x = snapshot dates from earliest 'at' to today
    // y = forecast due dates from earliest to latest forecast
    let xMin = parseDate(todayISO()), xMax = parseDate(todayISO());
    let yMin = parseDate(todayISO()), yMax = parseDate(todayISO());
    domainSource.forEach(({ hist }) => {
      hist.forEach(({ at, due }) => {
        const ad = parseDate(at), dd = parseDate(due);
        if (ad < xMin) xMin = ad;
        if (ad > xMax) xMax = ad;
        if (dd < yMin) yMin = dd;
        if (dd > yMax) yMax = dd;
      });
    });
    // Ensure 45° diagonal is visible: y range covers x range.
    if (yMin > xMin) yMin = xMin;
    if (yMax < xMax) yMax = xMax;
    // Pad a bit
    const pad = (xMax - xMin) * 0.08 + dayMs * 7;
    xMin = new Date(xMin - pad); xMax = new Date(xMax.getTime() + pad);
    yMin = new Date(yMin - pad); yMax = new Date(yMax.getTime() + pad);

    const xFor = (d) => padL + (d - xMin) / (xMax - xMin) * innerW;
    const yFor = (d) => padT + innerH - (d - yMin) / (yMax - yMin) * innerH;

    // 45° diagonal: where y = x — clip to chart bounds
    const lo = new Date(Math.max(xMin.getTime(), yMin.getTime()));
    const hi = new Date(Math.min(xMax.getTime(), yMax.getTime()));
    const diag = `<line class="chart-diag" x1="${xFor(lo)}" y1="${yFor(lo)}" x2="${xFor(hi)}" y2="${yFor(hi)}" />`;
    const todayX = xFor(parseDate(todayISO()));
    const todayLine = `<line class="chart-today" x1="${todayX}" y1="${padT}" x2="${todayX}" y2="${padT + innerH}" stroke-dasharray="2 3" />`;

    // Lines per action — only visible ones are drawn.
    const lines = visible.map(({ proj, a, hist }) => {
      const c = colorFor(a.id);
      const path = hist.map(({ at, due }, i) =>
        `${i === 0 ? 'M' : 'L'} ${xFor(parseDate(at)).toFixed(1)} ${yFor(parseDate(due)).toFixed(1)}`).join(' ');
      const dots = hist.map(({ at, due }) =>
        `<circle cx="${xFor(parseDate(at)).toFixed(1)}" cy="${yFor(parseDate(due)).toFixed(1)}" r="2.5" fill="rgb(${c.rgb})"><title>${escapeHTML(a.title)}: forecast ${due} on ${at}</title></circle>`).join('');
      return `<g><path d="${path}" stroke="rgba(${c.rgb},.85)" stroke-width="1.5" fill="none" /><title>${escapeHTML(a.title)} (${escapeHTML(proj.name)})</title>${dots}</g>`;
    }).join('');

    // X axis ticks (monthly)
    const xTicks = [];
    let dt = new Date(xMin.getFullYear(), xMin.getMonth(), 1);
    if (dt < xMin) dt = new Date(xMin.getFullYear(), xMin.getMonth() + 1, 1);
    while (dt <= xMax) {
      xTicks.push(`<g><line class="chart-grid" x1="${xFor(dt)}" x2="${xFor(dt)}" y1="${padT}" y2="${padT + innerH}" /><text class="chart-tick" x="${xFor(dt)}" y="${H - 16}" text-anchor="middle">${dt.toLocaleDateString(undefined, { month: 'short', year: '2-digit' })}</text></g>`);
      dt = new Date(dt.getFullYear(), dt.getMonth() + 1, 1);
    }
    // Y axis ticks
    const yTicks = [];
    dt = new Date(yMin.getFullYear(), yMin.getMonth(), 1);
    if (dt < yMin) dt = new Date(yMin.getFullYear(), yMin.getMonth() + 1, 1);
    while (dt <= yMax) {
      yTicks.push(`<g><line class="chart-grid" x1="${padL}" x2="${padL + innerW}" y1="${yFor(dt)}" y2="${yFor(dt)}" /><text class="chart-tick" x="${padL - 6}" y="${yFor(dt) + 3}" text-anchor="end">${dt.toLocaleDateString(undefined, { month: 'short', year: '2-digit' })}</text></g>`);
      dt = new Date(dt.getFullYear(), dt.getMonth() + 1, 1);
    }

    registerChartData('waterfall', () => {
      const rows = [];
      top.forEach(({ proj, a, hist }) => {
        hist.forEach(({ at, due }) => rows.push([a.id, a.title, proj.name, at, due]));
      });
      return {
        headers: ['Action ID', 'Action title', 'Project', 'Snapshot date', 'Forecast due'],
        rows,
      };
    });

    return `
      <svg viewBox="0 0 ${W} ${H}" class="chart-svg" preserveAspectRatio="xMidYMid meet">
        ${xTicks.join('')}
        ${yTicks.join('')}
        ${diag}
        <text class="chart-label" x="${xFor(hi) - 4}" y="${yFor(hi) - 4}" text-anchor="end">delivered (45°)</text>
        ${todayLine}
        ${lines}
        <text class="chart-axis-label" x="${padL + innerW/2}" y="${H - 2}" text-anchor="middle">snapshot date →</text>
        <text class="chart-axis-label" x="${padL - 50}" y="${padT + innerH/2}" text-anchor="middle" transform="rotate(-90 ${padL - 50} ${padT + innerH/2})">forecast due →</text>
      </svg>
      <div class="chart-legend chart-legend-wf" data-legend="waterfall" title="Click a name to hide / show that action's line">
        ${top.map(({ proj, a }) => {
          const c = colorFor(a.id);
          const hidden = _wfHidden.has(a.id);
          const projSuffix = state.projects.length > 1 ? ` · ${escapeHTML(proj.name)}` : '';
          return `
            <button type="button" class="legend-item ${hidden ? 'is-hidden' : ''}" data-wf-id="${escapeHTML(a.id)}" title="${hidden ? 'Show' : 'Hide'} — ${escapeHTML(a.title)}${projSuffix}">
              <span class="dot" style="background:rgba(${c.rgb},.95)"></span>${escapeHTML(a.title)}
            </button>`;
        }).join('')}
      </div>
      <div class="chart-foot">A line going up steeper than 45° = slipping faster than time. Horizontal = stable forecast. Crossing the diagonal = delivered.</div>`;
  }

  // ----- Chart 4: Per-person small multiples -----
  function chartPerPerson() {
    return `<div class="small-multiples">
      ${state.people.map((p) => {
        const series = weeklyLoad(p.id, 12);
        const peak = series.reduce((mx, s) => s.count > mx.count ? s : mx, series[0]);
        const peakCls = peak.count > (p.capacity || 5) ? 'over' : peak.count > (p.capacity || 5) * 0.8 ? 'warn' : 'ok';
        return `
          <div class="sm-cell copyable-chart" data-chart="spark-${escapeHTML(p.id)}">
            <div class="sm-head">
              <span class="avatar">${initials(p.name)}</span>
              <b>${escapeHTML(p.name)}</b>
              <span class="sm-peak ${peakCls}">peak ${peak.count}</span>
            </div>
            ${workloadSparkSVG(p, series)}
          </div>`;
      }).join('')}
    </div>`;
  }

  // Merged Review + Reports. Two reading modes share the same data:
  //  - 'walkthrough': stepper wizard with inline edits, for live meetings.
  //  - 'full':        single-page snapshot, for distribution.
  // Period selector + Markdown / Print exports are shared and always
  // visible. Default mode is walkthrough so the live meeting flow lands
  // first; users can flip to 'full' for the print-ready snapshot.
  const reviewModeState = { mode: 'walkthrough' };
  function renderReview(root) {
    const proj = curProject();
    const view = document.createElement('div');
    view.className = 'view';
    const isAll = state.currentProjectId === '__all__';
    if (isAll || !proj) {
      view.innerHTML = `
        <div class="page-head">
          <div>
            <div class="page-title">Review</div>
            <div class="page-sub">Pick a project to review.</div>
          </div>
        </div>
        <div class="empty">Review is project-scoped. Choose a project from the topbar.</div>`;
      root.appendChild(view);
      return;
    }
    const range = reportPeriodRange();
    const data = buildReportData(proj, range.since, range.until);
    const mode = reviewModeState.mode;
    const optSel = (val) => reportState.period === val ? 'selected' : '';

    view.innerHTML = `
      <div class="review">
        <div class="page-head" style="margin-bottom:10px;">
          <div>
            <div class="page-title">${escapeHTML(proj.name)} — Review</div>
            <div class="page-sub">${fmtFull(data.since)} – ${fmtFull(data.until)}</div>
          </div>
          <div class="page-actions">
            <select id="reportPeriod" class="report-period" title="Period for changes / decisions / CRs">
              <option value="7d"  ${optSel('7d')}>Last 7 days</option>
              <option value="30d" ${optSel('30d')}>Last 30 days</option>
              <option value="90d" ${optSel('90d')}>Last 90 days</option>
              <option value="custom" ${optSel('custom')}>Custom…</option>
            </select>
            <span class="report-custom" id="reportCustom" ${reportState.period === 'custom' ? '' : 'hidden'}>
              <input type="date" id="reportSince" value="${reportState.customSince || data.since}" />
              <span class="report-dash">–</span>
              <input type="date" id="reportUntil" value="${reportState.customUntil || data.until}" />
            </span>
            <div class="seg" role="tablist" aria-label="Review mode">
              <button type="button" class="seg-btn ${mode === 'walkthrough' ? 'active' : ''}" data-review-mode="walkthrough">Walk-through</button>
              <button type="button" class="seg-btn ${mode === 'full'        ? 'active' : ''}" data-review-mode="full">Full report</button>
            </div>
            <button class="ghost" id="btnReportCopyMd"  title="Copy as Markdown">Copy MD</button>
            <button class="ghost" id="btnReportDownload" title="Download .md file">Download</button>
            <button class="ghost" id="btnReportPrint"   title="Open print-ready HTML in a new tab">Print → PDF</button>
          </div>
        </div>
        ${mode === 'walkthrough' ? `
          <div class="review-stepper" id="reviewStepper"></div>
          <div class="review-card" id="reviewBody"></div>
          <div class="review-foot">
            <button class="ghost" id="btnReviewPrev">← Previous</button>
            <button class="primary" id="btnReviewNext">Next →</button>
          </div>
        ` : `
          <div class="report" id="reportBody"></div>
        `}
      </div>`;
    root.appendChild(view);

    // --- Period + custom-range wiring (shared) ---
    $('#reportPeriod').addEventListener('change', (e) => {
      reportState.period = e.target.value;
      render();
    });
    $('#reportSince')?.addEventListener('change', (e) => {
      reportState.customSince = e.target.value; reportState.period = 'custom'; render();
    });
    $('#reportUntil')?.addEventListener('change', (e) => {
      reportState.customUntil = e.target.value; reportState.period = 'custom'; render();
    });

    // --- Mode toggle ---
    $$('.seg-btn[data-review-mode]', view).forEach((b) => {
      b.addEventListener('click', () => {
        if (reviewModeState.mode === b.dataset.reviewMode) return;
        reviewModeState.mode = b.dataset.reviewMode;
        render();
      });
    });

    // --- Export buttons (always export the FULL report for the current
    // period — that's the most useful artifact regardless of mode) ---
    $('#btnReportCopyMd').addEventListener('click', async () => {
      const md = reportToMarkdown(buildReportData(curProject(), range.since, range.until));
      try { await navigator.clipboard.writeText(md); toast('Copied as Markdown'); }
      catch (e) { toast('Copy failed — clipboard unavailable'); }
    });
    $('#btnReportDownload').addEventListener('click', () => {
      const md = reportToMarkdown(buildReportData(curProject(), range.since, range.until));
      const blob = new Blob([md], { type: 'text/markdown' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = `report-${proj.id}-${range.until}.md`;
      a.click();
      URL.revokeObjectURL(url);
      toast('Report downloaded');
    });
    $('#btnReportPrint').addEventListener('click', () => {
      const html = reportToPrintHTML(buildReportData(curProject(), range.since, range.until));
      const w = window.open('', '_blank');
      if (!w) { toast('Pop-up blocked'); return; }
      w.document.write(html); w.document.close();
      setTimeout(() => { try { w.print(); } catch (e) { /* ignore */ } }, 250);
    });

    // --- Mode-specific body wiring ---
    if (mode === 'walkthrough') {
      drawReviewStep();
      $('#btnReviewPrev').addEventListener('click', () => {
        reviewStep = clamp(reviewStep - 1, 0, REVIEW_STEPS.length - 1);
        drawReviewStep();
      });
      $('#btnReviewNext').addEventListener('click', () => {
        reviewStep = clamp(reviewStep + 1, 0, REVIEW_STEPS.length - 1);
        drawReviewStep();
      });
    } else {
      // Full report — render the same body the old Reports view used,
      // wired with the same drilldowns.
      const k = data.kpis;
      const reportBody = $('#reportBody');
      reportBody.innerHTML = `
        <div class="report-kpis">
          <div class="report-kpi"><div class="report-kpi-num">${k.done}</div><div class="report-kpi-lbl">Done</div></div>
          <div class="report-kpi"><div class="report-kpi-num">${k.changed}</div><div class="report-kpi-lbl">Changed</div></div>
          <div class="report-kpi ${k.late > 0 ? 'bad' : ''}"><div class="report-kpi-num">${k.late}</div><div class="report-kpi-lbl">Late</div></div>
          <div class="report-kpi ${k.blocked > 0 ? 'warn' : ''}"><div class="report-kpi-num">${k.blocked}</div><div class="report-kpi-lbl">Blocked</div></div>
          <div class="report-kpi"><div class="report-kpi-num">${k.decisions}</div><div class="report-kpi-lbl">Decisions</div></div>
          <div class="report-kpi"><div class="report-kpi-num">${k.crs}</div><div class="report-kpi-lbl">CRs decided</div></div>
        </div>
        ${reportSection('What changed', data.changed.slice(0, 30), (a) =>
          `<div class="report-row clickable" data-open-action="${a.id}"><span>${escapeHTML(a.title)}</span><span class="report-meta">${escapeHTML(personName(a.owner))} · ${escapeHTML(a.status)}${a.updatedAt ? ' · ' + a.updatedAt : ''}</span></div>`,
          'No updates in this period.')}
        ${reportSection('Late & blocked', data.lateOrBlocked, (a) => {
          const reason = a.status === 'blocked' ? 'blocked' : `${Math.abs(dayDiff(a.due, data.today))}d late`;
          return `<div class="report-row clickable bad" data-open-action="${a.id}"><span>${escapeHTML(a.title)}</span><span class="report-meta">${escapeHTML(personName(a.owner))} · ${reason}</span></div>`;
        }, 'Nothing late or blocked — nice work.')}
        ${reportSection('Decisions made', data.decisions, (d) =>
          `<div class="report-row"><span>${escapeHTML(d.title)}</span><span class="report-meta">${escapeHTML(personName(d.owner))} · ${escapeHTML(d.date || '—')}</span></div>${d.rationale ? `<div class="report-rationale">${escapeHTML(d.rationale)}</div>` : ''}`,
          'No decisions logged in this period.')}
        ${reportSection('Change requests decided', data.crsDecided, (c) =>
          `<div class="report-row clickable" data-open-cr="${c.id}"><span>${escapeHTML(c.title)}</span><span class="report-meta">${escapeHTML(c.status)}${c.decisionDate ? ' · ' + c.decisionDate : ''}${c.decisionBy ? ' · ' + escapeHTML(personName(c.decisionBy)) : ''}</span></div>`,
          'No CRs decided in this period.')}
        ${reportSection('Top risks (by residual)', data.topRisks, (r) =>
          `<div class="report-row clickable" data-open-risk="${r.id}"><span>${escapeHTML(r.title)}</span><span class="report-meta">residual ${r._score}</span></div>${r.mitigation ? `<div class="report-rationale">${escapeHTML(r.mitigation)}</div>` : ''}`,
          'No risks logged.')}
        <div class="report-section">
          <div class="report-section-title">What's next (next 14 days)</div>
          ${(data.next.milestones.length || data.next.deliverables.length || data.next.actions.length)
            ? `${data.next.milestones.length ? '<div class="report-sub-title">Milestones</div>' + data.next.milestones.map((m) => `<div class="report-row"><span>${escapeHTML(m.name || m.title || '')}</span><span class="report-meta">${escapeHTML(m.date || '')}</span></div>`).join('') : ''}
               ${data.next.deliverables.length ? '<div class="report-sub-title">Deliverables</div>' + data.next.deliverables.map((d) => `<div class="report-row"><span>${escapeHTML(d.name || d.title || '')}</span><span class="report-meta">${escapeHTML(d.date || '')}</span></div>`).join('') : ''}
               ${data.next.actions.length ? '<div class="report-sub-title">Due actions</div>' + data.next.actions.slice(0, 30).map((a) => `<div class="report-row clickable" data-open-action="${a.id}"><span>${escapeHTML(a.title)}</span><span class="report-meta">${escapeHTML(personName(a.owner))} · ${escapeHTML(a.due || '')}</span></div>`).join('') : ''}`
            : '<div class="empty">Nothing scheduled in the next 14 days.</div>'}
        </div>`;
      reportBody.querySelectorAll('[data-open-action]').forEach((el) => {
        el.addEventListener('click', () => openDrawer(el.dataset.openAction));
      });
      reportBody.querySelectorAll('[data-open-cr]').forEach((el) => {
        el.addEventListener('click', () => openChangeRequestEditor(el.dataset.openCr));
      });
      reportBody.querySelectorAll('[data-open-risk]').forEach((el) => {
        el.addEventListener('click', () => openRiskEditor(el.dataset.openRisk));
      });
    }
  }

  function drawReviewStep() {
    const stepper = $('#reviewStepper');
    if (!stepper) return;
    stepper.innerHTML = REVIEW_STEPS.map((s, i) => {
      const cls = i === reviewStep ? 'active' : i < reviewStep ? 'done' : '';
      return `<div class="review-step ${cls}">${i + 1}. ${s.name}</div>`;
    }).join('');

    const body = $('#reviewBody');
    const proj = curProject();
    const today = todayISO();
    const since14 = fmtISO(new Date(Date.now() - 14 * dayMs));

    if (REVIEW_STEPS[reviewStep].id === 'changes') {
      const changed = (proj.actions || []).filter((a) => a.updatedAt >= since14).slice(0, 20);
      body.innerHTML = `<h2>What changed (last 14 days)</h2>` + reviewList(changed, 'No recent updates.');
    }
    else if (REVIEW_STEPS[reviewStep].id === 'late') {
      const items = (proj.actions || []).filter((a) =>
        a.status !== 'done' && ((a.due && dayDiff(a.due, today) < 0) || a.status === 'blocked'));
      body.innerHTML = `<h2>Late & blocked</h2>` + reviewList(items, 'No late or blocked items.');
    }
    else if (REVIEW_STEPS[reviewStep].id === 'next') {
      const items = (proj.actions || []).filter((a) =>
        a.status !== 'done' && a.due && dayDiff(a.due, today) >= 0 && dayDiff(a.due, today) <= 14)
        .sort((a, b) => a.due.localeCompare(b.due));
      body.innerHTML = `<h2>What's next (next 14 days)</h2>` + reviewList(items, 'Nothing scheduled.');
    }
    else if (REVIEW_STEPS[reviewStep].id === 'decisions') {
      const decs = proj.decisions || [];
      body.innerHTML = `<h2>Decisions</h2>
        <div class="row-list">
          ${decs.length ? decs.map((d) => `
            <div class="row">
              <span>${escapeHTML(d.title)}</span>
              <span class="row-meta">${escapeHTML(personName(d.owner))} • ${fmtDate(d.date)}</span>
            </div>`).join('') : '<div class="empty">No decisions logged.</div>'}
        </div>
        <div style="margin-top:14px;"><button class="ghost" id="btnAddDecision">+ Log a decision</button></div>`;
      $('#btnAddDecision').addEventListener('click', () => openQuickAdd('decision'));
    }
    else if (REVIEW_STEPS[reviewStep].id === 'summary') {
      const k = kpis();
      body.innerHTML = `<h2>Summary</h2>
        <p class="page-sub">A snapshot of where the project stands at the end of this review.</p>
        <div class="dashboard" style="grid-template-columns: repeat(4, 1fr);">
          <div class="kpi"><div class="kpi-label">Done</div><div class="kpi-value ok">${k.done}</div></div>
          <div class="kpi"><div class="kpi-label">In progress</div><div class="kpi-value">${k.doing}</div></div>
          <div class="kpi"><div class="kpi-label">Late</div><div class="kpi-value bad">${k.late}</div></div>
          <div class="kpi"><div class="kpi-label">Blocked</div><div class="kpi-value warn">${k.blocked}</div></div>
        </div>
        <p style="margin-top:14px;">Use <em>Export HTML</em> to save a standalone report.</p>`;
    }
  }

  function reviewList(items, emptyMsg) {
    if (!items.length) return `<div class="empty">${emptyMsg}</div>`;
    return `<div class="row-list">` +
      items.map((a) => {
        const dueCls = statusOfDue(a.due, a.status);
        return `
          <div class="row ${dueCls}">
            <span class="clickable" data-id="${a.id}">${escapeHTML(a.title)}</span>
            <span class="row-meta">
              <select class="inline" data-id="${a.id}" data-action="status">
                ${STATUSES.map((s) => `<option value="${s.id}" ${s.id === a.status ? 'selected' : ''}>${s.name}</option>`).join('')}
              </select>
              <select class="inline" data-id="${a.id}" data-action="owner">
                ${actorOptionsHTML(a.owner)}
              </select>
              <input class="inline" type="date" data-id="${a.id}" data-action="due" value="${a.due || ''}" />
            </span>
          </div>`;
      }).join('') +
      `</div>`;
  }

  function exportReviewHTML() {
    const proj = curProject();
    const k = kpis();
    const today = todayISO();
    const since14 = fmtISO(new Date(Date.now() - 14 * dayMs));
    const list = (arr) => arr.length
      ? '<ul>' + arr.map((a) => `<li><b>${escapeHTML(a.title)}</b> — ${escapeHTML(personName(a.owner))} • due ${a.due || '—'} • ${a.status}</li>`).join('') + '</ul>'
      : '<p><i>None.</i></p>';
    const changed = (proj.actions || []).filter((a) => a.updatedAt >= since14);
    const late = (proj.actions || []).filter((a) =>
      a.status !== 'done' && ((a.due && dayDiff(a.due, today) < 0) || a.status === 'blocked'));
    const next = (proj.actions || []).filter((a) =>
      a.status !== 'done' && a.due && dayDiff(a.due, today) >= 0 && dayDiff(a.due, today) <= 14)
      .sort((a, b) => a.due.localeCompare(b.due));
    const html = `<!doctype html><html><head><meta charset="utf-8"><title>Review — ${escapeHTML(proj.name)}</title>
      <style>
        body{font:14px -apple-system,Segoe UI,Helvetica,Arial,sans-serif;color:#1a1c24;max-width:780px;margin:30px auto;padding:0 20px;}
        h1{font-size:22px;margin:0 0 4px;} h2{font-size:16px;margin:24px 0 6px;border-bottom:1px solid #ddd;padding-bottom:4px;}
        .meta{color:#666;font-size:12px;margin-bottom:18px;}
        .kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0;}
        .kpi{background:#f6f7fb;border:1px solid #e5e7ee;border-radius:8px;padding:10px;}
        .kpi b{font-size:22px;display:block;}
        ul{padding-left:18px;} li{margin:4px 0;}
      </style></head><body>
      <h1>${escapeHTML(proj.name)} — Review</h1>
      <div class="meta">Generated ${fmtFull(today)}</div>
      <div class="kpis">
        <div class="kpi"><b>${k.done}</b>Done</div>
        <div class="kpi"><b>${k.doing}</b>In progress</div>
        <div class="kpi"><b>${k.late}</b>Late</div>
        <div class="kpi"><b>${k.blocked}</b>Blocked</div>
      </div>
      <h2>What changed (last 14 days)</h2>${list(changed)}
      <h2>Late & blocked</h2>${list(late)}
      <h2>Next 14 days</h2>${list(next)}
      <h2>Decisions</h2>${(proj.decisions || []).length
        ? '<ul>' + proj.decisions.map((d) => `<li><b>${escapeHTML(d.title)}</b> — ${escapeHTML(d.rationale || '')} <i>(${escapeHTML(personName(d.owner))}, ${d.date})</i></li>`).join('') + '</ul>'
        : '<p><i>None.</i></p>'}
      <h2>Risks</h2>${(proj.risks || []).length
        ? '<ul>' + proj.risks.map((r) => `<li><b>${escapeHTML(r.title)}</b> (P${r.probability}×I${r.impact}) — ${escapeHTML(r.mitigation || '')}</li>`).join('') + '</ul>'
        : '<p><i>None.</i></p>'}
      </body></html>`;
    const blob = new Blob([html], { type: 'text/html' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = `review-${proj.id}-${today}.html`;
    a.click();
    URL.revokeObjectURL(url);
    toast('Review exported');
  }

  function openDeliverableEditor(deliverableId) {
    const proj = curProject();
    const d = (proj.deliverables || []).find((x) => x.id === deliverableId);
    if (!d) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">Edit deliverable</div>
          <button class="icon-btn" id="dvClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Name</label><input id="dvName" value="${escapeHTML(d.name)}" /></div>
          <div class="qa-row">
            <div class="field"><label>Due</label><input id="dvDue" type="date" value="${d.dueDate || ''}" /></div>
            <div class="field"><label>Status</label>
              <select id="dvStatus">
                <option value="todo" ${d.status === 'todo' ? 'selected' : ''}>Not started</option>
                <option value="doing" ${d.status === 'doing' ? 'selected' : ''}>In progress</option>
                <option value="done" ${d.status === 'done' ? 'selected' : ''}>Done</option>
              </select>
            </div>
          </div>
          <div class="field"><label>Component <span class="muted">— optional</span></label>
            <select id="dvComp">
              <option value="">— None</option>
              ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === d.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
            </select>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="dvCancel">Cancel</button>
          <button class="ghost" id="dvDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="dvSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('dvName').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#dvClose').addEventListener('click', close);
    overlay.querySelector('#dvCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
    overlay.querySelector('#dvSave').addEventListener('click', () => {
      d.name = document.getElementById('dvName').value.trim() || d.name;
      d.dueDate = document.getElementById('dvDue').value || null;
      d.status = document.getElementById('dvStatus').value;
      d.component = document.getElementById('dvComp').value || null;
      commit('deliverable-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#dvDelete').addEventListener('click', () => {
      const linked = (proj.actions || []).filter((a) => a.deliverable === d.id).length;
      if (!confirm(`Delete "${d.name}"?` + (linked ? ` (${linked} action${linked === 1 ? '' : 's'} will become unlinked)` : ''))) return;
      proj.deliverables = (proj.deliverables || []).filter((x) => x.id !== d.id);
      (proj.actions || []).forEach((a) => { if (a.deliverable === d.id) a.deliverable = null; });
      commit('deliverable-delete');
      close();
      toast('Deleted');
    });
  }

  function openMilestoneEditor(milestoneId) {
    const proj = curProject();
    const m = (proj.milestones || []).find((x) => x.id === milestoneId);
    if (!m) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">Edit milestone</div>
          <button class="icon-btn" id="msClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Name</label><input id="msName" value="${escapeHTML(m.name)}" /></div>
          <div class="qa-row">
            <div class="field"><label>Start date</label><input id="msDate" type="date" value="${m.date || ''}" /></div>
            <div class="field"><label>End date <span class="muted">— leave empty for a single day</span></label><input id="msEndDate" type="date" value="${m.endDate || ''}" /></div>
            <div class="field"><label>Status</label>
              <select id="msStatus">
                <option value="todo" ${m.status === 'todo' ? 'selected' : ''}>Not started</option>
                <option value="doing" ${m.status === 'doing' ? 'selected' : ''}>In progress</option>
                <option value="done" ${m.status === 'done' ? 'selected' : ''}>Done</option>
              </select>
            </div>
          </div>
          <div class="field"><label>Component <span class="muted">— optional</span></label>
            <select id="msComp">
              <option value="">— None</option>
              ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === m.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
            </select>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="msCancel">Cancel</button>
          <button class="ghost" id="msDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="msSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('msName').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#msClose').addEventListener('click', close);
    overlay.querySelector('#msCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
    overlay.querySelector('#msSave').addEventListener('click', () => {
      m.name = document.getElementById('msName').value.trim() || m.name;
      m.date = document.getElementById('msDate').value || null;
      const ed = document.getElementById('msEndDate').value || null;
      // Reject end-before-start; treat equal as single-day (clear endDate).
      if (ed && m.date && ed < m.date) { toast('End date can\'t be before start date'); return; }
      m.endDate = (ed && ed !== m.date) ? ed : null;
      m.status = document.getElementById('msStatus').value;
      m.component = document.getElementById('msComp').value || null;
      commit('milestone-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#msDelete').addEventListener('click', () => {
      const linked = (proj.actions || []).filter((a) => a.milestone === m.id).length;
      if (!confirm(`Delete "${m.name}"?` + (linked ? ` (${linked} action${linked === 1 ? '' : 's'} will become unlinked)` : ''))) return;
      proj.milestones = (proj.milestones || []).filter((x) => x.id !== m.id);
      (proj.actions || []).forEach((a) => { if (a.milestone === m.id) a.milestone = null; });
      commit('milestone-delete');
      close();
      toast('Deleted');
    });
  }

  // Click a meeting chip in the calendar → open this small editor with
  // the same progressive-disclosure layout used by Quick Add: only the
  // fields required for the current selection are visible. Switching
  // toggle / unit re-shapes the schema in place so a meeting can be
  // promoted from one-off → recurring (or vice-versa, or a different
  // unit) without re-creating it.
  function openMeetingEditor(meetingId) {
    const proj = curProject();
    const m = (proj.meetings || []).find((x) => x.id === meetingId);
    if (!m) return;
    const repeating = m.kind === 'recurring';
    const initUnit  = repeating ? (m.recurUnit || 'week') : 'week';
    const initInterval = repeating ? (m.interval || 1) : 1;
    const dowOpts = [1,2,3,4,5,6,0].map((d) => {
      const names = { 0: 'Sunday', 1: 'Monday', 2: 'Tuesday', 3: 'Wednesday', 4: 'Thursday', 5: 'Friday', 6: 'Saturday' };
      return `<option value="${d}" ${d === (m.dayOfWeek ?? 1) ? 'selected' : ''}>${names[d]}</option>`;
    }).join('');
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">Edit meeting</div>
          <button class="icon-btn" id="mtgClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Title</label><input id="mtgTitle" value="${escapeHTML(m.title || '')}" /></div>
          <div class="qa-row">
            <div class="field"><label id="mtgDateLbl">${repeating ? 'Start date' : 'Date'}</label>
              <input id="mtgDate" type="date" value="${(repeating ? m.startDate : m.date) || ''}" />
            </div>
            <div class="field"><label>Time <span class="muted">— optional</span></label>
              <input id="mtgTime" type="time" value="${m.time || ''}" />
            </div>
          </div>
          <div class="field">
            <label class="qa-toggle">
              <input type="checkbox" id="mtgRepeats" ${repeating ? 'checked' : ''} />
              <span>Repeating meeting</span>
            </label>
          </div>
          <div id="mtgRecurWrap" ${repeating ? '' : 'hidden'}>
            <div class="qa-row qa-row-tight">
              <div class="field" style="flex: 0 0 auto;">
                <label>Every</label>
                <input id="mtgInterval" type="number" min="1" max="99" value="${initInterval}" style="width:64px;" />
              </div>
              <div class="field" style="flex: 1;">
                <label>&nbsp;</label>
                <select id="mtgUnit">
                  <option value="day"   ${initUnit === 'day'   ? 'selected' : ''}>Day(s)</option>
                  <option value="week"  ${initUnit === 'week'  ? 'selected' : ''}>Week(s)</option>
                  <option value="month" ${initUnit === 'month' ? 'selected' : ''}>Month(s)</option>
                </select>
              </div>
              <div class="field" id="mtgDowField" ${initUnit === 'week' ? '' : 'hidden'}>
                <label>On</label>
                <select id="mtgDow">${dowOpts}</select>
              </div>
            </div>
            <div class="field">
              <label>Ends <span class="muted">— optional</span></label>
              <input id="mtgEndDate" type="date" value="${m.endDate || ''}" />
            </div>
          </div>
          <div class="field"><label>Component <span class="muted">— optional</span></label>
            <select id="mtgComp">
              <option value="">— None</option>
              ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === m.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
            </select>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="mtgCancel">Cancel</button>
          <button class="ghost" id="mtgDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="mtgSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('mtgTitle').focus(), 30);
    const close = () => overlay.remove();
    const repeatChk = overlay.querySelector('#mtgRepeats');
    const recurWrap = overlay.querySelector('#mtgRecurWrap');
    const dateInput = overlay.querySelector('#mtgDate');
    const unitSel   = overlay.querySelector('#mtgUnit');
    const dowField  = overlay.querySelector('#mtgDowField');
    const dowSel    = overlay.querySelector('#mtgDow');
    const dateLbl   = overlay.querySelector('#mtgDateLbl');
    function syncDowFromDate() {
      const v = dateInput.value;
      if (v) dowSel.value = String(parseDate(v).getDay());
    }
    repeatChk.addEventListener('change', () => {
      recurWrap.hidden = !repeatChk.checked;
      dateLbl.textContent = repeatChk.checked ? 'Start date' : 'Date';
      if (repeatChk.checked && unitSel.value === 'week') syncDowFromDate();
    });
    unitSel.addEventListener('change', () => {
      dowField.hidden = unitSel.value !== 'week';
      if (unitSel.value === 'week') syncDowFromDate();
    });
    dateInput.addEventListener('change', () => {
      if (repeatChk.checked && unitSel.value === 'week') syncDowFromDate();
    });
    overlay.querySelector('#mtgClose').addEventListener('click', close);
    overlay.querySelector('#mtgCancel').addEventListener('click', close);
    overlay.querySelector('#mtgSave').addEventListener('click', () => {
      const title = overlay.querySelector('#mtgTitle').value.trim();
      if (!title) { toast('Title required'); return; }
      const date = dateInput.value || todayISO();
      const time = overlay.querySelector('#mtgTime').value || null;
      const isRepeating = repeatChk.checked;
      m.title = title;
      m.time = time;
      m.component = overlay.querySelector('#mtgComp').value || null;
      if (!isRepeating) {
        m.kind = 'oneoff';
        m.date = date;
        m.endDate = null;
        delete m.recurUnit;
        delete m.interval;
        delete m.dayOfWeek;
        delete m.startDate;
      } else {
        const interval = Math.max(1, parseInt(overlay.querySelector('#mtgInterval').value, 10) || 1);
        const unit = unitSel.value === 'day' ? 'day'
                   : unitSel.value === 'month' ? 'month'
                   : 'week';
        const ed = overlay.querySelector('#mtgEndDate').value || null;
        if (ed && ed < date) { toast('End date can\'t be before the start date'); return; }
        m.kind = 'recurring';
        m.recurUnit = unit;
        m.interval  = interval;
        m.startDate = date;
        m.endDate   = ed;
        if (unit === 'week') m.dayOfWeek = parseInt(dowSel.value, 10);
        else delete m.dayOfWeek;
        delete m.date;
      }
      commit('meeting-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#mtgDelete').addEventListener('click', () => {
      if (!confirm(`Delete "${m.title}"?`)) return;
      proj.meetings = (proj.meetings || []).filter((x) => x.id !== m.id);
      commit('meeting-delete');
      close();
      toast('Deleted');
    });
  }

  // Persistent filter state for the R&O page
  const roState = { kind: 'all', view: 'list' }; // view: 'list' | 'matrix'

  let _mtxTipEl = null;
  function ensureMtxTipEl() {
    if (_mtxTipEl) return _mtxTipEl;
    _mtxTipEl = document.createElement('div');
    _mtxTipEl.className = 'mtx-tooltip';
    document.body.appendChild(_mtxTipEl);
    return _mtxTipEl;
  }
  function wireRiskMatrixHover(scope) {
    const proj = curProject();
    const svg = scope.querySelector('.mtx-svg');
    if (!svg) return;
    const tip = ensureMtxTipEl();
    function show(circle, x, y) {
      const r = (proj.risks || []).find((rr) => rr.id === circle.dataset.riskId);
      if (!r) return;
      ensureRiskShape(r);
      const isOpp = (r.kind || 'risk') === 'opportunity';
      const stage = circle.dataset.ptStage; // 'inherent' | 'residual'
      const inh = r.inherent, res = r.residual;
      const inhScore = inh.probability * inh.impact;
      const resScore = res.probability * res.impact;
      const linked = r.actionId
        ? state.projects.flatMap((p) => p.actions || []).find((a) => a.id === r.actionId)
        : null;
      tip.innerHTML = `
        <div class="mtx-tip-head">
          <span class="mtx-tip-icon ${isOpp ? 'opp' : 'risk'}">${isOpp ? '▽' : '▲'}</span>
          <span class="mtx-tip-title">${escapeHTML(r.title)}</span>
          <span class="mtx-tip-stage">${stage}</span>
        </div>
        <div class="mtx-tip-row"><span class="mtx-tip-lbl">Inherent</span><span>P${inh.probability} × I${inh.impact} = <b>${inhScore}</b></span></div>
        <div class="mtx-tip-row"><span class="mtx-tip-lbl">Residual</span><span>P${res.probability} × I${res.impact} = <b>${resScore}</b></span></div>
        <div class="mtx-tip-row"><span class="mtx-tip-lbl">Owner</span><span>${escapeHTML(personName(r.owner))}</span></div>
        ${r.mitigation ? `<div class="mtx-tip-mit"><span class="mtx-tip-lbl">${isOpp ? 'Capture' : 'Mitigation'}</span>${escapeHTML(r.mitigation)}</div>` : ''}
        ${linked ? `<div class="mtx-tip-link">↗ ${escapeHTML(linked.title)} <span style="color:var(--text-faint)">— ${escapeHTML(personName(linked.owner))}</span></div>` : ''}`;
      tip.style.display = 'block';
      const rr = tip.getBoundingClientRect();
      let px = x + 14, py = y + 14;
      if (px + rr.width  > innerWidth  - 8) px = x - rr.width - 14;
      if (py + rr.height > innerHeight - 8) py = y - rr.height - 14;
      tip.style.left = Math.max(8, px) + 'px';
      tip.style.top  = Math.max(8, py) + 'px';
    }
    function hide() { tip.style.display = 'none'; }
    svg.addEventListener('mousemove', (e) => {
      const c = e.target.closest('.mtx-pt[data-risk-id]');
      if (!c) { hide(); return; }
      show(c, e.clientX, e.clientY);
    });
    svg.addEventListener('mouseleave', hide);
    // Left-click on a dot opens the editor for that risk / opportunity.
    // Same affordance as double-clicking a row in the list view.
    svg.addEventListener('click', (e) => {
      const c = e.target.closest('.mtx-pt[data-risk-id]');
      if (!c) return;
      e.preventDefault();
      hide();
      openRiskEditor(c.dataset.riskId);
    });
    svg.addEventListener('contextmenu', (e) => {
      const c = e.target.closest('.mtx-pt[data-risk-id]');
      if (!c) return;
      e.preventDefault();
      hide();
      const id = c.dataset.riskId;
      const r = (proj.risks || []).find((rr) => rr.id === id);
      if (!r) return;
      const isOpp = (r.kind || 'risk') === 'opportunity';
      showContextMenu(e.clientX, e.clientY, [
        { icon: '✎', label: 'Edit…', onClick: () => openRiskEditor(id) },
        { divider: true },
        { icon: '×', label: `Delete ${isOpp ? 'opportunity' : 'risk'}`, danger: true, onClick: () => {
          if (!confirm(`Delete "${r.title}"?`)) return;
          curProject().risks = (curProject().risks || []).filter((x) => x.id !== id);
          commit('risk-delete');
          toast('Deleted');
        }},
      ]);
    });
    // Double-click also opens the editor
    svg.addEventListener('dblclick', (e) => {
      const c = e.target.closest('.mtx-pt[data-risk-id]');
      if (c) openRiskEditor(c.dataset.riskId);
    });
  }

  function openRiskEditor(riskId, opts = {}) {
    const proj = curProject();
    const r = (proj.risks || []).find((x) => x.id === riskId);
    if (!r) return;
    ensureRiskShape(r);
    const isOpp = (r.kind || 'risk') === 'opportunity';
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:560px;">
        <div class="desc-head">
          <div class="desc-title">${escapeHTML(r.title)} — ${isOpp ? 'opportunity' : 'risk'}</div>
          <button class="icon-btn" id="reClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Type</label>
            <div class="seg" role="tablist" aria-label="Kind">
              <button type="button" class="seg-btn ${!isOpp ? 'active' : ''}" data-re-kind="risk">▲ Risk</button>
              <button type="button" class="seg-btn ${isOpp ? 'active' : ''}" data-re-kind="opportunity">▽ Opportunity</button>
            </div>
          </div>
          <div class="qa-row">
            <div class="field" style="flex: 0 0 140px;"><label>Identifier</label><input id="reIdent" value="${escapeHTML(r.identifier || '')}" placeholder="Auto" title="Short code (e.g. ${escapeHTML(projectInitialsForId(proj?.name))}-${isOpp ? 'O' : 'R'}-1). Editable." /></div>
            <div class="field" style="flex: 1;"><label>Title</label><input id="reTitle" value="${escapeHTML(r.title)}" /></div>
          </div>
          <div class="qa-row">
            <div class="field"><label id="rePILbl">${isOpp ? 'Baseline P (1-5)' : 'Inherent P (1-5)'}</label><input id="rePI" type="number" min="1" max="5" value="${r.inherent.probability}" title="${isOpp ? 'Baseline — value if you do nothing to pursue this opportunity.' : 'Inherent — worst case if nothing is done to mitigate.'}" /></div>
            <div class="field"><label id="reIILbl">${isOpp ? 'Baseline I (1-5)' : 'Inherent I (1-5)'}</label><input id="reII" type="number" min="1" max="5" value="${r.inherent.impact}" /></div>
          </div>
          <div class="qa-row">
            <div class="field"><label id="rePRLbl">${isOpp ? 'Captured P (post-action)' : 'Residual P (post-action)'}</label><input id="rePR" type="number" min="1" max="5" value="${r.residual.probability}" title="${isOpp ? 'Captured — value if you successfully pursue the capture plan below. Should be higher than baseline.' : 'Residual — what survives after mitigation. Should be lower than inherent.'}" /></div>
            <div class="field"><label id="reIRLbl">${isOpp ? 'Captured I (post-action)' : 'Residual I (post-action)'}</label><input id="reIR" type="number" min="1" max="5" value="${r.residual.impact}" /></div>
          </div>
          <div class="field"><label>Owner</label>
            <select id="reOwner">${actorOptionsHTML(r.owner)}</select>
          </div>
          <div class="field"><label id="reMitLbl">${isOpp ? 'Capture plan' : 'Mitigation'}</label><textarea id="reMit" style="min-height:80px;">${escapeHTML(r.mitigation || '')}</textarea></div>
          <div class="field"><label>Linked action (optional)</label>
            <select id="reActionLink">
              <option value="">— none —</option>
              ${(proj.actions || []).slice().sort((a, b) => a.title.localeCompare(b.title)).map((a) => `<option value="${a.id}" ${a.id === r.actionId ? 'selected' : ''}>${escapeHTML(a.title)} — ${escapeHTML(personName(a.owner))}</option>`).join('')}
            </select>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="reCancel">Cancel</button>
          <button class="ghost" id="reDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="reSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => {
      const tgt = opts.focusLink ? document.getElementById('reActionLink') : document.getElementById('reTitle');
      tgt?.focus();
      if (opts.focusLink) {
        // Scroll the picker into view + give it a brief halo so users
        // landing from a "+ link action" click see exactly where to act.
        tgt?.scrollIntoView({ block: 'center', behavior: 'smooth' });
        tgt?.classList.add('field-flash');
        setTimeout(() => tgt?.classList.remove('field-flash'), 1400);
      }
    }, 30);

    let kind = isOpp ? 'opportunity' : 'risk';
    overlay.querySelectorAll('.seg-btn[data-re-kind]').forEach((b) => {
      b.addEventListener('click', () => {
        kind = b.dataset.reKind;
        overlay.querySelectorAll('.seg-btn[data-re-kind]').forEach((x) =>
          x.classList.toggle('active', x.dataset.reKind === kind));
        const isOppNow = kind === 'opportunity';
        document.getElementById('reMitLbl').textContent = isOppNow ? 'Capture plan' : 'Mitigation';
        // Flip the P×I labels too — "residual" is a misnomer for upside.
        const setLbl = (id, txt) => { const el = document.getElementById(id); if (el) el.textContent = txt; };
        setLbl('rePILbl', isOppNow ? 'Baseline P (1-5)' : 'Inherent P (1-5)');
        setLbl('reIILbl', isOppNow ? 'Baseline I (1-5)' : 'Inherent I (1-5)');
        setLbl('rePRLbl', isOppNow ? 'Captured P (post-action)' : 'Residual P (post-action)');
        setLbl('reIRLbl', isOppNow ? 'Captured I (post-action)' : 'Residual I (post-action)');
      });
    });

    const close = () => overlay.remove();
    overlay.querySelector('#reClose').addEventListener('click', close);
    overlay.querySelector('#reCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.

    overlay.querySelector('#reSave').addEventListener('click', () => {
      const oldKind = r.kind || 'risk';
      r.kind = kind;
      r.title = document.getElementById('reTitle').value.trim() || r.title;
      // Identifier — accept the typed value as-is. Trim; if empty, OR
      // if the user just switched between risk / opportunity, re-derive
      // a fresh identifier so the R / O prefix stays in sync.
      const idInput = (document.getElementById('reIdent').value || '').trim();
      if (idInput) {
        r.identifier = idInput;
      } else if (!r.identifier || oldKind !== kind) {
        r.identifier = generateRiskIdentifier(proj, kind);
      }
      r.inherent = {
        probability: clamp(parseInt(document.getElementById('rePI').value, 10) || 3, 1, 5),
        impact:      clamp(parseInt(document.getElementById('reII').value, 10) || 3, 1, 5),
      };
      r.residual = {
        probability: clamp(parseInt(document.getElementById('rePR').value, 10) || r.inherent.probability, 1, 5),
        impact:      clamp(parseInt(document.getElementById('reIR').value, 10) || r.inherent.impact,      1, 5),
      };
      r.mitigation = document.getElementById('reMit').value;
      r.owner = document.getElementById('reOwner').value;
      r.actionId = document.getElementById('reActionLink').value || null;
      commit('risk-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#reDelete').addEventListener('click', () => {
      if (!confirm(`Delete "${r.title}"?`)) return;
      proj.risks = (proj.risks || []).filter((x) => x.id !== r.id);
      commit('risk-delete');
      close();
      toast('Deleted');
    });
  }

  // Helpers — work for both new schema (inherent/residual) and legacy (probability/impact)
  function getInherent(r) {
    if (r.inherent && typeof r.inherent.probability === 'number') return r.inherent;
    return { probability: r.probability || 0, impact: r.impact || 0 };
  }
  function getResidual(r) {
    if (r.residual && typeof r.residual.probability === 'number') return r.residual;
    return getInherent(r);
  }
  function ensureRiskShape(r) {
    if (!r.inherent) r.inherent = { probability: r.probability || 3, impact: r.impact || 3 };
    if (!r.residual) r.residual = { ...r.inherent };
  }

  // Build a short prefix from a project's name (acronym of the words,
  // fall back to the first 3 alphabetic chars of a single word). Used
  // as the base for risk identifiers.
  function projectInitialsForId(name) {
    const words = (name || '').split(/[^a-zA-Z]+/).filter(Boolean);
    if (!words.length) return 'PRJ';
    if (words.length === 1) return words[0].slice(0, 3).toUpperCase();
    return words.map((w) => w[0]).join('').toUpperCase().slice(0, 4);
  }
  // Next free identifier for this project + kind, in the form
  // "<initials>-R-<n>" or "<initials>-O-<n>". Walks the existing risks
  // to find the highest matching sequence and adds 1. User-edited
  // free-form identifiers don't participate, so manual overrides
  // never collide with the auto-generator.
  function generateRiskIdentifier(project, kind) {
    const prefix = projectInitialsForId(project?.name) + '-' + (kind === 'opportunity' ? 'O' : 'R') + '-';
    const escaped = prefix.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const re = new RegExp('^' + escaped + '(\\d+)$');
    let max = 0;
    (project?.risks || []).forEach((r) => {
      if (!r.identifier) return;
      const m = String(r.identifier).match(re);
      if (m) { const n = parseInt(m[1], 10); if (n > max) max = n; }
    });
    return prefix + (max + 1);
  }
  // Mirror of generateRiskIdentifier for open points: "<initials>-OP-<n>".
  // Open points have a single kind, so the prefix is fixed.
  function generateOpenPointIdentifier(project) {
    const prefix = projectInitialsForId(project?.name) + '-OP-';
    const escaped = prefix.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const re = new RegExp('^' + escaped + '(\\d+)$');
    let max = 0;
    (project?.openPoints || []).forEach((op) => {
      if (!op.identifier) return;
      const m = String(op.identifier).match(re);
      if (m) { const n = parseInt(m[1], 10); if (n > max) max = n; }
    });
    return prefix + (max + 1);
  }

  function riskMatrixSVG(items) {
    registerChartData('risk-matrix', () => ({
      headers: ['Identifier', 'Kind', 'Title', 'Owner', 'Inherent P', 'Inherent I', 'Inherent score', 'Residual P', 'Residual I', 'Residual score', 'Mitigation'],
      rows: items.map((r) => {
        const inh = getInherent(r), res = getResidual(r);
        return [
          r.identifier || '', r.kind || 'risk', r.title || '', personName(r.owner),
          inh.probability, inh.impact, inh.probability * inh.impact,
          res.probability, res.impact, res.probability * res.impact,
          r.mitigation || '',
        ];
      }),
    }));
    const W = 540, H = 460;
    const padL = 60, padR = 20, padT = 30, padB = 56;
    const cellW = (W - padL - padR) / 5;
    const cellH = (H - padT - padB) / 5;

    const cells = [];
    for (let p = 1; p <= 5; p++) {
      for (let i = 1; i <= 5; i++) {
        const score = p * i;
        const cls = score >= 15 ? 'extreme' : score >= 10 ? 'high' : score >= 5 ? 'mid' : 'low';
        const x = padL + (p - 1) * cellW;
        const y = padT + (5 - i) * cellH;
        cells.push(`<rect class="mtx-cell mtx-${cls}" x="${x.toFixed(1)}" y="${y.toFixed(1)}" width="${cellW.toFixed(1)}" height="${cellH.toFixed(1)}" />`);
        cells.push(`<text class="mtx-cell-num" x="${(x + cellW/2).toFixed(1)}" y="${(y + cellH/2 + 4).toFixed(1)}" text-anchor="middle">${score}</text>`);
      }
    }

    const xLabels = [1, 2, 3, 4, 5].map((p) =>
      `<text class="mtx-tick" x="${(padL + (p - 0.5) * cellW).toFixed(1)}" y="${padT + 5 * cellH + 16}" text-anchor="middle">${p}</text>`).join('');
    const yLabels = [1, 2, 3, 4, 5].map((i) =>
      `<text class="mtx-tick" x="${padL - 8}" y="${(padT + (5 - i + 0.5) * cellH + 4).toFixed(1)}" text-anchor="end">${i}</text>`).join('');

    // Cell-pack: group every marker by its (probability, impact, stage)
    // cell. When multiple risks share a cell, fan them out in a ring
    // around the cell centre so each dot is independently visible —
    // otherwise stacked risks would draw on top of each other and
    // become invisible. Inherent and residual each get their own
    // grouping so the arrow still connects to the correct dot.
    const cellGroups = { inherent: new Map(), residual: new Map() };
    items.forEach((r) => {
      const inh = getInherent(r);
      const res = getResidual(r);
      const inhKey = `${Math.max(1, inh.probability)},${Math.max(1, inh.impact)}`;
      const resKey = `${Math.max(1, res.probability)},${Math.max(1, res.impact)}`;
      if (!cellGroups.inherent.has(inhKey)) cellGroups.inherent.set(inhKey, []);
      if (!cellGroups.residual.has(resKey)) cellGroups.residual.set(resKey, []);
      cellGroups.inherent.get(inhKey).push(r.id);
      cellGroups.residual.get(resKey).push(r.id);
    });
    // The matrix's logical (0, 0) origin sits at the bottom-left
    // corner of the grid (probability = 0, impact = 0). Co-bucketed
    // markers are spread along the ARC of constant radius from that
    // origin so every dot in a cell is exactly equidistant from (0,0)
    // — preventing the visual reading that "outer-ring" risks are more
    // severe than "inner-ring" ones when they share the same rating.
    const origX = padL;
    const origY = padT + 5 * cellH;

    // Pack one item along its cell's iso-distance arc. For a single
    // item the position is the cell centre; for n > 1 the cluster
    // walks along the arc with a small angular step. Step is sized
    // from a target arc-length so all clusters look equally tight
    // regardless of their distance from the origin.
    function arcPosition(probability, impact, i, n, arcLen) {
      const cx = padL + (probability - 0.5) * cellW;
      const cy = padT + (5 - impact + 0.5) * cellH;
      if (n <= 1) return { x: cx, y: cy };
      const dx = cx - origX;
      const dy = cy - origY;
      const dist = Math.sqrt(dx * dx + dy * dy) || 1;
      const baseAngle = Math.atan2(dy, dx);
      // Arc length → angle: θ = s / r. Each step covers (arcLen / dist)
      // radians; centre the cluster on baseAngle so even-count packs
      // straddle the cell centre, odd-count packs put one dot dead-on.
      const stepAng = arcLen / dist;
      const t = (i - (n - 1) / 2);            // -…–1, 0, 1…+ (centred)
      const angle = baseAngle + t * stepAng;
      return {
        x: origX + Math.cos(angle) * dist,
        y: origY + Math.sin(angle) * dist,
      };
    }
    // Calibrate arc length so the cluster spread is ~1/3 of cell size
    // — wide enough that co-bucketed dots don't visually merge, narrow
    // enough that they read as a single "this rating" cluster.
    const inhArcLen = Math.min(cellW, cellH) * 0.22;
    const resArcLen = Math.min(cellW, cellH) * 0.20;

    const markers = items.map((r) => {
      const inh = getInherent(r);
      const res = getResidual(r);
      const inhP = Math.max(1, inh.probability);
      const inhI = Math.max(1, inh.impact);
      const resP = Math.max(1, res.probability);
      const resI = Math.max(1, res.impact);
      const inhKey = `${inhP},${inhI}`;
      const resKey = `${resP},${resI}`;
      const inhGroup = cellGroups.inherent.get(inhKey);
      const resGroup = cellGroups.residual.get(resKey);
      const inhIdx = inhGroup.indexOf(r.id);
      const resIdx = resGroup.indexOf(r.id);
      const inhPos = arcPosition(inhP, inhI, inhIdx, inhGroup.length, inhArcLen);
      const resPos = arcPosition(resP, resI, resIdx, resGroup.length, resArcLen);
      const xInh = inhPos.x;
      const yInh = inhPos.y;
      const xRes = resPos.x;
      const yRes = resPos.y;
      const isOpp = (r.kind || 'risk') === 'opportunity';
      // "Moved" now compares CELL identity (not pixel position) so the
      // arrow only renders when the residual is in a different bucket
      // than the inherent — packing offsets within a cell don't count.
      const moved = inhKey !== resKey;
      let svg = '';
      if (moved) {
        svg += `<line class="mtx-arrow ${isOpp ? 'opp' : 'risk'}" x1="${xInh.toFixed(1)}" y1="${yInh.toFixed(1)}" x2="${xRes.toFixed(1)}" y2="${yRes.toFixed(1)}" marker-end="url(#mtxArrow${isOpp ? 'Opp' : 'Risk'})" />`;
      }
      // Slightly smaller dots when many share a cell — keeps the pack
      // legible without overlapping. r=7 baseline → r=5 when 5+ share.
      const inhR = inhGroup.length >= 5 ? 5 : 7;
      const resR = resGroup.length >= 5 ? 4 : 5;
      svg += `<circle class="mtx-pt mtx-pt-inh ${isOpp ? 'opp' : 'risk'}" data-risk-id="${r.id}" data-pt-stage="inherent" cx="${xInh.toFixed(1)}" cy="${yInh.toFixed(1)}" r="${inhR}"></circle>`;
      if (moved) {
        svg += `<circle class="mtx-pt mtx-pt-res ${isOpp ? 'opp' : 'risk'}" data-risk-id="${r.id}" data-pt-stage="residual" cx="${xRes.toFixed(1)}" cy="${yRes.toFixed(1)}" r="${resR}"></circle>`;
      }
      // Identifier label — placed at the inherent dot's polar angle from
      // the origin, just past the dot's outer edge so it doesn't overlap
      // the circle. text-anchor is chosen by which side of the origin
      // the dot sits on so labels read left-to-right naturally.
      if (r.identifier) {
        // Use the cell-centre direction from the origin so labels
        // never overlap the arrow tip pointing toward the residual.
        const dx = xInh - origX;
        const dy = yInh - origY;
        const len = Math.sqrt(dx * dx + dy * dy) || 1;
        const offset = inhR + 5;
        const lx = xInh + (dx / len) * offset;
        const ly = yInh + (dy / len) * offset;
        const anchor = dx >= 0 ? 'start' : 'end';
        svg += `<text class="mtx-pt-lbl ${isOpp ? 'opp' : 'risk'}" x="${lx.toFixed(1)}" y="${ly.toFixed(1)}" text-anchor="${anchor}" dy="3">${escapeHTML(r.identifier)}</text>`;
      }
      return svg;
    }).join('');

    return `
      <svg viewBox="0 0 ${W} ${H}" class="mtx-svg" preserveAspectRatio="xMidYMid meet">
        <defs>
          <marker id="mtxArrowRisk" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" fill="rgba(248,113,113,.85)" />
          </marker>
          <marker id="mtxArrowOpp" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
            <path d="M 0 0 L 10 5 L 0 10 z" fill="rgba(74,222,128,.85)" />
          </marker>
        </defs>
        ${cells.join('')}
        ${xLabels}
        ${yLabels}
        <text class="mtx-axis-lbl" x="${(padL + (W - padL - padR) / 2).toFixed(1)}" y="${H - 18}" text-anchor="middle">Probability →</text>
        <text class="mtx-axis-lbl" x="${20}" y="${(padT + (H - padT - padB) / 2).toFixed(1)}" text-anchor="middle" transform="rotate(-90 20 ${(padT + (H - padT - padB) / 2).toFixed(1)})">Impact →</text>
        ${markers}
      </svg>
      <div class="mtx-legend">
        <span class="legend-item"><span class="dot mtx-low"></span>Low (≤4)</span>
        <span class="legend-item"><span class="dot mtx-mid"></span>Medium (5-9)</span>
        <span class="legend-item"><span class="dot mtx-high"></span>High (10-14)</span>
        <span class="legend-item"><span class="dot mtx-extreme"></span>Extreme (15+)</span>
        <span class="sep"></span>
        <span class="legend-item"><svg width="22" height="10"><circle cx="6" cy="5" r="4" class="mtx-pt-inh risk"/><line x1="10" y1="5" x2="20" y2="5" stroke="rgba(248,113,113,.7)" stroke-width="1.4" /></svg>inherent → residual</span>
      </div>`;
  }

  function renderRisks(root) {
    const proj = curProject();
    proj.risks = proj.risks || [];
    proj.risks.forEach(ensureRiskShape);
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Risks &amp; Opportunities</div>
          <div class="page-sub">Inherent (raw) vs residual (post-mitigation) on a 5×5 matrix. Risks track downside; opportunities track upside.</div>
        </div>
        <div class="page-actions">
          <div class="seg" role="tablist" aria-label="View">
            <button class="seg-btn ${roState.view === 'list' ? 'active' : ''}" data-ro-view="list">List</button>
            <button class="seg-btn ${roState.view === 'matrix' ? 'active' : ''}" data-ro-view="matrix">Matrix</button>
          </div>
          <div class="seg" role="tablist" aria-label="Kind">
            <button class="seg-btn ${roState.kind === 'all' ? 'active' : ''}" data-kind="all">All</button>
            <button class="seg-btn ${roState.kind === 'risk' ? 'active' : ''}" data-kind="risk">Risks</button>
            <button class="seg-btn ${roState.kind === 'opportunity' ? 'active' : ''}" data-kind="opportunity">Opportunities</button>
          </div>
          <button class="ghost" id="btnAddRisk">+ Risk</button>
          <button class="ghost" id="btnAddOpp">+ Opportunity</button>
        </div>
      </div>
      <div id="roBody"></div>`;
    root.appendChild(view);

    function draw() {
      const body = $('#roBody');
      const items = proj.risks
        .filter((r) => roState.kind === 'all' || (r.kind || 'risk') === roState.kind)
        .filter((r) => matchesSearch(r.title, r.identifier, r.mitigation, personName(r.owner)));
      if (!items.length) {
        body.innerHTML = `<div class="empty">${
          roState.kind === 'opportunity' ? 'No opportunities logged yet.' :
          roState.kind === 'risk'        ? 'No risks logged yet.' :
                                           'No risks or opportunities logged.'}</div>`;
        return;
      }
      if (roState.view === 'matrix') {
        body.innerHTML = `<div class="panel chart-panel copyable-chart" data-chart="risk-matrix">${riskMatrixSVG(items)}</div>`;
        wireRiskMatrixHover(body);
        return;
      }
      // List view — preserve project array order so drag-reorder is meaningful.
      const sorted = items.slice();
      body.innerHTML = `<div class="row-list" id="roList">${sorted.map((r) => {
        const kind = r.kind || 'rule';
        const inh = getInherent(r);
        const res = getResidual(r);
        const inhScore = inh.probability * inh.impact;
        const resScore = res.probability * res.impact;
        const moved = inhScore !== resScore;
        const sevCls = inhScore >= 12 ? 'high' : inhScore >= 6 ? 'mid' : 'low';
        const icon = kind === 'opportunity' ? '▽' : '△';
        const responseLbl = kind === 'opportunity' ? 'Capture' : 'Mitigation';
        const linkedAction = r.actionId ? state.projects.flatMap((p) => p.actions || []).find((a) => a.id === r.actionId) : null;
        // Mitigation status chip — colour-keyed to the linked action's
        // state so the PM can see at a glance whether the mitigation is
        // actually moving (done = green, doing = blue, todo = grey,
        // blocked / overdue = red). Missing link shows a discreet
        // "+ link" affordance that opens the editor focused on the
        // picker so users can wire it in one click.
        let mitChip = '';
        if (linkedAction) {
          const isLate = linkedAction.due && linkedAction.status !== 'done' && dayDiff(linkedAction.due, todayISO()) < 0;
          const sCls = isLate ? 'late'
            : linkedAction.status === 'done'   ? 'done'
            : linkedAction.status === 'blocked'? 'blocked'
            : linkedAction.status === 'doing'  ? 'doing'
            : 'todo';
          const sIcon = isLate ? '⏰'
            : linkedAction.status === 'done'   ? '✓'
            : linkedAction.status === 'blocked'? '⨯'
            : linkedAction.status === 'doing'  ? '◐'
            : '○';
          mitChip = `<span class="ro-mit-link ${sCls} clickable" data-open-action="${linkedAction.id}" title="${kind === 'opportunity' ? 'Capture action' : 'Mitigation action'}: ${escapeHTML(linkedAction.title)} (${escapeHTML(linkedAction.status)})">${sIcon} ${escapeHTML(linkedAction.title.length > 26 ? linkedAction.title.slice(0, 24) + '…' : linkedAction.title)}</span>`;
        } else if (resScore >= 6 || kind === 'opportunity') {
          // Only surface the "+ link" affordance when it actually matters —
          // i.e. residual is non-trivial or this is an opportunity (always
          // needs a capture plan). Quiet "0 risk" rows don't get noise.
          mitChip = `<span class="ro-mit-add clickable" data-link-add="${r.id}" title="Link a ${kind === 'opportunity' ? 'capture' : 'mitigation'} action — opens the editor">+ link action</span>`;
        }
        return `
          <div class="row ro-row kind-${kind === 'opportunity' ? 'opportunity' : 'risk'} sev-${sevCls}" data-risk-id="${r.id}">
            ${ROW_GRIP_HTML}
            <span class="ro-icon" title="${kind}">${icon}</span>
            ${r.identifier ? `<span class="ro-ident" title="Identifier">${escapeHTML(r.identifier)}</span>` : ''}
            <span class="ro-title">${escapeHTML(r.title)}</span>
            <span class="ro-score">
              ${moved ? `<span class="ro-pre">${inhScore}</span><span class="ro-arrow">→</span><b>${resScore}</b>` : `<b>${inhScore}</b>`}
            </span>
            <span class="row-meta">
              <span class="ro-resp" title="${responseLbl}">${escapeHTML(r.mitigation || '—')}</span>
              ${mitChip}
              <span class="ro-owner">${escapeHTML(personName(r.owner))}</span>
            </span>
          </div>`;
      }).join('')}</div>`;
      // Drag-reorder list rows
      const roListEl = body.querySelector('#roList');
      if (roListEl) {
        wireListReorder(roListEl, {
          rowSelector: '.ro-row[data-risk-id]',
          idAttr: 'riskId',
          getArray: () => proj.risks,
          setOrder: (ids) => { proj.risks = ids.map((id) => proj.risks.find((x) => x.id === id)).filter(Boolean); },
          commitName: 'risks-reorder',
        });
      }
      // Click linked action chip → open the action drawer.
      // Includes both the legacy .ro-link affordance (kept for compat
      // with reports) and the new .ro-mit-link colour-coded pill.
      $$('.ro-link[data-open-action], .ro-mit-link[data-open-action]', body).forEach((el) =>
        el.addEventListener('click', (e) => {
          e.stopPropagation();
          openDrawer(el.dataset.openAction);
        }));
      // "+ link action" → open the risk editor focused on the picker
      // (handled inside openRiskEditor via the `focusLink` option).
      $$('.ro-mit-add[data-link-add]', body).forEach((el) =>
        el.addEventListener('click', (e) => {
          e.stopPropagation();
          openRiskEditor(el.dataset.linkAdd, { focusLink: true });
        }));
      // Right-click on a risk/opportunity row → context menu (Edit / Delete)
      $$('.ro-row[data-risk-id]', body).forEach((row) => {
        row.addEventListener('contextmenu', (e) => {
          e.preventDefault();
          const id = row.dataset.riskId;
          const r = (proj.risks || []).find((x) => x.id === id);
          if (!r) return;
          const isOpp = (r.kind || 'risk') === 'opportunity';
          showContextMenu(e.clientX, e.clientY, [
            { icon: '✎', label: 'Edit…', onClick: () => openRiskEditor(id) },
            { divider: true },
            { icon: '×', label: `Delete ${isOpp ? 'opportunity' : 'risk'}`, danger: true, onClick: () => {
              if (!confirm(`Delete "${r.title}"? This cannot be undone (except via undo).`)) return;
              proj.risks = (proj.risks || []).filter((x) => x.id !== id);
              commit('risk-delete');
              toast('Deleted');
            }},
          ]);
        });
        // Double-click also opens the editor (consistent with kanban / gantt)
        row.addEventListener('dblclick', () => openRiskEditor(row.dataset.riskId));
      });
    }

    $$('.seg-btn[data-kind]', view).forEach((b) => {
      b.addEventListener('click', () => {
        roState.kind = b.dataset.kind;
        $$('.seg-btn[data-kind]', view).forEach((x) => x.classList.toggle('active', x.dataset.kind === roState.kind));
        draw();
      });
    });
    $$('.seg-btn[data-ro-view]', view).forEach((b) => {
      b.addEventListener('click', () => {
        roState.view = b.dataset.roView;
        $$('.seg-btn[data-ro-view]', view).forEach((x) => x.classList.toggle('active', x.dataset.roView === roState.view));
        draw();
      });
    });
    $('#btnAddRisk').addEventListener('click', () => openQuickAdd('risk', { kind: 'risk' }));
    $('#btnAddOpp').addEventListener('click', () => openQuickAdd('risk', { kind: 'opportunity' }));
    draw();
  }

  function renderDecisions(root) {
    const proj = curProject();
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div><div class="page-title">Decisions</div><div class="page-sub">Capture key choices and their rationale.</div></div>
        <div class="page-actions">
          <button class="ghost" id="btnAddDec">+ Decision</button>
        </div>
      </div>
      <div class="row-list" id="decList"></div>`;
    root.appendChild(view);
    const list = $('#decList');
    // Topbar component filter applies to every panel that has a
    // component dimension. Same semantics as the actions path:
    //   ''         → no filter
    //   '__none__' → only items with no component
    //   '<id>'     → only items with that component id
    const fComp = $('#filterComponent')?.value || '';
    const filteredDecisions = (proj.decisions || []).filter((d) => {
      if (fComp === '__none__' && d.component) return false;
      if (fComp && fComp !== '__none__' && d.component !== fComp) return false;
      return matchesSearch(d.title, d.rationale, personName(d.owner),
        findComponent(proj, d.component)?.name);
    });
    const noResultsCopy = (searchQuery() || fComp)
      ? 'No decisions match the current filters.'
      : 'No decisions logged.';
    if (!filteredDecisions.length) list.innerHTML = `<div class="empty">${noResultsCopy}</div>`;
    else {
      list.innerHTML = filteredDecisions.map((d) => {
        const cmp = findComponent(proj, d.component);
        const c   = cmp ? componentColor(cmp.color) : null;
        const compChip = cmp
          ? `<span class="row-comp" style="background:rgba(${c.rgb},.18);color:rgb(${c.rgb});border:1px solid rgba(${c.rgb},.55);">${escapeHTML(cmp.name)}</span>`
          : '';
        const status = d.status || 'in_effect';
        const statusLbl = status === 'open' ? 'OPEN' : status === 'superseded' ? 'SUPERSEDED' : 'IN EFFECT';
        const statusCls = status === 'open' ? 'open' : status === 'superseded' ? 'superseded' : 'in-effect';
        // Trigger chip — link to the entity that caused this decision.
        let triggerChip = '';
        if (d.triggerRef && typeof d.triggerRef === 'object' && d.triggerRef.type && d.triggerRef.id) {
          const tref = d.triggerRef;
          let lbl = null, attr = '';
          if (tref.type === 'risk') {
            const r = (proj.risks || []).find((x) => x.id === tref.id);
            if (r) { lbl = (r.identifier || r.title || 'Risk'); attr = `data-open-risk="${escapeHTML(r.id)}"`; }
          } else if (tref.type === 'cr') {
            const c2 = (proj.changes || []).find((x) => x.id === tref.id);
            if (c2) { lbl = (c2.title || 'CR'); attr = `data-open-cr="${escapeHTML(c2.id)}"`; }
          } else if (tref.type === 'action') {
            const a = (proj.actions || []).find((x) => x.id === tref.id);
            if (a) { lbl = (a.title || 'Action'); attr = `data-open-action="${escapeHTML(a.id)}"`; }
          }
          if (lbl) triggerChip = `<span class="dec-trigger ${tref.type} clickable" ${attr} title="Triggered by ${escapeHTML(tref.type)}">↳ ${escapeHTML(lbl.length > 36 ? lbl.slice(0, 34) + '…' : lbl)}</span>`;
        } else if (typeof d.triggerRef === 'string' && d.triggerRef.trim()) {
          triggerChip = `<span class="dec-trigger external" title="Trigger">↳ ${escapeHTML(d.triggerRef)}</span>`;
        }
        // Rationale preview — 1-2 line snippet inline. Falls back to em-dash
        // when not provided so the row layout stays consistent.
        const rationaleSnip = d.rationale && d.rationale.length > 120
          ? d.rationale.slice(0, 118).replace(/\s+\S*$/, '') + '…'
          : (d.rationale || '');
        return `
        <div class="row dec-row dec-${statusCls}" data-decision-id="${d.id}">
          ${ROW_GRIP_HTML}
          <div class="dec-main">
            <div class="dec-head">
              <span class="dec-icon">⬡</span>
              <span class="dec-title">${escapeHTML(d.title)}</span>
              <span class="dec-status-pill status-${statusCls}" title="Decision status">${statusLbl}</span>
              ${compChip}
              ${triggerChip}
            </div>
            ${rationaleSnip ? `<div class="dec-rat">${escapeHTML(rationaleSnip)}</div>` : ''}
          </div>
          <span class="row-meta dec-meta">${escapeHTML(personName(d.owner))} • ${d.date || ''}</span>
        </div>`;
      }).join('');
      wireListReorder(list, {
        rowSelector: '.row[data-decision-id]',
        idAttr: 'decisionId',
        getArray: () => proj.decisions,
        setOrder: (ids) => { proj.decisions = ids.map((id) => proj.decisions.find((x) => x.id === id)).filter(Boolean); },
        commitName: 'decisions-reorder',
      });
      $$('.row[data-decision-id]', list).forEach((row) => {
        row.addEventListener('contextmenu', (e) => {
          e.preventDefault();
          const id = row.dataset.decisionId;
          const d = (proj.decisions || []).find((x) => x.id === id);
          if (!d) return;
          showContextMenu(e.clientX, e.clientY, [
            { icon: '✎', label: 'Edit…', onClick: () => openDecisionEditor(id) },
            { divider: true },
            { icon: '×', label: 'Delete decision', danger: true, onClick: () => {
              if (!confirm(`Delete "${d.title}"?`)) return;
              proj.decisions = (proj.decisions || []).filter((x) => x.id !== id);
              commit('decision-delete');
              toast('Deleted');
            }},
          ]);
        });
        row.addEventListener('dblclick', () => openDecisionEditor(row.dataset.decisionId));
      });
      // Trigger chip clicks — jump to the linked entity. stopPropagation
      // so the click doesn't bubble to the row (which would also try to
      // do something useful).
      $$('.dec-trigger[data-open-action]', list).forEach((el) =>
        el.addEventListener('click', (e) => { e.stopPropagation(); openDrawer(el.dataset.openAction); }));
      $$('.dec-trigger[data-open-risk]', list).forEach((el) =>
        el.addEventListener('click', (e) => { e.stopPropagation(); openRiskEditor(el.dataset.openRisk); }));
      $$('.dec-trigger[data-open-cr]', list).forEach((el) =>
        el.addEventListener('click', (e) => {
          e.stopPropagation();
          const c = (proj.changes || []).find((x) => x.id === el.dataset.openCr);
          if (c) openChangeRequestEditor(c);
        }));
    }
    $('#btnAddDec').addEventListener('click', () => openQuickAdd('decision'));
  }

  function openDecisionEditor(decisionId) {
    const proj = curProject();
    const d = (proj.decisions || []).find((x) => x.id === decisionId);
    if (!d) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">Edit decision</div>
          <button class="icon-btn" id="deClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Title</label><input id="deTitle" value="${escapeHTML(d.title)}" /></div>
          <div class="field"><label>Rationale</label><textarea id="deRat" style="min-height:90px;">${escapeHTML(d.rationale || '')}</textarea></div>
          <div class="qa-row">
            <div class="field"><label>Owner</label>
              <select id="deOwner" data-person-select>${personOptionsHtml(d.owner)}</select>
            </div>
            <div class="field"><label>Date</label><input id="deDate" type="date" value="${d.date || ''}" /></div>
          </div>
          <div class="field"><label>Component <span class="muted">(optional)</span></label>
            <select id="deComponent">
              <option value="">—</option>
              ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === d.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
            </select>
          </div>
          <div class="qa-row">
            <div class="field"><label>Status</label>
              <select id="deStatus">
                <option value="open"        ${d.status === 'open'        ? 'selected' : ''}>Open</option>
                <option value="in_effect"   ${(d.status || 'in_effect') === 'in_effect' ? 'selected' : ''}>In effect</option>
                <option value="superseded"  ${d.status === 'superseded'  ? 'selected' : ''}>Superseded</option>
              </select>
            </div>
            <div class="field"><label>Triggered by <span class="muted">(optional)</span></label>
              <select id="deTriggerType">
                <option value="">—</option>
                <option value="risk"     ${d.triggerRef?.type === 'risk'     ? 'selected' : ''}>Risk / opportunity</option>
                <option value="cr"       ${d.triggerRef?.type === 'cr'       ? 'selected' : ''}>Change request</option>
                <option value="action"   ${d.triggerRef?.type === 'action'   ? 'selected' : ''}>Action</option>
                <option value="external" ${typeof d.triggerRef === 'string'  ? 'selected' : ''}>External (free text)</option>
              </select>
            </div>
          </div>
          <div class="field" id="deTriggerWrap" style="${d.triggerRef ? '' : 'display:none;'}">
            <label>Trigger target</label>
            <select id="deTriggerRef" style="${typeof d.triggerRef === 'string' ? 'display:none;' : ''}">
              <option value="">—</option>
              ${(d.triggerRef?.type === 'risk' || !d.triggerRef ? (proj.risks || []) : []).map((r) => `<option value="${r.id}" ${d.triggerRef?.id === r.id ? 'selected' : ''}>${escapeHTML((r.identifier ? r.identifier + ' — ' : '') + r.title)}</option>`).join('')}
              ${(d.triggerRef?.type === 'cr' ? (proj.changes || []) : []).map((c) => `<option value="${c.id}" ${d.triggerRef?.id === c.id ? 'selected' : ''}>${escapeHTML(c.title)}</option>`).join('')}
              ${(d.triggerRef?.type === 'action' ? (proj.actions || []) : []).map((a) => `<option value="${a.id}" ${d.triggerRef?.id === a.id ? 'selected' : ''}>${escapeHTML(a.title)}</option>`).join('')}
            </select>
            <input type="text" id="deTriggerText" placeholder="e.g. customer request, board direction…" value="${typeof d.triggerRef === 'string' ? escapeHTML(d.triggerRef) : ''}" style="${typeof d.triggerRef === 'string' ? '' : 'display:none;'}" />
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="deCancel">Cancel</button>
          <button class="ghost" id="deDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="deSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('deTitle').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#deClose').addEventListener('click', close);
    overlay.querySelector('#deCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
    // Hook the inline person-create flow on the Owner dropdown so a new
    // person can be added without leaving the editor.
    overlay.querySelectorAll('select[data-person-select]').forEach(wirePersonSelectInline);
    // Trigger picker: switching type re-populates the target dropdown so
    // it always shows the right entity list (or swaps in a free-text input
    // for "external").
    const repaintTriggerOptions = () => {
      const tt = document.getElementById('deTriggerType').value;
      const wrap = document.getElementById('deTriggerWrap');
      const sel  = document.getElementById('deTriggerRef');
      const txt  = document.getElementById('deTriggerText');
      if (!tt) { wrap.style.display = 'none'; return; }
      wrap.style.display = '';
      if (tt === 'external') {
        sel.style.display = 'none'; txt.style.display = '';
        return;
      }
      txt.style.display = 'none'; sel.style.display = '';
      let opts = [];
      if (tt === 'risk')   opts = (proj.risks || []).map((r) => ({ id: r.id, label: (r.identifier ? r.identifier + ' — ' : '') + r.title }));
      if (tt === 'cr')     opts = (proj.changes || []).map((c) => ({ id: c.id, label: c.title }));
      if (tt === 'action') opts = (proj.actions || []).filter((a) => !a.archivedAt).map((a) => ({ id: a.id, label: a.title }));
      const cur = (d.triggerRef && typeof d.triggerRef === 'object') ? d.triggerRef.id : '';
      sel.innerHTML = '<option value="">—</option>' + opts.map((o) =>
        `<option value="${escapeHTML(o.id)}" ${o.id === cur ? 'selected' : ''}>${escapeHTML(o.label)}</option>`).join('');
    };
    document.getElementById('deTriggerType').addEventListener('change', repaintTriggerOptions);

    overlay.querySelector('#deSave').addEventListener('click', () => {
      d.title = document.getElementById('deTitle').value.trim() || d.title;
      d.rationale = document.getElementById('deRat').value;
      d.owner = document.getElementById('deOwner').value;
      d.date = document.getElementById('deDate').value || d.date;
      d.component = document.getElementById('deComponent').value || null;
      d.status = document.getElementById('deStatus').value || 'in_effect';
      const tt = document.getElementById('deTriggerType').value;
      if (!tt) {
        d.triggerRef = null;
      } else if (tt === 'external') {
        const txt = document.getElementById('deTriggerText').value.trim();
        d.triggerRef = txt || null;
      } else {
        const id = document.getElementById('deTriggerRef').value;
        d.triggerRef = id ? { type: tt, id } : null;
      }
      stampEdit(d, null, 'edit');
      commit('decision-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#deDelete').addEventListener('click', () => {
      if (!confirm(`Delete "${d.title}"?`)) return;
      proj.decisions = (proj.decisions || []).filter((x) => x.id !== d.id);
      commit('decision-delete');
      close();
      toast('Deleted');
    });
  }

  // Reusable contenteditable rich-text editor with an icon-style toolbar:
  // bold, italic, underline, strikethrough, font colour, bullet / numbered list,
  // link, and clear formatting. The element with `id` is the editable region;
  // the toolbar is wired up via `wireRichEditor(overlay, id)`.
  function richEditorHTML(id, valueHtml, placeholder) {
    return `
      <div class="op-context-wrap rich-wrap" data-rich-for="${id}">
        <div class="rich-toolbar">
          <button type="button" class="rt-btn rt-bold"   data-cmd="bold"          title="Bold (Cmd/Ctrl+B)">B</button>
          <button type="button" class="rt-btn rt-ital"   data-cmd="italic"        title="Italic (Cmd/Ctrl+I)">I</button>
          <button type="button" class="rt-btn rt-und"    data-cmd="underline"     title="Underline (Cmd/Ctrl+U)">U</button>
          <button type="button" class="rt-btn rt-strike" data-cmd="strikeThrough" title="Strikethrough">S</button>
          <span class="rt-sep"></span>
          <label class="rt-btn rt-color" title="Text colour">
            <span class="rt-color-glyph">A</span>
            <span class="rt-color-bar"></span>
            <input type="color" data-cmd="foreColor" value="#6ea8ff" />
          </label>
          <span class="rt-sep"></span>
          <button type="button" class="rt-btn rt-ul" data-cmd="insertUnorderedList" title="Bullet list">
            <svg viewBox="0 0 16 16" width="14" height="14"><circle cx="2" cy="3.5" r="1.2" fill="currentColor"/><circle cx="2" cy="8" r="1.2" fill="currentColor"/><circle cx="2" cy="12.5" r="1.2" fill="currentColor"/><line x1="6" y1="3.5" x2="14" y2="3.5" stroke="currentColor" stroke-width="1.4"/><line x1="6" y1="8" x2="14" y2="8" stroke="currentColor" stroke-width="1.4"/><line x1="6" y1="12.5" x2="14" y2="12.5" stroke="currentColor" stroke-width="1.4"/></svg>
          </button>
          <button type="button" class="rt-btn rt-ol" data-cmd="insertOrderedList" title="Numbered list">
            <svg viewBox="0 0 16 16" width="14" height="14"><text x="0" y="5" font-size="4.5" font-weight="700" fill="currentColor">1.</text><text x="0" y="10" font-size="4.5" font-weight="700" fill="currentColor">2.</text><text x="0" y="15" font-size="4.5" font-weight="700" fill="currentColor">3.</text><line x1="6" y1="3.5" x2="14" y2="3.5" stroke="currentColor" stroke-width="1.4"/><line x1="6" y1="8" x2="14" y2="8" stroke="currentColor" stroke-width="1.4"/><line x1="6" y1="12.5" x2="14" y2="12.5" stroke="currentColor" stroke-width="1.4"/></svg>
          </button>
          <span class="rt-sep"></span>
          <button type="button" class="rt-btn rt-link" data-cmd="createLink" title="Insert link">
            <svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"><path d="M6.5 9.5l3-3"/><path d="M9 4.5a2.5 2.5 0 0 1 3.5 3.5l-2 2"/><path d="M7 11.5a2.5 2.5 0 0 1-3.5-3.5l2-2"/></svg>
          </button>
          <button type="button" class="rt-btn rt-clear" data-cmd="removeFormat" title="Clear formatting">
            <svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"><path d="M3 13l3-9h4l-3 9z"/><line x1="2" y1="13" x2="14" y2="13"/><line x1="11" y1="3" x2="14" y2="6"/><line x1="14" y1="3" x2="11" y2="6"/></svg>
          </button>
        </div>
        <div class="op-context rich-body" id="${id}" contenteditable="true" data-placeholder="${escapeHTML(placeholder || '')}">${valueHtml || ''}</div>
      </div>`;
  }
  function wireRichEditor(overlay, id) {
    const wrap = overlay.querySelector(`[data-rich-for="${id}"]`);
    if (!wrap) return;
    const body = overlay.querySelector(`#${id}`);
    wrap.querySelectorAll('.rt-btn[data-cmd]').forEach((btn) => {
      btn.addEventListener('mousedown', (e) => {
        e.preventDefault();
        const cmd = btn.dataset.cmd;
        body.focus();
        if (cmd === 'createLink') {
          const url = prompt('Link URL (https://…):');
          if (url) document.execCommand('createLink', false, url);
        } else if (cmd === 'foreColor') {
          // handled via colour input change below
        } else {
          document.execCommand(cmd, false, null);
        }
      });
    });
    const color = wrap.querySelector('input[type="color"]');
    const colorBar = wrap.querySelector('.rt-color-bar');
    if (color) {
      color.addEventListener('input', (e) => {
        body.focus();
        document.execCommand('foreColor', false, e.target.value);
        if (colorBar) colorBar.style.background = e.target.value;
      });
      if (colorBar) colorBar.style.background = color.value;
    }
  }

  // Change requests — track scope/schedule/cost change proposals through their lifecycle.
  const crFilterState = { status: 'all' };
  function renderChangeRequests(root) {
    const proj = curProject();
    proj.changes = proj.changes || [];
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Change requests</div>
          <div class="page-sub">Track proposed changes through proposed → reviewed → approved / rejected → implemented. Each CR captures impact (schedule, cost, scope, risk) and an optional online or local link.</div>
        </div>
        <div class="page-actions">
          <div class="seg" role="tablist" aria-label="Status filter">
            <button class="seg-btn ${crFilterState.status === 'all' ? 'active' : ''}" data-cr-filter="all">All</button>
            ${CR_STATUSES.map((s) => `<button class="seg-btn ${crFilterState.status === s.id ? 'active' : ''}" data-cr-filter="${s.id}">${s.label}</button>`).join('')}
          </div>
          <button class="ghost" id="btnAddCR">+ Change request</button>
        </div>
      </div>
      <div class="cr-kpis" id="crKpis"></div>
      <div class="cr-list" id="crList"></div>`;
    root.appendChild(view);

    function drawKpis() {
      const all = proj.changes || [];
      const kpis = $('#crKpis');
      if (!kpis) return;
      if (!all.length) { kpis.innerHTML = ''; return; }
      // Status mix
      const counts = {};
      CR_STATUSES.forEach((s) => { counts[s.id] = 0; });
      all.forEach((c) => { counts[c.status] = (counts[c.status] || 0) + 1; });
      const total = all.length;
      const open = (counts.proposed || 0) + (counts.under_review || 0);
      const decided = (counts.approved || 0) + (counts.rejected || 0) + (counts.implemented || 0) + (counts.cancelled || 0);
      const approvedLike = (counts.approved || 0) + (counts.implemented || 0);
      const approvalRate = decided > 0 ? Math.round((approvedLike / decided) * 100) : 0;
      // Sum schedule + cost impact for approved + implemented (those that "land")
      let schedSum = 0, costSum = 0;
      all.forEach((c) => {
        if (c.status === 'approved' || c.status === 'implemented') {
          schedSum += (c.impact?.schedule || 0);
          costSum  += (c.impact?.cost || 0);
        }
      });
      // Donut SVG — cumulative arcs by status (each arc is clickable to filter)
      const R = 32, CX = 36, CY = 36, IR = 22;
      const C = 2 * Math.PI * R;
      let cum = 0;
      const activeFilter = crFilterState.status;
      const arcs = CR_STATUSES.map((s) => {
        const n = counts[s.id] || 0;
        if (n === 0) return '';
        const frac = n / total;
        const dash = frac * C;
        const gap = C - dash;
        const off = -cum * C + C / 4; // start at 12 o'clock
        cum += frac;
        const dimmed = activeFilter !== 'all' && activeFilter !== s.id;
        const isActive = activeFilter === s.id;
        return `<circle class="cr-donut-slice ${isActive ? 'active' : ''} ${dimmed ? 'dimmed' : ''}" data-cr-status="${s.id}" cx="${CX}" cy="${CY}" r="${R}" fill="none" stroke="rgb(${s.rgb})" stroke-width="${R - IR}" stroke-dasharray="${dash.toFixed(2)} ${gap.toFixed(2)}" stroke-dashoffset="${off.toFixed(2)}" transform="rotate(-90 ${CX} ${CY})"><title>${s.label}: ${n} — click to filter</title></circle>`;
      }).join('');
      const fmtSchedKpi = (n) => n === 0 ? '0 d' : `${n > 0 ? '+' : ''}${n} d`;
      const fmtCostKpi  = (n) => {
        if (n === 0) return '0 €';
        const sign = n > 0 ? '+' : '−';
        const abs = Math.abs(n);
        if (abs >= 1000) return `${sign}${(abs/1000).toFixed(1)}k €`;
        return `${sign}${abs} €`;
      };
      kpis.innerHTML = `
        <div class="cr-kpi cr-kpi-total">
          <div class="cr-kpi-label">Change requests</div>
          <div class="cr-kpi-num">${total}</div>
          <div class="cr-kpi-sub"><span class="cr-kpi-pip" style="background:rgb(${crStatus('proposed').rgb})"></span>${open} open · ${decided} decided</div>
        </div>
        <div class="cr-kpi cr-kpi-mix">
          <div class="cr-kpi-donut">
            <svg viewBox="0 0 72 72" width="72" height="72" aria-hidden="true">
              <circle cx="${CX}" cy="${CY}" r="${R}" fill="none" stroke="var(--bg-3)" stroke-width="${R - IR}" />
              ${arcs}
              <text x="${CX}" y="${CY + 4}" text-anchor="middle" font-size="14" font-weight="700" fill="var(--text)">${total}</text>
            </svg>
          </div>
          <div class="cr-kpi-mix-list">
            ${CR_STATUSES.map((s) => {
              const isActive = activeFilter === s.id;
              const dimmed = activeFilter !== 'all' && !isActive;
              return `
                <div class="cr-kpi-mix-row clickable ${isActive ? 'active' : ''} ${dimmed ? 'dimmed' : ''}" data-cr-status="${s.id}" title="Click to filter">
                  <span class="cr-kpi-pip" style="background:rgb(${s.rgb})"></span>
                  <span class="cr-kpi-mix-lbl">${s.label}</span>
                  <span class="cr-kpi-mix-n">${counts[s.id] || 0}</span>
                </div>`;
            }).join('')}
          </div>
        </div>
        <div class="cr-kpi">
          <div class="cr-kpi-label">Schedule impact</div>
          <div class="cr-kpi-num ${schedSum > 0 ? 'bad' : schedSum < 0 ? 'ok' : ''}">${fmtSchedKpi(schedSum)}</div>
          <div class="cr-kpi-sub">cumulative · approved + implemented</div>
        </div>
        <div class="cr-kpi">
          <div class="cr-kpi-label">Cost impact</div>
          <div class="cr-kpi-num ${costSum > 0 ? 'bad' : costSum < 0 ? 'ok' : ''}">${fmtCostKpi(costSum)}</div>
          <div class="cr-kpi-sub">cumulative · approved + implemented</div>
        </div>
        <div class="cr-kpi">
          <div class="cr-kpi-label">Approval rate</div>
          <div class="cr-kpi-num ${approvalRate >= 60 ? 'ok' : approvalRate < 30 ? 'bad' : ''}">${decided ? approvalRate + '%' : '—'}</div>
          <div class="cr-kpi-sub">${approvedLike} of ${decided || 0} decided</div>
          <div class="cr-kpi-bar"><div class="cr-kpi-bar-fill ${approvalRate >= 60 ? 'ok' : approvalRate < 30 ? 'bad' : 'warn'}" style="width:${decided ? approvalRate : 0}%"></div></div>
        </div>`;

      // Wire click-to-filter on donut arcs and on the legend rows. Clicking the
      // currently-active filter clears it back to "all" (toggle behavior).
      const setFilter = (st) => {
        const next = (crFilterState.status === st) ? 'all' : st;
        crFilterState.status = next;
        // Sync the segmented filter buttons in the page-head
        $$('.seg-btn[data-cr-filter]', view).forEach((x) =>
          x.classList.toggle('active', x.dataset.crFilter === next));
        draw();
      };
      kpis.querySelectorAll('.cr-donut-slice[data-cr-status]').forEach((el) => {
        el.addEventListener('click', () => setFilter(el.dataset.crStatus));
      });
      kpis.querySelectorAll('.cr-kpi-mix-row[data-cr-status]').forEach((el) => {
        el.addEventListener('click', () => setFilter(el.dataset.crStatus));
      });
    }

    function draw() {
      drawKpis();
      const list = $('#crList');
      const items = (proj.changes || [])
        .filter((c) => crFilterState.status === 'all' || c.status === crFilterState.status)
        .filter((c) => matchesSearch(c.title, c.rationale, c.description, c.analysis, personName(c.originator), personName(c.decisionBy)));
      if (!items.length) {
        list.innerHTML = `<div class="empty">${
          searchQuery()
            ? 'No change requests match the current search.'
            : (crFilterState.status === 'all'
                ? 'No change requests yet — capture one with + Change request.'
                : 'No change requests in this status.')}</div>`;
        return;
      }
      // Sort by originated date desc (most recent first)
      const sorted = items.slice().sort((a, b) => (b.originatedDate || '').localeCompare(a.originatedDate || ''));
      list.innerHTML = sorted.map((c) => {
        const s = crStatus(c.status);
        const cmp = c.component ? findComponent(proj, c.component) : null;
        const cc = cmp ? componentColor(cmp.color) : null;
        const sched = (c.impact?.schedule ?? 0);
        const cost  = (c.impact?.cost ?? 0);
        const schedTxt = sched ? `${sched > 0 ? '+' : ''}${sched} d` : '—';
        const costTxt  = cost  ? `${cost > 0 ? '+' : ''}${cost} €` : '—';
        const linkUrl = c.linkUrl || '';
        const linkLbl = linkUrl ? (() => { try { return new URL(linkUrl).hostname.replace(/^www\./, ''); } catch (e) { return 'link'; } })() : '';
        const decisionInfo = (c.status === 'approved' || c.status === 'rejected' || c.status === 'implemented' || c.status === 'cancelled')
          ? `<span class="cr-decision">${c.status === 'rejected' || c.status === 'cancelled' ? '✗' : '✓'} ${escapeHTML(personName(c.decisionBy))}${c.decisionDate ? ' · ' + fmtDate(c.decisionDate) : ''}</span>` : '';
        // Rationale supports rich HTML; legacy plain entries render as text
        const rationaleHtml = c.rationale && /<\w+/.test(c.rationale) ? c.rationale : escapeHTML(c.rationale || '');
        const analysisHtml  = c.analysis  && /<\w+/.test(c.analysis)  ? c.analysis  : escapeHTML(c.analysis  || '');
        const prio = priorityLevel(c.priorityLevel);
        const showCrPrioChip = c.priorityLevel && c.priorityLevel !== 'med';
        return `
          <div class="cr-card" data-cr-id="${c.id}">
            <div class="cr-row1">
              <span class="cr-status" style="background:rgba(${s.rgb},.18);color:rgb(${s.rgb});border:1px solid rgb(${s.rgb})">${s.label}</span>
              ${showCrPrioChip ? `<span class="prio-chip prio-${prio.id}" title="Priority: ${prio.label}" style="background:rgba(${prio.rgb},.18);color:rgb(${prio.rgb});border:1px solid rgb(${prio.rgb})">${prio.label}</span>` : ''}
              <span class="cr-title">${escapeHTML(c.title)}</span>
              ${cmp ? `<span class="component-chip" style="background:rgba(${cc.rgb},.2);color:rgb(${cc.rgb})">${escapeHTML(cmp.name)}</span>` : ''}
            </div>
            ${c.rationale ? `<div class="cr-rationale"><span class="cr-section-lbl">Rationale</span><div class="cr-rich">${rationaleHtml}</div></div>` : ''}
            ${c.analysis  ? `<div class="cr-analysis"><span class="cr-section-lbl">Analysis</span><div class="cr-rich">${analysisHtml}</div></div>` : ''}
            <div class="cr-meta">
              <span class="cr-meta-item" title="Originator">⚐ ${escapeHTML(personName(c.originator))}</span>
              <span class="cr-meta-item" title="Originated">▦ ${c.originatedDate ? fmtFull(c.originatedDate) : '—'}</span>
              <span class="cr-meta-item ${sched ? (sched > 0 ? 'bad' : 'ok') : ''}" title="Schedule impact">⏱ ${schedTxt}</span>
              <span class="cr-meta-item ${cost ? (cost > 0 ? 'bad' : 'ok') : ''}" title="Cost impact">€ ${costTxt}</span>
              ${decisionInfo}
              ${linkUrl ? `<a class="cr-link" href="${escapeHTML(linkUrl)}" target="_blank" rel="noopener noreferrer" title="${escapeHTML(linkUrl)}">↗ ${escapeHTML(linkLbl || 'link')}</a>` : ''}
            </div>
          </div>`;
      }).join('');
      $$('.cr-card[data-cr-id]', list).forEach((card) => {
        card.addEventListener('contextmenu', (e) => {
          e.preventDefault();
          const id = card.dataset.crId;
          const c = (proj.changes || []).find((x) => x.id === id);
          if (!c) return;
          const items = [
            { icon: '✎', label: 'Edit…', onClick: () => openChangeRequestEditor(id) },
          ];
          // Status quick-set submenu items
          CR_STATUSES.forEach((s) => {
            if (s.id !== c.status) {
              items.push({
                icon: '◐',
                label: `Mark ${s.label.toLowerCase()}`,
                onClick: () => {
                  const cr = (proj.changes || []).find((x) => x.id === id);
                  if (!cr) return;
                  cr.status = s.id;
                  if (s.id === 'approved' || s.id === 'rejected' || s.id === 'implemented' || s.id === 'cancelled') {
                    cr.decisionDate = todayISO();
                  }
                  commit('cr-status');
                  toast(`Marked ${s.label.toLowerCase()}`);
                },
              });
            }
          });
          if (c.linkUrl) {
            items.push({ icon: '⧉', label: 'Copy link', onClick: () => {
              navigator.clipboard?.writeText(c.linkUrl).then(() => toast('Copied')).catch(() => toast('Copy failed'));
            }});
          }
          items.push({ divider: true });
          items.push({ icon: '×', label: 'Delete change request', danger: true, onClick: () => {
            if (!confirm(`Delete change request "${c.title}"?`)) return;
            proj.changes = (proj.changes || []).filter((x) => x.id !== id);
            commit('cr-delete');
            toast('Deleted');
          }});
          showContextMenu(e.clientX, e.clientY, items);
        });
        card.addEventListener('dblclick', (e) => {
          if (e.target.closest('a')) return;
          openChangeRequestEditor(card.dataset.crId);
        });
      });
    }

    $$('.seg-btn[data-cr-filter]', view).forEach((b) => {
      b.addEventListener('click', () => {
        crFilterState.status = b.dataset.crFilter;
        $$('.seg-btn[data-cr-filter]', view).forEach((x) =>
          x.classList.toggle('active', x.dataset.crFilter === crFilterState.status));
        draw();
      });
    });
    $('#btnAddCR').addEventListener('click', () => {
      if (curProjectIsMerged()) { toast('Pick a single project to add items.'); return; }
      openChangeRequestEditor(newChangeRequestDraft());
    });
    draw();
  }

  function newChangeRequestDraft() {
    return {
      id: uid('cr'),
      title: '',
      rationale: '',
      analysis: '',
      description: '',
      status: 'proposed',
      originator: null,
      originatedDate: todayISO(),
      decisionBy: null,
      decisionDate: null,
      impact: { schedule: 0, cost: 0, scope: '', risk: '' },
      component: null,
      linkUrl: null,
      priorityLevel: 'med',
    };
  }

  function openChangeRequestEditor(crIdOrDraft) {
    const proj = curProject();
    const isDraft = crIdOrDraft && typeof crIdOrDraft === 'object';
    const c = isDraft
      ? crIdOrDraft
      : (proj.changes || []).find((x) => x.id === crIdOrDraft);
    if (!c) return;
    c.impact = c.impact || { schedule: 0, cost: 0, scope: '', risk: '' };
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:640px;">
        <div class="desc-head">
          <div class="desc-title">${isDraft ? 'New change request' : 'Change request — ' + escapeHTML(c.title)}</div>
          <button class="icon-btn" id="crClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px; max-height:72vh; overflow-y:auto;">
          <div class="field"><label>Title</label><input id="crTitle" value="${escapeHTML(c.title)}" /></div>
          <div class="qa-row">
            <div class="field"><label>Status</label>
              <select id="crStatus">
                ${CR_STATUSES.map((s) => `<option value="${s.id}" ${s.id === c.status ? 'selected' : ''}>${s.label}</option>`).join('')}
              </select>
            </div>
            <div class="field"><label>Priority</label>
              <select id="crPriorityLevel">
                ${PRIORITY_LEVELS.map((p) => `<option value="${p.id}" ${p.id === (c.priorityLevel || 'med') ? 'selected' : ''}>${p.label}</option>`).join('')}
              </select>
            </div>
            <div class="field"><label>Originator</label>
              <select id="crOriginator">
                <option value="">—</option>
                ${actorOptionsHTML(c.originator)}
              </select>
            </div>
            <div class="field"><label>Originated</label><input id="crOrigDate" type="date" value="${c.originatedDate || ''}" /></div>
          </div>
          <div class="field"><label>Rationale (rich)</label>
            ${richEditorHTML('crRationale', c.rationale || '', 'Why this change is needed — bold, lists, links, colour…')}
          </div>
          <div class="field"><label>Analysis (rich)</label>
            ${richEditorHTML('crAnalysis', c.analysis || '', 'Trade-off analysis, options considered, recommendation…')}
          </div>
          <div class="field"><label>Description (rich)</label>
            ${richEditorHTML('crDesc', c.description || '', 'Detailed description…')}
          </div>
          <div class="qa-row">
            <div class="field"><label>Schedule impact (days)</label><input id="crSched" type="number" value="${c.impact.schedule || 0}" /></div>
            <div class="field"><label>Cost impact (€)</label><input id="crCost" type="number" value="${c.impact.cost || 0}" /></div>
          </div>
          <div class="field"><label>Scope impact</label><textarea id="crScope" placeholder="What scope changes (added/removed/modified)" style="min-height:50px;">${escapeHTML(c.impact.scope || '')}</textarea></div>
          <div class="field"><label>Risk impact</label><textarea id="crRisk" placeholder="New risks introduced or mitigated" style="min-height:50px;">${escapeHTML(c.impact.risk || '')}</textarea></div>
          <div class="field"><label>Link (URL or path)</label><input id="crLinkUrl" value="${escapeHTML(c.linkUrl || '')}" placeholder="https://… or file:///…" /></div>
          <div class="qa-row">
            <div class="field"><label>Component (optional)</label>
              <select id="crComp">
                <option value="">—</option>
                ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === c.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
              </select>
            </div>
            <div class="field"><label>Decision by</label>
              <select id="crDecisionBy">
                <option value="">—</option>
                ${actorOptionsHTML(c.decisionBy)}
              </select>
            </div>
            <div class="field"><label>Decision date</label><input id="crDecisionDate" type="date" value="${c.decisionDate || ''}" /></div>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="crCancel">Cancel</button>
          ${isDraft ? '' : '<button class="ghost" id="crDelete" style="margin-left:auto; color:var(--bad);">Delete</button>'}
          <button class="primary" id="crSave">${isDraft ? 'Create' : 'Save'}</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('crTitle').focus(), 30);
    // Wire each rich-text editor's toolbar
    wireRichEditor(overlay, 'crRationale');
    wireRichEditor(overlay, 'crAnalysis');
    wireRichEditor(overlay, 'crDesc');
    const close = () => overlay.remove();
    overlay.querySelector('#crClose').addEventListener('click', close);
    overlay.querySelector('#crCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
    overlay.querySelector('#crSave').addEventListener('click', () => {
      const title = document.getElementById('crTitle').value.trim();
      if (!title) return toast('Title required');
      c.title = title;
      const newStatus = document.getElementById('crStatus').value;
      const decisional = (s) => s === 'approved' || s === 'rejected' || s === 'implemented' || s === 'cancelled';
      const wasDecisional = decisional(c.status);
      c.status = newStatus;
      c.originator = document.getElementById('crOriginator').value || null;
      c.originatedDate = document.getElementById('crOrigDate').value || c.originatedDate || todayISO();
      c.rationale = document.getElementById('crRationale').innerHTML;
      c.analysis  = document.getElementById('crAnalysis').innerHTML;
      c.description = document.getElementById('crDesc').innerHTML;
      c.impact = {
        schedule: parseFloat(document.getElementById('crSched').value) || 0,
        cost:     parseFloat(document.getElementById('crCost').value)  || 0,
        scope:    document.getElementById('crScope').value,
        risk:     document.getElementById('crRisk').value,
      };
      c.linkUrl   = document.getElementById('crLinkUrl').value.trim() || null;
      c.priorityLevel = document.getElementById('crPriorityLevel')?.value || c.priorityLevel || 'med';
      c.component = document.getElementById('crComp').value || null;
      c.decisionBy = document.getElementById('crDecisionBy').value || null;
      const dDate = document.getElementById('crDecisionDate').value;
      // Auto-set decision date when entering a decisional status without an explicit date
      if (decisional(newStatus) && !wasDecisional && !dDate) {
        c.decisionDate = todayISO();
      } else {
        c.decisionDate = dDate || c.decisionDate || null;
      }
      if (isDraft) {
        // Hide list filter from masking the new item if needed
        if (crFilterState.status !== 'all' && crFilterState.status !== c.status) crFilterState.status = 'all';
        proj.changes = proj.changes || [];
        proj.changes.push(c);
        commit('cr-create');
        close();
        toast('Created');
      } else {
        commit('cr-edit');
        close();
        toast('Saved');
      }
    });
    if (!isDraft) {
      overlay.querySelector('#crDelete').addEventListener('click', () => {
        if (!confirm(`Delete change request "${c.title}"?`)) return;
        proj.changes = (proj.changes || []).filter((x) => x.id !== c.id);
        commit('cr-delete');
        close();
        toast('Deleted');
      });
    }
  }

  function renderLinks(root) {
    const proj = curProject();
    const isMerged = curProjectIsMerged();
    proj.links = proj.links || [];
    proj.linkFolders = proj.linkFolders || [];
    const view = document.createElement('div');
    view.className = 'view';
    const sub = isMerged
      ? 'All links across every project (read-only — switch to a single project to add or rearrange).'
      : 'Important references — websites, drives, files. Group into folders by drag & drop; reorder anything by its grip.';
    view.innerHTML = `
      <div class="page-head">
        <div><div class="page-title">Links</div><div class="page-sub">${escapeHTML(sub)}</div></div>
        <div class="page-actions">
          ${isMerged ? '' : '<button class="ghost" id="btnNewLinkFolder">+ Folder</button>'}
          ${isMerged ? '' : '<button class="ghost" id="btnAddLink">+ Link</button>'}
        </div>
      </div>
      <div class="link-tree ${isMerged ? 'read-only' : ''}" id="linkTree"></div>`;
    root.appendChild(view);
    const tree = $('#linkTree');

    function linksFor(folderId) {
      return proj.links
        .filter((l) => (l.folderId || null) === folderId)
        .filter((l) => matchesSearch(l.title, l.url, l.description));
    }

    function linkCardHTML(l) {
      const comp = l.component ? findComponent(proj, l.component) : null;
      const compRgb = comp ? componentColor(comp.color).rgb : null;
      const host = (() => { try { return new URL(l.url).hostname.replace(/^www\./, ''); } catch (e) { return l.url; } })();
      return `
        <div class="link-card" data-link-id="${l.id}">
          ${isMerged ? '' : '<span class="row-grip link-grip" title="Drag to reorder or move into a folder" aria-hidden="true">⋮⋮</span>'}
          <a class="link-title" href="${escapeHTML(l.url)}" target="_blank" rel="noopener noreferrer">↗ ${escapeHTML(l.title || host || l.url)}</a>
          <div class="link-host">${escapeHTML(host)}</div>
          ${l.description ? `<div class="link-desc">${escapeHTML(l.description)}</div>` : ''}
          ${comp ? `<div class="link-comp"><span class="link-comp-swatch" style="background:rgb(${compRgb})"></span>${escapeHTML(comp.name)}</div>` : ''}
        </div>`;
    }

    function folderHTML(folder) {
      const items = linksFor(folder.id);
      const collapsed = !!folder.collapsed;
      const emptyMsg = isMerged ? 'No links in this folder.' : 'Empty — drag a link here.';
      return `
        <div class="link-folder ${collapsed ? 'collapsed' : ''}" data-folder-id="${folder.id}">
          <div class="folder-head">
            ${isMerged ? '<span class="folder-caret-spacer" aria-hidden="true"></span>' : '<span class="row-grip folder-grip" title="Drag to reorder folders" aria-hidden="true">⋮⋮</span>'}
            <button type="button" class="folder-caret" title="Toggle">${collapsed ? '▸' : '▾'}</button>
            <span class="folder-name" ${isMerged ? '' : 'contenteditable="true"'} data-placeholder="Folder name…">${escapeHTML(folder.name)}</span>
            <span class="folder-count">${items.length}</span>
            ${isMerged ? '' : '<button type="button" class="folder-add" title="Add link to this folder">+ Link</button>'}
            ${isMerged ? '' : '<button type="button" class="folder-del" title="Delete folder">×</button>'}
          </div>
          <div class="folder-body" data-folder-id="${folder.id}">
            ${items.length ? items.map(linkCardHTML).join('') : `<div class="folder-empty">${emptyMsg}</div>`}
          </div>
        </div>`;
    }

    function ungroupedHTML() {
      const items = linksFor(null);
      return `
        <div class="link-folder loose" data-folder-id="">
          <div class="folder-head loose-head">
            <span class="folder-caret-spacer" aria-hidden="true"></span>
            <span class="folder-name loose-name">Loose links</span>
            <span class="folder-count">${items.length}</span>
          </div>
          <div class="folder-body" data-folder-id="">
            ${items.length ? items.map(linkCardHTML).join('') : '<div class="folder-empty">No loose links — drag any link here to ungroup it.</div>'}
          </div>
        </div>`;
    }

    function drawTree() {
      const total = proj.links.length;
      if (!total && !proj.linkFolders.length) {
        tree.innerHTML = '<div class="empty">No links yet — add one or create a folder to start.</div>';
        return;
      }
      // Folders first, loose links at the bottom — keeps named groups
      // (typically the curated content) visually prioritised, with the
      // unfiled overflow at the end of the page.
      tree.innerHTML = proj.linkFolders.map(folderHTML).join('') + ungroupedHTML();
      wireTree();
    }

    function wireTree() {
      // Folder header interactions
      $$('.link-folder', tree).forEach((folderEl) => {
        const fid = folderEl.dataset.folderId;
        if (fid) {
          // Caret toggle (works in merged mode too — local UI state)
          folderEl.querySelector('.folder-caret')?.addEventListener('click', () => {
            const f = proj.linkFolders.find((x) => x.id === fid);
            if (!f) return;
            f.collapsed = !f.collapsed;
            saveState();
            drawTree();
          });
          if (isMerged) return; // skip rename / delete / drag in merged mode
          // Edit name on blur
          const nameEl = folderEl.querySelector('.folder-name');
          if (nameEl) {
            nameEl.addEventListener('keydown', (e) => {
              if (e.key === 'Enter') { e.preventDefault(); nameEl.blur(); }
            });
            nameEl.addEventListener('blur', () => {
              const f = proj.linkFolders.find((x) => x.id === fid);
              if (!f) return;
              const v = nameEl.textContent.trim() || 'Folder';
              if (f.name !== v) { f.name = v; commit('folder-rename'); }
            });
          }
          // Add link to this folder
          folderEl.querySelector('.folder-add')?.addEventListener('click', () => {
            openQuickAdd('link', { folderId: fid });
          });
          // Delete folder (links inside become loose)
          folderEl.querySelector('.folder-del')?.addEventListener('click', () => {
            const f = proj.linkFolders.find((x) => x.id === fid);
            if (!f) return;
            const childCount = linksFor(fid).length;
            if (!confirm(`Delete folder "${f.name}"?` + (childCount ? `\n\nThe ${childCount} link${childCount === 1 ? '' : 's'} inside will become loose.` : ''))) return;
            proj.links.forEach((l) => { if (l.folderId === fid) l.folderId = null; });
            proj.linkFolders = proj.linkFolders.filter((x) => x.id !== fid);
            commit('folder-delete');
            toast('Folder deleted');
          });
          // Folder reorder drag (via folder-grip)
          const fgrip = folderEl.querySelector('.folder-grip');
          if (fgrip) wireFolderDrag(fgrip, folderEl);
        }
      });
      // Link cards
      $$('.link-card[data-link-id]', tree).forEach((card) => {
        const lid = card.dataset.linkId;
        // Context menu
        card.addEventListener('contextmenu', (e) => {
          e.preventDefault();
          const l = proj.links.find((x) => x.id === lid);
          if (!l) return;
          if (isMerged) {
            // In All Projects view, only allow read-only actions
            showContextMenu(e.clientX, e.clientY, [
              { icon: '⧉', label: 'Copy URL', onClick: () => {
                navigator.clipboard?.writeText(l.url).then(() => toast('Copied')).catch(() => toast('Copy failed'));
              }},
            ]);
            return;
          }
          const moveItems = [];
          if (l.folderId) moveItems.push({ icon: '↩', label: 'Move out (loose)', onClick: () => {
            l.folderId = null; commit('link-move'); toast('Moved out');
          }});
          proj.linkFolders.forEach((f) => {
            if (f.id !== l.folderId) moveItems.push({
              icon: '↪', label: `Move to "${f.name}"`,
              onClick: () => { l.folderId = f.id; commit('link-move'); toast(`Moved to ${f.name}`); },
            });
          });
          showContextMenu(e.clientX, e.clientY, [
            { icon: '✎', label: 'Edit…', onClick: () => openLinkEditor(lid) },
            { icon: '⧉', label: 'Copy URL', onClick: () => {
              navigator.clipboard?.writeText(l.url).then(() => toast('Copied')).catch(() => toast('Copy failed'));
            }},
            ...(moveItems.length ? [{ divider: true }, ...moveItems] : []),
            { divider: true },
            { icon: '×', label: 'Delete link', danger: true, onClick: () => {
              if (!confirm(`Delete "${l.title || l.url}"?`)) return;
              proj.links = proj.links.filter((x) => x.id !== lid);
              commit('link-delete');
              toast('Deleted');
            }},
          ]);
        });
        if (!isMerged) {
          card.addEventListener('dblclick', (e) => {
            if (e.target.closest('a')) return;
            openLinkEditor(lid);
          });
          // Drag to reorder / move between folders
          const grip = card.querySelector('.link-grip');
          if (grip) wireLinkDrag(grip, card);
        }
      });
    }

    // ----------- DRAG: link cards (reorder within + across folders) -----------
    function wireLinkDrag(grip, card) {
      grip.addEventListener('mousedown', (e) => {
        e.preventDefault(); e.stopPropagation();
        const lid = card.dataset.linkId;
        card.classList.add('dragging');
        document.body.classList.add('is-link-dragging');
        let dropTargetFolderId = null;
        let dropAfterLinkId = null; // null means "append to end of target folder"

        const clearIndicators = () => {
          tree.querySelectorAll('.link-folder.drop-target, .link-card.drop-before, .link-card.drop-after, .folder-empty.drop-target')
            .forEach((el) => el.classList.remove('drop-target', 'drop-before', 'drop-after'));
        };

        const onMove = (em) => {
          em.preventDefault();
          clearIndicators();
          // Find what's under the cursor
          const elAt = document.elementFromPoint(em.clientX, em.clientY);
          if (!elAt) return;
          // Drop on another link card → reorder before/after
          const overCard = elAt.closest('.link-card[data-link-id]');
          if (overCard && overCard !== card) {
            const r = overCard.getBoundingClientRect();
            const above = em.clientY < r.top + r.height / 2;
            overCard.classList.add(above ? 'drop-before' : 'drop-after');
            const folderEl = overCard.closest('.link-folder');
            dropTargetFolderId = folderEl ? (folderEl.dataset.folderId || null) : null;
            const overId = overCard.dataset.linkId;
            // dropAfterLinkId is the link directly preceding the new position
            // For "above": position is before overCard → dropAfterLinkId = link before overCard in same folder, or null if overCard is first
            const sameFolderLinks = proj.links.filter((l) => (l.folderId || null) === dropTargetFolderId);
            const overIdx = sameFolderLinks.findIndex((l) => l.id === overId);
            if (above) {
              dropAfterLinkId = overIdx > 0 ? sameFolderLinks[overIdx - 1].id : '__START__';
              if (dropAfterLinkId === lid) dropAfterLinkId = '__SELF_NOOP__';
            } else {
              dropAfterLinkId = overId === lid ? '__SELF_NOOP__' : overId;
            }
            return;
          }
          // Drop on a folder body / header / empty area
          const folderEl = elAt.closest('.link-folder');
          if (folderEl) {
            folderEl.classList.add('drop-target');
            const emptyEl = folderEl.querySelector('.folder-empty');
            if (emptyEl) emptyEl.classList.add('drop-target');
            dropTargetFolderId = folderEl.dataset.folderId || null;
            dropAfterLinkId = null; // append to end
          }
        };

        const onUp = () => {
          card.classList.remove('dragging');
          document.body.classList.remove('is-link-dragging');
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          document.removeEventListener('selectstart', onSelectStart);
          try { window.getSelection()?.removeAllRanges(); } catch (_) {}
          clearIndicators();

          if (dropAfterLinkId === '__SELF_NOOP__') return;

          const link = proj.links.find((l) => l.id === lid);
          if (!link) return;
          const targetFolderId = dropTargetFolderId; // null means loose

          // Update folder membership
          link.folderId = targetFolderId;
          // Reorder proj.links: remove the link, then insert at the right place
          const without = proj.links.filter((l) => l.id !== lid);
          let newIdx;
          if (dropAfterLinkId == null) {
            // Append to the end of the target folder's slice
            // Find last index of any link in target folder
            let lastIdx = -1;
            without.forEach((l, i) => { if ((l.folderId || null) === targetFolderId) lastIdx = i; });
            newIdx = lastIdx + 1;
          } else if (dropAfterLinkId === '__START__') {
            // Insert before the first link of the target folder
            const firstIdx = without.findIndex((l) => (l.folderId || null) === targetFolderId);
            newIdx = firstIdx === -1 ? without.length : firstIdx;
          } else {
            const afterIdx = without.findIndex((l) => l.id === dropAfterLinkId);
            newIdx = afterIdx === -1 ? without.length : afterIdx + 1;
          }
          without.splice(newIdx, 0, link);
          proj.links = without;
          commit('link-reorder');
        };
        const onSelectStart = (ev) => ev.preventDefault();
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
        document.addEventListener('selectstart', onSelectStart);
      });
    }

    // ----------- DRAG: folder reorder (folders only, not into other folders) -----------
    function wireFolderDrag(grip, folderEl) {
      grip.addEventListener('mousedown', (e) => {
        e.preventDefault(); e.stopPropagation();
        folderEl.classList.add('dragging');
        document.body.classList.add('is-folder-dragging');

        const onMove = (em) => {
          em.preventDefault();
          // Reorder among siblings of the same parent
          const sibs = [...tree.querySelectorAll('.link-folder:not(.loose):not(.dragging)')];
          const after = sibs.find((sib) => {
            const r = sib.getBoundingClientRect();
            return em.clientY < r.top + r.height / 2;
          });
          if (after) tree.insertBefore(folderEl, after);
          else tree.appendChild(folderEl);
        };
        const onUp = () => {
          folderEl.classList.remove('dragging');
          document.body.classList.remove('is-folder-dragging');
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          document.removeEventListener('selectstart', onSelectStart);
          try { window.getSelection()?.removeAllRanges(); } catch (_) {}
          const newOrder = [...tree.querySelectorAll('.link-folder:not(.loose)')].map((el) => el.dataset.folderId);
          const before = proj.linkFolders.map((f) => f.id).join(',');
          proj.linkFolders = newOrder.map((fid) => proj.linkFolders.find((f) => f.id === fid)).filter(Boolean);
          if (proj.linkFolders.map((f) => f.id).join(',') !== before) commit('folder-reorder');
        };
        const onSelectStart = (ev) => ev.preventDefault();
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
        document.addEventListener('selectstart', onSelectStart);
      });
    }

    drawTree();

    $('#btnNewLinkFolder')?.addEventListener('click', () => {
      proj.linkFolders.push({ id: uid('lf'), name: 'New folder', collapsed: false });
      commit('folder-create');
      toast('Folder added');
    });
    $('#btnAddLink')?.addEventListener('click', () => openQuickAdd('link'));
  }

  function openLinkEditor(linkId) {
    const proj = curProject();
    const l = (proj.links || []).find((x) => x.id === linkId);
    if (!l) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:560px;">
        <div class="desc-head">
          <div class="desc-title">Edit link</div>
          <button class="icon-btn" id="lnClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Title</label><input id="lnTitle" value="${escapeHTML(l.title || '')}" /></div>
          <div class="field"><label>URL or path</label><input id="lnUrl" value="${escapeHTML(l.url || '')}" placeholder="https://… or file:///…" /></div>
          <div class="field"><label>Description</label><textarea id="lnDesc" style="min-height:80px;">${escapeHTML(l.description || '')}</textarea></div>
          <div class="qa-row">
            <div class="field"><label>Component (optional)</label>
              <select id="lnComp">
                <option value="">— none —</option>
                ${(proj.components || []).map((c) => `<option value="${c.id}" ${c.id === l.component ? 'selected' : ''}>${escapeHTML(c.name)}</option>`).join('')}
              </select>
            </div>
            <div class="field"><label>Folder</label>
              <select id="lnFolder">
                <option value="">— Loose —</option>
                ${(proj.linkFolders || []).map((f) => `<option value="${f.id}" ${f.id === l.folderId ? 'selected' : ''}>${escapeHTML(f.name)}</option>`).join('')}
              </select>
            </div>
          </div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="lnCancel">Cancel</button>
          <button class="ghost" id="lnDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
          <button class="primary" id="lnSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('lnTitle').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#lnClose').addEventListener('click', close);
    overlay.querySelector('#lnCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × button (and Cancel where present).
    // Backdrop click and Escape are intentionally NOT bound — losing
    // in-progress edits to a stray click outside the modal is too easy.
    overlay.querySelector('#lnSave').addEventListener('click', () => {
      const url = document.getElementById('lnUrl').value.trim();
      if (!url) return toast('URL required');
      l.title = document.getElementById('lnTitle').value.trim() || url;
      l.url = url;
      l.description = document.getElementById('lnDesc').value;
      l.component = document.getElementById('lnComp').value || null;
      l.folderId = document.getElementById('lnFolder')?.value || null;
      commit('link-edit');
      close();
      toast('Saved');
    });
    overlay.querySelector('#lnDelete').addEventListener('click', () => {
      if (!confirm(`Delete "${l.title || l.url}"?`)) return;
      proj.links = (proj.links || []).filter((x) => x.id !== l.id);
      commit('link-delete');
      close();
      toast('Deleted');
    });
  }

  // Floating colour-picker popover anchored to a swatch button. Closes
  // on outside click, Escape, or selecting a colour. Lives in module
  // scope so multiple call sites (Components panel, etc.) reuse it.
  let _compColorPopoverEl = null;
  function closeComponentColorPopover() {
    if (_compColorPopoverEl) {
      _compColorPopoverEl.remove();
      _compColorPopoverEl = null;
      document.removeEventListener('mousedown', _compColorPopoverDismiss, true);
      document.removeEventListener('keydown', _compColorPopoverKey, true);
    }
  }
  function _compColorPopoverDismiss(e) {
    if (_compColorPopoverEl && !_compColorPopoverEl.contains(e.target) && !e.target.closest('.component-swatch-btn')) {
      closeComponentColorPopover();
    }
  }
  function _compColorPopoverKey(e) {
    if (e.key === 'Escape') closeComponentColorPopover();
  }
  function openComponentColorPopover(anchor, pt) {
    closeComponentColorPopover();
    const r = anchor.getBoundingClientRect();
    const pop = document.createElement('div');
    pop.className = 'comp-color-pop';
    pop.style.position = 'fixed';
    pop.style.left = Math.round(r.left) + 'px';
    pop.style.top  = Math.round(r.bottom + 6) + 'px';
    pop.innerHTML = COMPONENT_COLORS.map((co) => {
      const active = co.id === pt.color ? ' is-active' : '';
      return `<button type="button" class="comp-color-pick${active}" data-color="${co.id}" title="${escapeHTML(co.name)}" style="background: rgb(${co.rgb});"></button>`;
    }).join('');
    document.body.appendChild(pop);
    _compColorPopoverEl = pop;
    pop.querySelectorAll('.comp-color-pick').forEach((b) => {
      b.addEventListener('click', (e) => {
        e.stopPropagation();
        pt.color = b.dataset.color;
        closeComponentColorPopover();
        commit('component-recolor');
      });
    });
    // Defer listener-attach so the click that opened the popover doesn't
    // immediately close it via the document mousedown handler.
    setTimeout(() => {
      document.addEventListener('mousedown', _compColorPopoverDismiss, true);
      document.addEventListener('keydown', _compColorPopoverKey, true);
    }, 0);
  }

  function renderComponents(root) {
    const proj = curProject();
    proj.components = proj.components || [];
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div><div class="page-title">Components</div><div class="page-sub">Logical components or work packages — colour cards on the board.</div></div>
        <div class="page-actions"><button class="ghost" id="btnAddComponent">+ Component</button></div>
      </div>
      <div class="row-list" id="componentList"></div>`;
    root.appendChild(view);
    const list = $('#componentList');
    const filteredComponents = (proj.components || []).filter((pt) =>
      matchesSearch(pt.name, pt.costCenter));
    if (!filteredComponents.length) {
      list.innerHTML = `<div class="empty">${
        searchQuery()
          ? 'No components match the current search.'
          : 'No components yet — add one to start colour-coding actions.'}</div>`;
    } else {
      const knownCCs = getCostCentres();
      list.innerHTML = filteredComponents.map((pt) => {
        const c = componentColor(pt.color);
        const count = (proj.actions || []).filter((a) => a.component === pt.id).length;
        const ccOptions = [...new Set([...knownCCs, ...(pt.costCenter ? [pt.costCenter] : [])])];
        return `
          <div class="row component-row" data-component-id="${pt.id}">
            ${ROW_GRIP_HTML}
            <button type="button" class="component-swatch component-swatch-btn" style="background: rgba(${c.rgb},.9);" title="Click to change colour" aria-label="Change colour"></button>
            <input class="inline component-name" value="${escapeHTML(pt.name)}" />
            <input class="inline component-desc" type="text" placeholder="Description — what this component covers (optional)" value="${escapeHTML(pt.description || '')}" />
            <select class="inline component-cc" title="Cost centre">
              <option value="">— none —</option>
              ${ccOptions.map((code) => `<option value="${escapeHTML(code)}" ${code === pt.costCenter ? 'selected' : ''}>${escapeHTML(code)}</option>`).join('')}
              <option value="__new__">+ New cost centre…</option>
            </select>
            <span class="row-meta">${count} action${count === 1 ? '' : 's'}</span>
            <button class="icon-btn component-del" title="Delete">×</button>
          </div>`;
      }).join('');
      wireListReorder(list, {
        rowSelector: '.row[data-component-id]',
        idAttr: 'componentId',
        getArray: () => proj.components,
        setOrder: (ids) => { proj.components = ids.map((id) => proj.components.find((x) => x.id === id)).filter(Boolean); },
        commitName: 'components-reorder',
      });
      $$('.component-name', list).forEach((inp) => {
        inp.addEventListener('change', () => {
          const id = inp.closest('[data-component-id]').dataset.componentId;
          const pt = proj.components.find((p) => p.id === id);
          if (pt) { pt.name = inp.value.trim() || pt.name; commit('component-rename'); }
        });
      });
      $$('.component-desc', list).forEach((inp) => {
        inp.addEventListener('change', () => {
          const id = inp.closest('[data-component-id]').dataset.componentId;
          const pt = proj.components.find((p) => p.id === id);
          if (!pt) return;
          const next = inp.value.trim();
          if ((pt.description || '') !== next) {
            pt.description = next;
            commit('component-desc');
          }
        });
      });
      // Component colour is now changed exclusively by clicking the
      // swatch (popover with full palette). The dropdown was redundant
      // and has been removed for a tighter row.
      // Clicking the colour swatch opens a popover with the full
      // palette so the user can change colour without scrolling
      // through a select dropdown.
      $$('.component-swatch-btn', list).forEach((btn) => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const id = btn.closest('[data-component-id]').dataset.componentId;
          const pt = proj.components.find((p) => p.id === id);
          if (!pt) return;
          openComponentColorPopover(btn, pt);
        });
      });
      $$('.component-cc', list).forEach((sel) => {
        sel.addEventListener('change', () => {
          const id = sel.closest('[data-component-id]').dataset.componentId;
          const pt = proj.components.find((p) => p.id === id);
          if (!pt) return;
          let v = sel.value;
          if (v === '__new__') {
            const code = prompt('New cost-centre name:');
            if (!code) { sel.value = pt.costCenter || ''; return; }
            const trimmed = code.trim();
            if (!trimmed) { sel.value = pt.costCenter || ''; return; }
            state.budgets = state.budgets || {};
            state.budgets[trimmed] = state.budgets[trimmed] || {};
            v = trimmed;
          }
          v = v || null;
          if (pt.costCenter !== v) {
            pt.costCenter = v;
            commit('component-cc');
          }
        });
      });
      $$('.component-del', list).forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.closest('[data-component-id]').dataset.componentId;
          const pt = proj.components.find((p) => p.id === id);
          const count = (proj.actions || []).filter((a) => a.component === id).length;
          if (!confirm(`Delete component "${pt?.name}"?` + (count ? ` (${count} action${count === 1 ? '' : 's'} will become unassigned)` : ''))) return;
          proj.components = proj.components.filter((p) => p.id !== id);
          (proj.actions || []).forEach((a) => { if (a.component === id) a.component = null; });
          commit('component-delete');
        });
      });
    }
    $('#btnAddComponent').addEventListener('click', () => openQuickAdd("component"));
  }

  // EVM KPI hover tooltip — single floating element, populated on demand
  let evmTipEl = null;
  function ensureEvmTipEl() {
    if (evmTipEl) return evmTipEl;
    evmTipEl = document.createElement('div');
    evmTipEl.className = 'evm-tooltip';
    document.body.appendChild(evmTipEl);
    return evmTipEl;
  }
  function showEvmTooltip(metric, x, y) {
    const def = EVM_DEFS[metric];
    if (!def) return;
    const tip = ensureEvmTipEl();
    tip.innerHTML = `
      <div class="evm-name">${escapeHTML(metric)} — ${escapeHTML(def.name)}</div>
      <div class="evm-formula"><span class="evm-lbl">Formula</span><code>${escapeHTML(def.formula)}</code></div>
      <div class="evm-desc">${escapeHTML(def.desc)}</div>`;
    tip.style.display = 'block';
    const r = tip.getBoundingClientRect();
    let px = x + 14, py = y + 14;
    if (px + r.width  > innerWidth  - 8) px = x - r.width - 14;
    if (py + r.height > innerHeight - 8) py = y - r.height - 14;
    tip.style.left = Math.max(8, px) + 'px';
    tip.style.top  = Math.max(8, py) + 'px';
  }
  function hideEvmTooltip() { if (evmTipEl) evmTipEl.style.display = 'none'; }

  function wireEvmTooltipsOnce() {
    if (document._evmWired) return;
    document._evmWired = true;
    document.addEventListener('mouseover', (e) => {
      const card = e.target.closest('.bk[data-evm]');
      if (card) showEvmTooltip(card.dataset.evm, e.clientX, e.clientY);
    });
    document.addEventListener('mousemove', (e) => {
      if (!evmTipEl || evmTipEl.style.display === 'none') return;
      const card = e.target.closest('.bk[data-evm]');
      if (!card) { hideEvmTooltip(); return; }
      // keep showing — re-position smoothly
      showEvmTooltip(card.dataset.evm, e.clientX, e.clientY);
    });
    document.addEventListener('mouseout', (e) => {
      const card = e.target.closest('.bk[data-evm]');
      if (!card) return;
      const next = e.relatedTarget;
      if (next && card.contains(next)) return;
      hideEvmTooltip();
    });
  }

  /* ------------------------ Cost-centre budgets ---------------------- */

  // Hours per FTE-week. Used to convert commitment % → planned hours.
  const HOURS_PER_WEEK = 40;

  // Earned-Value Management metric definitions (used for the budget KPI tooltips)
  const EVM_DEFS = {
    BAC: { name: 'Budget at Completion',     formula: 'Σ weekly budget across the horizon',
           desc: 'The total approved budget for the cost centre.' },
    PV:  { name: 'Planned Value',             formula: 'Σ budget up to today',
           desc: 'How much work, in budgeted terms, was planned to be done by now.' },
    AC:  { name: 'Actual Cost',               formula: 'Σ actual cost up to today',
           desc: 'Cost actually incurred so far, summed from action commitments × hourly rates.' },
    EV:  { name: 'Earned Value',              formula: 'Σ planned cost × % complete',
           desc: 'The budgeted value of the work actually performed. Done actions credit full cost; in-flight ones credit by elapsed/total duration.' },
    CV:  { name: 'Cost Variance',             formula: 'EV − AC',
           desc: 'Positive = under budget for the work done. Negative = over budget.' },
    SV:  { name: 'Schedule Variance',         formula: 'EV − PV',
           desc: 'Positive = ahead of schedule. Negative = behind: less work delivered than planned.' },
    CPI: { name: 'Cost Performance Index',    formula: 'EV ÷ AC',
           desc: '> 1: getting more value per € spent than budgeted. < 1: cost overrun.' },
    SPI: { name: 'Schedule Performance Index', formula: 'EV ÷ PV',
           desc: '> 1: ahead of schedule. < 1: behind schedule.' },
    EAC: { name: 'Estimate at Completion',    formula: 'BAC ÷ CPI',
           desc: 'Forecast of the total cost at the end of the horizon, extrapolating current cost performance.' },
    ETC: { name: 'Estimate to Complete',      formula: 'EAC − AC',
           desc: 'Forecast of remaining spend from today to completion.' },
    VAC: { name: 'Variance at Completion',    formula: 'BAC − EAC',
           desc: 'Forecast surplus (positive) or shortfall (negative) at completion vs the original budget.' },
  };
  function personRate(personId) {
    const p = state.people.find((x) => x.id === personId);
    return (p && typeof p.hourlyRate === 'number') ? p.hourlyRate : 100;
  }
  function avgHourlyRate() {
    const ppl = state.people || [];
    if (!ppl.length) return 100;
    return ppl.reduce((s, p) => s + (p.hourlyRate || 100), 0) / ppl.length;
  }

  function getCostCentres() {
    const inScope = projectsInScope();
    const set = new Set();
    inScope.forEach((p) => {
      (p.components || []).forEach((c) => { if (c.costCenter) set.add(c.costCenter); });
      // Also include CCs explicitly declared on the project (e.g. via "+ Cost centre"
      // before any component is mapped) so they're visible in the Budgets page.
      (p.costCenters || []).forEach((cc) => { if (cc) set.add(cc); });
    });
    // Order: user-defined via drag on the Budgets page, then any newly-seen
    // CCs alphabetically at the end. Cleared entries (deleted CCs) are
    // filtered out.
    const persisted = Array.isArray(state.settings?.costCentreOrder) ? state.settings.costCentreOrder : [];
    const known = new Set(set);
    const ordered = persisted.filter((cc) => known.has(cc));
    const seen = new Set(ordered);
    [...set].sort().forEach((cc) => { if (!seen.has(cc)) ordered.push(cc); });
    return ordered;
  }

  function componentsForCC(cc) {
    const out = [];
    projectsInScope().forEach((p) => (p.components || []).forEach((c) => {
      if (c.costCenter === cc) out.push({ proj: p, component: c });
    }));
    return out;
  }

  function actionsForCC(cc) {
    const ids = new Set(componentsForCC(cc).map(({ component }) => component.id));
    const out = [];
    projectsInScope().forEach((p) => (p.actions || []).forEach((a) => {
      // Cost-centre rollups include archived — they were real spend.
      if (ids.has(a.component)) out.push(a);
    }));
    return out;
  }

  function actionPlannedHours(a) {
    if (!a.due) return 0;
    const cmt = (typeof a.commitment === 'number') ? a.commitment : 100;
    const startD = a.startDate ? parseDate(a.startDate) : new Date(parseDate(a.due).getTime() - 2 * dayMs);
    const endD = parseDate(a.due);
    const days = Math.max(1, Math.round((endD - startD) / dayMs) + 1);
    return (cmt / 100) * HOURS_PER_WEEK * (days / 7);
  }
  function actionPlannedCost(a) {
    return actionPlannedHours(a) * personRate(a.owner);
  }

  function _ccWeekOverlap(cc, weekStartISO) {
    const wsT = parseDate(weekStartISO).getTime();
    const weT = wsT + 7 * dayMs - 1;
    const out = [];
    actionsForCC(cc).forEach((a) => {
      if (!a.due) return;
      const startT = a.startDate
        ? parseDate(a.startDate).getTime()
        : parseDate(a.due).getTime() - 2 * dayMs;
      const endT = parseDate(a.due).getTime();
      if (endT < wsT || startT > weT) return;
      const overlapStart = Math.max(startT, wsT);
      const overlapEnd   = Math.min(endT,   weT);
      const overlapDays  = Math.max(1, Math.round((overlapEnd - overlapStart) / dayMs) + 1);
      const cmt = (typeof a.commitment === 'number') ? a.commitment : 100;
      const hours = (cmt / 100) * HOURS_PER_WEEK * (overlapDays / 7);
      out.push({ a, hours, cost: hours * personRate(a.owner) });
    });
    return out;
  }
  function actualHoursForCCWeek(cc, weekStartISO) {
    return _ccWeekOverlap(cc, weekStartISO).reduce((s, x) => s + x.hours, 0);
  }
  function actualCostForCCWeek(cc, weekStartISO) {
    return _ccWeekOverlap(cc, weekStartISO).reduce((s, x) => s + x.cost, 0);
  }

  // Per-component breakdown for a CC for a given week.
  // Returns { [componentId]: { hours, cost, name, color } }
  function _ccComponentBreakdownForWeek(cc, weekStartISO) {
    const out = {};
    componentsForCC(cc).forEach(({ component }) => {
      out[component.id] = { hours: 0, cost: 0, name: component.name, color: component.color };
    });
    _ccWeekOverlap(cc, weekStartISO).forEach((x) => {
      const cid = x.a.component;
      if (!out[cid]) return;
      out[cid].hours += x.hours;
      out[cid].cost  += x.cost;
    });
    return out;
  }

  // Per-person breakdown for a CC for a given week.
  function _ccPersonBreakdownForWeek(cc, weekStartISO) {
    const out = {};
    _ccWeekOverlap(cc, weekStartISO).forEach((x) => {
      const pid = x.a.owner;
      if (!out[pid]) {
        const p = state.people.find((pp) => pp.id === pid);
        out[pid] = { hours: 0, cost: 0, name: p?.name || 'Unassigned', id: pid };
      }
      out[pid].hours += x.hours;
      out[pid].cost  += x.cost;
    });
    return out;
  }
  function peopleForCC(cc) {
    const set = new Set();
    actionsForCC(cc).forEach((a) => { if (a.owner) set.add(a.owner); });
    return [...set].map((pid) => state.people.find((p) => p.id === pid)).filter(Boolean);
  }

  // Budgets are stored in HOURS (canonical). evmFor returns measurements in
  // the requested mode ('cost' or 'hours').
  function evmFor(cc, weeks, mode) {
    state.budgets = state.budgets || {};
    state.budgets[cc] = state.budgets[cc] || {};
    const today = new Date(); today.setHours(0, 0, 0, 0);
    const todayT = today.getTime();
    const isCost = mode === 'cost';
    const avg = avgHourlyRate();
    const conv = (h) => isCost ? h * avg : h;
    const budgetsHours = weeks.map((w) => Number(state.budgets[cc][w.isoStart] || 0));
    const budgets = budgetsHours.map(conv);
    const actuals = weeks.map((w) => isCost
      ? actualCostForCCWeek(cc, w.isoStart)
      : actualHoursForCCWeek(cc, w.isoStart));
    const BAC = budgets.reduce((s, v) => s + v, 0);
    let PV = 0, AC = 0;
    weeks.forEach((w, i) => {
      if (w.start.getTime() <= todayT) { PV += budgets[i]; AC += actuals[i]; }
    });
    let EV = 0;
    actionsForCC(cc).forEach((a) => {
      const planned = isCost ? actionPlannedCost(a) : actionPlannedHours(a);
      if (a.status === 'done') { EV += planned; return; }
      if (!a.startDate || !a.due) return;
      const startT = parseDate(a.startDate).getTime();
      const endT   = parseDate(a.due).getTime();
      if (todayT <= startT) return;
      const elapsed = Math.min(todayT, endT) - startT;
      const dur     = Math.max(1, endT - startT);
      EV += planned * (elapsed / dur);
    });
    const CV  = EV - AC;
    const SV  = EV - PV;
    const CPI = AC > 0 ? EV / AC : 1;
    const SPI = PV > 0 ? EV / PV : 1;
    const EAC = CPI > 0 ? BAC / CPI : BAC;
    const ETC = Math.max(0, EAC - AC);
    const VAC = BAC - EAC;
    return { BAC, PV, AC, EV, CV, SV, CPI, SPI, EAC, ETC, VAC, budgets, actuals, mode };
  }

  function fmtBudgetVal(v, mode) {
    if (mode === 'hours') return v >= 100 ? Math.round(v) + 'h' : v.toFixed(1) + 'h';
    if (v === 0) return '0 €';
    if (Math.abs(v) >= 1000) return (v / 1000).toFixed(1) + 'k €';
    return Math.round(v) + ' €';
  }
  function fmtMoney(v) { return fmtBudgetVal(v, 'cost'); }

  // Per-cost-centre legend filter — Set of entity IDs (component or
  // person) that are currently HIDDEN in that cost centre's chart.
  // Session-local: not persisted to state, resets when the user
  // reloads the app. The map is keyed by cc code; the inner Set is
  // shared between component and person mode for that CC since the
  // entity id namespaces don't overlap.
  const _budgetHidden = {};
  const isBudgetHidden = (cc, id) => !!_budgetHidden[cc]?.has(id);
  const toggleBudgetHidden = (cc, id) => {
    if (!_budgetHidden[cc]) _budgetHidden[cc] = new Set();
    if (_budgetHidden[cc].has(id)) _budgetHidden[cc].delete(id);
    else _budgetHidden[cc].add(id);
  };

  // Wire HTML5 drag-and-drop on the budgets list. The ⋮⋮ handle carries
  // draggable=true; on dragstart we stash the card's cc code; on dragover
  // of another card we position an .is-drop-target class so the user
  // sees where the drop will land; on drop we splice into the persisted
  // order and re-render.
  function _wireCostCentreDrag(list) {
    if (!list) return;
    let dragCc = null;
    list.querySelectorAll('.cc-drag-handle').forEach((h) => {
      h.addEventListener('dragstart', (e) => {
        const card = h.closest('.budget-card');
        if (!card) return;
        dragCc = card.dataset.cc;
        e.dataTransfer.effectAllowed = 'move';
        e.dataTransfer.setData('text/plain', dragCc);
        card.classList.add('is-dragging');
        // Use the whole card as the drag ghost.
        try { e.dataTransfer.setDragImage(card, 20, 20); } catch (_) {}
      });
      h.addEventListener('dragend', () => {
        list.querySelectorAll('.budget-card.is-dragging').forEach((el) => el.classList.remove('is-dragging'));
        list.querySelectorAll('.budget-card.is-drop-target').forEach((el) => el.classList.remove('is-drop-target'));
        dragCc = null;
      });
    });
    list.addEventListener('dragover', (e) => {
      if (!dragCc) return;
      const overCard = e.target.closest('.budget-card');
      if (!overCard || overCard.dataset.cc === dragCc) return;
      e.preventDefault();
      e.dataTransfer.dropEffect = 'move';
      list.querySelectorAll('.budget-card.is-drop-target').forEach((el) => el.classList.remove('is-drop-target'));
      overCard.classList.add('is-drop-target');
    });
    list.addEventListener('drop', (e) => {
      if (!dragCc) return;
      const overCard = e.target.closest('.budget-card');
      if (!overCard || overCard.dataset.cc === dragCc) return;
      e.preventDefault();
      const targetCc = overCard.dataset.cc;
      // Rebuild the order: drop dragged before target (or after if we
      // dragged upward from below — determined by pre-existing position).
      const currentOrder = getCostCentres();
      const from = currentOrder.indexOf(dragCc);
      const to   = currentOrder.indexOf(targetCc);
      if (from === -1 || to === -1) return;
      currentOrder.splice(from, 1);
      currentOrder.splice(to, 0, dragCc);
      state.settings = state.settings || {};
      state.settings.costCentreOrder = currentOrder;
      saveState();
      // Re-render whole panel — cheapest way to redraw every chart in
      // the new order.
      render();
    });
  }

  function renderBudgets(root) {
    state.budgets = state.budgets || {};
    state.settings = state.settings || {};
    if (state.settings.budgetView !== 'hours' && state.settings.budgetView !== 'cost') {
      state.settings.budgetView = 'cost';
    }
    if (state.settings.budgetGroupBy !== 'component' && state.settings.budgetGroupBy !== 'person') {
      state.settings.budgetGroupBy = 'component';
    }
    // Per-cost-centre date ranges. Each entry is { start, end } in
    // ISO YYYY-MM-DD, snapped to the previous Monday at read-time so
    // the week grid stays aligned. Missing entries fall back to the
    // default (26 w past + 78 w future, ≈ 2 years centred on now).
    state.settings.budgetRanges = state.settings.budgetRanges || {};

    const view = document.createElement('div');
    view.className = 'view';
    const ccs = getCostCentres();
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Cost-centre budgets</div>
          <div class="page-sub">Drag the green points to set a weekly budget. Bars show actual workload on components mapped to the cost centre. Toggle between € and hours; rates are per-person (edit on the People page).</div>
        </div>
        <div class="page-actions">
          <div class="seg" role="tablist" aria-label="Group by">
            <button class="seg-btn ${state.settings.budgetGroupBy === 'component' ? 'active' : ''}" data-budget-group="component">By component</button>
            <button class="seg-btn ${state.settings.budgetGroupBy === 'person' ? 'active' : ''}" data-budget-group="person">By person</button>
          </div>
          <div class="seg" role="tablist" aria-label="Unit">
            <button class="seg-btn ${state.settings.budgetView === 'cost' ? 'active' : ''}" data-budget-view="cost">€ Cost</button>
            <button class="seg-btn ${state.settings.budgetView === 'hours' ? 'active' : ''}" data-budget-view="hours">⏱ Hours</button>
          </div>
          <button class="primary" id="btnNewCC">+ Cost centre</button>
        </div>
      </div>
      ${ccs.length ? '' : '<div class="empty">No cost centres yet — click <b>+ Cost centre</b> above, or add one to a component on the <b>Components</b> page.</div>'}
      <div id="budgetsList"></div>`;
    root.appendChild(view);

    $$('.seg-btn[data-budget-view]', view).forEach((b) => {
      b.addEventListener('click', () => {
        state.settings.budgetView = b.dataset.budgetView;
        saveState();
        $$('.seg-btn[data-budget-view]', view).forEach((x) => x.classList.toggle('active', x.dataset.budgetView === state.settings.budgetView));
        drawList();
      });
    });
    $$('.seg-btn[data-budget-group]', view).forEach((b) => {
      b.addEventListener('click', () => {
        state.settings.budgetGroupBy = b.dataset.budgetGroup;
        saveState();
        $$('.seg-btn[data-budget-group]', view).forEach((x) => x.classList.toggle('active', x.dataset.budgetGroup === state.settings.budgetGroupBy));
        drawList();
      });
    });
    $('#btnNewCC').addEventListener('click', () => {
      if (curProjectIsMerged()) { toast('Pick a single project first'); return; }
      const code = prompt('New cost-centre name:');
      if (!code) return;
      const trimmed = code.trim();
      if (!trimmed) return;
      if (ccs.includes(trimmed)) { toast('Already exists in this project'); return; }
      state.budgets[trimmed] = state.budgets[trimmed] || {};
      const proj = curProject();
      proj.costCenters = proj.costCenters || [];
      if (!proj.costCenters.includes(trimmed)) proj.costCenters.push(trimmed);
      saveState();
      render();
    });

    // Snap an arbitrary date to the Monday of its week (Monday = 1).
    function snapToMonday(d) {
      const out = new Date(d); out.setHours(0, 0, 0, 0);
      const diff = (out.getDay() + 6) % 7; // days to subtract to reach Mon
      return new Date(out.getTime() - diff * dayMs);
    }
    // Default range for a freshly-created CC: 26 past + 78 future weeks
    // centred on this Monday — same total span as the old 2-year default.
    function defaultBudgetRange() {
      const today = new Date(); today.setHours(0, 0, 0, 0);
      const mon = snapToMonday(today);
      const start = new Date(mon.getTime() - 26 * 7 * dayMs);
      const end   = new Date(mon.getTime() + 78 * 7 * dayMs);
      return { start: fmtISO(start), end: fmtISO(end) };
    }
    // Read the persisted range for `cc`, falling back to the default.
    // Clamps end >= start + 4 weeks so the chart always has a usable axis.
    function getBudgetRange(cc) {
      const stored = state.settings.budgetRanges?.[cc];
      const def = defaultBudgetRange();
      const start = stored?.start || def.start;
      let end = stored?.end || def.end;
      const startD = snapToMonday(parseDate(start) || new Date());
      let   endD   = snapToMonday(parseDate(end)   || new Date());
      const minEnd = new Date(startD.getTime() + 4 * 7 * dayMs);
      if (endD.getTime() < minEnd.getTime()) endD = minEnd;
      return { start: fmtISO(startD), end: fmtISO(endD) };
    }
    // Build the per-week table for a given range.
    function weekStartsForRange(range) {
      const startMon = snapToMonday(parseDate(range.start));
      const endMon   = snapToMonday(parseDate(range.end));
      const out = [];
      for (let t = startMon.getTime(); t <= endMon.getTime(); t += 7 * dayMs) {
        const s = new Date(t);
        out.push({ start: s, isoStart: fmtISO(s) });
      }
      return out;
    }

    function drawList() {
      const list = $('#budgetsList');
      if (!ccs.length) { list.innerHTML = ''; return; }
      const proj = curProject();
      const componentsByCC = (cc) => (proj.components || []).filter((c) => c.costCenter === cc);
      const visible = ccs.filter((cc) =>
        matchesSearch(cc, componentsByCC(cc).map((c) => c.name).join(' '))
      );
      if (!visible.length) {
        list.innerHTML = `<div class="empty">No cost centres match the current search.</div>`;
        return;
      }
      list.innerHTML = visible.map((cc) => {
        const safe = cc.replace(/[^A-Za-z0-9_-]/g, '_');
        const range = getBudgetRange(cc);
        return `
          <div class="budget-card copyable-chart" data-cc="${escapeHTML(cc)}" data-chart="budget-${safe}">
            <div class="budget-head">
              <span class="cc-drag-handle" draggable="true" title="Drag to reorder cost centres" aria-label="Drag handle">⋮⋮</span>
              <h3>${escapeHTML(cc)}</h3>
              <span class="budget-range" title="Time range for this cost centre — snaps to weeks">
                <input type="date" class="bc-start" data-cc="${escapeHTML(cc)}" value="${range.start}" />
                <span class="bc-range-sep" aria-hidden="true">→</span>
                <input type="date" class="bc-end"   data-cc="${escapeHTML(cc)}" value="${range.end}" />
              </span>
              <div class="budget-legend" id="legend-${safe}"></div>
              <button type="button" class="bc-delete-cc" data-cc="${escapeHTML(cc)}" title="Delete this cost centre" aria-label="Delete cost centre ${escapeHTML(cc)}">×</button>
            </div>
            <div class="budget-kpis" id="kpis-${safe}"></div>
            <div class="budget-chart-wrap"><svg class="budget-chart" id="chart-${safe}"></svg></div>
          </div>`;
      }).join('');
      // Native HTML5 drag on the ⋮⋮ handle. The whole card follows because
      // dragstart on the handle bubbles up — we set the effect + dataTransfer
      // once, then handle dragover / drop at the list level to reorder.
      _wireCostCentreDrag(list);
      visible.forEach((cc) => {
        const weeks = weekStartsForRange(getBudgetRange(cc));
        drawChart(cc, weeks);
      });
      // Wire per-CC range inputs. On change, persist the new range and
      // redraw only the affected card — the others stay scrolled where
      // the user left them.
      $$('.bc-start, .bc-end', list).forEach((inp) => {
        inp.addEventListener('change', () => {
          const cc = inp.dataset.cc;
          const card = inp.closest('.budget-card');
          if (!cc || !card) return;
          const startInp = card.querySelector('.bc-start');
          const endInp   = card.querySelector('.bc-end');
          state.settings.budgetRanges = state.settings.budgetRanges || {};
          state.settings.budgetRanges[cc] = {
            start: startInp.value || getBudgetRange(cc).start,
            end:   endInp.value   || getBudgetRange(cc).end,
          };
          saveState();
          // Snap-back the input values to the resolved (possibly clamped)
          // range so the user sees the actual displayed window.
          const eff = getBudgetRange(cc);
          startInp.value = eff.start;
          endInp.value   = eff.end;
          const weeks = weekStartsForRange(eff);
          drawChart(cc, weeks);
        });
      });
      // Wire per-CC delete buttons. Counts linked components + actions
      // across ALL projects (not just the scoped one — a CC is portfolio
      // level), spells the impact out in the confirm, and on yes drops
      // every reference: budgets, range, project lists, and component
      // costCenter pointers. Components and actions themselves stay.
      $$('.bc-delete-cc', list).forEach((btn) => {
        btn.addEventListener('click', () => {
          const cc = btn.dataset.cc;
          if (!cc) return;
          let linkedComponents = 0;
          let projectsTouching = 0;
          const compIds = new Set();
          state.projects.forEach((p) => {
            let projHasLink = false;
            (p.components || []).forEach((c) => {
              if (c.costCenter === cc) {
                linkedComponents++;
                compIds.add(c.id);
                projHasLink = true;
              }
            });
            if ((p.costCenters || []).includes(cc)) projHasLink = true;
            if (projHasLink) projectsTouching++;
          });
          let linkedActions = 0;
          state.projects.forEach((p) => (p.actions || []).forEach((a) => {
            // Archived actions still count — the warning should reflect
            // everything real, hidden or not.
            if (compIds.has(a.component)) linkedActions++;
          }));
          const weekRows = Object.values(state.budgets?.[cc] || {}).filter((v) => Number(v) > 0).length;
          const lines = [`Delete cost centre "${cc}"?`, ''];
          if (weekRows)         lines.push(`• ${weekRows} week${weekRows === 1 ? '' : 's'} of budget data will be removed.`);
          if (linkedComponents) lines.push(`• ${linkedComponents} component${linkedComponents === 1 ? '' : 's'} in ${projectsTouching} project${projectsTouching === 1 ? '' : 's'} will be unlinked from this cost centre.`);
          if (linkedActions)    lines.push(`• ${linkedActions} action${linkedActions === 1 ? '' : 's'} on those components will keep working but no longer roll up to this CC.`);
          if (!weekRows && !linkedComponents) lines.push('No linked data — safe to delete.');
          lines.push('', 'This cannot be undone (except via the in-session undo). Continue?');
          if (!confirm(lines.join('\n'))) return;
          // Mutate
          if (state.budgets) delete state.budgets[cc];
          if (state.settings?.budgetRanges) delete state.settings.budgetRanges[cc];
          state.projects.forEach((p) => {
            if (Array.isArray(p.costCenters)) p.costCenters = p.costCenters.filter((x) => x !== cc);
            (p.components || []).forEach((c) => { if (c.costCenter === cc) c.costCenter = null; });
          });
          // Forget session-local legend hidden state for this CC
          delete _budgetHidden[cc];
          saveState();
          renderBudgets(root);
        });
      });
    }

    function drawChart(cc, weeks) {
      const safe = cc.replace(/[^A-Za-z0-9_-]/g, '_');
      const mode = state.settings.budgetView;
      const evm = evmFor(cc, weeks, mode);
      const fmtV = (v) => fmtBudgetVal(v, mode);
      // Hide interactive UI controls (date pickers, delete button) when
      // the card is captured as PNG — they're noise in a shareable image.
      registerChartCapture(`budget-${safe}`, {
        hideSelectors: ['.bc-delete-cc', '.budget-range'],
      });
      registerChartData(`budget-${safe}`, () => {
        const unit = mode === 'cost' ? '€' : 'h';
        return {
          headers: ['Week start', `Budget (${unit})`, `Actual (${unit})`, `Cumulative budget (${unit})`, `Cumulative actual (${unit})`],
          rows: weeks.map((w, i) => {
            let cb = 0, ca = 0;
            for (let j = 0; j <= i; j++) { cb += evm.budgets[j]; ca += evm.actuals[j]; }
            return [w.isoStart, Number(evm.budgets[i].toFixed(2)), Number(evm.actuals[i].toFixed(2)), Number(cb.toFixed(2)), Number(ca.toFixed(2))];
          }),
        };
      });
      const kpiEl = document.getElementById(`kpis-${safe}`);
      const k = (l, v, cls = '') => `<div class="bk" data-evm="${l}"><div class="bk-l">${l}</div><div class="bk-v ${cls}">${v}</div></div>`;
      kpiEl.innerHTML = [
        k('BAC', fmtV(evm.BAC)),
        k('PV',  fmtV(evm.PV)),
        k('AC',  fmtV(evm.AC)),
        k('EV',  fmtV(evm.EV)),
        k('CV',  (evm.CV >= 0 ? '+' : '') + fmtV(evm.CV), evm.CV >= 0 ? 'ok' : 'bad'),
        k('SV',  (evm.SV >= 0 ? '+' : '') + fmtV(evm.SV), evm.SV >= 0 ? 'ok' : 'bad'),
        k('CPI', evm.CPI.toFixed(2), evm.CPI >= 1 ? 'ok' : 'bad'),
        k('SPI', evm.SPI.toFixed(2), evm.SPI >= 1 ? 'ok' : 'bad'),
        k('EAC', fmtV(evm.EAC), evm.EAC > evm.BAC ? 'bad' : 'ok'),
        k('ETC', fmtV(evm.ETC)),
        k('VAC', (evm.VAC >= 0 ? '+' : '') + fmtV(evm.VAC), evm.VAC >= 0 ? 'ok' : 'bad'),
      ].join('');

      const svg = document.getElementById(`chart-${safe}`);
      const H = 240;
      const padL = 64, padR = 18, padT = 14, padB = 32;
      // Allocate a comfortable per-week column on screen (~22 px) so a
      // 52-week year renders ~1144 px wide; the container scrolls
      // horizontally when this exceeds the panel width. Floor at 800 px
      // so short windows still have a usable axis range.
      const WEEK_PX = 22;
      const minW   = 800;
      const W      = Math.max(minW, weeks.length * WEEK_PX);
      const innerW = W - padL - padR, innerH = H - padT - padB;
      // viewBox AND inline width set to the same number — no stretching,
      // text and lines render at their natural aspect ratio. Default
      // preserveAspectRatio (xMidYMid meet) is fine here.
      svg.setAttribute('viewBox', `0 0 ${W} ${H}`);
      svg.removeAttribute('preserveAspectRatio');
      svg.style.width = W + 'px';

      const xLeft = (i) => padL + (i / weeks.length) * innerW;
      const xMid  = (i) => padL + ((i + 0.5) / weeks.length) * innerW;
      const barW = innerW / weeks.length;

      // Build month groupings up-front — both the budget line and the
      // stacked actuals are now drawn at monthly resolution so they
      // share Y units (was the cause of the chart-overflow bug).
      const monthGroups = [];
      weeks.forEach((w, i) => {
        const key = w.start.getFullYear() + '-' + String(w.start.getMonth() + 1).padStart(2, '0');
        let g = monthGroups[monthGroups.length - 1];
        if (!g || g.key !== key) {
          g = { key, indices: [], firstIdx: i, lastIdx: i };
          monthGroups.push(g);
        }
        g.indices.push(i);
        g.lastIdx = i;
      });
      monthGroups.forEach((g) => {
        g.total = g.indices.reduce((s, i) => s + (evm.budgets[i] || 0), 0);
        g.midX  = (xLeft(g.firstIdx) + xLeft(g.lastIdx + 1)) / 2;
      });

      const today = new Date(); today.setHours(0, 0, 0, 0);
      const todayT = today.getTime();
      // Precise X for today inside the chart — find the week that
      // contains today (start ≤ today < start + 7d) and interpolate by
      // weekday within that week. Returns null when today is outside
      // the visible range so the marker disappears cleanly.
      let todayX = null;
      const firstStart = weeks[0]?.start.getTime();
      const lastEnd    = weeks.length ? weeks[weeks.length - 1].start.getTime() + 7 * dayMs : null;
      if (firstStart != null && todayT >= firstStart && todayT < lastEnd) {
        const weekIdx = Math.floor((todayT - firstStart) / (7 * dayMs));
        const dayFrac = ((todayT - firstStart) / dayMs % 7) / 7; // 0..1 within the week
        todayX = xLeft(weekIdx) + dayFrac * (xLeft(weekIdx + 1) - xLeft(weekIdx));
      }

      const isCostMode = mode === 'cost';
      const groupBy = state.settings.budgetGroupBy;

      // Build the layer entities (component or person), per-week values, and color.
      // allLayerEntities = full list for legend rendering (so the user can
      // click to unhide). layerEntities = visible-only subset used by chart.
      let allLayerEntities;
      if (groupBy === 'person') {
        const ppl = peopleForCC(cc);
        allLayerEntities = ppl.map((person, idx) => {
          const series = weeks.map((w) => {
            const ent = _ccPersonBreakdownForWeek(cc, w.isoStart)[person.id];
            if (!ent) return 0;
            return isCostMode ? ent.cost : ent.hours;
          });
          return { kind: 'person', id: person.id, name: person.name, series, color: personColorByIndex(idx) };
        });
      } else {
        const comps = componentsForCC(cc).map(({ component }) => component);
        allLayerEntities = comps.map((component) => {
          const series = weeks.map((w) => {
            const ent = _ccComponentBreakdownForWeek(cc, w.isoStart)[component.id];
            if (!ent) return 0;
            return isCostMode ? ent.cost : ent.hours;
          });
          return { kind: 'component', id: component.id, name: component.name, series, color: componentColor(component.color) };
        });
      }
      // Filter hidden entities out of the drawn stack — legend still shows
      // them (with a muted style) so they can be re-enabled.
      const layerEntities = allLayerEntities.filter((ent) => !isBudgetHidden(cc, ent.id));

      // Aggregate each layer's per-week series to monthly totals so they
      // share Y units with the budget step line. Stacking is done at the
      // monthly resolution; each layer becomes a stepped band that's
      // flat across the month.
      layerEntities.forEach((ent) => {
        ent.monthSeries = monthGroups.map((g) =>
          g.indices.reduce((s, i) => s + (ent.series[i] || 0), 0)
        );
      });

      // Determine Y range from MONTHLY units — both the budget totals
      // and the cumulative monthly actuals (sum across all layers) need
      // to fit. With per-week scaling, monthly totals overflowed the
      // chart top; this is the fix.
      const monthlyActualTotals = monthGroups.map((_, gi) =>
        layerEntities.reduce((s, ent) => s + (ent.monthSeries[gi] || 0), 0)
      );
      const monthlyBudgetTotals = monthGroups.map((g) => g.total);
      const dataMax = Math.max(0, ...monthlyBudgetTotals, ...monthlyActualTotals);
      const maxData = dataMax > 0 ? dataMax : (mode === 'cost' ? 100 : 5);
      const maxY = maxData * 1.12;
      const yFor = (v) => padT + innerH - (v / maxY) * innerH;

      // Stack the series at MONTHLY resolution — each layer's TOP is its
      // running sum at each month. The forward path traces the top of
      // the layer as a step function (flat per month, vertical at month
      // boundaries); the back path returns along cumPrev to close the
      // band.
      let cumPrev = monthGroups.map(() => 0);
      const layers = layerEntities.map((ent) => {
        const top = ent.monthSeries.map((v, i) => cumPrev[i] + v);
        let fwd = '';
        monthGroups.forEach((g, i) => {
          const left  = xLeft(g.firstIdx);
          const right = xLeft(g.lastIdx + 1);
          const y     = yFor(top[i]);
          if (i === 0) fwd += `M ${left.toFixed(1)} ${y.toFixed(1)} `;
          else         fwd += `L ${left.toFixed(1)} ${y.toFixed(1)} `;
          fwd += `L ${right.toFixed(1)} ${y.toFixed(1)} `;
        });
        let back = '';
        for (let i = monthGroups.length - 1; i >= 0; i--) {
          const g = monthGroups[i];
          const left  = xLeft(g.firstIdx);
          const right = xLeft(g.lastIdx + 1);
          const y     = yFor(cumPrev[i]);
          back += `L ${right.toFixed(1)} ${y.toFixed(1)} `;
          back += `L ${left.toFixed(1)}  ${y.toFixed(1)} `;
        }
        const path = fwd + back + 'Z';
        const layer = { path, ent };
        cumPrev = top;
        return layer;
      });
      const areas = layers.map((l) => {
        const c = l.ent.color;
        return `<path class="b-comp-area" data-${l.ent.kind}-id="${l.ent.id}"
          d="${l.path}"
          fill="rgba(${c.rgb},.55)"
          stroke="rgba(${c.rgb},.95)"
          stroke-width="0.7"><title>${escapeHTML(l.ent.name)}</title></path>`;
      }).join('');

      // Populate the chip legend with EVERY entity (including hidden
      // ones, styled muted) so the user can toggle each on / off.
      const legendEl = document.getElementById(`legend-${safe}`);
      if (legendEl) {
        legendEl.innerHTML = allLayerEntities.map((ent) => {
          const c = ent.color;
          const hidden = isBudgetHidden(cc, ent.id);
          return `<button type="button" class="component-chip b-legend-chip${hidden ? ' is-hidden' : ''}" data-cc="${escapeHTML(cc)}" data-ent-id="${escapeHTML(ent.id)}" style="background:rgba(${c.rgb},${hidden ? .06 : .2});color:rgb(${c.rgb});border-color:rgba(${c.rgb},${hidden ? .35 : .6})" title="Click to ${hidden ? 'show' : 'hide'} ${escapeHTML(ent.name)} in this chart">${escapeHTML(ent.name)}</button>`;
        }).join('');
        // Wire clicks — toggle the entity's hidden state and redraw
        // only this chart (other cost centres are untouched).
        legendEl.querySelectorAll('.b-legend-chip').forEach((chip) => {
          chip.addEventListener('click', () => {
            toggleBudgetHidden(cc, chip.dataset.entId);
            drawChart(cc, weeks);
          });
        });
      }

      // monthGroups is precomputed at the top of drawChart so the Y-axis
      // scaling can include monthly totals. Just rebuild the budget step
      // line from the existing monthGroups.totals here.
      // Step line: each month flat at its total, vertical jumps between
      // months. M xL yT  L xR yT  L xR(next) yT(next)  L … and so on.
      let linePath = '';
      monthGroups.forEach((g, gi) => {
        const left  = xLeft(g.firstIdx);
        const right = xLeft(g.lastIdx + 1); // right edge of last week in month
        const y     = yFor(g.total);
        if (gi === 0) linePath += `M ${left.toFixed(1)} ${y.toFixed(1)} `;
        else          linePath += `L ${left.toFixed(1)} ${y.toFixed(1)} `;
        linePath += `L ${right.toFixed(1)} ${y.toFixed(1)} `;
      });

      const points = monthGroups.map((g) => {
        const monthLbl = weeks[g.firstIdx].start.toLocaleDateString(undefined, { month: 'short', year: 'numeric' });
        return `<circle class="b-point" data-cc="${escapeHTML(cc)}" data-month-key="${g.key}" data-first-idx="${g.firstIdx}" data-last-idx="${g.lastIdx}" cx="${g.midX.toFixed(1)}" cy="${yFor(g.total).toFixed(1)}" r="4">
          <title>${escapeHTML(monthLbl)} — drag to set budget for the whole month (${fmtV(g.total)})</title>
        </circle>`;
      }).join('');

      const todayLine = todayX != null
        ? `<line class="b-today" x1="${todayX.toFixed(1)}" x2="${todayX.toFixed(1)}" y1="${padT}" y2="${padT + innerH}" />
           <text class="b-today-lbl" x="${(todayX + 4).toFixed(1)}" y="${padT + 10}">today</text>`
        : '';

      const months = weeks.map((w, i) => {
        if (w.start.getDate() > 7) return '';
        return `<text class="b-tick" x="${xMid(i).toFixed(1)}" y="${H - 12}" text-anchor="middle">${w.start.toLocaleDateString(undefined, { month: 'short', year: '2-digit' })}</text>`;
      }).join('');

      // Minor vertical gridlines at each month boundary — one line at
      // each group's left edge. Skip the very first one because the chart
      // border already shows that x = padL. Quiet stroke; sits BEHIND
      // the actuals area so it doesn't distract from the data.
      const monthGrid = monthGroups.slice(1).map((g) => {
        const x = xLeft(g.firstIdx).toFixed(1);
        return `<line class="b-month-grid" x1="${x}" x2="${x}" y1="${padT}" y2="${(padT + innerH).toFixed(1)}" />`;
      }).join('');

      const yTicks = [0, maxY / 2, maxY].map((v) =>
        `<g class="b-ytick">
          <line x1="${padL - 3}" x2="${padL}" y1="${yFor(v).toFixed(1)}" y2="${yFor(v).toFixed(1)}" />
          <text x="${padL - 6}" y="${(yFor(v) + 3).toFixed(1)}" text-anchor="end">${fmtV(v)}</text>
        </g>`).join('');

      svg.innerHTML = `
        ${yTicks}
        ${monthGrid}
        ${areas}
        <path class="b-budget-line" d="${linePath}" />
        ${todayLine}
        <line class="b-hover-line" x1="0" x2="0" y1="${padT}" y2="${padT + innerH}" style="display:none" />
        ${points}
        ${months}`;

      // Hover tracking — vertical crosshair + side panel listing components & budget
      let hoverPanel = svg.parentElement.querySelector('.b-hover-panel');
      if (!hoverPanel) {
        hoverPanel = document.createElement('div');
        hoverPanel.className = 'b-hover-panel';
        svg.parentElement.appendChild(hoverPanel);
      }
      const hoverLine = svg.querySelector('.b-hover-line');
      svg.addEventListener('mousemove', (e) => {
        const r = svg.getBoundingClientRect();
        const localX = (e.clientX - r.left) * (W / r.width);
        if (localX < padL || localX > padL + innerW) {
          hoverLine.style.display = 'none';
          hoverPanel.style.display = 'none';
          return;
        }
        // Snap the crosshair to the closest MONTH (since the chart now
        // renders at monthly resolution). The vertical line lands at the
        // month's centre and the panel reports monthly aggregates so the
        // numbers line up with what's drawn.
        let bestG = 0, bestD = Infinity;
        for (let g = 0; g < monthGroups.length; g++) {
          const d = Math.abs(monthGroups[g].midX - localX);
          if (d < bestD) { bestD = d; bestG = g; }
        }
        const grp  = monthGroups[bestG];
        const lineX = grp.midX;
        hoverLine.setAttribute('x1', lineX.toFixed(1));
        hoverLine.setAttribute('x2', lineX.toFixed(1));
        hoverLine.style.display = '';
        const monthLabel = weeks[grp.firstIdx].start.toLocaleDateString(undefined, { month: 'long', year: 'numeric' });
        const budget = grp.total;
        const totalActual = grp.indices.reduce((s, i) => s + (
          isCostMode ? actualCostForCCWeek(cc, weeks[i].isoStart)
                     : actualHoursForCCWeek(cc, weeks[i].isoStart)
        ), 0);
        const compRows = layerEntities.map((ent) => {
          const v = ent.monthSeries[bestG];
          if (v === 0) return '';
          return `<div class="b-h-row"><span class="b-h-dot" style="background:rgba(${ent.color.rgb},.95)"></span><b>${escapeHTML(ent.name)}</b><span class="b-h-val">${fmtV(v)}</span></div>`;
        }).filter(Boolean).join('');
        const overBudget = totalActual > budget && budget > 0;
        hoverPanel.innerHTML = `
          <div class="b-h-head">${escapeHTML(monthLabel)} · ${grp.indices.length} week${grp.indices.length === 1 ? '' : 's'}</div>
          ${compRows || '<div class="b-h-empty">No activity</div>'}
          <div class="b-h-row b-h-tot"><b>Total actual</b><span class="b-h-val">${fmtV(totalActual)}</span></div>
          <div class="b-h-row"><span class="b-h-dot b-h-dot-budget"></span><b>Budget</b><span class="b-h-val ${overBudget ? 'bad' : 'ok'}">${fmtV(budget)}</span></div>
          ${budget > 0 ? `<div class="b-h-delta ${overBudget ? 'bad' : 'ok'}">${overBudget ? 'Over' : 'Under'} by ${fmtV(Math.abs(totalActual - budget))}</div>` : ''}`;
        hoverPanel.style.display = 'block';
        const pr = hoverPanel.getBoundingClientRect();
        let px = e.clientX + 14;
        let py = e.clientY + 14;
        if (px + pr.width  > innerWidth  - 8) px = e.clientX - pr.width - 14;
        if (py + pr.height > innerHeight - 8) py = e.clientY - pr.height - 14;
        hoverPanel.style.left = Math.max(8, px) + 'px';
        hoverPanel.style.top  = Math.max(8, py) + 'px';
      });
      svg.addEventListener('mouseleave', () => {
        hoverLine.style.display = 'none';
        hoverPanel.style.display = 'none';
      });

      svg.querySelectorAll('.b-point').forEach((pt) => {
        pt.addEventListener('mousedown', (e) => {
          e.preventDefault();
          e.stopPropagation();
          const firstIdx = parseInt(pt.dataset.firstIdx, 10);
          const lastIdx  = parseInt(pt.dataset.lastIdx, 10);
          const monthKey = pt.dataset.monthKey;
          const monthIndices = [];
          for (let i = firstIdx; i <= lastIdx; i++) monthIndices.push(i);
          const liveBudgets = evm.budgets.slice();
          state.budgets[cc] = state.budgets[cc] || {};
          const isCost = mode === 'cost';
          const avg = avgHourlyRate();
          const step = isCost ? 100 : 1;
          // Displayed value = the month's total budget, summed across its weeks.
          let displayed = monthIndices.reduce((s, i) => s + (liveBudgets[i] || 0), 0);

          // Floating editable input — shown near the point during drag,
          // focusable on release for fine-tuning.
          let editor = document.getElementById('budgetEditor');
          if (!editor) {
            editor = document.createElement('input');
            editor.id = 'budgetEditor';
            editor.type = 'text';
            editor.className = 'budget-edit';
            document.body.appendChild(editor);
          }
          editor._cc = cc; editor._monthKey = monthKey;
          editor._isCost = isCost; editor._avg = avg; editor._step = step;
          editor._chartCtx = { cc, weeks, redraw: () => drawChart(cc, weeks) };

          function applyDisplayedValue(v) {
            displayed = Math.max(0, Math.round(v / step) * step);
            // Distribute the month's total evenly across its weeks. Each
            // week's stored value is in HOURS — convert from cost mode
            // when needed before division.
            const totalHours = isCost ? displayed / avg : displayed;
            const perWeekHours = totalHours / monthIndices.length;
            const perWeekDisplayed = displayed / monthIndices.length;
            monthIndices.forEach((i) => {
              const iso = weeks[i].isoStart;
              state.budgets[cc][iso] = Math.round(perWeekHours * 10) / 10;
              liveBudgets[i] = perWeekDisplayed;
            });
            // Repaint the dot at the month's centre at the new total.
            pt.setAttribute('cy', yFor(displayed).toFixed(1));
            // Repaint the step line — recompute month totals from liveBudgets
            // (only the dragged month's total changes, but recomputing the
            // whole path is cheap).
            let newPath = '';
            monthGroups.forEach((g, gi) => {
              const total = g.indices.reduce((s, i) => s + (liveBudgets[i] || 0), 0);
              const left  = xLeft(g.firstIdx);
              const right = xLeft(g.lastIdx + 1);
              const y     = yFor(total);
              if (gi === 0) newPath += `M ${left.toFixed(1)} ${y.toFixed(1)} `;
              else          newPath += `L ${left.toFixed(1)} ${y.toFixed(1)} `;
              newPath += `L ${right.toFixed(1)} ${y.toFixed(1)} `;
            });
            svg.querySelector('.b-budget-line').setAttribute('d', newPath);
            editor.value = isCost ? `${displayed} €` : `${displayed} h`;
          }
          function placeEditor(clientX, clientY) {
            editor.style.display = 'block';
            editor.style.left = (clientX + 14) + 'px';
            editor.style.top  = (clientY - 28) + 'px';
          }
          function onMove(em) {
            const r = svg.getBoundingClientRect();
            const localY = (em.clientY - r.top) * (H / r.height);
            let v = ((padT + innerH) - localY) / innerH * maxY;
            // Clamp to the chart's visible Y range so the dot and the line never
            // disappear above the chart top during drag. Higher values can still
            // be set via the floating editor input — drawChart on blur rescales.
            v = Math.max(0, Math.min(maxY, v));
            applyDisplayedValue(v);
            placeEditor(em.clientX, em.clientY);
          }
          function commitDrag() {
            saveState();
            drawChart(cc, weeks);
            const monthLabel = weeks[firstIdx].start.toLocaleDateString(undefined, { month: 'short', year: 'numeric' });
            toast(`${cc}: ${fmtV(displayed)} for ${monthLabel} (split across ${monthIndices.length} week${monthIndices.length === 1 ? '' : 's'})`);
          }
          function onUp(em) {
            window.removeEventListener('mousemove', onMove);
            window.removeEventListener('mouseup', onUp);
            // Don't redraw chart yet — let the user fine-tune via the input
            saveState();
            placeEditor(em.clientX, em.clientY);
            editor.focus();
            editor.select();
          }

          // Bind editor handlers fresh each drag (closure-scoped state)
          editor.oninput = () => {
            const v = parseFloat(editor.value.replace(/[^0-9.\-]/g, '')) || 0;
            applyDisplayedValue(v);
          };
          editor.onkeydown = (ev) => {
            if (ev.key === 'Enter') { ev.preventDefault(); editor.blur(); }
            if (ev.key === 'Escape') { ev.preventDefault(); editor.blur(); }
          };
          editor.onblur = () => {
            editor.style.display = 'none';
            saveState();
            drawChart(cc, weeks);
          };

          // Initial position + value
          applyDisplayedValue(displayed);
          placeEditor(e.clientX, e.clientY);

          window.addEventListener('mousemove', onMove);
          window.addEventListener('mouseup', onUp);
        });
      });
    }

    drawList();
  }

  /* -------------------------- Portfolio / People --------------------- */

  function renderPortfolio(root) {
    const view = document.createElement('div');
    view.className = 'view';
    view.innerHTML = `
      <div class="page-head">
        <div><div class="page-title">Portfolio</div><div class="page-sub">${state.projects.length} projects</div></div>
        <div class="page-actions"><button class="ghost" id="btnNewProj2">+ Project</button></div>
      </div>
      <div class="dashboard" id="portfolioGrid">
        ${state.projects
          .filter((p) => state.settings.showArchivedProjects ? true : !p.archived)
          .filter((p) => matchesSearch(p.name, p.description))
          .map((p) => {
          const acts = p.actions || [];
          const total = acts.length;
          const done = acts.filter((a) => a.status === 'done').length;
          const late = acts.filter((a) => a.status !== 'done' && a.due && dayDiff(a.due, todayISO()) < 0).length;
          const pct = total ? Math.round((done / total) * 100) : 0;
          return `
            <div class="kpi clickable ${p.archived ? 'is-archived' : ''}" data-pid="${p.id}" style="grid-column: span 2; cursor:pointer;${p.archived ? ' opacity:.55;' : ''}">
              ${ROW_GRIP_HTML}
              <div class="kpi-label">${p.archived ? '📦 ' : ''}${escapeHTML(p.name)}</div>
              <div class="kpi-value">${pct}%</div>
              <div class="kpi-sub">${total} actions • ${late} late • ${done} done${p.archived ? ' • <em>archived</em>' : ''}</div>
            </div>`;
        }).join('')}
      </div>`;
    root.appendChild(view);
    wireListReorder(view.querySelector('#portfolioGrid'), {
      rowSelector: '.kpi.clickable[data-pid]',
      idAttr: 'pid',
      getArray: () => state.projects,
      setOrder: (ids) => { state.projects = ids.map((id) => state.projects.find((x) => x.id === id)).filter(Boolean); },
      commitName: 'portfolio-reorder',
    });
    $$('.kpi.clickable', view).forEach((el) => {
      el.addEventListener('click', () => {
        state.currentProjectId = el.dataset.pid;
        state.currentView = 'board';
        saveState(); render();
      });
      el.addEventListener('contextmenu', (e) => {
        e.preventDefault();
        const pid = el.dataset.pid;
        const p = state.projects.find((x) => x.id === pid);
        if (!p) return;
        showContextMenu(e.clientX, e.clientY, [
          { icon: '✎', label: 'Edit project…', onClick: () => openProjectEditor(pid) },
          { icon: '⌕', label: 'Open project',  onClick: () => {
            state.currentProjectId = pid;
            state.currentView = 'board';
            saveState(); render();
          }},
          { icon: '⊕', label: 'Save as template…', onClick: () => saveProjectAsTemplate(pid) },
          { divider: true },
          p.archived
            ? { icon: '📤', label: 'Unarchive project', onClick: () => {
                p.archived = false;
                commit('project-unarchive');
                toast(`Unarchived "${p.name}"`);
              }}
            : { icon: '📦', label: 'Archive project', onClick: () => {
                if (state.projects.filter((x) => !x.archived).length <= 1) {
                  toast('Cannot archive the only active project');
                  return;
                }
                p.archived = true;
                if (state.currentProjectId === pid) {
                  state.currentProjectId = state.projects.find((x) => !x.archived)?.id || null;
                }
                commit('project-archive');
                toast(`Archived "${p.name}" — restore from the project picker`);
              }},
          { divider: true },
          { icon: '×', label: 'Delete project', danger: true, onClick: () => {
            if (state.projects.length <= 1) { toast('Cannot delete the only project'); return; }
            const counts = {
              actions: (p.actions || []).length,
              deliverables: (p.deliverables || []).length,
              milestones: (p.milestones || []).length,
              risks: (p.risks || []).length,
            };
            const summary = Object.entries(counts).filter(([, n]) => n > 0).map(([k, n]) => `${n} ${k}`).join(', ');
            const msg = `Delete project "${p.name}"?` + (summary ? `\n\nThis will permanently remove ${summary}.\n\nThis cannot be undone (except via Undo).` : '');
            if (!confirm(msg)) return;
            const typed = prompt(`Type DELETE to confirm permanently removing "${p.name}":`);
            if ((typed || '').trim() !== 'DELETE') { toast('Cancelled — nothing was changed'); return; }
            state.projects = state.projects.filter((x) => x.id !== pid);
            if (state.currentProjectId === pid) state.currentProjectId = state.projects[0]?.id || null;
            commit('project-delete');
            toast('Deleted');
          }},
        ]);
      });
    });
    $('#btnNewProj2').addEventListener('click', () => openQuickAdd('project'));
  }

  /* --------------------- Phase J: project templates -------------------- */
  // Build a structural skeleton from a project — strip ids, action data,
  // decisions, notes, history, comments. Keep components, deliverable
  // stubs, milestone stubs, risk skeletons (title + inherent matrix).
  function projectToTemplate(p, name) {
    const t = {
      id: uid('tpl'),
      name: (name || p.name + ' (template)'),
      createdAt: todayISO(),
      sourceProjectId: p.id,
      shape: {
        description: p.description || '',
        components: (p.components || []).map((c) => ({
          name: c.name, color: c.color, costCenter: c.costCenter || null,
        })),
        deliverables: (p.deliverables || []).map((d) => ({
          name: d.name, status: 'todo',
          // intentionally drop dates — instantiation should re-plan
        })),
        milestones: (p.milestones || []).map((m) => ({
          name: m.name, status: 'planned',
        })),
        risks: (p.risks || []).map((r) => ({
          title: r.title,
          kind: r.kind || 'risk',
          inherent: r.inherent ? { ...r.inherent } : { probability: 0, impact: 0 },
          mitigation: r.mitigation || '',
        })),
        tags: (p.tags || []).map((tg) => ({ name: tg.name, rgb: tg.rgb })),
        // Intentionally NOT cloned: actions, decisions, change requests,
        // notes, links, meetings, history. The shape is the skeleton, not
        // the in-flight work.
      },
    };
    return t;
  }
  function applyTemplateToProject(np, tpl) {
    const shape = tpl.shape || {};
    if (shape.description && !np.description) np.description = shape.description;
    np.components   = (shape.components   || []).map((c) => ({ id: uid('cm'), ...c }));
    np.deliverables = (shape.deliverables || []).map((d) => ({ id: uid('d'), ...d }));
    np.milestones   = (shape.milestones   || []).map((m) => ({ id: uid('m'), ...m }));
    np.risks        = (shape.risks        || []).map((r) => ({
      id: uid('rk'), ...r,
      residual: r.inherent ? { ...r.inherent } : { probability: 0, impact: 0 },
      owner: null,
      actionId: null,
    }));
    np.tags         = (shape.tags         || []).map((t) => ({ id: uid('tg'), ...t }));
  }
  function saveProjectAsTemplate(projectId) {
    const p = state.projects.find((x) => x.id === projectId);
    if (!p) return;
    const suggested = p.name + ' template';
    const name = prompt('Template name:', suggested);
    if (!name) return;
    const tpl = projectToTemplate(p, name.trim());
    state.templates = state.templates || [];
    state.templates.push(tpl);
    commit('template-create');
    toast(`Saved as template: ${tpl.name}`);
  }

  function openProjectEditor(projectId) {
    const p = state.projects.find((x) => x.id === projectId);
    if (!p) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width:520px;">
        <div class="desc-head">
          <div class="desc-title">Edit project</div>
          <button class="icon-btn" id="prClose" title="Close">×</button>
        </div>
        <div style="padding:14px 16px; display:flex; flex-direction:column; gap:10px;">
          <div class="field"><label>Name</label><input id="prName" value="${escapeHTML(p.name)}" /></div>
          <div class="field"><label>Description</label><textarea id="prDesc" style="min-height:100px;">${escapeHTML(p.description || '')}</textarea></div>
        </div>
        <div class="desc-foot">
          <button class="ghost" id="prCancel">Cancel</button>
          <button class="primary" id="prSave">Save</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    setTimeout(() => document.getElementById('prName').focus(), 30);
    const close = () => overlay.remove();
    overlay.querySelector('#prClose').addEventListener('click', close);
    overlay.querySelector('#prCancel').addEventListener('click', close);
    // Modal closes ONLY via the explicit × / Cancel — no backdrop or Escape.
    overlay.querySelector('#prSave').addEventListener('click', () => {
      const name = document.getElementById('prName').value.trim();
      if (!name) return toast('Name required');
      p.name = name;
      p.description = document.getElementById('prDesc').value;
      commit('project-edit');
      close();
      toast('Saved');
    });
  }

  // Compute weekly workload for a person across the next `weeks` weeks.
  // Returns array of { weekStart: Date, count: number, items: action[] }.
  function weeklyLoad(personId, weeks = 12) {
    const today = new Date(); today.setHours(0, 0, 0, 0);
    let weekStart = new Date(today);
    while (weekStart.getDay() !== 1) weekStart = new Date(weekStart.getTime() - dayMs);
    const out = [];
    for (let w = 0; w < weeks; w++) {
      const wStart = new Date(weekStart.getTime() + w * 7 * dayMs);
      const wEnd = new Date(wStart.getTime() + 6 * dayMs);
      const items = [];
      let commitmentSum = 0;
      state.projects.forEach((proj) => {
        (proj.actions || []).forEach((a) => {
          // Archived-but-open work still consumes capacity — archiving
          // hides it from lists, not from a person's real load.
          // Closed actions (done OR cancelled) don't consume capacity
          if (a.owner !== personId || isClosedStatus(a.status)) return;
          if (!a.due) return;
          const due = parseDate(a.due);
          const start = a.startDate ? parseDate(a.startDate) :
            new Date(due.getTime() - 2 * dayMs);
          if (start <= wEnd && due >= wStart) {
            items.push({ a, proj });
            commitmentSum += (typeof a.commitment === 'number') ? a.commitment : 100;
          }
        });
      });
      // `count` is now in % of FTE (commitment sum), not action headcount.
      out.push({ weekStart: wStart, count: commitmentSum, items });
    }
    return out;
  }

  function workloadSparkSVG(person, series) {
    const cap = person.capacity || 5;
    const chartId = `spark-${person.id}`;
    registerChartData(chartId, () => ({
      headers: ['Week start', 'Load %', 'Hours', 'Capacity %'],
      rows: series.map((s) => [
        fmtISO(s.weekStart),
        s.count,
        Math.round((s.count / 100) * HOURS_PER_WEEK),
        cap,
      ]),
    }));
    const W = 480, H = 70;
    const padL = 0, padR = 0, padT = 6, padB = 14;
    const innerW = W - padL - padR;
    const innerH = H - padT - padB;
    const n = series.length;
    const barW = innerW / n;
    const maxVal = Math.max(cap * 1.5, ...series.map((s) => s.count), 4);
    // Capacity baseline y
    const yFor = (v) => padT + innerH - (v / maxVal) * innerH;
    const capY = yFor(cap);

    const bars = series.map((s, i) => {
      const x = padL + i * barW;
      const y = yFor(s.count);
      const h = padT + innerH - y;
      const cls = s.count > cap ? 'over' : s.count > cap * 0.8 ? 'warn' : 'ok';
      const label = s.weekStart.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
      const hours = Math.round((s.count / 100) * HOURS_PER_WEEK);
      return `<rect class="spark-bar ${cls}" x="${x + 2}" y="${y}" width="${Math.max(2, barW - 4)}" height="${Math.max(0, h)}" rx="2">
        <title>Week of ${label} — ${s.count}% FTE (${hours} h) · cap ${cap}%</title>
      </rect>`;
    }).join('');

    const ticks = series.map((s, i) => {
      // Show monthly tick + label at month start
      const isMonthStart = s.weekStart.getDate() <= 7;
      if (!isMonthStart) return '';
      const x = padL + i * barW + barW / 2;
      const lbl = s.weekStart.toLocaleDateString(undefined, { month: 'short' });
      return `<text class="spark-tick" x="${x}" y="${H - 2}" text-anchor="middle">${lbl}</text>`;
    }).join('');

    return `
      <svg class="spark copyable-chart" data-chart="${chartId}" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none" role="img" aria-label="Workload over time">
        <line class="spark-cap" x1="${padL}" x2="${W - padR}" y1="${capY}" y2="${capY}" stroke-dasharray="4 3" />
        <text class="spark-cap-label" x="${W - padR - 4}" y="${Math.max(10, capY - 3)}" text-anchor="end">cap ${cap}%</text>
        ${bars}
        ${ticks}
      </svg>`;
  }

  function renderPeople(root) {
    const view = document.createElement('div');
    view.className = 'view';
    const wl = state.people.map((p) => {
      const open = state.projects.flatMap((pr) => pr.actions || []).filter((a) => a.owner === p.id && a.status !== 'done').length;
      const series = weeklyLoad(p.id, 12);
      const peakWeek = series.reduce((mx, s) => s.count > mx.count ? s : mx, series[0] || { count: 0 });
      return { p, open, series, peakWeek };
    });
    view.innerHTML = `
      <div class="page-head">
        <div><div class="page-title">People</div><div class="page-sub">${state.people.length} members • capacity in % of FTE (1 FTE = 8h/day × 5 days/week, 212 working days/year) • workload across all projects, projected over the next 12 weeks</div></div>
        <div class="page-actions">
          <button class="ghost" id="btnNewPerson">+ Person</button>
        </div>
      </div>
      <div class="panel">
        <div class="panel-title">
          Workload <span class="panel-sub">— bars are % of FTE (1 FTE ≈ ${HOURS_PER_WEEK} h/week)</span>
          <span class="legend">
            <span class="legend-item"><span class="dot ok"></span>≤80% cap</span>
            <span class="legend-item"><span class="dot warn"></span>≤100% cap</span>
            <span class="legend-item"><span class="dot bad"></span>over cap</span>
          </span>
        </div>
        <div id="peopleWl">
          ${wl.filter(({ p }) => matchesSearch(p.name, p.role)).map(({ p, open, series, peakWeek }) => {
            const cap = p.capacity || 100;
            // CONCURRENT commitment, not summed across all open actions:
            // weeklyLoad[0] is the current week's overlapping commitment.
            // Previously this summed every open action regardless of when
            // its [startDate, due] window fell, producing absurd figures
            // (20 actions × 100% = 2000%) when a person had a long backlog.
            const openCmt = series[0]?.count ?? 0;
            const pct = clamp(Math.round((openCmt / cap) * 100), 0, 200);
            const cls = pct > 100 ? 'over' : pct > 80 ? 'warn' : 'ok';
            const peakCls = peakWeek.count > cap ? 'over' : peakWeek.count > cap * 0.8 ? 'warn' : 'ok';
            return `
              <div class="person-row clickable" data-owner-id="${p.id}" title="Click to filter Register to ${escapeHTML(p.name)}">
                ${ROW_GRIP_HTML}
                <div class="name-cell">
                  <span class="avatar">${initials(p.name)}</span>
                  <span class="who">
                    <b>${escapeHTML(p.name)}</b>
                    <span>${escapeHTML(p.role || '')}</span>
                  </span>
                </div>
                <div class="now-load">
                  <div class="bar-track"><div class="bar-fill ${cls}" style="width:${Math.min(100, pct)}%"></div></div>
                  <div class="bar-val">${openCmt}% / ${cap}%</div>
                </div>
                <div class="spark-wrap">
                  ${workloadSparkSVG(p, series)}
                  <div class="spark-meta"><span class="${peakCls}">peak ${peakWeek.count}% (${Math.round((peakWeek.count / 100) * HOURS_PER_WEEK)} h)</span></div>
                </div>
              </div>`;
          }).join('')}
        </div>
      </div>`;
    root.appendChild(view);
    $('#btnNewPerson').addEventListener('click', () => openQuickAdd('person'));
    wireListReorder(view.querySelector('#peopleWl'), {
      rowSelector: '.person-row[data-owner-id]',
      idAttr: 'ownerId',
      getArray: () => state.people,
      setOrder: (ids) => { state.people = ids.map((id) => state.people.find((x) => x.id === id)).filter(Boolean); },
      commitName: 'people-reorder',
    });
    $$('.person-row.clickable', view).forEach((row) => {
      row.addEventListener('click', () => {
        openPersonDashboard(row.dataset.ownerId);
      });
      row.addEventListener('contextmenu', (e) => {
        e.preventDefault();
        const id = row.dataset.ownerId;
        const p = state.people.find((x) => x.id === id);
        showContextMenu(e.clientX, e.clientY, [
          { icon: '◰', label: 'Open dashboard',  onClick: () => openPersonDashboard(id) },
          { icon: '✎', label: 'Edit person…',    onClick: () => openPersonEditor(id) },
          { icon: '⌕', label: `Filter Register to ${p?.name || ''}`, onClick: () => applyTopbarFilter({ owner: id, view: 'register' }) },
          { divider: true },
          { icon: '×', label: 'Delete person…', danger: true, onClick: () => {
            const open = state.projects.flatMap((pr) => pr.actions || []).filter((a) => a.owner === id && a.status !== 'done').length;
            if (!confirm(`Delete ${p?.name}?` + (open ? ` (${open} open action${open === 1 ? '' : 's'} will be unassigned).` : ''))) return;
            state.projects.forEach((pr) => (pr.actions || []).forEach((a) => { if (a.owner === id) a.owner = null; }));
            state.people = state.people.filter((x) => x.id !== id);
            commit('person-delete');
            toast('Deleted');
          }},
        ]);
      });
    });
  }

  /* ---------------------- Phase I: Person dashboard -------------------- */
  // Opens the existing drawer with an aggregated read-only view of one
  // person: open / late actions, weekly load + spark, originated CRs,
  // decisions made, owned risks with non-trivial residual. Each list item
  // routes to its underlying drawer / editor.
  function openPersonDashboard(personId) {
    const p = state.people.find((x) => x.id === personId);
    if (!p) return;
    $('#drawerTitle').textContent = 'Person dashboard';
    const today = todayISO();
    const allActs = state.projects.flatMap((pr) =>
      (pr.actions || []).filter((a) => !a.archivedAt && a.owner === p.id).map((a) => ({ a, pr })));
    const open  = allActs.filter(({ a }) => !isClosedStatus(a.status));
    const late  = open.filter(({ a }) => a.due && dayDiff(a.due, today) < 0);
    const blocked = open.filter(({ a }) => a.status === 'blocked');
    const allCRs       = state.projects.flatMap((pr) => (pr.changes || []).map((c) => ({ c, pr })));
    const myCRs        = allCRs.filter(({ c }) => c.originator === p.id);
    const myDecisions  = state.projects.flatMap((pr) => (pr.decisions || []).filter((d) => d.owner === p.id).map((d) => ({ d, pr })));
    const myRisks      = state.projects.flatMap((pr) =>
      (pr.risks || []).filter((r) => r.owner === p.id).map((r) => {
        const res = r.residual || r.inherent || { probability: 0, impact: 0 };
        return { r, pr, _score: (res.probability || 0) * (res.impact || 0) };
      }))
      .filter((x) => x._score >= 6)
      .sort((a, b) => b._score - a._score);

    const series = weeklyLoad(p.id, 12);
    const cap = p.capacity || 100;
    const peakWeek = series.reduce((mx, s) => s.count > mx.count ? s : mx, series[0] || { count: 0 });
    // Concurrent commitment for the current week (see renderPeople bar
    // comment) — summing all open actions over-counts non-overlapping ones.
    const openCmt = series[0]?.count ?? 0;
    const pct = clamp(Math.round((openCmt / cap) * 100), 0, 200);
    const cls = pct > 100 ? 'over' : pct > 80 ? 'warn' : 'ok';

    const listRow = (title, sub, run) => `
      <div class="dash-row${run ? ' clickable' : ''}" data-run="1">
        <div class="dash-row-title">${escapeHTML(title)}</div>
        <div class="dash-row-sub">${escapeHTML(sub)}</div>
      </div>`;
    const listOrEmpty = (title, count, html) => `
      <div class="dash-section">
        <div class="dash-section-title">${escapeHTML(title)}<span class="dash-section-count">${count}</span></div>
        ${count ? html : '<div class="empty">— none</div>'}
      </div>`;

    $('#drawerBody').innerHTML = `
      <div class="dash-head">
        <span class="avatar lg">${initials(p.name)}</span>
        <div class="dash-head-body">
          <div class="dash-name">${escapeHTML(p.name)}</div>
          <div class="dash-role">${escapeHTML(p.role || '—')}</div>
          <div class="dash-head-actions">
            <button class="ghost" id="dashEdit">Edit person…</button>
            <button class="ghost" id="dashFilter">Filter Register to ${escapeHTML(p.name)}</button>
          </div>
        </div>
      </div>
      <div class="dash-kpis">
        <div class="dash-kpi"><div class="dash-kpi-num">${open.length}</div><div class="dash-kpi-lbl">Open</div></div>
        <div class="dash-kpi ${late.length ? 'bad' : ''}"><div class="dash-kpi-num">${late.length}</div><div class="dash-kpi-lbl">Late</div></div>
        <div class="dash-kpi ${blocked.length ? 'warn' : ''}"><div class="dash-kpi-num">${blocked.length}</div><div class="dash-kpi-lbl">Blocked</div></div>
        <div class="dash-kpi ${cls === 'over' ? 'bad' : cls === 'warn' ? 'warn' : ''}"><div class="dash-kpi-num">${pct}%</div><div class="dash-kpi-lbl">Load</div></div>
      </div>
      <div class="dash-section">
        <div class="dash-section-title">Workload — next 12 weeks<span class="dash-section-count">peak ${peakWeek.count}%</span></div>
        ${workloadSparkSVG(p, series)}
      </div>
      ${listOrEmpty('Late actions', late.length,
        late.slice(0, 12).map(({ a, pr }) => `
          <div class="dash-row clickable" data-action-id="${a.id}">
            <div class="dash-row-title">${escapeHTML(a.title)}</div>
            <div class="dash-row-sub">${escapeHTML(pr.name)} · ${Math.abs(dayDiff(a.due, today))}d late</div>
          </div>`).join(''))}
      ${listOrEmpty('Open actions', open.length,
        open.slice(0, 20).map(({ a, pr }) => `
          <div class="dash-row clickable" data-action-id="${a.id}">
            <div class="dash-row-title">${escapeHTML(a.title)}</div>
            <div class="dash-row-sub">${escapeHTML(pr.name)} · ${escapeHTML(a.status)}${a.due ? ' · due ' + a.due : ''}</div>
          </div>`).join(''))}
      ${listOrEmpty('Originated change requests', myCRs.length,
        myCRs.slice(0, 12).map(({ c, pr }) => `
          <div class="dash-row clickable" data-cr-id="${c.id}" data-proj-id="${pr.id}">
            <div class="dash-row-title">${escapeHTML(c.title)}</div>
            <div class="dash-row-sub">${escapeHTML(pr.name)} · ${escapeHTML(c.status)}${c.decisionDate ? ' · ' + c.decisionDate : ''}</div>
          </div>`).join(''))}
      ${listOrEmpty('Decisions made', myDecisions.length,
        myDecisions.slice(0, 12).map(({ d, pr }) => `
          <div class="dash-row" data-dec-id="${d.id}">
            <div class="dash-row-title">${escapeHTML(d.title)}</div>
            <div class="dash-row-sub">${escapeHTML(pr.name)}${d.date ? ' · ' + d.date : ''}</div>
          </div>`).join(''))}
      ${listOrEmpty('Owned risks (residual ≥ 6)', myRisks.length,
        myRisks.slice(0, 12).map(({ r, pr, _score }) => `
          <div class="dash-row clickable" data-risk-id="${r.id}" data-proj-id="${pr.id}">
            <div class="dash-row-title">${escapeHTML(r.title)}</div>
            <div class="dash-row-sub">${escapeHTML(pr.name)} · residual ${_score}</div>
          </div>`).join(''))}`;
    $('#drawer').hidden = false;
    $('#dashEdit').addEventListener('click', () => openPersonEditor(personId));
    $('#dashFilter').addEventListener('click', () => {
      closeDrawer();
      applyTopbarFilter({ owner: personId, view: 'register' });
    });
    $('#drawerBody').querySelectorAll('[data-action-id]').forEach((row) => {
      row.addEventListener('click', () => openDrawer(row.dataset.actionId));
    });
    $('#drawerBody').querySelectorAll('[data-cr-id]').forEach((row) => {
      row.addEventListener('click', () => {
        state.currentProjectId = row.dataset.projId;
        openChangeRequestEditor(row.dataset.crId);
      });
    });
    $('#drawerBody').querySelectorAll('[data-risk-id]').forEach((row) => {
      row.addEventListener('click', () => {
        state.currentProjectId = row.dataset.projId;
        openRiskEditor(row.dataset.riskId);
      });
    });
  }

  /* -------------------------- Detail drawer -------------------------- */

  function openDrawer(actionId) {
    const proj = curProject();
    const a = proj.actions.find((x) => x.id === actionId);
    if (!a) return;
    $('#drawerTitle').textContent = 'Action details';
    const body = $('#drawerBody');
    body.innerHTML = `
      <div class="field"><label>Title</label><input id="dTitle" value="${escapeHTML(a.title)}" /></div>
      <div style="display:grid; grid-template-columns:1fr 1fr 1fr; gap:10px;">
        <div class="field"><label>Owner</label>
          <select id="dOwner">${actorOptionsHTML(a.owner)}</select>
        </div>
        <div class="field"><label>Originator</label>
          <select id="dOriginator">${actorOptionsHTML(a.originator, true)}</select>
        </div>
        <div class="field"><label>Originator date</label>
          <input id="dOriginatorDate" type="date" value="${a.originatorDate || a.createdAt || ''}" title="When this action was originated" />
        </div>
      </div>
      <div style="display:grid; grid-template-columns:1fr 1fr 1fr; gap:10px;">
        <div class="field"><label>Status</label>
          <select id="dStatus">${STATUSES.map((s) => `<option value="${s.id}" ${s.id === a.status ? 'selected' : ''}>${s.name}</option>`).join('')}</select>
        </div>
        <div class="field"><label>Priority</label>
          <select id="dPriorityLevel">${PRIORITY_LEVELS.map((p) => `<option value="${p.id}" ${p.id === (a.priorityLevel || 'med') ? 'selected' : ''}>${p.label}</option>`).join('')}</select>
        </div>
        <div class="field"><label>Due</label><input id="dDue" type="date" value="${a.due || ''}" /></div>
      </div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:10px;">
        <div class="field">
          <label>Start date <span class="muted" id="dStartMode">${a.status === 'todo' ? 'set once Status leaves Not started' : (a.startDate ? 'override' : 'auto: today when moved past Not started')}</span></label>
          <input id="dStartDate" type="date" value="${a.startDate || ''}" ${a.status === 'todo' ? 'disabled' : ''} title="When work on this action began. Auto-filled the day the status first leaves Not started — override here if the actual start was earlier or later." />
        </div>
        <div class="field">
          <label>Completed date <span class="muted" id="dCompletedMode">${a.status === 'done' ? (a.completedAt ? 'override' : 'auto: today when Done was set') : 'set Status = Done first'}</span></label>
          <input id="dCompletedAt" type="date" value="${a.completedAt || (a.status === 'done' ? actionCompletedAt(a) || '' : '')}" ${a.status === 'done' ? '' : 'disabled'} title="When the work actually landed. Auto-filled the day the status flips to Done — override here if the completion happened on a different date." />
        </div>
      </div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:10px;">
        <div class="field">
          <label>Commitment <span class="muted" id="dCmtVal">${typeof a.commitment === 'number' ? a.commitment : 100}%</span></label>
          <input id="dCmt" type="range" min="5" max="100" step="5" value="${typeof a.commitment === 'number' ? a.commitment : 100}" />
        </div>
        <div class="field">
          <label>% complete <span class="muted" id="dPctMode">${typeof a.percentComplete === 'number' ? 'manual' : 'auto from due date'}</span></label>
          <div style="display:flex; gap:6px; align-items:center; min-width: 0;">
            <input id="dPct" type="range" min="0" max="100" step="1" value="${percentComplete(a)}" style="flex:1; min-width: 0;" />
            <input id="dPctNum" type="number" min="0" max="100" step="1" value="${percentComplete(a)}" style="width: 56px;" />
            <button type="button" class="ghost" id="dPctAuto" title="Reset to auto — % complete is computed from elapsed time between Originated and Due dates" style="padding: 3px 7px; font-size: 11px;">auto</button>
          </div>
        </div>
      </div>
      <div class="field"><label>Component</label>
        <select id="dComponent"><option value="">—</option>${(proj.components || []).map((pt) => `<option value="${pt.id}" ${pt.id === a.component ? 'selected' : ''}>${escapeHTML(pt.name)}</option>`).join('')}</select>
      </div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:10px;">
        <div class="field"><label>Deliverable</label>
          <select id="dDel"><option value="">—</option>${(proj.deliverables || []).map((d) => `<option value="${d.id}" ${d.id === a.deliverable ? 'selected' : ''}>${escapeHTML(d.name)}</option>`).join('')}</select>
        </div>
        <div class="field"><label>Milestone</label>
          <select id="dMile"><option value="">—</option>${(proj.milestones || []).map((m) => `<option value="${m.id}" ${m.id === a.milestone ? 'selected' : ''}>${escapeHTML(m.name)}</option>`).join('')}</select>
        </div>
      </div>
      <div class="field"><label>Depends on <span class="muted">— actions that must finish before this one</span></label>
        <div class="depends-input" id="dDependsWrap"></div>
      </div>
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px;">
        <div class="field">
          <label>Recurring <span class="muted">— spawn next instance when marked done</span></label>
          <div style="display: flex; gap: 6px; align-items: center;">
            <select id="dRecurUnit">
              <option value=""      ${!a.recurrence                    ? 'selected' : ''}>— no recurrence —</option>
              <option value="day"   ${a.recurrence?.unit === 'day'      ? 'selected' : ''}>Daily</option>
              <option value="week"  ${a.recurrence?.unit === 'week'     ? 'selected' : ''}>Weekly</option>
              <option value="month" ${a.recurrence?.unit === 'month'    ? 'selected' : ''}>Monthly</option>
            </select>
            <span class="muted" id="dRecurEvery"${!a.recurrence ? ' style="display:none;"' : ''}>every</span>
            <input id="dRecurInterval" type="number" min="1" max="52" value="${a.recurrence?.interval || 1}" style="width:56px;${!a.recurrence ? ' display:none;' : ''}" />
          </div>
        </div>
        <div class="field">
          <label>Snooze until <span class="muted">— hide from views until this date</span></label>
          <input id="dSnoozeUntil" type="date" value="${escapeHTML(a.snoozedUntil || '')}" />
        </div>
      </div>
      <div class="field">
        <label>Tasks <span class="muted" id="dTasksProg"></span></label>
        <div class="tasks-editor" id="dTasksEditor"></div>
      </div>
      <div class="field"><label>Tags</label>
        <div class="tags-input" id="dTagsWrap"></div>
      </div>
      <div class="field">
        <label>Effort tracking <span class="muted">— actuals feed CPI / EAC on the Dashboard</span></label>
        <div class="effort-track" id="dEffort"></div>
      </div>
      <div class="field"><label>Notes / justification</label><textarea id="dNotes">${escapeHTML(a.notes || '')}</textarea></div>
      <div class="field"><label>Comments</label>
        <div class="comments" id="dComments"></div>
      </div>
      <div class="field"><label>History</label>
        <div class="history">${(() => {
          // Merge state-transitions (a.history) with audit edits (a.__history)
          // into a single chronological list, newest first.
          const transitions = (a.history || []).map((h) => ({ at: h.at, by: null, summary: h.what }));
          const audits      = (a.__history || []).map((h) => ({ at: h.at, by: h.by, summary: h.summary }));
          const merged = [...transitions, ...audits].sort((x, y) => (y.at || '').localeCompare(x.at || ''));
          if (!merged.length) return '<div class="history-item muted">No history yet — saving an edit will record an entry here.</div>';
          return merged.slice(0, 24).map((h) =>
            `<div class="history-item"><b>${escapeHTML(h.at || '')}</b>${h.by ? ` <span class="muted">· ${escapeHTML(h.by)}</span>` : ''} — ${escapeHTML(h.summary || '')}</div>`
          ).join('');
        })()}</div>
      </div>
      <div style="display:flex; gap:8px; margin-top:6px;">
        <button class="primary" id="dSave">Save</button>
        <button class="ghost" id="dDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
      </div>`;
    $('#drawer').hidden = false;
    $('#dCmt').addEventListener('input', (e) => { $('#dCmtVal').textContent = e.target.value + '%'; });
    // % complete — manual override. Slider and number input mirror
    // each other; pressing "auto" clears the override so the value
    // returns to the time-based default.
    const pctRange = $('#dPct'), pctNum = $('#dPctNum'), pctMode = $('#dPctMode');
    const setPctManual = (v) => {
      const n = Math.max(0, Math.min(100, Math.round(+v || 0)));
      pctRange.value = n;
      pctNum.value = n;
      pctMode.textContent = 'manual';
    };
    pctRange?.addEventListener('input', (e) => setPctManual(e.target.value));
    pctNum?.addEventListener('input',   (e) => setPctManual(e.target.value));
    $('#dPctAuto')?.addEventListener('click', () => {
      // Clear manual override; show what auto would give.
      a.percentComplete = null;
      const auto = percentComplete(a);
      pctRange.value = auto;
      pctNum.value = auto;
      pctMode.textContent = 'auto from due date';
    });
    // Recurrence — show / hide the interval input based on the selected unit.
    $('#dRecurUnit')?.addEventListener('change', (e) => {
      const on = !!e.target.value;
      const every = $('#dRecurEvery'), input = $('#dRecurInterval');
      if (every) every.style.display = on ? '' : 'none';
      if (input) input.style.display = on ? '' : 'none';
    });

    // Depends-on multi-select. Local working list mirrored back to a.dependsOn
    // on save. Self-references and would-be cycles are filtered from the
    // candidate dropdown so users can't create them in the first place.
    const drawerDeps = Array.isArray(a.dependsOn) ? a.dependsOn.slice() : [];
    function reverseDepGraph(actsList) {
      // For cycle prevention: collect every action that already (transitively)
      // depends on the action we're editing — those can't be added as deps.
      const reverse = new Map();
      actsList.forEach((x) => (x.dependsOn || []).forEach((depId) => {
        if (!reverse.has(depId)) reverse.set(depId, new Set());
        reverse.get(depId).add(x.id);
      }));
      const blocked = new Set();
      const stack = [a.id];
      while (stack.length) {
        const id = stack.pop();
        if (blocked.has(id)) continue;
        blocked.add(id);
        (reverse.get(id) || []).forEach((parent) => stack.push(parent));
      }
      return blocked;
    }
    function renderDepsUI() {
      const wrap = $('#dDependsWrap');
      const acts = (proj.actions || []).filter((x) => !x.archivedAt);
      const byId = new Map(acts.map((x) => [x.id, x]));
      const blocked = reverseDepGraph(acts); // includes a.id itself
      wrap.innerHTML = `
        <div class="depends-chips">
          ${drawerDeps.map((id) => {
            const dep = byId.get(id);
            if (!dep) return '';
            return `<span class="dep-chip" data-id="${escapeHTML(id)}">
              <span class="dep-chip-text">${escapeHTML(dep.title)}</span>
              <button type="button" class="dep-chip-x" data-action="remove" title="Remove">×</button>
            </span>`;
          }).join('')}
        </div>
        <div class="depends-search" data-empty="Add a dependency…">
          <input type="text" id="dDepSearch" placeholder="Type to search actions…" autocomplete="off" />
          <div class="depends-results" id="dDepResults" hidden></div>
        </div>`;
      wrap.querySelectorAll('[data-action="remove"]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.closest('.dep-chip')?.dataset.id;
          const idx = drawerDeps.indexOf(id);
          if (idx >= 0) drawerDeps.splice(idx, 1);
          renderDepsUI();
        });
      });
      const inp = $('#dDepSearch');
      const results = $('#dDepResults');
      const updateResults = () => {
        const q = inp.value.trim().toLowerCase();
        const candidates = acts.filter((x) =>
          x.id !== a.id &&
          !drawerDeps.includes(x.id) &&
          !blocked.has(x.id));
        const ranked = q
          ? candidates.map((x) => ({ x, s: fuzzyScore(q, (x.title + ' ' + personName(x.owner))) }))
              .filter((r) => r.s > 0)
              .sort((p, q2) => q2.s - p.s)
          : candidates.slice(0, 8).map((x) => ({ x, s: 0 }));
        const list = ranked.slice(0, 8);
        if (!list.length) {
          results.innerHTML = '<div class="depends-empty">No matches</div>';
        } else {
          results.innerHTML = list.map((r) => `
            <div class="depends-result" data-id="${escapeHTML(r.x.id)}">
              <span class="depends-result-title">${escapeHTML(r.x.title)}</span>
              <span class="depends-result-meta">${escapeHTML(personName(r.x.owner))}${r.x.due ? ' · ' + r.x.due : ''}</span>
            </div>`).join('');
        }
        results.hidden = false;
        results.querySelectorAll('.depends-result').forEach((row) => {
          row.addEventListener('mousedown', (e) => {
            e.preventDefault();
            const id = row.dataset.id;
            if (!drawerDeps.includes(id)) drawerDeps.push(id);
            renderDepsUI();
            setTimeout(() => $('#dDepSearch')?.focus(), 0);
          });
        });
      };
      inp.addEventListener('focus', updateResults);
      inp.addEventListener('input', updateResults);
      inp.addEventListener('blur', () => setTimeout(() => { results.hidden = true; }, 120));
    }
    renderDepsUI();

    // Phase G — tags multi-select. Tags are project-scoped; new tags are
    // appended to proj.tags. Removing a chip just removes the id from the
    // record's tags array (does not delete the project tag).
    const drawerTags = Array.isArray(a.tags) ? a.tags.slice() : [];
    function renderTagsUI() {
      const wrap = $('#dTagsWrap');
      const projTags = proj.tags || [];
      const byId = new Map(projTags.map((t) => [t.id, t]));
      const remaining = projTags.filter((t) => !drawerTags.includes(t.id));
      wrap.innerHTML = `
        <div class="tags-chips">
          ${drawerTags.map((id) => {
            const t = byId.get(id);
            if (!t) return '';
            const rgb = t.rgb || '120, 120, 140';
            return `<span class="dep-chip tag-chip-edit" data-id="${escapeHTML(id)}" style="background:rgba(${rgb},.18);color:rgb(${rgb});border:1px solid rgba(${rgb},.40)">
              <span class="dep-chip-text">${escapeHTML(t.name)}</span>
              <button type="button" class="dep-chip-x" data-action="remove-tag" title="Remove">×</button>
            </span>`;
          }).join('')}
        </div>
        <div class="depends-search">
          <input type="text" id="dTagSearch" placeholder="${remaining.length || drawerTags.length ? 'Type to search or create…' : 'Type a name to create your first tag…'}" autocomplete="off" />
          <div class="depends-results" id="dTagResults" hidden></div>
        </div>`;
      wrap.querySelectorAll('[data-action="remove-tag"]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.closest('.tag-chip-edit')?.dataset.id;
          const idx = drawerTags.indexOf(id);
          if (idx >= 0) drawerTags.splice(idx, 1);
          renderTagsUI();
        });
      });
      const inp = $('#dTagSearch');
      const results = $('#dTagResults');
      const updateResults = () => {
        const q = inp.value.trim();
        const candidates = (proj.tags || []).filter((t) => !drawerTags.includes(t.id));
        const ranked = q
          ? candidates.map((t) => ({ t, s: fuzzyScore(q, t.name) })).filter((r) => r.s > 0).sort((p, q2) => q2.s - p.s)
          : candidates.slice(0, 8).map((t) => ({ t, s: 0 }));
        const list = ranked.slice(0, 8);
        const exact = q && (proj.tags || []).find((t) => t.name.toLowerCase() === q.toLowerCase());
        const canCreate = q && !exact;
        let html = list.map((r) => {
          const rgb = r.t.rgb || '120, 120, 140';
          return `<div class="depends-result tag-result" data-id="${escapeHTML(r.t.id)}">
            <span class="tag-color-dot" style="background:rgb(${rgb})"></span>
            <span class="depends-result-title">${escapeHTML(r.t.name)}</span>
          </div>`;
        }).join('');
        if (canCreate) {
          html += `<div class="depends-result tag-create" data-create="${escapeHTML(q)}">
            <span class="tag-color-dot" style="background:rgb(${TAG_PALETTE[(proj.tags || []).length % TAG_PALETTE.length]})"></span>
            <span class="depends-result-title">+ Create "<b>${escapeHTML(q)}</b>"</span>
          </div>`;
        }
        if (!list.length && !canCreate) {
          html = '<div class="depends-empty">Type a name to create a new tag.</div>';
        }
        results.innerHTML = html;
        results.hidden = false;
        results.querySelectorAll('.tag-result').forEach((row) => {
          row.addEventListener('mousedown', (e) => {
            e.preventDefault();
            const id = row.dataset.id;
            if (!drawerTags.includes(id)) drawerTags.push(id);
            inp.value = '';
            renderTagsUI();
            setTimeout(() => $('#dTagSearch')?.focus(), 0);
          });
        });
        results.querySelector('.tag-create')?.addEventListener('mousedown', (e) => {
          e.preventDefault();
          const name = e.currentTarget.dataset.create;
          const t = { id: uid('tg'), name, rgb: TAG_PALETTE[(proj.tags || []).length % TAG_PALETTE.length] };
          proj.tags = proj.tags || [];
          proj.tags.push(t);
          drawerTags.push(t.id);
          inp.value = '';
          renderTagsUI();
          setTimeout(() => $('#dTagSearch')?.focus(), 0);
        });
      };
      inp.addEventListener('focus', updateResults);
      inp.addEventListener('input', updateResults);
      inp.addEventListener('blur', () => setTimeout(() => { results.hidden = true; }, 120));
    }
    renderTagsUI();

    // Tasks (sub-actions) — simple decomposition of an action into
    // 5-status items. Mutations commit immediately (like op-steps) so
    // ticking a task doesn't get lost if the user cancels the drawer.
    // Cycle order on click: todo → doing → done → todo. Blocked and
    // Cancelled are one click each via the right-click menu.
    const TASK_CYCLE = ['todo', 'doing', 'done'];
    const TASK_STATUS_LABEL = { todo: 'Not started', doing: 'In progress', blocked: 'Blocked', done: 'Done', cancelled: 'Cancelled' };
    const TASK_STATUS_ICON  = { todo: '○',           doing: '◐',           blocked: '◼',       done: '✓',    cancelled: '✕' };
    function taskProgressStats(list) {
      const items = (list || []).filter((t) => t.status !== 'cancelled');
      const total = items.length;
      const done = items.filter((t) => t.status === 'done').length;
      const doing = items.filter((t) => t.status === 'doing').length;
      const blocked = items.filter((t) => t.status === 'blocked').length;
      return { total, done, doing, blocked };
    }
    function renderTasksUI() {
      const wrap = $('#dTasksEditor');
      if (!wrap) return;
      const list = a.tasks || [];
      const stats = taskProgressStats(list);
      const prog = $('#dTasksProg');
      if (prog) {
        prog.textContent = stats.total
          ? `${stats.done}/${stats.total} done${stats.blocked ? ` · ${stats.blocked} blocked` : ''}`
          : '';
      }
      wrap.innerHTML = `
        <div class="tasks-list">
          ${list.map((t) => `
            <div class="task-row status-${t.status}" data-task-id="${escapeHTML(t.id)}">
              <span class="task-grip" title="Drag to reorder" aria-hidden="true">⋮⋮</span>
              <button type="button" class="task-status-pill status-${t.status}" title="Click to cycle · right-click for Blocked / Cancelled">
                <span class="task-status-dot"></span>
                <span class="task-status-lbl">${escapeHTML(TASK_STATUS_LABEL[t.status])}</span>
              </button>
              <span class="task-title" contenteditable="true" data-placeholder="Task title…">${escapeHTML(t.title)}</span>
              <button type="button" class="task-del" title="Delete task" aria-label="Delete task">×</button>
            </div>`).join('')}
        </div>
        <button type="button" class="task-add">+ Add task</button>`;
      // Cycle on left-click.
      wrap.querySelectorAll('.task-status-pill').forEach((btn) => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const id = btn.closest('.task-row').dataset.taskId;
          const t = (a.tasks || []).find((x) => x.id === id);
          if (!t) return;
          // Off-cycle statuses (blocked, cancelled) fall back to todo when clicked.
          const idx = TASK_CYCLE.indexOf(t.status);
          t.status = idx === -1 ? 'doing' : TASK_CYCLE[(idx + 1) % TASK_CYCLE.length];
          commit('task-cycle');
          renderTasksUI();
        });
      });
      // Right-click menu for off-cycle statuses.
      wrap.querySelectorAll('.task-status-pill').forEach((btn) => {
        btn.addEventListener('contextmenu', (e) => {
          e.preventDefault();
          const id = btn.closest('.task-row').dataset.taskId;
          const t = (a.tasks || []).find((x) => x.id === id);
          if (!t) return;
          const items = ['todo', 'doing', 'blocked', 'done', 'cancelled'].map((s) => ({
            icon: TASK_STATUS_ICON[s],
            label: TASK_STATUS_LABEL[s],
            onClick: () => { t.status = s; commit('task-set-status'); renderTasksUI(); },
          }));
          showContextMenu(e.clientX, e.clientY, items);
        });
      });
      // Inline title edit.
      wrap.querySelectorAll('.task-title').forEach((txt) => {
        txt.addEventListener('keydown', (e) => {
          if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); txt.blur(); }
          else if (e.key === 'Backspace' && txt.textContent === '') {
            e.preventDefault();
            const id = txt.closest('.task-row').dataset.taskId;
            a.tasks = (a.tasks || []).filter((x) => x.id !== id);
            commit('task-delete');
            renderTasksUI();
          }
        });
        txt.addEventListener('blur', () => {
          const id = txt.closest('.task-row').dataset.taskId;
          const t = (a.tasks || []).find((x) => x.id === id);
          if (!t) return;
          const v = txt.textContent.trim();
          if (t.title !== v) { t.title = v; commit('task-edit'); }
        });
      });
      // Delete
      wrap.querySelectorAll('.task-del').forEach((btn) => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          const id = btn.closest('.task-row').dataset.taskId;
          a.tasks = (a.tasks || []).filter((x) => x.id !== id);
          commit('task-delete');
          renderTasksUI();
        });
      });
      // Add
      wrap.querySelector('.task-add')?.addEventListener('click', () => {
        a.tasks = a.tasks || [];
        const newTask = { id: uid('tk'), title: '', status: 'todo' };
        a.tasks.push(newTask);
        commit('task-add');
        renderTasksUI();
        setTimeout(() => {
          const el = wrap.querySelector(`.task-row[data-task-id="${newTask.id}"] .task-title`);
          if (el) {
            el.focus();
            const range = document.createRange();
            range.selectNodeContents(el);
            range.collapse(false);
            const sel = window.getSelection();
            sel.removeAllRanges(); sel.addRange(range);
          }
        }, 0);
      });
      // Drag reorder — reuse the mouse-drag pattern from op-steps.
      const listEl = wrap.querySelector('.tasks-list');
      wrap.querySelectorAll('.task-grip').forEach((grip) => {
        grip.addEventListener('mousedown', (e) => {
          e.preventDefault();
          const row = grip.closest('.task-row');
          if (!row || !listEl) return;
          row.classList.add('dragging');
          document.body.classList.add('is-step-dragging');
          const onMove = (em) => {
            const siblings = [...listEl.querySelectorAll('.task-row:not(.dragging)')];
            const after = siblings.find((sib) => {
              const r = sib.getBoundingClientRect();
              return em.clientY < r.top + r.height / 2;
            });
            if (after) listEl.insertBefore(row, after);
            else listEl.appendChild(row);
          };
          const onUp = () => {
            row.classList.remove('dragging');
            document.body.classList.remove('is-step-dragging');
            window.removeEventListener('mousemove', onMove);
            window.removeEventListener('mouseup', onUp);
            const before = (a.tasks || []).map((t) => t.id).join(',');
            const byId = Object.fromEntries((a.tasks || []).map((t) => [t.id, t]));
            const newOrderIds = [...listEl.querySelectorAll('.task-row')].map((r) => r.dataset.taskId);
            a.tasks = newOrderIds.map((id) => byId[id]).filter(Boolean);
            if (a.tasks.map((t) => t.id).join(',') !== before) commit('task-reorder');
          };
          window.addEventListener('mousemove', onMove);
          window.addEventListener('mouseup', onUp);
        });
      });
    }
    renderTasksUI();

    // Phase G — comments thread. Local working list; mutations are persisted
    // immediately on Post (independent of Save) so a user typing a long
    // comment doesn't lose it if they hit Cancel on the rest.
    function renderCommentsUI() {
      const wrap = $('#dComments');
      const items = (a.comments || []).slice().sort((x, y) => (x.at || '').localeCompare(y.at || ''));
      wrap.innerHTML = `
        <div class="comments-list">
          ${items.length ? items.map((c) => {
            const author = personName(c.by) || (c.by ? c.by : 'someone');
            const when = c.at ? c.at.replace('T', ' ').slice(0, 16) : '';
            return `<div class="comment-row" data-comment-id="${escapeHTML(c.id)}">
              <div class="comment-head">
                <span class="comment-author">${escapeHTML(author)}</span>
                <span class="comment-when">${escapeHTML(when)}</span>
                <button class="comment-del" data-action="del-comment" title="Delete comment">×</button>
              </div>
              <div class="comment-text">${escapeHTML(c.text)}</div>
            </div>`;
          }).join('') : '<div class="comments-empty">No comments yet.</div>'}
        </div>
        <div class="comment-compose">
          <textarea id="dCommentNew" placeholder="Add a comment…" rows="2"></textarea>
          <button class="ghost" id="dCommentPost" disabled>Post</button>
        </div>`;
      wrap.querySelectorAll('[data-action="del-comment"]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.closest('[data-comment-id]')?.dataset.commentId;
          if (!id) return;
          if (!confirm('Delete this comment?')) return;
          a.comments = (a.comments || []).filter((c) => c.id !== id);
          a.updatedAt = todayISO();
          commit('comment-delete');
          // Re-render the drawer so the comment thread updates immediately
          // without re-opening, but other drawer fields keep their state.
          renderCommentsUI();
        });
      });
      const ta = $('#dCommentNew');
      const post = $('#dCommentPost');
      ta?.addEventListener('input', () => { post.disabled = !ta.value.trim(); });
      post?.addEventListener('click', () => {
        const text = ta.value.trim();
        if (!text) return;
        a.comments = a.comments || [];
        a.comments.push({
          id: uid('cm'),
          by: state.settings?.localUser || null,
          at: new Date().toISOString(),
          text,
        });
        a.updatedAt = todayISO();
        commit('comment-add');
        renderCommentsUI();
      });
    }
    renderCommentsUI();

    // ── Effort tracking widget — actuals + baseline ──────────────────
    // Rendered inline so the user can log time and freeze a baseline
    // without leaving the drawer. Mutations commit immediately (separate
    // from the main Save) so log-time-then-cancel doesn't lose data.
    function renderEffortUI() {
      const el = $('#dEffort'); if (!el) return;
      const ah  = totalLoggedHours(a);
      const ph  = plannedHoursTotal(a);
      const evm = evmForActions(a);
      const rate = ownerRate(a);
      const bl  = a.baseline;
      const slip = baselineSlipDays(a); // +late vs baseline
      const ahPct = ph > 0 ? Math.min(200, Math.round((ah / ph) * 100)) : 0;
      el.innerHTML = `
        <div class="effort-grid">
          <div class="effort-cell"><span class="effort-lbl">Logged</span><b>${ah.toFixed(1)} h</b><span class="effort-sub">@ €${rate}/h = €${Math.round(evm.ac).toLocaleString()}</span></div>
          <div class="effort-cell"><span class="effort-lbl">Planned (BAC)</span><b>${ph.toFixed(1)} h</b><span class="effort-sub">€${Math.round(evm.bac).toLocaleString()}</span></div>
          <div class="effort-cell"><span class="effort-lbl">CPI</span><b class="${evm.cpi == null ? 'muted' : evm.cpi >= 1 ? 'ok' : 'warn'}">${evm.cpi == null ? '—' : evm.cpi.toFixed(2)}</b><span class="effort-sub">${evm.cpi == null ? 'no actuals' : evm.cpi >= 1 ? 'under budget' : 'over budget'}</span></div>
          <div class="effort-cell"><span class="effort-lbl">SPI</span><b class="${evm.spi == null ? 'muted' : evm.spi >= 1 ? 'ok' : 'warn'}">${evm.spi == null ? '—' : evm.spi.toFixed(2)}</b><span class="effort-sub">${evm.spi == null ? 'no progress yet' : evm.spi >= 1 ? 'on schedule' : 'behind schedule'}</span></div>
        </div>
        <div class="effort-bar-wrap" title="Logged vs planned">
          <div class="effort-bar"><div class="effort-bar-fill ${ahPct > 100 ? 'over' : ahPct > 80 ? 'warn' : ''}" style="width:${Math.min(100, ahPct)}%"></div></div>
          <span class="effort-bar-val">${ahPct}%</span>
        </div>
        <div class="effort-quick" data-role="quick">
          <span class="effort-lbl">Log time</span>
          <button type="button" class="ghost" data-log="0.5">+½h</button>
          <button type="button" class="ghost" data-log="1">+1h</button>
          <button type="button" class="ghost" data-log="2">+2h</button>
          <button type="button" class="ghost" data-log="4">+4h</button>
          <button type="button" class="ghost" data-log="8">+8h</button>
          <input type="number" class="effort-custom" min="0.1" max="40" step=".25" placeholder="custom" />
          <button type="button" class="ghost" data-log="custom">Log</button>
        </div>
        ${(a.loggedHours || []).length ? `
          <div class="effort-entries">
            ${a.loggedHours.slice().sort((x,y) => (y.at||'').localeCompare(x.at||'')).slice(0, 6).map((h) => `
              <div class="effort-entry" data-entry="${escapeHTML(h.id)}">
                <span class="effort-entry-h"><b>${(h.hours || 0).toFixed(2)}h</b></span>
                <span class="effort-entry-by">${escapeHTML(personName(h.by))}</span>
                <span class="effort-entry-at muted">${escapeHTML(h.at || '')}</span>
                ${h.note ? `<span class="effort-entry-note">${escapeHTML(h.note)}</span>` : ''}
                <button type="button" class="effort-entry-x" title="Delete entry">×</button>
              </div>`).join('')}
            ${a.loggedHours.length > 6 ? `<div class="effort-more muted">+${a.loggedHours.length - 6} earlier entries</div>` : ''}
          </div>` : ''}
        <div class="effort-baseline">
          ${bl ? `
            <span class="effort-lbl">Baseline taken ${escapeHTML(bl.takenAt)}</span>
            <span class="effort-baseline-vals">
              <span title="Baseline due date">due ${escapeHTML(bl.due || '—')}</span>
              ${slip != null ? `<span class="${slip > 0 ? 'warn' : slip < 0 ? 'ok' : 'muted'}" title="Variance vs baseline due">${slip > 0 ? '+' : ''}${slip}d</span>` : ''}
              <span title="Baseline commitment">@${bl.commitment}%</span>
            </span>
            <button type="button" class="ghost" data-baseline="rebase">Re-baseline</button>
            <button type="button" class="ghost" data-baseline="clear">Clear</button>
          ` : `
            <span class="effort-lbl muted">No baseline yet — freeze the plan to enable schedule variance + SPI.</span>
            <button type="button" class="ghost" data-baseline="take">Take baseline</button>
          `}
        </div>`;
      // Log-time clicks
      el.querySelectorAll('[data-log]').forEach((btn) => {
        btn.addEventListener('click', () => {
          let hrs;
          if (btn.dataset.log === 'custom') {
            const custom = el.querySelector('.effort-custom');
            hrs = parseFloat(custom?.value || '0');
            if (custom) custom.value = '';
          } else {
            hrs = parseFloat(btn.dataset.log);
          }
          if (!hrs || hrs <= 0 || hrs > 40) return;
          a.loggedHours.push({ id: uid('lh'), hours: hrs, at: todayISO(), by: a.owner || null, note: '' });
          a.updatedAt = todayISO();
          a.history.push({ at: todayISO(), what: `Logged ${hrs}h (${ah.toFixed(1)}h → ${(ah + hrs).toFixed(1)}h)` });
          commit('time-log');
          renderEffortUI();
        });
      });
      // Delete entry
      el.querySelectorAll('.effort-entry-x').forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.closest('.effort-entry')?.dataset.entry;
          if (!id) return;
          const e = a.loggedHours.find((x) => x.id === id);
          if (!e) return;
          if (!confirm(`Remove the ${e.hours}h entry from ${e.at}?`)) return;
          a.loggedHours = a.loggedHours.filter((x) => x.id !== id);
          a.updatedAt = todayISO();
          commit('time-log-delete');
          renderEffortUI();
        });
      });
      // Baseline controls
      el.querySelectorAll('[data-baseline]').forEach((btn) => {
        btn.addEventListener('click', () => {
          const kind = btn.dataset.baseline;
          if (kind === 'take' || kind === 'rebase') {
            a.baseline = {
              due: a.due || null,
              startDate: a.startDate || null,
              commitment: (typeof a.commitment === 'number') ? a.commitment : 100,
              takenAt: todayISO(),
            };
            a.history.push({ at: todayISO(), what: kind === 'take' ? 'Baseline taken' : `Baseline re-set (due ${a.baseline.due || '—'})` });
            commit('baseline-set');
            toast('Baseline saved');
          } else if (kind === 'clear') {
            if (!confirm('Clear baseline? Variance and SPI will no longer be computed for this action.')) return;
            a.baseline = null;
            a.history.push({ at: todayISO(), what: 'Baseline cleared' });
            commit('baseline-clear');
          }
          renderEffortUI();
        });
      });
    }
    renderEffortUI();

    // Toggle the Completed and Start date fields on live status changes so
    // the user sees immediately whether they can override each date.
    $('#dStatus')?.addEventListener('change', () => {
      const nowStatus = $('#dStatus').value;
      const nowDone = nowStatus === 'done';
      const nowStarted = nowStatus !== 'todo';
      const dc = $('#dCompletedAt');
      const dm = $('#dCompletedMode');
      if (dc) {
        dc.disabled = !nowDone;
        if (dm) {
          dm.textContent = nowDone
            ? (dc.value ? 'override' : 'auto: today when Done was saved')
            : 'set Status = Done first';
        }
      }
      const ds = $('#dStartDate');
      const sm = $('#dStartMode');
      if (ds) {
        ds.disabled = !nowStarted;
        if (sm) {
          sm.textContent = nowStarted
            ? (ds.value ? 'override' : 'auto: today when moved past Not started')
            : 'set once Status leaves Not started';
        }
      }
    });

    $('#dSave').addEventListener('click', () => {
      const oldStatus = a.status;
      const oldOwner = a.owner;
      const oldDue = a.due;
      const oldCmt = typeof a.commitment === 'number' ? a.commitment : 100;
      const oldCompletedAt = a.completedAt;
      const oldStartDate = a.startDate;
      a.title = $('#dTitle').value.trim() || a.title;
      a.owner = $('#dOwner').value;
      a.status = $('#dStatus').value;
      a.due = $('#dDue').value || null;
      // Completed date — only meaningful when status is Done. Save the
      // user's explicit value if they typed one; otherwise let the
      // auto-fill logic below populate. When leaving Done, keep the
      // stored value (the reader gates on status anyway).
      if (a.status === 'done') {
        const explicit = $('#dCompletedAt')?.value || null;
        if (explicit) a.completedAt = explicit;
      }
      // Start date — editable once the action is past Not started. An
      // explicit user value wins; otherwise the sync helper below fills
      // it the first time status leaves todo. Clearing the field while
      // enabled resets to null (which lets the auto-fill run again on
      // the next status change).
      if (a.status !== 'todo') {
        a.startDate = $('#dStartDate')?.value || null;
      }
      a.deliverable = $('#dDel').value || null;
      a.milestone = $('#dMile').value || null;
      a.component = $('#dComponent').value || null;
      a.originator = $('#dOriginator')?.value || null;
      const oldOrigDate = a.originatorDate;
      a.originatorDate = $('#dOriginatorDate')?.value || a.originatorDate || a.createdAt || todayISO();
      if (oldOrigDate !== a.originatorDate) a.history.push({ at: todayISO(), what: `Originator date: ${oldOrigDate || '—'} → ${a.originatorDate || '—'}` });
      const oldPriorityLevel = a.priorityLevel || 'med';
      a.priorityLevel = $('#dPriorityLevel')?.value || a.priorityLevel || 'med';
      if (oldPriorityLevel !== a.priorityLevel) {
        a.history.push({ at: todayISO(), what: `Priority: ${priorityLevel(oldPriorityLevel).label} → ${priorityLevel(a.priorityLevel).label}` });
      }
      a.commitment = clamp(parseInt($('#dCmt').value, 10) || 100, 5, 100);
      // % complete — save only if the mode is currently "manual"
      // (the user touched the slider/number). Pressing "auto" clears
      // the override at the moment of click; that null gets preserved
      // here too, which is what we want.
      const pctModeNow = $('#dPctMode')?.textContent?.trim();
      if (pctModeNow === 'manual') {
        const pctVal = clamp(parseInt($('#dPct').value, 10) || 0, 0, 100);
        a.percentComplete = pctVal;
      } else {
        a.percentComplete = null;
      }
      if (oldCmt !== a.commitment) a.history.push({ at: todayISO(), what: `Commitment: ${oldCmt}% → ${a.commitment}%` });
      // Recurrence + snooze
      const recurUnit = $('#dRecurUnit')?.value || '';
      if (recurUnit && (recurUnit === 'day' || recurUnit === 'week' || recurUnit === 'month')) {
        const interval = clamp(parseInt($('#dRecurInterval')?.value, 10) || 1, 1, 52);
        a.recurrence = { unit: recurUnit, interval };
      } else {
        a.recurrence = null;
      }
      const snoozeRaw = $('#dSnoozeUntil')?.value || '';
      a.snoozedUntil = snoozeRaw || null;
      const oldDeps = Array.isArray(a.dependsOn) ? a.dependsOn.slice() : [];
      a.dependsOn = drawerDeps.slice();
      const depsChanged = oldDeps.length !== a.dependsOn.length || oldDeps.some((d, i) => d !== a.dependsOn[i]);
      if (depsChanged) a.history.push({ at: todayISO(), what: `Dependencies: ${oldDeps.length} → ${a.dependsOn.length}` });
      const oldTags = Array.isArray(a.tags) ? a.tags.slice() : [];
      a.tags = drawerTags.slice();
      const tagsChanged = oldTags.length !== a.tags.length || oldTags.some((t, i) => t !== a.tags[i]);
      if (tagsChanged) a.history.push({ at: todayISO(), what: `Tags: ${oldTags.length} → ${a.tags.length}` });
      a.notes = $('#dNotes').value;
      a.updatedAt = todayISO();
      if (oldStatus !== a.status) {
        a.history.push({ at: todayISO(), what: `Status: ${oldStatus} → ${a.status}` });
        _syncCompletedAtOnStatus(a, oldStatus);
        _syncStartDateOnStatus(a, oldStatus);
      }
      if (oldCompletedAt !== a.completedAt) {
        a.history.push({ at: todayISO(), what: `Completed date: ${oldCompletedAt || '—'} → ${a.completedAt || '—'}` });
      }
      if (oldStartDate !== a.startDate) {
        a.history.push({ at: todayISO(), what: `Start date: ${oldStartDate || '—'} → ${a.startDate || '—'}` });
      }
      if (oldOwner !== a.owner) a.history.push({ at: todayISO(), what: `Owner: ${personName(oldOwner)} → ${personName(a.owner)}` });
      if (oldDue !== a.due) a.history.push({ at: todayISO(), what: `Due: ${oldDue || '—'} → ${a.due || '—'}` });
      // Audit stamp — short, human summary of what changed in this save.
      const auditBits = [];
      if (oldStatus !== a.status) auditBits.push(`status ${oldStatus}→${a.status}`);
      if (oldOwner !== a.owner) auditBits.push('owner');
      if (oldDue !== a.due) auditBits.push('due date');
      if (oldCmt !== a.commitment) auditBits.push('commitment');
      stampEdit(a, null, auditBits.length ? `edit · ${auditBits.join(', ')}` : 'edit');
      // Recurring actions spawn the next occurrence the moment they're
      // completed. The user sees a toast confirming the new instance.
      let recurNote = '';
      if (oldStatus !== 'done' && a.status === 'done' && a.recurrence) {
        const sourceProj = projectOfAction(a.id) || proj;
        const nextA = spawnRecurrenceFromDone(a, sourceProj);
        if (nextA) {
          stampEdit(nextA, null, 'spawned by recurrence');
          recurNote = ` · next due ${nextA.due}`;
        }
      }
      commit('edit');
      closeDrawer();
      toast('Saved' + recurNote);
    });
    $('#dDelete').addEventListener('click', () => {
      if (!confirm('Move this action to Archive? You can restore it later.')) return;
      const sourceProj = projectOfAction(a.id) || proj;
      const target = (sourceProj.actions || []).find((x) => x.id === a.id);
      if (target) {
        target.archivedAt = todayISO();
        target.history.push({ at: todayISO(), what: 'Moved to Archive' });
        target.updatedAt = todayISO();
      }
      commit('delete');
      closeDrawer();
      toast('Moved to Archive');
    });
  }
  function closeDrawer() { $('#drawer').hidden = true; }

  /* --------------------------- Quick add ----------------------------- */

  // Build the option list for a person <select> with a sentinel "+ New
  // person…" entry at the bottom. Used everywhere the Quick Add picks
  // an Owner / Originator / etc. so users can create a person without
  // leaving the form. Pair with wirePersonSelectInline().
  function personOptionsHtml(selectedId, placeholder) {
    const placeOpt = placeholder ? `<option value="">${escapeHTML(placeholder)}</option>` : '';
    return placeOpt
      + actorOptionsHTML(selectedId)
      + '<option disabled>──────────</option>'
      + '<option value="__new__">+ New person…</option>';
  }

  // Inline person-creation dialog: a small modal with Name + Role in a
  // single form so the user can fill both at once. Returns a Promise
  // that resolves to the new person's id, or null if the user cancels.
  function createPersonInlineDialog(initialName = '') {
    return new Promise((resolve) => {
      const overlay = document.createElement('div');
      overlay.className = 'overlay desc-overlay';
      overlay.style.zIndex = '9700'; // above Quick Add
      overlay.innerHTML = `
        <div class="desc-modal" style="width: 380px;">
          <div class="desc-head">
            <div class="desc-title">+ Add person</div>
            <button class="icon-btn" id="newPersonClose" title="Cancel" aria-label="Cancel">×</button>
          </div>
          <div style="padding: 14px 16px; display: flex; flex-direction: column; gap: 10px;">
            <div class="field">
              <label>Name <span class="muted">(required)</span></label>
              <input id="newPersonName" type="text" autocomplete="off" placeholder="e.g. Carol Davis" value="${escapeHTML(initialName)}" />
            </div>
            <div class="field">
              <label>Role <span class="muted">(optional)</span></label>
              <input id="newPersonRole" type="text" autocomplete="off" placeholder="e.g. Reviewer" />
            </div>
            <div style="display: flex; gap: 8px; justify-content: flex-end; margin-top: 4px;">
              <button class="ghost" id="newPersonCancel">Cancel</button>
              <button class="primary" id="newPersonSave">Add person</button>
            </div>
          </div>
        </div>`;
      document.body.appendChild(overlay);
      const nameInp = overlay.querySelector('#newPersonName');
      const roleInp = overlay.querySelector('#newPersonRole');
      const close = (id) => { overlay.remove(); resolve(id); };
      const save = () => {
        const name = (nameInp.value || '').trim();
        if (!name) { nameInp.focus(); nameInp.setAttribute('placeholder', 'Name is required'); nameInp.classList.add('field-err'); return; }
        const role = (roleInp.value || '').trim();
        const person = { id: uid('p'), name, role, capacity: 100, hourlyRate: 100 };
        state.people.push(person);
        stampEdit(person, null, 'created inline from Quick Add');
        saveState();
        close(person.id);
      };
      overlay.querySelector('#newPersonClose').addEventListener('click', () => close(null));
      overlay.querySelector('#newPersonCancel').addEventListener('click', () => close(null));
      overlay.querySelector('#newPersonSave').addEventListener('click', save);
      // Enter on either field saves; Escape cancels. Tab moves between
      // the two inputs as expected.
      [nameInp, roleInp].forEach((inp) => {
        inp.addEventListener('keydown', (e) => {
          if (e.key === 'Enter')   { e.preventDefault(); save(); }
          if (e.key === 'Escape')  { e.preventDefault(); close(null); }
        });
        inp.addEventListener('input', () => inp.classList.remove('field-err'));
      });
      setTimeout(() => {
        // When the name is already filled in (e.g. typed via @ in the
        // notes), skip ahead to Role so the user can keep typing
        // without first jumping fields.
        if (initialName && initialName.trim().length) roleInp.focus();
        else nameInp.focus();
      }, 30);
    });
  }

  // Wire a <select> built with personOptionsHtml. Captures the previous
  // value so that cancelling the inline dialog reverts the dropdown
  // instead of leaving "+ New person…" selected. Refreshes ALL the
  // person-selects inside the same Quick Add panel so a person added
  // via the Owner dropdown appears in the Originator dropdown too.
  function wirePersonSelectInline(sel) {
    let prev = sel.value;
    sel.addEventListener('change', async () => {
      if (sel.value !== '__new__') { prev = sel.value; return; }
      // Revert the visible value immediately while the dialog is open
      // so the dropdown doesn't read "+ New person…" if the user
      // glances away. We re-set it to the new id on resolve.
      sel.value = prev;
      const newId = await createPersonInlineDialog();
      const scope = sel.closest('.qa-panel') || document;
      if (newId) {
        $$('select[data-person-select]', scope).forEach((other) => {
          const cur = other === sel ? newId : (other.value === '__new__' ? '' : other.value);
          const placeholder = other.dataset.personPlaceholder || '';
          other.innerHTML = personOptionsHtml(cur, placeholder || null);
          other.value = cur;
        });
        prev = newId;
        toast(`Added ${state.people.find((p) => p.id === newId)?.name}`);
      }
    });
  }

  let qaType = 'action';
  let qaInit = {};
  let qaSaveCallback = null;
  function openQuickAdd(type = 'action', init = {}, onSave = null) {
    // Change requests use the full editor instead of the simplified Quick Add
    // form, so the create flow has the same fields as the edit flow.
    if (type === 'change') {
      if (curProjectIsMerged()) { toast('Pick a single project to add items.'); return; }
      openChangeRequestEditor(newChangeRequestDraft());
      return;
    }
    qaType = type;
    qaInit = init || {};
    qaSaveCallback = onSave || null;
    $$('.qa-tab').forEach((t) => t.classList.toggle('active', t.dataset.qa === type));
    drawQA();
    $('#quickAdd').hidden = false;
    setTimeout(() => $('#qaBody input, #qaBody select, #qaBody textarea')?.focus(), 30);
  }
  function closeQuickAdd() { $('#quickAdd').hidden = true; }

  function drawQA() {
    const body = $('#qaBody');
    const proj = curProject();
    if (qaType === 'action') {
      const initTitle = qaInit.title || '';
      const initNotes = qaInit.notes || '';
      body.innerHTML = `
        <div class="field"><label>Title</label><input id="qTitle" placeholder="What needs to be done?" value="${escapeHTML(initTitle)}" /></div>
        <div class="qa-row">
          <div class="field"><label>Owner</label>
            <select id="qOwner" data-person-select>${personOptionsHtml(qaInit.owner)}</select>
          </div>
          <div class="field"><label>Originator</label>
            <select id="qOriginator" data-person-select data-person-placeholder="— same as owner">${personOptionsHtml('', '— same as owner')}</select>
          </div>
          <div class="field"><label>Originator date</label>
            <input id="qOriginatorDate" type="date" value="${todayISO()}" title="Auto-set to today; editable" />
          </div>
        </div>
        <div class="qa-row">
          <div class="field"><label>Status</label>
            <select id="qStatus">${STATUSES.map((s) => `<option value="${s.id}">${s.name}</option>`).join('')}</select>
          </div>
          <div class="field"><label>Priority</label>
            <select id="qPriorityLevel">${PRIORITY_LEVELS.map((p) => `<option value="${p.id}" ${p.id === (qaInit.priorityLevel || 'med') ? 'selected' : ''}>${p.label}</option>`).join('')}</select>
          </div>
          <div class="field"><label>Due</label><input id="qDue" type="date" value="${todayISO()}" /></div>
        </div>
        <div class="qa-row">
          <div class="field"><label>Component (optional)</label>
            <select id="qComponent"><option value="">—</option>${(proj.components || []).map((pt) => `<option value="${pt.id}" ${pt.id === qaInit.component ? 'selected' : ''}>${escapeHTML(pt.name)}</option>`).join('')}</select>
          </div>
          <div class="field"><label>Deliverable (optional)</label>
            <select id="qDel"><option value="">—</option>${(proj.deliverables || []).map((d) => `<option value="${d.id}">${escapeHTML(d.name)}</option>`).join('')}</select>
          </div>
        </div>
        <div class="field">
          <label>Commitment <span class="muted" id="qCmtVal">100%</span></label>
          <input id="qCmt" type="range" min="5" max="100" step="5" value="100" oninput="document.getElementById('qCmtVal').textContent = this.value + '%';" />
        </div>
        <div class="field"><label>Notes</label><textarea id="qNotes" placeholder="Optional context">${escapeHTML(initNotes)}</textarea></div>`;
    } else if (qaType === "component") {
      const knownCCs = getCostCentres();
      body.innerHTML = `
        <div class="field"><label>Name</label><input id="qName" placeholder="e.g. Power, AOCS, Backend…" /></div>
        <div class="field"><label>Cost centre (optional)</label>
          <select id="qCC">
            <option value="">— none —</option>
            ${knownCCs.map((c) => `<option value="${escapeHTML(c)}">${escapeHTML(c)}</option>`).join('')}
            <option value="__new__">+ New cost centre…</option>
          </select>
        </div>
        <div class="field"><label>Color</label>
          <div id="qComponentColors" class="color-grid">
            ${COMPONENT_COLORS.map((c, i) => `
              <label class="color-swatch" title="${c.name}">
                <input type="radio" name="qComponentColor" value="${c.id}" ${i === 0 ? 'checked' : ''} />
                <span style="background: rgba(${c.rgb},.9);"></span>
                <em>${c.name}</em>
              </label>`).join('')}
          </div>
        </div>`;
    } else if (qaType === 'deliverable') {
      body.innerHTML = `
        <div class="field"><label>Name</label><input id="qName" /></div>
        <div class="qa-row">
          <div class="field"><label>Due</label><input id="qDue" type="date" /></div>
          <div class="field"><label>Status</label>
            <select id="qStatus"><option value="todo">Not started</option><option value="doing">In progress</option><option value="done">Done</option></select>
          </div>
        </div>
        <div class="field"><label>Component <span class="muted">— optional</span></label>
          <select id="qComp">
            <option value="">— None</option>
            ${(proj.components || []).map((cmp) => `<option value="${cmp.id}">${escapeHTML(cmp.name)}</option>`).join('')}
          </select>
        </div>`;
    } else if (qaType === 'milestone') {
      body.innerHTML = `
        <div class="field"><label>Name</label><input id="qName" /></div>
        <div class="qa-row">
          <div class="field"><label>Start date</label><input id="qDate" type="date" /></div>
          <div class="field"><label>End date <span class="muted">— optional, for a range</span></label><input id="qEndDate" type="date" /></div>
        </div>
        <div class="field"><label>Component <span class="muted">— optional</span></label>
          <select id="qComp">
            <option value="">— None</option>
            ${(proj.components || []).map((cmp) => `<option value="${cmp.id}">${escapeHTML(cmp.name)}</option>`).join('')}
          </select>
        </div>`;
    } else if (qaType === 'risk') {
      const kind = qaInit.kind === 'opportunity' ? 'opportunity' : 'risk';
      const actionsList = (proj.actions || []).slice().sort((a, b) => a.title.localeCompare(b.title));
      body.innerHTML = `
        <div class="field"><label>Type</label>
          <div class="seg" role="tablist" aria-label="Kind">
            <button type="button" class="seg-btn ${kind === 'risk' ? 'active' : ''}" data-qa-kind="risk">▲ Risk</button>
            <button type="button" class="seg-btn ${kind === 'opportunity' ? 'active' : ''}" data-qa-kind="opportunity">▽ Opportunity</button>
          </div>
        </div>
        <div class="field"><label>Title</label><input id="qTitle" placeholder="${kind === 'opportunity' ? 'Upside event to chase' : 'Downside event to mitigate'}" /></div>
        <div class="qa-row">
          <div class="field"><label>Inherent P (1-5)</label><input id="qProb" type="number" min="1" max="5" value="3" /></div>
          <div class="field"><label>Inherent I (1-5)</label><input id="qImp" type="number" min="1" max="5" value="3" /></div>
        </div>
        <div class="qa-row">
          <div class="field"><label>Residual P (post-action)</label><input id="qProbR" type="number" min="1" max="5" value="2" /></div>
          <div class="field"><label>Residual I (post-action)</label><input id="qImpR" type="number" min="1" max="5" value="2" /></div>
        </div>
        <div class="field"><label>Owner</label>
          <select id="qOwner" data-person-select>${personOptionsHtml('')}</select>
        </div>
        <div class="field"><label id="qMitLbl">${kind === 'opportunity' ? 'Capture plan' : 'Mitigation'}</label><textarea id="qMit" placeholder="Brief description of the response"></textarea></div>
        <div class="field"><label>Linked action (optional)</label>
          <select id="qActionLink">
            <option value="">— none —</option>
            ${actionsList.map((a) => `<option value="${a.id}">${escapeHTML(a.title)} — ${escapeHTML(personName(a.owner))}</option>`).join('')}
          </select>
        </div>`;
      $$('.seg-btn[data-qa-kind]', body).forEach((b) => {
        b.addEventListener('click', () => {
          qaInit.kind = b.dataset.qaKind;
          $$('.seg-btn[data-qa-kind]', body).forEach((x) => x.classList.toggle('active', x.dataset.qaKind === qaInit.kind));
          $('#qMitLbl').textContent = qaInit.kind === 'opportunity' ? 'Capture plan' : 'Mitigation';
          $('#qTitle').placeholder = qaInit.kind === 'opportunity' ? 'Upside event to chase' : 'Downside event to mitigate';
        });
      });
    } else if (qaType === 'decision') {
      body.innerHTML = `
        <div class="field"><label>Title</label><input id="qTitle" /></div>
        <div class="field"><label>Rationale</label><textarea id="qRat"></textarea></div>
        <div class="qa-row">
          <div class="field"><label>Owner</label>
            <select id="qOwner" data-person-select>${personOptionsHtml('')}</select>
          </div>
          <div class="field"><label>Date</label><input id="qDate" type="date" value="${todayISO()}" /></div>
        </div>
        <div class="field"><label>Component <span class="muted">(optional)</span></label>
          <select id="qComponent">
            <option value="">—</option>
            ${(proj.components || []).map((cmp) => `<option value="${cmp.id}" ${cmp.id === qaInit.component ? 'selected' : ''}>${escapeHTML(cmp.name)}</option>`).join('')}
          </select>
        </div>`;
    } else if (qaType === 'meeting') {
      // Progressive disclosure: title / date / time / repeating-toggle are
      // always visible. When repeating is on, reveal a single periodicity
      // row ("Every [n] [unit]") plus optional end-date — and only show the
      // day-of-week selector when the unit is "Week(s)". When off, none
      // of those controls render.
      const repeating = qaInit.mtKind === 'recurring';
      const initUnit  = qaInit.mtUnit || 'week';
      body.innerHTML = `
        <div class="field"><label>Title</label><input id="qTitle" placeholder="e.g. Weekly standup, PDR walkthrough" /></div>
        <div class="qa-row">
          <div class="field"><label id="qDateLbl">${repeating ? 'Start date' : 'Date'}</label><input id="qDate" type="date" value="${todayISO()}" /></div>
          <div class="field"><label>Time <span class="muted">— optional</span></label><input id="qTime" type="time" /></div>
        </div>
        <div class="field">
          <label class="qa-toggle">
            <input type="checkbox" id="qMtRepeats" ${repeating ? 'checked' : ''} />
            <span>Repeating meeting</span>
          </label>
        </div>
        <div id="qMtRecurWrap" ${repeating ? '' : 'hidden'}>
          <div class="qa-row qa-row-tight">
            <div class="field" style="flex: 0 0 auto;">
              <label>Every</label>
              <input id="qInterval" type="number" min="1" max="99" value="1" style="width:64px;" />
            </div>
            <div class="field" style="flex: 1;">
              <label>&nbsp;</label>
              <select id="qUnit">
                <option value="day"   ${initUnit === 'day'   ? 'selected' : ''}>Day(s)</option>
                <option value="week"  ${initUnit === 'week'  ? 'selected' : ''}>Week(s)</option>
                <option value="month" ${initUnit === 'month' ? 'selected' : ''}>Month(s)</option>
              </select>
            </div>
            <div class="field" id="qDowField" ${initUnit === 'week' ? '' : 'hidden'}>
              <label>On</label>
              <select id="qDow">
                <option value="1">Monday</option>
                <option value="2">Tuesday</option>
                <option value="3">Wednesday</option>
                <option value="4">Thursday</option>
                <option value="5">Friday</option>
                <option value="6">Saturday</option>
                <option value="0">Sunday</option>
              </select>
            </div>
          </div>
          <div class="field">
            <label>Ends <span class="muted">— optional</span></label>
            <input id="qEndDate" type="date" />
          </div>
        </div>
        <div class="field"><label>Component <span class="muted">— optional</span></label>
          <select id="qComp">
            <option value="">— None</option>
            ${(proj.components || []).map((cmp) => `<option value="${cmp.id}">${escapeHTML(cmp.name)}</option>`).join('')}
          </select>
        </div>`;
      const repeatChk = body.querySelector('#qMtRepeats');
      const recurWrap = body.querySelector('#qMtRecurWrap');
      const dateInput = body.querySelector('#qDate');
      const unitSel   = body.querySelector('#qUnit');
      const dowField  = body.querySelector('#qDowField');
      const dowSel    = body.querySelector('#qDow');
      const dateLbl   = body.querySelector('#qDateLbl');
      function syncDowFromDate() {
        const v = dateInput.value;
        if (v) dowSel.value = String(parseDate(v).getDay());
      }
      if (repeating && initUnit === 'week') syncDowFromDate();
      repeatChk.addEventListener('change', () => {
        recurWrap.hidden = !repeatChk.checked;
        dateLbl.textContent = repeatChk.checked ? 'Start date' : 'Date';
        qaInit.mtKind = repeatChk.checked ? 'recurring' : 'oneoff';
        if (repeatChk.checked && unitSel.value === 'week') syncDowFromDate();
      });
      unitSel.addEventListener('change', () => {
        qaInit.mtUnit = unitSel.value;
        // Day-of-week selector is only relevant for the 'week' unit.
        dowField.hidden = unitSel.value !== 'week';
        if (unitSel.value === 'week') syncDowFromDate();
      });
      dateInput.addEventListener('change', () => {
        if (repeatChk.checked && unitSel.value === 'week') syncDowFromDate();
      });
    } else if (qaType === 'person') {
      body.innerHTML = `
        <div class="field"><label>Name</label><input id="qName" /></div>
        <div class="qa-row">
          <div class="field"><label>Role</label><input id="qRole" /></div>
          <div class="field"><label>Capacity</label><input id="qCap" type="number" min="1" max="20" value="5" /></div>
        </div>`;
    } else if (qaType === 'project') {
      const tpls = state.templates || [];
      body.innerHTML = `
        <div class="field"><label>Name</label><input id="qName" /></div>
        <div class="field"><label>Description</label><textarea id="qDesc"></textarea></div>
        ${tpls.length ? `
        <div class="field"><label>Start from <span class="muted">— template, or empty</span></label>
          <select id="qFrom">
            <option value="">Empty project</option>
            ${tpls.map((t) => `<option value="${escapeHTML(t.id)}">From template — ${escapeHTML(t.name)}</option>`).join('')}
          </select>
        </div>` : ''}`;
    } else if (qaType === 'link') {
      body.innerHTML = `
        <div class="field"><label>Title</label><input id="qTitle" placeholder="Display name" /></div>
        <div class="field"><label>URL or path</label><input id="qUrl" placeholder="https://… or file:///…" /></div>
        <div class="field"><label>Description</label><textarea id="qDesc" placeholder="What this is and when to use it"></textarea></div>
        <div class="qa-row">
          <div class="field"><label>Component (optional)</label>
            <select id="qComp">
              <option value="">— none —</option>
              ${(proj.components || []).map((c) => `<option value="${c.id}">${escapeHTML(c.name)}</option>`).join('')}
            </select>
          </div>
          <div class="field"><label>Folder</label>
            <select id="qFolder">
              <option value="">— Loose —</option>
              ${(proj.linkFolders || []).map((f) => `<option value="${f.id}" ${f.id === qaInit.folderId ? 'selected' : ''}>${escapeHTML(f.name)}</option>`).join('')}
            </select>
          </div>
        </div>`;
    }
    // Hook every person-picker rendered in the body so the "+ New
    // person…" sentinel option triggers an inline create flow without
    // leaving the Quick Add.
    $$('select[data-person-select]', body).forEach(wirePersonSelectInline);
  }

  function saveQA() {
    if (curProjectIsMerged()) {
      toast('Pick a single project to add items.');
      closeQuickAdd();
      return;
    }
    const proj = curProject();
    if (qaType === 'action') {
      const title = $('#qTitle').value.trim();
      if (!title) return toast('Title required');
      const a = {
        id: uid('a'), title,
        owner: $('#qOwner').value,
        originator: $('#qOriginator')?.value || null,
        originatorDate: $('#qOriginatorDate')?.value || todayISO(),
        due: $('#qDue').value || null,
        status: $('#qStatus').value,
        priority: typeof qaInit.priority === 'number' ? qaInit.priority : 0,
        priorityLevel: $('#qPriorityLevel')?.value || qaInit.priorityLevel || 'med',
        commitment: clamp(parseInt($('#qCmt')?.value, 10) || 100, 5, 100),
        component: $('#qComponent')?.value || null,
        deliverable: $('#qDel').value || null,
        milestone: null,
        description: qaInit.description || null,
        notes: $('#qNotes').value || '',
        createdAt: todayISO(), updatedAt: todayISO(),
        history: [{ at: todayISO(), what: 'Created' }],
      };
      proj.actions.push(a);
    } else if (qaType === 'component') {
      const name = $('#qName').value.trim();
      if (!name) return toast('Name required');
      const color = (document.querySelector('input[name="qComponentColor"]:checked')?.value) || COMPONENT_COLORS[0].id;
      let ccRaw = $('#qCC')?.value || '';
      if (ccRaw === '__new__') {
        const code = prompt('New cost-centre name:');
        const trimmed = (code || '').trim();
        if (!trimmed) ccRaw = '';
        else {
          ccRaw = trimmed;
          state.budgets = state.budgets || {};
          state.budgets[ccRaw] = state.budgets[ccRaw] || {};
        }
      }
      proj.components = proj.components || [];
      proj.components.push({ id: uid('cm'), name, color, costCenter: ccRaw || null });
    } else if (qaType === 'deliverable') {
      const name = $('#qName').value.trim();
      if (!name) return toast('Name required');
      proj.deliverables = proj.deliverables || [];
      proj.deliverables.push({
        id: uid('d'),
        name,
        dueDate: $('#qDue').value || null,
        status: $('#qStatus').value,
        component: $('#qComp')?.value || null,
      });
    } else if (qaType === 'milestone') {
      const name = $('#qName').value.trim();
      if (!name) return toast('Name required');
      const date = $('#qDate').value || null;
      const ed = $('#qEndDate')?.value || null;
      if (ed && date && ed < date) { toast('End date can\'t be before start date'); return; }
      proj.milestones = proj.milestones || [];
      proj.milestones.push({
        id: uid('m'),
        name,
        date,
        // Range: only stored if it's strictly after the start.
        endDate: (ed && ed !== date) ? ed : null,
        status: 'todo',
        component: $('#qComp')?.value || null,
      });
    } else if (qaType === 'risk') {
      const title = $('#qTitle').value.trim();
      if (!title) return toast('Title required');
      const inhP = clamp(parseInt($('#qProb').value, 10) || 3, 1, 5);
      const inhI = clamp(parseInt($('#qImp').value, 10) || 3, 1, 5);
      const resP = clamp(parseInt($('#qProbR').value, 10) || inhP, 1, 5);
      const resI = clamp(parseInt($('#qImpR').value, 10) || inhI, 1, 5);
      const isOpp = qaInit.kind === 'opportunity';
      proj.risks = proj.risks || [];
      const kind = isOpp ? 'opportunity' : 'risk';
      proj.risks.push({
        id: uid(isOpp ? 'o' : 'r'),
        kind,
        title,
        // Auto-assigned short code (e.g. "OSB-R-3"); user can override
        // in the risk editor. Generated AFTER the kind so the right
        // R / O prefix is used.
        identifier: generateRiskIdentifier(proj, kind),
        inherent: { probability: inhP, impact: inhI },
        residual: { probability: resP, impact: resI },
        mitigation: $('#qMit').value || '',
        actionId: $('#qActionLink').value || null,
        owner: $('#qOwner').value,
      });
      // If the R&O page is filtered in a way that would hide this new item,
      // open the filter so the user actually sees what they just added.
      if (roState.kind === (isOpp ? 'risk' : 'opportunity')) roState.kind = 'all';
    } else if (qaType === 'decision') {
      const title = $('#qTitle').value.trim();
      if (!title) return toast('Title required');
      proj.decisions = proj.decisions || [];
      proj.decisions.push({
        id: uid('dec'), title,
        rationale: $('#qRat').value || '',
        owner: $('#qOwner').value,
        date: $('#qDate').value || todayISO(),
        component: $('#qComponent')?.value || null,
      });
    } else if (qaType === 'meeting') {
      const title = $('#qTitle').value.trim();
      if (!title) return toast('Title required');
      const repeating = !!$('#qMtRepeats')?.checked;
      const date = $('#qDate').value || todayISO();
      const time = $('#qTime').value || null;
      const component = $('#qComp')?.value || null;
      const m = { id: uid('mtg'), title, time, endDate: null, component };
      if (!repeating) {
        m.kind = 'oneoff';
        m.date = date;
      } else {
        const interval = Math.max(1, parseInt($('#qInterval').value, 10) || 1);
        const unit = $('#qUnit').value === 'day' ? 'day'
                   : $('#qUnit').value === 'month' ? 'month'
                   : 'week';
        const ed = $('#qEndDate')?.value || '';
        if (ed && ed < date) { toast('End date can\'t be before the start date'); return; }
        m.kind      = 'recurring';
        m.recurUnit = unit;
        m.interval  = interval;
        m.startDate = date;
        m.endDate   = ed || null;
        if (unit === 'week') m.dayOfWeek = parseInt($('#qDow').value, 10);
      }
      proj.meetings = proj.meetings || [];
      proj.meetings.push(m);
    } else if (qaType === 'person') {
      const name = $('#qName').value.trim();
      if (!name) return toast('Name required');
      state.people.push({
        id: uid('p'), name,
        role: $('#qRole').value || '',
        capacity: clamp(parseInt($('#qCap').value, 10) || 5, 1, 20),
      });
    } else if (qaType === 'project') {
      const name = $('#qName').value.trim();
      if (!name) return toast('Name required');
      const np = {
        id: uid('pr'), name,
        description: $('#qDesc').value || '',
        actions: [], deliverables: [], milestones: [], risks: [], decisions: [], changes: [], components: [], links: [],
      };
      const fromId = $('#qFrom')?.value;
      if (fromId) {
        const tpl = (state.templates || []).find((t) => t.id === fromId);
        if (tpl) {
          applyTemplateToProject(np, tpl);
          np.templateOf = tpl.id;
        }
      }
      state.projects.push(np);
      state.currentProjectId = np.id;
    } else if (qaType === 'link') {
      const url = $('#qUrl').value.trim();
      if (!url) return toast('URL required');
      const title = $('#qTitle').value.trim() || url;
      proj.links = proj.links || [];
      proj.links.push({
        id: uid('lk'), title, url,
        description: $('#qDesc').value || '',
        component: $('#qComp').value || null,
        folderId: $('#qFolder')?.value || qaInit.folderId || null,
      });
    }
    // Capture the just-pushed action for callback consumers (e.g. notes panel).
    let createdAction = null;
    if (qaType === 'action') createdAction = proj.actions[proj.actions.length - 1];
    const cb = qaSaveCallback;
    qaSaveCallback = null;
    // Invalidate the cached calendar window so the next render
    // re-runs ensureCalendarWindow and auto-extends to include the
    // new record's date — important for items dated outside the
    // current window (e.g. far-future actions / milestones / CRs).
    calState.windowAnchorOffset = null;
    calState.firstWeekStart = null;
    commit('add');
    closeQuickAdd();
    const addedLabel = qaType === 'risk'
      ? (qaInit.kind === 'opportunity' ? 'Opportunity added' : 'Risk added')
      : 'Added';
    toast(addedLabel);
    if (cb && createdAction) cb(createdAction);
  }

  /* -------------------------- Context menu --------------------------- */

  let _ctxOutsideHandler = null;
  // Small floating picker anchored under a chip. Used by open-point
  // criticality + priority chips so they replace the old <select>s
  // without losing the all-options-visible affordance.
  let _levelPopOutsideHandler = null;
  function closeLevelPopover() {
    document.querySelectorAll('.level-pop').forEach((m) => m.remove());
    if (_levelPopOutsideHandler) {
      document.removeEventListener('mousedown', _levelPopOutsideHandler);
      document.removeEventListener('keydown', _levelPopOutsideHandler);
      _levelPopOutsideHandler = null;
    }
  }
  function showLevelPopover(anchorEl, kind, currentValue, onPick) {
    closeLevelPopover();
    closeContextMenu();
    const opts = (kind === 'criticality')
      ? ['low', 'med', 'high', 'critical'].map((id) => ({ id, label: CRITICALITY_LABEL[id], rgb: CRITICALITY_RGB[id] }))
      : PRIORITY_LEVELS.map((p) => ({ id: p.id, label: p.label, rgb: p.rgb }));
    const subtitle = kind === 'criticality'
      ? 'Severity if not addressed'
      : 'Urgency to act';
    const heading = kind === 'criticality' ? 'Criticality' : 'Priority';

    const pop = document.createElement('div');
    pop.className = 'level-pop';
    pop.innerHTML = `
      <div class="level-pop-head">
        <div class="level-pop-title">${escapeHTML(heading)}</div>
        <div class="level-pop-sub">${escapeHTML(subtitle)}</div>
      </div>
      <div class="level-pop-list">
        ${opts.map((o) => `
          <button type="button" class="level-pop-item ${o.id === currentValue ? 'sel' : ''}" data-val="${escapeHTML(o.id)}">
            <span class="level-pop-dot" style="background:rgb(${o.rgb})"></span>
            <span class="level-pop-label">${escapeHTML(o.label)}</span>
            ${o.id === currentValue ? '<span class="level-pop-check">✓</span>' : ''}
          </button>`).join('')}
      </div>`;
    document.body.appendChild(pop);

    // Anchor below the chip; clamp to viewport so right-edge chips don't
    // push the popover off-screen
    const r = anchorEl.getBoundingClientRect();
    const w = pop.getBoundingClientRect().width;
    const h = pop.getBoundingClientRect().height;
    let x = r.left;
    let y = r.bottom + 6;
    if (x + w > innerWidth - 8)  x = innerWidth - w - 8;
    if (y + h > innerHeight - 8) y = r.top - h - 6; // flip above
    pop.style.left = Math.max(8, x) + 'px';
    pop.style.top  = Math.max(8, y) + 'px';

    pop.querySelectorAll('.level-pop-item').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const val = btn.dataset.val;
        closeLevelPopover();
        try { onPick(val); } catch (err) { console.error(err); }
      });
    });

    // Outside-click + Escape close (popover is transient — same rule as
    // the existing ctx-menu / palette).
    _levelPopOutsideHandler = (e) => {
      if (e.type === 'keydown') {
        if (e.key === 'Escape') closeLevelPopover();
        return;
      }
      if (pop.contains(e.target)) return;
      closeLevelPopover();
    };
    setTimeout(() => {
      document.addEventListener('mousedown', _levelPopOutsideHandler);
      document.addEventListener('keydown', _levelPopOutsideHandler);
    }, 0);
  }

  function showContextMenu(x, y, items) {
    closeContextMenu();
    const menu = document.createElement('div');
    menu.className = 'ctx-menu';
    menu.style.left = x + 'px';
    menu.style.top = y + 'px';
    items.forEach((it) => {
      if (it.divider) {
        const d = document.createElement('div');
        d.className = 'ctx-divider';
        menu.appendChild(d);
        return;
      }
      const b = document.createElement('button');
      b.className = 'ctx-item' + (it.danger ? ' danger' : '');
      b.innerHTML = `<span class="ctx-icon">${it.icon || ''}</span><span>${escapeHTML(it.label)}</span>`;
      b.addEventListener('click', (e) => {
        e.stopPropagation();
        closeContextMenu();
        try { it.onClick?.(); } catch (err) { console.error(err); }
      });
      menu.appendChild(b);
    });
    document.body.appendChild(menu);
    const r = menu.getBoundingClientRect();
    if (r.right > innerWidth)  menu.style.left = (innerWidth - r.width - 6) + 'px';
    if (r.bottom > innerHeight) menu.style.top  = (innerHeight - r.height - 6) + 'px';
    // Outside-click closer: only fires when click is OUTSIDE the menu, so
    // item button clicks aren't pre-empted by removing the menu mid-event.
    _ctxOutsideHandler = (e) => {
      if (menu.contains(e.target)) return;
      closeContextMenu();
    };
    setTimeout(() => document.addEventListener('mousedown', _ctxOutsideHandler), 0);
  }
  function closeContextMenu() {
    document.querySelectorAll('.ctx-menu').forEach((m) => m.remove());
    if (_ctxOutsideHandler) {
      document.removeEventListener('mousedown', _ctxOutsideHandler);
      _ctxOutsideHandler = null;
    }
  }

  /* ────────────────── Chart copy: PNG / data via right-click ──────────
   * Any SVG carrying class="copyable-chart" + data-chart="<id>" gets a
   * right-click menu with Copy as PNG / Copy as data. The PNG path
   * serialises the SVG, inlines the document stylesheet so styling
   * survives serialisation, rasterises at 2x to an offscreen canvas,
   * and writes to the clipboard. The data path looks up the chart id
   * in a registry of { headers, rows } extractors, formats TSV, and
   * writes that. Each chart render call (re-)registers its extractor.
   */
  const _chartDataExtractors = new Map();
  function registerChartData(chartId, fn) {
    if (chartId && typeof fn === 'function') _chartDataExtractors.set(chartId, fn);
  }

  // Concatenate every accessible stylesheet rule. Cross-origin sheets
  // silently fail .cssRules access; we ignore those (the app's CSS is
  // inlined so the main rules are always reachable).
  let _cachedDocCss = null;
  function _gatherDocCss() {
    if (_cachedDocCss != null) return _cachedDocCss;
    let out = '';
    for (const sheet of Array.from(document.styleSheets)) {
      try {
        for (const rule of Array.from(sheet.cssRules || [])) out += rule.cssText + '\n';
      } catch (_) { /* cross-origin or restricted */ }
    }
    _cachedDocCss = out;
    return out;
  }

  function _serializeSvgWithStyles(svg) {
    const clone = svg.cloneNode(true);
    if (!clone.getAttribute('xmlns')) clone.setAttribute('xmlns', 'http://www.w3.org/2000/svg');
    const styleEl = document.createElementNS('http://www.w3.org/2000/svg', 'style');
    styleEl.textContent = _gatherDocCss();
    clone.insertBefore(styleEl, clone.firstChild);
    const bbox = svg.getBoundingClientRect();
    if (!clone.getAttribute('width'))  clone.setAttribute('width',  Math.max(1, Math.round(bbox.width)));
    if (!clone.getAttribute('height')) clone.setAttribute('height', Math.max(1, Math.round(bbox.height)));
    return new XMLSerializer().serializeToString(clone);
  }

  async function _svgToPngBlob(svgEl, scale = 2) {
    const svgStr = _serializeSvgWithStyles(svgEl);
    const blob = new Blob([svgStr], { type: 'image/svg+xml;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    try {
      const img = new Image();
      img.src = url;
      await new Promise((resolve, reject) => { img.onload = resolve; img.onerror = reject; });
      const bbox = svgEl.getBoundingClientRect();
      const w = Math.max(1, Math.round(bbox.width  * scale));
      const h = Math.max(1, Math.round(bbox.height * scale));
      const canvas = document.createElement('canvas');
      canvas.width = w; canvas.height = h;
      const ctx = canvas.getContext('2d');
      // Fill panel-coloured background so the PNG isn't transparent —
      // the chart looks washed out when pasted into a light email.
      const bg = (getComputedStyle(svgEl.closest('.panel') || document.body).getPropertyValue('--bg-1') || '#0f1115').trim();
      ctx.fillStyle = bg || '#0f1115';
      ctx.fillRect(0, 0, w, h);
      ctx.drawImage(img, 0, 0, w, h);
      return await new Promise((resolve, reject) =>
        canvas.toBlob((b) => b ? resolve(b) : reject(new Error('toBlob returned null')), 'image/png'));
    } finally {
      URL.revokeObjectURL(url);
    }
  }

  // Snapshot every CSS custom property on :root so the cloned subtree
  // inside <foreignObject> still resolves var(--bg-1) etc. without
  // walking the whole document tree.
  function _rootCustomProps() {
    const cs = getComputedStyle(document.documentElement);
    let out = '';
    for (let i = 0; i < cs.length; i++) {
      const name = cs[i];
      if (name.startsWith('--')) out += `${name}:${cs.getPropertyValue(name).trim()};`;
    }
    return out;
  }

  // Rasterise a non-SVG element by serialising it inside an SVG
  // <foreignObject>. The clone has overflow stripped on every node so
  // off-screen content (Kanban columns past the scroll fold, Gantt
  // bars past the horizontal viewport) lands in the PNG.
  // expandSelectors → extra CSS selectors whose matching nodes should
  // have their explicit width/height cleared so they grow with content.
  async function _htmlToPngBlob(el, { scale = 2, expandSelectors = [], hideSelectors = [] } = {}) {
    const clone = el.cloneNode(true);
    // Push var(--…) onto the clone wrapper so descendants inherit them.
    const rootVars = _rootCustomProps();
    clone.setAttribute('style', `${rootVars};${clone.getAttribute('style') || ''}`);
    // Strip every constraint that hides content: overflow/max-h/max-w.
    const all = [clone, ...clone.querySelectorAll('*')];
    all.forEach((n) => {
      if (n.nodeType !== 1) return;
      n.style.maxHeight = 'none';
      n.style.maxWidth  = 'none';
      n.style.overflow  = 'visible';
    });
    // Caller-specified selectors get an extra forced width-relaxation
    // so flex/grid containers grow with their natural content rather
    // than staying pinned to their original computed width.
    if (expandSelectors.length) {
      expandSelectors.forEach((sel) => {
        clone.querySelectorAll(sel).forEach((n) => {
          n.style.width = 'auto';
          n.style.height = 'auto';
        });
      });
    }
    // hideSelectors → remove UI controls that don't belong in the PNG
    // (delete buttons, range pickers, etc.). Using `display:none` keeps
    // the layout calculation clean.
    if (hideSelectors.length) {
      hideSelectors.forEach((sel) => {
        clone.querySelectorAll(sel).forEach((n) => { n.style.display = 'none'; });
      });
    }
    // Mount off-screen at the source element's width so layout settles
    // before we read scroll dimensions. The visibility:hidden keeps it
    // out of the user's view while still triggering layout.
    const sandbox = document.createElement('div');
    sandbox.style.cssText = `position:absolute;left:-99999px;top:0;visibility:hidden;width:${el.scrollWidth}px;`;
    sandbox.appendChild(clone);
    document.body.appendChild(sandbox);
    let w, h;
    try {
      w = Math.max(el.scrollWidth, clone.scrollWidth);
      h = Math.max(el.scrollHeight, clone.scrollHeight);
    } finally {
      document.body.removeChild(sandbox);
    }
    // Re-mount inside the foreignObject. XMLSerializer produces valid
    // XHTML for HTML elements, satisfying foreignObject's strictness.
    const css = _gatherDocCss();
    const cloneHtml = new XMLSerializer().serializeToString(clone);
    const xml = `<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}">`
      + `<foreignObject x="0" y="0" width="${w}" height="${h}">`
      + `<div xmlns="http://www.w3.org/1999/xhtml" style="${rootVars}">`
      + `<style>${css}</style>${cloneHtml}`
      + `</div></foreignObject></svg>`;
    // Encode via base64 data URL — blob: URLs taint the canvas when the
    // SVG carries a <foreignObject>; data: URLs are treated as same-
    // origin and slip through in Chrome.
    const url = 'data:image/svg+xml;base64,' + btoa(unescape(encodeURIComponent(xml)));
    const img = new Image();
    img.src = url;
    await new Promise((resolve, reject) => { img.onload = resolve; img.onerror = reject; });
    const canvas = document.createElement('canvas');
    canvas.width  = Math.max(1, Math.round(w * scale));
    canvas.height = Math.max(1, Math.round(h * scale));
    const ctx = canvas.getContext('2d');
    const bg = (getComputedStyle(el.closest('.panel') || document.body).getPropertyValue('--bg-1') || '#0f1115').trim();
    ctx.fillStyle = bg || '#0f1115';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
    return await new Promise((resolve, reject) =>
      canvas.toBlob((b) => b ? resolve(b) : reject(new Error('toBlob returned null — likely a tainted-canvas limitation when capturing HTML with embedded resources')), 'image/png'));
  }

  // Per-chart-id capture overrides — lets a chart specify which
  // selectors to "open up" before HTML capture (used for the Kanban
  // board so column-bodies expand past their scroll fold).
  const _chartCaptureOpts = new Map();
  function registerChartCapture(chartId, opts) {
    if (chartId) _chartCaptureOpts.set(chartId, opts);
  }

  // Reusable kick-off — returns the Promise of a PNG blob without
  // awaiting it. ClipboardItem accepts a Promise, which preserves
  // Safari's user-activation across the async render step. Falls back
  // to a real Blob await for the download path.
  function _chartPngBlobPromise(el) {
    const opts = _chartCaptureOpts.get(el.dataset.chart) || {};
    return el instanceof SVGElement
      ? _svgToPngBlob(el, 2)
      : _htmlToPngBlob(el, { scale: 2, ...opts });
  }

  async function _copyChartPng(el) {
    const blobPromise = _chartPngBlobPromise(el);
    // 1) Preferred path — write to clipboard. Pass the BLOB PROMISE
    //    (not an awaited Blob) so the ClipboardItem is constructed
    //    synchronously inside the user gesture, satisfying Safari's
    //    user-activation requirement for the async-clipboard API.
    if (navigator.clipboard?.write && typeof ClipboardItem !== 'undefined') {
      try {
        await navigator.clipboard.write([new ClipboardItem({ 'image/png': blobPromise })]);
        toast('Plot copied as PNG');
        return;
      } catch (err) {
        // Falls through to the download fallback. Common causes: the
        // permission was denied, the document lost focus mid-render,
        // or the browser does not allow image MIME types on the
        // clipboard (Firefox until recently).
        console.warn('Clipboard image write failed — falling back to download:', err);
      }
    }
    // 2) Fallback — download the PNG. Always works, no permissions.
    try {
      const blob = await blobPromise;
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = (el.dataset.chart || 'chart') + '.png';
      a.click();
      URL.revokeObjectURL(url);
      toast('Saved as PNG (clipboard unavailable)');
    } catch (err) {
      console.error('Copy PNG failed:', err);
      toast('Copy PNG failed — ' + (err?.message || 'see console'));
    }
  }

  function _formatCell(v) {
    if (v == null) return '';
    if (typeof v === 'number') return Number.isFinite(v) ? String(v) : '';
    // Strip tabs and newlines so the TSV stays a clean grid.
    return String(v).replace(/[\t\r\n]+/g, ' ');
  }
  async function _copyChartData(dataFn) {
    try {
      const data = dataFn();
      if (!data || !Array.isArray(data.rows) || !data.rows.length) {
        toast('No data to copy');
        return;
      }
      const headers = data.headers || [];
      const lines = [headers.map(_formatCell).join('\t')];
      data.rows.forEach((r) => lines.push(r.map(_formatCell).join('\t')));
      const tsv = lines.join('\n');
      await navigator.clipboard.writeText(tsv);
      toast(`Copied ${data.rows.length} row${data.rows.length === 1 ? '' : 's'} of data`);
    } catch (err) {
      console.error('Copy data failed:', err);
      toast('Copy data failed — ' + (err?.message || 'see console'));
    }
  }

  // Delegated contextmenu — one listener for the whole app. Captures
  // right-clicks anywhere inside a `.copyable-chart` SVG (including on
  // bars/lines/etc., since "empty space" inside a busy chart is hard to
  // hit). Bails when a more-specific handler has already preventDefault'd
  // the event (e.g. the risk-matrix right-click on a dot opens that dot's
  // edit menu — we don't want to also show the chart copy menu on top).
  document.addEventListener('contextmenu', (e) => {
    if (e.defaultPrevented) return;
    let svg = e.target.closest('.copyable-chart');
    if (!svg) return;
    // Prefer the OUTERmost copyable wrapper. Chart cards (panels,
    // budget cards, sm-cells) typically contain a chart SVG that was
    // itself marked copyable in an earlier iteration; the wrapper
    // includes the title / legend / footer the user wants captured.
    let outer = svg.parentElement?.closest('.copyable-chart');
    while (outer) {
      svg = outer;
      outer = svg.parentElement?.closest('.copyable-chart');
    }
    e.preventDefault();
    const chartId = svg.dataset.chart;
    const dataFn = chartId ? _chartDataExtractors.get(chartId) : null;
    const items = [
      { icon: '🖼', label: 'Copy as PNG',  onClick: () => _copyChartPng(svg) },
    ];
    if (dataFn) items.push({ icon: '⊞', label: 'Copy as data', onClick: () => _copyChartData(dataFn) });
    showContextMenu(e.clientX, e.clientY, items);
  });

  function openPersonEditor(personId) {
    const p = state.people.find((x) => x.id === personId);
    if (!p) return;
    p.assignments = Array.isArray(p.assignments) ? p.assignments : [];
    const projectRow = (proj) => {
      const existing = p.assignments.find((a) => a.projectId === proj.id);
      const val = existing ? existing.commitment : 0;
      return `
        <div class="assign-row">
          <label class="assign-name" for="assign-${escapeHTML(proj.id)}">${escapeHTML(proj.name)}</label>
          <input type="number" min="0" max="100" step="5" class="assign-input" id="assign-${escapeHTML(proj.id)}" data-project-id="${escapeHTML(proj.id)}" value="${val}" />
          <span class="assign-unit">%</span>
        </div>`;
    };
    const visibleProjects = state.projects.filter((pr) => !pr.archived);
    $('#drawerTitle').textContent = 'Edit person';
    $('#drawerBody').innerHTML = `
      <div class="field"><label>Name</label><input id="pEdName" value="${escapeHTML(p.name)}" /></div>
      <div class="field"><label>Role</label><input id="pEdRole" value="${escapeHTML(p.role || '')}" placeholder="Job title" /></div>
      <div class="field"><label>Expertise / skills</label><textarea id="pEdSkills" placeholder="e.g. Avionics design, EMC testing">${escapeHTML(p.expertise || '')}</textarea></div>
      <div style="display:grid; grid-template-columns:1fr 1fr; gap:10px;">
        <div class="field"><label>Capacity (% of FTE)</label><input id="pEdCap" type="number" min="0" max="200" value="${p.capacity || 100}" title="100% = full-time. 1 FTE = 8h/day × 5 days/week, 212 working days/year." /></div>
        <div class="field"><label>Hourly rate (€/h)</label><input id="pEdRate" type="number" min="0" step="5" value="${p.hourlyRate || 100}" /></div>
      </div>
      <div class="field">
        <label>Project assignments <span class="muted" id="pEdAssignSum">— % assigned</span></label>
        <div class="assign-hint">Share of this person's FTE dedicated to each active project. Sum should be ≤ Capacity above.</div>
        <div class="assign-list">
          ${visibleProjects.length ? visibleProjects.map(projectRow).join('') : '<div class="empty">No live projects to assign to.</div>'}
        </div>
      </div>
      <div style="display:flex; gap:8px; margin-top:6px;">
        <button class="primary" id="pEdSave">Save</button>
        <button class="ghost" id="pEdDelete" style="margin-left:auto; color:var(--bad);">Delete</button>
      </div>`;
    $('#drawer').hidden = false;
    // Live sum indicator — updates as the user types.
    const refreshSum = () => {
      const inputs = document.querySelectorAll('#drawerBody .assign-input');
      let sum = 0;
      inputs.forEach((inp) => { sum += Math.max(0, Number(inp.value) || 0); });
      const cap = Math.max(0, parseInt($('#pEdCap').value, 10) || 0);
      const el = $('#pEdAssignSum');
      if (el) {
        const over = cap > 0 && sum > cap;
        el.textContent = `${sum}% assigned of ${cap}% capacity${over ? ' — over-committed' : ''}`;
        el.classList.toggle('over', !!over);
      }
    };
    document.querySelectorAll('#drawerBody .assign-input').forEach((inp) => inp.addEventListener('input', refreshSum));
    $('#pEdCap').addEventListener('input', refreshSum);
    refreshSum();
    $('#pEdSave').addEventListener('click', () => {
      const oldName = p.name;
      p.name = $('#pEdName').value.trim() || p.name;
      p.role = $('#pEdRole').value.trim();
      p.expertise = $('#pEdSkills').value.trim();
      p.capacity = clamp(parseInt($('#pEdCap').value, 10) || 100, 0, 200);
      p.hourlyRate = Math.max(0, parseFloat($('#pEdRate').value) || 100);
      const newAssignments = [];
      document.querySelectorAll('#drawerBody .assign-input').forEach((inp) => {
        const projectId = inp.dataset.projectId;
        const commitment = clamp(parseInt(inp.value, 10) || 0, 0, 100);
        if (commitment > 0) newAssignments.push({ projectId, commitment });
      });
      p.assignments = newAssignments;
      commit('person-edit');
      closeDrawer();
      toast(oldName !== p.name ? 'Renamed to ' + p.name : 'Saved');
    });
    $('#pEdDelete').addEventListener('click', () => {
      const open = state.projects.flatMap((pr) => pr.actions || []).filter((a) => a.owner === p.id && a.status !== 'done').length;
      if (!confirm(`Delete ${p.name}?` + (open ? ` (${open} open action${open === 1 ? '' : 's'} will be unassigned).` : ''))) return;
      state.projects.forEach((pr) => (pr.actions || []).forEach((a) => { if (a.owner === p.id) a.owner = null; }));
      state.people = state.people.filter((x) => x.id !== p.id);
      commit('person-delete');
      closeDrawer();
      toast('Deleted');
    });
  }

  /* ---------------------------- Notes panel -------------------------- */

  let notesSaveTimer = null;
  let savedRange = null; // selection range in the notes body, captured before opening modals

  function notesIsOpen() { return !$('#notesPanel').hidden; }

  // Notes panel width (px) — clamped to keep the panel usable on small screens
  // and to leave room for the main view.
  const NOTES_W_MIN = 240;
  const NOTES_W_MAX = 720;
  const NOTES_W_DEFAULT = 340;
  /**
   * Generic drag-to-reorder for any list. Each row needs a `.row-grip`
   * element (the drag handle) and a data attribute carrying the item's id.
   * @param {Element} listEl       container that holds the rows directly
   * @param {object}  opts
   *   - rowSelector  CSS selector that matches the rows (e.g. '.row[data-component-id]')
   *   - idAttr       dataset key on each row (e.g. 'componentId')
   *   - getArray     () => array of items in current order (live reference)
   *   - setOrder     (idsInOrder: string[]) => void; mutate the source array
   *   - commitName   action name passed to commit() after a real reorder
   */
  function wireListReorder(listEl, opts) {
    if (!listEl) return;
    const rows = listEl.querySelectorAll(opts.rowSelector);
    rows.forEach((row) => {
      const grip = row.querySelector('.row-grip');
      if (!grip) return;
      grip.addEventListener('mousedown', (e) => {
        e.preventDefault();
        e.stopPropagation();
        row.classList.add('dragging');
        document.body.classList.add('is-row-dragging');
        const onMove = (em) => {
          const siblings = [...listEl.querySelectorAll(opts.rowSelector + ':not(.dragging)')];
          const after = siblings.find((sib) => {
            const r = sib.getBoundingClientRect();
            return em.clientY < r.top + r.height / 2;
          });
          if (after) listEl.insertBefore(row, after);
          else listEl.appendChild(row);
        };
        const onUp = () => {
          row.classList.remove('dragging');
          document.body.classList.remove('is-row-dragging');
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          const newOrder = [...listEl.querySelectorAll(opts.rowSelector)].map((r) => r.dataset[opts.idAttr]);
          const arr = opts.getArray();
          const before = arr.map((x) => x.id).join(',');
          if (newOrder.join(',') === before) return; // no-op
          opts.setOrder(newOrder);
          commit(opts.commitName || 'reorder');
        };
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
      });
    });
  }

  // Tiny inline grip glyph used by every reorderable list row
  const ROW_GRIP_HTML = '<span class="row-grip" title="Drag to reorder" aria-hidden="true">⋮⋮</span>';

  function clampNotesWidth(w) {
    const max = Math.min(NOTES_W_MAX, Math.max(NOTES_W_MIN, innerWidth - 360));
    return Math.max(NOTES_W_MIN, Math.min(max, w));
  }
  function applyNotesWidth(w) {
    const px = clampNotesWidth(w);
    document.documentElement.style.setProperty('--notes-w', px + 'px');
    const app = $('#app');
    if (app) app.style.setProperty('--notes-w', px + 'px');
    return px;
  }

  function applyNotesPanel() {
    const open = state.notesOpen === true;
    const panel = $('#notesPanel');
    const app = $('#app');
    if (!panel || !app) return;
    state.settings = state.settings || {};
    applyNotesWidth(state.settings.notesWidth || NOTES_W_DEFAULT);
    if (open) {
      panel.hidden = false;
      app.classList.add('notes-open');
      loadNotesForCurrentProject();
    } else {
      panel.hidden = true;
      app.classList.remove('notes-open');
    }
  }

  function loadNotesForCurrentProject() {
    const proj = curProject();
    if (!proj) return;
    state.notes = state.notes || {};
    const body = $('#notesBody');
    const html = state.notes[proj.id];
    body.innerHTML = html || `<p><i>Notes for <b>${escapeHTML(proj.name)}</b> — type freely. Use the toolbar to format and to insert actions assigned to people.</i></p><p></p>`;
    $('#notesMeta').textContent = proj.name;
    buildNotesToc();
    // Tag each block's language so the browser's native spellcheck
    // picks the right dictionary on first open, not just after typing.
    applyNotesLangDetection();
    applyNotesTocHeight();
  }

  function saveNotesNow() {
    const proj = curProject();
    if (!proj) return;
    state.notes = state.notes || {};
    state.notes[proj.id] = $('#notesBody').innerHTML;
    saveState();
    const s = $('#notesSaved');
    if (s) { s.textContent = 'Saved'; s.classList.remove('saving'); }
    buildNotesToc();
  }

  // Auto-built table of contents — scans the notes for H1–H6 elements,
  // assigns stable ids, and renders a compact clickable list above the
  // editable body. Hidden when there are no headings (keeps the panel calm).
  function buildNotesToc() {
    const body = $('#notesBody');
    const toc  = $('#notesToc');
    if (!body || !toc) return;
    const headings = body.querySelectorAll('h1, h2, h3, h4, h5, h6');
    const items = [];
    headings.forEach((h, i) => {
      const text = (h.textContent || '').trim();
      if (!text) return;
      // Stable id per heading position; preserve any author-set id.
      if (!h.id) {
        const slug = text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 32) || 'h';
        h.id = `nt-h-${i}-${slug}`;
      }
      items.push({ id: h.id, text, lvl: parseInt(h.tagName[1], 10) || 3 });
    });
    const resize = $('#notesTocResize');
    if (!items.length) {
      toc.hidden = true; toc.innerHTML = '';
      if (resize) resize.hidden = true;
      return;
    }
    toc.innerHTML = `
      <div class="notes-toc-head">Contents</div>
      <ol class="notes-toc-list">
        ${items.map((it) => `
          <li class="notes-toc-item lvl-${it.lvl}">
            <a href="#${escapeHTML(it.id)}" data-toc-target="${escapeHTML(it.id)}">${escapeHTML(it.text)}</a>
          </li>`).join('')}
      </ol>`;
    toc.hidden = false;
    if (resize) resize.hidden = false;
    // Apply the user's persisted TOC height (if any) on every rebuild
    // — TOC content changes but the chosen visual height should stick.
    applyNotesTocHeight();
  }

  // Apply state.settings.notesTocHeight (px) to the TOC element. Clamped
  // to a sensible range so a stale very-small or very-large persisted
  // value doesn't make the TOC unusable.
  function applyNotesTocHeight() {
    const toc = $('#notesToc');
    if (!toc) return;
    const h = state.settings?.notesTocHeight;
    if (typeof h === 'number' && h > 0) {
      toc.style.height = Math.max(36, Math.min(900, h)) + 'px';
    } else {
      toc.style.height = '';
    }
  }
  // ---- Per-line language detection ----------------------------------
  // Lightweight signature-word detector. Each language has a small set
  // of high-frequency function words; counting how many of each block's
  // tokens fall into each language's set gives a usable score for
  // blocks of ≥ ~6 words. Shorter blocks inherit the nearest previous
  // block's language so spell-check still uses a sensible dictionary.
  //
  // The detector sets each block's `lang` attribute; the browser's
  // native spellcheck (Path A) picks the matching dictionary, draws
  // squiggle underlines for unknown words, and supplies its own
  // right-click suggestions menu — all without our help.
  const NOTES_LANG_SIGS = {
    en: new Set(['the','and','of','to','in','is','it','that','for','was','with','this','are','from','have','not','but','they','you','will','can','their','has','one','our','its']),
    fr: new Set(['le','la','les','des','de','et','que','un','une','est','dans','pour','avec','plus','mais','nous','vous','sont','aux','par','sur','ils','elle','elles','ses','ces','ont','cette','comme','tout','sans','si','où','même','aussi']),
    de: new Set(['der','die','das','und','ist','von','den','ein','eine','mit','sich','nicht','auch','aber','sind','aus','dem','wenn','wir','sie','ich','zu','auf','für','im','an','des','dass','war','noch','nur','wie','oder','als','bei']),
    es: new Set(['el','la','los','las','de','que','y','es','un','una','en','para','con','por','sobre','del','sus','este','esta','son','como','más','también','pero','sin','muy','sus','está','han','hay','ese','esa','esto']),
    it: new Set(['il','la','le','di','e','che','non','un','una','del','dei','per','con','sono','come','più','anche','ma','dove','gli','degli','della','delle','dal','dalla','suoi','questa','questo','molto']),
    pt: new Set(['o','a','os','as','de','que','e','do','da','em','para','com','não','uma','um','por','isso','mais','ser','muito','está','são','dos','das','isso','também','seu','sua','pelo','pela']),
  };
  function detectNotesLang(text) {
    const tokens = (text || '').toLowerCase().match(/[a-záàâäçéèêëîïôöûùüÿñæœßãõ]+/g) || [];
    if (tokens.length < 6) return null; // not enough signal
    let best = null, bestScore = 0;
    for (const lang of Object.keys(NOTES_LANG_SIGS)) {
      const sig = NOTES_LANG_SIGS[lang];
      let hits = 0;
      for (const t of tokens) if (sig.has(t)) hits++;
      const score = hits / tokens.length;
      if (score > bestScore) { best = lang; bestScore = score; }
    }
    // Require at least 4 % of tokens to be signature words — guards
    // against false positives on technical / sparse text.
    return bestScore >= 0.04 ? best : null;
  }
  // Walk every direct block-level descendant and tag it with `lang`.
  // Short blocks inherit the previous block's language so a one-line
  // comment doesn't fall back to the browser's UI locale.
  function applyNotesLangDetection() {
    const body = $('#notesBody');
    if (!body) return;
    const blocks = body.querySelectorAll('p, h1, h2, h3, h4, h5, h6, li, blockquote, div');
    let lastLang = body.getAttribute('lang') || 'en';
    blocks.forEach((b) => {
      const lang = detectNotesLang(b.textContent || '') || lastLang;
      if (b.getAttribute('lang') !== lang) b.setAttribute('lang', lang);
      lastLang = lang;
    });
    if (body.getAttribute('lang') !== lastLang) body.setAttribute('lang', lastLang);
  }
  let notesLangTimer = null;
  function scheduleNotesLangDetection() {
    clearTimeout(notesLangTimer);
    // Run after a longer pause than save — language doesn't shift mid-sentence
    // and the walk has measurable cost on long documents.
    notesLangTimer = setTimeout(applyNotesLangDetection, 700);
  }

  function scheduleNotesSave() {
    clearTimeout(notesSaveTimer);
    const s = $('#notesSaved');
    if (s) { s.textContent = 'Saving…'; s.classList.add('saving'); }
    // Reflect the unsaved state in the global storage pill too, not just
    // the notes-panel status. The pill clears once saveNotesNow → saveState
    // runs at the end of the debounce window.
    markDirty();
    notesSaveTimer = setTimeout(saveNotesNow, 350);
  }

  function snapshotSelection() {
    const sel = window.getSelection();
    const body = $('#notesBody');
    if (sel.rangeCount && body.contains(sel.anchorNode)) {
      savedRange = sel.getRangeAt(0).cloneRange();
    }
  }
  function restoreSelection() {
    if (!savedRange) {
      const body = $('#notesBody');
      body.focus();
      const sel = window.getSelection();
      const range = document.createRange();
      range.selectNodeContents(body);
      range.collapse(false);
      sel.removeAllRanges(); sel.addRange(range);
      return;
    }
    const sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(savedRange);
    $('#notesBody').focus();
  }

  function insertActionChip(action) {
    restoreSelection();
    const due = action.due ? fmtDate(action.due) : '—';
    const owner = personName(action.owner);
    const safe = (s) => String(s).replace(/[<>"&]/g, (c) =>
      ({ '<': '&lt;', '>': '&gt;', '"': '&quot;', '&': '&amp;' }[c]));
    const html = ` <span class="note-chip" contenteditable="false" data-action-id="${action.id}">` +
      `<span class="chip-mark">✓</span>` +
      `<b>${safe(action.title)}</b>` +
      `<span class="chip-meta">${safe(owner)} · ${safe(due)}</span>` +
      `</span>&nbsp;`;
    document.execCommand('insertHTML', false, html);
    scheduleNotesSave();
  }

  /* --- Inline @/# autocomplete inside the meeting-notes editor ---
   * Typing `@` opens a people picker; on selection the Quick Add form
   * opens for a brand-new action with that person pre-set as Owner
   * (replacing the old "insert a person mention chip" behaviour).
   * Typing `#` opens an existing-action picker; on selection an action
   * chip is inserted that links to the action's drawer.
   *
   * Popup filters as the user types; ArrowUp / ArrowDown navigate,
   * Enter / Tab insert, Escape dismisses. Legacy person-chips in
   * older notes (saved before this change) still render and stay
   * click-to-filter-Register thanks to the chip click handler below.
   */
  let notesAcState = null;

  function getMentionContext(body) {
    const sel = window.getSelection();
    if (!sel.rangeCount) return null;
    const range = sel.getRangeAt(0);
    if (!range.collapsed) return null;
    if (!body.contains(range.startContainer)) return null;
    const node = range.startContainer;
    if (node.nodeType !== Node.TEXT_NODE) return null;
    const text = node.textContent.slice(0, range.startOffset);
    // Trigger at start-of-line or after whitespace; query allows letters,
    // digits, hyphen, underscore, and a single space so "Sofia R" works.
    const m = text.match(/(?:^|[\s ])([@#])([\w\-]*(?: [\w\-]*)?)$/);
    if (!m) return null;
    const kind = m[1];
    const query = m[2];
    const triggerStart = range.startOffset - query.length - 1;
    if (triggerStart < 0) return null;
    const triggerRange = document.createRange();
    triggerRange.setStart(node, triggerStart);
    triggerRange.setEnd(node, range.startOffset);
    return { kind, query, triggerRange };
  }

  function showNotesAutocomplete(ctx) {
    const popup = $('#notesAutocomplete');
    if (!popup) return;
    const q = ctx.query.toLowerCase();
    let items;
    if (ctx.kind === '@') {
      items = state.people
        .filter((p) => p.name.toLowerCase().includes(q))
        .slice(0, 8);
      // If the typed query doesn't match an existing person, surface a
      // synthetic "+ Add '<query>' as a new person…" option so the user
      // can create the person inline (same dialog the Quick Add owner
      // dropdown uses). Requires at least 1 character so an empty @
      // doesn't offer to create a blank person.
      const trimmed = ctx.query.trim();
      if (trimmed.length > 0) {
        const exactMatch = items.find((p) => p.name.toLowerCase() === q);
        if (!exactMatch) items.push({ __newPerson: true, name: trimmed });
      }
    } else {
      items = state.projects
        .flatMap((proj) => (proj.actions || []).filter((a) => !a.archivedAt).map((a) => ({ ...a, _proj: proj })))
        .filter((a) => a.title.toLowerCase().includes(q))
        .slice(0, 8);
    }
    if (!items.length) { hideNotesAutocomplete(); return; }
    notesAcState = { kind: ctx.kind, query: ctx.query, items, idx: 0, triggerRange: ctx.triggerRange };
    renderNotesAutocomplete();
    positionNotesAutocomplete();
  }

  function hideNotesAutocomplete() {
    notesAcState = null;
    const popup = $('#notesAutocomplete');
    if (popup) { popup.hidden = true; popup.innerHTML = ''; }
  }

  function renderNotesAutocomplete() {
    const popup = $('#notesAutocomplete');
    if (!popup || !notesAcState) return;
    const { kind, items, idx } = notesAcState;
    popup.innerHTML = items.map((it, i) => {
      const sel = i === idx ? 'active' : '';
      if (kind === '@') {
        if (it.__newPerson) {
          return `<button type="button" class="ac-item ac-item-new ${sel}" data-ac-idx="${i}" role="option" title="Create a new person and start an action for them">
            <span class="avatar ac-avatar-new">+</span>
            <span class="ac-text">
              <span class="ac-name">Add &ldquo;${escapeHTML(it.name)}&rdquo; as a new person…</span>
              <span class="ac-meta">Press Enter — opens the add-person form, then a new action</span>
            </span>
          </button>`;
        }
        return `<button type="button" class="ac-item ${sel}" data-ac-idx="${i}" role="option">
          <span class="avatar">${initials(it.name)}</span>
          <span class="ac-text"><span class="ac-name">${escapeHTML(it.name)}</span><span class="ac-meta">${escapeHTML(it.role || '')}</span></span>
        </button>`;
      }
      const due = it.due ? fmtDate(it.due) : '—';
      return `<button type="button" class="ac-item ${sel}" data-ac-idx="${i}" role="option">
        <span class="ac-icon">▤</span>
        <span class="ac-text"><span class="ac-name">${escapeHTML(it.title)}</span><span class="ac-meta">${escapeHTML(personName(it.owner))} · ${due}</span></span>
      </button>`;
    }).join('');
    popup.hidden = false;
  }

  function positionNotesAutocomplete() {
    const popup = $('#notesAutocomplete');
    if (!popup || !notesAcState) return;
    const r = notesAcState.triggerRange.getBoundingClientRect();
    let left = r.left;
    let top  = r.bottom + 4;
    // Keep on-screen — flip above if there's no room below
    const popupRect = popup.getBoundingClientRect();
    if (top + popupRect.height > innerHeight - 8) {
      top = Math.max(8, r.top - popupRect.height - 4);
    }
    if (left + popupRect.width > innerWidth - 8) {
      left = Math.max(8, innerWidth - popupRect.width - 8);
    }
    popup.style.left = left + 'px';
    popup.style.top  = top  + 'px';
  }

  function selectAutocompleteItem(idx) {
    if (!notesAcState) return;
    const it = notesAcState.items[idx];
    if (!it) return;
    const range = notesAcState.triggerRange;
    range.deleteContents();
    const sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(range);
    savedRange = range.cloneRange();
    snapshotSelection();
    const kind = notesAcState.kind;
    hideNotesAutocomplete();
    if (kind === '@') {
      // New seamless flow: picking a person via @ pops Quick Add for a
      // brand-new action with that person pre-set as Owner. If the user
      // typed a name that doesn't match anyone yet ("__newPerson"
      // synthetic item), first open the inline add-person dialog with
      // the typed name pre-filled — then continue to Quick Add with the
      // freshly-created person as Owner. Result: type "@bob" and three
      // confirmations later you have a new person AND a new action
      // assigned to them, linked from this note.
      const openActionFor = (ownerId) => {
        openQuickAdd('action', { owner: ownerId }, (action) => {
          if (action) insertActionChip(action);
        });
      };
      if (it.__newPerson) {
        createPersonInlineDialog(it.name).then((newId) => {
          if (newId) openActionFor(newId);
        });
        return;
      }
      openActionFor(it.id);
      return;
    }
    insertActionChip(it);
  }

  function refreshNoteChips() {
    const body = $('#notesBody');
    if (!body) return;
    body.querySelectorAll('.note-chip').forEach((chip) => {
      const id = chip.dataset.actionId;
      const a = state.projects.flatMap((p) => p.actions || []).find((x) => x.id === id);
      if (!a) {
        chip.classList.add('chip-stale');
        return;
      }
      chip.classList.remove('chip-stale');
      // Status glyphs:
      //   done      ✓
      //   blocked   ⨯
      //   doing     ◐
      //   cancelled ⊘   (distinct from "todo" — visually struck-through too)
      //   todo / *  ○
      const mark =
        a.status === 'done'       ? '✓' :
        a.status === 'blocked'    ? '⨯' :
        a.status === 'doing'      ? '◐' :
        a.status === 'cancelled'  ? '⊘' : '○';
      const markEl = chip.querySelector('.chip-mark');
      if (markEl) markEl.textContent = mark;
      chip.classList.toggle('done',      a.status === 'done');
      chip.classList.toggle('blocked',   a.status === 'blocked');
      chip.classList.toggle('cancelled', a.status === 'cancelled');
    });
  }

  function wireNotesPanel() {
    const panel = $('#notesPanel');
    if (!panel || panel.dataset.wired === '1') return;
    panel.dataset.wired = '1';

    $('#btnNotesClose').addEventListener('click', () => {
      state.notesOpen = false;
      saveState();
      applyNotesPanel();
    });

    // Drag-to-resize on the panel's left edge
    const handle = $('#notesResize');
    if (handle) {
      handle.addEventListener('mousedown', (e) => {
        e.preventDefault();
        const startX = e.clientX;
        const startW = parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--notes-w')) || NOTES_W_DEFAULT;
        handle.classList.add('dragging');
        document.body.classList.add('is-notes-resizing');
        const onMove = (em) => {
          // Dragging right shrinks the panel (cursor moves toward main content);
          // dragging left expands it.
          const delta = startX - em.clientX;
          applyNotesWidth(startW + delta);
        };
        const onUp = () => {
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          handle.classList.remove('dragging');
          document.body.classList.remove('is-notes-resizing');
          // Persist the final width
          const px = parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--notes-w')) || NOTES_W_DEFAULT;
          state.settings = state.settings || {};
          state.settings.notesWidth = Math.round(px);
          saveState();
        };
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
      });
      // Double-click to reset to the default width
      handle.addEventListener('dblclick', () => {
        applyNotesWidth(NOTES_W_DEFAULT);
        state.settings = state.settings || {};
        state.settings.notesWidth = NOTES_W_DEFAULT;
        saveState();
      });
    }
    // TOC resize handle — drag vertically to change the table-of-contents
    // height; double-click to reset to default (~30 vh). Mirrors the
    // panel-width pattern above.
    const tocHandle = $('#notesTocResize');
    const tocEl     = $('#notesToc');
    if (tocHandle && tocEl) {
      tocHandle.addEventListener('mousedown', (e) => {
        e.preventDefault();
        const startY = e.clientY;
        const startH = tocEl.getBoundingClientRect().height;
        tocHandle.classList.add('is-dragging');
        document.body.classList.add('is-notes-resizing');
        const onMove = (em) => {
          const next = Math.round(startH + (em.clientY - startY));
          state.settings = state.settings || {};
          state.settings.notesTocHeight = Math.max(36, Math.min(900, next));
          applyNotesTocHeight();
        };
        const onUp = () => {
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          tocHandle.classList.remove('is-dragging');
          document.body.classList.remove('is-notes-resizing');
          saveState();
        };
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
      });
      tocHandle.addEventListener('dblclick', () => {
        state.settings = state.settings || {};
        delete state.settings.notesTocHeight;
        applyNotesTocHeight();
        saveState();
      });
    }
    // Re-clamp when the window resizes (keeps the panel usable if the user
    // shrinks the viewport while the panel is wide).
    window.addEventListener('resize', () => {
      if (state.notesOpen) {
        applyNotesWidth(state.settings?.notesWidth || NOTES_W_DEFAULT);
      }
    });

    // Toolbar formatting
    panel.querySelectorAll('.notes-toolbar [data-cmd]').forEach((btn) => {
      // mousedown to preserve selection in contenteditable
      btn.addEventListener('mousedown', (e) => {
        e.preventDefault();
        document.execCommand(btn.dataset.cmd, false, btn.dataset.arg || null);
        $('#notesBody').focus();
        scheduleNotesSave();
      });
    });
    // Font-colour picker — applies foreColor to current selection. The bar
    // beneath the "A" glyph previews the most recently chosen colour.
    const ntColor    = $('#ntColorInput');
    const ntColorBar = $('#ntColorBar');
    if (ntColor) {
      // Restore last-used colour from settings
      const last = (state.settings && state.settings.notesColor) || '#6ea8ff';
      ntColor.value = last;
      if (ntColorBar) ntColorBar.style.background = last;
      ntColor.addEventListener('input', (e) => {
        const c = e.target.value;
        $('#notesBody').focus();
        document.execCommand('foreColor', false, c);
        if (ntColorBar) ntColorBar.style.background = c;
        state.settings = state.settings || {};
        state.settings.notesColor = c;
        scheduleNotesSave();
      });
      // Keep the swatch from stealing focus from the editor on click
      ntColor.parentElement.addEventListener('mousedown', (e) => { if (e.target === ntColor.parentElement) e.preventDefault(); });
    }

    const body = $('#notesBody');
    body.addEventListener('input', () => {
      scheduleNotesSave();
      scheduleNotesLangDetection();
      // Update the @/# autocomplete based on what's now under the caret
      const ctx = getMentionContext(body);
      if (ctx) showNotesAutocomplete(ctx);
      else hideNotesAutocomplete();
    });
    body.addEventListener('keydown', (e) => {
      // Autocomplete navigation takes priority when the popup is open
      if (notesAcState) {
        if (e.key === 'ArrowDown') {
          e.preventDefault();
          notesAcState.idx = (notesAcState.idx + 1) % notesAcState.items.length;
          renderNotesAutocomplete();
          return;
        }
        if (e.key === 'ArrowUp') {
          e.preventDefault();
          notesAcState.idx = (notesAcState.idx - 1 + notesAcState.items.length) % notesAcState.items.length;
          renderNotesAutocomplete();
          return;
        }
        if (e.key === 'Enter' || e.key === 'Tab') {
          e.preventDefault();
          selectAutocompleteItem(notesAcState.idx);
          return;
        }
        if (e.key === 'Escape') {
          e.preventDefault();
          hideNotesAutocomplete();
          return;
        }
      }
      // Shift+Cmd/Ctrl+A → insert (new) action
      if ((e.metaKey || e.ctrlKey) && e.shiftKey && e.key.toLowerCase() === 'a') {
        e.preventDefault();
        snapshotSelection();
        openQuickAdd('action', {}, (action) => insertActionChip(action));
      }
      // Tab / Shift+Tab — indent / outdent inside a list item. Browsers
      // normally use Tab to move focus; inside a list we want it to
      // nest. Outside a list, swallow Tab so the editor doesn't lose
      // focus (matches every other rich-text tool).
      if (e.key === 'Tab') {
        const sel = window.getSelection();
        const node = sel?.anchorNode;
        if (node) {
          const inLi = (node.nodeType === 1 ? node : node.parentElement)?.closest('li');
          if (inLi) {
            e.preventDefault();
            document.execCommand(e.shiftKey ? 'outdent' : 'indent');
            scheduleNotesSave();
            return;
          }
        }
        // Outside a list — keep Tab as a no-op so focus doesn't escape
        // (rather than moving focus to the toolbar).
        e.preventDefault();
      }
    });
    body.addEventListener('click', (e) => {
      const chip = e.target.closest('.note-chip');
      if (!chip) return;
      e.preventDefault();
      // Person chips → filter Register to that person; action chips → drawer
      if (chip.classList.contains('person-chip') && chip.dataset.personId) {
        applyTopbarFilter({ owner: chip.dataset.personId, view: 'register' });
      } else if (chip.dataset.actionId) {
        openDrawer(chip.dataset.actionId);
      }
    });
    // Close the autocomplete when the body loses focus or scrolls
    body.addEventListener('blur',   () => setTimeout(hideNotesAutocomplete, 150));
    body.addEventListener('scroll', hideNotesAutocomplete);
    // Click an autocomplete item → insert
    const popup = $('#notesAutocomplete');
    if (popup) {
      popup.addEventListener('mousedown', (e) => {
        e.preventDefault(); // keep selection in body
        const btn = e.target.closest('.ac-item[data-ac-idx]');
        if (!btn) return;
        selectAutocompleteItem(parseInt(btn.dataset.acIdx, 10));
      });
      popup.addEventListener('mouseover', (e) => {
        const btn = e.target.closest('.ac-item[data-ac-idx]');
        if (!btn || !notesAcState) return;
        const idx = parseInt(btn.dataset.acIdx, 10);
        if (idx !== notesAcState.idx) {
          notesAcState.idx = idx;
          renderNotesAutocomplete();
        }
      });
    }

    // TOC navigation — click a contents entry → scroll the heading into view
    const toc = $('#notesToc');
    if (toc) {
      toc.addEventListener('click', (e) => {
        const a = e.target.closest('a[data-toc-target]');
        if (!a) return;
        e.preventDefault();
        const id = a.dataset.tocTarget;
        const target = body.querySelector('#' + (window.CSS && CSS.escape ? CSS.escape(id) : id));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth', block: 'start' });
          // Subtle pulse to confirm where we landed
          target.classList.add('nt-flash');
          setTimeout(() => target.classList.remove('nt-flash'), 900);
        }
      });
    }
  }

  /* --------------------------- Import/Export ------------------------- */

  // Current export schema version. Bump on a breaking schema change.
  const EXPORT_SCHEMA_VERSION = 2;

  // Walk every collection and backfill defaults so that any state — produced by
  // an older app version, hand-edited JSON, or imported from elsewhere — has all
  // fields the rest of the app expects. Idempotent.
  function normalizeState(s) {
    if (!s || typeof s !== 'object') throw new Error('Invalid state');
    s.people = Array.isArray(s.people) ? s.people : [];
    s.stakeholders = Array.isArray(s.stakeholders) ? s.stakeholders : [];
    s.projects = Array.isArray(s.projects) ? s.projects : [];
    // People carry per-project assignments — see the People panel editor.
    // Shape: [{ projectId, commitment }] where commitment is % of THIS
    // person's FTE. Sum should be ≤ capacity. Missing = not on project.
    s.people.forEach((p) => {
      if (!Array.isArray(p.assignments)) p.assignments = [];
      p.assignments = p.assignments.filter((a) => a && typeof a.projectId === 'string' && a.projectId);
      p.assignments.forEach((a) => {
        a.commitment = Math.max(0, Math.min(100, Number(a.commitment) || 0));
      });
    });
    // Stakeholders — external contacts (customer POCs, regulators, sister
    // projects). No capacity, no hourly rate. Still ownable so we can
    // track actions we're waiting on outside the team.
    s.stakeholders.forEach((st) => {
      st.id = st.id || uid('st');
      st.name = st.name || 'Unnamed stakeholder';
      st.role = st.role || '';
      st.organization = st.organization || '';
    });
    s.settings = s.settings || {};
    s.settings.theme = s.settings.theme || 'dark';
    s.settings.holidayCountries = s.settings.holidayCountries || [];
    if (s.settings.budgetView !== 'hours' && s.settings.budgetView !== 'cost') s.settings.budgetView = 'cost';
    if (s.settings.budgetGroupBy !== 'component' && s.settings.budgetGroupBy !== 'person') s.settings.budgetGroupBy = 'component';
    // Legacy global window — replaced by per-CC date ranges (below).
    // Kept on the schema for backward compat with older exports, but no
    // longer read at render time.
    if (![52, 104, 156, 260].includes(s.settings.budgetWeeks)) s.settings.budgetWeeks = 104;
    // Per-cost-centre date range. Map of cc-code → { start, end } in
    // ISO YYYY-MM-DD; missing entries fall back to a 2-year default
    // centred on today. Filter out malformed entries.
    if (!s.settings.budgetRanges || typeof s.settings.budgetRanges !== 'object') s.settings.budgetRanges = {};
    Object.keys(s.settings.budgetRanges).forEach((k) => {
      const v = s.settings.budgetRanges[k];
      if (!v || typeof v.start !== 'string' || typeof v.end !== 'string') {
        delete s.settings.budgetRanges[k];
      }
    });
    // Notes panel width in pixels — added later; default keeps full-screen layouts breathing.
    if (typeof s.settings.notesWidth !== 'number' || s.settings.notesWidth < 200 || s.settings.notesWidth > 800) {
      s.settings.notesWidth = 340;
    }
    s.budgets = s.budgets || {};
    s.notes = s.notes || {};
    s.notesOpen = !!s.notesOpen;
    // Personal to-do list — project-independent, lives at the state level.
    // Each item: { id, text, done }. Defaulted to [] for retrocompat.
    s.todos = Array.isArray(s.todos) ? s.todos : [];
    s.todos.forEach((t) => {
      t.id = t.id || uid('td');
      t.text = t.text || '';
      t.done = !!t.done;
    });
    // Auto-backup configuration (folder handle lives in IndexedDB).
    s.settings.autoBackup = s.settings.autoBackup || {};
    const ab = s.settings.autoBackup;
    if (typeof ab.enabled !== 'boolean') ab.enabled = false;
    const allowedMins = [5, 15, 30, 60, 360, 1440];
    if (!allowedMins.includes(ab.intervalMinutes)) ab.intervalMinutes = 60;
    if (typeof ab.dirName !== 'string') ab.dirName = '';
    if (typeof ab.lastBackupAt !== 'string') ab.lastBackupAt = null;
    // Mode: 'folder' (showDirectoryPicker + rotation), 'download'
    // (browser download fallback), or 'gdrive' (Google Drive REST API).
    // Default unset so renderer can pick the best available mode at runtime.
    if (ab.mode !== 'folder' && ab.mode !== 'download' && ab.mode !== 'gdrive') ab.mode = undefined;
    // Google Drive — user-supplied OAuth Client ID (paste-once).
    if (typeof ab.gdriveClientId !== 'string') ab.gdriveClientId = '';
    if (typeof ab.gdriveSignedIn !== 'boolean') ab.gdriveSignedIn = false;
    // Phase A — added flags / structures used by later phases. All retrocompat.
    if (typeof s.settings.tourSeen     !== 'boolean') s.settings.tourSeen = false;
    if (typeof s.settings.safetySeen   !== 'boolean') s.settings.safetySeen = false;
    // Per-topic walkthrough progress. Structure: { [topicId]: 'completed' }.
    // Any prior first-run tour completion migrates to the 'getting-started'
    // topic so returning users don't see it flagged as unfinished.
    if (typeof s.settings.walkthroughsSeen !== 'object' || !s.settings.walkthroughsSeen) {
      s.settings.walkthroughsSeen = {};
    }
    // Manual ordering for the Budgets page cost-centre cards. Newly-seen
    // CCs get appended alphabetically by getCostCentres.
    if (!Array.isArray(s.settings.costCentreOrder)) s.settings.costCentreOrder = [];
    if (s.settings.tourSeen && !s.settings.walkthroughsSeen['getting-started']) {
      s.settings.walkthroughsSeen['getting-started'] = 'completed';
    }
    if (typeof s.settings.notifyEnabled !== 'boolean') s.settings.notifyEnabled = false;
    if (typeof s.settings.sidebarGroups !== 'object' || !s.settings.sidebarGroups) {
      s.settings.sidebarGroups = { workspace: true, work: true, insight: true };
    }
    // Migration: legacy 'project' / 'engineering' group keys were renamed
    // to 'work' / 'insight' when the sidebar was re-grouped by intent.
    // Carry over the user's collapse state so a tidy sidebar stays tidy.
    if (s.settings.sidebarGroups.project !== undefined && s.settings.sidebarGroups.work === undefined) {
      s.settings.sidebarGroups.work = s.settings.sidebarGroups.project;
      delete s.settings.sidebarGroups.project;
    }
    if (s.settings.sidebarGroups.engineering !== undefined && s.settings.sidebarGroups.insight === undefined) {
      s.settings.sidebarGroups.insight = s.settings.sidebarGroups.engineering;
      delete s.settings.sidebarGroups.engineering;
    }
    // Ensure the three current keys exist so first-render doesn't fall
    // through to undefined (which the helper treats as 'expanded').
    ['workspace', 'work', 'insight'].forEach((k) => {
      if (s.settings.sidebarGroups[k] === undefined) s.settings.sidebarGroups[k] = true;
    });
    s.inbox = s.inbox || {};
    s.inbox.dismissed = Array.isArray(s.inbox.dismissed) ? s.inbox.dismissed : [];
    s.templates = Array.isArray(s.templates) ? s.templates : [];
    s.templates.forEach((t) => {
      t.id = t.id || uid('tpl');
      t.name = t.name || 'Template';
      t.createdAt = t.createdAt || todayISO();
      t.shape = t.shape || {};
    });
    // Saved views — named filter / search combinations the user can
    // apply with one click. Each: { id, name, filters: {search, owner,
    // component, status, due, view? } }. Defaults to []  for retrocompat.
    s.savedViews = Array.isArray(s.savedViews) ? s.savedViews : [];
    s.savedViews.forEach((v) => {
      v.id = v.id || uid('sv');
      v.name = v.name || 'View';
      v.filters = v.filters || {};
    });
    s.currentView = s.currentView || 'board';
    if (s.currentView === 'teams') s.currentView = 'people';
    // Panels merged into others over time — migrate stale state so saved
    // sessions and old exports land on the replacement panel.
    if (s.currentView === 'milestones' || s.currentView === 'deliverables') s.currentView = 'calendar';
    if (s.currentView === 'charts') s.currentView = 'dashboard';
    if (s.currentView === 'reports') s.currentView = 'review';
    if (!s.currentProjectId || (s.currentProjectId !== '__all__' && !s.projects.some((p) => p.id === s.currentProjectId))) {
      s.currentProjectId = s.projects[0]?.id || null;
    }

    // People — capacity is % of FTE; old data using small numbers is migrated.
    s.people.forEach((p) => {
      if (!p.id) p.id = uid('p');
      if (typeof p.capacity !== 'number') p.capacity = 100;
      if (p.capacity < 30) p.capacity = Math.round(p.capacity * 20); // legacy unit
      if (typeof p.hourlyRate !== 'number') p.hourlyRate = 100;
      p.role = p.role || '';
    });

    s.projects.forEach((p) => {
      p.id = p.id || uid('pr');
      p.name = p.name || 'Untitled project';
      p.description = p.description || '';
      // Archived projects are hidden from the picker by default but kept
      // in state so their data isn't lost. Boolean (defaults to false).
      p.archived = !!p.archived;
      // Per-collection arrays (presence is enough for renderers to short-circuit)
      p.actions      = Array.isArray(p.actions) ? p.actions : [];
      p.deliverables = Array.isArray(p.deliverables) ? p.deliverables : [];
      p.milestones   = Array.isArray(p.milestones) ? p.milestones : [];
      p.risks        = Array.isArray(p.risks) ? p.risks : [];
      p.decisions    = Array.isArray(p.decisions) ? p.decisions : [];
      p.changes      = Array.isArray(p.changes) ? p.changes : [];
      p.components   = Array.isArray(p.components) ? p.components : [];
      p.meetings     = Array.isArray(p.meetings) ? p.meetings : [];
      p.openPoints   = Array.isArray(p.openPoints) ? p.openPoints : [];
      p.links        = Array.isArray(p.links) ? p.links : [];
      p.costCenters  = Array.isArray(p.costCenters) ? p.costCenters : [];
      p.archive      = Array.isArray(p.archive) ? p.archive : [];
      // Phase A: project-scoped tags. { id, name, rgb } — used as labels on
      // actions / open points / CRs. Default empty for retrocompat.
      p.tags         = Array.isArray(p.tags) ? p.tags : [];
      p.tags.forEach((t) => {
        t.id = t.id || uid('tg');
        t.name = t.name || 'Tag';
        t.rgb = t.rgb || '148,163,184';
      });
      // Optional: track template lineage so future merges can detect drift
      if (p.templateOf === undefined) p.templateOf = null;

      p.actions.forEach((a) => {
        a.id = a.id || uid('a');
        a.title = a.title || '';
        a.status = a.status || 'todo';
        a.priority = (typeof a.priority === 'number') ? a.priority : 0;
        // priorityLevel — added later; older exports default to 'med' so the
        // existing rank-by-priority sort behaviour stays unchanged. Retrocompat.
        a.priorityLevel = (a.priorityLevel && PRIORITY_LEVELS.some((p) => p.id === a.priorityLevel)) ? a.priorityLevel : 'med';
        a.commitment = (typeof a.commitment === 'number') ? a.commitment : 100;
        // Manual % complete override. null = use the time-based / actuals
        // auto-calc in percentComplete(). A number 0..100 wins over both.
        if (typeof a.percentComplete !== 'number' && a.percentComplete !== null) a.percentComplete = null;
        // Completed date — the day the work actually landed. Nullable
        // even when status = Done (reader falls back to the history
        // "Status: → done" entry or updatedAt). User-overridable in
        // the drawer; auto-filled to today whenever status transitions
        // from anything else INTO 'done' and the field is still null.
        if (typeof a.completedAt !== 'string' || !a.completedAt) a.completedAt = null;
        // Archive semantics: `archivedAt` is a UI-visibility flag, not a
        // tombstone. Hides the action from lists but keeps it counted in
        // every KPI. Historic field name was `deletedAt` (which was a
        // misnomer — nothing was ever deleted). Migrate in place.
        if (a.deletedAt && !a.archivedAt) a.archivedAt = a.deletedAt;
        if ('deletedAt' in a) delete a.deletedAt;
        if (typeof a.archivedAt !== 'string' || !a.archivedAt) a.archivedAt = null;
        a.owner = a.owner || null;
        a.originator = a.originator || null;
        a.due = a.due || null;
        a.startDate = a.startDate || null;
        a.component = a.component || null;
        a.deliverable = a.deliverable || null;
        a.milestone = a.milestone || null;
        a.description = a.description || null;
        a.notes = a.notes || '';
        a.createdAt = a.createdAt || todayISO();
        a.updatedAt = a.updatedAt || a.createdAt || todayISO();
        // originatorDate — when the action was originated. Added in a later
        // schema version; for older exports without this field, default to the
        // existing createdAt so legacy data behaves as if it were already set.
        a.originatorDate = a.originatorDate || a.createdAt || todayISO();
        a.history = Array.isArray(a.history) ? a.history : [];
        // Phase A — dependencies, comments, tags, signed metadata. All defaulted.
        a.dependsOn = Array.isArray(a.dependsOn) ? a.dependsOn : [];
        a.tags      = Array.isArray(a.tags) ? a.tags : [];
        a.comments  = Array.isArray(a.comments) ? a.comments : [];
        // Subtasks — small checklist items with the same 5 statuses as the
        // parent action. Kept flat + simple by design: no owner, no due
        // date, no history. If a task grows real complexity, promote it
        // to its own action. Rendered as a dot strip on the Kanban card
        // and as a full editor in the drawer.
        a.tasks = Array.isArray(a.tasks) ? a.tasks : [];
        a.tasks.forEach((t) => {
          t.id = t.id || uid('tk');
          t.title = typeof t.title === 'string' ? t.title : '';
          const validStatuses = ['todo', 'doing', 'blocked', 'done', 'cancelled'];
          if (!validStatuses.includes(t.status)) t.status = 'todo';
        });
        // Actuals — list of time entries against this action. Each entry
        // is { id, hours, at (ISO date), by (personId|null), note? }.
        // Sum of hours feeds CPI and the variance display in the Register.
        a.loggedHours = Array.isArray(a.loggedHours) ? a.loggedHours : [];
        a.loggedHours.forEach((h) => {
          h.id = h.id || uid('lh');
          h.hours = (typeof h.hours === 'number' && h.hours > 0) ? h.hours : 0;
          h.at = h.at || todayISO();
          h.by = h.by || null;
          h.note = typeof h.note === 'string' ? h.note : '';
        });
        // Baseline — snapshot of the action's plan at a fixed point in
        // time (typically taken once at kickoff). Drives SPI, CPI, and
        // variance-to-baseline reporting. Null until the user freezes
        // a baseline; mutating the action after that does not change
        // the baseline. Shape: { due, startDate, commitment, takenAt }
        // OR null. Re-baselining replaces the whole object.
        if (a.baseline && typeof a.baseline === 'object') {
          a.baseline.due        = a.baseline.due        || null;
          a.baseline.startDate  = a.baseline.startDate  || null;
          a.baseline.commitment = (typeof a.baseline.commitment === 'number') ? a.baseline.commitment : 100;
          a.baseline.takenAt    = a.baseline.takenAt    || todayISO();
        } else {
          a.baseline = null;
        }
        a.comments.forEach((c) => {
          c.id  = c.id  || uid('cm');
          c.by  = c.by  || null;
          c.at  = c.at  || todayISO();
          c.text = c.text || '';
        });
        if (a.__lastEditor === undefined) a.__lastEditor = null;
        if (a.__lastEditAt === undefined) a.__lastEditAt = null;
        if (!Array.isArray(a.__history)) a.__history = [];
        // Recurrence — when set, marking the action 'done' spawns the
        // next instance with due date pushed forward by interval × unit.
        // { unit: 'day'|'week'|'month', interval: 1..N } or null.
        if (a.recurrence !== undefined && a.recurrence) {
          const u = a.recurrence.unit;
          a.recurrence.unit = (u === 'day' || u === 'week' || u === 'month') ? u : 'week';
          a.recurrence.interval = (typeof a.recurrence.interval === 'number' && a.recurrence.interval >= 1)
            ? Math.floor(a.recurrence.interval) : 1;
        } else if (a.recurrence === undefined) {
          a.recurrence = null;
        }
        // Snooze — ISO date; while today < snoozedUntil, the action is
        // hidden from default views.
        if (a.snoozedUntil === undefined) a.snoozedUntil = null;
        // a.archivedAt is null for live, ISO string for archived — preserve as-is
      });

      p.deliverables.forEach((d) => {
        d.id = d.id || uid('d');
        d.name = d.name || '';
        d.dueDate = d.dueDate || null;
        d.status = d.status || 'todo';
        // Optional component link, surfaced as a colour stripe on calendar
        // chips so the user can scan-by-component across kinds.
        if (d.component === undefined) d.component = null;
      });

      p.milestones.forEach((m) => {
        m.id = m.id || uid('m');
        m.name = m.name || '';
        m.date = m.date || null;
        // Optional range end-date. When null, the milestone is a single-day
        // event (legacy behaviour); when set, the milestone spans
        // [date … endDate] inclusive in the calendar.
        if (m.endDate === undefined) m.endDate = null;
        if (m.component === undefined) m.component = null;
        m.status = m.status || 'todo';
      });

      p.risks.forEach((r) => {
        r.id = r.id || uid('r');
        r.kind = r.kind || 'risk';
        r.title = r.title || '';
        r.owner = r.owner || null;
        r.mitigation = r.mitigation || '';
        r.actionId = r.actionId || null;
        // ensureRiskShape backfills inherent/residual from legacy probability/impact
        if (!r.inherent) r.inherent = { probability: r.probability || 3, impact: r.impact || 3 };
        if (!r.residual) r.residual = { ...r.inherent };
        // Identifier — short human-readable code (e.g. "OSB-R-1" /
        // "OSB-O-3") based on the project's initials + R/O + sequence.
        // Auto-assigned for legacy data on first load; users can
        // override to any unique string in the editor.
        if (typeof r.identifier !== 'string' || !r.identifier.trim()) {
          r.identifier = generateRiskIdentifier(p, r.kind || 'risk');
        }
      });

      p.decisions.forEach((d) => {
        d.id = d.id || uid('dec');
        d.title = d.title || '';
        d.rationale = d.rationale || '';
        d.owner = d.owner || null;
        d.date = d.date || todayISO();
        // Optional component link, mirrors the action / deliverable /
        // milestone / change-request schemas. null = no allocation.
        if (d.component === undefined) d.component = null;
        // Status — open (newly recorded), in-effect (active basis for
        // current work), or superseded (replaced by a later decision).
        // Defaults to in-effect for legacy decisions so old logs keep
        // their "this is what was decided" reading.
        const validStatuses = ['open', 'in_effect', 'superseded'];
        if (!validStatuses.includes(d.status)) d.status = 'in_effect';
        // Trigger reference — what caused this decision. Optional
        // free-form string OR a structured object {type: 'risk'|'cr'|'action',
        // id: '<entity-id>'}. Free-form covers external triggers
        // ("customer request", "board direction"); structured covers
        // links to in-tool entities.
        if (d.triggerRef === undefined) d.triggerRef = null;
      });

      p.changes.forEach((c) => {
        c.id = c.id || uid('cr');
        c.title = c.title || '';
        c.rationale = c.rationale || '';
        c.analysis = c.analysis || '';
        c.description = c.description || '';
        c.status = c.status || 'proposed';
        c.originator = c.originator || null;
        c.originatedDate = c.originatedDate || todayISO();
        c.decisionBy = c.decisionBy || null;
        c.decisionDate = c.decisionDate || null;
        c.impact = c.impact || {};
        c.impact.schedule = (typeof c.impact.schedule === 'number') ? c.impact.schedule : 0;
        c.impact.cost     = (typeof c.impact.cost     === 'number') ? c.impact.cost     : 0;
        c.impact.scope    = c.impact.scope || '';
        c.impact.risk     = c.impact.risk  || '';
        c.component = c.component || null;
        c.linkUrl = c.linkUrl || null;
        // priorityLevel — added later; default to 'med' for retrocompat
        c.priorityLevel = (c.priorityLevel && PRIORITY_LEVELS.some((p) => p.id === c.priorityLevel)) ? c.priorityLevel : 'med';
        delete c.linkTitle; // dropped from schema
        // Phase A — comments + tags + signed metadata
        c.tags = Array.isArray(c.tags) ? c.tags : [];
        c.comments = Array.isArray(c.comments) ? c.comments : [];
        c.comments.forEach((cm) => {
          cm.id   = cm.id   || uid('cm');
          cm.by   = cm.by   || null;
          cm.at   = cm.at   || todayISO();
          cm.text = cm.text || '';
        });
        if (c.__lastEditor === undefined) c.__lastEditor = null;
        if (c.__lastEditAt === undefined) c.__lastEditAt = null;
        if (!Array.isArray(c.__history)) c.__history = [];
      });

      p.components.forEach((cmp) => {
        cmp.id = cmp.id || uid('cm');
        cmp.name = cmp.name || '';
        cmp.color = cmp.color || (COMPONENT_COLORS[0] && COMPONENT_COLORS[0].id) || 'sky';
        cmp.costCenter = cmp.costCenter || null;
        // Free-form description — what this component covers / its scope.
        // Defaults to '' for retrocompat.
        if (typeof cmp.description !== 'string') cmp.description = '';
      });

      p.meetings.forEach((m) => {
        m.id = m.id || uid('mtg');
        m.kind = m.kind || 'oneoff';
        m.title = m.title || '';
        m.time = m.time || null;
        // Migration: legacy 'weekly' kind → unified 'recurring' with
        // recurUnit/interval. Old data continues to work because all
        // consumers go through expandMeetingDates() which only sees the
        // new shape after normalizeState has run.
        if (m.kind === 'weekly') {
          m.kind = 'recurring';
          m.recurUnit = 'week';
          m.interval = m.interval || 1;
        }
        if (m.kind === 'oneoff') {
          m.date = m.date || todayISO();
        } else if (m.kind === 'recurring') {
          m.recurUnit = (m.recurUnit === 'day' || m.recurUnit === 'week' || m.recurUnit === 'month') ? m.recurUnit : 'week';
          m.interval  = (typeof m.interval === 'number' && m.interval >= 1) ? Math.floor(m.interval) : 1;
          m.startDate = m.startDate || todayISO();
          if (m.recurUnit === 'week') {
            m.dayOfWeek = (typeof m.dayOfWeek === 'number') ? m.dayOfWeek : parseDate(m.startDate).getDay();
          }
        }
        if (m.endDate === undefined) m.endDate = null;
        if (m.component === undefined) m.component = null;
      });

      p.openPoints.forEach((op) => {
        op.id = op.id || uid('op');
        op.title = op.title || '';
        op.notes = op.notes || '';
        op.component = op.component || null;
        op.criticality = op.criticality || 'med';
        op.createdAt = op.createdAt || todayISO();
        // Identifier — short human-readable code (e.g. "OSB-OP-1") based
        // on the project's initials + sequence. Auto-assigned for legacy
        // data; users can override to any unique string from the list
        // card.
        if (typeof op.identifier !== 'string' || !op.identifier.trim()) {
          op.identifier = generateOpenPointIdentifier(p);
        }
        // priorityLevel — added later; default to 'med' for retrocompat
        op.priorityLevel = (op.priorityLevel && PRIORITY_LEVELS.some((p) => p.id === op.priorityLevel)) ? op.priorityLevel : 'med';
        // Resolution steps — added in a later schema version; default to []
        // for full retrocompat with older exports that never had this field.
        op.steps = Array.isArray(op.steps) ? op.steps : [];
        op.steps.forEach((s) => {
          s.id = s.id || uid('st');
          s.text = s.text || '';
          s.done = !!s.done;
        });
        // Phase A — comments + tags + signed metadata
        op.tags = Array.isArray(op.tags) ? op.tags : [];
        op.comments = Array.isArray(op.comments) ? op.comments : [];
        op.comments.forEach((cm) => {
          cm.id = cm.id || uid('cm');
          cm.by = cm.by || null;
          cm.at = cm.at || todayISO();
          cm.text = cm.text || '';
        });
        if (op.__lastEditor === undefined) op.__lastEditor = null;
        if (op.__lastEditAt === undefined) op.__lastEditAt = null;
        if (!Array.isArray(op.__history)) op.__history = [];
      });

      p.links.forEach((l) => {
        l.id = l.id || uid('lk');
        l.title = l.title || '';
        l.url = l.url || '';
        l.description = l.description || '';
        l.component = l.component || null;
        // folderId — added later for the folder-organized links view. Default
        // to null (= "Loose links" / no folder) for retrocompat.
        if (l.folderId === undefined) l.folderId = null;
      });
      // Link folders — added later. Each: { id, name, collapsed }
      p.linkFolders = Array.isArray(p.linkFolders) ? p.linkFolders : [];
      p.linkFolders.forEach((f) => {
        f.id = f.id || uid('lf');
        f.name = f.name || 'Folder';
        f.collapsed = !!f.collapsed;
      });
      // Drop any link.folderId that no longer points to an existing folder
      const folderIds = new Set(p.linkFolders.map((f) => f.id));
      p.links.forEach((l) => { if (l.folderId && !folderIds.has(l.folderId)) l.folderId = null; });
    });
    return s;
  }

  // Roll up every collection in state into a count breakdown. Stamped
  // into the export envelope (and shown after manual exports) so the
  // user can confirm at a glance that no data has gone missing.
  function summarizeState(s) {
    const summary = {
      // Top-level collections.
      projects: s.projects?.length || 0,
      archivedProjects: 0, // populated below from project.archived flag
      people:   s.people?.length || 0,
      todos:    s.todos?.length || 0,
      templates: s.templates?.length || 0,
      savedViews: s.savedViews?.length || 0,
      // Cross-project record totals — every per-project array surfaces here.
      actions: 0, deliverables: 0, milestones: 0, risks: 0, decisions: 0,
      changes: 0, components: 0, meetings: 0, openPoints: 0,
      links: 0, linkFolders: 0, costCenters: 0, tags: 0, archived: 0,
      // Action-level signals worth surfacing so the user can spot if
      // recurrence / snooze data went missing on round-trip.
      recurringActions: 0,
      snoozedActions:   0,
      actionsWithHistory: 0,
      projectNotes: 0,
    };
    const today = todayISO();
    (s.projects || []).forEach((p) => {
      if (p.archived) summary.archivedProjects += 1;
      summary.actions      += p.actions?.length      || 0;
      summary.deliverables += p.deliverables?.length || 0;
      summary.milestones   += p.milestones?.length   || 0;
      summary.risks        += p.risks?.length        || 0;
      summary.decisions    += p.decisions?.length    || 0;
      summary.changes      += p.changes?.length      || 0;
      summary.components   += p.components?.length   || 0;
      summary.meetings     += p.meetings?.length     || 0;
      summary.openPoints   += p.openPoints?.length   || 0;
      summary.links        += p.links?.length        || 0;
      summary.linkFolders  += p.linkFolders?.length  || 0;
      summary.costCenters  += p.costCenters?.length  || 0;
      summary.tags         += p.tags?.length         || 0;
      summary.archived     += p.archive?.length      || 0;
      (p.actions || []).forEach((a) => {
        if (a.recurrence) summary.recurringActions += 1;
        if (a.snoozedUntil && a.snoozedUntil > today) summary.snoozedActions += 1;
        if (Array.isArray(a.__history) && a.__history.length) summary.actionsWithHistory += 1;
      });
    });
    summary.projectNotes = Object.keys(s.notes || {}).length;
    return summary;
  }

  // Build the export payload — used by both manual Export and auto-backup
  // so the on-disk shape is always identical.
  function buildExportPayload() {
    return {
      __schemaVersion: EXPORT_SCHEMA_VERSION,
      __exportedAt: new Date().toISOString(),
      __app: 'cockpit',
      __summary: summarizeState(state),
      ...state,
    };
  }

  // Generic CSV export. `columns` is an array of { header, value }
  // where `value` is either a string field name on each row, or a
  // function (row) => any. Numbers serialise unquoted, everything
  // else as a CSV-escaped string. Uses CRLF line endings + UTF-8 BOM
  // so Excel/Numbers open it without prompting for encoding.
  /* ────────────────── Project export (.xlsx with tabs) ──────────────────
   * Single download that bundles Actions, Open points, Risks & Opps,
   * Decisions, Change requests, People, and Budgets — one sheet per
   * domain — into one Excel file. The xlsx is OOXML inside a PKZIP, and
   * we write both ourselves so the app stays dependency-free.
   *
   * Compression mode is "store" (no DEFLATE) — Excel still opens it, the
   * code is ~80 lines instead of bundling a deflate implementation, and
   * project-sized payloads (a few thousand rows) stay well under a few
   * MB. CRC32 is computed per entry as the spec requires.
   */

  // CRC32 with cached table — needed for every zip entry's checksum.
  function _crc32(buf) {
    let table = _crc32._t;
    if (!table) {
      table = new Uint32Array(256);
      for (let i = 0; i < 256; i++) {
        let c = i;
        for (let j = 0; j < 8; j++) c = (c & 1) ? (0xedb88320 ^ (c >>> 1)) : (c >>> 1);
        table[i] = c;
      }
      _crc32._t = table;
    }
    let c = 0xffffffff;
    for (let i = 0; i < buf.length; i++) c = table[(c ^ buf[i]) & 0xff] ^ (c >>> 8);
    return (c ^ 0xffffffff) >>> 0;
  }

  // Minimal store-only PKZIP writer. `entries` is [{name, data:Uint8Array}].
  function _zipStore(entries) {
    const enc = new TextEncoder();
    const parts = [];
    const central = [];
    let offset = 0;
    for (const ent of entries) {
      const nameB = enc.encode(ent.name);
      const data = ent.data;
      const crc = _crc32(data);
      const lfh = new Uint8Array(30 + nameB.length);
      const dv = new DataView(lfh.buffer);
      dv.setUint32(0, 0x04034b50, true);
      dv.setUint16(4, 20, true);      // version needed
      dv.setUint16(6, 0x0800, true);  // utf-8 flag
      dv.setUint16(8, 0, true);       // method: store
      dv.setUint16(10, 0, true);      // mod time
      dv.setUint16(12, 0, true);      // mod date
      dv.setUint32(14, crc, true);
      dv.setUint32(18, data.length, true);
      dv.setUint32(22, data.length, true);
      dv.setUint16(26, nameB.length, true);
      dv.setUint16(28, 0, true);
      lfh.set(nameB, 30);
      parts.push(lfh, data);

      const cdh = new Uint8Array(46 + nameB.length);
      const cv = new DataView(cdh.buffer);
      cv.setUint32(0, 0x02014b50, true);
      cv.setUint16(4, 20, true);
      cv.setUint16(6, 20, true);
      cv.setUint16(8, 0x0800, true);
      cv.setUint16(10, 0, true);
      cv.setUint16(12, 0, true);
      cv.setUint16(14, 0, true);
      cv.setUint32(16, crc, true);
      cv.setUint32(20, data.length, true);
      cv.setUint32(24, data.length, true);
      cv.setUint16(28, nameB.length, true);
      cv.setUint16(30, 0, true);
      cv.setUint16(32, 0, true);
      cv.setUint16(34, 0, true);
      cv.setUint16(36, 0, true);
      cv.setUint32(38, 0, true);
      cv.setUint32(42, offset, true);
      cdh.set(nameB, 46);
      central.push(cdh);

      offset += lfh.length + data.length;
    }
    const cdOffset = offset;
    const cdLen = central.reduce((s, p) => s + p.length, 0);
    const eocd = new Uint8Array(22);
    const ev = new DataView(eocd.buffer);
    ev.setUint32(0, 0x06054b50, true);
    ev.setUint16(4, 0, true);
    ev.setUint16(6, 0, true);
    ev.setUint16(8, entries.length, true);
    ev.setUint16(10, entries.length, true);
    ev.setUint32(12, cdLen, true);
    ev.setUint32(16, cdOffset, true);
    ev.setUint16(20, 0, true);
    const totalLen = offset + cdLen + 22;
    const out = new Uint8Array(totalLen);
    let pos = 0;
    for (const p of parts) { out.set(p, pos); pos += p.length; }
    for (const p of central) { out.set(p, pos); pos += p.length; }
    out.set(eocd, pos);
    return out;
  }

  // Tiny XML escape + column-letter helper for cell refs (A, B, …, Z, AA, AB…).
  function _xmlEsc(s) {
    return String(s).replace(/[<>&"']/g, (c) => ({'<':'&lt;','>':'&gt;','&':'&amp;','"':'&quot;',"'":'&apos;'}[c]));
  }
  function _colLetter(i) {
    let s = '';
    i++;
    while (i > 0) { i--; s = String.fromCharCode(65 + (i % 26)) + s; i = Math.floor(i / 26); }
    return s;
  }

  // Style index constants — see _buildStylesXml below for the
  // corresponding cellXfs entries. Used by sheet builders to apply
  // formatting (currency, percent, header bg, KPI band colours).
  const STYLE = {
    DEFAULT:    0,
    HEADER:     1,
    CURRENCY:   2,
    PERCENT:    3,
    MULT:       4,
    DATE:       5,
    SECTION:    6,  // section heading inside a sheet
    KPI_BIG:    7,  // large bold number for headline KPIs
    INT:        8,
    DEC2:       9,
    GOOD:       10,
    WARN:       11,
    BAD:        12,
    LABEL:      13, // small uppercase label
    TILE_BG:    14, // tinted background panel
    BIG_TITLE:  15,
  };

  // styles.xml — minimum cell-format stylesheet. Keep in sync with the
  // STYLE constants above; the cellXfs index of each entry IS the style
  // id that cells reference via the `s` attribute.
  function _buildStylesXml() {
    // numFmtId 0-163 are built-in. Custom formats start at 164.
    const numFmts = [
      { id: 164, code: '&quot;€&quot;#,##0' },           // currency, integer
      { id: 165, code: '0.0&quot;%&quot;' },              // percent (we store as 0..100)
      { id: 166, code: '0.00&quot;×&quot;' },             // multiplier
      { id: 167, code: 'yyyy-mm-dd' },                     // date
      { id: 168, code: '#,##0' },                          // integer with thousands
      { id: 169, code: '0.00' },                           // decimal 2 places
    ];
    const fonts = [
      '<font><sz val="11"/><name val="Calibri"/></font>',                                                       // 0: default
      '<font><b/><sz val="11"/><color rgb="FFFFFFFF"/><name val="Calibri"/></font>',                            // 1: header (white on dark bg)
      '<font><b/><sz val="13"/><color rgb="FF1F4E79"/><name val="Calibri"/></font>',                            // 2: section heading
      '<font><b/><sz val="22"/><color rgb="FF1F4E79"/><name val="Calibri"/></font>',                            // 3: KPI big number
      '<font><sz val="9"/><color rgb="FF7F7F7F"/><name val="Calibri"/></font>',                                 // 4: label / muted
      '<font><b/><sz val="18"/><name val="Calibri"/></font>',                                                   // 5: big title
      '<font><b/><sz val="11"/><color rgb="FF2E7D32"/><name val="Calibri"/></font>',                            // 6: good (green)
      '<font><b/><sz val="11"/><color rgb="FFF57F17"/><name val="Calibri"/></font>',                            // 7: warn (amber)
      '<font><b/><sz val="11"/><color rgb="FFC62828"/><name val="Calibri"/></font>',                            // 8: bad (red)
    ];
    const fills = [
      '<fill><patternFill patternType="none"/></fill>',                                                          // 0: none (required)
      '<fill><patternFill patternType="gray125"/></fill>',                                                       // 1: default gray (required)
      '<fill><patternFill patternType="solid"><fgColor rgb="FF1F4E79"/><bgColor indexed="64"/></patternFill></fill>', // 2: dark blue (header)
      '<fill><patternFill patternType="solid"><fgColor rgb="FFE7EEF7"/><bgColor indexed="64"/></patternFill></fill>', // 3: light blue (tile bg)
      '<fill><patternFill patternType="solid"><fgColor rgb="FFE8F5E9"/><bgColor indexed="64"/></patternFill></fill>', // 4: light green (good cell bg)
      '<fill><patternFill patternType="solid"><fgColor rgb="FFFFF8E1"/><bgColor indexed="64"/></patternFill></fill>', // 5: light amber
      '<fill><patternFill patternType="solid"><fgColor rgb="FFFFEBEE"/><bgColor indexed="64"/></patternFill></fill>', // 6: light red
    ];
    const borders = [
      '<border><left/><right/><top/><bottom/><diagonal/></border>',                                                                                                   // 0: no border
      '<border><left style="thin"><color rgb="FFCCCCCC"/></left><right style="thin"><color rgb="FFCCCCCC"/></right><top style="thin"><color rgb="FFCCCCCC"/></top><bottom style="thin"><color rgb="FFCCCCCC"/></bottom></border>', // 1: thin gray
    ];
    // cellXfs MUST match the STYLE.* indices order.
    const xfs = [
      // 0: default
      '<xf numFmtId="0" fontId="0" fillId="0" borderId="0" xfId="0"/>',
      // 1: header (bold white, dark blue bg, thin border, left aligned)
      '<xf numFmtId="0" fontId="1" fillId="2" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="left" vertical="center"/></xf>',
      // 2: currency (€)
      '<xf numFmtId="164" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 3: percent
      '<xf numFmtId="165" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 4: multiplier
      '<xf numFmtId="166" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 5: date
      '<xf numFmtId="167" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 6: section heading (large bold blue)
      '<xf numFmtId="0" fontId="2" fillId="0" borderId="0" xfId="0" applyFont="1" applyAlignment="1"><alignment horizontal="left" vertical="center"/></xf>',
      // 7: KPI_BIG — big bold number, tile bg, centered
      '<xf numFmtId="0" fontId="3" fillId="3" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="center" vertical="center"/></xf>',
      // 8: INT — integer with thousands
      '<xf numFmtId="168" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 9: DEC2 — 2-decimal
      '<xf numFmtId="169" fontId="0" fillId="0" borderId="0" xfId="0" applyNumberFormat="1"/>',
      // 10: GOOD — green bold, light-green bg
      '<xf numFmtId="0" fontId="6" fillId="4" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="center" vertical="center"/></xf>',
      // 11: WARN — amber bold, light-amber bg
      '<xf numFmtId="0" fontId="7" fillId="5" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="center" vertical="center"/></xf>',
      // 12: BAD — red bold, light-red bg
      '<xf numFmtId="0" fontId="8" fillId="6" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="center" vertical="center"/></xf>',
      // 13: LABEL — small muted uppercase
      '<xf numFmtId="0" fontId="4" fillId="3" borderId="1" xfId="0" applyFont="1" applyFill="1" applyBorder="1" applyAlignment="1"><alignment horizontal="center" vertical="center"/></xf>',
      // 14: TILE_BG — plain tile-coloured cell
      '<xf numFmtId="0" fontId="0" fillId="3" borderId="1" xfId="0" applyFill="1" applyBorder="1"/>',
      // 15: BIG_TITLE — sheet headline
      '<xf numFmtId="0" fontId="5" fillId="0" borderId="0" xfId="0" applyFont="1"/>',
    ];
    let xml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    xml += '<styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main">';
    xml += `<numFmts count="${numFmts.length}">`;
    numFmts.forEach((nf) => { xml += `<numFmt numFmtId="${nf.id}" formatCode="${nf.code}"/>`; });
    xml += '</numFmts>';
    xml += `<fonts count="${fonts.length}">${fonts.join('')}</fonts>`;
    xml += `<fills count="${fills.length}">${fills.join('')}</fills>`;
    xml += `<borders count="${borders.length}">${borders.join('')}</borders>`;
    xml += '<cellStyleXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0"/></cellStyleXfs>';
    xml += `<cellXfs count="${xfs.length}">${xfs.join('')}</cellXfs>`;
    xml += '<cellStyles count="1"><cellStyle name="Normal" xfId="0" builtinId="0"/></cellStyles>';
    xml += '</styleSheet>';
    return xml;
  }

  // Build the OOXML for one worksheet. Cell values may be:
  //   • a number → <c t="n"><v>…</v>
  //   • a string → <c t="inlineStr"><is><t>…</t></is></c>
  //   • an object { f, v?, t?, s? } → formula cell <c><f>…</f><v>…</v></c>
  //     (`v` is the cached value, `t="str"` marks string formulas,
  //     `s` is a style index from the STYLE map)
  //   • an object { v, s, t? } → plain value with explicit style
  // Sheet options:
  //   opts.colWidths   — array of widths (Excel char units) per column
  //   opts.colStyles   — { colIndex: STYLE.X } default style per column
  //   opts.frozenHeader — true to freeze row 1
  //   opts.headerStyle  — style id for row 1 (default STYLE.HEADER)
  //   opts.skipHeaderStyling — true to leave row 1 unstyled (for Dashboard)
  //   opts.drawingRid   — if set, adds <drawing r:id="rIdN"/> at end
  function _buildSheetXml(headers, rows, opts = {}) {
    const colStyles = opts.colStyles || {};
    const headerStyle = opts.headerStyle != null ? opts.headerStyle : STYLE.HEADER;
    let xml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    xml += '<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"';
    xml += ' xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships">';
    if (opts.frozenHeader) {
      xml += '<sheetViews><sheetView workbookViewId="0"><pane ySplit="1" topLeftCell="A2" activePane="bottomLeft" state="frozen"/></sheetView></sheetViews>';
    }
    if (opts.colWidths && opts.colWidths.length) {
      xml += '<cols>';
      opts.colWidths.forEach((w, i) => {
        if (w == null) return;
        xml += `<col min="${i + 1}" max="${i + 1}" width="${w}" customWidth="1"/>`;
      });
      xml += '</cols>';
    }
    xml += '<sheetData>';
    if (headers && headers.length) {
      xml += '<row r="1">';
      headers.forEach((h, i) => {
        const sAttr = opts.skipHeaderStyling ? '' : ` s="${headerStyle}"`;
        xml += `<c r="${_colLetter(i)}1"${sAttr} t="inlineStr"><is><t>${_xmlEsc(h)}</t></is></c>`;
      });
      xml += '</row>';
    }
    const headerOffset = (headers && headers.length) ? 2 : 1;
    rows.forEach((row, rIdx) => {
      const r = rIdx + headerOffset;
      xml += `<row r="${r}">`;
      row.forEach((v, cIdx) => {
        if (v == null || v === '') return;
        const ref = `${_colLetter(cIdx)}${r}`;
        // Cell objects (formula or value with explicit style) take precedence.
        if (typeof v === 'object' && (typeof v.f === 'string' || 'v' in v)) {
          const sId = (v.s != null) ? v.s : colStyles[cIdx];
          const sAttr = (sId != null) ? ` s="${sId}"` : '';
          const tAttr = v.t ? ` t="${v.t}"` : '';
          const cachedRaw = v.v;
          const cached = (cachedRaw == null || cachedRaw === '')
            ? ''
            : (typeof cachedRaw === 'number' && Number.isFinite(cachedRaw))
              ? `<v>${cachedRaw}</v>`
              : `<v>${_xmlEsc(String(cachedRaw))}</v>`;
          if (typeof v.f === 'string') {
            xml += `<c r="${ref}"${sAttr}${tAttr}><f>${_xmlEsc(v.f)}</f>${cached}</c>`;
          } else {
            // Plain styled value
            if (typeof cachedRaw === 'number' && Number.isFinite(cachedRaw)) {
              xml += `<c r="${ref}"${sAttr} t="n"><v>${cachedRaw}</v></c>`;
            } else {
              xml += `<c r="${ref}"${sAttr} t="inlineStr"><is><t>${_xmlEsc(String(cachedRaw))}</t></is></c>`;
            }
          }
        } else {
          const sId = colStyles[cIdx];
          const sAttr = (sId != null) ? ` s="${sId}"` : '';
          if (typeof v === 'number' && Number.isFinite(v)) {
            xml += `<c r="${ref}"${sAttr} t="n"><v>${v}</v></c>`;
          } else {
            xml += `<c r="${ref}"${sAttr} t="inlineStr"><is><t>${_xmlEsc(String(v))}</t></is></c>`;
          }
        }
      });
      xml += '</row>';
    });
    xml += '</sheetData>';
    if (opts.drawingRid) {
      xml += `<drawing r:id="rId${opts.drawingRid}"/>`;
    }
    xml += '</worksheet>';
    return xml;
  }

  // Assemble the full xlsx archive. Sheet names are sanitised to Excel's
  // 31-char limit and stripped of disallowed characters.
  function _buildXLSX(sheets) {
    const enc = new TextEncoder();
    const safeName = (n) => n.replace(/[\\\/?*[\]:]/g, '_').slice(0, 31);
    // Per-sheet parts. Each sheet may carry `charts` (array of chart
    // specs) — these become extra entries in the archive and the sheet
    // gets a <drawing r:id> pointing at the drawing file.
    const sheetItems = sheets.map((s, i) => ({
      name: safeName(s.name) || `Sheet${i + 1}`,
      idx: i + 1,
      raw: s,
    }));
    const chartItems = [];   // { sheetIdx, chartIdx, drawingIdx, chartXml, drawingXml, sheetRelsXml, drawingRelsXml }
    let chartCounter = 0;
    let drawingCounter = 0;
    sheetItems.forEach((s) => {
      const charts = s.raw.charts || [];
      if (!charts.length) {
        s.xml = _buildSheetXml(s.raw.headers, s.raw.rows, s.raw);
        return;
      }
      drawingCounter += 1;
      const drawingIdx = drawingCounter;
      const chartIds = [];
      charts.forEach((cspec) => {
        chartCounter += 1;
        chartIds.push(chartCounter);
        chartItems.push({
          chartIdx: chartCounter,
          drawingIdx,
          chartXml: _buildChartXml(cspec),
        });
      });
      // Drawing references chart files via local rIds.
      const drawingRels = [];
      const drawingAnchors = [];
      charts.forEach((cspec, k) => {
        const rid = k + 1;
        drawingRels.push(`<Relationship Id="rId${rid}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/chart" Target="../charts/chart${chartIds[k]}.xml"/>`);
        drawingAnchors.push(_buildDrawingAnchor(cspec, rid, k + 1));
      });
      const drawingXml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>'
        + '<xdr:wsDr xmlns:xdr="http://schemas.openxmlformats.org/drawingml/2006/spreadsheetDrawing" xmlns:a="http://schemas.openxmlformats.org/drawingml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships">'
        + drawingAnchors.join('')
        + '</xdr:wsDr>';
      const drawingRelsXml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>'
        + '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">'
        + drawingRels.join('')
        + '</Relationships>';
      // Sheet references the drawing via rId1.
      const sheetRelsXml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>'
        + '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">'
        + `<Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/drawing" Target="../drawings/drawing${drawingIdx}.xml"/>`
        + '</Relationships>';
      s.xml = _buildSheetXml(s.raw.headers, s.raw.rows, { ...s.raw, drawingRid: 1 });
      s.sheetRelsXml = sheetRelsXml;
      s.drawingIdx = drawingIdx;
      s.drawingXml = drawingXml;
      s.drawingRelsXml = drawingRelsXml;
    });

    // workbook.xml + workbook rels — same as before but reserves
    // additional rId for the styles part.
    let wb = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    wb += '<workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships">';
    wb += '<sheets>';
    sheetItems.forEach((s, i) => {
      wb += `<sheet name="${_xmlEsc(s.name)}" sheetId="${i + 1}" r:id="rId${i + 1}"/>`;
    });
    wb += '</sheets></workbook>';
    let wbRels = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    wbRels += '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">';
    sheetItems.forEach((s, i) => {
      wbRels += `<Relationship Id="rId${i + 1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${i + 1}.xml"/>`;
    });
    // Styles part — last rel id after all sheets.
    const stylesRid = sheetItems.length + 1;
    wbRels += `<Relationship Id="rId${stylesRid}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/>`;
    wbRels += '</Relationships>';
    const rootRels = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>'
      + '<Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">'
      + '<Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/>'
      + '</Relationships>';
    let ct = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    ct += '<Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types">';
    ct += '<Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/>';
    ct += '<Default Extension="xml" ContentType="application/xml"/>';
    ct += '<Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>';
    ct += '<Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/>';
    sheetItems.forEach((s, i) => {
      ct += `<Override PartName="/xl/worksheets/sheet${i + 1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>`;
    });
    sheetItems.forEach((s) => {
      if (s.drawingXml) {
        ct += `<Override PartName="/xl/drawings/drawing${s.drawingIdx}.xml" ContentType="application/vnd.openxmlformats-officedocument.drawing+xml"/>`;
      }
    });
    chartItems.forEach((c) => {
      ct += `<Override PartName="/xl/charts/chart${c.chartIdx}.xml" ContentType="application/vnd.openxmlformats-officedocument.drawingml.chart+xml"/>`;
    });
    ct += '</Types>';

    const entries = [
      { name: '[Content_Types].xml', data: enc.encode(ct) },
      { name: '_rels/.rels', data: enc.encode(rootRels) },
      { name: 'xl/workbook.xml', data: enc.encode(wb) },
      { name: 'xl/_rels/workbook.xml.rels', data: enc.encode(wbRels) },
      { name: 'xl/styles.xml', data: enc.encode(_buildStylesXml()) },
      ...sheetItems.map((s) => ({ name: `xl/worksheets/sheet${s.idx}.xml`, data: enc.encode(s.xml) })),
    ];
    // Drawings + sheet rels for charts
    sheetItems.forEach((s) => {
      if (s.drawingXml) {
        entries.push({ name: `xl/worksheets/_rels/sheet${s.idx}.xml.rels`, data: enc.encode(s.sheetRelsXml) });
        entries.push({ name: `xl/drawings/drawing${s.drawingIdx}.xml`, data: enc.encode(s.drawingXml) });
        entries.push({ name: `xl/drawings/_rels/drawing${s.drawingIdx}.xml.rels`, data: enc.encode(s.drawingRelsXml) });
      }
    });
    chartItems.forEach((c) => {
      entries.push({ name: `xl/charts/chart${c.chartIdx}.xml`, data: enc.encode(c.chartXml) });
    });
    return _zipStore(entries);
  }

  // ─── Chart pipeline ────────────────────────────────────────────────
  // _buildChartXml takes a chart spec and produces the chartN.xml body.
  // Spec shape:
  //   { title, type: 'column' | 'bar' | 'pie',
  //     catRef: 'Dashboard!$A$4:$E$4',
  //     series: [{ name, valRef, colorRgb? }],
  //     colors: ['FF1F4E79', ...]   // optional per-point palette (pie)
  //   }
  function _buildChartXml(spec) {
    const type = spec.type || 'column';
    const series = spec.series || [];
    const seriesXml = series.map((s, i) => {
      const color = s.colorRgb || (spec.colors && spec.colors[i]) || ['1F4E79','388E3C','C62828','F57F17','6A1B9A'][i % 5];
      const titleXml = s.name
        ? `<c:tx><c:strRef><c:f>${_xmlEsc(s.nameRef || '')}</c:f><c:strCache><c:ptCount val="1"/><c:pt idx="0"><c:v>${_xmlEsc(s.name)}</c:v></c:pt></c:strCache></c:strRef></c:tx>`
        : '';
      // For pie charts, per-point colours via dPt entries make the slices distinguishable.
      let dPtXml = '';
      if (type === 'pie' && spec.colors) {
        spec.colors.forEach((cc, ptIdx) => {
          dPtXml += `<c:dPt><c:idx val="${ptIdx}"/><c:bubble3D val="0"/><c:spPr><a:solidFill><a:srgbClr val="${cc}"/></a:solidFill></c:spPr></c:dPt>`;
        });
      }
      const spPr = (type === 'pie')
        ? ''
        : `<c:spPr><a:solidFill><a:srgbClr val="${color}"/></a:solidFill><a:ln><a:noFill/></a:ln></c:spPr>`;
      return ''
        + `<c:ser><c:idx val="${i}"/><c:order val="${i}"/>${titleXml}${spPr}${dPtXml}`
        + `<c:cat><c:strRef><c:f>${_xmlEsc(spec.catRef)}</c:f></c:strRef></c:cat>`
        + `<c:val><c:numRef><c:f>${_xmlEsc(s.valRef)}</c:f></c:numRef></c:val>`
        + '</c:ser>';
    }).join('');
    const titleBlock = spec.title
      ? `<c:title><c:tx><c:rich><a:bodyPr rot="0" spcFirstLastPara="1" vertOverflow="ellipsis" wrap="square" anchor="ctr" anchorCtr="1"/><a:lstStyle/><a:p><a:pPr><a:defRPr sz="1400" b="1"><a:solidFill><a:srgbClr val="1F4E79"/></a:solidFill></a:defRPr></a:pPr><a:r><a:rPr lang="en-US" sz="1400" b="1"><a:solidFill><a:srgbClr val="1F4E79"/></a:solidFill></a:rPr><a:t>${_xmlEsc(spec.title)}</a:t></a:r></a:p></c:rich></c:tx><c:overlay val="0"/></c:title>`
      : '';
    let plotInner = '';
    if (type === 'pie') {
      plotInner = `<c:pieChart><c:varyColors val="1"/>${seriesXml}<c:firstSliceAng val="0"/></c:pieChart>`;
    } else {
      const barDir = (type === 'bar') ? 'bar' : 'col';
      plotInner = `<c:barChart><c:barDir val="${barDir}"/><c:grouping val="clustered"/><c:varyColors val="0"/>${seriesXml}<c:axId val="111111"/><c:axId val="222222"/></c:barChart>`
        + '<c:catAx><c:axId val="111111"/><c:scaling><c:orientation val="minMax"/></c:scaling><c:delete val="0"/>'
        + `<c:axPos val="${barDir === 'bar' ? 'l' : 'b'}"/><c:crossAx val="222222"/></c:catAx>`
        + '<c:valAx><c:axId val="222222"/><c:scaling><c:orientation val="minMax"/></c:scaling><c:delete val="0"/>'
        + `<c:axPos val="${barDir === 'bar' ? 'b' : 'l'}"/><c:crossAx val="111111"/></c:valAx>`;
    }
    let xml = '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>';
    xml += '<c:chartSpace xmlns:c="http://schemas.openxmlformats.org/drawingml/2006/chart" xmlns:a="http://schemas.openxmlformats.org/drawingml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships">';
    xml += '<c:chart>';
    xml += titleBlock;
    xml += '<c:autoTitleDeleted val="0"/>';
    xml += '<c:plotArea><c:layout/>';
    xml += plotInner;
    xml += '</c:plotArea>';
    xml += type === 'pie'
      ? '<c:plotVisOnly val="1"/><c:dispBlanksAs val="gap"/>'
      : '<c:plotVisOnly val="1"/><c:dispBlanksAs val="gap"/>';
    xml += '</c:chart></c:chartSpace>';
    return xml;
  }

  // Drawing anchor — positions the chart at a cell-range in the sheet.
  // Coordinates are zero-indexed cell positions; colOff/rowOff are EMUs
  // (English Metric Units) but we leave them at 0 for cell-snap.
  function _buildDrawingAnchor(spec, rid, idCounter) {
    const f = spec.from || { col: 0, row: 0 };
    const t = spec.to   || { col: f.col + 8, row: f.row + 16 };
    return '<xdr:twoCellAnchor>'
      + `<xdr:from><xdr:col>${f.col}</xdr:col><xdr:colOff>0</xdr:colOff><xdr:row>${f.row}</xdr:row><xdr:rowOff>0</xdr:rowOff></xdr:from>`
      + `<xdr:to><xdr:col>${t.col}</xdr:col><xdr:colOff>0</xdr:colOff><xdr:row>${t.row}</xdr:row><xdr:rowOff>0</xdr:rowOff></xdr:to>`
      + '<xdr:graphicFrame macro="">'
      + `<xdr:nvGraphicFramePr><xdr:cNvPr id="${idCounter}" name="Chart ${idCounter}"/><xdr:cNvGraphicFramePr/></xdr:nvGraphicFramePr>`
      + '<xdr:xfrm><a:off x="0" y="0"/><a:ext cx="0" cy="0"/></xdr:xfrm>'
      + '<a:graphic><a:graphicData uri="http://schemas.openxmlformats.org/drawingml/2006/chart">'
      + `<c:chart xmlns:c="http://schemas.openxmlformats.org/drawingml/2006/chart" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships" r:id="rId${rid}"/>`
      + '</a:graphicData></a:graphic></xdr:graphicFrame>'
      + '<xdr:clientData/>'
      + '</xdr:twoCellAnchor>';
  }

  // ── Sheet builders — each produces { name, headers, rows } from current
  // project scope. `projects` is the list to walk (one project, or every
  // non-archived project for the All-projects view).
  function _buildProjectExportSheets(projects, multiProject) {
    const projLabel = (p) => p?.name || '';
    const sheets = [];

    // Cache milestone / deliverable lookups per project — used by both the
    // Actions sheet (name columns) and the dedicated sheets below.
    const milestoneIndex = new Map();
    const deliverableIndex = new Map();
    projects.forEach((p) => {
      const mMap = {}; (p.milestones || []).forEach((m) => { mMap[m.id] = m; });
      const dMap = {}; (p.deliverables || []).forEach((d) => { dMap[d.id] = d; });
      milestoneIndex.set(p, mMap);
      deliverableIndex.set(p, dMap);
    });

    // Per-person hourly-rate lookup — joined into every action row so
    // the EVM formulas (BAC / EV / AC) work without VLOOKUP overhead.
    const personRateById = new Map();
    state.people.forEach((p) => personRateById.set(p.id, Number(p.hourlyRate) || 0));

    // 1) Actions — register-style columns + Hourly rate, Completed date,
    // and live formula cells EV hours / PV hours so KPIs can SUM them
    // and the workbook recomputes when data is edited in Excel.
    // Column letters that the KPI sheet's formulas need to reference.
    // Keep this in sync with the headers array below.
    const A = {};
    [
      ...(multiProject ? ['Project'] : []),
      'ID', 'Title', 'Component', 'Milestone', 'Deliverable',
      'Owner', 'Hourly rate', 'Status', 'Priority', 'Commitment %',
      'Start', 'Due', 'Completed date', 'Promised due', 'Predicted',
      'Originator', 'Originator date',
      'Baseline due', 'Variance days', 'Logged hours', 'Planned hours',
      '% Complete', 'EV hours', 'PV hours',
      'Tags', 'Depends on', 'Created', 'Updated',
    ].forEach((name, i) => { A[name] = _colLetter(i); });

    const actionRows = [];
    projects.forEach((p) => {
      const mMap = milestoneIndex.get(p);
      const dMap = deliverableIndex.get(p);
      (p.actions || []).filter((a) => !a.archivedAt).forEach((a) => {
        const planned = Number(plannedHoursTotal(a).toFixed(2));
        const pct = percentComplete(a);
        const evCached = Number(((planned * pct) / 100).toFixed(2));
        // PV per row: planned × clamp01(elapsed/window). Pre-compute for
        // the cached value so Excel shows the right number even before
        // a recalc; the formula recomputes on edit / file open.
        let pvCached = 0;
        if (a.startDate && a.due) {
          const s = parseDate(a.startDate).getTime();
          const e = parseDate(a.due).getTime();
          const t = new Date().setHours(0,0,0,0);
          const frac = Math.max(0, Math.min(1, (t - s) / Math.max(1, e - s)));
          pvCached = Number((planned * frac).toFixed(2));
        }
        const rowNum = actionRows.length + 2; // header is row 1
        // Formula refs use the column letters from the A map.
        const plannedRef = `${A['Planned hours']}${rowNum}`;
        const pctRef     = `${A['% Complete']}${rowNum}`;
        const startRef   = `${A['Start']}${rowNum}`;
        const dueRef     = `${A['Due']}${rowNum}`;
        const baselineRef = `${A['Baseline due']}${rowNum}`;
        const evFormula  = `IFERROR(${plannedRef}*${pctRef}/100, 0)`;
        const pvFormula  = `IFERROR(IF(OR(${startRef}="",${dueRef}=""),0,${plannedRef}*MAX(0,MIN(1,(TODAY()-DATEVALUE(${startRef}))/MAX(1,DATEVALUE(${dueRef})-DATEVALUE(${startRef}))))), 0)`;
        // Promised due: baseline if frozen, else current due. Single-cell
        // IF — works everywhere; KPI / Dashboard formulas just reference
        // this column instead of fighting Excel's quirky IF-inside-
        // SUMPRODUCT behaviour.
        const promisedFormula = `IF(${baselineRef}<>"",${baselineRef},${dueRef})`;
        const promisedCached = a.baseline?.due || a.due || '';
        actionRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          a.id,
          a.title,
          findComponent(p, a.component)?.name || '',
          mMap[a.milestone]?.name || '',
          dMap[a.deliverable]?.name || '',
          personName(a.owner),
          personRateById.get(a.owner) || 0,
          (STATUSES.find((s) => s.id === a.status) || {}).name || a.status,
          priorityLevel(a.priorityLevel).label,
          (typeof a.commitment === 'number') ? a.commitment : '',
          a.startDate || '',
          a.due || '',
          actionCompletedAt(a) || '',
          { f: promisedFormula, v: promisedCached, t: 'str' },
          predictedCompletion(a) || '',
          personName(a.originator),
          a.originatorDate || a.createdAt || '',
          a.baseline?.due || '',
          baselineSlipDays(a) ?? '',
          Number(totalLoggedHours(a).toFixed(2)),
          planned,
          pct,
          { f: evFormula, v: evCached },
          { f: pvFormula, v: pvCached },
          (a.tags || []).map((tid) => (p.tags || []).find((t) => t.id === tid)?.name || '').filter(Boolean).join('; '),
          (a.dependsOn || []).join('; '),
          a.createdAt || '',
          a.updatedAt || '',
        ]);
      });
    });
    // Column index offset when an extra Project column is prepended.
    const ofs = multiProject ? 1 : 0;
    // Currency / decimal cell styling for the Actions sheet. Column
    // indices below match the headers array — Promised due bumped every
    // subsequent column +1 from the previous schema.
    const actionsColStyles = {};
    actionsColStyles[6 + ofs]  = STYLE.CURRENCY;  // Hourly rate
    actionsColStyles[19 + ofs] = STYLE.DEC2;      // Logged hours
    actionsColStyles[20 + ofs] = STYLE.DEC2;      // Planned hours
    actionsColStyles[22 + ofs] = STYLE.DEC2;      // EV hours
    actionsColStyles[23 + ofs] = STYLE.DEC2;      // PV hours
    sheets.push({
      name: 'Actions',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'ID', 'Title', 'Component', 'Milestone', 'Deliverable',
        'Owner', 'Hourly rate', 'Status', 'Priority', 'Commitment %',
        'Start', 'Due', 'Completed date', 'Promised due', 'Predicted',
        'Originator', 'Originator date',
        'Baseline due', 'Variance days', 'Logged hours', 'Planned hours',
        '% Complete', 'EV hours', 'PV hours',
        'Tags', 'Depends on', 'Created', 'Updated',
      ],
      rows: actionRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        14, 42, 20, 24, 24,    // ID, Title, Component, Milestone, Deliverable
        18, 12, 14, 12, 14,    // Owner, Hourly rate, Status, Priority, Commitment %
        12, 12, 14, 13, 14,    // Start, Due, Completed date, Promised due, Predicted
        18, 14,                // Originator, Originator date
        13, 14, 13, 13,        // Baseline due, Variance days, Logged hours, Planned hours
        12, 12, 12,            // % Complete, EV hours, PV hours
        30, 30, 12, 12,        // Tags, Depends on, Created, Updated
      ],
      colStyles: actionsColStyles,
    });

    // 1b) Milestones — one row per milestone, with linked-action rollups so
    // a PM can see "this gate has 7 actions, 4 open, 3 done" at a glance.
    const milestoneRows = [];
    projects.forEach((p) => {
      (p.milestones || []).forEach((m) => {
        const linked = (p.actions || []).filter((a) => !a.archivedAt && a.milestone === m.id);
        const open = linked.filter((a) => !isClosedStatus(a.status)).length;
        const done = linked.filter((a) => a.status === 'done').length;
        const dueStr = m.date || '';
        const today = todayISO();
        const daysToGo = dueStr ? dayDiff(dueStr, today) : '';
        milestoneRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          m.id,
          m.name || '',
          dueStr,
          (STATUSES.find((s) => s.id === m.status) || {}).name || m.status || '',
          daysToGo === '' ? '' : Number(daysToGo),
          linked.length,
          open,
          done,
        ]);
      });
    });
    sheets.push({
      name: 'Milestones',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'ID', 'Name', 'Date', 'Status', 'Days to go',
        'Linked actions', 'Open', 'Done',
      ],
      rows: milestoneRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        16, 38, 12, 16, 11, 14, 8, 8,
      ],
    });

    // 1c) Deliverables — same shape as milestones, keyed by dueDate.
    const deliverableRows = [];
    projects.forEach((p) => {
      (p.deliverables || []).forEach((d) => {
        const linked = (p.actions || []).filter((a) => !a.archivedAt && a.deliverable === d.id);
        const open = linked.filter((a) => !isClosedStatus(a.status)).length;
        const done = linked.filter((a) => a.status === 'done').length;
        const dueStr = d.dueDate || '';
        const today = todayISO();
        const daysToGo = dueStr ? dayDiff(dueStr, today) : '';
        deliverableRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          d.id,
          d.name || '',
          dueStr,
          (STATUSES.find((s) => s.id === d.status) || {}).name || d.status || '',
          daysToGo === '' ? '' : Number(daysToGo),
          linked.length,
          open,
          done,
        ]);
      });
    });
    sheets.push({
      name: 'Deliverables',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'ID', 'Name', 'Due date', 'Status', 'Days to go',
        'Linked actions', 'Open', 'Done',
      ],
      rows: deliverableRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        16, 38, 12, 16, 11, 14, 8, 8,
      ],
    });

    // 2) Open points
    const opRows = [];
    projects.forEach((p) => {
      (p.openPoints || []).forEach((op) => {
        opRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          op.identifier || '',
          op.title || '',
          findComponent(p, op.component)?.name || '',
          CRITICALITY_LABEL[op.criticality] || op.criticality || '',
          priorityLevel(op.priorityLevel).label,
          op.createdAt || '',
          (op.steps || []).filter((s) => s.done).length,
          (op.steps || []).length,
          (op.notes || '').replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').trim(),
        ]);
      });
    });
    sheets.push({
      name: 'Open points',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'Identifier', 'Title', 'Component', 'Criticality', 'Priority',
        'Created', 'Steps done', 'Steps total', 'Notes (plain)',
      ],
      rows: opRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        16, 40, 18, 14, 14, 12, 12, 12, 60,
      ],
    });

    // 3) Risks & opportunities
    const riskRows = [];
    projects.forEach((p) => {
      (p.risks || []).forEach((r) => {
        const inh = getInherent(r), res = getResidual(r);
        riskRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          r.identifier || '',
          r.kind || 'risk',
          r.title || '',
          personName(r.owner),
          inh.probability, inh.impact, inh.probability * inh.impact,
          res.probability, res.impact, res.probability * res.impact,
          r.mitigation || '',
          r.actionId || '',
        ]);
      });
    });
    sheets.push({
      name: 'Risks & Opps',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'Identifier', 'Kind', 'Title', 'Owner',
        'Inherent probability', 'Inherent impact', 'Inherent score',
        'Residual probability', 'Residual impact', 'Residual score',
        'Mitigation / Capture', 'Linked action',
      ],
      rows: riskRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        14, 12, 40, 18, 11, 11, 11, 11, 11, 11, 50, 14,
      ],
    });

    // 4) Decisions
    const decRows = [];
    projects.forEach((p) => {
      (p.decisions || []).forEach((d) => {
        decRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          d.id,
          d.title || '',
          d.rationale || '',
          personName(d.owner),
          d.date || '',
          findComponent(p, d.component)?.name || '',
          d.status || '',
          d.triggerRef || '',
        ]);
      });
    });
    sheets.push({
      name: 'Decisions',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'ID', 'Title', 'Rationale', 'Owner', 'Date', 'Component', 'Status', 'Trigger',
      ],
      rows: decRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        14, 38, 50, 18, 12, 18, 14, 20,
      ],
    });

    // 5) Change requests
    const crRows = [];
    projects.forEach((p) => {
      (p.changes || []).forEach((c) => {
        crRows.push([
          ...(multiProject ? [projLabel(p)] : []),
          c.id,
          c.title || '',
          (CR_STATUSES.find((s) => s.id === c.status) || {}).label || c.status || '',
          priorityLevel(c.priorityLevel).label,
          personName(c.originator),
          c.originatedDate || '',
          personName(c.decisionBy),
          c.decisionDate || '',
          findComponent(p, c.component)?.name || '',
          c.impact?.schedule ?? '',
          c.impact?.cost ?? '',
          c.impact?.scope || '',
          c.impact?.risk || '',
          c.rationale || '',
          c.description || '',
          c.analysis || '',
          c.linkUrl || '',
        ]);
      });
    });
    // CR has a cost column we can format as currency.
    const crColStyles = {};
    crColStyles[11 + (multiProject ? 1 : 0)] = STYLE.CURRENCY;
    sheets.push({
      name: 'Change requests',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'ID', 'Title', 'Status', 'Priority', 'Originator', 'Originated',
        'Decision by', 'Decision date', 'Component',
        'Impact: schedule (d)', 'Impact: cost (€)', 'Impact: scope', 'Impact: risk',
        'Rationale', 'Description', 'Analysis', 'Link',
      ],
      rows: crRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        14, 38, 14, 12, 18, 12, 18, 13, 18, 14, 14, 20, 20, 40, 50, 50, 30,
      ],
      colStyles: crColStyles,
    });

    // 6) People — always portfolio-wide; no Project column.
    const peopleRows = state.people.map((p) => [
      p.id,
      p.name || '',
      p.role || '',
      p.capacity || 100,
      p.hourlyRate || '',
    ]);
    sheets.push({
      name: 'People',
      headers: ['ID', 'Name', 'Role', 'Capacity %', 'Hourly rate'],
      rows: peopleRows,
      frozenHeader: true,
      colWidths: [14, 24, 22, 14, 14],
      colStyles: { 4: STYLE.CURRENCY },
    });

    // 7) Budgets — one row per (cost-centre, week-start) with the weekly
    // budget figure. Cost centres are project-level but the budget map is
    // portfolio-level, so we list the projects each CC appears under.
    const budgetRows = [];
    if (state.budgets) {
      const ccProjects = (cc) => state.projects
        .filter((pp) => (pp.costCenters || []).includes(cc) || (pp.components || []).some((c) => c.costCenter === cc))
        .map((pp) => pp.name)
        .join('; ');
      Object.keys(state.budgets).sort().forEach((cc) => {
        const byWeek = state.budgets[cc] || {};
        Object.keys(byWeek).sort().forEach((iso) => {
          const v = Number(byWeek[iso]);
          if (!Number.isFinite(v) || v === 0) return;
          budgetRows.push([cc, ccProjects(cc), iso, v]);
        });
      });
    }
    sheets.push({
      name: 'Budgets',
      headers: ['Cost centre', 'Projects', 'Week start (ISO)', 'Budget hours'],
      rows: budgetRows,
      frozenHeader: true,
      colWidths: [20, 36, 16, 14],
      colStyles: { 3: STYLE.DEC2 },
    });

    // 8) KPIs — formula-driven so the workbook recomputes as the user
    // edits the underlying data. Each Value cell is an Excel formula
    // referencing Actions / Milestones / etc.; the cached value Excel
    // displays before recalculation matches what the app's runtime
    // shows today.
    //
    // Multi-project mode prepends a Project column to most sheets, which
    // shifts every other column by one — we keep that mapping inline so
    // formulas pick the right letter regardless of mode.
    //
    // Column letters used in formulas (see Actions sheet builder above
    // for the source of truth).
    const aDue        = A['Due'];
    const aStatus     = A['Status'];
    const aCompleted  = A['Completed date'];
    const aPromised   = A['Promised due'];
    const aPlanned    = A['Planned hours'];
    const aLogged     = A['Logged hours'];
    const aRate       = A['Hourly rate'];
    const aBaseline   = A['Baseline due'];
    const aEV         = A['EV hours'];
    const aPV         = A['PV hours'];
    const aId         = A['ID'];
    const aProj       = A['Project']; // undefined in single-project mode

    // Helper: build a SUMPRODUCT condition that filters by project when in
    // multi-project mode, so each project's KPI rows only count their own
    // actions. We use an explicit upper bound (10001 rows) so SUMPRODUCT
    // can resolve to a stable array — whole-column refs aren't legal in
    // SUMPRODUCT arrays.
    const N_MAX = 10001;
    const _r = (col) => `Actions!${col}2:${col}${N_MAX}`;
    const projFilter = (projName) => multiProject
      ? `(${_r(aProj)}="${String(projName).replace(/"/g, '""')}")*`
      : '';

    // Pre-load todayISO + dayMs to compute cached values that match
    // the formulas' first-recalc result.
    const todayStr = todayISO();
    const todayT   = (() => { const d = new Date(); d.setHours(0,0,0,0); return d.getTime(); })();
    const dayISO = (n) => fmtISO(new Date(todayT - n * dayMs));

    const kpiRows = [];
    // Map a KPI's unit to the corresponding cell style id so the value
    // cell renders with the right number format (€, %, ×, integer).
    const styleForUnit = (unit) => {
      if (unit === '€')     return STYLE.CURRENCY;
      if (unit === '%')     return STYLE.PERCENT;
      if (unit === '×')     return STYLE.MULT;
      if (unit === 'count') return STYLE.INT;
      return STYLE.DEFAULT;
    };
    const pushF = (proj, metric, formula, cachedV, unit, notes, opts = {}) => kpiRows.push([
      ...(multiProject ? [projLabel(proj)] : []),
      metric,
      { f: formula, v: cachedV, t: opts.t, s: styleForUnit(unit) },
      unit || '',
      notes || '',
    ]);
    const pushS = (proj, metric, value, unit, notes) => kpiRows.push([
      ...(multiProject ? [projLabel(proj)] : []),
      metric,
      { v: value, s: styleForUnit(unit) },
      unit || '', notes || '',
    ]);

    projects.forEach((p) => {
      // Cached values mirror the runtime computation so Excel shows the
      // right number even before a recalc. Reuses the existing JS logic.
      // KPI rollup — archived actions still count toward every metric.
      const live = (p.actions || []);
      const scopedLive = live.filter((a) => true); // already scoped to p
      const total = scopedLive.length;
      const done = scopedLive.filter((a) => a.status === 'done').length;
      const open = scopedLive.filter((a) => !isClosedStatus(a.status)).length;
      const late = scopedLive.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, todayStr) < 0).length;
      const blocked = scopedLive.filter((a) => a.status === 'blocked').length;
      const upcoming = scopedLive.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, todayStr) >= 0 && dayDiff(a.due, todayStr) <= 7).length;
      const completion = total ? Number((done / total * 100).toFixed(1)) : 0;
      const since14 = dayISO(14);
      const throughput14 = scopedLive.filter((a) => actionCompletedAt(a) && actionCompletedAt(a) >= since14).length;
      // EVM cached — match formula scope (status ≠ Cancelled).
      const evmScope = scopedLive.filter((a) => a.status !== 'cancelled');
      const evm = evmForActions(evmScope);
      // Adherence + productivity cached.
      const savedId = state.currentProjectId;
      state.currentProjectId = p.id;
      const delivery = deliveryMetricsForWindow(dayISO(30), todayStr);
      state.currentProjectId = savedId;

      // Formula filter prefix (empty in single-project mode).
      const pf = projFilter(p.name);

      // ── Counts ────────────────────────────────────────────────────────
      pushF(p, 'Total actions',
        multiProject
          ? `SUMPRODUCT(${pf}(${_r(aId)}<>""))`
          : `COUNTA(Actions!${aId}2:${aId}${N_MAX})`,
        total, 'count', 'Every live (non-deleted) action');

      pushF(p, 'Done actions',
        multiProject
          ? `SUMPRODUCT(${pf}(${_r(aStatus)}="Done"))`
          : `COUNTIF(Actions!${aStatus}:${aStatus},"Done")`,
        done, 'count');

      pushF(p, 'Open actions',
        `SUMPRODUCT(${pf}(${_r(aStatus)}<>"Done")*(${_r(aStatus)}<>"Cancelled")*(${_r(aStatus)}<>""))`,
        open, 'count', 'Status is Not started, In progress, or Blocked');

      pushF(p, 'Late actions',
        `SUMPRODUCT(${pf}(${_r(aDue)}<>"")*(${_r(aDue)}<TEXT(TODAY(),"yyyy-mm-dd"))*(${_r(aStatus)}<>"Done")*(${_r(aStatus)}<>"Cancelled"))`,
        late, 'count', 'Open + past due');

      pushF(p, 'Blocked actions',
        multiProject
          ? `SUMPRODUCT(${pf}(${_r(aStatus)}="Blocked"))`
          : `COUNTIF(Actions!${aStatus}:${aStatus},"Blocked")`,
        blocked, 'count');

      pushF(p, 'Due within 7 days',
        `SUMPRODUCT(${pf}(${_r(aDue)}<>"")*(${_r(aDue)}>=TEXT(TODAY(),"yyyy-mm-dd"))*(${_r(aDue)}<=TEXT(TODAY()+7,"yyyy-mm-dd"))*(${_r(aStatus)}<>"Done")*(${_r(aStatus)}<>"Cancelled"))`,
        upcoming, 'count', 'Open + due in the next 7 days');

      pushF(p, 'Completion',
        `IFERROR(SUMPRODUCT(${pf}(${_r(aStatus)}="Done")) / SUMPRODUCT(${pf}(${_r(aId)}<>"")) * 100, 0)`,
        completion, '%', 'Done ÷ total live actions');

      pushF(p, 'Throughput (14d)',
        `SUMPRODUCT(${pf}(${_r(aCompleted)}<>"")*(${_r(aCompleted)}>=TEXT(TODAY()-14,"yyyy-mm-dd")))`,
        throughput14, 'count', 'Actions whose Completed date is in the last 14 days');

      // ── Delivery KPIs ────────────────────────────────────────────────
      // Adherence: on-time done in the 30-day window ÷ all done in window.
      // "Promised due" lives in its own column on the Actions sheet
      // (=IF(Baseline<>"", Baseline, Due) per-row), so the KPI formula
      // is a single boolean-arithmetic SUMPRODUCT — no IF inline, no
      // splits — and Excel handles it identically to LibreOffice.
      const adhWindow  = `${pf}(${_r(aCompleted)}<>"")*(${_r(aCompleted)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))`;
      const adherenceF = `IFERROR(SUMPRODUCT(${adhWindow}*(${_r(aCompleted)}<=${_r(aPromised)}))/SUMPRODUCT(${adhWindow})*100, "")`;
      pushF(p, 'Schedule adherence (30d)', adherenceF,
        (delivery.adherenceN >= 5 && delivery.adherence != null) ? Number((delivery.adherence * 100).toFixed(1)) : '',
        '%',
        delivery.adherenceN >= 5
          ? `${delivery.adherenceN} actions completed in the last 30 days`
          : `Insufficient data (${delivery.adherenceN}/5 done in 30d) — formula returns blank below threshold`);

      // Productivity: Σ planned hrs ÷ Σ logged hrs on actions completed in window.
      const prodF =
        `IFERROR(SUMPRODUCT(${pf}(${_r(aCompleted)}<>"")*(${_r(aCompleted)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))*${_r(aPlanned)})`
        + ` / SUMPRODUCT(${pf}(${_r(aCompleted)}<>"")*(${_r(aCompleted)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))*${_r(aLogged)}), "")`;
      pushF(p, 'Productivity (30d)', prodF,
        (delivery.doneInWindow >= 5 && delivery.productivity != null) ? Number(delivery.productivity.toFixed(3)) : '',
        '×',
        delivery.doneInWindow >= 5
          ? `${Math.round(delivery.plannedHrs)}h planned / ${Math.round(delivery.loggedHrs)}h logged across ${delivery.doneInWindow} done`
          : `Insufficient data (${delivery.doneInWindow}/5 done with logged hours)`);

      // ── EVM rollup ────────────────────────────────────────────────────
      // All four EVM raw values are SUMPRODUCT(rate × hours) over the
      // appropriate slice. CPI/SPI/EAC/ETC are derived.
      const bacF = `SUMPRODUCT(${pf}(${_r(aStatus)}<>"Cancelled")*(${_r(aStatus)}<>"")*${_r(aPlanned)}*${_r(aRate)})`;
      const pvF  = `SUMPRODUCT(${pf}(${_r(aStatus)}<>"Cancelled")*(${_r(aStatus)}<>"")*${_r(aPV)}*${_r(aRate)})`;
      const evF  = `SUMPRODUCT(${pf}(${_r(aStatus)}<>"Cancelled")*(${_r(aStatus)}<>"")*${_r(aEV)}*${_r(aRate)})`;
      const acF  = `SUMPRODUCT(${pf}(${_r(aStatus)}<>"Cancelled")*(${_r(aStatus)}<>"")*${_r(aLogged)}*${_r(aRate)})`;
      pushF(p, 'EVM — BAC', bacF, Math.round(evm.bac), '€', 'Σ planned hrs × hourly rate (non-cancelled)');
      pushF(p, 'EVM — PV',  pvF,  Math.round(evm.pv),  '€', 'Σ PV hrs × hourly rate (PV hrs uses TODAY() vs Start/Due)');
      pushF(p, 'EVM — EV',  evF,  Math.round(evm.ev),  '€', 'Σ EV hrs × hourly rate (EV hrs = planned × %Complete/100)');
      pushF(p, 'EVM — AC',  acF,  Math.round(evm.ac),  '€', 'Σ logged hrs × hourly rate');
      pushF(p, 'EVM — CPI', `IFERROR(${evF}/${acF}, "")`,
        evm.cpi == null ? '' : Number(evm.cpi.toFixed(3)), '×', 'EV ÷ AC. >1 = under budget');
      pushF(p, 'EVM — SPI', `IFERROR(${evF}/${pvF}, "")`,
        evm.spi == null ? '' : Number(evm.spi.toFixed(3)), '×', 'EV ÷ PV. >1 = ahead of schedule');
      pushF(p, 'EVM — EAC', `IFERROR(${bacF}/(${evF}/${acF}), "")`,
        evm.eac == null ? '' : Math.round(evm.eac), '€', 'BAC ÷ CPI');
      pushF(p, 'EVM — ETC',
        `IFERROR(MAX(0, ${bacF}/(${evF}/${acF}) - ${acF}), "")`,
        evm.etc == null ? '' : Math.round(evm.etc), '€', 'EAC − AC');

      // ── Reference counts ─────────────────────────────────────────────
      // Each domain sheet has one row per item plus a header, so subtract
      // 1 from COUNTA in single-project mode; in multi-project filter by
      // the Project column. Project column is first in those sheets.
      // Sheet names with spaces / special chars MUST be wrapped in
      // single quotes per the A1 syntax — otherwise Excel returns
      // #NAME? when it recalculates and the cell renders blank.
      const sheetRef = (sheet) => /[\s&!@#$%^*()+={}[\]|\\:;"'<>,?/]/.test(sheet)
        ? `'${sheet.replace(/'/g, "''")}'`
        : sheet;
      const refF = (sheet) => multiProject
        ? `SUMPRODUCT((${sheetRef(sheet)}!A2:A${N_MAX}="${p.name.replace(/"/g, '""')}")*(${sheetRef(sheet)}!B2:B${N_MAX}<>""))`
        : `MAX(0, COUNTA(${sheetRef(sheet)}!A:A) - 1)`;
      pushF(p, 'Milestones',      refF('Milestones'),      (p.milestones    || []).length, 'count');
      pushF(p, 'Deliverables',    refF('Deliverables'),    (p.deliverables  || []).length, 'count');
      pushF(p, 'Open points',     refF('Open points'),     (p.openPoints    || []).length, 'count');
      pushF(p, 'Risks & Opps',    refF('Risks & Opps'),    (p.risks         || []).length, 'count');
      pushF(p, 'Decisions',       refF('Decisions'),       (p.decisions     || []).length, 'count');
      pushF(p, 'Change requests', refF('Change requests'), (p.changes       || []).length, 'count');
    });
    sheets.push({
      name: 'KPIs',
      headers: [
        ...(multiProject ? ['Project'] : []),
        'Metric', 'Value', 'Unit', 'Notes',
      ],
      rows: kpiRows,
      frozenHeader: true,
      colWidths: [
        ...(multiProject ? [20] : []),
        32, 16, 10, 70,
      ],
    });

    // ── 9) Dashboard — at-a-glance KPI tiles + 3 embedded charts.
    // Built last (so all formula helpers are in scope) then unshifted
    // to position 0 so it opens first.
    {
      // Portfolio-wide cached values for the headline tiles. Multi-project
      // mode aggregates across every in-scope project; single-project
      // uses just the current project.
      // Dashboard rollup — archived actions still count toward every metric.
      const allLive = projects.flatMap((p) => (p.actions || []));
      const _todayStr = todayISO();
      const dashTotal    = allLive.length;
      const dashDone     = allLive.filter((a) => a.status === 'done').length;
      const dashOpen     = allLive.filter((a) => !isClosedStatus(a.status)).length;
      const dashLate     = allLive.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, _todayStr) < 0).length;
      const dashBlocked  = allLive.filter((a) => a.status === 'blocked').length;
      const dashUpcoming = allLive.filter((a) => a.due && !isClosedStatus(a.status) && dayDiff(a.due, _todayStr) >= 0 && dayDiff(a.due, _todayStr) <= 7).length;
      const dashCancelled = allLive.filter((a) => a.status === 'cancelled').length;
      const dashTodo     = allLive.filter((a) => a.status === 'todo').length;
      const dashDoing    = allLive.filter((a) => a.status === 'doing').length;
      const dashCompletion = dashTotal ? Number((dashDone / dashTotal * 100).toFixed(1)) : 0;
      const dashEvm = evmForActions(allLive.filter((a) => a.status !== 'cancelled'));

      // Adherence / productivity for the dashboard (portfolio scope).
      const _savedProjectId = state.currentProjectId;
      state.currentProjectId = multiProject ? '__all__' : projects[0].id;
      const dashDelivery = deliveryMetricsForWindow(dayISO(30), _todayStr);
      state.currentProjectId = _savedProjectId;

      // Formula builders — same patterns as the KPI sheet but unfiltered
      // (or filtered to portfolio scope). Range bounded at row 10001.
      const aId = A['ID'];
      const aSt = A['Status'];
      const aDu = A['Due'];
      const aCm = A['Completed date'];
      const aPm = A['Promised due'];
      const aPl = A['Planned hours'];
      const aLg = A['Logged hours'];
      const aRt = A['Hourly rate'];
      const aBl = A['Baseline due'];
      const aPv = A['PV hours'];
      const aEv = A['EV hours'];
      const _ref = (col) => `Actions!${col}2:${col}10001`;

      const fTotal    = `COUNTA(${_ref(aId)})`;
      const fDone     = `COUNTIF(Actions!${aSt}:${aSt},"Done")`;
      const fOpen     = `SUMPRODUCT((${_ref(aSt)}<>"Done")*(${_ref(aSt)}<>"Cancelled")*(${_ref(aSt)}<>""))`;
      const fLate     = `SUMPRODUCT((${_ref(aDu)}<>"")*(${_ref(aDu)}<TEXT(TODAY(),"yyyy-mm-dd"))*(${_ref(aSt)}<>"Done")*(${_ref(aSt)}<>"Cancelled"))`;
      const fBlocked  = `COUNTIF(Actions!${aSt}:${aSt},"Blocked")`;
      const fUpcoming = `SUMPRODUCT((${_ref(aDu)}<>"")*(${_ref(aDu)}>=TEXT(TODAY(),"yyyy-mm-dd"))*(${_ref(aDu)}<=TEXT(TODAY()+7,"yyyy-mm-dd"))*(${_ref(aSt)}<>"Done")*(${_ref(aSt)}<>"Cancelled"))`;
      const fCompletion = `IFERROR(${fDone}/${fTotal}*100, 0)`;
      const fTodo      = `COUNTIF(Actions!${aSt}:${aSt},"Not started")`;
      const fDoing     = `COUNTIF(Actions!${aSt}:${aSt},"In progress")`;
      const fCancelled = `COUNTIF(Actions!${aSt}:${aSt},"Cancelled")`;
      // Adherence: see the comment on adherenceF in the KPI builder.
      // Uses the Promised due column so the formula is a single clean
      // SUMPRODUCT, no IF, no array semantics — works in every Excel.
      const dashAdhWindow = `(${_ref(aCm)}<>"")*(${_ref(aCm)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))`;
      const fAdherence = `IFERROR(SUMPRODUCT(${dashAdhWindow}*(${_ref(aCm)}<=${_ref(aPm)}))/SUMPRODUCT(${dashAdhWindow})*100, "")`;
      const fProductivity = `IFERROR(SUMPRODUCT((${_ref(aCm)}<>"")*(${_ref(aCm)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))*${_ref(aPl)})/SUMPRODUCT((${_ref(aCm)}<>"")*(${_ref(aCm)}>=TEXT(TODAY()-30,"yyyy-mm-dd"))*${_ref(aLg)}), "")`;
      const fBac = `SUMPRODUCT((${_ref(aSt)}<>"Cancelled")*(${_ref(aSt)}<>"")*${_ref(aPl)}*${_ref(aRt)})`;
      const fPv  = `SUMPRODUCT((${_ref(aSt)}<>"Cancelled")*(${_ref(aSt)}<>"")*${_ref(aPv)}*${_ref(aRt)})`;
      const fEv  = `SUMPRODUCT((${_ref(aSt)}<>"Cancelled")*(${_ref(aSt)}<>"")*${_ref(aEv)}*${_ref(aRt)})`;
      const fAc  = `SUMPRODUCT((${_ref(aSt)}<>"Cancelled")*(${_ref(aSt)}<>"")*${_ref(aLg)}*${_ref(aRt)})`;
      const fCpi = `IFERROR(${fEv}/${fAc}, "")`;
      const fSpi = `IFERROR(${fEv}/${fPv}, "")`;
      const fEac = `IFERROR(${fBac}/(${fEv}/${fAc}), "")`;
      const fEtc = `IFERROR(MAX(0, ${fBac}/(${fEv}/${fAc}) - ${fAc}), "")`;

      // Band picker for value tiles. >= goodAt → green; <= badAt → red;
      // between → amber. higherIsBetter flips the polarity.
      const bandStyle = (val, goodAt, badAt, higherIsBetter = true) => {
        if (val == null || val === '' || !Number.isFinite(val)) return STYLE.KPI_BIG;
        if (higherIsBetter) {
          if (val >= goodAt) return STYLE.GOOD;
          if (val <= badAt)  return STYLE.BAD;
          return STYLE.WARN;
        } else {
          if (val <= goodAt) return STYLE.GOOD;
          if (val >= badAt)  return STYLE.BAD;
          return STYLE.WARN;
        }
      };

      const dashTitle = multiProject
        ? 'Portfolio dashboard — all projects'
        : `Project dashboard — ${projects[0].name}`;

      // Row content — uses formula cells so the dashboard is live.
      const dashRows = [
        // Row 1: big title
        [{ v: dashTitle, s: STYLE.BIG_TITLE }],
        // Row 2: generation timestamp
        [{ v: `Generated ${_todayStr} — values recompute on data edit`, s: STYLE.LABEL }],
        // Row 3: blank
        [],
        // Row 4: section
        [{ v: 'Headline KPIs', s: STYLE.SECTION }],
        // Row 5: tile labels (6 wide)
        [
          { v: 'TOTAL ACTIONS', s: STYLE.LABEL },
          { v: 'DONE %',        s: STYLE.LABEL },
          { v: 'OPEN',          s: STYLE.LABEL },
          { v: 'LATE',          s: STYLE.LABEL },
          { v: 'BLOCKED',       s: STYLE.LABEL },
          { v: 'DUE ≤ 7 DAYS',  s: STYLE.LABEL },
        ],
        // Row 6: tile values
        [
          { f: fTotal,      v: dashTotal,      s: STYLE.KPI_BIG },
          { f: fCompletion, v: dashCompletion, s: bandStyle(dashCompletion, 70, 40, true) },
          { f: fOpen,       v: dashOpen,       s: STYLE.KPI_BIG },
          { f: fLate,       v: dashLate,       s: bandStyle(dashLate, 0, 10, false) },
          { f: fBlocked,    v: dashBlocked,    s: bandStyle(dashBlocked, 0, 5, false) },
          { f: fUpcoming,   v: dashUpcoming,   s: STYLE.KPI_BIG },
        ],
        // Row 7: blank
        [],
        // Row 8: delivery + EVM labels
        [
          { v: 'ADHERENCE 30d',    s: STYLE.LABEL },
          { v: 'PRODUCTIVITY 30d', s: STYLE.LABEL },
          { v: 'EVM CPI',          s: STYLE.LABEL },
          { v: 'EVM SPI',          s: STYLE.LABEL },
          { v: 'EAC (€)',          s: STYLE.LABEL },
          { v: 'ETC (€)',          s: STYLE.LABEL },
        ],
        // Row 9: values
        [
          { f: fAdherence, v: (dashDelivery.adherenceN >= 5 && dashDelivery.adherence != null) ? Number((dashDelivery.adherence * 100).toFixed(1)) : '',
            s: bandStyle(dashDelivery.adherence != null ? dashDelivery.adherence * 100 : null, 85, 70, true) },
          { f: fProductivity, v: (dashDelivery.doneInWindow >= 5 && dashDelivery.productivity != null) ? Number(dashDelivery.productivity.toFixed(3)) : '',
            s: bandStyle(dashDelivery.productivity, 1.0, 0.85, true) },
          { f: fCpi, v: dashEvm.cpi == null ? '' : Number(dashEvm.cpi.toFixed(3)),
            s: bandStyle(dashEvm.cpi, 1.0, 0.85, true) },
          { f: fSpi, v: dashEvm.spi == null ? '' : Number(dashEvm.spi.toFixed(3)),
            s: bandStyle(dashEvm.spi, 1.0, 0.85, true) },
          { f: fEac, v: dashEvm.eac == null ? '' : Math.round(dashEvm.eac), s: STYLE.KPI_BIG },
          { f: fEtc, v: dashEvm.etc == null ? '' : Math.round(dashEvm.etc), s: STYLE.KPI_BIG },
        ],
        // Row 10: blank
        [],
        // Row 11: section
        [{ v: 'Action status mix', s: STYLE.SECTION }],
        // Row 12: status names (chart categories)
        [
          { v: 'Not started', s: STYLE.HEADER },
          { v: 'In progress', s: STYLE.HEADER },
          { v: 'Blocked',     s: STYLE.HEADER },
          { v: 'Done',        s: STYLE.HEADER },
          { v: 'Cancelled',   s: STYLE.HEADER },
        ],
        // Row 13: status counts (chart values)
        [
          { f: fTodo,      v: dashTodo,      s: STYLE.INT },
          { f: fDoing,     v: dashDoing,     s: STYLE.INT },
          { f: fBlocked,   v: dashBlocked,   s: STYLE.INT },
          { f: fDone,      v: dashDone,      s: STYLE.INT },
          { f: fCancelled, v: dashCancelled, s: STYLE.INT },
        ],
        // Row 14: blank (chart anchored from row 15)
        [], [], [], [], [], [], [], [], [], [], [], [], [], [], [], [], // rows 14-29
        // Row 31: section (after chart 1's anchor zone)
        [{ v: 'EVM rollup (€)', s: STYLE.SECTION }],
        // Row 32: EVM labels
        [
          { v: 'BAC', s: STYLE.HEADER },
          { v: 'PV',  s: STYLE.HEADER },
          { v: 'EV',  s: STYLE.HEADER },
          { v: 'AC',  s: STYLE.HEADER },
        ],
        // Row 33: EVM values
        [
          { f: fBac, v: Math.round(dashEvm.bac), s: STYLE.CURRENCY },
          { f: fPv,  v: Math.round(dashEvm.pv),  s: STYLE.CURRENCY },
          { f: fEv,  v: Math.round(dashEvm.ev),  s: STYLE.CURRENCY },
          { f: fAc,  v: Math.round(dashEvm.ac),  s: STYLE.CURRENCY },
        ],
        // Rows 34-49: chart 2 anchor zone
        [], [], [], [], [], [], [], [], [], [], [], [], [], [], [], [],
        // Row 51: section
        [{ v: 'Action counts overview', s: STYLE.SECTION }],
        // Row 52: count category labels
        [
          { v: 'Total',   s: STYLE.HEADER },
          { v: 'Done',    s: STYLE.HEADER },
          { v: 'Open',    s: STYLE.HEADER },
          { v: 'Late',    s: STYLE.HEADER },
          { v: 'Blocked', s: STYLE.HEADER },
        ],
        // Row 53: count values
        [
          { f: fTotal,   v: dashTotal,   s: STYLE.INT },
          { f: fDone,    v: dashDone,    s: STYLE.INT },
          { f: fOpen,    v: dashOpen,    s: STYLE.INT },
          { f: fLate,    v: dashLate,    s: STYLE.INT },
          { f: fBlocked, v: dashBlocked, s: STYLE.INT },
        ],
      ];
      sheets.unshift({
        name: 'Dashboard',
        headers: null,
        rows: dashRows,
        colWidths: [22, 22, 22, 22, 22, 22, 14, 14, 14, 14],
        // Three column / bar charts. Each anchors to a 7-col × 14-row
        // patch of the sheet, leaving space for the data tables.
        charts: [
          {
            title: 'Action status mix',
            type: 'column',
            catRef: 'Dashboard!$A$12:$E$12',
            series: [{ name: 'Count', valRef: 'Dashboard!$A$13:$E$13' }],
            colors: ['8C8C8C', '1F77B4', 'F57F17', '2E7D32', 'B71C1C'],
            from: { col: 0, row: 14 }, to: { col: 7, row: 29 },
          },
          {
            title: 'EVM rollup',
            type: 'column',
            catRef: 'Dashboard!$A$32:$D$32',
            series: [{ name: 'Value (€)', valRef: 'Dashboard!$A$33:$D$33' }],
            colors: ['1F4E79', '5B9BD5', '70AD47', 'C00000'],
            from: { col: 0, row: 34 }, to: { col: 7, row: 49 },
          },
          {
            title: 'Action counts overview',
            type: 'bar',
            catRef: 'Dashboard!$A$52:$E$52',
            series: [{ name: 'Count', valRef: 'Dashboard!$A$53:$E$53' }],
            colors: ['1F4E79', '2E7D32', 'F57F17', 'B71C1C', '8C8C8C'],
            from: { col: 0, row: 54 }, to: { col: 7, row: 69 },
          },
        ],
      });
    }

    return sheets;
  }

  // Public entry point — wired to the sidebar's spreadsheet button.
  function exportProjectXLSX() {
    flushPendingSaves();
    const merged = curProjectIsMerged();
    const projects = merged
      ? (state.settings.showArchivedProjects ? state.projects : state.projects.filter((p) => !p.archived))
      : [curProject()];
    const sheets = _buildProjectExportSheets(projects, merged);
    const bytes = _buildXLSX(sheets);
    const blob = new Blob([bytes], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    const label = merged ? 'portfolio' : (curProject()?.name || 'project').replace(/\s+/g, '_');
    a.href = url;
    a.download = `cockpit-${label}-${todayISO()}.xlsx`;
    a.click();
    URL.revokeObjectURL(url);
    const totalRows = sheets.reduce((s, sh) => s + sh.rows.length, 0);
    toast(`Exported ${sheets.length} sheets · ${totalRows} rows → ${a.download}`);
  }

  function exportJSON() {
    flushPendingSaves();
    // The envelope wraps the state but the state is also spread at the
    // top level for backward compat — older importers that just
    // `JSON.parse(file).projects` still work.
    const payload = buildExportPayload();
    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = `cockpit-${todayISO()}.json`;
    a.click();
    URL.revokeObjectURL(url);
    const s = payload.__summary;
    toast(`Exported · ${s.projects} projects · ${s.people} people · ${s.actions} actions`);
  }

  // Flush any debounced notes timer + force a saveState immediately. Called by
  // unload / visibility handlers and before export so nothing pending is lost.
  function flushPendingSaves() {
    if (notesSaveTimer) {
      clearTimeout(notesSaveTimer);
      notesSaveTimer = null;
      try { saveNotesNow(); } catch (e) { /* notes panel may not be open */ }
    }
    try { saveState(); } catch (e) { /* quota */ }
    // Force OPFS flush so on-unload paths don't lose the most recent change.
    try { opfsFlushNow(); } catch (e) { /* opfs may be unavailable */ }
  }

  /* ------------------------ OPFS secondary backup --------------------- */
  // Origin Private File System — a sandboxed filesystem available in every
  // modern browser (Chrome, Edge, Safari, Firefox), no user permission
  // prompt, no Secure Context constraint. Survives localStorage clearing
  // (in most cases) and gives us a true safety net at zero user setup.
  //
  // Layout under the OPFS root:
  //   /cockpit/
  //     latest.json                    — most recent save (single file)
  //     saves/<isoStamp>.json          — ring buffer, last OPFS_RING_LIMIT
  //     snapshots/<YYYY-MM-DD>.json    — first save of each day, last
  //                                      OPFS_SNAPSHOT_LIMIT days
  //
  // All files share the export envelope shape (buildExportPayload()), so a
  // raw OPFS file can be imported through the same path as a manual export.
  const OPFS_ROOT_DIR = 'cockpit';
  const OPFS_RING_LIMIT = 20;
  const OPFS_SNAPSHOT_LIMIT = 7;
  let _opfsAvailable = null;     // lazy-detected on first use
  let _opfsLastSaveAt = null;    // ISO string — when we last persisted
  let _opfsLastError  = null;    // last write/read error message, for status UI
  let _opfsDebounceTimer = null;
  // Set during the brief window between a fresh-install seed and the
  // OPFS-recovery decision so the seeded sample doesn't overwrite real
  // backups before the user has a chance to restore them.
  let _opfsLocked = false;

  function opfsSupported() {
    if (_opfsAvailable !== null) return _opfsAvailable;
    _opfsAvailable = !!(navigator.storage && typeof navigator.storage.getDirectory === 'function');
    return _opfsAvailable;
  }

  // Resolve our app directory inside the OPFS root, creating sub-paths
  // lazily so a brand-new origin needs no setup beyond the first save.
  async function opfsAppDir() {
    if (!opfsSupported()) throw new Error('OPFS unavailable in this browser');
    const root = await navigator.storage.getDirectory();
    return root.getDirectoryHandle(OPFS_ROOT_DIR, { create: true });
  }
  async function opfsSubDir(name) {
    const app = await opfsAppDir();
    return app.getDirectoryHandle(name, { create: true });
  }

  async function opfsWriteFile(dir, name, text) {
    const fh = await dir.getFileHandle(name, { create: true });
    const w  = await fh.createWritable();
    await w.write(text);
    await w.close();
  }
  async function opfsReadFile(dir, name) {
    const fh = await dir.getFileHandle(name);
    const f  = await fh.getFile();
    return f.text();
  }
  // List children of a directory handle — entries(...) where supported,
  // .keys() as a fallback. Returns an array of FileSystemHandle.
  async function opfsListEntries(dir) {
    const out = [];
    if (typeof dir.values === 'function') {
      for await (const entry of dir.values()) out.push(entry);
    } else if (typeof dir.entries === 'function') {
      for await (const [, entry] of dir.entries()) out.push(entry);
    }
    return out;
  }

  // Persist the current state to OPFS — `latest.json`, plus a ring-buffer
  // entry, plus a daily snapshot if today's hasn't been written yet. All
  // failures are swallowed (logged to _opfsLastError) — the secondary
  // backup must never be allowed to crash the primary save path.
  async function opfsBackupNow() {
    if (!opfsSupported()) return false;
    if (_recoveryMode) return false; // never write the placeholder seed
    if (_opfsLocked) return false;   // pause while the recovery dialog is open
    try {
      const payload = buildExportPayload();
      const json = JSON.stringify(payload, null, 2);
      const stamp = new Date().toISOString();
      const dayKey = stamp.slice(0, 10); // YYYY-MM-DD (UTC — fine as a bucket)
      const fileStamp = stamp.replace(/[:.]/g, '-');

      const app   = await opfsAppDir();
      const saves = await app.getDirectoryHandle('saves',     { create: true });
      const snaps = await app.getDirectoryHandle('snapshots', { create: true });

      // 1. latest.json (always overwritten — quick to read on recovery)
      await opfsWriteFile(app, 'latest.json', json);

      // 2. Ring buffer
      await opfsWriteFile(saves, fileStamp + '.json', json);
      const ringEntries = (await opfsListEntries(saves))
        .filter((e) => e.kind === 'file' && e.name.endsWith('.json'))
        .sort((a, b) => a.name.localeCompare(b.name));
      while (ringEntries.length > OPFS_RING_LIMIT) {
        const old = ringEntries.shift();
        try { await saves.removeEntry(old.name); } catch (_) {}
      }

      // 3. Daily snapshot — write only if today's file is missing OR older
      //    than 6 hours, so a long single-day session captures more than
      //    just the morning. Keep last 7 days.
      let writeToday = true;
      try {
        const todayHandle = await snaps.getFileHandle(dayKey + '.json');
        const todayFile = await todayHandle.getFile();
        const ageMs = Date.now() - todayFile.lastModified;
        if (ageMs < 6 * 60 * 60 * 1000) writeToday = false;
      } catch (_) { /* missing → write */ }
      if (writeToday) {
        await opfsWriteFile(snaps, dayKey + '.json', json);
      }
      const snapEntries = (await opfsListEntries(snaps))
        .filter((e) => e.kind === 'file' && /^\d{4}-\d{2}-\d{2}\.json$/.test(e.name))
        .sort((a, b) => a.name.localeCompare(b.name));
      while (snapEntries.length > OPFS_SNAPSHOT_LIMIT) {
        const old = snapEntries.shift();
        try { await snaps.removeEntry(old.name); } catch (_) {}
      }

      _opfsLastSaveAt = stamp;
      _opfsLastError  = null;
      // Quietly nudge any open Storage panel / status line to refresh.
      refreshStorageUI();
      return true;
    } catch (e) {
      _opfsLastError = String(e?.message || e);
      console.warn('[cockpit] OPFS backup failed:', e);
      return false;
    }
  }

  // Debounced trigger called from commit() / saveState(). Keystroke-rate
  // changes coalesce into one write per ~3 s; idle states still flush via
  // visibilitychange / beforeunload.
  function scheduleOpfsBackup() {
    if (!opfsSupported()) return;
    if (_opfsDebounceTimer) return;
    _opfsDebounceTimer = setTimeout(() => {
      _opfsDebounceTimer = null;
      opfsBackupNow();
    }, 3000);
  }
  function opfsFlushNow() {
    if (_opfsDebounceTimer) {
      clearTimeout(_opfsDebounceTimer);
      _opfsDebounceTimer = null;
    }
    if (opfsSupported() && !_recoveryMode) opfsBackupNow();
  }

  // Listing + restore APIs used by the Storage panel.
  async function opfsListSaves() {
    if (!opfsSupported()) return [];
    try {
      const app   = await opfsAppDir();
      const saves = await app.getDirectoryHandle('saves', { create: true });
      const items = [];
      for (const e of await opfsListEntries(saves)) {
        if (e.kind !== 'file' || !e.name.endsWith('.json')) continue;
        try {
          const f = await e.getFile();
          items.push({ name: e.name, size: f.size, modified: new Date(f.lastModified).toISOString() });
        } catch (_) {}
      }
      return items.sort((a, b) => b.name.localeCompare(a.name));
    } catch (_) { return []; }
  }
  async function opfsListSnapshots() {
    if (!opfsSupported()) return [];
    try {
      const app   = await opfsAppDir();
      const snaps = await app.getDirectoryHandle('snapshots', { create: true });
      const items = [];
      for (const e of await opfsListEntries(snaps)) {
        if (e.kind !== 'file' || !/^\d{4}-\d{2}-\d{2}\.json$/.test(e.name)) continue;
        try {
          const f = await e.getFile();
          items.push({ name: e.name, date: e.name.replace(/\.json$/, ''), size: f.size, modified: new Date(f.lastModified).toISOString() });
        } catch (_) {}
      }
      return items.sort((a, b) => b.name.localeCompare(a.name));
    } catch (_) { return []; }
  }
  // Read a single OPFS file (saves or snapshots) and return its parsed
  // payload, or null if missing / unreadable.
  async function opfsReadSave(subdir, fileName) {
    if (!opfsSupported()) return null;
    try {
      const app = await opfsAppDir();
      const dir = await app.getDirectoryHandle(subdir, { create: false });
      const text = await opfsReadFile(dir, fileName);
      return JSON.parse(text);
    } catch (_) { return null; }
  }

  // Recovery from OPFS — the path that runs when the primary store
  // (localStorage) is empty BUT the secondary backup (OPFS) has saved
  // data. This is the "memory was wiped, but my work is still here"
  // scenario the user explicitly wanted protected against.
  let _emptyStateBoot = false; // set by init when localStorage was empty
  async function maybeOfferOpfsRecovery() {
    if (!_emptyStateBoot) return;
    if (!opfsSupported()) return;
    _opfsLocked = true; // freeze OPFS writes while the user decides
    let saves = [], snaps = [];
    try {
      [saves, snaps] = await Promise.all([opfsListSaves(), opfsListSnapshots()]);
    } catch (e) {
      _opfsLocked = false;
      return;
    }
    if (saves.length === 0 && snaps.length === 0) {
      _opfsLocked = false;
      return; // nothing to recover — proceed with seeded sample data
    }
    showOpfsRecoveryOverlay(saves, snaps);
  }
  function showOpfsRecoveryOverlay(saves, snaps) {
    if ($('#opfsRecoveryOverlay')) return;
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.id = 'opfsRecoveryOverlay';
    overlay.style.zIndex = '9100';

    // Render snapshot of the lists at the moment of decision so a
    // background OPFS write (which we've locked anyway) couldn't change
    // what the user sees.
    const recentSaves = saves.slice(0, 8);
    const recentSnaps = snaps.slice(0, 8);
    overlay.innerHTML = `
      <div class="desc-modal storage-modal" style="width: 600px;">
        <div class="desc-head">
          <div class="desc-title">⟳ Backups found — restore your data?</div>
        </div>
        <div class="storage-body">
          <div>
            Your primary store (<code>localStorage</code>) is empty, but
            this browser's secondary backup (<code>OPFS</code>) has earlier
            saves. <strong>Your work is recoverable</strong> — pick which
            point to restore from.
          </div>
          <div class="ab-muted" style="font-size: 11px; margin-top: 6px;">
            This usually happens when site data was cleared (cookies wipe,
            browser eviction, switching origin between <code>http://</code>
            and <code>file://</code>). Until you choose, secondary backups
            are paused so the sample data won't overwrite your saves.
          </div>

          ${recentSaves.length ? `
            <div class="storage-section" style="margin-top: 12px;">
              <div class="storage-section-head">Recent saves</div>
              <div class="storage-history">
                ${recentSaves.map((s) => `
                  <div class="storage-history-row">
                    <span class="storage-history-name">${escapeHTML(s.name)}</span>
                    <span class="storage-history-meta">${fmtRelTime(s.modified)}</span>
                    <button class="primary" data-recover-save="${escapeHTML(s.name)}">Restore</button>
                  </div>`).join('')}
              </div>
            </div>` : ''}

          ${recentSnaps.length ? `
            <div class="storage-section">
              <div class="storage-section-head">Daily snapshots</div>
              <div class="storage-history">
                ${recentSnaps.map((s) => `
                  <div class="storage-history-row">
                    <span class="storage-history-name">${escapeHTML(s.date)}</span>
                    <span class="storage-history-meta">${fmtRelTime(s.modified)}</span>
                    <button class="primary" data-recover-snap="${escapeHTML(s.name)}">Restore</button>
                  </div>`).join('')}
              </div>
            </div>` : ''}

          <div class="storage-actions" style="margin-top: 12px; justify-content: flex-end;">
            <button class="ghost" id="opfsRecKeepSample">Keep sample data instead</button>
          </div>
        </div>
      </div>`;
    document.body.appendChild(overlay);

    overlay.querySelectorAll('[data-recover-save]').forEach((b) => {
      b.addEventListener('click', () => doRecoveryFromOpfs('saves', b.dataset.recoverSave));
    });
    overlay.querySelectorAll('[data-recover-snap]').forEach((b) => {
      b.addEventListener('click', () => doRecoveryFromOpfs('snapshots', b.dataset.recoverSnap));
    });
    overlay.querySelector('#opfsRecKeepSample').addEventListener('click', () => {
      _opfsLocked = false;
      overlay.remove();
      // Commit the in-memory seed to localStorage + OPFS, then run the
      // first-run prompts the user would have seen on a clean install.
      saveState();
      maybeRunFirstRunSafety();
      maybeRunFirstRunTour();
    });
  }
  async function doRecoveryFromOpfs(subdir, name) {
    const obj = await opfsReadSave(subdir, name);
    if (!obj) {
      toast('Could not read that backup — try another', 3000);
      return;
    }
    try {
      delete obj.__schemaVersion;
      delete obj.__exportedAt;
      delete obj.__app;
      delete obj.__summary;
      state = normalizeState(obj);
      _opfsLocked = false;
      saveState();              // pushes the recovered state into localStorage + OPFS
      const ov = $('#opfsRecoveryOverlay');
      if (ov) ov.remove();
      render();
      const s = summarizeState(state);
      toast(`Restored · ${s.projects} projects · ${s.actions} actions`, 3000);
      // If the recovered state has the safety flag already set, these are
      // both no-ops. If not (older backup that pre-dates the safety flag),
      // the user gets a chance to set up backups now.
      maybeRunFirstRunSafety();
    } catch (e) {
      toast('Restore failed: ' + (e?.message || e), 4000);
    }
  }

  /* ----------------------- Storage status + panel -------------------- */
  // Bottom-right status pill + the Storage modal that opens from it.
  // The pill is always visible and gives the user three things at a
  // glance: confirmation that saving works, the storage location, and a
  // single-click route to a complete inventory of where their data is.
  function fmtRelTime(iso) {
    if (!iso) return 'never';
    const ms = Date.now() - new Date(iso).getTime();
    if (ms < 0) return 'just now';
    if (ms < 5 * 1000)        return 'just now';
    if (ms < 60 * 1000)       return Math.round(ms / 1000) + ' s ago';
    if (ms < 60 * 60 * 1000)  return Math.round(ms / 60000) + ' min ago';
    if (ms < 24 * 3600 * 1000) return Math.round(ms / 3600000) + ' h ago';
    return Math.round(ms / 86400000) + ' d ago';
  }
  function localStorageBytes() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      return raw ? new Blob([raw]).size : 0;
    } catch (_) { return 0; }
  }

  // Repaint the bottom-right pill. Cheap; called on every saveState(),
  // every markDirty(), and on a 30-s tick so the "X min ago" stays fresh
  // even when nothing else changes. Reflects four states:
  //   - Recovery (red, pulsing)        — saving is paused, original safe
  //   - localStorage write error (red) — saveState threw (quota etc.)
  //   - Unsaved edit (orange, pulsing) — change in flight, not yet saved
  //   - OPFS pending (yellow)          — localStorage saved, OPFS scheduled
  //   - OPFS error (yellow)            — last OPFS write failed
  //   - Idle (green)                   — everything caught up
  function refreshStorageStatusLine() {
    const el = $('#storageStatus');
    if (!el) return;
    const txt = $('#storageStatusText');
    el.classList.remove('is-saving', 'is-warn', 'is-error');
    if (_recoveryMode) {
      el.classList.add('is-error');
      txt.textContent = 'Saving paused — recovery mode';
      return;
    }
    if (_lastSaveError) {
      el.classList.add('is-error');
      txt.textContent = 'Save failed: ' + _lastSaveError.slice(0, 50);
      return;
    }
    // Unsaved edit — _lastEditAt is newer than the last successful save.
    // For localStorage this is essentially the notes-debounce window; for
    // commit() the gap is microseconds so we won't notice. ISO strings
    // sort lexicographically so direct comparison works.
    if (_lastEditAt && (!_lastSaveAt || _lastEditAt > _lastSaveAt)) {
      el.classList.add('is-saving');
      txt.textContent = 'Unsaved changes…';
      return;
    }
    if (_opfsLastError) {
      el.classList.add('is-warn');
      txt.textContent = `Saved · OPFS error (${_opfsLastError.slice(0, 40)})`;
      return;
    }
    // OPFS write pending — localStorage already wrote, but the durable
    // backup is still inside its 3-s debounce window. Show this so the
    // user can see "the safety net is catching up".
    if (_opfsDebounceTimer || (_lastSaveAt && _opfsLastSaveAt && _lastSaveAt > _opfsLastSaveAt)) {
      el.classList.add('is-warn');
      txt.textContent = `Saved ${fmtRelTime(_lastSaveAt)} · backup pending`;
      return;
    }
    const rel = fmtRelTime(_lastSaveAt);
    txt.textContent = `Saved ${rel}`;
  }
  let _statusLineInterval = null;
  function wireStorageStatus() {
    const btn = $('#storageStatus');
    if (!btn) return;
    btn.addEventListener('click', openStorageOverlay);
    refreshStorageStatusLine();
    if (_statusLineInterval) clearInterval(_statusLineInterval);
    // Refresh every 30 s so relative times decay into "X min ago".
    _statusLineInterval = setInterval(refreshStorageStatusLine, 30 * 1000);
  }

  // Combined refresh — called by saveState() / opfsBackupNow() / panel
  // wiring so any UI surface that depends on storage state stays in sync.
  function refreshStorageUI() {
    refreshStorageStatusLine();
    if ($('#storageOverlay')) renderStorageOverlay();
  }

  // Storage panel — modal listing every place the data lives, with
  // one-click restore from any OPFS save / snapshot, plus links to the
  // existing manual export, auto-backup folder, and import paths.
  function openStorageOverlay() {
    closeStorageOverlay();
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.id = 'storageOverlay';
    document.body.appendChild(overlay);
    renderStorageOverlay();
  }
  function closeStorageOverlay() {
    const ov = $('#storageOverlay');
    if (ov) ov.remove();
  }

  function renderStorageOverlay() {
    const overlay = $('#storageOverlay');
    if (!overlay) return;

    const ab = state.settings.autoBackup || {};
    const mode = ab.mode || (pickerAvailable ? 'folder' : 'download');
    const lsBytes = localStorageBytes();
    const lsKb = (lsBytes / 1024).toFixed(1);
    const opfsAvail = opfsSupported();

    // Render placeholder synchronously, fill OPFS lists asynchronously
    // so the modal opens instantly.
    overlay.innerHTML = `
      <div class="desc-modal storage-modal">
        <div class="desc-head">
          <div class="desc-title">Where your data lives</div>
          <button class="icon-btn" id="storageClose" title="Close">×</button>
        </div>
        <div class="storage-body">

          <div class="storage-section">
            <div class="storage-section-head">
              <span class="storage-state-dot${_recoveryMode ? ' bad' : ''}"></span>
              <span>Primary store · localStorage</span>
            </div>
            <div class="storage-section-row"><span>Origin</span><code>${escapeHTML(location.origin || location.protocol + '//' + location.hostname)}</code></div>
            <div class="storage-section-row"><span>Key</span><code>${escapeHTML(STORAGE_KEY)}</code></div>
            <div class="storage-section-row"><span>Size</span><span>${lsKb} KB</span></div>
            <div class="storage-section-row"><span>Last save</span><span>${fmtRelTime(_lastSaveAt)}</span></div>
            ${_recoveryMode ? '<div class="ab-warn" style="margin-top: 6px;">Recovery mode is active — saving is paused so the original (broken-but-recoverable) bytes aren\'t overwritten.</div>' : ''}
          </div>

          <div class="storage-section">
            <div class="storage-section-head">
              <span class="storage-state-dot${opfsAvail ? '' : ' warn'}"></span>
              <span>Secondary backup · OPFS (in-browser)</span>
            </div>
            ${opfsAvail
              ? `<div class="storage-section-row"><span>Path</span><code>OPFS:/${escapeHTML(OPFS_ROOT_DIR)}/</code></div>
                 <div class="storage-section-row"><span>Last write</span><span>${fmtRelTime(_opfsLastSaveAt)}</span></div>
                 ${_opfsLastError ? `<div class="storage-section-row" style="color: var(--bad);"><span>Error</span><span>${escapeHTML(_opfsLastError)}</span></div>` : ''}
                 <div class="ab-muted" style="font-size: 11px; padding-top: 2px;">
                   Always-on safety net inside this browser. Survives most
                   localStorage clearing. Browseable below — restore any
                   save with one click.
                 </div>`
              : '<div class="ab-muted" style="font-size: 11px;">This browser doesn\'t expose the Origin Private File System — secondary backup unavailable. The folder backup and manual Export below are still active.</div>'}
            ${opfsAvail ? `
              <div style="margin-top: 8px;">
                <div class="storage-section-row" style="margin-bottom: 4px;"><strong>Recent saves (rolling)</strong><span id="opfsSavesCount">…</span></div>
                <div class="storage-history" id="opfsSavesList"><div style="padding: 8px; color: var(--text-faint); font-size: 11px;">Loading…</div></div>
              </div>
              <div style="margin-top: 8px;">
                <div class="storage-section-row" style="margin-bottom: 4px;"><strong>Daily snapshots</strong><span id="opfsSnapsCount">…</span></div>
                <div class="storage-history" id="opfsSnapsList"><div style="padding: 8px; color: var(--text-faint); font-size: 11px;">Loading…</div></div>
              </div>
              <div class="storage-actions" style="margin-top: 8px;">
                <button class="ghost" id="storageOpfsBackupNow">Snapshot now</button>
              </div>` : ''}
          </div>

          <div class="storage-section">
            <div class="storage-section-head">
              <span class="storage-state-dot${ab.enabled ? '' : ' warn'}"></span>
              <span>Folder / file auto-backup</span>
            </div>
            <div class="storage-section-row"><span>Mode</span><span>${mode === 'folder' ? 'Save to chosen folder' : 'Auto-download to Downloads'}</span></div>
            <div class="storage-section-row"><span>Frequency</span><span>${ab.enabled ? `every ${ab.intervalMinutes} min` : '<em>disabled</em>'}</span></div>
            <div class="storage-section-row"><span>Folder / label</span><span>${ab.dirName ? escapeHTML(ab.dirName) : '<em>not set</em>'}</span></div>
            <div class="storage-section-row"><span>Last successful run</span><span>${fmtRelTime(ab.lastBackupAt)}</span></div>
            <div class="storage-actions">
              <button class="ghost" id="storageOpenAB">Open auto-backup settings…</button>
            </div>
          </div>

          <div class="storage-section">
            <div class="storage-section-head">
              <span class="storage-state-dot"></span>
              <span>Manual export · import</span>
            </div>
            <div class="ab-muted" style="font-size: 11px;">
              Save a snapshot to a file you choose, or restore from one. Always available, in any browser.
            </div>
            <div class="storage-actions">
              <button class="primary" id="storageExportNow">↑ Export JSON now</button>
              <button class="ghost"   id="storageImportNow">↓ Import a backup file…</button>
            </div>
          </div>

        </div>
      </div>`;

    overlay.querySelector('#storageClose').addEventListener('click', closeStorageOverlay);
    overlay.querySelector('#storageExportNow')?.addEventListener('click', () => exportJSON());
    overlay.querySelector('#storageImportNow')?.addEventListener('click', () => { closeStorageOverlay(); openImportPicker(); });
    overlay.querySelector('#storageOpenAB')?.addEventListener('click', () => { closeStorageOverlay(); openAutoBackupPanel(); });
    overlay.querySelector('#storageOpfsBackupNow')?.addEventListener('click', async () => {
      const ok = await opfsBackupNow();
      toast(ok ? 'OPFS snapshot saved' : 'OPFS snapshot failed — see console', 2200);
      renderStorageOverlay();
    });

    if (opfsAvail) populateOpfsLists(overlay);
  }

  // Async OPFS list population — placed in its own function so the modal
  // shell can render synchronously, with each list filling in shortly
  // after.
  async function populateOpfsLists(overlay) {
    const saves     = await opfsListSaves();
    const snapshots = await opfsListSnapshots();

    const savesEl = overlay.querySelector('#opfsSavesList');
    const savesCt = overlay.querySelector('#opfsSavesCount');
    if (savesEl) {
      savesEl.innerHTML = saves.length
        ? saves.map((s) => `
            <div class="storage-history-row" data-save-name="${escapeHTML(s.name)}">
              <span class="storage-history-name">${escapeHTML(s.name)}</span>
              <span class="storage-history-meta">${fmtRelTime(s.modified)}</span>
              <button class="ghost" data-restore-save="${escapeHTML(s.name)}">Restore</button>
              <button class="ghost" data-download-save="${escapeHTML(s.name)}">↓</button>
            </div>`).join('')
        : '<div style="padding: 8px; color: var(--text-faint); font-size: 11px;">No saves yet — make any change and one will appear here within 3 s.</div>';
    }
    if (savesCt) savesCt.textContent = saves.length ? `${saves.length} kept (last ${OPFS_RING_LIMIT})` : '';

    const snapsEl = overlay.querySelector('#opfsSnapsList');
    const snapsCt = overlay.querySelector('#opfsSnapsCount');
    if (snapsEl) {
      snapsEl.innerHTML = snapshots.length
        ? snapshots.map((s) => `
            <div class="storage-history-row" data-snap-name="${escapeHTML(s.name)}">
              <span class="storage-history-name">${escapeHTML(s.date)}</span>
              <span class="storage-history-meta">${fmtRelTime(s.modified)}</span>
              <button class="ghost" data-restore-snap="${escapeHTML(s.name)}">Restore</button>
              <button class="ghost" data-download-snap="${escapeHTML(s.name)}">↓</button>
            </div>`).join('')
        : '<div style="padding: 8px; color: var(--text-faint); font-size: 11px;">No daily snapshots yet — one is written automatically the first time you save each day.</div>';
    }
    if (snapsCt) snapsCt.textContent = snapshots.length ? `${snapshots.length} kept (last ${OPFS_SNAPSHOT_LIMIT} days)` : '';

    // Wire each row's Restore / Download — done after innerHTML so the
    // closures see the current button.
    overlay.querySelectorAll('[data-restore-save]').forEach((b) => {
      b.addEventListener('click', () => restoreFromOpfs('saves', b.dataset.restoreSave));
    });
    overlay.querySelectorAll('[data-restore-snap]').forEach((b) => {
      b.addEventListener('click', () => restoreFromOpfs('snapshots', b.dataset.restoreSnap));
    });
    overlay.querySelectorAll('[data-download-save]').forEach((b) => {
      b.addEventListener('click', () => downloadOpfsFile('saves', b.dataset.downloadSave));
    });
    overlay.querySelectorAll('[data-download-snap]').forEach((b) => {
      b.addEventListener('click', () => downloadOpfsFile('snapshots', b.dataset.downloadSnap));
    });
  }

  async function restoreFromOpfs(subdir, name) {
    const obj = await opfsReadSave(subdir, name);
    if (!obj) { toast('Could not read backup'); return; }
    const cur = {
      projects: state.projects?.length || 0,
      actions:  (state.projects || []).reduce((n, p) => n + (p.actions?.length || 0), 0),
    };
    const next = {
      projects: obj.projects?.length || 0,
      actions:  (obj.projects || []).reduce((n, p) => n + (p.actions?.length || 0), 0),
    };
    const ok = confirm(
      `Restore from OPFS ${subdir}/${name}?\n\n` +
      `Current  →  ${cur.projects} projects · ${cur.actions} actions\n` +
      `Restored →  ${next.projects} projects · ${next.actions} actions\n\n` +
      `This will replace your current data. Undo (⌘Z) reverts.`,
    );
    if (!ok) return;
    try {
      delete obj.__schemaVersion;
      delete obj.__exportedAt;
      delete obj.__app;
      delete obj.__summary;
      undoStack.push(JSON.stringify(state));
      state = normalizeState(obj);
      saveState();
      render();
      closeStorageOverlay();
      toast('Restored from backup');
    } catch (e) {
      toast('Restore failed: ' + (e?.message || e));
    }
  }

  async function downloadOpfsFile(subdir, name) {
    const obj = await opfsReadSave(subdir, name);
    if (!obj) { toast('Could not read backup'); return; }
    const blob = new Blob([JSON.stringify(obj, null, 2)], { type: 'application/json' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = name.endsWith('.json') ? name : (name + '.json');
    a.click();
    URL.revokeObjectURL(a.href);
    toast('Downloaded');
  }

  /* ----------------------- Google Drive sync mode -------------------- */
  // Native Drive integration via Google Identity Services + Drive REST.
  // Uses the `drive.file` scope so the app only sees files it created;
  // backups land in the user's My Drive as cockpit-latest.json /
  // cockpit-previous.json. The OAuth client ID is supplied by the user
  // (paste-it-once into Storage settings) — no secret server needed.

  const GIS_SCRIPT_URL = 'https://accounts.google.com/gsi/client';
  const GDRIVE_SCOPES  = 'https://www.googleapis.com/auth/drive.file';
  const GDRIVE_FILES = { latest: 'cockpit-latest.json', previous: 'cockpit-previous.json' };
  let _gdriveToken = null;        // { access_token, expires_at }
  let _gdriveTokenClient = null;
  let _gisLoadingPromise = null;

  function gdriveSupported() {
    // GIS only works in a secure context with an HTTPS or http://localhost origin.
    return typeof window !== 'undefined'
      && (window.isSecureContext === true)
      && location.protocol !== 'file:';
  }

  function loadGoogleIdentityServices() {
    if (window.google?.accounts?.oauth2) return Promise.resolve();
    if (_gisLoadingPromise) return _gisLoadingPromise;
    _gisLoadingPromise = new Promise((resolve, reject) => {
      const s = document.createElement('script');
      s.src = GIS_SCRIPT_URL;
      s.async = true;
      s.defer = true;
      s.onload  = () => resolve();
      s.onerror = () => reject(new Error('Failed to load Google Identity Services'));
      document.head.appendChild(s);
    });
    return _gisLoadingPromise;
  }

  function gdriveTokenValid() {
    return !!_gdriveToken && _gdriveToken.expires_at > Date.now() + 30_000;
  }

  // Request an access token. MUST be invoked from a click handler so the
  // OAuth popup isn't blocked. Returns a Promise that resolves with the
  // token string.
  async function gdriveSignIn() {
    const ab = state.settings.autoBackup;
    const clientId = ab.gdriveClientId;
    if (!clientId) throw new Error('Set a Google OAuth Client ID first');
    if (!gdriveSupported()) throw new Error('Google Drive sync requires https:// or http://localhost (Secure Context)');
    await loadGoogleIdentityServices();
    return new Promise((resolve, reject) => {
      _gdriveTokenClient = google.accounts.oauth2.initTokenClient({
        client_id: clientId,
        scope: GDRIVE_SCOPES,
        callback: (tok) => {
          if (tok.error) { reject(new Error(tok.error)); return; }
          _gdriveToken = {
            access_token: tok.access_token,
            // Drive tokens default to 3600 s; subtract a slack buffer.
            expires_at:   Date.now() + ((tok.expires_in || 3600) - 60) * 1000,
          };
          ab.gdriveSignedIn = true;
          saveState();
          refreshStorageUI();
          resolve(_gdriveToken.access_token);
        },
        error_callback: (err) => reject(new Error(err?.message || 'OAuth error')),
      });
      _gdriveTokenClient.requestAccessToken({ prompt: '' });
    });
  }

  function gdriveSignOut() {
    if (_gdriveToken && window.google?.accounts?.oauth2) {
      try { google.accounts.oauth2.revoke(_gdriveToken.access_token, () => {}); } catch (_) {}
    }
    _gdriveToken = null;
    state.settings.autoBackup.gdriveSignedIn = false;
    saveState();
    refreshStorageUI();
  }

  async function gdriveAuthHeader() {
    if (!gdriveTokenValid()) {
      // Try a silent refresh first (no popup) — works inside the token's
      // implicit-grant SSO window. Falls back to the user re-clicking
      // "Sign in" if it fails.
      if (_gdriveTokenClient) {
        await new Promise((resolve, reject) => {
          _gdriveTokenClient.callback = (tok) => {
            if (tok.error) reject(new Error(tok.error));
            else { _gdriveToken = { access_token: tok.access_token, expires_at: Date.now() + ((tok.expires_in || 3600) - 60) * 1000 }; resolve(); }
          };
          _gdriveTokenClient.requestAccessToken({ prompt: 'none' });
        }).catch(() => { _gdriveToken = null; });
      }
      if (!gdriveTokenValid()) throw new Error('Google Drive sign-in expired — open the Storage panel and re-authenticate');
    }
    return 'Bearer ' + _gdriveToken.access_token;
  }

  async function gdriveFindFile(name) {
    const auth = await gdriveAuthHeader();
    const url = `https://www.googleapis.com/drive/v3/files?q=${encodeURIComponent(`name='${name}' and trashed=false`)}&fields=files(id,name,modifiedTime,size)`;
    const res = await fetch(url, { headers: { Authorization: auth } });
    if (!res.ok) throw new Error(`Drive list failed: ${res.status}`);
    const j = await res.json();
    return (j.files || [])[0] || null;
  }

  async function gdriveUploadJson(name, jsonText, existingId) {
    const auth = await gdriveAuthHeader();
    const metadata = existingId ? { name } : { name, mimeType: 'application/json' };
    const boundary = '-------cockpit' + Math.random().toString(36).slice(2);
    const body =
      `--${boundary}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n` +
      JSON.stringify(metadata) +
      `\r\n--${boundary}\r\nContent-Type: application/json\r\n\r\n` +
      jsonText +
      `\r\n--${boundary}--`;
    const url = existingId
      ? `https://www.googleapis.com/upload/drive/v3/files/${existingId}?uploadType=multipart&fields=id,name,modifiedTime`
      : `https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&fields=id,name,modifiedTime`;
    const res = await fetch(url, {
      method: existingId ? 'PATCH' : 'POST',
      headers: { Authorization: auth, 'Content-Type': `multipart/related; boundary="${boundary}"` },
      body,
    });
    if (!res.ok) {
      const txt = await res.text().catch(() => '');
      throw new Error(`Drive upload failed: ${res.status} ${txt.slice(0, 120)}`);
    }
    return res.json();
  }

  async function gdriveDownloadJson(fileId) {
    const auth = await gdriveAuthHeader();
    const res = await fetch(`https://www.googleapis.com/drive/v3/files/${fileId}?alt=media`, { headers: { Authorization: auth } });
    if (!res.ok) throw new Error(`Drive download failed: ${res.status}`);
    return res.text();
  }

  // The actual sync — same rotation as folder mode (latest → previous).
  async function gdriveBackupNow() {
    if (!state.settings.autoBackup.gdriveClientId) throw new Error('Configure Drive client ID first');
    flushPendingSaves();
    const payload = buildExportPayload();
    const json = JSON.stringify(payload, null, 2);
    // Look up the existing latest + previous so we can rotate atomically.
    const [latest, previous] = await Promise.all([
      gdriveFindFile(GDRIVE_FILES.latest),
      gdriveFindFile(GDRIVE_FILES.previous),
    ]);
    // Step 1: copy current latest contents into previous (overwrite).
    if (latest) {
      try {
        const oldText = await gdriveDownloadJson(latest.id);
        await gdriveUploadJson(GDRIVE_FILES.previous, oldText, previous?.id || null);
      } catch (e) { console.warn('[cockpit] Drive previous-rotation failed (non-fatal):', e); }
    }
    // Step 2: write fresh latest.
    await gdriveUploadJson(GDRIVE_FILES.latest, json, latest?.id || null);
    state.settings.autoBackup.lastBackupAt = new Date().toISOString();
    saveState();
    refreshAutoBackupUI();
    refreshStorageUI();
    return true;
  }

  /* --------------------------- Auto-backup ---------------------------- */
  // Persists a FileSystemDirectoryHandle in IndexedDB so the user grants
  // folder access once and the app keeps writing backup JSONs there at the
  // chosen cadence. Browsers without showDirectoryPicker (Safari/Firefox)
  // fall back to a "Download backup now" button only.
  const IDB_NAME = 'cockpit-meta';
  const IDB_STORE = 'kv';
  function idbOpen() {
    return new Promise((resolve, reject) => {
      const req = indexedDB.open(IDB_NAME, 1);
      req.onupgradeneeded = () => req.result.createObjectStore(IDB_STORE);
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => reject(req.error);
    });
  }
  async function idbSet(key, value) {
    const db = await idbOpen();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(IDB_STORE, 'readwrite');
      tx.objectStore(IDB_STORE).put(value, key);
      tx.oncomplete = () => { db.close(); resolve(); };
      tx.onerror = () => { db.close(); reject(tx.error); };
    });
  }
  async function idbGet(key) {
    const db = await idbOpen();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(IDB_STORE, 'readonly');
      const req = tx.objectStore(IDB_STORE).get(key);
      req.onsuccess = () => { db.close(); resolve(req.result); };
      req.onerror = () => { db.close(); reject(req.error); };
    });
  }
  async function idbDelete(key) {
    const db = await idbOpen();
    return new Promise((resolve, reject) => {
      const tx = db.transaction(IDB_STORE, 'readwrite');
      tx.objectStore(IDB_STORE).delete(key);
      tx.oncomplete = () => { db.close(); resolve(); };
      tx.onerror = () => { db.close(); reject(tx.error); };
    });
  }

  let _backupDirHandle = null;       // FileSystemDirectoryHandle once granted
  let _autoBackupTimer = null;       // setInterval id
  const supportsDirPicker = typeof window.showDirectoryPicker === 'function';
  // showDirectoryPicker requires a Secure Context — `file://` URLs do not
  // qualify in Chromium. Detect this so we can explain it to the user
  // instead of throwing a generic SecurityError on click.
  const isSecureContextHttp =
    typeof window.isSecureContext === 'boolean'
      ? window.isSecureContext && location.protocol !== 'file:'
      : (location.protocol === 'https:' ||
         location.hostname === 'localhost' ||
         location.hostname === '127.0.0.1');
  const pickerAvailable = supportsDirPicker && isSecureContextHttp;

  async function loadBackupDirHandle() {
    if (!supportsDirPicker) return null;
    try {
      const h = await idbGet('autoBackupDir');
      if (!h) return null;
      // Verify we still have permission without prompting
      const perm = await h.queryPermission?.({ mode: 'readwrite' });
      if (perm === 'granted') {
        _backupDirHandle = h;
        return h;
      }
      // Need user gesture to re-prompt; leave for user to re-pick
      return null;
    } catch (e) { return null; }
  }

  // Open the OS folder picker. MUST be invoked synchronously from a click
  // handler — any await before the showDirectoryPicker() call can drop the
  // user-gesture flag and Chromium will throw SecurityError ("Must be
  // handling a user gesture to show a file picker"). We therefore call the
  // picker directly and return the Promise; the caller chains .then for the
  // post-pick work, which can safely contain awaits.
  function requestBackupDir() {
    if (!supportsDirPicker) {
      toast('This browser doesn\'t support folder picking — use Auto-download mode instead.');
      return Promise.resolve(null);
    }
    if (!isSecureContextHttp) {
      // file:// URLs are not Secure Contexts in Chromium. The picker will
      // throw SecurityError immediately. Tell the user the actual reason.
      toast('Folder picking is blocked when opening the file via file://. Serve over http(s) (e.g. python -m http.server) or use Auto-download mode.', 6000);
      console.warn('showDirectoryPicker unavailable: location.protocol =', location.protocol, '— Secure Context required.');
      return Promise.resolve(null);
    }
    let pickerPromise;
    try {
      pickerPromise = window.showDirectoryPicker({ mode: 'readwrite', id: 'cockpit-backup' });
    } catch (e) {
      // Synchronous throw (very rare) — surface the real reason.
      toast('Could not open folder picker: ' + (e?.message || e), 6000);
      console.warn('showDirectoryPicker sync error:', e);
      return Promise.resolve(null);
    }
    return pickerPromise.then(async (h) => {
      const perm = await h.requestPermission({ mode: 'readwrite' });
      if (perm !== 'granted') { toast('Permission denied'); return null; }
      _backupDirHandle = h;
      await idbSet('autoBackupDir', h);
      state.settings.autoBackup.dirName = h.name || 'Selected folder';
      saveState();
      return h;
    }).catch((e) => {
      // AbortError = user dismissed the picker (no toast — that's normal UX).
      // Anything else is worth surfacing so the user knows why nothing happened.
      if (e?.name === 'AbortError') return null;
      const msg = e?.message || String(e);
      const hint = e?.name === 'SecurityError' && location.protocol === 'file:'
        ? ' (this happens when opening the file via file:// — serve over http(s) instead)'
        : '';
      toast('Folder picker error: ' + msg + hint, 6000);
      console.warn('showDirectoryPicker failed:', e);
      return null;
    });
  }

  // Keep only the two most recent backups in the chosen folder:
  //   cockpit-latest.json    — the save just written
  //   cockpit-previous.json  — the save that was 'latest' before this one
  // Older timestamped files from earlier versions of this app are removed
  // so the folder doesn't accumulate.
  const BACKUP_LATEST = 'cockpit-latest.json';
  const BACKUP_PREVIOUS = 'cockpit-previous.json';

  // One-shot download flow used by both "Download now" and the
  // download-mode tick. Filename includes the user-set folder label so the
  // user can identify multiple workspaces in their Downloads folder.
  function downloadBackupNow() {
    flushPendingSaves();
    const payload = buildExportPayload();
    const ab = state.settings.autoBackup;
    const label = (ab.dirName || 'backup').replace(/[^a-zA-Z0-9_\-]/g, '-').slice(0, 48) || 'backup';
    const fname = `cockpit-${label}-latest.json`;
    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url; a.download = fname;
    a.click();
    URL.revokeObjectURL(url);
    state.settings.autoBackup.lastBackupAt = new Date().toISOString();
    saveState();
    refreshAutoBackupUI();
    const s = payload.__summary;
    toast(`Downloaded · ${s.projects} projects · ${s.actions} actions`);
    return true;
  }

  async function writeBackupNow() {
    const ab = state.settings.autoBackup;
    const mode = ab.mode || (pickerAvailable ? 'folder' : 'download');
    // Download mode: trigger a download and we're done. Works in any
    // browser, including non-secure contexts (e.g. opened via file://).
    if (mode === 'download') {
      return downloadBackupNow();
    }
    if (mode === 'gdrive') {
      try {
        await gdriveBackupNow();
        return true;
      } catch (e) {
        toast('Drive backup failed: ' + (e?.message || e), 4000);
        console.warn('[cockpit] gdrive backup error:', e);
        return false;
      }
    }
    if (!_backupDirHandle) {
      toast('Pick a backup folder first', 3000);
      return false;
    }
    try {
      // Ensure permission still granted (some browsers revoke on idle)
      const perm = await _backupDirHandle.queryPermission?.({ mode: 'readwrite' });
      if (perm !== 'granted') {
        const next = await _backupDirHandle.requestPermission?.({ mode: 'readwrite' });
        if (next !== 'granted') { toast('Folder permission expired — pick again', 3000); return false; }
      }
      flushPendingSaves();
      const payload = buildExportPayload();
      const json = JSON.stringify(payload, null, 2);

      // Step 1: rotate latest → previous (read existing latest, write to
      // previous, then we can safely overwrite latest below). If there is
      // no existing latest yet (first backup), skip this step.
      try {
        const oldLatest = await _backupDirHandle.getFileHandle(BACKUP_LATEST).catch(() => null);
        if (oldLatest) {
          const oldFile = await oldLatest.getFile();
          const oldText = await oldFile.text();
          const prevHandle = await _backupDirHandle.getFileHandle(BACKUP_PREVIOUS, { create: true });
          const pw = await prevHandle.createWritable();
          await pw.write(oldText);
          await pw.close();
        }
      } catch (e) { /* non-fatal — keep going so we still write the new latest */ }

      // Step 2: write the new latest.
      const latestHandle = await _backupDirHandle.getFileHandle(BACKUP_LATEST, { create: true });
      const w  = await latestHandle.createWritable();
      await w.write(json);
      await w.close();

      // Step 3: prune any stragglers from old timestamped naming
      // (cockpit-2025-04-23T10-15-22.json and similar).
      try {
        for await (const entry of _backupDirHandle.values()) {
          if (entry.kind !== 'file') continue;
          if (entry.name === BACKUP_LATEST || entry.name === BACKUP_PREVIOUS) continue;
          if (/^cockpit-\d{4}-\d{2}-\d{2}T/.test(entry.name)) {
            await _backupDirHandle.removeEntry(entry.name).catch(() => {});
          }
        }
      } catch (e) { /* iterating fails on some browsers; skip silently */ }

      state.settings.autoBackup.lastBackupAt = new Date().toISOString();
      saveState();
      refreshAutoBackupUI();
      return true;
    } catch (e) {
      toast('Backup failed: ' + (e?.message || e));
      return false;
    }
  }

  // Returns true iff auto-backup is currently armed — i.e. enabled AND
  // the chosen mode has the prerequisites it needs (a folder handle for
  // folder mode; a configured client ID + signed-in token for Drive
  // mode; nothing extra for download mode).
  function isAutoBackupArmed() {
    const ab = state.settings.autoBackup;
    if (!ab.enabled) return false;
    const mode = ab.mode || (pickerAvailable ? 'folder' : 'download');
    if (mode === 'folder' && !_backupDirHandle) return false;
    if (mode === 'gdrive' && (!ab.gdriveClientId || !gdriveTokenValid())) return false;
    return true;
  }

  // Reflect the armed/idle state on the toolbar icon so the user can
  // tell at a glance whether auto-backup is running.
  function updateAutoBackupBadge() {
    const btn = $('#btnAutoBackup');
    if (!btn) return;
    const armed = isAutoBackupArmed();
    btn.classList.toggle('is-active', armed);
    const ab = state.settings.autoBackup;
    if (armed) {
      const mode = ab.mode || (pickerAvailable ? 'folder' : 'download');
      const where = mode === 'folder'
        ? (ab.dirName ? `folder "${ab.dirName}"` : 'chosen folder')
        : 'Downloads folder';
      btn.title = `Auto-backup: every ${ab.intervalMinutes}m → ${where}`;
    } else {
      btn.title = 'Auto-backup — pick a folder + frequency';
    }
  }

  function scheduleAutoBackup() {
    // Always start clean
    if (_autoBackupTimer) { clearInterval(_autoBackupTimer); _autoBackupTimer = null; }
    if (!isAutoBackupArmed()) { updateAutoBackupBadge(); return; }
    const ab = state.settings.autoBackup;
    const ms = ab.intervalMinutes * 60 * 1000;
    _autoBackupTimer = setInterval(() => { writeBackupNow(); }, ms);
    updateAutoBackupBadge();
  }

  function refreshAutoBackupUI() {
    const panel = $('#autoBackupPanel');
    if (!panel || panel.hidden) return;
    renderAutoBackupPanel();
  }

  function renderAutoBackupPanel() {
    const panel = $('#autoBackupPanel');
    if (!panel) return;
    const ab = state.settings.autoBackup;
    const hasHandle = !!_backupDirHandle;
    const mode = ab.mode || (pickerAvailable ? 'folder' : 'download');
    const lastTxt = ab.lastBackupAt ? new Date(ab.lastBackupAt).toLocaleString() : 'never';

    // Capabilities banner — explain WHY folder mode is unavailable, if so.
    let banner = '';
    if (!supportsDirPicker) {
      banner = `<div class="ab-warn">
        Folder auto-backup needs the File System Access API, available
        only in Chromium browsers (Chrome, Edge, Brave, Arc). On this
        browser, use <strong>Auto-download</strong> mode below — backups
        are saved to your browser's Downloads folder.
      </div>`;
    } else if (!isSecureContextHttp) {
      banner = `<div class="ab-warn">
        The OS folder picker is blocked when this page is opened directly
        via <code>file://</code>. To enable folder auto-backup, serve the
        file over <code>http(s)</code> (e.g. <code>python -m http.server</code>
        in the file's directory, then open
        <code>http://localhost:8000/cockpit.html</code>). Otherwise, use
        <strong>Auto-download</strong> mode below.
      </div>`;
    }

    // Mode tile state. Only enable the 'folder' tile when the picker is
    // actually usable — otherwise force 'download'.
    const folderModeUsable = pickerAvailable;
    const effectiveMode = folderModeUsable ? mode : 'download';
    const folderEnabledForToggle = effectiveMode === 'folder' ? hasHandle : true;

    // Mode-specific explanation + input markup. The three modes have
    // very different semantics, so we render them separately to avoid the
    // user thinking they can type a destination path / forgetting they
    // need to sign in.
    let modeBlock;
    if (effectiveMode === 'gdrive') {
      const clientId = ab.gdriveClientId || '';
      const signedIn = !!gdriveTokenValid() || !!ab.gdriveSignedIn;
      const supported = gdriveSupported();
      modeBlock = `
        ${supported ? '' : `<div class="ab-warn">
          Google Drive sync needs a Secure Context (https:// or
          http://localhost). Open Cockpit via a local server to use this
          mode.
        </div>`}
        <div class="ab-row">
          <span class="ab-lbl">OAuth Client ID</span>
          <input type="text" id="abGdriveClient" value="${escapeHTML(clientId)}" placeholder="123456789-abc.apps.googleusercontent.com" style="flex: 1; min-width: 0; background: var(--bg-2); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 6px 8px; color: var(--text); font: monospace 11px;" />
        </div>
        <div class="ab-muted" style="font-size: 11px; padding-left: 4px;">
          Create a free OAuth Client ID at
          <a href="https://console.cloud.google.com/apis/credentials" target="_blank" rel="noopener noreferrer">console.cloud.google.com</a>
          → APIs & Services → Credentials → <em>Create Credentials → OAuth client ID</em>
          (Web application). Add <code>${escapeHTML(location.origin || '')}</code>
          as an authorised JavaScript origin. Paste the client ID here. The
          ID itself isn't a secret — it's safe to keep in the bundle.
        </div>
        <div class="ab-row">
          <span class="ab-lbl">Sign-in</span>
          ${signedIn
            ? '<span class="ab-val" style="color: var(--ok); font-weight: 600;">✓ Connected to Google</span><button class="ghost" id="abGdriveSignout">Sign out</button>'
            : '<button class="primary" id="abGdriveSignin"' + (clientId && supported ? '' : ' disabled') + '>Sign in with Google</button>'}
        </div>`;
    } else if (effectiveMode === 'folder') {
      modeBlock = `
        <div class="ab-row">
          <span class="ab-lbl">Folder</span>
          <span class="ab-val" style="font-family: var(--mono, monospace); font-size: 12px;">${ab.dirName ? escapeHTML(ab.dirName) + (hasHandle ? '' : ' <span class="ab-muted">(reconnect needed)</span>') : '<span class="ab-muted">— none picked —</span>'}</span>
          <button class="ghost" id="abPick">${hasHandle ? 'Re-pick…' : 'Choose folder…'}</button>
        </div>
        ${!hasHandle ? '<div class="ab-muted" style="font-size: 11px; padding-left: 4px;">Click <strong>Choose folder…</strong> and pick the directory you want backups written to. Your OS folder picker will open.</div>' : ''}`;
    } else {
      // Download mode — the text input is purely a filename label, NOT a
      // destination path (browsers cannot write to arbitrary filesystem
      // locations from JS). Make this very explicit so the user doesn't
      // type a path expecting it to redirect the file.
      const labelLooksLikePath = (ab.dirName || '').match(/[\/\\~]/);
      modeBlock = `
        <div class="ab-warn" style="background: rgba(110,168,255,.10); border-color: rgba(110,168,255,.3); color: var(--text);">
          <strong>Browsers cannot save to a custom folder via download.</strong>
          Auto-download mode always writes to your browser's default
          Downloads folder. The field below is just a filename label.
        </div>
        <div class="ab-row">
          <span class="ab-lbl">Filename label</span>
          <input type="text" id="abDirName" value="${escapeHTML(ab.dirName || '')}" placeholder="e.g. cockpit-orbit7" style="flex: 1; min-width: 0; background: var(--bg-2); border: 1px solid var(--line); border-radius: var(--radius-sm); padding: 6px 8px; color: var(--text); font: inherit;" />
        </div>
        <div class="ab-muted" style="font-size: 11px; padding-left: 4px;">
          Files will be named <code>cockpit-${escapeHTML((ab.dirName || 'backup').replace(/[^a-zA-Z0-9_\-]/g, '-').slice(0, 48) || 'backup')}-latest.json</code>.
          ${labelLooksLikePath ? '<br><span style="color: var(--bad);">⚠ Slashes / tildes will be stripped — paths cannot redirect downloads.</span>' : ''}
        </div>`;
    }

    panel.innerHTML = `
      <div class="ab-head">
        <span class="ab-title">Auto-backup</span>
        <button class="icon-btn" id="abClose" title="Close" aria-label="Close">×</button>
      </div>
      <div class="ab-body">
        ${banner}
        <div class="ab-row">
          <span class="ab-lbl">Mode</span>
          <select id="abMode">
            <option value="folder"   ${effectiveMode === 'folder'   ? 'selected' : ''} ${folderModeUsable ? '' : 'disabled'}>Save to chosen folder${folderModeUsable ? '' : ' (unavailable)'}</option>
            <option value="gdrive"   ${effectiveMode === 'gdrive'   ? 'selected' : ''} ${gdriveSupported() ? '' : 'disabled'}>Sync to Google Drive${gdriveSupported() ? '' : ' (needs http(s))'}</option>
            <option value="download" ${effectiveMode === 'download' ? 'selected' : ''}>Auto-download to Downloads folder</option>
          </select>
        </div>
        ${modeBlock}
        <div class="ab-row">
          <span class="ab-lbl">Frequency</span>
          <select id="abInterval">
            <option value="5"    ${ab.intervalMinutes === 5    ? 'selected' : ''}>Every 5 minutes</option>
            <option value="15"   ${ab.intervalMinutes === 15   ? 'selected' : ''}>Every 15 minutes</option>
            <option value="30"   ${ab.intervalMinutes === 30   ? 'selected' : ''}>Every 30 minutes</option>
            <option value="60"   ${ab.intervalMinutes === 60   ? 'selected' : ''}>Every hour</option>
            <option value="360"  ${ab.intervalMinutes === 360  ? 'selected' : ''}>Every 6 hours</option>
            <option value="1440" ${ab.intervalMinutes === 1440 ? 'selected' : ''}>Once a day</option>
          </select>
        </div>
        <div class="ab-row">
          <label class="ab-toggle">
            <input type="checkbox" id="abEnabled" ${ab.enabled && folderEnabledForToggle ? 'checked' : ''} ${folderEnabledForToggle ? '' : 'disabled'} />
            <span>Enable auto-backup</span>
          </label>
        </div>
        <div class="ab-row ab-status">
          <span class="ab-lbl">Last backup</span>
          <span class="ab-val">${lastTxt}</span>
        </div>
        <div class="ab-note ab-muted" style="font-size: 11px; line-height: 1.45; padding-top: 4px;">
          ${effectiveMode === 'folder'
            ? `Folder mode keeps just two files in the chosen folder: <code>cockpit-latest.json</code> (this save) and <code>cockpit-previous.json</code> (the one before). Older backups are pruned on each rotation.`
            : effectiveMode === 'gdrive'
              ? `Drive mode pushes <code>cockpit-latest.json</code> + <code>cockpit-previous.json</code> to your My Drive on every save. Off-machine durability + multi-device sync (open Cockpit at the same URL on another machine, sign in, restore). Files are visible at <a href="https://drive.google.com/" target="_blank" rel="noopener noreferrer">drive.google.com</a>.`
              : `Browsers may append <code>(1)</code>, <code>(2)</code>… to avoid overwriting. To rotate cleanly, turn off "Ask where to save each file" in your browser's download settings.`}
        </div>
        <div class="ab-actions">
          <button class="ghost" id="abDownload" title="Trigger one backup right now (downloads to your default Downloads folder)">Download now</button>
          ${effectiveMode === 'folder'
            ? `<button class="primary" id="abBackupNow" ${hasHandle ? '' : 'disabled'}>Back up to folder</button>`
            : effectiveMode === 'gdrive'
              ? `<button class="primary" id="abBackupNow" ${gdriveTokenValid() ? '' : 'disabled'}>Push to Drive</button>`
              : `<button class="primary" id="abBackupNow">Download backup</button>`}
        </div>
      </div>`;

    // Wire
    panel.querySelector('#abClose')?.addEventListener('click', closeAutoBackupPanel);
    panel.querySelector('#abMode')?.addEventListener('change', (e) => {
      state.settings.autoBackup.mode = e.target.value;
      saveState();
      renderAutoBackupPanel();
      scheduleAutoBackup();
    });
    panel.querySelector('#abPick')?.addEventListener('click', () => {
      // Call requestBackupDir() synchronously — no `async () =>` wrapper —
      // so window.showDirectoryPicker() runs inside the user-gesture window.
      // Post-pick UI work happens in the .then continuation.
      requestBackupDir().then(() => {
        renderAutoBackupPanel();
        scheduleAutoBackup();
      });
    });
    // Google Drive — client-id input + sign-in / sign-out
    panel.querySelector('#abGdriveClient')?.addEventListener('change', (e) => {
      state.settings.autoBackup.gdriveClientId = (e.target.value || '').trim();
      saveState();
      renderAutoBackupPanel();
    });
    panel.querySelector('#abGdriveSignin')?.addEventListener('click', () => {
      gdriveSignIn().then(() => {
        renderAutoBackupPanel();
        scheduleAutoBackup();
        toast('Connected to Google Drive', 2500);
      }).catch((e) => {
        toast('Sign-in failed: ' + (e?.message || e), 4000);
        console.warn('[cockpit] gdrive sign-in error:', e);
      });
    });
    panel.querySelector('#abGdriveSignout')?.addEventListener('click', () => {
      gdriveSignOut();
      renderAutoBackupPanel();
      scheduleAutoBackup();
    });
    // Editable folder-name field — used as the display label and as the
    // filename suffix in download mode. User can rename freely; the change
    // is committed when the field loses focus or Enter is pressed.
    const dirNameEl = panel.querySelector('#abDirName');
    if (dirNameEl) {
      const commitName = () => {
        const v = dirNameEl.value.trim();
        if (state.settings.autoBackup.dirName !== v) {
          state.settings.autoBackup.dirName = v;
          saveState();
        }
      };
      dirNameEl.addEventListener('blur', commitName);
      dirNameEl.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') { e.preventDefault(); dirNameEl.blur(); }
      });
    }
    panel.querySelector('#abInterval')?.addEventListener('change', (e) => {
      const v = parseInt(e.target.value, 10);
      state.settings.autoBackup.intervalMinutes = v;
      saveState();
      scheduleAutoBackup();
    });
    panel.querySelector('#abEnabled')?.addEventListener('change', (e) => {
      state.settings.autoBackup.enabled = !!e.target.checked;
      saveState();
      scheduleAutoBackup();
      if (e.target.checked) toast('Auto-backup enabled');
      else toast('Auto-backup disabled');
    });
    panel.querySelector('#abBackupNow')?.addEventListener('click', async () => {
      const ok = await writeBackupNow();
      if (ok) toast('Backed up');
    });
    panel.querySelector('#abDownload')?.addEventListener('click', () => {
      downloadBackupNow();
    });
  }

  function openAutoBackupPanel() {
    const panel = $('#autoBackupPanel');
    if (!panel) return;
    panel.hidden = false;
    renderAutoBackupPanel();
  }
  function closeAutoBackupPanel() {
    const panel = $('#autoBackupPanel');
    if (panel) panel.hidden = true;
  }
  function wireAutoBackup() {
    const btn = $('#btnAutoBackup');
    const panel = $('#autoBackupPanel');
    if (!btn || !panel) return;
    btn.addEventListener('click', () => {
      panel.hidden ? openAutoBackupPanel() : closeAutoBackupPanel();
    });
    // Click outside the panel and the button → close
    document.addEventListener('mousedown', (e) => {
      if (panel.hidden) return;
      if (e.target.closest('#autoBackupPanel') || e.target.closest('#btnAutoBackup')) return;
      closeAutoBackupPanel();
    });
  }
  async function initAutoBackup() {
    // Always run — download mode works on browsers without showDirectoryPicker
    // and scheduleAutoBackup() also drives the toolbar-icon badge.
    if (supportsDirPicker) {
      try { await loadBackupDirHandle(); } catch (_) {}
    }
    scheduleAutoBackup();
  }
  function importJSON() {
    const input = document.createElement('input');
    input.type = 'file'; input.accept = 'application/json';
    input.addEventListener('change', () => {
      const file = input.files?.[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try {
          const obj = JSON.parse(reader.result);
          if (!obj || typeof obj !== 'object') throw new Error('Invalid JSON');
          if (!obj.projects || !obj.people) throw new Error('Missing projects/people');
          // Strip metadata fields if present (envelope) — they're not state
          delete obj.__schemaVersion;
          delete obj.__exportedAt;
          delete obj.__app;
          // Confirmation guard — replacing the entire workspace is destructive,
          // so make sure the user really wants this. Show a quick summary of
          // what's about to be wiped + what's coming in.
          const cur = {
            projects: state.projects?.length || 0,
            people:   state.people?.length || 0,
            actions:  (state.projects || []).reduce((n, p) => n + (p.actions?.length || 0), 0),
          };
          const next = {
            projects: obj.projects?.length || 0,
            people:   obj.people?.length || 0,
            actions:  (obj.projects || []).reduce((n, p) => n + (p.actions?.length || 0), 0),
          };
          const ok = confirm(
            `Replace ALL current data with the contents of "${file.name}"?\n\n` +
            `Current  →  ${cur.projects} projects · ${cur.people} people · ${cur.actions} actions\n` +
            `Imported →  ${next.projects} projects · ${next.people} people · ${next.actions} actions\n\n` +
            `You can Undo (⌘Z) once after this.\n` +
            `Tip: Cancel and choose "Merge" if you want to combine instead of replace.`,
          );
          if (!ok) { toast('Import cancelled — nothing changed'); return; }
          undoStack.push(JSON.stringify(state));
          state = normalizeState(obj);
          saveState();
          render();
          toast('Imported');
        } catch (e) { toast('Import failed: ' + e.message); }
      };
      reader.readAsText(file);
    });
    input.click();
  }

  // Unified Import / Merge entry point — replaces the two separate
  // bottom-bar buttons with one that asks the user which mode they want.
  function openImportPicker() {
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.innerHTML = `
      <div class="desc-modal" style="width: 480px; max-width: 92vw;">
        <div class="desc-head">
          <div class="desc-title">Import data</div>
          <button class="icon-btn" id="impClose" title="Close">×</button>
        </div>
        <div style="padding: 14px 16px; display: flex; flex-direction: column; gap: 10px;">
          <div style="color: var(--text-dim); font-size: 13px;">How should the file be loaded?</div>
          <button class="ghost" id="impReplace" style="text-align: left; padding: 12px 14px; display: flex; flex-direction: column; gap: 4px; align-items: flex-start;">
            <strong style="font-size: 14px;">Replace existing data</strong>
            <span style="color: var(--text-dim); font-size: 12px;">Wipes the current workspace and loads the file. You'll be asked to confirm before anything is overwritten. (Undo works.)</span>
          </button>
          <button class="ghost" id="impMerge" style="text-align: left; padding: 12px 14px; display: flex; flex-direction: column; gap: 4px; align-items: flex-start;">
            <strong style="font-size: 14px;">Merge into existing data</strong>
            <span style="color: var(--text-dim); font-size: 12px;">Combines the file's records with what you have. New records are added; conflicts get a per-row "keep mine / take theirs" resolution UI.</span>
          </button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    const close = () => overlay.remove();
    overlay.querySelector('#impClose').addEventListener('click', close);
    overlay.querySelector('#impReplace').addEventListener('click', () => { close(); importJSON(); });
    overlay.querySelector('#impMerge').addEventListener('click', () => { close(); openMergeOverlay(); });
  }

  /* ----------------------- Phase K: light merge ------------------------ */
  // 2-way merge between local state ("mine") and a teammate's JSON
  // ("theirs"). The diff is computed per record-kind by id; conflicts
  // (same id, differing payload) get a per-row resolution UI. Records
  // that exist on only one side default to 'include' so nothing is lost
  // unless the user explicitly drops it.
  function openMergeOverlay() {
    const input = document.createElement('input');
    input.type = 'file'; input.accept = 'application/json';
    input.addEventListener('change', () => {
      const file = input.files?.[0];
      if (!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try {
          const obj = JSON.parse(reader.result);
          if (!obj || !obj.projects || !obj.people) throw new Error('Not a Cockpit JSON');
          delete obj.__schemaVersion; delete obj.__exportedAt; delete obj.__app;
          const theirs = normalizeState(JSON.parse(JSON.stringify(obj)));
          showMergeUI(state, theirs);
        } catch (e) { toast('Merge failed: ' + e.message); }
      };
      reader.readAsText(file);
    });
    input.click();
  }
  function recordKey(rec) { return rec.id || ''; }
  function recordSig(rec) {
    // Signature for change detection: stringify everything except edit
    // metadata (so a re-stamp without value change doesn't count as a diff).
    const { __lastEditor, __lastEditAt, ...rest } = rec || {};
    return JSON.stringify(rest);
  }
  function diffArr(mineArr, theirsArr) {
    const mineMap = new Map((mineArr || []).map((r) => [recordKey(r), r]));
    const theirsMap = new Map((theirsArr || []).map((r) => [recordKey(r), r]));
    const onlyMine = [], onlyTheirs = [], conflicts = [], same = [];
    mineMap.forEach((m, id) => {
      if (!theirsMap.has(id)) { onlyMine.push(m); return; }
      const t = theirsMap.get(id);
      if (recordSig(m) === recordSig(t)) same.push(m);
      else conflicts.push({ id, mine: m, theirs: t });
    });
    theirsMap.forEach((t, id) => { if (!mineMap.has(id)) onlyTheirs.push(t); });
    return { onlyMine, onlyTheirs, conflicts, same };
  }
  function showMergeUI(mine, theirs) {
    // Build per-project diffs across actions / openPoints / changes /
    // risks / decisions / deliverables / milestones / components / links.
    const projDiffs = [];
    const mineProj = new Map(mine.projects.map((p) => [p.id, p]));
    const theirsProj = new Map(theirs.projects.map((p) => [p.id, p]));
    const projIds = new Set([...mineProj.keys(), ...theirsProj.keys()]);
    projIds.forEach((pid) => {
      const m = mineProj.get(pid);
      const t = theirsProj.get(pid);
      if (!m && t) {
        projDiffs.push({ pid, name: t.name, only: 'theirs', proj: t });
        return;
      }
      if (m && !t) {
        projDiffs.push({ pid, name: m.name, only: 'mine', proj: m });
        return;
      }
      const arrays = ['actions', 'openPoints', 'changes', 'risks', 'decisions', 'deliverables', 'milestones', 'components', 'links', 'meetings'];
      const perKind = {};
      arrays.forEach((k) => { perKind[k] = diffArr(m[k] || [], t[k] || []); });
      projDiffs.push({ pid, name: m.name, perKind });
    });
    // Resolution state: choices per record-key
    const choices = {};
    // Default: include only-theirs (auto-add new from teammate), keep
    // only-mine (don't drop), conflicts default to 'mine'.
    projDiffs.forEach((pd) => {
      if (!pd.perKind) return;
      Object.entries(pd.perKind).forEach(([kind, d]) => {
        d.onlyTheirs.forEach((r) => { choices[`${pd.pid}:${kind}:${r.id}`] = 'include'; });
        d.onlyMine.forEach((r)   => { choices[`${pd.pid}:${kind}:${r.id}`] = 'keep'; });
        d.conflicts.forEach((c)  => {
          // Tiebreaker: take whichever has the most-recent __lastEditAt
          const mAt = c.mine.__lastEditAt || c.mine.updatedAt || '';
          const tAt = c.theirs.__lastEditAt || c.theirs.updatedAt || '';
          choices[`${pd.pid}:${kind}:${c.id}`] = (tAt > mAt) ? 'theirs' : 'mine';
        });
      });
    });

    const overlay = document.createElement('div');
    overlay.className = 'overlay merge-overlay';
    overlay.innerHTML = `
      <div class="merge-modal">
        <div class="merge-head">
          <div class="merge-title">Merge teammate's JSON</div>
          <button class="icon-btn" id="mClose" title="Close">×</button>
        </div>
        <div class="merge-body" id="mBody"></div>
        <div class="merge-foot">
          <span class="merge-summary" id="mSummary"></span>
          <button class="ghost" id="mCancel">Cancel</button>
          <button class="primary" id="mApply">Apply merge</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    const close = () => overlay.remove();
    $('#mClose', overlay).addEventListener('click', close);
    $('#mCancel', overlay).addEventListener('click', close);

    function renderMergeBody() {
      const body = $('#mBody', overlay);
      let totalConflicts = 0, totalAdds = 0;
      body.innerHTML = projDiffs.map((pd) => {
        if (pd.only === 'theirs') {
          totalAdds++;
          return `<div class="merge-section">
            <div class="merge-section-title">${escapeHTML(pd.name)} <span class="merge-tag merge-tag-add">new project</span></div>
            <div class="merge-row">
              <label class="merge-choice">
                <input type="radio" name="proj-${escapeHTML(pd.pid)}" value="include" checked />
                <span>Include teammate's project</span>
              </label>
              <label class="merge-choice">
                <input type="radio" name="proj-${escapeHTML(pd.pid)}" value="skip" />
                <span>Skip</span>
              </label>
            </div>
          </div>`;
        }
        if (pd.only === 'mine') {
          return `<div class="merge-section">
            <div class="merge-section-title">${escapeHTML(pd.name)} <span class="merge-tag merge-tag-keep">only in yours</span></div>
            <div class="merge-row muted">Project will remain as-is.</div>
          </div>`;
        }
        const blocks = Object.entries(pd.perKind).map(([kind, d]) => {
          const adds = d.onlyTheirs.length;
          const conflicts = d.conflicts.length;
          const onlyMineN = d.onlyMine.length;
          totalAdds += adds;
          totalConflicts += conflicts;
          if (!adds && !conflicts && !onlyMineN) return '';
          const summary = [
            adds      ? `<span class="merge-tag merge-tag-add">+${adds} new</span>`     : '',
            conflicts ? `<span class="merge-tag merge-tag-conflict">${conflicts} differ</span>` : '',
            onlyMineN ? `<span class="merge-tag merge-tag-keep">${onlyMineN} only-yours</span>` : '',
          ].join(' ');
          const conflictRows = d.conflicts.map((c) => {
            const key = `${pd.pid}:${kind}:${c.id}`;
            const cur = choices[key] || 'mine';
            const label = c.mine.title || c.mine.name || c.theirs.title || c.theirs.name || c.id;
            const mAt = c.mine.__lastEditAt || c.mine.updatedAt || '—';
            const tAt = c.theirs.__lastEditAt || c.theirs.updatedAt || '—';
            return `<div class="merge-conflict" data-key="${escapeHTML(key)}">
              <div class="merge-conflict-title">${escapeHTML(label)}</div>
              <div class="merge-conflict-versions">
                <label class="merge-version ${cur === 'mine' ? 'sel' : ''}">
                  <input type="radio" name="${escapeHTML(key)}" value="mine" ${cur === 'mine' ? 'checked' : ''} />
                  <span class="merge-version-label">Yours <span class="merge-version-when">${escapeHTML(mAt)}</span></span>
                </label>
                <label class="merge-version ${cur === 'theirs' ? 'sel' : ''}">
                  <input type="radio" name="${escapeHTML(key)}" value="theirs" ${cur === 'theirs' ? 'checked' : ''} />
                  <span class="merge-version-label">Theirs <span class="merge-version-when">${escapeHTML(tAt)}</span></span>
                </label>
                <label class="merge-version ${cur === 'both' ? 'sel' : ''}">
                  <input type="radio" name="${escapeHTML(key)}" value="both" ${cur === 'both' ? 'checked' : ''} />
                  <span class="merge-version-label">Keep both</span>
                </label>
              </div>
            </div>`;
          }).join('');
          return `<div class="merge-block">
            <div class="merge-block-head">${kind} ${summary}</div>
            ${conflicts ? `<div class="merge-conflicts">${conflictRows}</div>` : ''}
          </div>`;
        }).filter(Boolean).join('');
        return `<div class="merge-section">
          <div class="merge-section-title">${escapeHTML(pd.name)}</div>
          ${blocks || '<div class="merge-row muted">No differences.</div>'}
        </div>`;
      }).join('') || '<div class="empty">No differences detected.</div>';

      $('#mSummary', overlay).textContent =
        `${totalAdds} additions · ${totalConflicts} conflicts`;

      // Wire change events for conflict + project radios
      body.querySelectorAll('input[type=radio]').forEach((rd) => {
        rd.addEventListener('change', () => {
          const name = rd.name;
          if (name.startsWith('proj-')) {
            const pid = name.slice(5);
            choices[`__proj__:${pid}`] = rd.value;
          } else {
            choices[name] = rd.value;
          }
        });
      });
    }
    renderMergeBody();

    $('#mApply', overlay).addEventListener('click', () => {
      // Apply choices to mine
      undoStack.push(JSON.stringify(state));
      const result = JSON.parse(JSON.stringify(mine));
      const resultProjMap = new Map(result.projects.map((p, i) => [p.id, i]));
      projDiffs.forEach((pd) => {
        if (pd.only === 'theirs') {
          const choice = choices[`__proj__:${pd.pid}`] || 'include';
          if (choice === 'include') {
            result.projects.push(JSON.parse(JSON.stringify(pd.proj)));
          }
          return;
        }
        if (pd.only === 'mine') return;
        const targetIdx = resultProjMap.get(pd.pid);
        if (targetIdx == null) return;
        const target = result.projects[targetIdx];
        Object.entries(pd.perKind).forEach(([kind, d]) => {
          // 1. Apply only-theirs additions for which the user kept default 'include'
          d.onlyTheirs.forEach((r) => {
            const key = `${pd.pid}:${kind}:${r.id}`;
            if ((choices[key] || 'include') !== 'include') return;
            target[kind] = target[kind] || [];
            target[kind].push(JSON.parse(JSON.stringify(r)));
          });
          // 2. Conflicts
          d.conflicts.forEach((c) => {
            const key = `${pd.pid}:${kind}:${c.id}`;
            const choice = choices[key] || 'mine';
            const list = target[kind] || (target[kind] = []);
            const idx = list.findIndex((x) => x.id === c.id);
            if (choice === 'theirs') {
              if (idx >= 0) list[idx] = JSON.parse(JSON.stringify(c.theirs));
            } else if (choice === 'both') {
              const dup = JSON.parse(JSON.stringify(c.theirs));
              const prefix = (c.theirs.id || 'id').split('_')[0] || 'id';
              dup.id = uid(prefix);
              if (dup.title) dup.title = dup.title + ' (theirs)';
              else if (dup.name) dup.name = dup.name + ' (theirs)';
              list.push(dup);
            }
            // 'mine' = no-op
          });
        });
      });
      state = normalizeState(result);
      saveState();
      render();
      close();
      toast('Merged');
    });
  }

  /* ------------------- Personal to-do (floating widget) ------------------ */

  // The widget lives at the state level (project-independent), so changes only
  // need a saveState() — no commit() (we don't want personal todos showing up
  // in the global undo stack, and we want quick local-only redraws).
  function refreshTodoFab() {
    const fab = $('#todoFab');
    const badge = $('#todoFabBadge');
    if (!fab) return;
    const todos = state.todos || [];
    const open = todos.filter((t) => !t.done).length;
    const total = todos.length;
    fab.classList.toggle('empty',  total === 0);
    fab.classList.toggle('all-done', total > 0 && open === 0);
    fab.classList.toggle('has-open', open > 0);
    if (badge) {
      if (open > 0) { badge.textContent = String(open); badge.hidden = false; }
      else { badge.hidden = true; }
    }
    fab.title = total === 0
      ? 'My to-do — empty'
      : (open === 0 ? `My to-do — ${total} done` : `My to-do — ${open} open of ${total}`);
  }

  function renderTodoList() {
    const list = $('#todoList');
    const meta = $('#todoMeta');
    if (!list) return;
    const todos = state.todos || [];
    const open = todos.filter((t) => !t.done).length;
    if (meta) meta.textContent = todos.length ? `${open}/${todos.length}` : '';
    if (!todos.length) {
      list.innerHTML = '<div class="todo-empty">No personal to-dos yet — add one above.</div>';
      return;
    }
    list.innerHTML = todos.map((t) => `
      <div class="todo-item ${t.done ? 'done' : ''}" data-todo-id="${t.id}">
        <span class="todo-grip" title="Drag to reorder" aria-hidden="true">⋮⋮</span>
        <button type="button" class="todo-check ${t.done ? 'on' : ''}" aria-label="${t.done ? 'Mark not done' : 'Mark done'}"></button>
        <span class="todo-text" contenteditable="true" data-placeholder="To-do…">${escapeHTML(t.text)}</span>
        <button type="button" class="todo-del" aria-label="Delete" title="Delete">×</button>
      </div>`).join('');

    // Wire each row
    list.querySelectorAll('.todo-item').forEach((row) => {
      const id = row.dataset.todoId;
      row.querySelector('.todo-check').addEventListener('click', () => {
        const t = (state.todos || []).find((x) => x.id === id);
        if (!t) return;
        t.done = !t.done;
        saveState();
        renderTodoList(); refreshTodoFab();
      });
      const text = row.querySelector('.todo-text');
      text.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); text.blur(); }
        else if (e.key === 'Escape') { e.preventDefault(); text.blur(); }
        else if (e.key === 'Backspace' && text.textContent === '') {
          e.preventDefault();
          state.todos = (state.todos || []).filter((x) => x.id !== id);
          saveState(); renderTodoList(); refreshTodoFab();
        }
      });
      text.addEventListener('blur', () => {
        const t = (state.todos || []).find((x) => x.id === id);
        if (!t) return;
        const v = text.textContent.trim();
        if (t.text !== v) { t.text = v; saveState(); refreshTodoFab(); }
      });
      row.querySelector('.todo-del').addEventListener('click', (e) => {
        e.stopPropagation();
        state.todos = (state.todos || []).filter((x) => x.id !== id);
        saveState(); renderTodoList(); refreshTodoFab();
      });
      // Drag-to-reorder via the grip — same custom-mouse pattern as op-steps
      row.querySelector('.todo-grip').addEventListener('mousedown', (e) => {
        e.preventDefault();
        const listEl = $('#todoList');
        row.classList.add('dragging');
        document.body.classList.add('is-todo-dragging');
        const onMove = (em) => {
          const sibs = [...listEl.querySelectorAll('.todo-item:not(.dragging)')];
          const after = sibs.find((sib) => {
            const r = sib.getBoundingClientRect();
            return em.clientY < r.top + r.height / 2;
          });
          if (after) listEl.insertBefore(row, after);
          else listEl.appendChild(row);
        };
        const onUp = () => {
          row.classList.remove('dragging');
          document.body.classList.remove('is-todo-dragging');
          window.removeEventListener('mousemove', onMove);
          window.removeEventListener('mouseup', onUp);
          const newOrder = [...listEl.querySelectorAll('.todo-item')].map((r) => r.dataset.todoId);
          const before = (state.todos || []).map((t) => t.id).join(',');
          state.todos = newOrder.map((tid) => (state.todos || []).find((x) => x.id === tid)).filter(Boolean);
          if (state.todos.map((t) => t.id).join(',') !== before) { saveState(); }
        };
        window.addEventListener('mousemove', onMove);
        window.addEventListener('mouseup', onUp);
      });
    });
  }

  function wireTodoWidget() {
    const fab = $('#todoFab');
    const panel = $('#todoPanel');
    const input = $('#todoInput');
    if (!fab || !panel) return;
    const open = () => {
      panel.hidden = false;
      renderTodoList();
      setTimeout(() => input?.focus(), 30);
    };
    const close = () => { panel.hidden = true; };
    const toggle = () => panel.hidden ? open() : close();
    fab.addEventListener('click', toggle);
    $('#todoClose').addEventListener('click', close);
    // Add via Enter in the input or click of +
    const addItem = () => {
      const v = (input.value || '').trim();
      if (!v) return;
      state.todos = state.todos || [];
      state.todos.push({ id: uid('td'), text: v, done: false });
      input.value = '';
      saveState();
      renderTodoList(); refreshTodoFab();
    };
    $('#todoAdd').addEventListener('click', addItem);
    input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') { e.preventDefault(); addItem(); }
      else if (e.key === 'Escape') { e.preventDefault(); close(); }
    });
    // Click outside to close — but ignore clicks inside the panel or the FAB
    document.addEventListener('mousedown', (e) => {
      if (panel.hidden) return;
      if (e.target.closest('#todoPanel') || e.target.closest('#todoFab')) return;
      close();
    });
    refreshTodoFab();
  }

  /* ----------------------- Phase D: Inbox + bell ---------------------- */
  // Aggregate items that need user attention. Each item has a stable id so
  // the user can dismiss it and the dismissal sticks across reloads.
  const STALE_DAYS = 14;
  const SOON_DAYS = 3;
  function inboxItems() {
    const today = todayISO();
    const dismissed = new Set(state.inbox?.dismissed || []);
    const out = [];
    state.projects.forEach((proj) => {
      // Late actions
      (proj.actions || []).forEach((a) => {
        if (a.archivedAt) return;
        if (isClosedStatus(a.status)) return;
        if (a.due && dayDiff(a.due, today) < 0) {
          out.push({
            id: 'late-action:' + a.id,
            kind: 'late', icon: '⏰', tone: 'bad',
            title: a.title,
            sub: `${proj.name} · ${personName(a.owner)} · ${Math.abs(dayDiff(a.due, today))}d late`,
            run: () => { state.currentProjectId = proj.id; state.currentView = 'board'; render(); setTimeout(() => openDrawer(a.id), 30); },
          });
        } else if (a.due && dayDiff(a.due, today) <= SOON_DAYS) {
          out.push({
            id: 'due-soon:' + a.id,
            kind: 'soon', icon: '⏳', tone: 'warn',
            title: a.title,
            sub: `${proj.name} · ${personName(a.owner)} · due in ${dayDiff(a.due, today)}d`,
            run: () => { state.currentProjectId = proj.id; state.currentView = 'board'; render(); setTimeout(() => openDrawer(a.id), 30); },
          });
        }
        // Stale (open + not updated in N days)
        if (!isClosedStatus(a.status) && a.updatedAt && dayDiff(today, a.updatedAt) >= STALE_DAYS) {
          out.push({
            id: 'stale-action:' + a.id,
            kind: 'stale', icon: '·', tone: 'muted',
            title: a.title,
            sub: `${proj.name} · ${personName(a.owner)} · untouched ${dayDiff(today, a.updatedAt)}d`,
            run: () => { state.currentProjectId = proj.id; state.currentView = 'board'; render(); setTimeout(() => openDrawer(a.id), 30); },
          });
        }
      });
      // Pending CRs > 14d
      (proj.changes || []).forEach((c) => {
        if (c.status !== 'proposed' && c.status !== 'under_review') return;
        if (c.originatedDate && dayDiff(today, c.originatedDate) >= STALE_DAYS) {
          out.push({
            id: 'cr-pending:' + c.id,
            kind: 'cr', icon: '⇆', tone: 'warn',
            title: c.title,
            sub: `${proj.name} · ${c.status.replace('_', ' ')} · ${dayDiff(today, c.originatedDate)}d ago`,
            run: () => { state.currentProjectId = proj.id; state.currentView = 'changes'; render(); setTimeout(() => openChangeRequestEditor(c.id), 30); },
          });
        }
      });
      // High-criticality unmitigated risks
      (proj.risks || []).forEach((r) => {
        if ((r.kind || 'risk') === 'opportunity') return;
        const inh = r.inherent || { probability: 0, impact: 0 };
        const res = r.residual || inh;
        const score = (res.probability || 0) * (res.impact || 0);
        if (score >= 12 && !r.actionId) {
          out.push({
            id: 'risk-unmit:' + r.id,
            kind: 'risk', icon: '△', tone: 'bad',
            title: r.title,
            sub: `${proj.name} · residual ${score} · no linked action`,
            run: () => { state.currentProjectId = proj.id; state.currentView = 'risks'; render(); setTimeout(() => openRiskEditor(r.id), 30); },
          });
        }
      });
    });
    // Personal todos due / open
    (state.todos || []).forEach((t) => {
      if (t.done) return;
      out.push({
        id: 'todo:' + t.id,
        kind: 'todo', icon: '✓', tone: 'muted',
        title: t.text || '(untitled)',
        sub: 'Personal to-do',
        run: () => $('#todoFab')?.click(),
      });
    });
    return out.filter((it) => !dismissed.has(it.id));
  }
  function inboxCount() { return inboxItems().length; }
  function refreshBell() {
    const bell = $('#bellBadge');
    if (!bell) return;
    const n = inboxCount();
    if (n > 0) { bell.textContent = String(n); bell.hidden = false; }
    else bell.hidden = true;
  }
  function dismissInbox(id) {
    state.inbox = state.inbox || { dismissed: [] };
    state.inbox.dismissed = state.inbox.dismissed || [];
    if (!state.inbox.dismissed.includes(id)) state.inbox.dismissed.push(id);
    saveState();
  }
  function renderInbox(root) {
    const view = document.createElement('div');
    view.className = 'view';
    const items = inboxItems();
    const grouped = { late: [], soon: [], cr: [], risk: [], stale: [], todo: [] };
    items.forEach((it) => { (grouped[it.kind] || (grouped[it.kind] = [])).push(it); });
    const sectionTitle = { late: 'Late', soon: 'Due soon', cr: 'CR aging', risk: 'Unmitigated risks', stale: 'Stale', todo: 'Personal to-do' };
    const order = ['late', 'soon', 'cr', 'risk', 'stale', 'todo'];
    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">Inbox</div>
          <div class="page-sub">${items.length ? `${items.length} item${items.length === 1 ? '' : 's'} need your attention` : 'Nothing requires action right now — nice work.'}</div>
        </div>
        <div class="page-actions">
          ${state.settings.notifyEnabled ? '' : '<button class="ghost" id="btnEnableNotify" title="Browser notifications when new items arrive">Enable notifications</button>'}
          ${items.length ? '<button class="ghost" id="btnDismissAll" title="Dismiss all">Dismiss all</button>' : ''}
        </div>
      </div>
      <div class="inbox">
        ${items.length === 0 ? '<div class="empty">Your inbox is clear.</div>' : ''}
        ${order.filter((k) => (grouped[k] || []).length).map((k) => `
          <div class="inbox-section">
            <div class="inbox-section-title">${escapeHTML(sectionTitle[k] || k)}<span class="inbox-section-count">${grouped[k].length}</span></div>
            <div class="inbox-list">
              ${grouped[k].map((it) => `
                <div class="inbox-item tone-${it.tone}" data-id="${escapeHTML(it.id)}">
                  <span class="inbox-icon">${escapeHTML(it.icon)}</span>
                  <div class="inbox-text">
                    <div class="inbox-title">${escapeHTML(it.title)}</div>
                    <div class="inbox-sub">${escapeHTML(it.sub)}</div>
                  </div>
                  <button class="inbox-dismiss" data-action="dismiss" title="Dismiss" aria-label="Dismiss">×</button>
                </div>
              `).join('')}
            </div>
          </div>
        `).join('')}
      </div>`;
    root.appendChild(view);

    view.querySelectorAll('.inbox-item').forEach((row) => {
      row.addEventListener('click', (e) => {
        if (e.target.closest('[data-action="dismiss"]')) return;
        const id = row.dataset.id;
        const it = inboxItems().find((x) => x.id === id);
        if (it) it.run();
      });
      row.querySelector('[data-action="dismiss"]')?.addEventListener('click', (e) => {
        e.stopPropagation();
        const id = row.dataset.id;
        dismissInbox(id);
        render();
      });
    });
    $('#btnDismissAll')?.addEventListener('click', () => {
      if (!confirm('Dismiss all inbox items? They\'ll re-appear if their condition still holds tomorrow.')) return;
      items.forEach((it) => dismissInbox(it.id));
      render();
    });
    $('#btnEnableNotify')?.addEventListener('click', async () => {
      try {
        const perm = await Notification.requestPermission();
        if (perm === 'granted') {
          state.settings.notifyEnabled = true;
          saveState(); render();
          toast('Notifications enabled');
        } else {
          toast('Notifications denied');
        }
      } catch (e) { toast('Notifications not supported'); }
    });
  }
  function maybeNotifyInbox() {
    if (!state.settings.notifyEnabled) return;
    if (typeof Notification === 'undefined' || Notification.permission !== 'granted') return;
    const today = todayISO();
    if (state.inbox?.lastNotifyDate === today) return;
    const items = inboxItems();
    if (!items.length) return;
    state.inbox = state.inbox || {};
    state.inbox.lastNotifyDate = today;
    saveState();
    try {
      new Notification('Cockpit — ' + items.length + ' item' + (items.length === 1 ? '' : 's') + ' need your attention', {
        body: items.slice(0, 3).map((it) => '• ' + it.title).join('\n'),
        silent: false,
      });
    } catch (e) { /* ignore */ }
  }

  /* ----------------------- Phase E: Status Report ---------------------- */
  // Compose a period-bounded status report from existing data, render as
  // a live HTML preview, then export as Markdown / Markdown file / Print.
  // Period state lives outside the rendered view so re-renders preserve it.
  const reportState = {
    period: '7d',          // '7d' | '30d' | '90d' | 'custom'
    customSince: '',
    customUntil: '',
  };
  function reportPeriodRange() {
    const today = todayISO();
    if (reportState.period === 'custom' && reportState.customSince && reportState.customUntil) {
      return { since: reportState.customSince, until: reportState.customUntil };
    }
    const days = reportState.period === '30d' ? 30 : reportState.period === '90d' ? 90 : 7;
    const since = fmtISO(new Date(Date.now() - days * dayMs));
    return { since, until: today };
  }
  function buildReportData(proj, since, until) {
    const acts = (proj.actions || []).filter((a) => !a.archivedAt);
    const today = todayISO();
    const horizon = fmtISO(new Date(parseDate(today).getTime() + 14 * dayMs));
    const inRange = (d) => d && d >= since && d <= until;

    const changed = acts.filter((a) => a.updatedAt && inRange(a.updatedAt))
      .sort((a, b) => (b.updatedAt || '').localeCompare(a.updatedAt || ''));
    const lateOrBlocked = acts.filter((a) =>
      !isClosedStatus(a.status) && ((a.due && dayDiff(a.due, today) < 0) || a.status === 'blocked'));
    const doneInPeriod = acts.filter((a) => a.status === 'done' && inRange(a.updatedAt));
    const decisions = (proj.decisions || []).filter((d) => inRange(d.date))
      .sort((a, b) => (b.date || '').localeCompare(a.date || ''));
    const crsDecided = (proj.changes || []).filter((c) =>
      ['approved', 'rejected', 'implemented', 'cancelled'].includes(c.status) && inRange(c.decisionDate))
      .sort((a, b) => (b.decisionDate || '').localeCompare(a.decisionDate || ''));
    const topRisks = (proj.risks || [])
      .filter((r) => (r.kind || 'risk') === 'risk')
      .map((r) => {
        const res = r.residual || r.inherent || { probability: 0, impact: 0 };
        return { ...r, _score: (res.probability || 0) * (res.impact || 0) };
      })
      .filter((r) => r._score > 0)
      .sort((a, b) => b._score - a._score)
      .slice(0, 5);
    const upcomingMilestones = (proj.milestones || []).filter((m) =>
      !m.done && m.date && m.date >= today && m.date <= horizon)
      .sort((a, b) => (a.date || '').localeCompare(b.date || ''));
    const upcomingDeliverables = (proj.deliverables || []).filter((d) =>
      !d.done && d.date && d.date >= today && d.date <= horizon)
      .sort((a, b) => (a.date || '').localeCompare(b.date || ''));
    const upcomingDue = acts.filter((a) =>
      !isClosedStatus(a.status) && a.due && a.due >= today && a.due <= horizon)
      .sort((a, b) => (a.due || '').localeCompare(b.due || ''));

    const k = {
      done: doneInPeriod.length,
      changed: changed.length,
      late: lateOrBlocked.filter((a) => a.due && dayDiff(a.due, today) < 0).length,
      blocked: lateOrBlocked.filter((a) => a.status === 'blocked').length,
      decisions: decisions.length,
      crs: crsDecided.length,
    };
    return {
      proj, since, until, today,
      kpis: k, changed, lateOrBlocked, doneInPeriod,
      decisions, crsDecided, topRisks,
      next: { milestones: upcomingMilestones, deliverables: upcomingDeliverables, actions: upcomingDue },
    };
  }
  function reportPeriodLabel() {
    const r = reportPeriodRange();
    return `${fmtFull(r.since)} – ${fmtFull(r.until)}`;
  }
  function reportToMarkdown(data) {
    const lines = [];
    const proj = data.proj;
    lines.push(`# ${proj.name} — Status report`);
    lines.push('');
    lines.push(`_${fmtFull(data.since)} – ${fmtFull(data.until)}_`);
    lines.push('');
    lines.push('## KPIs');
    lines.push('');
    lines.push(`| Done | Changed | Late | Blocked | Decisions | CRs decided |`);
    lines.push(`|---:|---:|---:|---:|---:|---:|`);
    lines.push(`| ${data.kpis.done} | ${data.kpis.changed} | ${data.kpis.late} | ${data.kpis.blocked} | ${data.kpis.decisions} | ${data.kpis.crs} |`);
    lines.push('');
    const section = (title, items, fmt) => {
      lines.push(`## ${title}`);
      lines.push('');
      if (!items.length) { lines.push('_None._'); lines.push(''); return; }
      items.forEach((it) => lines.push('- ' + fmt(it)));
      lines.push('');
    };
    section('What changed', data.changed.slice(0, 30), (a) =>
      `**${mdEscape(a.title)}** — ${mdEscape(personName(a.owner))} · ${a.status}${a.updatedAt ? ' · ' + a.updatedAt : ''}`);
    section('Late & blocked', data.lateOrBlocked, (a) => {
      const reason = a.status === 'blocked'
        ? 'blocked'
        : `${Math.abs(dayDiff(a.due, data.today))}d late`;
      return `**${mdEscape(a.title)}** — ${mdEscape(personName(a.owner))} · ${reason}`;
    });
    section('Decisions made', data.decisions, (d) =>
      `**${mdEscape(d.title)}** — ${mdEscape(personName(d.owner))} · ${d.date || '—'}${d.rationale ? '\n  > ' + mdEscape(d.rationale) : ''}`);
    section('Change requests decided', data.crsDecided, (c) =>
      `**${mdEscape(c.title)}** — ${c.status}${c.decisionDate ? ' · ' + c.decisionDate : ''}${c.decisionBy ? ' · ' + mdEscape(personName(c.decisionBy)) : ''}`);
    section('Top risks (by residual)', data.topRisks, (r) =>
      `**${mdEscape(r.title)}** — residual ${r._score}${r.mitigation ? ' · ' + mdEscape(r.mitigation) : ''}`);
    lines.push(`## What's next (next 14 days)`);
    lines.push('');
    if (!data.next.milestones.length && !data.next.deliverables.length && !data.next.actions.length) {
      lines.push('_Nothing scheduled in the next 14 days._');
    } else {
      if (data.next.milestones.length) {
        lines.push('**Milestones**');
        data.next.milestones.forEach((m) => lines.push(`- ${mdEscape(m.name || m.title || '')} · ${m.date}`));
        lines.push('');
      }
      if (data.next.deliverables.length) {
        lines.push('**Deliverables**');
        data.next.deliverables.forEach((d) => lines.push(`- ${mdEscape(d.name || d.title || '')} · ${d.date}`));
        lines.push('');
      }
      if (data.next.actions.length) {
        lines.push('**Due actions**');
        data.next.actions.slice(0, 30).forEach((a) =>
          lines.push(`- ${mdEscape(a.title)} — ${mdEscape(personName(a.owner))} · ${a.due}`));
      }
    }
    return lines.join('\n');
  }
  function reportToPrintHTML(data) {
    const k = data.kpis;
    const proj = data.proj;
    const list = (arr, fmt, empty) => arr.length
      ? '<ul>' + arr.map(fmt).join('') + '</ul>'
      : `<p class="empty">${empty}</p>`;
    const li = (a) => `<li><b>${escapeHTML(a.title)}</b> — ${escapeHTML(personName(a.owner))} · ${escapeHTML(a.status)}${a.updatedAt ? ' · ' + a.updatedAt : ''}</li>`;
    const liDue = (a) => {
      const reason = a.status === 'blocked' ? 'blocked' : `${Math.abs(dayDiff(a.due, data.today))}d late`;
      return `<li><b>${escapeHTML(a.title)}</b> — ${escapeHTML(personName(a.owner))} · ${reason}</li>`;
    };
    const liDec = (d) => `<li><b>${escapeHTML(d.title)}</b> — ${escapeHTML(personName(d.owner))} · ${d.date || '—'}${d.rationale ? '<br/><i>' + escapeHTML(d.rationale) + '</i>' : ''}</li>`;
    const liCR = (c) => `<li><b>${escapeHTML(c.title)}</b> — ${escapeHTML(c.status)}${c.decisionDate ? ' · ' + c.decisionDate : ''}${c.decisionBy ? ' · ' + escapeHTML(personName(c.decisionBy)) : ''}</li>`;
    const liRisk = (r) => `<li><b>${escapeHTML(r.title)}</b> — residual ${r._score}${r.mitigation ? '<br/><i>' + escapeHTML(r.mitigation) + '</i>' : ''}</li>`;
    const liNext = (it, dateField) => `<li><b>${escapeHTML(it.name || it.title || '')}</b> · ${escapeHTML(it[dateField] || '—')}</li>`;
    const liActDue = (a) => `<li><b>${escapeHTML(a.title)}</b> — ${escapeHTML(personName(a.owner))} · due ${a.due}</li>`;

    return `<!doctype html><html><head><meta charset="utf-8"><title>${escapeHTML(proj.name)} — Status report</title>
<style>
  body{font:14px -apple-system,Segoe UI,Helvetica,Arial,sans-serif;color:#1a1c24;max-width:820px;margin:30px auto;padding:0 20px;}
  h1{font-size:22px;margin:0 0 4px;} h2{font-size:15px;margin:22px 0 8px;border-bottom:1px solid #ddd;padding-bottom:4px;letter-spacing:.04em;text-transform:uppercase;color:#444;}
  .meta{color:#666;font-size:12px;margin-bottom:18px;}
  .kpis{display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin:14px 0;}
  .kpi{background:#f6f7fb;border:1px solid #e5e7ee;border-radius:8px;padding:10px;text-align:center;}
  .kpi b{font-size:22px;display:block;font-weight:700;}
  .kpi span{font-size:11px;color:#666;text-transform:uppercase;letter-spacing:.06em;}
  ul{padding-left:18px;margin:6px 0;} li{margin:5px 0;} .empty{color:#888;font-style:italic;margin:6px 0;}
  @media print { body { margin: 0 auto; } }
</style></head><body>
<h1>${escapeHTML(proj.name)} — Status report</h1>
<div class="meta">${fmtFull(data.since)} – ${fmtFull(data.until)} · generated ${fmtFull(data.today)}</div>
<div class="kpis">
  <div class="kpi"><b>${k.done}</b><span>Done</span></div>
  <div class="kpi"><b>${k.changed}</b><span>Changed</span></div>
  <div class="kpi"><b>${k.late}</b><span>Late</span></div>
  <div class="kpi"><b>${k.blocked}</b><span>Blocked</span></div>
  <div class="kpi"><b>${k.decisions}</b><span>Decisions</span></div>
  <div class="kpi"><b>${k.crs}</b><span>CRs decided</span></div>
</div>
<h2>What changed</h2>${list(data.changed.slice(0, 30), li, 'No updates in this period.')}
<h2>Late &amp; blocked</h2>${list(data.lateOrBlocked, liDue, 'Nothing late or blocked — nice work.')}
<h2>Decisions made</h2>${list(data.decisions, liDec, 'No decisions logged in this period.')}
<h2>Change requests decided</h2>${list(data.crsDecided, liCR, 'No CRs decided in this period.')}
<h2>Top risks (by residual)</h2>${list(data.topRisks, liRisk, 'No risks logged.')}
<h2>What's next (next 14 days)</h2>
${data.next.milestones.length ? '<p><b>Milestones</b></p>' + list(data.next.milestones, (m) => liNext(m, 'date'), '') : ''}
${data.next.deliverables.length ? '<p><b>Deliverables</b></p>' + list(data.next.deliverables, (d) => liNext(d, 'date'), '') : ''}
${data.next.actions.length ? '<p><b>Due actions</b></p>' + list(data.next.actions.slice(0, 30), liActDue, '') : ''}
${(!data.next.milestones.length && !data.next.deliverables.length && !data.next.actions.length) ? '<p class="empty">Nothing scheduled in the next 14 days.</p>' : ''}
</body></html>`;
  }
  // Reports is merged into Review — keep the function name for any
  // remaining callers (palette, stale state.currentView), but route to
  // the merged Review in 'full' mode so the old entry-point gives the
  // single-page snapshot users expected.
  function renderReports(root) {
    reviewModeState.mode = 'full';
    return renderReview(root);
  }
  function reportSection(title, items, fmt, empty) {
    return `
      <div class="report-section">
        <div class="report-section-title">${escapeHTML(title)}<span class="report-section-count">${items.length}</span></div>
        ${items.length ? items.map(fmt).join('') : `<div class="empty">${escapeHTML(empty)}</div>`}
      </div>`;
  }

  /* ----------------------- Phase H: Calendar view ---------------------- */
  // Month grid (7 cols × N rows) of every dated item. Each cell shows
  // colour-tinted chips per kind: action due, milestone, deliverable,
  // CR decided, meeting. Arrow keys navigate months. Clicking a chip
  // opens the relevant drawer / editor.
  const calState = {
    monthOffset: 0, // 0 = current month, -1 = previous, +1 = next
    keyWired: false,
    // 'month' = vertically-scrolling weekly grid with continuous reveal
    // (default); 'timeline' = horizontal swim-lane view.
    format: 'month',
    // Month-mode window state. firstWeekStart = Monday of the first
    // rendered week. weekCount = how many weeks are currently rendered.
    // windowAnchorOffset tracks the monthOffset the window was last
    // built for; mismatch means re-center.
    firstWeekStart: null,
    weekCount: 12,
    windowAnchorOffset: null,
    // Timeline-mode zoom — continuous (px-per-day). Tick granularity
    // is auto-derived from this value:
    //   pxPerDay ≥ 12 → 'day'    (one tick per day)
    //   pxPerDay ≥  3 → 'week'   (one tick per Monday)
    //   else          → 'month'  (one tick per 1st-of-month)
    tlPxPerDay: 26,
    // Per-kind visibility filters. The legend doubles as the filter
    // strip: clicking a pill toggles its kind on/off. Persists across
    // month navigation but resets on reload (intentional — fresh
    // sessions start with everything shown).
    visible: { action: true, deliverable: true, milestone: true, cr: true, meeting: true },
  };
  // ISO 8601 week number — Mon-anchored, week 1 is the one containing
  // the first Thursday of the year.
  function isoWeekNumber(d) {
    const dt = new Date(d.getFullYear(), d.getMonth(), d.getDate());
    dt.setDate(dt.getDate() + 3 - ((dt.getDay() + 6) % 7));
    const week1 = new Date(dt.getFullYear(), 0, 4);
    return 1 + Math.round(((dt.getTime() - week1.getTime()) / 86400000 - 3 + ((week1.getDay() + 6) % 7)) / 7);
  }
  function ensureCalendarWindow() {
    const today = new Date();
    if (calState.windowAnchorOffset !== calState.monthOffset || !calState.firstWeekStart) {
      const anchor = new Date(today.getFullYear(), today.getMonth() + calState.monthOffset, 1);
      const dow = (anchor.getDay() + 6) % 7; // Mon=0
      const monthGridStart = new Date(anchor.getFullYear(), anchor.getMonth(), 1 - dow);
      // Default: 4 weeks lead-in, 12 weeks total
      let firstWeekStart = new Date(monthGridStart.getTime() - 4 * 7 * dayMs);
      let weekCount = 12;

      // Auto-extend the window so every dated record in the project is
      // visible regardless of how far in the past or future it sits.
      // Covers milestones (start + endDate), deliverables (dueDate),
      // actions (due), and decided change requests (decisionDate).
      const proj = curProject();
      if (proj) {
        const dates = [];
        (proj.milestones   || []).forEach((m) => {
          if (m.date)    dates.push(parseDate(m.date).getTime());
          if (m.endDate) dates.push(parseDate(m.endDate).getTime());
        });
        (proj.deliverables || []).forEach((d) => {
          const dt = d.dueDate || d.date;
          if (dt) dates.push(parseDate(dt).getTime());
        });
        (proj.actions || []).forEach((a) => {
          if (!a.archivedAt && a.due) dates.push(parseDate(a.due).getTime());
        });
        (proj.changes || []).forEach((c) => {
          if (c.decisionDate) dates.push(parseDate(c.decisionDate).getTime());
        });
        if (dates.length) {
          const minMs = Math.min(...dates);
          const maxMs = Math.max(...dates);
          // Extend backward if any date is before the current window start
          const winStartMs = firstWeekStart.getTime();
          if (minMs < winStartMs) {
            let newStart = new Date(minMs);
            while (newStart.getDay() !== 1) newStart = new Date(newStart.getTime() - dayMs);
            newStart = new Date(newStart.getTime() - 7 * dayMs); // 1-week buffer
            const addedWeeks = Math.ceil((winStartMs - newStart.getTime()) / (7 * dayMs));
            firstWeekStart = newStart;
            weekCount += addedWeeks;
          }
          // Extend forward if any date is after the current window end
          const winEndMs = firstWeekStart.getTime() + weekCount * 7 * dayMs;
          if (maxMs > winEndMs) {
            const extraWeeks = Math.ceil((maxMs - winEndMs) / (7 * dayMs)) + 1;
            weekCount += extraWeeks;
          }
        }
      }

      calState.firstWeekStart = firstWeekStart;
      calState.weekCount = weekCount;
      calState.windowAnchorOffset = calState.monthOffset;
      calState.scrollToTodayPending = true;
    }
  }
  function calendarMonthBounds() {
    ensureCalendarWindow();
    const today = new Date();
    const anchor = new Date(today.getFullYear(), today.getMonth() + calState.monthOffset, 1);
    const year = anchor.getFullYear();
    const month = anchor.getMonth();
    const monthLabel = anchor.toLocaleDateString(undefined, { year: 'numeric', month: 'long' });
    const cells = [];
    for (let i = 0; i < calState.weekCount * 7; i++) {
      const d = new Date(calState.firstWeekStart);
      d.setDate(calState.firstWeekStart.getDate() + i);
      cells.push(d);
    }
    return { year, month, monthLabel, cells };
  }
  // Honour the topbar filters across every calendar item kind. Actions
  // accept the full set (owner / component / status / due bucket).
  // Other kinds (milestones, deliverables, change requests, meetings)
  // accept the subset their record carries: component is shared by all,
  // status by everything except meetings, and the owner filter only
  // matters for actions — the calendar is meant to also surface gates
  // and reviews regardless of owner.
  function calendarItemMatchesTopbar(it) {
    const rec = it._record;
    if (!rec) return true;
    if (it.kind === 'action') return actionMatchesFilters(rec);
    const fComp = $('#filterComponent')?.value || '';
    if (fComp) {
      const recComp = rec.component || '';
      if (fComp === '__none__' && recComp) return false;
      if (fComp !== '__none__' && recComp !== fComp) return false;
    }
    const fStatus = $('#filterStatus')?.value || '';
    if (fStatus && rec.status && rec.status !== fStatus) return false;
    const fDue = $('#filterDue')?.value || '';
    if (fDue && it.date) {
      const today = todayISO();
      const diff = dayDiff(it.date, today);
      if (fDue === 'late'  && !(diff <  0)) return false;
      if (fDue === 'week'  && !(diff >= 0 && diff <=  7)) return false;
      if (fDue === 'month' && !(diff >= 0 && diff <= 30)) return false;
    }
    return true;
  }

  function buildCalendarItems(proj, year, month, gridStartISO, gridEndISO) {
    const inRange = (d) => d && d >= gridStartISO && d <= gridEndISO;
    const items = []; // { date, kind, label, tone, run, icon, rangePos }
    const acts = (proj.actions || []).filter((a) => !a.archivedAt);
    acts.forEach((a) => {
      if (!a.due || !inRange(a.due)) return;
      const cmp = a.component ? findComponent(proj, a.component) : null;
      const cmpRgb = cmp ? componentColor(cmp.color)?.rgb : null;
      const subBase = personName(a.owner);
      items.push({
        date: a.due,
        kind: 'action',
        tone: a.status === 'done' ? 'muted' : (a.status === 'blocked' ? 'bad' : 'accent'),
        label: a.title,
        sub: cmp ? `${subBase} · ${cmp.name}` : subBase,
        // Checkbox glyph reflects status — filled box for done, empty
        // outline otherwise. Reads at-a-glance like a to-do checklist.
        icon: a.status === 'done' ? '☑' : '☐',
        tint: cmpRgb,
        _record: a,                  // exposed so the timeline can drag-mutate
        run: () => openDrawer(a.id),
      });
    });
    (proj.milestones || []).forEach((m) => {
      if (!m.date) return;
      // Ranged milestones expand into one chip per day in [date … endDate],
      // each tagged with its position (start / middle / end / single) so the
      // calendar can paint a connected band across cells.
      const start = m.date;
      const end = m.endDate || m.date;
      if (!inRange(start) && !inRange(end) && !(start <= gridStartISO && end >= gridEndISO)) return;
      const startT = parseDate(start).getTime();
      const endT   = parseDate(end).getTime();
      const isRange = endT > startT;
      const cmp = m.component ? findComponent(proj, m.component) : null;
      const cmpRgb = cmp ? componentColor(cmp.color)?.rgb : null;
      for (let t = startT; t <= endT; t += dayMs) {
        const iso = fmtISO(new Date(t));
        if (!inRange(iso)) continue;
        const isStart = iso === start;
        const isEnd   = iso === end;
        const rangePos = !isRange ? 'single' : isStart ? 'start' : isEnd ? 'end' : 'middle';
        const icon = !isRange ? '◇' : isStart ? '▷' : isEnd ? '▭' : '─';
        const baseSub = isRange
          ? (isStart ? `Milestone start · ends ${end}` : isEnd ? `Milestone end · started ${start}` : 'Milestone in progress')
          : 'Milestone';
        items.push({
          date: iso,
          kind: 'milestone',
          tone: 'milestone',
          label: m.name,
          sub: cmp ? `${baseSub} · ${cmp.name}` : baseSub,
          icon,
          rangePos,
          tint: cmpRgb,
          _record: m,
          run: () => openMilestoneEditor(m.id),
        });
      }
    });
    (proj.deliverables || []).forEach((d) => {
      const dt = d.dueDate || d.date;
      if (!dt || !inRange(dt)) return;
      const cmp = d.component ? findComponent(proj, d.component) : null;
      const cmpRgb = cmp ? componentColor(cmp.color)?.rgb : null;
      items.push({
        date: dt,
        kind: 'deliverable',
        tone: 'deliverable',
        label: d.name,
        sub: cmp ? `Deliverable · ${cmp.name}` : 'Deliverable',
        icon: '◆',
        tint: cmpRgb,
        _record: d,
        run: () => openDeliverableEditor(d.id),
      });
    });
    (proj.changes || []).forEach((c) => {
      if (!c.decisionDate || !inRange(c.decisionDate)) return;
      const decided = ['approved', 'rejected', 'implemented', 'cancelled'].includes(c.status);
      if (!decided) return;
      items.push({
        date: c.decisionDate,
        kind: 'cr',
        tone: c.status === 'rejected' || c.status === 'cancelled' ? 'bad' : 'good',
        label: c.title,
        sub: 'CR ' + c.status,
        icon: '⇆',
        run: () => openChangeRequestEditor(c.id),
      });
    });
    (proj.meetings || []).forEach((m) => {
      const dates = expandMeetingDates(m, gridStartISO, gridEndISO);
      if (!dates.length) return;
      const baseLabel = meetingRecurrenceLabel(m);
      const cmp = m.component ? findComponent(proj, m.component) : null;
      const cmpRgb = cmp ? componentColor(cmp.color)?.rgb : null;
      const subBase = m.time ? `${baseLabel} · ${m.time}` : baseLabel;
      const subWithComponent = cmp ? `${subBase} · ${cmp.name}` : subBase;
      dates.forEach((iso) => {
        items.push({
          date: iso,
          kind: 'meeting',
          tone: 'meeting',
          label: m.title,
          sub: subWithComponent,
          icon: '⊕',
          // When a component is set, the chip's icon + label pick up the
          // component's colour so meetings cluster visually with their
          // related work area. Stored on the item so the chip renderer
          // can apply it inline alongside the existing tone class.
          tint: cmpRgb,
          run: () => openMeetingEditor(m.id),
        });
      });
    });
    return items;
  }
  // Timeline format. A graduated arrow axis on top, swim-lanes underneath
  // (one per kind), events packed greedily into sub-rows per lane so two
  // overlapping events never occlude each other. The zoom tier strictly
  // controls tick granularity (Day / Week / Month — never intermediates)
  // and the pixel-per-day scale.
  // Tick-granularity thresholds. Tuned so each visible tick has at least
  // ~24 px of horizontal room — readable but not crowded.
  const TL_ZOOM_MIN = 0.5;
  const TL_ZOOM_MAX = 80;
  function tlGranularityFor(pxPerDay) {
    if (pxPerDay >= 12) return 'day';
    if (pxPerDay >= 3)  return 'week';
    return 'month';
  }
  function tlWindowDaysFor(pxPerDay) {
    // Render enough days to keep ~3000 px of total stage width regardless
    // of zoom, with a minimum of ~12 weeks so the user always has context
    // around the anchor month.
    return Math.max(84, Math.ceil(3000 / Math.max(0.1, pxPerDay)));
  }
  // Table format: sortable list of every dated item across a generous
  // window. Same data as the Calendar / Timeline views (going through
  // buildCalendarItems), same kind filters via the legend pills, same
  // click-through opening the relevant editor — just as a flat table
  // ordered by date so users can scan upcoming work at a glance.
  const tableState = {
    sortBy: 'date',     // 'date' | 'kind' | 'title' | 'sub'
    sortDir: 'asc',     // 'asc' | 'desc'
  };
  function renderCalendarTable() {
    const proj = curProject();
    const todayD = new Date();
    const todayISO_ = todayISO();
    const anchor = new Date(todayD.getFullYear(), todayD.getMonth() + calState.monthOffset, 1);

    // Window covers every dated record in the project so no row is
    // hidden by an arbitrary period cap. Falls back to anchor ± 6
    // months when the project has nothing dated yet.
    const dates = [];
    (proj.milestones   || []).forEach((m) => {
      if (m.date)    dates.push(parseDate(m.date).getTime());
      if (m.endDate) dates.push(parseDate(m.endDate).getTime());
    });
    (proj.deliverables || []).forEach((d) => {
      const dt = d.dueDate || d.date;
      if (dt) dates.push(parseDate(dt).getTime());
    });
    (proj.actions || []).forEach((a) => {
      if (!a.archivedAt && a.due) dates.push(parseDate(a.due).getTime());
    });
    (proj.changes || []).forEach((c) => {
      if (c.decisionDate) dates.push(parseDate(c.decisionDate).getTime());
    });
    let startDate, endDate;
    if (dates.length) {
      startDate = new Date(Math.min(...dates) - 30 * dayMs);
      endDate   = new Date(Math.max(...dates) + 30 * dayMs);
    } else {
      startDate = new Date(anchor.getFullYear(), anchor.getMonth() - 6, 1);
      endDate   = new Date(anchor.getFullYear(), anchor.getMonth() + 7, 0);
    }
    const startISO  = fmtISO(startDate);
    const endISO    = fmtISO(endDate);

    const allItems = buildCalendarItems(proj, anchor.getFullYear(), anchor.getMonth(), startISO, endISO);
    const filtered = allItems
      .filter((it) => calState.visible[it.kind] !== false)
      .filter((it) => matchesSearch(it.label, it.sub))
      .filter((it) => calendarItemMatchesTopbar(it));
    // Drop range middle/end (we'll show the start row with the range
    // length appended in the date column).
    const items = filtered.filter((it) => !(it.kind === 'milestone' && (it.rangePos === 'middle' || it.rangePos === 'end')));

    // Per-kind label + icon for the Kind column
    const kindLabels = { milestone: 'Milestone', deliverable: 'Deliverable', action: 'Action', meeting: 'Meeting', cr: 'Change request' };

    // For range milestones, find the matching 'end' position so we can
    // surface 'May 1 → May 4' in the Date column.
    function endIsoFor(it) {
      if (it.kind === 'milestone' && it.rangePos === 'start') {
        const same = filtered.filter((x) => x.kind === 'milestone' && x.label === it.label && x.date >= it.date);
        return same.reduce((mx, x) => x.date > mx ? x.date : mx, it.date);
      }
      return null;
    }
    function dateLabelOf(it) {
      const end = endIsoFor(it);
      const fmt = { month: 'short', day: 'numeric', year: 'numeric' };
      const base = parseDate(it.date).toLocaleDateString(undefined, fmt);
      if (end && end !== it.date) {
        const e = parseDate(end).toLocaleDateString(undefined, fmt);
        return `${base} → ${e}`;
      }
      return base;
    }

    // Component label (for the Kind column subtitle when present)
    function componentName(it) {
      // The chip's tint is derived from item.component → lookup name.
      // We don't have a direct id reference on `it`, but `it.sub` already
      // includes the component name when set (set in buildCalendarItems).
      return null;
    }

    // Sorting
    const dir = tableState.sortDir === 'asc' ? 1 : -1;
    const cmp = (a, b) => {
      const get = (it) => {
        switch (tableState.sortBy) {
          case 'date':  return it.date;
          case 'kind':  return it.kind;
          case 'title': return (it.label || '').toLowerCase();
          case 'sub':   return (it.sub || '').toLowerCase();
          default:      return it.date;
        }
      };
      const av = get(a), bv = get(b);
      if (av < bv) return -1 * dir;
      if (av > bv) return  1 * dir;
      return 0;
    };
    items.sort(cmp);

    // Run-callback resolution map (mirrors timeline)
    calState._tlRunMap = new Map();
    let _ctr = 0;
    function runKey(it) { const k = 'tbl-' + (++_ctr); calState._tlRunMap.set(k, it.run); return k; }

    const sortIcon = (col) => tableState.sortBy === col
      ? (tableState.sortDir === 'asc' ? ' ▲' : ' ▼')
      : '';

    const rowsHTML = items.map((it) => {
      const past   = it.date < todayISO_;
      const today  = it.date === todayISO_;
      const tintStyle = it.tint ? `--cal-chip-tint:${it.tint};` : '';
      const tintCls   = it.tint ? ' has-tint' : '';
      const dateCls   = today ? 'is-today' : (past ? 'is-past' : '');
      return `
        <tr class="cal-tbl-row kind-${it.kind}${tintCls}" data-tl-run-key="${runKey(it)}" style="${tintStyle}">
          <td class="cal-tbl-date ${dateCls}">${escapeHTML(dateLabelOf(it))}</td>
          <td class="cal-tbl-kind">
            <span class="cal-chip-icon kind-${it.kind}">${escapeHTML(it.icon || '·')}</span>
            <span class="cal-tbl-kind-name">${escapeHTML(kindLabels[it.kind] || it.kind)}</span>
          </td>
          <td class="cal-tbl-title">${escapeHTML(it.label || '')}</td>
          <td class="cal-tbl-sub">${escapeHTML(it.sub || '')}</td>
        </tr>`;
    }).join('');

    const empty = items.length ? '' : `<div class="empty">No events in the visible window — toggle a filter or scroll to a different month.</div>`;

    return `
      <div class="cal-table-wrap">
        ${empty}
        ${items.length ? `
          <table class="cal-table">
            <thead>
              <tr>
                <th class="cal-tbl-date sortable" data-tbl-sort="date">Date${sortIcon('date')}</th>
                <th class="cal-tbl-kind sortable" data-tbl-sort="kind">Kind${sortIcon('kind')}</th>
                <th class="cal-tbl-title sortable" data-tbl-sort="title">Title${sortIcon('title')}</th>
                <th class="cal-tbl-sub sortable" data-tbl-sort="sub">Detail${sortIcon('sub')}</th>
              </tr>
            </thead>
            <tbody>${rowsHTML}</tbody>
          </table>` : ''}
      </div>`;
  }

  function renderCalendarTimelineV2() {
    const proj = curProject();
    const todayISO_ = todayISO();
    const todayD = parseDate(todayISO_);
    const pxPerDay = clamp(calState.tlPxPerDay || 26, TL_ZOOM_MIN, TL_ZOOM_MAX);
    const granularity = tlGranularityFor(pxPerDay);
    let windowDays = tlWindowDaysFor(pxPerDay);

    // Anchor near a Monday so axis ticks land cleanly. Lead the window
    // with ~30% behind the anchor month so a bit of history is visible.
    const anchor = new Date(todayD.getFullYear(), todayD.getMonth() + calState.monthOffset, 1);
    let windowStart = new Date(anchor.getTime() - Math.floor(windowDays * 0.3) * dayMs);
    while (windowStart.getDay() !== 1) windowStart = new Date(windowStart.getTime() - dayMs);

    // Extend to cover every dated record (milestone / deliverable /
    // action / decided CR) so nothing falls outside the timeline.
    const allDates = [];
    (proj.milestones   || []).forEach((m) => {
      if (m.date)    allDates.push(parseDate(m.date).getTime());
      if (m.endDate) allDates.push(parseDate(m.endDate).getTime());
    });
    (proj.deliverables || []).forEach((d) => {
      const dt = d.dueDate || d.date;
      if (dt) allDates.push(parseDate(dt).getTime());
    });
    (proj.actions || []).forEach((a) => {
      if (!a.archivedAt && a.due) allDates.push(parseDate(a.due).getTime());
    });
    (proj.changes || []).forEach((c) => {
      if (c.decisionDate) allDates.push(parseDate(c.decisionDate).getTime());
    });
    if (allDates.length) {
      const minMs = Math.min(...allDates);
      const maxMs = Math.max(...allDates);
      // Extend windowStart back to a Monday before/at the earliest date
      if (minMs < windowStart.getTime()) {
        let newStart = new Date(minMs);
        while (newStart.getDay() !== 1) newStart = new Date(newStart.getTime() - dayMs);
        newStart = new Date(newStart.getTime() - 7 * dayMs); // 1-week buffer
        windowStart = newStart;
      }
      // Extend windowDays so the latest date is within the window
      const winEndMs = windowStart.getTime() + windowDays * dayMs;
      if (maxMs > winEndMs) {
        windowDays += Math.ceil((maxMs - winEndMs) / dayMs) + 7;
      }
    }

    const totalDays = windowDays;
    const windowEnd = new Date(windowStart.getTime() + (totalDays - 1) * dayMs);
    const totalPx = Math.round(totalDays * pxPerDay);
    const startISO = fmtISO(windowStart);
    const endISO   = fmtISO(windowEnd);
    const xFor = (iso) => Math.round((parseDate(iso).getTime() - windowStart.getTime()) / dayMs * pxPerDay);

    // Build items for the visible window
    const allItems = buildCalendarItems(proj, anchor.getFullYear(), anchor.getMonth(), startISO, endISO);
    const filtered = allItems
      .filter((it) => calState.visible[it.kind] !== false)
      .filter((it) => matchesSearch(it.label, it.sub))
      .filter((it) => calendarItemMatchesTopbar(it));
    // Drop milestone middle/end positions — the start chip's width covers
    // the range as one continuous bar.
    const items = filtered.filter((it) => !(it.kind === 'milestone' && (it.rangePos === 'middle' || it.rangePos === 'end')));

    function endIsoFor(it) {
      if (it.kind === 'milestone' && it.rangePos === 'start') {
        const same = filtered.filter((x) => x.kind === 'milestone' && x.label === it.label && x.date >= it.date);
        return same.reduce((mx, x) => x.date > mx ? x.date : mx, it.date);
      }
      return it.date;
    }

    // ---- Layout: split events into RANGE bars (sit on the axis) and POINT
    // markers (cards alternating above/below the axis with a stem). ----
    const CARD_W_HINT = 150;     // typical card width — used for collision packing
    const CARD_H = 36;           // card body height
    const STEM_H = 16;           // stem from card to axis
    const CARD_GAP = 6;          // vertical gap between rows of cards on same side
    const ROW_H = CARD_H + STEM_H + CARD_GAP;
    const RANGE_H = 22;          // a range bar's height
    const RANGE_GAP = 4;
    const AXIS_GAP = 14;         // breathing room around the axis line
    const AXIS_LABEL_H = 22;     // tick-label area below the axis line

    const ranges = [];
    const points = [];
    items.forEach((it) => {
      const startIso = it.date;
      const endIso   = endIsoFor(it);
      const left  = xFor(startIso);
      const isRange = it.kind === 'milestone' && it.rangePos === 'start' && endIso > startIso;
      if (isRange) {
        const span = (parseDate(endIso).getTime() - parseDate(startIso).getTime()) / dayMs + 1;
        const width = Math.max(40, Math.round(span * pxPerDay));
        ranges.push({ ...it, _start: startIso, _end: endIso, _left: left, _width: width });
      } else {
        // Point card: width is the typical card width (used for overlap
        // detection only; actual chip width is set by content).
        points.push({ ...it, _start: startIso, _end: endIso, _left: left, _width: CARD_W_HINT });
      }
    });

    // Pack ranges into rows above the axis (greedy, by left).
    ranges.sort((a, b) => a._left - b._left);
    const rangeRowMaxRight = [];
    ranges.forEach((it) => {
      const right = it._left + it._width;
      let placed = -1;
      for (let i = 0; i < rangeRowMaxRight.length; i++) {
        if (rangeRowMaxRight[i] + 4 <= it._left) { rangeRowMaxRight[i] = right; placed = i; break; }
      }
      if (placed < 0) { placed = rangeRowMaxRight.length; rangeRowMaxRight.push(right); }
      it._row = placed;
    });
    const rangeRowCount = rangeRowMaxRight.length;

    // Pack points alternating above/below the axis. Each side has its own
    // greedy row stack; events alternate sides on insertion (sorted by left)
    // so adjacent overlapping events naturally split between top + bottom.
    points.sort((a, b) => a._left - b._left);
    const aboveRowMaxRight = [];
    const belowRowMaxRight = [];
    let nextSide = 'above';
    points.forEach((it) => {
      const rows = nextSide === 'above' ? aboveRowMaxRight : belowRowMaxRight;
      const right = it._left + it._width;
      let placed = -1;
      for (let i = 0; i < rows.length; i++) {
        if (rows[i] + 6 <= it._left) { rows[i] = right; placed = i; break; }
      }
      if (placed < 0) { placed = rows.length; rows.push(right); }
      it._side = nextSide;
      it._row = placed;
      nextSide = (nextSide === 'above' ? 'below' : 'above');
    });
    const aboveRowCount = aboveRowMaxRight.length;
    const belowRowCount = belowRowMaxRight.length;

    // ---- Vertical layout coordinates ----
    const aboveBlockH = aboveRowCount * ROW_H + (aboveRowCount ? AXIS_GAP : 0);
    const rangeBlockH = rangeRowCount * (RANGE_H + RANGE_GAP);
    const axisLineY = aboveBlockH + rangeBlockH + AXIS_GAP / 2;
    const axisBlockH = AXIS_GAP + AXIS_LABEL_H;
    const belowBlockH = belowRowCount * ROW_H + (belowRowCount ? AXIS_GAP : 0);
    const stageH = Math.max(160, aboveBlockH + rangeBlockH + axisBlockH + belowBlockH);

    // Card top for a packed event:
    //   above: row 0 sits closest to the axis (just above the range bars).
    //   below: row 0 sits closest to the axis (just under the labels).
    function cardTopAbove(row) {
      // axis at axisLineY; cards above it. row 0 closest.
      return aboveBlockH - (row + 1) * ROW_H + CARD_GAP;
    }
    function cardTopBelow(row) {
      return aboveBlockH + rangeBlockH + axisBlockH + row * ROW_H;
    }

    // Tick generation — strict per-zoom granularity (Day / Week / Month),
    // auto-derived from the current pxPerDay.
    const ticks = [];
    if (granularity === 'day') {
      for (let d = 0; d < totalDays; d++) {
        const dt = new Date(windowStart.getTime() + d * dayMs);
        const x = Math.round(d * pxPerDay);
        const isMon = dt.getDay() === 1;
        const isFirst = dt.getDate() === 1;
        const label = isFirst
          ? dt.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })
          : isMon ? String(dt.getDate()) : '';
        ticks.push({ x, label, major: isMon || isFirst });
      }
    } else if (granularity === 'week') {
      let d = new Date(windowStart);
      while (d <= windowEnd) {
        const offset = Math.round((d.getTime() - windowStart.getTime()) / dayMs);
        const x = Math.round(offset * pxPerDay);
        const isFirst = d.getDate() <= 7;
        const label = isFirst
          ? d.toLocaleDateString(undefined, { month: 'short' })
          : `W${isoWeekNumber(d)}`;
        ticks.push({ x, label, major: isFirst });
        d = new Date(d.getTime() + 7 * dayMs);
      }
    } else {
      let cur = new Date(windowStart.getFullYear(), windowStart.getMonth(), 1);
      if (cur < windowStart) cur = new Date(cur.getFullYear(), cur.getMonth() + 1, 1);
      while (cur <= windowEnd) {
        const offset = Math.round((cur.getTime() - windowStart.getTime()) / dayMs);
        const x = Math.round(offset * pxPerDay);
        const isJan = cur.getMonth() === 0;
        const label = isJan
          ? cur.toLocaleDateString(undefined, { month: 'short', year: 'numeric' })
          : cur.toLocaleDateString(undefined, { month: 'short' });
        ticks.push({ x, label, major: isJan });
        cur = new Date(cur.getFullYear(), cur.getMonth() + 1, 1);
      }
    }

    const todayX = (todayISO_ >= startISO && todayISO_ <= endISO) ? xFor(todayISO_) : null;

    // Run-callback resolution + drag-mutation pointer per chip. Each
    // entry holds { run, drag } where drag (when present) carries
    // { kind, record } so the timeline drag handler can mutate the
    // source record directly without going through the editor.
    calState._tlRunMap = new Map();
    // Stash window/zoom for the hover marker + drag math
    calState._tlWindowStartMs = windowStart.getTime();
    let _tlRunCounter = 0;
    function runKey(it) {
      const k = 'tl-' + (++_tlRunCounter);
      const drag = (it.kind === 'action' || it.kind === 'deliverable' || it.kind === 'milestone') && it._record
        ? { kind: it.kind, record: it._record }
        : null;
      calState._tlRunMap.set(k, { run: it.run, drag });
      return k;
    }

    // ---- SVG axis: arrow line + ticks + labels ----
    const axisOvershoot = 14;
    const axisW = totalPx + axisOvershoot;
    const axisSVG = `
      <svg class="tl-axis-svg" width="${axisW}" height="${axisBlockH}" viewBox="0 0 ${axisW} ${axisBlockH}" preserveAspectRatio="none" aria-hidden="true">
        <line x1="0" y1="${AXIS_GAP / 2}" x2="${totalPx}" y2="${AXIS_GAP / 2}" class="tl-axis-line"/>
        <polygon points="${totalPx},${AXIS_GAP / 2 - 5} ${axisW},${AXIS_GAP / 2} ${totalPx},${AXIS_GAP / 2 + 5}" class="tl-axis-arrow"/>
        ${ticks.map((t) => `
          <line x1="${t.x}" y1="${t.major ? AXIS_GAP / 2 - 6 : AXIS_GAP / 2 - 3}" x2="${t.x}" y2="${t.major ? AXIS_GAP / 2 + 6 : AXIS_GAP / 2 + 3}" class="tl-axis-tick ${t.major ? 'major' : ''}"/>
          ${t.label ? `<text x="${t.x + 3}" y="${AXIS_GAP + 12}" class="tl-axis-label ${t.major ? 'major' : ''}">${escapeHTML(t.label)}</text>` : ''}
        `).join('')}
      </svg>`;

    // ---- Range bars (phase-arrow style on the axis) ----
    const rangeBarsHTML = ranges.map((it) => {
      const top = aboveBlockH + it._row * (RANGE_H + RANGE_GAP);
      const tintStyle = it.tint ? `--cal-chip-tint:${it.tint};` : '';
      const tintCls = it.tint ? ' has-tint' : '';
      return `
        <button class="tl-range-bar kind-${it.kind}${tintCls}"
                style="left:${it._left}px;top:${top}px;width:${it._width}px;height:${RANGE_H}px;${tintStyle}"
                data-tl-run-key="${runKey(it)}"
                title="${escapeHTML(it.label)} — ${escapeHTML(it.sub || '')}">
          <span class="tl-range-icon">${escapeHTML(it.icon || '◇')}</span>
          <span class="tl-range-label">${escapeHTML(it.label)}</span>
        </button>`;
    }).join('');

    // ---- Point markers (above/below alternating) ----
    function markerHTML(it) {
      const isAbove = it._side === 'above';
      const cardTop = isAbove ? cardTopAbove(it._row) : cardTopBelow(it._row);
      // Stem: from the card to the axis line
      const stemTop    = isAbove ? cardTop + CARD_H : axisLineY;
      const stemHeight = isAbove ? (axisLineY - (cardTop + CARD_H)) : (cardTop - axisLineY);
      const dotTop = axisLineY - 4; // dot 8px tall, centered on axis
      const tintStyle = it.tint ? `--cal-chip-tint:${it.tint};` : '';
      const tintCls = it.tint ? ' has-tint' : '';
      const sideCls = isAbove ? ' is-above' : ' is-below';
      // Date label format for the card sub
      const dateLabel = (() => {
        try { return parseDate(it.date).toLocaleDateString(undefined, { month: 'short', day: 'numeric' }); } catch (e) { return it.date || ''; }
      })();
      return `
        <button class="tl-marker kind-${it.kind}${tintCls}${sideCls}"
                style="left:${it._left}px;${tintStyle}"
                data-tl-run-key="${runKey(it)}"
                title="${escapeHTML(it.label)} — ${escapeHTML(it.sub || '')}">
          <span class="tl-marker-card" style="top:${cardTop}px;">
            <span class="tl-marker-icon">${escapeHTML(it.icon || '·')}</span>
            <span class="tl-marker-text">
              <span class="tl-marker-label">${escapeHTML(it.label)}</span>
              <span class="tl-marker-date">${escapeHTML(dateLabel)}</span>
            </span>
          </span>
          <span class="tl-marker-stem" style="top:${stemTop}px;height:${Math.max(0, stemHeight)}px;"></span>
          <span class="tl-marker-dot" style="top:${dotTop}px;"></span>
        </button>`;
    }
    const pointsHTML = points.map(markerHTML).join('');

    // ---- Today vertical line ----
    const todayLineHTML = todayX != null
      ? `<div class="tl-today-line" style="left:${todayX}px;height:${stageH}px;"></div>`
      : '';

    // ---- Empty hint ----
    const emptyHTML = (!ranges.length && !points.length)
      ? `<div class="tl-empty">No events in the visible window — scroll, drag, or zoom out to find some.</div>`
      : '';

    return `
      <div class="cal-timeline">
        <div class="tl-scroll">
          <div class="tl-stage" style="width:${axisW}px;height:${stageH}px;">
            ${todayLineHTML}
            ${rangeBarsHTML}
            <div class="tl-axis-host" style="top:${aboveBlockH + rangeBlockH}px;width:${axisW}px;">
              ${axisSVG}
            </div>
            ${pointsHTML}
            ${emptyHTML}
          </div>
        </div>
      </div>`;
  }
  // Old timeline (kept temporarily for reference; renderCalendar now uses v2)
  function renderCalendarTimeline(cells, items, byDate, todayISO_) {
    const startMs = cells[0].getTime();
    const totalDays = cells.length;
    const dayPx = 30; // column width per day
    const totalPx = totalDays * dayPx;

    // Build per-kind lanes from the (already-filter-aware) flat items list,
    // skipping milestone middle/end positions — they're absorbed into the
    // start chip's wider bar so the range reads as one continuous element
    // rather than 4 separate chips.
    const laneOrder = ['milestone', 'deliverable', 'action', 'meeting', 'cr'];
    const laneLabels = { milestone: 'Milestones', deliverable: 'Deliverables', action: 'Actions', meeting: 'Meetings', cr: 'CRs' };
    const laneIcons  = { milestone: '◇', deliverable: '◆', action: '☐', meeting: '⊕', cr: '⇆' };
    const lanes = laneOrder
      .filter((k) => calState.visible[k] !== false)
      .map((k) => ({ kind: k, items: [] }));
    const laneByKind = Object.fromEntries(lanes.map((l) => [l.kind, l]));

    // Track which (item.key) we've already added so a milestone band's
    // start chip is the only one we keep (its width covers the range).
    const seenMilestoneIds = new Set();
    items.forEach((it) => {
      const lane = laneByKind[it.kind];
      if (!lane) return;
      // Range-aware milestone packing: only the 'start' / 'single' positions
      // create a chip; middle/end positions are skipped because the start
      // chip's width spans the range.
      if (it.kind === 'milestone' && it.rangePos && (it.rangePos === 'middle' || it.rangePos === 'end')) {
        return;
      }
      lane.items.push(it);
    });

    // Index helper (offset days from the grid start)
    const offsetDays = (iso) => Math.round((parseDate(iso).getTime() - startMs) / dayMs);

    // Today indicator x-position
    const todayOffset = todayISO_ >= fmtISO(cells[0]) && todayISO_ <= fmtISO(cells[cells.length - 1])
      ? offsetDays(todayISO_) * dayPx + dayPx / 2 : null;

    // Axis: render a tick + label every 7 days, plus highlight Mondays.
    // Labels show 'Mon Apr 27' / 'May 4' etc; abbreviated to fit 30px column.
    const axisCells = cells.map((d, i) => {
      const iso = fmtISO(d);
      const isToday = iso === todayISO_;
      const isMon = d.getDay() === 1;
      const showLabel = isMon || i === 0;
      const lbl = showLabel
        ? (d.getDate() === 1 || i === 0
            ? d.toLocaleDateString(undefined, { month: 'short', day: 'numeric' })
            : String(d.getDate()))
        : '';
      return `<div class="tl-axis-cell ${isToday ? 'is-today' : ''} ${isMon ? 'is-mon' : ''}" data-iso="${iso}" style="width:${dayPx}px;">${lbl}</div>`;
    }).join('');

    // For each item we need to know its index in byDate.get(iso) so chip
    // clicks can resolve back to the run() callback. We re-derive that here.
    function indexInDateBucket(it) {
      const bucket = byDate.get(it.date) || [];
      return bucket.indexOf(it);
    }

    // Render lanes. Each chip is absolutely-positioned by left/width.
    // Range milestones get width = (endOffset - startOffset + 1) * dayPx;
    // everything else gets width = dayPx.
    function renderLane(lane) {
      const chipsHTML = lane.items.map((it) => {
        const idx = indexInDateBucket(it);
        const x = offsetDays(it.date) * dayPx;
        // Compute width for milestone ranges; use the underlying record so
        // we can read endDate (item itself only carries rangePos+date).
        let w = dayPx - 2;
        if (it.kind === 'milestone' && it.rangePos === 'start') {
          // Find the matching 'end' chip to derive width
          const startD = it.date;
          // The day-bucketed items include 'end' positions; locate the same
          // milestone label across the whole `items` array.
          const same = items.filter((x) => x.kind === 'milestone' && x.label === it.label);
          const lastIso = same.reduce((mx, x) => x.date > mx ? x.date : mx, startD);
          const span = offsetDays(lastIso) - offsetDays(startD) + 1;
          w = Math.max(dayPx - 2, span * dayPx - 2);
        }
        const tintStyle = it.tint ? `--cal-chip-tint: ${it.tint};` : '';
        const tintCls = it.tint ? ' has-tint' : '';
        const rangeCls = it.rangePos ? ` is-${it.rangePos}` : '';
        return `
          <button class="tl-chip cal-chip cal-chip-${it.tone} kind-${it.kind}${rangeCls}${tintCls}"
                  style="left:${x}px;width:${w}px;${tintStyle}"
                  data-tl-item-key="${escapeHTML(it.date)}|${idx}"
                  title="${escapeHTML(it.label)} — ${escapeHTML(it.sub || '')}">
            <span class="cal-chip-icon" aria-hidden="true">${escapeHTML(it.icon || '·')}</span>
            <span class="cal-chip-label">${escapeHTML(it.label)}</span>
          </button>`;
      }).join('');
      const empty = !lane.items.length ? '<div class="tl-lane-empty">— none</div>' : '';
      return `
        <div class="tl-lane">
          <div class="tl-lane-label">
            <span class="cal-chip-icon kind-${lane.kind}">${laneIcons[lane.kind]}</span>
            ${escapeHTML(laneLabels[lane.kind])}
          </div>
          <div class="tl-lane-track" style="width:${totalPx}px;">
            ${chipsHTML}
            ${empty}
          </div>
        </div>`;
    }

    return `
      <div class="cal-timeline">
        <div class="tl-scroll">
          <div class="tl-axis-row">
            <div class="tl-lane-label tl-axis-spacer"></div>
            <div class="tl-axis-track" style="width:${totalPx}px;">
              ${axisCells}
              ${todayOffset != null ? `<div class="tl-today-line" style="left:${todayOffset}px;"></div>` : ''}
            </div>
          </div>
          ${lanes.map(renderLane).join('')}
        </div>
      </div>`;
  }

  function renderCalendar(root) {
    const proj = curProject(); // merged project (id: '__all__') when all-projects is selected
    const view = document.createElement('div');
    view.className = 'view';
    if (!proj) {
      view.innerHTML = `
        <div class="page-head">
          <div>
            <div class="page-title">Calendar</div>
            <div class="page-sub">No project loaded.</div>
          </div>
        </div>
        <div class="empty">Pick a project (or 'All projects') from the topbar.</div>`;
      root.appendChild(view);
      return;
    }
    const isMerged = state.currentProjectId === '__all__';
    const { year, month, monthLabel, cells } = calendarMonthBounds();
    const gridStartISO = fmtISO(cells[0]);
    const gridEndISO   = fmtISO(cells[cells.length - 1]);
    const allItems = buildCalendarItems(proj, year, month, gridStartISO, gridEndISO);
    // Apply per-kind visibility filters (driven by the interactive legend
    // below). buildCalendarItems still returns everything so the toggle
    // state can flip without rebuilding the source data.
    const items = allItems
      .filter((it) => calState.visible[it.kind] !== false)
      .filter((it) => matchesSearch(it.label, it.sub))
      .filter((it) => calendarItemMatchesTopbar(it));
    const byDate = new Map();
    items.forEach((it) => {
      if (!byDate.has(it.date)) byDate.set(it.date, []);
      byDate.get(it.date).push(it);
    });
    // Per-kind counts for the legend (using the unfiltered set so users
    // see the underlying total, not the post-filter count).
    const kindCounts = { milestone: 0, deliverable: 0, action: 0, cr: 0, meeting: 0 };
    allItems.forEach((it) => {
      // Ranged milestones expand into one item per day in buildCalendarItems
      // — but for the legend counter we want one count per *record*, not
      // per day. Skip the middle/end positions so a 5-day range
      // milestone counts as 1.
      if (it.kind === 'milestone' && (it.rangePos === 'middle' || it.rangePos === 'end')) return;
      if (it.kind in kindCounts) kindCounts[it.kind]++;
    });

    const todayISO_ = todayISO();
    const dayHeaders = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
    // Build week-row HTML. Each row is one week-number cell + 7 day cells.
    // Cells stay addressable via the original linear cell index so chip
    // wiring (data-cell-idx) keeps working without changes elsewhere.
    function dayCellHTML(d, i) {
      const iso = fmtISO(d);
      // Month bands instead of an 'in-month' anchor: with continuous
      // scroll the anchor doesn't move, so highlighting one month and
      // greying everything else reads incorrectly. Alternate odd/even
      // months with a subtle background difference so users can scan
      // month boundaries while scrolling.
      const monthBand = d.getMonth() % 2 === 1 ? 'month-band' : '';
      const isToday = iso === todayISO_;
      const isWeekend = d.getDay() === 0 || d.getDay() === 6;
      const isFirstOfMonth = d.getDate() === 1;
      const cellItems = byDate.get(iso) || [];
      // Every chip is rendered into the DOM — the CSS caps the visible
      // area to ~4 rows so the cell layout stays compact, and on cell
      // hover the chips container reveals a slim scrollbar so users can
      // reach the hidden items without a click. A discreet corner badge
      // (▾ N) still opens the click-to-list popover for touch/keyboard.
      const HINT = 4;
      const hidden = Math.max(0, cellItems.length - HINT);
      const chipsHTML = cellItems.map((it, idx) => {
        const icon = it.icon || '·';
        const rangeCls = it.kind === 'milestone' && it.rangePos
          ? ` is-${it.rangePos}` : '';
        const tintStyle = it.tint ? ` style="--cal-chip-tint: ${it.tint};"` : '';
        const tintCls = it.tint ? ' has-tint' : '';
        return `
          <button class="cal-chip cal-chip-${it.tone} kind-${it.kind}${rangeCls}${tintCls}" data-cell-idx="${i}" data-item-idx="${idx}" title="${escapeHTML(it.label)} — ${escapeHTML(it.sub || '')}"${tintStyle}>
            <span class="cal-chip-icon" aria-hidden="true">${escapeHTML(icon)}</span>
            <span class="cal-chip-label">${escapeHTML(it.label)}</span>
          </button>`;
      }).join('');
      const overflowBadge = hidden > 0
        ? `<button class="cal-more" data-cell-idx="${i}" title="${hidden} more — hover the cell to scroll · click to list them all" aria-label="${hidden} more items">▾${hidden}</button>`
        : '';
      // First-of-month gets a small inline label so the user can find
      // month boundaries while scrolling.
      const dayLabel = isFirstOfMonth
        ? `<span class="cal-day-month-tag">${d.toLocaleDateString(undefined, { month: 'short' })}</span> ${d.getDate()}`
        : `${d.getDate()}`;
      const overflowCls = hidden > 0 ? ' has-overflow' : '';
      return `
        <div class="cal-cell ${monthBand} ${isToday ? 'today' : ''} ${isWeekend ? 'weekend' : ''}${overflowCls}" data-cell-idx="${i}" data-iso="${iso}">
          <div class="cal-day-num">${dayLabel}</div>
          ${overflowBadge}
          <div class="cal-chips">${chipsHTML}</div>
        </div>`;
    }
    const weekRowsHTML = [];
    for (let w = 0; w < calState.weekCount; w++) {
      const startIdx = w * 7;
      const weekStart = cells[startIdx];
      const wn = isoWeekNumber(weekStart);
      const cellsInWeek = [];
      for (let j = 0; j < 7; j++) cellsInWeek.push(dayCellHTML(cells[startIdx + j], startIdx + j));
      weekRowsHTML.push(`
        <div class="cal-week-num" title="ISO week ${wn} (starts ${fmtISO(weekStart)})">${wn}</div>
        ${cellsInWeek.join('')}`);
    }
    const cellHTML = weekRowsHTML.join('');

    view.innerHTML = `
      <div class="page-head">
        <div>
          <div class="page-title">${escapeHTML(proj.name)} — Calendar</div>
          <div class="page-sub" id="calPageSub">${escapeHTML(monthLabel)} · scroll vertically for more weeks · ← / → for months</div>
        </div>
        <div class="page-actions">
          <div class="seg" role="tablist" aria-label="Calendar format">
            <button type="button" class="seg-btn ${calState.format === 'month'    ? 'active' : ''}" data-cal-format="month"    title="Calendar view (vertical week grid)">Calendar</button>
            <button type="button" class="seg-btn ${calState.format === 'timeline' ? 'active' : ''}" data-cal-format="timeline" title="Horizontal timeline view">Timeline</button>
            <button type="button" class="seg-btn ${calState.format === 'table'    ? 'active' : ''}" data-cal-format="table"    title="Sortable table of dated items">Table</button>
          </div>
          ${isMerged ? '' : `
            <button class="ghost" id="calAddMile" title="Add a milestone">+ Milestone</button>
            <button class="ghost" id="calAddDel"  title="Add a deliverable">+ Deliverable</button>
            <button class="ghost" id="calAddMtg"  title="Add a meeting (one-off or recurring)">+ Meeting</button>
          `}
          <button class="icon-btn" id="calPrev" title="Previous month (←)">‹</button>
          <button class="ghost"   id="calToday" title="Jump to current month">Today</button>
          <button class="icon-btn" id="calNext" title="Next month (→)">›</button>
        </div>
      </div>
      <div class="cal-legend" role="group" aria-label="Show / hide on calendar">
        <button type="button" class="cal-legend-item ${calState.visible.milestone   ? 'on' : 'off'}" data-toggle-kind="milestone"   title="Show / hide milestones">
          <span class="cal-chip-icon kind-milestone">◇</span> Milestones <span class="cal-legend-count">${kindCounts.milestone}</span>
        </button>
        <button type="button" class="cal-legend-item ${calState.visible.deliverable ? 'on' : 'off'}" data-toggle-kind="deliverable" title="Show / hide deliverables">
          <span class="cal-chip-icon kind-deliverable">◆</span> Deliverables <span class="cal-legend-count">${kindCounts.deliverable}</span>
        </button>
        <button type="button" class="cal-legend-item ${calState.visible.action      ? 'on' : 'off'}" data-toggle-kind="action"      title="Show / hide actions">
          <span class="cal-chip-icon kind-action">☐</span> Actions <span class="cal-legend-count">${kindCounts.action}</span>
        </button>
        <button type="button" class="cal-legend-item ${calState.visible.meeting     ? 'on' : 'off'}" data-toggle-kind="meeting"     title="Show / hide meetings">
          <span class="cal-chip-icon kind-meeting">⊕</span> Meetings <span class="cal-legend-count">${kindCounts.meeting}</span>
        </button>
        <button type="button" class="cal-legend-item ${calState.visible.cr          ? 'on' : 'off'}" data-toggle-kind="cr"          title="Show / hide CR decisions">
          <span class="cal-chip-icon kind-cr">⇆</span> CRs <span class="cal-legend-count">${kindCounts.cr}</span>
        </button>
      </div>
      ${calState.format === 'month' ? `
        <div class="calendar">
          <div class="cal-head">
            <div class="cal-head-cell cal-head-wk" title="ISO week number">Wk</div>
            ${dayHeaders.map((h) => `<div class="cal-head-cell">${h}</div>`).join('')}
          </div>
          <div class="cal-grid" id="calGrid">${cellHTML}</div>
        </div>
      ` : calState.format === 'table'
          ? renderCalendarTable()
          : renderCalendarTimelineV2()}`;
    root.appendChild(view);

    $('#calPrev').addEventListener('click', () => { calState.monthOffset -= 1; render(); });
    $('#calNext').addEventListener('click', () => { calState.monthOffset += 1; render(); });
    $('#calToday').addEventListener('click', () => {
      // Always re-anchor to today's month, even if monthOffset is already
      // 0 (the user may have scrolled away and 'Today' must still snap
      // them back). Forcing firstWeekStart to null re-runs the
      // ensureCalendarWindow re-center.
      calState.monthOffset = 0;
      calState.firstWeekStart = null;
      calState.scrollToTodayPending = true;
      render();
    });
    $('#calAddMile')?.addEventListener('click', () => openQuickAdd('milestone'));
    $('#calAddDel')?.addEventListener('click', () => openQuickAdd('deliverable'));
    $('#calAddMtg')?.addEventListener('click', () => openQuickAdd('meeting'));

    // Format toggle (Month / Timeline)
    view.querySelectorAll('[data-cal-format]').forEach((btn) => {
      btn.addEventListener('click', () => {
        const f = btn.dataset.calFormat;
        if (calState.format === f) return;
        calState.format = f;
        render();
      });
    });

    // Timeline zoom — continuous via Ctrl/Cmd + scroll-wheel. Granularity
    // (day / week / month) auto-derives from pxPerDay during render.

    // Ctrl/Cmd + wheel anchors zoom at the cursor position so the date
    // under the pointer stays put. Plain wheel still scrolls the timeline
    // horizontally (browser default for an overflow-x container).
    if (calState.format === 'timeline') {
      const scroll = view.querySelector('.tl-scroll');
      if (scroll) {
        scroll.addEventListener('wheel', (e) => {
          if (!(e.ctrlKey || e.metaKey)) return;
          e.preventDefault();
          const factor = e.deltaY < 0 ? 1.12 : 1 / 1.12;
          const cur = clamp(calState.tlPxPerDay || 26, 0.5, 80);
          const next = clamp(cur * factor, 0.5, 80);
          if (Math.abs(next - cur) < 1e-3) return;
          // Pin the date under the cursor
          const rect = scroll.getBoundingClientRect();
          const xInScroll = e.clientX - rect.left + scroll.scrollLeft;
          const dayAtCursor = xInScroll / cur;
          calState.tlPxPerDay = next;
          render();
          const newScroll = $('.tl-scroll');
          if (newScroll) {
            const newX = dayAtCursor * next;
            newScroll.scrollLeft = Math.max(0, newX - (e.clientX - rect.left));
          }
        }, { passive: false });
      }
    }

    // Legend pills double as visibility filters
    view.querySelectorAll('[data-toggle-kind]').forEach((btn) => {
      btn.addEventListener('click', () => {
        const k = btn.dataset.toggleKind;
        calState.visible[k] = !calState.visible[k];
        render();
      });
    });

    // Timeline-mode chip / Table-mode row wiring — both populate
    // calState._tlRunMap, so a single resolver works for both. Each
    // map entry is { run, drag }; click invokes run.
    if (calState.format === 'timeline' || calState.format === 'table') {
      view.querySelectorAll('[data-tl-run-key]').forEach((el) => {
        el.addEventListener('click', (e) => {
          if (calState._tlSuppressNextClick) {
            calState._tlSuppressNextClick = false;
            e.stopPropagation();
            e.preventDefault();
            return;
          }
          e.stopPropagation();
          const entry = calState._tlRunMap?.get(el.dataset.tlRunKey);
          const run = entry && (entry.run || entry); // back-compat: also accept raw fns
          if (typeof run === 'function') run();
        });
      });
    }
    // Table-mode column sorting
    if (calState.format === 'table') {
      view.querySelectorAll('[data-tbl-sort]').forEach((th) => {
        th.addEventListener('click', () => {
          const col = th.dataset.tblSort;
          if (tableState.sortBy === col) {
            tableState.sortDir = tableState.sortDir === 'asc' ? 'desc' : 'asc';
          } else {
            tableState.sortBy = col;
            tableState.sortDir = 'asc';
          }
          render();
        });
      });
    }

    // Per-chip click: dispatch to the item's run() callback. We store
    // (cell, item) indices on the button so the data closure stays simple.
    view.querySelectorAll('.cal-chip').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const cellIdx = +btn.dataset.cellIdx;
        const itemIdx = +btn.dataset.itemIdx;
        const iso = fmtISO(cells[cellIdx]);
        const it = (byDate.get(iso) || [])[itemIdx];
        if (it && typeof it.run === 'function') it.run();
      });
    });
    // "+ N more" expands into a quick popover listing every chip for the day
    view.querySelectorAll('.cal-more').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const cellIdx = +btn.dataset.cellIdx;
        const iso = fmtISO(cells[cellIdx]);
        const list = byDate.get(iso) || [];
        const r = btn.getBoundingClientRect();
        showContextMenu(r.left, r.bottom + 4, list.map((it) => ({
          icon: it.icon || '·',
          label: it.label,
          onClick: it.run || (() => {}),
        })));
      });
    });

    // Wire arrow-key navigation once globally (re-renders share state)
    if (!calState.keyWired) {
      calState.keyWired = true;
      document.addEventListener('keydown', (e) => {
        if (state.currentView !== 'calendar') return;
        const inField = ['INPUT', 'TEXTAREA', 'SELECT'].includes(e.target.tagName) || e.target.isContentEditable;
        if (inField) return;
        if (e.key === 'ArrowLeft')  { e.preventDefault(); calState.monthOffset -= 1; render(); }
        if (e.key === 'ArrowRight') { e.preventDefault(); calState.monthOffset += 1; render(); }
      });
    }

    // Drag-to-pan time. Both formats accept click-and-drag on empty
    // body space (chips and buttons opt out). Month view lives-translates
    // the grid then snaps to whole months on release; Timeline view
    // drag-scrolls within the existing horizontally-scrollable container,
    // and any over-pull at the edges accumulates into a month-shift on
    // release so the user can pan continuously past the visible window.
    if (calState.format === 'month') wireMonthGridDrag(view);
    else if (calState.format === 'timeline') {
      wireTimelineDragPan(view);
      wireTimelineHoverMarker(view);
      wireTimelineEventDrag(view);
      // Today / format-toggle / first mount: park scrollLeft on today.
      // Timeline windows can stretch years to cover historical records,
      // so the freshly-rendered .tl-scroll starts at scrollLeft = 0 and
      // today may be far to the right — clicking "Today" used to look
      // like it did nothing because the visible viewport hadn't moved.
      if (calState.scrollToTodayPending) {
        const tlScroll = view.querySelector('.tl-scroll');
        if (tlScroll && calState._tlWindowStartMs != null) {
          const todayMs = parseDate(todayISO()).getTime();
          const pxPerDay = clamp(calState.tlPxPerDay || 26, 0.5, 80);
          const todayX = (todayMs - calState._tlWindowStartMs) / dayMs * pxPerDay;
          const max = Math.max(0, tlScroll.scrollWidth - tlScroll.clientWidth);
          // Place today ~30% from the left of the viewport — matches the
          // ~30% "history lead" the window-math already builds in.
          const target = clamp(todayX - tlScroll.clientWidth * 0.30, 0, max);
          tlScroll.scrollLeft = target;
          calState._tlLastScrollLeft = target;
        }
        calState.scrollToTodayPending = false;
      } else if (typeof calState._tlLastScrollLeft === 'number') {
        // Preserve horizontal scroll across re-renders that aren't an
        // explicit Today press (e.g. data change while zoomed in).
        const tlScroll = view.querySelector('.tl-scroll');
        if (tlScroll) tlScroll.scrollLeft = calState._tlLastScrollLeft;
      }
    }

    // Month view: vertical scroll-to-reveal-more-weeks. When the user
    // scrolls within ~200 px of the top or bottom edge, prepend / append
    // 4 more weeks and adjust scrollTop so the visual position holds
    // steady — no jump under the cursor. Also keep the page-sub label
    // tracking the dominant month in the viewport.
    if (calState.format === 'month') wireMonthGridScroll(view);
  }
  function wireMonthGridScroll(viewEl) {
    const grid = viewEl.querySelector('#calGrid');
    if (!grid) return;
    const sub = viewEl.querySelector('#calPageSub');
    function rowHeight() {
      const first = grid.querySelector('.cal-cell');
      return first ? first.getBoundingClientRect().height : 110;
    }
    function visibleCenterDate() {
      const rh = rowHeight();
      if (!rh) return null;
      const rowsFromTop = Math.floor((grid.scrollTop + grid.clientHeight / 2) / rh);
      const idx = clamp(rowsFromTop * 7, 0, calState.weekCount * 7 - 1);
      const d = new Date(calState.firstWeekStart);
      d.setDate(calState.firstWeekStart.getDate() + idx);
      return d;
    }
    function refreshSub() {
      const d = visibleCenterDate();
      if (!d || !sub) return;
      const lbl = d.toLocaleDateString(undefined, { year: 'numeric', month: 'long' });
      sub.textContent = `${lbl} · scroll vertically for more weeks · ← / → for months`;
    }
    function extendTop() {
      if (calState.calExtending) return;
      calState.calExtending = true;
      const ADD = 4;
      const beforeH = grid.scrollHeight;
      const newStart = new Date(calState.firstWeekStart);
      newStart.setDate(newStart.getDate() - ADD * 7);
      calState.firstWeekStart = newStart;
      calState.weekCount += ADD;
      const prevTop = grid.scrollTop;
      // Cache the scroll target so the next wireMonthGridScroll mount
      // (which runs as part of render()) can apply it without re-doing
      // a scroll-to-today and without firing a recursive extend.
      calState.suppressNextExtendUntilScrollTop = true;
      render();
      const newGrid = $('#calGrid');
      if (newGrid) {
        const afterH = newGrid.scrollHeight;
        newGrid.scrollTop = prevTop + (afterH - beforeH);
      }
      calState.calExtending = false;
    }
    function extendBottom() {
      if (calState.calExtending) return;
      calState.calExtending = true;
      const ADD = 4;
      calState.weekCount += ADD;
      const prevTop = grid.scrollTop;
      calState.suppressNextExtendUntilScrollTop = true;
      render();
      const newGrid = $('#calGrid');
      if (newGrid) newGrid.scrollTop = prevTop;
      calState.calExtending = false;
    }
    grid.addEventListener('scroll', () => {
      // Suppress one round of edge-detection when the scrollTop was set
      // programmatically (during extend / scroll-to-today). Without this
      // guard, the synthetic scroll event re-triggers extendTop and we
      // loop forever.
      if (calState.suppressNextExtendUntilScrollTop) {
        calState.suppressNextExtendUntilScrollTop = false;
        calState.lastScrollTop = grid.scrollTop;
        refreshSub();
        return;
      }
      calState.lastScrollTop = grid.scrollTop;
      const max = grid.scrollHeight - grid.clientHeight;
      if (grid.scrollTop < 200) extendTop();
      else if (grid.scrollTop > max - 200) extendBottom();
      refreshSub();
    });
    // Scroll-to-today only when the window was just (re-)anchored —
    // i.e. on first mount, after Today, after ‹ / ›, or after the format
    // toggle. Re-renders triggered by extendTop / extendBottom keep
    // their explicit scrollTop set by the extend caller.
    if (calState.scrollToTodayPending) {
      // Show the WHOLE anchor month: scroll so the row holding the
      // 1st-of-month is at the top of the viewport. Anchor =
      // today's month + monthOffset, so:
      //   Today  (monthOffset=0)   → today's month at top
      //   ‹      (monthOffset-=1)  → previous month at top
      //   ›      (monthOffset+=1)  → next month at top
      // Use getBoundingClientRect-relative math because the grid
      // doesn't have position:relative and offsetTop would resolve
      // against a far ancestor.
      const today = new Date();
      const anchor = new Date(today.getFullYear(), today.getMonth() + calState.monthOffset, 1);
      const ymPrefix = `${anchor.getFullYear()}-${String(anchor.getMonth() + 1).padStart(2, '0')}-01`;
      const targetCell = grid.querySelector(`.cal-cell[data-iso="${ymPrefix}"]`)
                       || grid.querySelector('.cal-cell.today');
      if (targetCell) {
        const gridTop = grid.getBoundingClientRect().top;
        const cellTop = targetCell.getBoundingClientRect().top;
        const delta = cellTop - gridTop;
        const max = Math.max(0, grid.scrollHeight - grid.clientHeight);
        calState.suppressNextExtendUntilScrollTop = true;
        grid.scrollTop = clamp(grid.scrollTop + delta, 0, max);
        calState.lastScrollTop = grid.scrollTop;
      }
      calState.scrollToTodayPending = false;
    } else if (typeof calState.lastScrollTop === 'number') {
      // Preserve scroll position across re-renders that don't re-anchor
      // the window. Without this, switching to the calendar view resets
      // scrollTop to 0 and the user loses their place.
      calState.suppressNextExtendUntilScrollTop = true;
      grid.scrollTop = calState.lastScrollTop;
    }
    refreshSub();
  }

  // — Drag-pan helpers —
  // Common rule: ignore drags that started on a chip / button / link /
  // form control so click-throughs still work.
  function _dragIgnoreTarget(t) {
    return !!t.closest('.cal-chip, .tl-chip, .cal-more, button, a, input, select, textarea, [contenteditable="true"]');
  }
  function wireMonthGridDrag(viewEl) {
    const grid = viewEl.querySelector('.cal-grid');
    if (!grid) return;
    let active = false;
    let startX = 0;
    let dx = 0;
    const PX_PER_MONTH = 200; // sensitivity — ~200 px of horizontal drag = 1 month
    grid.style.cursor = 'grab';
    grid.addEventListener('mousedown', (e) => {
      if (e.button !== 0) return;
      if (_dragIgnoreTarget(e.target)) return;
      active = true; startX = e.clientX; dx = 0;
      grid.style.cursor = 'grabbing';
      document.body.style.userSelect = 'none';
      e.preventDefault();
    });
    // Attach move/up at render time and leave them attached for the
    // lifetime of this view's render. They no-op when `active` is false,
    // which is the rest state. (Earlier we used { once: true } for mouseup
    // — that broke after the first drag because the listener was removed.)
    function onMove(e) {
      if (!active) return;
      dx = e.clientX - startX;
      grid.style.transform = `translateX(${dx}px)`;
    }
    function onUp() {
      if (!active) return;
      active = false;
      grid.style.cursor = 'grab';
      grid.style.transform = '';
      document.body.style.userSelect = '';
      const months = Math.round(-dx / PX_PER_MONTH);
      if (months !== 0) {
        calState.monthOffset += months;
        render();
      }
    }
    document.addEventListener('mousemove', onMove);
    document.addEventListener('mouseup', onUp);
  }
  function wireTimelineDragPan(viewEl) {
    const scroll = viewEl.querySelector('.tl-scroll');
    if (!scroll) return;
    // Mirror user-driven scroll into calState so subsequent re-renders
    // (zoom, data change) restore the position the user was looking at.
    // The Today button bypasses this by setting scrollToTodayPending,
    // which the render path consumes BEFORE reading _tlLastScrollLeft.
    scroll.addEventListener('scroll', () => {
      calState._tlLastScrollLeft = scroll.scrollLeft;
    }, { passive: true });
    let active = false;
    let startX = 0;
    let startScrollLeft = 0;
    let overflowPx = 0; // accumulated pull past the edges, used for month-shift on release
    const PX_PER_MONTH = 220;
    scroll.style.cursor = 'grab';
    scroll.addEventListener('mousedown', (e) => {
      if (e.button !== 0) return;
      if (_dragIgnoreTarget(e.target)) return;
      active = true;
      startX = e.clientX;
      startScrollLeft = scroll.scrollLeft;
      overflowPx = 0;
      scroll.style.cursor = 'grabbing';
      document.body.style.userSelect = 'none';
      e.preventDefault();
    });
    function onMove(e) {
      if (!active) return;
      const dx = e.clientX - startX;
      const desired = startScrollLeft - dx;
      const max = Math.max(0, scroll.scrollWidth - scroll.clientWidth);
      // Clamp scroll to [0, max] and remember any overflow so we can
      // convert it to a month-shift on release.
      if (desired < 0)        { scroll.scrollLeft = 0;   overflowPx = desired; }
      else if (desired > max) { scroll.scrollLeft = max; overflowPx = desired - max; }
      else                    { scroll.scrollLeft = desired; overflowPx = 0; }
    }
    function onUp() {
      if (!active) return;
      active = false;
      scroll.style.cursor = 'grab';
      document.body.style.userSelect = '';
      // If the user pulled past either edge, convert the overflow to
      // a month shift so panning feels continuous past the visible window.
      const months = Math.round(overflowPx / PX_PER_MONTH);
      if (months !== 0) {
        calState.monthOffset += months;
        render();
      }
    }
    document.addEventListener('mousemove', onMove);
    document.addEventListener('mouseup', onUp);
  }

  // — Hover indicator on the timeline. Shows a small vertical mark + a
  //   date label that follows the cursor. Suppressed during drag so the
  //   visuals don't fight each other.
  function wireTimelineHoverMarker(viewEl) {
    const scroll = viewEl.querySelector('.tl-scroll');
    const stage  = viewEl.querySelector('.tl-stage');
    if (!scroll || !stage) return;
    const marker = document.createElement('div');
    marker.className = 'tl-hover-marker';
    marker.innerHTML = '<div class="tl-hover-line"></div><div class="tl-hover-label"></div>';
    marker.style.display = 'none';
    stage.appendChild(marker);
    const label = marker.querySelector('.tl-hover-label');
    function updateFromEvent(e) {
      if (calState._tlDragging) { marker.style.display = 'none'; return; }
      const stageRect = stage.getBoundingClientRect();
      const xInStage = e.clientX - stageRect.left;
      if (xInStage < 0 || xInStage > stage.clientWidth) { marker.style.display = 'none'; return; }
      const pxPerDay = clamp(calState.tlPxPerDay || 26, 0.5, 80);
      const dayOffset = xInStage / pxPerDay;
      const ms = (calState._tlWindowStartMs || 0) + dayOffset * dayMs;
      const d = new Date(ms);
      const txt = d.toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric' });
      marker.style.left = xInStage + 'px';
      label.textContent = txt;
      marker.style.display = 'block';
    }
    scroll.addEventListener('mousemove', updateFromEvent);
    scroll.addEventListener('mouseleave', () => { marker.style.display = 'none'; });
  }

  // — Drag-to-move (and edge-resize for milestone ranges) on the timeline.
  //   Click without movement still opens the editor (the click event fires
  //   normally); a real drag updates the underlying record's date(s) and
  //   commits, suppressing the trailing click so the editor doesn't pop up.
  function wireTimelineEventDrag(viewEl) {
    const stage = viewEl.querySelector('.tl-stage');
    if (!stage) return;
    const RESIZE_HANDLE_PX = 8;
    function shiftIso(iso, days) {
      if (!iso) return iso;
      const d = parseDate(iso);
      d.setDate(d.getDate() + days);
      return fmtISO(d);
    }
    stage.addEventListener('mousedown', (e) => {
      if (e.button !== 0) return;
      const target = e.target.closest('.tl-marker, .tl-range-bar');
      if (!target) return;
      const key = target.dataset.tlRunKey;
      const entry = calState._tlRunMap?.get(key);
      if (!entry?.drag) return;
      const drag = entry.drag;

      // Determine drag mode for range bars: edges resize, middle moves.
      let mode = 'move';
      const isRange = target.classList.contains('tl-range-bar');
      if (isRange) {
        const r = target.getBoundingClientRect();
        if (e.clientX - r.left < RESIZE_HANDLE_PX) mode = 'resize-left';
        else if (r.right - e.clientX < RESIZE_HANDLE_PX) mode = 'resize-right';
      }

      e.stopPropagation();
      e.preventDefault();
      calState._tlDragging = true;
      const startX = e.clientX;
      const initialLeft  = parseFloat(target.style.left)  || 0;
      const initialWidth = isRange ? (parseFloat(target.style.width) || 0) : 0;
      const pxPerDay = clamp(calState.tlPxPerDay || 26, 0.5, 80);
      const prevCursor = document.body.style.cursor;
      document.body.style.cursor = (mode === 'move') ? 'grabbing' : 'ew-resize';
      target.style.zIndex = 10;
      let movedEnough = false;

      function onMove(em) {
        const dx = em.clientX - startX;
        if (Math.abs(dx) >= 4) movedEnough = true;
        if (mode === 'move') {
          target.style.left = (initialLeft + dx) + 'px';
        } else if (mode === 'resize-right') {
          target.style.width = Math.max(20, initialWidth + dx) + 'px';
        } else if (mode === 'resize-left') {
          const newWidth = initialWidth - dx;
          if (newWidth >= 20) {
            target.style.left  = (initialLeft + dx) + 'px';
            target.style.width = newWidth + 'px';
          }
        }
      }
      function onUp(em) {
        document.removeEventListener('mousemove', onMove);
        document.removeEventListener('mouseup', onUp);
        document.body.style.cursor = prevCursor;
        calState._tlDragging = false;
        if (!movedEnough) return; // pure click — let the click handler open the editor

        // Suppress the trailing click on this marker (otherwise the editor
        // would open after every drag).
        calState._tlSuppressNextClick = true;
        // Reset for next render
        setTimeout(() => { calState._tlSuppressNextClick = false; }, 200);

        const dx = em.clientX - startX;
        const dDays = Math.round(dx / pxPerDay);
        if (dDays === 0) return;

        const r = drag.record;
        const today = todayISO();
        if (mode === 'move') {
          if (drag.kind === 'action') {
            const oldDue = r.due;
            if (r.due) r.due = shiftIso(r.due, dDays);
            r.updatedAt = today;
            r.history = r.history || [];
            r.history.push({ at: today, what: `Due: ${oldDue || '—'} → ${r.due || '—'} (timeline drag)` });
          } else if (drag.kind === 'deliverable') {
            if (r.dueDate) r.dueDate = shiftIso(r.dueDate, dDays);
          } else if (drag.kind === 'milestone') {
            if (r.date) r.date = shiftIso(r.date, dDays);
            if (r.endDate) r.endDate = shiftIso(r.endDate, dDays);
          }
        } else if (mode === 'resize-right' && drag.kind === 'milestone') {
          if (r.endDate) {
            const ne = shiftIso(r.endDate, dDays);
            if (ne >= r.date) r.endDate = ne;
          }
        } else if (mode === 'resize-left' && drag.kind === 'milestone') {
          if (r.date) {
            const ns = shiftIso(r.date, dDays);
            if (!r.endDate || ns <= r.endDate) r.date = ns;
          }
        }
        // commit() rebuilds the calendar DOM, which makes the new
        // .tl-scroll start at scrollLeft=0. Capture the user's current
        // horizontal scroll and restore it on the new element so the
        // drag doesn't auto-pan the view.
        const liveScroll = stage.closest('.tl-scroll');
        const savedScrollLeft = liveScroll ? liveScroll.scrollLeft : 0;
        commit('timeline-drag');
        const newScroll = $('.tl-scroll');
        if (newScroll) newScroll.scrollLeft = savedScrollLeft;
      }
      document.addEventListener('mousemove', onMove);
      document.addEventListener('mouseup', onUp);
    });
  }

  /* ---------------------- Phase C: command palette --------------------- */
  // Universal Cmd+K palette. Indexes everything searchable + a small menu of
  // "slash commands" (action you can take). Up/Down + Enter to navigate +
  // open. Type `/` to filter to commands only.
  const paletteState = { items: [], idx: 0, query: '' };

  // Build a flat searchable index from current state. Cheap to rebuild on
  // each open since project data is in-memory.
  function buildPaletteIndex() {
    const out = [];
    // Slash-commands always available
    const sc = (label, hint, run) => out.push({ kind: 'cmd', label, hint, run, sortBoost: 5 });
    sc('+ New action',          'Open Quick Add',    () => openQuickAdd('action'));
    sc('+ New deliverable',     'Open Quick Add',    () => openQuickAdd('deliverable'));
    sc('+ New milestone',       'Open Quick Add',    () => openQuickAdd('milestone'));
    sc('+ New risk',            'Open Quick Add',    () => openQuickAdd('risk', { kind: 'risk' }));
    sc('+ New opportunity',     'Open Quick Add',    () => openQuickAdd('risk', { kind: 'opportunity' }));
    sc('+ New decision',        'Open Quick Add',    () => openQuickAdd('decision'));
    sc('+ New change request',  'Open editor',       () => openQuickAdd('change'));
    sc('+ New link',            'Open Quick Add',    () => openQuickAdd('link'));
    sc('+ New meeting',         'Open Quick Add',    () => openQuickAdd('meeting'));
    sc('+ New person',          'Open Quick Add',    () => openQuickAdd('person'));
    sc('+ New project',         'Open Quick Add',    () => openQuickAdd('project'));
    sc('Save current project as template', 'Project skeleton',
      () => { const p = curProject(); if (!p || curProjectIsMerged()) { toast('Pick a single project'); return; } saveProjectAsTemplate(p.id); });
    sc('Open Inbox',            'Reminders + alerts',() => { state.currentView = 'inbox'; render(); });
    sc('Open Calendar',         'Month view',        () => { state.currentView = 'calendar'; render(); });
    sc('Open Review (walk-through)', 'Live review wizard', () => { reviewModeState.mode = 'walkthrough'; state.currentView = 'review'; render(); });
    sc('Open Status report',         'Single-page snapshot', () => { reviewModeState.mode = 'full';        state.currentView = 'review'; render(); });
    sc('Toggle theme',          'Light / dark',      () => $('#btnTheme')?.click());
    sc('Toggle notes',          'Side panel',        () => $('#btnNotesToggle')?.click());
    sc('Run tour',              '5-step intro',      () => runTour(0));

    state.projects.forEach((proj) => {
      // Project itself
      out.push({ kind: 'project', label: proj.name, hint: 'Project', run: () => {
        state.currentProjectId = proj.id;
        state.currentView = 'board';
        saveState(); render();
      }});
      (proj.actions || []).forEach((a) => {
        if (a.archivedAt) return;
        const haystack = [a.title, personName(a.owner), a.notes || '', a.description || ''].join(' ');
        out.push({ kind: 'action', label: a.title, hint: `${proj.name} · ${personName(a.owner)} · ${a.due ? fmtDate(a.due) : 'no date'}`, hay: haystack, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'board';
          render();
          setTimeout(() => openDrawer(a.id), 30);
        }});
      });
      (proj.openPoints || []).forEach((op) => {
        const hay = [op.title, op.notes || '', (op.steps || []).map((s) => s.text).join(' ')].join(' ');
        out.push({ kind: 'open-point', label: op.title, hint: `${proj.name} · open point`, hay, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'openpoints';
          render();
        }});
      });
      (proj.changes || []).forEach((c) => {
        const hay = [c.title, c.rationale, c.analysis, c.description].join(' ');
        out.push({ kind: 'change', label: c.title, hint: `${proj.name} · ${c.status}`, hay, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'changes';
          render();
          setTimeout(() => openChangeRequestEditor(c.id), 30);
        }});
      });
      (proj.decisions || []).forEach((d) => {
        out.push({ kind: 'decision', label: d.title, hint: `${proj.name} · decision · ${d.date || ''}`, hay: d.title + ' ' + (d.rationale || ''), run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'decisions';
          render();
        }});
      });
      (proj.risks || []).forEach((r) => {
        out.push({ kind: 'risk', label: r.title, hint: `${proj.name} · ${r.kind || 'risk'}`, hay: r.title + ' ' + (r.mitigation || ''), run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'risks';
          render();
          setTimeout(() => openRiskEditor(r.id), 30);
        }});
      });
      (proj.deliverables || []).forEach((d) => {
        out.push({ kind: 'deliverable', label: d.name, hint: `${proj.name} · deliverable · ${d.dueDate ? fmtDate(d.dueDate) : 'no date'}`, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'calendar';
          render();
          setTimeout(() => openDeliverableEditor(d.id), 30);
        }});
      });
      (proj.milestones || []).forEach((m) => {
        out.push({ kind: 'milestone', label: m.name, hint: `${proj.name} · milestone · ${m.date ? fmtDate(m.date) : 'no date'}`, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'calendar';
          render();
          setTimeout(() => openMilestoneEditor(m.id), 30);
        }});
      });
      (proj.components || []).forEach((cmp) => {
        out.push({ kind: 'component', label: cmp.name, hint: `${proj.name} · component`, run: () => {
          state.currentProjectId = proj.id;
          state.currentView = 'components';
          render();
        }});
      });
      (proj.links || []).forEach((l) => {
        const hay = [l.title, l.description, l.url].join(' ');
        out.push({ kind: 'link', label: l.title || l.url, hint: `${proj.name} · link`, hay, run: () => {
          window.open(l.url, '_blank', 'noopener,noreferrer');
        }});
      });
      // Project notes — searchable as one big chunk
      const notesHTML = state.notes?.[proj.id] || '';
      if (notesHTML) {
        const text = notesHTML.replace(/<[^>]+>/g, ' ').replace(/&nbsp;/g, ' ').trim();
        if (text) out.push({ kind: 'notes', label: 'Meeting notes — ' + proj.name, hint: 'Project notes', hay: text, run: () => {
          state.currentProjectId = proj.id;
          if (!state.notesOpen) { state.notesOpen = true; saveState(); applyNotesPanel(); loadNotesForCurrentProject(); }
          render();
        }});
      }
    });
    state.people.forEach((p) => {
      out.push({ kind: 'person', label: p.name, hint: `${p.role || 'Person'} · ${p.capacity || 100}% FTE`, run: () => {
        state.currentView = 'people';
        render();
      }});
    });
    return out;
  }

  function paletteRank(items, query) {
    if (!query) {
      // Default — slash-commands first, then alphabetical
      return items.slice().sort((a, b) => {
        const sa = (a.kind === 'cmd' ? 1 : 0);
        const sb = (b.kind === 'cmd' ? 1 : 0);
        if (sa !== sb) return sb - sa;
        return a.label.localeCompare(b.label);
      }).slice(0, 60);
    }
    const isCmdOnly = query.startsWith('/');
    const q = (isCmdOnly ? query.slice(1) : query).trim();
    const scored = items
      .filter((it) => !isCmdOnly || it.kind === 'cmd')
      .map((it) => {
        const labelScore = fuzzyScore(q, it.label) * 3;
        const hayScore   = fuzzyScore(q, it.hay || '') * 1;
        const hintScore  = fuzzyScore(q, it.hint || '') * 0.5;
        const total = labelScore + hayScore + hintScore + (it.sortBoost || 0);
        return { it, total };
      })
      .filter((x) => x.total > 0)
      .sort((a, b) => b.total - a.total)
      .slice(0, 40)
      .map((x) => x.it);
    return scored;
  }

  function renderPalette() {
    const list = $('#paletteResults');
    if (!list) return;
    const { items, idx } = paletteState;
    if (!items.length) {
      list.innerHTML = '<div class="palette-empty">No matches. Try fewer characters or remove the leading <kbd>/</kbd>.</div>';
      return;
    }
    const kindIcon = {
      cmd: '⌘', action: '✓', 'open-point': '⚐', change: '⇆', decision: '⬡',
      risk: '△', deliverable: '◆', milestone: '◇', component: '▣',
      link: '↗', notes: '✎', project: '▦', person: '◔',
    };
    list.innerHTML = items.map((it, i) => `
      <button class="palette-item ${i === idx ? 'active' : ''}" data-idx="${i}" role="option">
        <span class="palette-kind">${kindIcon[it.kind] || '·'}</span>
        <span class="palette-label">${escapeHTML(it.label)}</span>
        <span class="palette-hint">${escapeHTML(it.hint || '')}</span>
      </button>`).join('');
    // Scroll active into view
    list.querySelector('.palette-item.active')?.scrollIntoView({ block: 'nearest' });
  }

  function openPalette() {
    const overlay = $('#paletteOverlay');
    if (!overlay) return;
    overlay.hidden = false;
    paletteState.items = paletteRank(buildPaletteIndex(), '');
    paletteState.idx = 0;
    paletteState.query = '';
    const inp = $('#paletteInput');
    inp.value = '';
    renderPalette();
    setTimeout(() => inp.focus(), 30);
  }
  function closePalette() {
    const overlay = $('#paletteOverlay');
    if (overlay) overlay.hidden = true;
    paletteState.items = [];
  }
  function paletteRunSelected() {
    const it = paletteState.items[paletteState.idx];
    if (!it) return;
    closePalette();
    setTimeout(() => { try { it.run(); } catch (e) { /* ignore */ } }, 0);
  }

  function wirePalette() {
    const overlay = $('#paletteOverlay');
    const inp = $('#paletteInput');
    const list = $('#paletteResults');
    if (!overlay || !inp) return;
    inp.addEventListener('input', () => {
      paletteState.query = inp.value;
      paletteState.items = paletteRank(buildPaletteIndex(), paletteState.query);
      paletteState.idx = 0;
      renderPalette();
    });
    inp.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowDown') { e.preventDefault(); paletteState.idx = (paletteState.idx + 1) % Math.max(1, paletteState.items.length); renderPalette(); }
      else if (e.key === 'ArrowUp')   { e.preventDefault(); paletteState.idx = (paletteState.idx - 1 + paletteState.items.length) % Math.max(1, paletteState.items.length); renderPalette(); }
      else if (e.key === 'Enter')     { e.preventDefault(); paletteRunSelected(); }
      else if (e.key === 'Escape')    { e.preventDefault(); closePalette(); }
    });
    list.addEventListener('mousedown', (e) => {
      const btn = e.target.closest('.palette-item[data-idx]');
      if (!btn) return;
      e.preventDefault();
      paletteState.idx = parseInt(btn.dataset.idx, 10);
      paletteRunSelected();
    });
    list.addEventListener('mouseover', (e) => {
      const btn = e.target.closest('.palette-item[data-idx]');
      if (!btn) return;
      const i = parseInt(btn.dataset.idx, 10);
      if (i !== paletteState.idx) { paletteState.idx = i; renderPalette(); }
    });
    overlay.addEventListener('click', (e) => { if (e.target === overlay) closePalette(); });
    $('#paletteClose')?.addEventListener('click', closePalette);
  }

  /* --------------------- Phase B: sidebar groups + help ---------------- */
  function wireSidebarGroups() {
    document.querySelectorAll('.nav-section-toggle').forEach((toggle) => {
      const group = toggle.dataset.group;
      const body = document.querySelector(`[data-group-body="${group}"]`);
      if (!body) return;
      // Apply persisted state
      const open = state.settings.sidebarGroups?.[group] !== false;
      body.hidden = !open;
      toggle.setAttribute('aria-expanded', String(open));
      toggle.classList.toggle('collapsed', !open);
      toggle.addEventListener('click', () => {
        const isOpen = toggle.getAttribute('aria-expanded') === 'true';
        const next = !isOpen;
        body.hidden = !next;
        toggle.setAttribute('aria-expanded', String(next));
        toggle.classList.toggle('collapsed', !next);
        state.settings.sidebarGroups = state.settings.sidebarGroups || {};
        state.settings.sidebarGroups[group] = next;
        saveState();
      });
    });
  }

  // Keyboard cheatsheet
  function wireHelpModal() {
    const overlay = $('#helpOverlay');
    const btn = $('#btnHelp');
    if (!overlay || !btn) return;
    const close = () => { overlay.hidden = true; };
    btn.addEventListener('click', () => openHelp());
    $('#helpClose')?.addEventListener('click', close);
    $('#helpDone')?.addEventListener('click', close);
    // ? to open from anywhere outside an input
    document.addEventListener('keydown', (e) => {
      if (e.key !== '?' && !(e.shiftKey && e.key === '/')) return;
      const inField = ['INPUT','TEXTAREA','SELECT'].includes(e.target.tagName) || e.target.isContentEditable;
      if (inField) return;
      e.preventDefault();
      overlay.hidden ? openHelp() : close();
    });
  }
  function openHelp() {
    const overlay = $('#helpOverlay');
    const body = $('#helpBody');
    if (!overlay || !body) return;
    const isMac = navigator.platform.toLowerCase().includes('mac');
    const mod = isMac ? '⌘' : 'Ctrl';
    const sections = [
      ['Global', [
        [`${mod}+K`,             'Open command palette'],
        [`${mod}+Z`,             'Undo'],
        [`${mod}+Shift+Z`,       'Redo'],
        [`${mod}+\\`,            'Toggle meeting notes'],
        [`/`,                    'Focus search'],
        [`?`,                    'Open this cheatsheet'],
        [`← / →`,                'Calendar: previous / next month'],
      ]],
      ['Editing', [
        ['Right-click any row',  'Edit / status / delete menu'],
        ['Double-click any row', 'Open editor'],
        ['Drag the ⋮⋮ grip',     'Reorder rows'],
        ['Backspace on empty',   'Delete the current step / todo / row'],
        ['×',                    'Always closes the modal — click-outside is intentionally disabled'],
      ]],
      ['Notes', [
        ['@ then type',          'Mention a person'],
        ['# then type',          'Insert an existing action'],
        [`${mod}+Shift+A`,       'Create + insert a new action'],
        [`${mod}+B / I / U`,     'Bold / italic / underline'],
      ]],
      ['Lists & boards', [
        ['Drag card / row',      'Move within or across columns / folders'],
        ['Drop on Done bin',     'Mark done from the board'],
        ['Drop on Archive bin',  'Soft-delete'],
      ]],
    ];
    const seen = state.settings?.walkthroughsSeen || {};
    const wtHTML = `
      <div class="help-section">
        <div class="help-section-title">Guided walkthroughs</div>
        <div class="wt-list">
          ${WALKTHROUGHS.map((t) => {
            const done = seen[t.id] === 'completed';
            return `
              <button type="button" class="wt-item${done ? ' is-done' : ''}" data-wt="${escapeHTML(t.id)}">
                <span class="wt-check" aria-hidden="true">${done ? '✓' : ''}</span>
                <span class="wt-body">
                  <span class="wt-label">${escapeHTML(t.label)}</span>
                  <span class="wt-tagline">${escapeHTML(t.tagline)}</span>
                </span>
                <span class="wt-meta">${t.steps.length} steps · ~${t.estMinutes} min</span>
              </button>
            `;
          }).join('')}
        </div>
      </div>`;
    const shortcutHTML = sections.map(([title, rows]) => `
      <div class="help-section">
        <div class="help-section-title">${escapeHTML(title)}</div>
        <div class="help-rows">
          ${rows.map(([k, v]) => `
            <div class="help-row"><kbd>${escapeHTML(k)}</kbd><span>${escapeHTML(v)}</span></div>
          `).join('')}
        </div>
      </div>`).join('');
    body.innerHTML = wtHTML + shortcutHTML;
    overlay.hidden = false;
    body.querySelectorAll('.wt-item').forEach((btn) => {
      btn.addEventListener('click', () => {
        const id = btn.dataset.wt;
        overlay.hidden = true;
        startWalkthrough(id);
      });
    });
  }

  /* ----------------------------- wire-up ----------------------------- */

  /* ----------------------- Guided walkthroughs --------------------- */
  // Topical mini-tours, launched from the ? help modal or auto-fired
  // on first run. Each topic is a small list of steps; each step can
  // optionally `before()` — switch to a panel, open the drawer, etc. —
  // before the spotlight is placed. Progress persists per topic in
  // state.settings.walkthroughsSeen[topicId].
  //
  // Selector hygiene: prefer stable IDs (#btnHelp) or unique data-view
  // attributes ([data-view="gantt"]). Fall back to a class that only
  // exists on the target panel to avoid mis-anchoring when several
  // panels are cached in the DOM.
  const _setView = (v) => new Promise((resolve) => {
    if (state.currentView === v) { resolve(); return; }
    state.currentView = v;
    saveState();
    render();
    // Two RAFs — first flushes the new view into the DOM, second lets
    // any per-panel effects (canvas resize, chart layout) settle.
    requestAnimationFrame(() => requestAnimationFrame(resolve));
  });
  const WALKTHROUGHS = [
    {
      id: 'getting-started',
      label: 'Getting started',
      tagline: 'The five things every user should know on day one.',
      estMinutes: 2,
      steps: [
        {
          target: '#sidebar .nav, .sidebar nav',
          side: 'right',
          title: 'Navigate the project',
          body: 'Switch views from here — Board, Register, Gantt, Calendar, Dashboard, plus engineering side-views like Risks and Change Requests.',
        },
        {
          target: '#btnQuickAdd',
          side: 'bottom',
          title: 'One palette for everything',
          body: 'Press <b>⌘K</b> (or click here) to open the universal palette. Type to search across actions, links, decisions, risks — or run quick commands like <i>+ action</i>, <i>report</i>, <i>today</i>.',
        },
        {
          before: () => _setView('board'),
          target: '.card[data-id]',
          side: 'right',
          title: 'Edit anywhere',
          body: 'Click any card to open its drawer. Drag between columns to change status. Hover to reveal a <b>⋯</b> menu with quick actions like Mark blocked / Add note / Archive.',
        },
        {
          target: '#btnInbox',
          side: 'bottom',
          title: 'Stay on top of what\'s slipping',
          body: 'The bell aggregates late actions, due-soon items, aging change requests and uncovered risks. Click an item to jump to it; dismissals stick.',
        },
        {
          target: '#btnHelp',
          side: 'bottom',
          title: 'Guided walkthroughs live here',
          body: 'Press <b>?</b> any time to open this menu — the shortcut cheatsheet is one section, guided walkthroughs are another. Come back when you\'re ready to explore a new area.',
        },
      ],
    },
    {
      id: 'board-register',
      label: 'Boards & Register — track work day-to-day',
      tagline: 'Two ways to look at the same actions, plus the drawer that ties them together.',
      estMinutes: 4,
      steps: [
        {
          before: () => _setView('board'),
          target: '.board',
          side: 'top',
          title: 'The Board is your daily heartbeat',
          body: 'One column per status — <i>Not started, In progress, Blocked, Done, Cancelled</i>. Cards you own float to the top; the newest work sits at the bottom of each column.',
        },
        {
          before: () => _setView('board'),
          target: '.card[data-id]',
          side: 'right',
          title: 'Drag to change status',
          body: 'Drop a card in another column to change its status. Two bins at the far right — <b>Done</b> and <b>Archive</b> — accept quick drops; Done marks complete, Archive soft-deletes (restorable from the Archive view).',
        },
        {
          before: () => _setView('board'),
          target: '.card[data-id]',
          side: 'right',
          title: 'Card → drawer',
          body: 'Click a card to open its drawer. Edit any field, override the auto-computed Start / Completed dates, log hours, add dependencies, snooze, or set recurrence. Every change is written to the action history.',
        },
        {
          before: () => _setView('register'),
          target: '.register',
          side: 'top',
          title: 'The Register is the same data, tabular',
          body: 'Everything the board shows, plus columns board cards hide (predicted vs actual completion, commitment, baseline variance). Sort any column; drag the ⋮⋮ grip to reorder within a status.',
        },
        {
          before: () => _setView('register'),
          target: '.reg-head',
          side: 'bottom',
          title: 'Bulk edits',
          body: 'Multi-select rows with the checkboxes, then a floating bar surfaces at the bottom with <b>Set status</b>, <b>Set owner</b>, <b>Set component</b>, <b>Set priority</b>. Great for triage after a review meeting.',
        },
        {
          target: '#filterOwner, #filterStatus, .topbar',
          side: 'bottom',
          title: 'Topbar filters apply everywhere',
          body: 'Owner / component / status / due — set once, then Board, Register, Gantt, and Calendar all honour the same filter. Save the current combo as a named view for one-click return.',
        },
      ],
    },
    {
      id: 'gantt-calendar',
      label: 'Gantt & Calendar — plan the schedule',
      tagline: 'Bars, dependencies, critical path, and three calendar formats.',
      estMinutes: 5,
      steps: [
        {
          before: () => _setView('timeline'),
          target: '.timeline',
          side: 'top',
          title: 'The Gantt lays every action along time',
          body: 'Bars span Start date → Due. If no Start is set, the bar starts two days before Due — set an explicit Start in the drawer to widen the plan window.',
        },
        {
          before: () => _setView('timeline'),
          target: '.tl-bar',
          side: 'top',
          title: 'Drag bars to reschedule',
          body: 'Drag a bar horizontally to move both Start and Due together. Drag either edge to resize just that end. Double-click empty timeline space to create a new action starting on that day.',
        },
        {
          before: () => _setView('timeline'),
          target: '#btnTLCritical',
          side: 'bottom',
          title: 'Critical-path toggle',
          body: 'The Gantt draws dependency lines automatically. Flip the <b>Critical path</b> toggle to dim everything that isn\'t on a chain feeding an open milestone — the fastest way to see where a slip actually hurts.',
        },
        {
          before: () => _setView('calendar'),
          target: '[data-cal-format]',
          side: 'bottom',
          title: 'Calendar comes in three formats',
          body: 'Month grid, Timeline strip, Table. Each format renders the same underlying items — actions, milestones, meetings — filtered by the same topbar controls. Pick whichever answers today\'s question fastest.',
        },
        {
          before: async () => { await _setView('calendar'); const btn = document.querySelector('[data-cal-format="month"]'); if (btn && !btn.classList.contains('active')) btn.click(); await new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r))); },
          target: '#calGrid',
          side: 'top',
          title: 'Discreet overflow, not truncation',
          body: 'When a day has more items than fit in a cell, a small <b>+N</b> chip appears at the bottom. Hover to smooth-scroll the cell — every chip stays reachable without a modal.',
        },
        {
          before: () => _setView('milestones'),
          target: '.cal-timeline, .cal-grid, .view',
          side: 'top',
          title: 'Milestones anchor the plan',
          body: 'Set a milestone\'s date, mark it critical, and any action that depends on it inherits the critical-path treatment. Miss-alerts fire when today crosses a milestone whose blockers aren\'t all Done.',
        },
        {
          before: () => _setView('changes'),
          target: '#crList, .cr-kpis',
          side: 'top',
          title: 'Change Requests are the schedule\'s audit trail',
          body: 'When a milestone or major action moves, log a CR: what changed, why, impact, decision. The Register\'s baseline column shows variance against the last snapshot.',
        },
      ],
    },
    {
      id: 'dashboard-evm',
      label: 'Dashboard & EVM — steer with KPIs',
      tagline: 'Every KPI tells you what it needs to compute — hover for the fine print.',
      estMinutes: 4,
      steps: [
        {
          before: () => _setView('dashboard'),
          target: '.dashboard',
          side: 'top',
          title: 'A single page for project health',
          body: 'Late, Blocked, Due-soon, Schedule Adherence, Team utilisation, Risk exposure, Stale actions, CPI, SPI, Change governance, and the trend charts. Everything scales to your topbar filters — so a Dashboard filtered to one owner is a valid personal review.',
        },
        {
          before: () => _setView('dashboard'),
          target: '.kpi[data-dash-filter]',
          side: 'bottom',
          title: 'Hover a KPI for the recipe',
          body: 'Every tile has a hover tooltip explaining its <b>formula</b>, the <b>inputs</b> it read, its <b>meaning</b>, and — critically — the <b>minimum data</b> a task or person must have for the number to be accurate.',
        },
        {
          before: () => _setView('dashboard'),
          target: '.dkpi-grid',
          side: 'top',
          title: 'CPI & SPI live in Project performance',
          body: 'Cost Performance = Earned Value / Actual Cost. Schedule Performance = Earned Value / Planned Value. Both need Commitment %, Start & Due, and either logged hours or a % complete override on each action. If it reads "—", the tooltip lists which field is missing.',
        },
        {
          before: () => _setView('dashboard'),
          target: '.kpi[data-tile]',
          side: 'top',
          title: 'Charts have their own tooltips',
          body: 'Adherence trend, throughput, burndown, and workload projection all explain their inputs on hover. If a chart looks empty, the tooltip tells you which field is missing across which subset of actions.',
        },
        {
          before: () => _setView('dashboard'),
          target: '.kpi[data-tile], .dkpi',
          side: 'top',
          title: 'Right-click Copy',
          body: 'Right-click any chart, board, or Gantt to <b>Copy as PNG</b> (full panel: title + legend + foot) or <b>Copy as data</b> (CSV to paste into Excel).',
        },
        {
          before: () => _setView('review'),
          target: '.review-card, .review-stepper, .view',
          side: 'top',
          title: 'The Reports view is the export target',
          body: 'Reports composes a whole project XLSX (Actions, Register, KPIs, Milestones, Deliverables, Dashboard sheet). KPIs come through as <b>live Excel formulas</b>, so you can slice further outside the app.',
        },
      ],
    },
    {
      id: 'notes-knowledge',
      label: 'Notes & knowledge capture',
      tagline: 'Meeting notes wired straight into actions, people, and decisions.',
      estMinutes: 3,
      steps: [
        {
          target: '#btnNotesToggle',
          side: 'bottom',
          title: 'Notes live in a slide-in panel',
          body: 'Open with <b>⌘\\</b> or the notes button. The notes panel travels with you between views — take a meeting note without losing your place in the Gantt.',
        },
        {
          before: async () => { const p = document.getElementById('notesPanel'); if (p && p.hidden) document.getElementById('btnNotesToggle')?.click(); await new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r))); },
          target: '#notesPanel',
          side: 'left',
          title: 'Structured, not free-form',
          body: 'Multi-level bullet lists with Tab / Shift-Tab. Compound numbering (1, 1.A, 1.A.2, 1.A.2.a) is automatic. The TOC on the left is resizable — grab its edge and pull.',
        },
        {
          before: async () => { const p = document.getElementById('notesPanel'); if (p && p.hidden) document.getElementById('btnNotesToggle')?.click(); await new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r))); },
          target: '#notesBody',
          side: 'left',
          title: '@ mentions and # actions',
          body: 'Type <b>@</b> then a name to mention a person. Type <b>#</b> then part of a title to insert an existing action as a live chip — status updates in your note the moment the action moves.',
        },
        {
          before: async () => { const p = document.getElementById('notesPanel'); if (p && p.hidden) document.getElementById('btnNotesToggle')?.click(); await new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r))); },
          target: '#notesBody',
          side: 'left',
          title: 'Create actions from the note',
          body: 'Press <b>⌘Shift+A</b> anywhere in a note to spawn a new action and drop its chip inline. Every action created this way carries the note as its origin — clickable back-link on the action drawer.',
        },
        {
          before: () => _setView('decisions'),
          target: '.dec-main, .view',
          side: 'top',
          title: 'Decisions get their own log',
          body: 'The Decisions view captures what was decided, why, and what alternatives were considered. Link decisions to actions or risks and they cross-reference automatically.',
        },
      ],
    },
    {
      id: 'data-safety',
      label: 'Data safety & backups',
      tagline: 'Where your data lives and how to keep it after a browser purge.',
      estMinutes: 2,
      steps: [
        {
          target: '#storageStatus',
          side: 'top',
          title: 'The Saved pill is your storage HUD',
          body: 'Bottom-right corner. Green means all writes committed to every layer (localStorage + IndexedDB + OPFS + your backup target). Click it to open the Storage panel.',
        },
        {
          target: '#storageStatus',
          side: 'top',
          title: 'Three layers already run for you',
          body: '<b>localStorage</b> is fast and primary. <b>IndexedDB</b> mirrors every write. <b>OPFS</b> keeps a hidden per-origin file copy that survives most cache clears. You get all three without configuring anything.',
        },
        {
          target: '#btnAutoBackup',
          side: 'bottom',
          title: 'Pick an off-browser target',
          body: 'From the Storage panel, pick a <b>folder</b> (Chrome / Edge — a real file you can back up yourself) or <b>auto-download</b> (works everywhere, drops timestamped snapshots into Downloads). Do this once and your work stops depending on any single browser store.',
        },
        {
          target: '#btnImport',
          side: 'bottom',
          title: 'Restore is a single JSON',
          body: 'Every backup is one JSON file — click the import arrow to restore. There\'s no proprietary format to worry about; you can inspect the file yourself.',
        },
      ],
    },
  ];
  // Active-run state — cleared on close. Kept as a module-level so
  // Back button can walk backwards through the current topic.
  let _wtRun = null;
  function maybeRunFirstRunTour() {
    if (state.settings?.walkthroughsSeen?.['getting-started'] === 'completed') return;
    if (state.settings?.tourSeen) return; // legacy flag, still honoured
    setTimeout(() => startWalkthrough('getting-started'), 600);
  }
  function startWalkthrough(topicId) {
    const topic = WALKTHROUGHS.find((t) => t.id === topicId);
    if (!topic) return;
    _wtRun = { topic, idx: 0 };
    runStep();
  }
  async function runStep() {
    closeTour();
    if (!_wtRun) return;
    const { topic, idx } = _wtRun;
    if (idx < 0 || idx >= topic.steps.length) { finishWalkthrough(); return; }
    const step = topic.steps[idx];
    if (typeof step.before === 'function') {
      try { await step.before(); } catch (_) { /* keep going even if the before-hook glitches */ }
    }
    // Some panels finish rendering a frame or two after render() returns
    // (calendar month grid, gantt lanes, dashboard charts). Give the
    // selector up to 800 ms to appear before we fall back to the
    // centered-card treatment.
    let target = step.target ? document.querySelector(step.target) : null;
    if (step.target && !target) {
      const deadline = performance.now() + 800;
      while (!target && performance.now() < deadline) {
        await new Promise((r) => setTimeout(r, 40));
        target = document.querySelector(step.target);
      }
    }
    const overlay = document.createElement('div');
    overlay.className = 'tour-overlay';
    const dots = topic.steps.map((_, i) =>
      `<span class="tour-dot${i === idx ? ' is-current' : ''}${i < idx ? ' is-done' : ''}"></span>`
    ).join('');
    const isLast = idx === topic.steps.length - 1;
    const isFirst = idx === 0;
    overlay.innerHTML = `
      <div class="tour-mask" id="tourMask"></div>
      <div class="tour-card" id="tourCard">
        <div class="tour-step">${escapeHTML(topic.label)} · step ${idx + 1} of ${topic.steps.length}</div>
        <div class="tour-title">${escapeHTML(step.title)}</div>
        <div class="tour-body">${step.body}</div>
        <div class="tour-dots">${dots}</div>
        <div class="tour-foot">
          <button class="ghost" id="tourSkip">Exit</button>
          <button class="ghost" id="tourBack" ${isFirst ? 'disabled' : ''}>← Back</button>
          <button class="primary" id="tourNext">${isLast ? 'Finish' : 'Next →'}</button>
        </div>
      </div>`;
    document.body.appendChild(overlay);
    const card = overlay.querySelector('#tourCard');
    const mask = overlay.querySelector('#tourMask');
    if (target) {
      target.classList.add('tour-highlight');
      // Ensure the anchor is visible before we measure — nothing shipped
      // gets less useful than a spotlight aimed off-screen.
      try { target.scrollIntoView({ block: 'center', inline: 'center', behavior: 'auto' }); } catch (_) {}
      const r = target.getBoundingClientRect();
      mask.style.setProperty('--spot-x', (r.left + r.width / 2) + 'px');
      mask.style.setProperty('--spot-y', (r.top + r.height / 2) + 'px');
      mask.style.setProperty('--spot-r', (Math.max(r.width, r.height) / 2 + 12) + 'px');
      const W = card.getBoundingClientRect().width || 340;
      const H = card.getBoundingClientRect().height || 200;
      const margin = 16;
      let x = r.right + margin, y = r.top;
      if (step.side === 'bottom') { x = Math.max(margin, r.left); y = r.bottom + margin; }
      if (step.side === 'top')    { x = Math.max(margin, r.left); y = Math.max(margin, r.top - H - margin); }
      if (step.side === 'left')   { x = Math.max(margin, r.left - W - margin); y = r.top; }
      if (step.side === 'right')  { x = r.right + margin; y = Math.max(margin, r.top); }
      x = clamp(x, margin, innerWidth - W - margin);
      y = clamp(y, margin, innerHeight - H - margin);
      card.style.left = x + 'px';
      card.style.top = y + 'px';
    } else {
      mask.classList.add('full');
      card.style.left = '50%';
      card.style.top = '50%';
      card.style.transform = 'translate(-50%, -50%)';
    }
    overlay.querySelector('#tourSkip').addEventListener('click', () => { closeTour(); exitWalkthrough(); });
    overlay.querySelector('#tourBack').addEventListener('click', () => {
      if (!_wtRun || _wtRun.idx === 0) return;
      _wtRun.idx -= 1;
      runStep();
    });
    overlay.querySelector('#tourNext').addEventListener('click', () => {
      if (!_wtRun) return;
      _wtRun.idx += 1;
      runStep();
    });
  }
  function closeTour() {
    document.querySelectorAll('.tour-highlight').forEach((el) => el.classList.remove('tour-highlight'));
    document.querySelectorAll('.tour-overlay').forEach((el) => el.remove());
  }
  function exitWalkthrough() {
    _wtRun = null;
  }
  function finishWalkthrough() {
    if (!_wtRun) return;
    const topicId = _wtRun.topic.id;
    _wtRun = null;
    state.settings = state.settings || {};
    state.settings.walkthroughsSeen = state.settings.walkthroughsSeen || {};
    state.settings.walkthroughsSeen[topicId] = 'completed';
    // Keep the legacy tourSeen flag in sync so the first-run guard
    // never re-fires the Getting Started tour on old builds.
    if (topicId === 'getting-started') state.settings.tourSeen = true;
    saveState();
    toast('Walkthrough finished — press ? to explore another');
  }
  // Legacy alias — a few call-sites still reference runTour.
  function runTour(idx) {
    startWalkthrough('getting-started');
    if (_wtRun && typeof idx === 'number') { _wtRun.idx = clamp(idx, 0, _wtRun.topic.steps.length - 1); runStep(); }
  }

  // First-run data-safety prompt — shown once, before the feature tour,
  // so the user has consciously chosen a backup posture before investing
  // hours of work. Marks settings.safetySeen on any of its outcomes so
  // it never reappears.
  function maybeRunFirstRunSafety() {
    if (state.settings?.safetySeen) return;
    setTimeout(() => runFirstRunSafety(), 200);
  }
  function runFirstRunSafety() {
    if ($('#firstRunSafety')) return; // already up
    const overlay = document.createElement('div');
    overlay.className = 'overlay desc-overlay';
    overlay.id = 'firstRunSafety';
    overlay.style.zIndex = '9000';
    const httpsHint = !isSecureContextHttp
      ? `<div class="ab-warn" style="margin-top: 6px;">
          You're opening this page via <code>${escapeHTML(location.protocol)}</code>.
          The "Pick a folder" option below needs Chromium + http(s).
          See <strong>Auto-download</strong> below as a fallback.
        </div>`
      : '';
    overlay.innerHTML = `
      <div class="desc-modal storage-modal" style="width: 560px;">
        <div class="desc-head">
          <div class="desc-title">Welcome — let's keep your work safe</div>
        </div>
        <div style="padding: 14px 16px; display: flex; flex-direction: column; gap: 12px;">
          <div>
            Cockpit stores your data in this browser. That's fast and private — but
            <strong>browsers occasionally clear site data</strong> (manual cookie
            clearing, low-disk eviction, idle timeouts on Safari). A few hours of
            careful work shouldn't depend on a single browser store.
          </div>
          <div class="ab-muted" style="font-size: 12px;">
            A hidden in-browser safety net (OPFS) is already running for you.
            Picking <em>one</em> of the off-browser options below makes a real
            file copy you can also see and back up yourself.
          </div>
          ${httpsHint}
          <div style="display: flex; flex-direction: column; gap: 6px;">
            <button class="primary" id="frsPickFolder"${pickerAvailable ? '' : ' disabled'}>📁 Pick a folder for backups (Chrome / Edge)${pickerAvailable ? '' : ' — needs http(s)'}</button>
            <button class="ghost"   id="frsDownload">↓ Auto-download to my Downloads folder (works everywhere)</button>
            <button class="ghost"   id="frsLater">I'll set this up later</button>
          </div>
          <div class="ab-muted" style="font-size: 11px;">
            You can change this anytime — click the bottom-right <em>Saved</em>
            pill to open the Storage panel.
          </div>
        </div>
      </div>`;
    document.body.appendChild(overlay);

    const seen = () => {
      state.settings.safetySeen = true;
      saveState();
      overlay.remove();
    };
    overlay.querySelector('#frsPickFolder')?.addEventListener('click', () => {
      // Mark seen FIRST so canceling the picker doesn't re-trigger this.
      state.settings.safetySeen = true;
      // Force folder mode so the post-pick continuation persists it.
      state.settings.autoBackup.mode = 'folder';
      state.settings.autoBackup.enabled = true;
      saveState();
      requestBackupDir().then((handle) => {
        if (!handle) {
          // User dismissed — leave their choice as folder mode but
          // disabled until they actually pick.
          state.settings.autoBackup.enabled = false;
          saveState();
        } else {
          scheduleAutoBackup();
          toast('Folder backup ready · ' + (handle.name || ''), 2400);
        }
        overlay.remove();
      });
    });
    overlay.querySelector('#frsDownload')?.addEventListener('click', () => {
      state.settings.autoBackup.mode = 'download';
      state.settings.autoBackup.enabled = true;
      // 1-hour default — frequent enough to matter, infrequent enough
      // not to spam the Downloads folder.
      if (!state.settings.autoBackup.intervalMinutes) {
        state.settings.autoBackup.intervalMinutes = 60;
      }
      saveState();
      scheduleAutoBackup();
      seen();
      toast('Auto-download backups enabled', 2400);
    });
    overlay.querySelector('#frsLater')?.addEventListener('click', seen);
  }
  function init() {
    // Load: must distinguish between empty storage (first run — fine to seed)
    // and corrupted storage (must NOT overwrite, surface a recovery overlay).
    const result = loadState();
    if (result.kind === 'empty') {
      // Empty localStorage — could be a brand-new install, OR the user's
      // browser cleared site data while OPFS / IndexedDB still has their
      // work. Seed sample data in memory so the UI has something to
      // render, mark the boot as empty-state, but DO NOT saveState yet:
      // the seed would overwrite the recovered state if the user picks
      // Restore in the OPFS-recovery overlay below. The decision branch
      // in maybeOfferOpfsRecovery() handles the saveState() call.
      _emptyStateBoot = true;
      _opfsLocked = true;        // freeze OPFS writes until recovery decision
      state = seedState();
      try { normalizeState(state); }
      catch (e) { state = { people: [], projects: [], settings: {}, currentView: 'board', currentProjectId: null }; normalizeState(state); }
      // Note: no saveState() here — committed by maybeOfferOpfsRecovery
      // (or its no-data fallback below). We also check the IndexedDB
      // mirror for state — see init's tail below.
    } else if (result.kind === 'corrupted') {
      enterRecoveryMode('parse', result.error, result.raw);
    } else {
      state = result.value;
      try {
        normalizeState(state);
        saveState();
      } catch (e) {
        enterRecoveryMode('normalize', String(e?.message || e), result.raw);
      }
    }

    // Self-check: confirm exporting and re-importing the current state
    // preserves every record. If a future field is added but missed in
    // normalizeState, the summary diff makes it visible early. Console
    // only — never disrupts the user.
    if (!_recoveryMode) {
      try {
        const before = summarizeState(state);
        const reimported = normalizeState(JSON.parse(JSON.stringify(buildExportPayload())));
        const after = summarizeState(reimported);
        const drift = Object.keys(before).filter((k) => before[k] !== after[k]);
        if (drift.length) {
          console.warn('[cockpit] export round-trip drift detected:',
            Object.fromEntries(drift.map((k) => [k, { before: before[k], after: after[k] }])));
        }
      } catch (e) {
        console.warn('[cockpit] export round-trip self-check failed:', e);
      }
    }
    applyTheme(state.settings.theme || 'dark');

    $('#btnSidebarToggle').addEventListener('click', () => {
      $('#app').classList.toggle('sidebar-collapsed');
    });
    $$('.nav-item').forEach((b) => {
      b.addEventListener('click', () => {
        state.currentView = b.dataset.view;
        saveState(); render();
      });
    });
    $('#projectSelect').addEventListener('change', (e) => {
      const v = e.target.value;
      // Special pseudo-values toggle archived-project visibility without
      // changing the active project.
      if (v === '__show_archived__') {
        state.settings.showArchivedProjects = true;
        saveState(); renderTopbar(); return;
      }
      if (v === '__hide_archived__') {
        state.settings.showArchivedProjects = false;
        saveState(); renderTopbar(); return;
      }
      state.currentProjectId = v;
      saveState(); render();
      if (state.notesOpen) loadNotesForCurrentProject();
    });
    $('#btnNewProject').addEventListener('click', () => openQuickAdd('project'));

    ['#search', '#filterOwner', '#filterComponent', '#filterStatus', '#filterDue'].forEach((sel) => {
      $(sel).addEventListener('input', () => { render(); renderSavedViewsRow(); });
      $(sel).addEventListener('change', () => { render(); renderSavedViewsRow(); });
    });

    $('#btnUndo').addEventListener('click', undo);
    $('#btnRedo').addEventListener('click', redo);
    $('#btnQuickAdd').addEventListener('click', () => openPalette());

    $('#btnNotesToggle').addEventListener('click', () => {
      state.notesOpen = !state.notesOpen;
      saveState();
      applyNotesPanel();
    });
    $('#btnInbox').addEventListener('click', () => {
      state.currentView = 'inbox';
      saveState(); render();
    });
    wireNotesPanel();
    applyNotesPanel();
    wireHoverDescOnce();
    wireEvmTooltipsOnce();
    wireTodoWidget();
    wireAutoBackup();
    initAutoBackup();
    wireStorageStatus();
    wireSidebarGroups();
    wireHelpModal();
    wirePalette();

    $('#btnExport').addEventListener('click', exportJSON);
    $('#btnExportXLSX')?.addEventListener('click', exportProjectXLSX);
    // Single Import button now hosts both replace + merge — picker chooses.
    $('#btnImport').addEventListener('click', openImportPicker);
    $('#btnEmpty').addEventListener('click', () => {
      if (!confirm('Wipe everything and start from zero?\n\nThis removes every project, person, action, deliverable, milestone, and note. You can Export first if you want to keep what\'s here.\n\nThis cannot be undone (except via Undo).')) return;
      const typed = prompt('Type EMPTY (in caps) to confirm wiping all data:');
      if ((typed || '').trim() !== 'EMPTY') { toast('Cancelled — nothing was changed'); return; }
      undoStack.push(JSON.stringify(state));
      state = emptyState();
      saveState();
      render();
      toast('Started fresh');
    });

    $('#btnTheme').addEventListener('click', () => {
      const next = (state.settings.theme === 'light') ? 'dark' : 'light';
      state.settings.theme = next;
      applyTheme(next);
      saveState();
    });

    // Quick add controls
    $$('.qa-tab').forEach((t) => t.addEventListener('click', () => openQuickAdd(t.dataset.qa)));
    $('#qaCancel').addEventListener('click', closeQuickAdd);
    // Quick Add closes only via Cancel / Save / × — backdrop clicks ignored.
    $('#qaSave').addEventListener('click', saveQA);
    // Backdrop click does NOT close Quick Add — user must use Cancel / Save / ×.

    // Drawer
    $('#drawerClose').addEventListener('click', closeDrawer);
    // Backdrop click does NOT close the action drawer — user must use × so
    // mid-edit fields aren't dismissed by an accidental click outside.

    // Inline edits in review
    document.addEventListener('change', (e) => {
      const t = e.target;
      if (!t.classList?.contains('inline')) return;
      const id = t.dataset.id;
      const action = t.dataset.action;
      const proj = curProject();
      const a = proj.actions.find((x) => x.id === id);
      if (!a) return;
      if (action === 'status') {
        a.history.push({ at: todayISO(), what: `Status: ${a.status} → ${t.value}` });
        a.status = t.value;
      } else if (action === 'owner') {
        a.history.push({ at: todayISO(), what: `Owner: ${personName(a.owner)} → ${personName(t.value)}` });
        a.owner = t.value;
      } else if (action === 'due') {
        a.history.push({ at: todayISO(), what: `Due: ${a.due || '—'} → ${t.value || '—'}` });
        a.due = t.value || null;
      }
      a.updatedAt = todayISO();
      commit('inline');
    });

    // Click into action title from review
    document.addEventListener('click', (e) => {
      const span = e.target.closest('.row .clickable');
      if (span && span.dataset.id) openDrawer(span.dataset.id);
    });

    // Close popovers on outside click (registered once)
    document.addEventListener('click', (e) => {
      const pop = $('#holidayPopover');
      if (!pop || pop.hidden) return;
      if (pop.contains(e.target)) return;
      if (e.target.closest('#btnTLHolidays')) return;
      pop.hidden = true;
    });

    // Keyboard shortcuts
    document.addEventListener('keydown', (e) => {
      // `inField` should also be true for contenteditable regions (notes
      // panel, open-point context, CR rich fields, action description editor)
      // — otherwise typing characters like `/` is swallowed by the global
      // search-focus shortcut, and Cmd+Z would undo app state instead of text.
      const inField = ['INPUT', 'TEXTAREA', 'SELECT'].includes(e.target.tagName) || e.target.isContentEditable;
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') {
        e.preventDefault(); openPalette();
      } else if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'z' && !e.shiftKey) {
        if (!inField) { e.preventDefault(); undo(); }
      } else if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'z' && e.shiftKey) {
        if (!inField) { e.preventDefault(); redo(); }
      // Escape no longer closes Quick Add / drawer / editor modals — losing
      // an in-progress edit to a stray Escape was too easy. Use × or Cancel.
      } else if (e.key === '/' && !inField) {
        e.preventDefault(); $('#search').focus();
      } else if ((e.metaKey || e.ctrlKey) && e.key === '\\') {
        e.preventDefault();
        state.notesOpen = !state.notesOpen;
        saveState();
        applyNotesPanel();
      }
    });

    // Persistence safety net — flush on tab close / hide / refresh, and run a
    // low-cost periodic save in case a crash skips the unload handlers.
    window.addEventListener('beforeunload', flushPendingSaves);
    window.addEventListener('pagehide',     flushPendingSaves);
    document.addEventListener('visibilitychange', () => {
      if (document.visibilityState === 'hidden') flushPendingSaves();
    });
    setInterval(flushPendingSaves, 10000);

    // Phase D — refresh the bell once per minute so day-rollover late-actions
    // surface without needing a manual reload, and try the daily notification
    // once per app load.
    setInterval(refreshBell, 60000);
    setTimeout(maybeNotifyInbox, 1500);

    render();
    // Empty-state boot: check IDB first (fastest, simplest restore),
    // then OPFS, then fall through to first-run prompts. Each path
    // ends in either a saveState() of the chosen state OR an overlay
    // that owns the saveState() call once the user decides.
    if (_emptyStateBoot) {
      stateIdbRead().then((idbState) => {
        if (idbState && idbState.projects && idbState.people) {
          // localStorage was wiped but IDB has the user's data — restore
          // silently. (No overlay; this is the same data they had.)
          try {
            state = normalizeState(idbState);
            _opfsLocked = false;
            saveState();
            render();
            toast('Restored from IndexedDB mirror', 2400);
            return;
          } catch (e) {
            console.warn('[cockpit] IDB state failed schema check, falling back:', e);
          }
        }
        // No IDB recovery — try OPFS.
        return maybeOfferOpfsRecovery().then(() => {
          if (!$('#opfsRecoveryOverlay')) {
            _opfsLocked = false;
            saveState();
            maybeRunFirstRunSafety();
            maybeRunFirstRunTour();
          }
        });
      }).catch(() => {
        _opfsLocked = false;
        saveState();
        maybeRunFirstRunSafety();
        maybeRunFirstRunTour();
      });
    } else {
      // Normal boot — safety prompt first (no-op if already seen), tour
      // second.
      maybeRunFirstRunSafety();
      maybeRunFirstRunTour();
    }
  }

  document.addEventListener('DOMContentLoaded', init);
})();
